import React, { useState, useEffect, useRef, useCallback, useLayoutEffect } from "react";
import { flushSync } from "react-dom";
import gsap from "gsap";

// ═══════════════════════════════════════════════════════════════
// WORLD & LEVEL DEFINITIONS
// ═══════════════════════════════════════════════════════════════
const WORLDS = [
  { id:1,  name:"Dragon's Lair",    emoji:"🐉", color:"#ff6b35", bg:"#1a0500", grid:"rgba(255,107,53,0.08)",    accent:"#c13b00", atmos:"cave"   },
  { id:2,  name:"Troll Forest",     emoji:"🌿", color:"#4ecb71", bg:"#021408", grid:"rgba(78,203,113,0.08)",    accent:"#1e7a3a", atmos:"forest" },
  { id:3,  name:"Elven Kingdom",    emoji:"✨", color:"#e8c84e", bg:"#080a02", grid:"rgba(232,200,78,0.07)",    accent:"#9a8020", atmos:"magic"  },
  { id:4,  name:"Magic Tower",      emoji:"🔮", color:"#b06de8", bg:"#0a0218", grid:"rgba(176,109,232,0.08)",   accent:"#7a3ab0", atmos:"arcane" },
  { id:5,  name:"Viking Fjords",    emoji:"❄️", color:"#6ec0f5", bg:"#020c1c", grid:"rgba(110,192,245,0.07)",   accent:"#1e66a0", atmos:"frost"  },
  { id:6,  name:"Goblin Mines",     emoji:"⛏️", color:"#f5d240", bg:"#100900", grid:"rgba(245,210,64,0.07)",    accent:"#c09010", atmos:"mine"   },
  { id:7,  name:"Haunted Castle",   emoji:"👻", color:"#9be09b", bg:"#020a04", grid:"rgba(155,224,155,0.07)",   accent:"#3a8a40", atmos:"undead" },
  { id:8,  name:"Ocean Deep",       emoji:"🌊", color:"#22d4b4", bg:"#010c0a", grid:"rgba(34,212,180,0.07)",    accent:"#0e9078", atmos:"ocean"  },
  { id:9,  name:"Giant's Peak",     emoji:"⛰️", color:"#c8d0da", bg:"#060808", grid:"rgba(200,208,218,0.07)",   accent:"#6a7880", atmos:"storm"  },
  { id:10, name:"Rainbow Dragon",   emoji:"🌈", color:"#ffd700", bg:"#120800", grid:"rgba(255,215,0,0.08)",     accent:"#c8a000", atmos:"divine" },
];

// ═══════════════════════════════════════════════════════════════
// MASCOTS — 10 animal companions, each with unique dance
// ═══════════════════════════════════════════════════════════════
const MASCOTS = [
  // ── STARTER ──────────────────────────────────────────────────
  { id:"dragon",    name:"Pip",     color:"#ff8c5a", rarity:"starter",
    e:{idle:"🐲",happy:"🐲",excited:"✨",fire:"🌟",fever:"💛",sad:"🥹",victory:"🥳",scared:"🫣"},
    dance:"danceDragon", catchphrase:"Small dragon, BIG heart! 🐲💕",
    speeches:{
      idle:["Pip is ready! 🐲","You've got this, I believe in you! 💕","Let's do this together! 🌟"],
      happy:["Yay yay yay!! 🐲✨","Pip is SO happy right now! 💛","You're incredible! 🥳"],
      streak:["Streak streak!! 🌟🌟","Pip can't believe how good you are!! 🐲","We're AMAZING together!! ✨"],
      fever:["FEVER TIME!! Pip is glowing!! 💛💛","Best adventure EVER!! 🐲✨"],
      close:["Almost! Don't give up! 🐲","You can do it, I know it! 💕"],
      miss:["It's okay, try again! 🐲💕","Pip still believes in you! 🌟"],
      victory:["WE DID IT!! 🥳","Pip is SO proud of you!! 🐲💕"],
      bonus:["Oh wow FREE bonus!! 🐲✨","Pip is bouncing with joy!! 💛"],
      mystery:["Ooh what could it be?! 🐲🎁","A surprise! Pip loves surprises!! ✨"],
    }},
  { id:"fox",       name:"Rusty",   color:"#e07030", rarity:"starter",
    e:{idle:"🦊",happy:"🦊",excited:"🌟",fire:"✨",fever:"🌸",sad:"🥺",victory:"🎉",scared:"🫣"},
    dance:"danceFox",    catchphrase:"Foxy and clever — that's us! 🦊",
    speeches:{
      idle:["Heyyy! Ready to play? 🦊","You're so good at this! 🌟","Let's go on an adventure! 🦊"],
      happy:["Foxy loves this!! 🦊","Oh yes oh yes!! ✨","You clever, wonderful person! 🌟"],
      streak:["Sneaky streak!! 🦊🌟","Rusty can't keep up with you!! 💨","So fast and smart!! 🌸"],
      fever:["Fox fever!! Woohoo!! 🌸🌸","You're unstoppable!! 🦊"],
      close:["Sooo close! You've got it! 🦊","Almost there! Rusty believes! 🌟"],
      miss:["Aw, you'll get it next time! 🦊","Rusty has faith in you! 💕"],
      victory:["Fox and friend WIN!! 🦊🎉","Best team ever!! 🌟"],
      bonus:["Oh oh oh, bonus time!! 🦊","Free round! Rusty is dancing!! 🌸"],
      mystery:["Ooh a mystery! How exciting!! 🦊🎁","What could it be?! 👀"],
    }},
  // ── COMMON ───────────────────────────────────────────────────
  { id:"frog",      name:"Hoppy",   color:"#4ecb71", rarity:"common",
    unlockCond:{type:"level",value:3}, unlockHint:"Complete Level 3",
    e:{idle:"🐸",happy:"🐸",excited:"💚",fire:"🌿",fever:"🌼",sad:"🥺",victory:"🎊",scared:"🫣"},
    dance:"danceFrog",   catchphrase:"Every hop counts! 🐸💚",
    speeches:{
      idle:["Ribbit! Let's go! 🐸","Hoppy is SO excited! 💚","Boing boing! Ready! 🐸"],
      happy:["Hop hop hooray!! 🐸","Ribbit of joy!! 💚","Best day ever! 🌿"],
      streak:["Hopping streak!! 🐸💚","Frog power activated!! 🌼","Can't stop won't stop! 🐸"],
      fever:["Frog fever!! Hop hop hop!! 💚💚","Lily pad to the stars!! 🐸"],
      close:["So close! One more hop! 🐸","Almost! Hoppy believes! 💚"],
      miss:["Oopsie! Ribbit... 🥺","Frogs always bounce back! 🐸💕"],
      victory:["Frog wins!! Ribbit ribbit!! 🎊","Best hop ever!! 🐸💚"],
      bonus:["Bonus flies!! So many flies!! 🐸","Free round! Hip hip hooray! 💚"],
      mystery:["Ooh what's in the lily pad?! 🐸🎁","Hoppy is so curious!! 🌿"],
    }},
  { id:"cat",       name:"Luna",    color:"#b094d4", rarity:"common",
    unlockCond:{type:"level",value:6}, unlockHint:"Complete Level 6",
    e:{idle:"😸",happy:"😻",excited:"😻",fire:"💜",fever:"✨",sad:"😿",victory:"👑",scared:"🙈"},
    dance:"danceCat",    catchphrase:"Purr-fectly amazing, just like you! 😸",
    speeches:{
      idle:["Purrr... shall we? 😸","Meow! Luna is here! 😸","Luna thinks you're wonderful! 💜"],
      happy:["Purrfect!! 😻","*purrs the happiest purr* ✨","Luna is so pleased! 😸"],
      streak:["Purr-fect streak!! 😻💜","Luna is impressed!! ✨","This is magnificent! 😻"],
      fever:["Luna fever! All sparkly!! ✨✨","The queen is delighted!! 👑"],
      close:["Almost! Luna knows you can! 👑","So close! One more! 😻"],
      miss:["Hmph... Luna still believes! 😿","Try again, little adventurer! 💜"],
      victory:["Luna purrs with pride!! 👑","We won! Luna chose wisely! 😸"],
      bonus:["BONUS! Luna gets ALL the toys!! ✨","Mine mine mine! 😻💜"],
      mystery:["Ooh shiny thing!! Luna WANTS!! 🎁✨","A box?! Luna adores boxes! 😸"],
    }},
  // ── RARE ─────────────────────────────────────────────────────
  { id:"panda",     name:"Bao",     color:"#94a3b8", rarity:"rare",
    unlockCond:{type:"level",value:10}, unlockHint:"Complete Level 10",
    e:{idle:"🐼",happy:"🐼",excited:"⭐",fire:"🌿",fever:"🌟",sad:"🥺",victory:"🎊",scared:"🙈"},
    dance:"dancePanda",  catchphrase:"Chill vibes, big wins! 🐼🍃",
    speeches:{
      idle:["*munch munch* Oh, we playing? 🐼","Bao is calm and ready! 🌿","Very zen, very win. 🐼"],
      happy:["Bao approves! 🐼⭐","*happy panda noises* 🌟","Oh, this is so nice! 🐼"],
      streak:["Bao is on a roll!! ⭐⭐","Panda power! So gentle, so strong! 🐼","Zen master mode!! 🌟"],
      fever:["Panda fever!! Bao is glowing!! 🌟🌟","This is unexpectedly wonderful!! 🐼"],
      close:["Almost! Bao stays calm... 🐼","Breathe and tap! ⭐"],
      miss:["That's okay! Bao has bamboo! 🥺","*gently tries again* 🌿"],
      victory:["Pandas win! Bao is so happy! 🎊","Best day! 🐼⭐🌟"],
      bonus:["Bonus!! Bamboo rain!! 🐼","Free round! Bao is delighted! 🌟"],
      mystery:["Ooh, is it bamboo?! 🐼🎁","Bao is curious and excited! ⭐"],
    }},
  { id:"penguin",   name:"Waddles", color:"#6ec0f5", rarity:"rare",
    unlockCond:{type:"level",value:15}, unlockHint:"Complete Level 15",
    e:{idle:"🐧",happy:"🐧",excited:"❄️",fire:"💙",fever:"🌊",sad:"🥺",victory:"🎉",scared:"🙈"},
    dance:"dancePenguin",catchphrase:"Waddling to victory together! 🐧❄️",
    speeches:{
      idle:["Waddle waddle! Let's go! 🐧","Penguins never give up! ❄️","Slip, slide, smile! 🐧"],
      happy:["Waddles is SO happy!! 🐧❄️","Sliding into success! 💙","Best friends win! 🎉"],
      streak:["Penguin streak! Waddletastic!! 🐧","Sliding on through!! ❄️❄️","Cool cool cool streak! 🐧💙"],
      fever:["Penguin fever!! So cozy but so fast!! 🌊🌊","Waddling at light speed!! 🐧"],
      close:["Almost! Penguins stick together! 🐧","One more slide! ❄️"],
      miss:["*cute slip* That's okay!! 🥺","Penguins bounce right back! ❄️💕"],
      victory:["Waddles wins!! Best day ever!! 🎉🐧","Penguin parade time!! ❄️🎊"],
      bonus:["Bonus!! More fish!! More fun!! 🐧","Free round! Waddles is spinning! ❄️"],
      mystery:["Is it a fish?! Please! 🐧🎁","*slides over excitedly* What is it?! ❄️"],
    }},
  { id:"lion",      name:"Simba",   color:"#fbbf24", rarity:"rare",
    unlockCond:{type:"streak",value:30}, unlockHint:"Achieve a 30-streak",
    e:{idle:"🦁",happy:"🦁",excited:"🌟",fire:"🌻",fever:"💫",sad:"🥺",victory:"🏅",scared:"🙈"},
    dance:"danceLion",   catchphrase:"Brave little lion, brave big heart! 🦁💛",
    speeches:{
      idle:["*tiny roar* Hi!! 🦁","Simba is your friend! 🌟","Ready to be brave together? 🦁"],
      happy:["Roarsome!! 🦁💛","Simba does a little happy dance! 🌟","So wonderful! I love this! 🌻"],
      streak:["Go go go!! 🦁🌟","Simba is cheering SO loud!! 💫","The pride is watching and they're PROUD!! 🦁"],
      fever:["Lion fever!! Simba can't stop smiling!! 💫💫","Bravest lion ever!! 🦁"],
      close:["Almost! Be brave! 🦁","One more! Simba believes in you! 🌟"],
      miss:["Oopsie roar! Try again! 🥺","Even brave lions have oopsies! 🦁💕"],
      victory:["Simba roars with joy!! 🦁🏅","We're the best team!! 🌟💛"],
      bonus:["Bonus!! Simba does zoomies!! 🦁","Free round! So exciting!! 🌻"],
      mystery:["Simba sniffs a gift!! 🦁🎁","What could it be?! Simba wants to know!! 🌟"],
    }},
  // ── EPIC ─────────────────────────────────────────────────────
  { id:"octopus",   name:"Inky",    color:"#b06de8", rarity:"epic",
    unlockCond:{type:"threestars",value:5}, unlockHint:"Get 3 stars on 5 levels",
    e:{idle:"🐙",happy:"🐙",excited:"💜",fire:"🌈",fever:"🌀",sad:"🥺",victory:"🎊",scared:"🙈"},
    dance:"danceOctopus",catchphrase:"Eight hugs for eight friends! 🐙💜",
    speeches:{
      idle:["Eight arms, all for hugging! 🐙","Inky loves you very much! 💜","Let's make waves! 🌊"],
      happy:["Ink-credible!! 🐙💜","Eight arms of happiness!! ✨","Tentacles of triumph! 🎊"],
      streak:["Octopus streak!! Inky is wiggling!! 🐙","Ink everywhere — happy ink!! 💜💜","All eight arms cheering!! 🌀"],
      fever:["Inky fever!! Whirling with joy!! 🌀🌀","Ink all the colours!! 🐙💜"],
      close:["So close! Inky uses all eight to cheer! 🐙","One more! Inky loves you! 💜"],
      miss:["*releases happy ink cloud* It's okay! 🥺","Inky gives you a gentle squeeze! 🐙💕"],
      victory:["Inky wins!! Eight-armed champion of love!! 🎊🐙","Ink-redibly done!! 💜"],
      bonus:["Bonus!! Inky grabs all with eight!! 🐙","Free round! Unlimited wiggling! 🌀"],
      mystery:["Eight arms reach for the gift! 🐙🎁","What is it?! Inky MUST see!! 💜"],
    }},
  { id:"butterfly", name:"Flutter", color:"#f9a8d4", rarity:"epic",
    unlockCond:{type:"coins",value:1000}, unlockHint:"Earn 1,000 total coins",
    e:{idle:"🦋",happy:"🦋",excited:"🌸",fire:"🌺",fever:"🌼",sad:"🥺",victory:"🌟",scared:"🙈"},
    dance:"danceButterfly",catchphrase:"Every tap is a tiny miracle! 🦋🌸",
    speeches:{
      idle:["Flutter flutter! Let's fly! 🦋","Beautiful things happen here! 🌸","Wings open, heart ready! 🦋"],
      happy:["Flutter-mazing!! 🦋🌸","Blooming with joy!! ✨","So beautiful it makes me wiggle! 🌺"],
      streak:["Flutter streak!! Soaring!! 🦋🌸","Wings of wonder!! 🌼","Dancing on the gentlest wind!! 🌺"],
      fever:["Flutter fever!! Blossoming!! 🌼🌼","All petals everywhere!! 🦋✨"],
      close:["Almost! Flutter believes in you! 🦋","One more graceful wingbeat! 🌸"],
      miss:["Even butterflies have wobbly days! 🥺","Flutter flutter, try again! 🦋💕"],
      victory:["Flutter wins!! Most beautiful day!! 🌟🦋","Bloomed into victory!! 🌸🎊"],
      bonus:["Bonus!! Gardens of sparkle!! 🦋","Free round! All the flowers are for you! 🌺"],
      mystery:["A gift wrapped in petals?! 🦋🎁","Flutter trembles with excitement! 🌸"],
    }},
  // ── LEGENDARY ────────────────────────────────────────────────
  { id:"unicorn",   name:"Sparky",  color:"#ffd700", rarity:"legendary",
    unlockCond:{type:"level",value:25}, unlockHint:"Complete Level 25",
    e:{idle:"🦄",happy:"🦄",excited:"🌈",fire:"💖",fever:"💫",sad:"🥺",victory:"💫",scared:"🙈"},
    dance:"danceUnicorn",catchphrase:"You ARE the magic! 🦄✨",
    speeches:{
      idle:["Magic is real, and so are you! 🦄","Sparky sprinkles love everywhere! 🌈","Rainbow hugs! READY! 🌈"],
      happy:["Sparkle sparkle!! 🌈🦄","Magical!! So wonderfully magical!! 💫","Rainbow heart activated! ✨"],
      streak:["Unicorn streak!! Rainbow power!! 🌈🌈","Sparky is doing a happy gallop!! 🦄","Pure magic is happening!! 💫💫"],
      fever:["Unicorn fever!! Maximum sparkle!! 💫💫🌈","Rainbow of love exploding!! 🦄✨🌈"],
      close:["So close!! The magic is almost here!! 🦄","One last spark of belief!! 🌈"],
      miss:["Even unicorns have cloudy days! 🥺","Shine on! Sparky believes! 🦄💕"],
      victory:["Sparky wins!! Most magical day!! 💫🌈","Believe in magic — we made it!! 🦄"],
      bonus:["Bonus!! Rainbow treasure shower!! 🌈🦄","Free round!! Sparky grants ALL wishes!! 💫"],
      mystery:["A magical gift!! Sparky vibrates with joy!! 🦄🎁","Is it rainbow treasure?! 🌈💫"],
    }},
];

// Per-mascot audio profile (base frequency + oscillator wave type)
const MASCOT_AUDIO={
  dragon:   {base:523,wave:"sawtooth"},  fox:      {base:659,wave:"square"},
  cat:      {base:587,wave:"sine"},      frog:     {base:392,wave:"sine"},
  lion:     {base:440,wave:"sawtooth"},  panda:    {base:392,wave:"sine"},
  penguin:  {base:659,wave:"triangle"}, octopus:  {base:349,wave:"sine"},
  butterfly:{base:587,wave:"sine"},      unicorn:  {base:698,wave:"sine"},
};

// Per-mascot vibration pattern on each tap
const MASCOT_HAPTIC={
  dragon:[15],      fox:[8,5,8],    cat:[6],      frog:[10,5,15],
  lion:[20,10,20],  panda:[8],      penguin:[5,8,5], octopus:[4,4,4,4],
  butterfly:[5],    unicorn:[8,4,8,4,12],
};

// Mascot accessories catalog
const MASCOT_ACCESSORIES=[
  {id:"hat",   emoji:"🎩",name:"Top Hat",    cost:200},
  {id:"crown", emoji:"👑",name:"Crown",       cost:350},
  {id:"glasses",emoji:"😎",name:"Sunglasses",  cost:250},
  {id:"halo",  emoji:"✨",name:"Golden Halo",  cost:400},
  {id:"bow",   emoji:"🎀",name:"Bow Tie",      cost:150},
  {id:"star",  emoji:"⭐",name:"Star Badge",   cost:180},
];

// Week key for weekly challenge
function getWeekKey(){const d=new Date();const jan=new Date(d.getFullYear(),0,1);const wk=Math.ceil(((d-jan)/86400000+jan.getDay()+1)/7);return`${d.getFullYear()}-W${wk}`;}

// Daily challenge level (deterministic by date)
function getDailyChallengeLevel(){const d=new Date();const seed=d.getFullYear()*10000+(d.getMonth()+1)*100+d.getDate();const rng=seededRng(seed);return Math.floor(rng()*18)+5;}
function getWeeklyChallengeLevel(){const d=new Date();const jan=new Date(d.getFullYear(),0,1);const wk=Math.ceil(((d-jan)/86400000+jan.getDay()+1)/7);const rng=seededRng(d.getFullYear()*100+wk);return Math.floor(rng()*20)+20;}

// Mystery Box prizes — variable ratio (weights, not percentages)
const MYSTERY_PRIZES=[
  {label:"+500 pts",  emoji:"⭐", type:"points", value:500,  weight:30},
  {label:"+200 pts",  emoji:"✨", type:"points", value:200,  weight:25},
  {label:"+80 🪙",    emoji:"🪙", type:"coins",  value:80,   weight:20},
  {label:"+150 🪙",   emoji:"💰", type:"coins",  value:150,  weight:10},
  {label:"EXTRA LIV", emoji:"❤️", type:"life",   value:1,    weight:10},
  {label:"JACKPOT!",  emoji:"🌈", type:"jackpot",value:2000, weight:5 },
];

// Boss loot table — one guaranteed drop per boss kill
const BOSS_LOOT=[
  {type:"coins",  label:"COIN BURST",    emoji:"💰", min:50,  max:200, weight:40, color:"#ffd700"},
  {type:"xp",     label:"XP SURGE",      emoji:"⚡", min:30,  max:80,  weight:35, color:"#60a5fa"},
  {type:"fragment",label:"RARE FRAGMENT",emoji:"💎", min:1,   max:1,   weight:25, color:"#c084fc"},
];

const LEVEL_NAMES = [
  // World 1 — Dragon's Lair
  "Cave Entrance","Warm Tunnel","Ember Glow","Scale Watch","Fireball Fun","Lava Bridge","Flame Trail","Dragon's Den","Fire Festival","Dragon Buddy",
  // World 2 — Troll Forest
  "Sunny Glade","Mossy Trail","Root Maze","Mushroom Path","Frog Pond","Gnarly Trees","Troll Bridge","Berry Grove","Forest Sing","Troll Chief",
  // World 3 — Elven Kingdom
  "Flower Arch","Elven Trail","Magic Glade","Silver Bow","Arrow Dance","Elven Gates","Crystal Spire","Magic School","Star Garden","Elf Queen",
  // World 4 — Magic Tower
  "Spell School","Wand Study","Magic Maze","Rune Room","Bubble Spell","Rainbow Portal","Sparkle Room","Potion Walk","Star Ritual","Wizard Pal",
  // World 5 — Viking Fjords
  "Shore Landing","Ice Trail","Rune Road","Frost Path","Snow Crossing","Viking Fun","Longship Run","Aurora Path","Rainbow Gate","Viking King",
  // World 6 — Goblin Mines
  "Mine Entry","Gold Vein","Gem Tunnel","Goblin Run","Treasure Hunt","Pickaxe Path","Crystal Cave","Deep Mine","Gem Blitz","Goblin Chief",
  // World 7 — Haunted Castle
  "Spooky Hall","Silly Creep","Ghost Slide","Pumpkin Road","Candy Maze","Friendly Gate","Castle Trail","Giggle Run","Silly Mass","Friendly Ghost",
  // World 8 — Ocean Deep
  "Coral Reef","Sea Cave","Tide Rush","Current Dash","Deep Dive","Jellyfish Trail","Bubble Gate","Seahorse Path","Rainbow Ocean","Friendly Whale",
  // World 9 — Giant's Peak
  "Mountain Base","Boulder Path","Cloud Climb","Thunder Trail","Giant Steps","Peak Rush","Cloud Walk","Lightning Pass","Summit Fun","Gentle Giant",
  // World 10 — Rainbow Dragon
  "Rainbow Path","Treasure Gate","Dragon's Eye","Golden Fire","Rainbow Trial","Colour Flame","Gem Rush","Sparkle Wrath","Final Fun","RAINBOW DRAGON",
];

// Special milestone modifiers
const MILESTONE_MODS = {
  10:  { type:"boss_kill",   desc:"Defeat the Dragon Buddy! 🐉" },
  20:  { type:"combo_20",    desc:"Tap 20 times without stopping! ⭐" },
  30:  { type:"fever_2",     desc:"Activate Magic Mode twice! ✨" },
  40:  { type:"no_miss",     desc:"Don't miss a single target! 🎯" },
  50:  { type:"speed_run",   desc:"Race to the finish! 🏃" },
  60:  { type:"moving_only", desc:"Tap only the moving targets! 💨" },
  70:  { type:"ghost_rush",  desc:"Find the hiding ghosts! 👻" },
  80:  { type:"combo_40",    desc:"Reach a 40× tap chain! 🔥" },
  90:  { type:"fever_3",     desc:"Activate Magic Mode 3 times! 🌟" },
  100: { type:"final_boss",  desc:"Defeat the RAINBOW DRAGON! 🌈" },
};

function getLevelConfig(n) {
  const world = Math.ceil(n / 10);
  const w = WORLDS[world - 1];
  const diff = (n - 1) / 99;
  const scoreGoal = Math.floor(200 + n * 100 + n * n * 0.6);
  const lives = n <= 10 ? 5 : n <= 30 ? 4 : n <= 70 ? 3 : 2;
  const spawnInterval = Math.max(420, 1400 - n * 9.5);
  const targetLifetime = Math.max(800, 3200 - n * 19);
  const bombRate = n < 6  ? 0 : Math.min(0.26, n * 0.0024);
  const movingRate = n < 11 ? 0 : Math.min(0.60, (n - 10) * 0.0068);
  const ghostRate = n < 21 ? 0 : Math.min(0.26, (n - 20) * 0.0032);
  const bossEnabled = n >= 25 || n % 10 === 0;
  const bossRate = n % 10 === 0 && n < 25 ? 0.055 : n >= 25 ? Math.min(0.07, (n-25)*0.001) : 0;
  const mod = MILESTONE_MODS[n] || null;
  return {
    id: n, world, worldName: w.name, worldColor: w.color, worldBg: w.bg, worldGrid: w.grid,
    name: LEVEL_NAMES[n - 1] || `Level ${n}`,
    scoreGoal, lives, spawnInterval, targetLifetime,
    bombRate, movingRate, ghostRate, bossEnabled, bossRate,
    modifier: mod,
    rarityBonus: Math.min(0.14, diff * 0.14),
    isBoss: n % 10 === 0,
    isLast: n === 100,
  };
}

const ALL_LEVELS = Array.from({ length: 100 }, (_, i) => getLevelConfig(i + 1));

function getInfinityLevelConfig(round) {
  const n = 100 + round; // equivalent difficulty level
  const worldId = ((round - 1) % 10) + 1;
  const w = WORLDS[worldId - 1];
  const scoreGoal = Math.floor(8000 + round * 600 + round * round * 1.2);
  const spawnInterval = Math.max(280, 520 - round * 3.5);
  const targetLifetime = Math.max(650, 1400 - round * 7);
  const bombRate = Math.min(0.32, 0.18 + round * 0.003);
  const movingRate = Math.min(0.70, 0.40 + round * 0.006);
  const ghostRate = Math.min(0.30, 0.15 + round * 0.003);
  return {
    id:`inf${round}`, world:worldId, worldName:w.name, worldColor:w.color, worldBg:w.bg, worldGrid:w.grid,
    name:`∞ Round ${round}`, scoreGoal, lives:2, spawnInterval, targetLifetime,
    bombRate, movingRate, ghostRate, bossEnabled:false, bossRate:0,
    modifier:null, rarityBonus:0.18, isBoss:false, isLast:false, isInfinity:true,
  };
}

function getZenConfig(worldId=1) {
  const w=WORLDS[(worldId-1)%10];
  return {
    id:`zen${worldId}`, world:worldId, worldName:w.name, worldColor:w.color, worldBg:w.bg, worldGrid:w.grid,
    name:`☯ Zen — ${w.name}`, scoreGoal:99999, lives:99, spawnInterval:700,
    targetLifetime:3000, bombRate:0, movingRate:0.30, ghostRate:0.15,
    bossEnabled:false, bossRate:0, modifier:{type:"zen_timer",desc:"90s Zen — Pure tapping"},
    rarityBonus:0.15, isBoss:false, isLast:false, isZen:true, zenDuration:90000,
  };
}

function getTimeAttackConfig(worldId=1) {
  const w=WORLDS[(worldId-1)%10];
  return {
    id:`timeattack${worldId}`, world:worldId, worldName:w.name, worldColor:w.color, worldBg:w.bg, worldGrid:w.grid,
    name:`⚡ Time Attack — ${w.name}`, scoreGoal:99999, lives:3, spawnInterval:450,
    targetLifetime:2200, bombRate:0.04, movingRate:0.45, ghostRate:0.12,
    bossEnabled:false, bossRate:0, modifier:{type:"time_attack",desc:"45s sprint! Max score wins!"},
    rarityBonus:0.12, isBoss:false, isLast:false, isZen:true, zenDuration:45000, isTimeAttack:true,
  };
}

// ═══════════════════════════════════════════════════════════════
// GAME CONSTANTS
// ═══════════════════════════════════════════════════════════════
const BASE_R       = 26;
const MAX_LIVES    = 5;
const FEVER_STREAK = 15;
const FEVER_DUR    = 8000;
const XP_PER_LVL   = 150;

// Skill tree — 6 skills × 3 levels, costs 1 skill point per upgrade
const SKILL_TREE = [
  { id:"coin_magnet",  icon:"🪙", name:"Coin Magnet",  desc:"Earn more coins per tap",       levels:["+5% coins","+10% coins","+20% coins"],  cost:[1,2,3] },
  { id:"target_sense", icon:"👁", name:"Target Sense", desc:"Targets stay longer",            levels:["+0.5s life","+1s life","+2s life"],      cost:[1,2,3] },
  { id:"fever_rush",   icon:"🔥", name:"Fever Rush",   desc:"Fever lasts longer & fills faster",levels:["+20% fever","+40% fever","+60% fever"],cost:[1,2,3] },
  { id:"critical_eye", icon:"🎯", name:"Critical Eye", desc:"Perfect-tap zone is wider",       levels:["+10% zone","+20% zone","+35% zone"],   cost:[1,2,3] },
  { id:"combo_guard",  icon:"🛡", name:"Combo Guard",  desc:"Streak survives more misses",     levels:["1st miss kept","2nd miss kept","3rd miss kept"],cost:[2,3,4]},
  { id:"xp_boost",     icon:"⚡", name:"XP Boost",     desc:"Earn more XP from levels",        levels:["+10% XP","+20% XP","+30% XP"],         cost:[1,2,3] },
];

const RARITY = {
  COMMON:    { name:"common",    chance:0.55, color:"#a78bfa", glow:"#7c3aed", mult:1,  size:1.0,  label:""            },
  UNCOMMON:  { name:"uncommon",  chance:0.23, color:"#34d399", glow:"#059669", mult:2,  size:1.15, label:"LUCKY ×2"    },
  RARE:      { name:"rare",      chance:0.13, color:"#fbbf24", glow:"#d97706", mult:5,  size:1.4,  label:"MAGIC ×5"    },
  EPIC:      { name:"epic",      chance:0.07, color:"#f472b6", glow:"#db2777", mult:15, size:1.8,  label:"SUPER ×15"   },
  LEGENDARY: { name:"legendary", chance:0.02, color:"#ff6030", glow:"#ff2000", mult:50, size:2.2,  label:"RAINBOW ×50" },
};

const THEMES = [
  { id:"neon",    name:"Arcane Night",  bg:"#0c0718",accent:"#a78bfa",secondary:"#6d28d9",grid:"rgba(167,139,250,0.05)",unlockLevel:1  },
  { id:"cyber",   name:"Ice Kingdom",   bg:"#020c18",accent:"#22d3ee",secondary:"#0e7490",grid:"rgba(34,211,238,0.04)", unlockLevel:5  },
  { id:"inferno", name:"Dragon Fire",   bg:"#180400",accent:"#ff6030",secondary:"#b45309",grid:"rgba(255,96,48,0.05)", unlockLevel:10 },
  { id:"matrix",  name:"Elven Forest",  bg:"#000e00",accent:"#39ff14",secondary:"#166534",grid:"rgba(57,255,20,0.04)", unlockLevel:20 },
  { id:"abyss",   name:"Dark Abyss",    bg:"#00000f",accent:"#6060ff",secondary:"#1a1a9a",grid:"rgba(96,96,255,0.04)",unlockLevel:30 },
  { id:"rose",    name:"Sacred Gold",   bg:"#180810",accent:"#ff8fab",secondary:"#9d174d",grid:"rgba(255,143,171,0.04)",unlockLevel:40},
];

const COMBO_LABELS = [
  [50,"AMAZING!! 🌈"],[35,"SUPER HERO! ⚡"],[20,"CHAMPION! 💥"],[10,"AWESOME! ⭐"],[5,"NICE! 😄"],
];

const ACHIEVEMENTS = [
  { id:"first_tap",    label:"First Tap!",       desc:"Your very first tap in the game",  icon:"👆", xp:10  },
  { id:"streak_10",    label:"On Fire",          desc:"10-tap streak — keep going!",      icon:"🔥", xp:20  },
  { id:"streak_25",    label:"Super Tapper",     desc:"25-tap streak!",                   icon:"⭐", xp:40  },
  { id:"streak_50",    label:"Tap Champion",     desc:"50-tap streak — incredible!",      icon:"🏆", xp:80  },
  { id:"first_rare",   label:"Magic Find",       desc:"Tap a Magic target",               icon:"✨", xp:15  },
  { id:"first_epic",   label:"Super Touch",      desc:"Tap a Super target",               icon:"💎", xp:30  },
  { id:"legendary",    label:"Rainbow Power",    desc:"Tap a Rainbow target",             icon:"🌈", xp:100 },
  { id:"boss_kill",    label:"Boss Beater",      desc:"Defeat a world boss",              icon:"🐉", xp:50  },
  { id:"perfect_tap",  label:"Bulls-Eye!",       desc:"Land a Perfect Tap",               icon:"🎯", xp:15  },
  { id:"level_10",     label:"Dragon Friend",    desc:"Complete Dragon's Lair",           icon:"🐉", xp:50  },
  { id:"level_25",     label:"World Explorer",   desc:"Reach World 3",                   icon:"🧭", xp:80  },
  { id:"level_50",     label:"Adventure Hero",   desc:"Reach World 5",                   icon:"🏆", xp:120 },
  { id:"level_100",    label:"QUEST MASTER",     desc:"Complete all 100 levels!",         icon:"🌈", xp:500 },
  { id:"three_stars",  label:"3 Stars!",         desc:"Get 3 stars on any level",         icon:"🌟", xp:25  },
  { id:"fever_mode",   label:"Magic Mode!",      desc:"Activate Magic Mode",              icon:"⚡", xp:30  },
  { id:"score_2000",   label:"Score Hero",       desc:"Score 2000 points in a level",     icon:"🚀", xp:50  },
  { id:"missions_all", label:"Quest Finisher",   desc:"Complete all daily quests",        icon:"📜", xp:50  },
  { id:"daily_7",      label:"Daily Hero",       desc:"Play 7 days in a row",             icon:"🌞", xp:75  },
  { id:"five_star",    label:"Perfect Boss Win",  desc:"3-star a boss level",             icon:"⭐", xp:100 },
  { id:"weekly_done",  label:"Weekly Champion",   desc:"Clear the Weekly Challenge",       icon:"🗓️", xp:150 },
  // ── Mid-tier achievements (added Phase 4A) ──
  { id:"streak_5",     label:"Warming Up",        desc:"5-tap streak",                     icon:"🌟", xp:8  },
  { id:"combo_15",     label:"Combo Master",      desc:"Reach a 15× combo",                icon:"💫", xp:30 },
  { id:"three_stars_5",label:"Star Collector",    desc:"Get 3 stars on 5 different levels",icon:"✨", xp:60 },
  { id:"mimic_hit",    label:"Mirror Master",     desc:"Tap a Mimic target",               icon:"🪞", xp:25 },
  { id:"shielded_hit", label:"Shield Breaker",    desc:"Destroy a Shielded target",        icon:"🛡", xp:30 },
  { id:"chain_4",      label:"Chain Reaction",    desc:"Trigger 4 chains in one tap",      icon:"⚡", xp:45 },
  { id:"mascot_lv10",  label:"Best Friends",      desc:"Level up a mascot to LV10",        icon:"🐾", xp:80 },
  { id:"prestige_1",   label:"Prestige Pioneer",  desc:"Prestige for the first time",      icon:"👑", xp:200 },
  { id:"tournament_top",label:"Tournament Top",   desc:"Reach Top 15% in a daily tournament",icon:"🏆", xp:90 },
  // Phase 10A — 5 new achievements
  { id:"zen_master",   label:"Zen Master",       desc:"Complete a Zen Mode session",         icon:"☯",  xp:40  },
  { id:"streak_50",    label:"Streak Legend",    desc:"Achieve a 50× streak",                icon:"🔥", xp:75  },
  { id:"coins_500",    label:"Coin Hoarder",     desc:"Collect 500 coins total",             icon:"💰", xp:35  },
  { id:"offline_earn",    label:"Passive Earner",    desc:"Earn coins while offline",          icon:"🌙", xp:20  },
  { id:"all_worlds",      label:"World Traveler",    desc:"Play in all 10 worlds",             icon:"🌍", xp:100 },
  { id:"friday_fever",    label:"Friday Fever",      desc:"Play on a Friday for 2× XP",       icon:"🔥", xp:25  },
  { id:"weekend_warrior", label:"Weekend Warrior",   desc:"Play on a weekend for bonus coins", icon:"🎉", xp:25  },
  { id:"speed_demon",     label:"Speed Demon",       desc:"Complete a level with a speed bonus", icon:"⚡", xp:30 },
  { id:"phantom_catch",   label:"Ghost Hunter",      desc:"Catch a Phantom target",              icon:"👻", xp:40 },
  { id:"volatile_defuse", label:"Bomb Squad",        desc:"Defuse a Volatile target",            icon:"💣", xp:35 },
  { id:"healer_catch",    label:"First Aid",         desc:"Tap a Healer to restore a life",      icon:"❤️", xp:25 },
  { id:"overkill_5x",    label:"OVERKILL",          desc:"Score 5× the level goal",             icon:"🌟", xp:80 },
  { id:"twin_hit",        label:"Dynamic Duo",       desc:"Tap a Twin target pair",              icon:"✦", xp:30  },
  { id:"rainbow_catch",   label:"Color Catcher",     desc:"Tap a Rainbow target",                icon:"🌈", xp:20  },
  { id:"rainbow_legendary",label:"Perfect Rainbow",  desc:"Tap a Rainbow at Legendary tier",     icon:"🌟", xp:60  },
  { id:"frozen_catch",    label:"Ice Breaker",       desc:"Tap a Frozen target's small zone",    icon:"❄️", xp:45  },
  { id:"bouncy_catch",    label:"Reflex Master",     desc:"Catch a fast Bouncy target",          icon:"⚡", xp:30  },
  { id:"ninja_catch",     label:"Ninja Hunter",      desc:"Catch a Ninja target when it appears",icon:"🥷", xp:75  },
  { id:"loot_chest",     label:"Treasure Hunter",   desc:"Tap a Loot Chest for coin drops",     icon:"🪙", xp:35  },
  { id:"first_tap_fever",label:"Hot Start",          desc:"Get First Tap Fever on a rare+ target",icon:"🔥", xp:40  },
  { id:"bounty_hit",     label:"Bounty Hunter",      desc:"Tap a crowned Bounty target",          icon:"👑", xp:30  },
  { id:"tornado_catch",  label:"Eye of the Storm",   desc:"Tap a Tornado target",                 icon:"🌀", xp:45  },
  { id:"bubble_pop",     label:"Pop Star",           desc:"Pop a soap Bubble target",             icon:"🫧", xp:15  },
  { id:"bomb_defuse",    label:"Bomb Squad",         desc:"Double-tap a bomb to defuse it!",      icon:"💚", xp:50  },
  { id:"echo_tap",       label:"Resonance",          desc:"Tap an Echo target",                   icon:"◎",  xp:25  },
  { id:"echo_bonus",     label:"Double Echo",        desc:"Catch the Echo ghost for bonus points",icon:"🔮", xp:55  },
  { id:"gold_rush",      label:"Gold Rush!",          desc:"Trigger a Gold Rush Mode",             icon:"🥇", xp:40  },
  { id:"crystal_shatter",label:"Gem Hunter",         desc:"Shatter a Crystal target",             icon:"💎", xp:30  },
  { id:"crystal_chain",  label:"Full Crystal",       desc:"Tap all 3 crystal shards after shattering",icon:"💠",xp:80 },
  { id:"chain_lightning_hit",label:"Lightning Rod",  desc:"Use the Chain Lightning power-up",     icon:"⚡", xp:45  },
  { id:"rage_tap",           label:"Anger Manager",  desc:"Tap a Rage target",                    icon:"😠", xp:30  },
  { id:"rage_max",           label:"Rage Quit",      desc:"Tap a Rage target at maximum rage",    icon:"😡", xp:75  },
  { id:"divider_tap",        label:"Cell Division",  desc:"Tap a Divider to split it",            icon:"÷",  xp:25  },
  { id:"vanishing_tap",      label:"Ghost Buster",   desc:"Tap a Vanishing target",               icon:"👻", xp:30  },
  { id:"vanishing_blind",    label:"Sixth Sense",    desc:"Tap a Vanishing target while invisible",icon:"🎯", xp:90  },
  { id:"tap_frenzy",         label:"Tap Frenzy",     desc:"Hit 5 targets in 2 seconds",           icon:"⚡", xp:35  },
  { id:"homing_tap",         label:"Target Acquired", desc:"Tap a Homing target",                  icon:"🎯", xp:20  },
  { id:"homing_center",      label:"Bullseye",        desc:"Tap a Homing target near screen center",icon:"🎯", xp:60  },
  { id:"world_complete",     label:"World Conqueror", desc:"Complete all levels in any world",      icon:"🌍", xp:100 },
  { id:"gemstone_tap",       label:"Gem Collector",   desc:"Find and tap a Gemstone",              icon:"💎", xp:120 },
  { id:"poison_tap",         label:"Antidote",        desc:"Neutralize a Poison target",           icon:"☣️", xp:30  },
  { id:"morph_tap",          label:"Shape Shifter",   desc:"Tap a Morph target",                   icon:"🔄", xp:25  },
  { id:"morph_legendary",    label:"Perfect Morph",   desc:"Catch a Morph at Legendary tier",      icon:"🌟", xp:120 },
  { id:"time_warp_use",      label:"Time Lord",       desc:"Use the Time Warp power-up",           icon:"⏱️", xp:40  },
  { id:"conductor_tap",      label:"Music Maestro",   desc:"Tap a Conductor target",               icon:"♪",  xp:30  },
  { id:"siphon_tap",         label:"Danger Seeker",   desc:"Block a Siphon before it drains life", icon:"⚠️", xp:65  },
  { id:"glitch_tap",         label:"Bug Hunter",      desc:"Tap a Glitch target",                  icon:"🟢", xp:25  },
  { id:"glitch_perfect",     label:"Mid-Glitch!",     desc:"Tap a Glitch target mid-teleport",     icon:"⚡", xp:90  },
  { id:"prism_tap",          label:"Prism Hunter",    desc:"Tap a Prism target",                   icon:"🔮", xp:35  },
  { id:"lucky_streak",       label:"Lucky Seven",     desc:"Hit 7 targets in a row for lucky coins",icon:"🍀", xp:30  },
  { id:"score_boost_use",    label:"Rocket Launch",   desc:"Use the Score Boost power-up",         icon:"🚀", xp:25  },
  { id:"comet_tap",          label:"Star Gazer",      desc:"Tap a Comet target",                   icon:"🌠", xp:30  },
  { id:"comet_early",        label:"Shooting Star",   desc:"Catch a Comet in the first half of its flight",icon:"⭐",xp:75 },
  { id:"mirrorball_tap",     label:"Disco King",      desc:"Tap a Mirror Ball target",             icon:"🪩", xp:35  },
  { id:"nexus_tap",          label:"NEXUS!",           desc:"Find and tap the legendary Nexus",     icon:"🌟", xp:500 },
  { id:"phoenix_tap",        label:"Fire Tamer",       desc:"Tap a Phoenix target",                 icon:"🔥", xp:25  },
  { id:"phoenix_risen",      label:"Born Again",       desc:"Tap a Phoenix in its risen Legendary form",icon:"🦋",xp:80 },
  { id:"icecomet_tap",       label:"Frost Strike",     desc:"Tap an Ice Comet and freeze nearby targets",icon:"❄️",xp:40 },
  { id:"voltage_tap",        label:"Lightning Tapper", desc:"Tap a Voltage target and trigger a chain zap",icon:"⚡",xp:35 },
  { id:"void_tap",           label:"Into the Void",    desc:"Tap a Void target",                        icon:"🌀",xp:45 },
  { id:"void_master",        label:"Black Hole",       desc:"Absorb 3+ targets with a single Void tap", icon:"🕳️",xp:120 },
  { id:"clover_tap",         label:"Lucky Tap",        desc:"Tap a Lucky Clover target",                icon:"🍀",xp:30 },
  { id:"clover_jackpot",     label:"Four-Leaf Fortune",desc:"Roll the maximum ×10 on a Lucky Clover",  icon:"🌟",xp:150 },
  { id:"ricochet_tap",       label:"Billiard Shot",    desc:"Tap a Ricochet target",                   icon:"🎯",xp:30 },
  { id:"ricochet_double",    label:"Bank Shot",        desc:"Ricochet kills 2 targets at once",         icon:"💫",xp:75 },
  { id:"aurora_tap",         label:"Borealis Bop",     desc:"Tap an Aurora target",                    icon:"🌌",xp:35 },
  { id:"aurora_perfect",     label:"Perfect Rhythm",   desc:"Tap Aurora with perfect timing rhythm",   icon:"✨",xp:100 },
  { id:"fury_mode",          label:"Fury Mode",        desc:"Reach a 35× streak — unleash the fury!",  icon:"🌑",xp:90 },
  { id:"score_10k",          label:"Ten Thousand",     desc:"Score 10,000 points in a single session", icon:"💎",xp:100 },
  { id:"portal_tap",         label:"Warp Tapper",      desc:"Tap a Portal target",                     icon:"🌀",xp:40 },
  { id:"portal_chaos",       label:"Chaos Agent",      desc:"Teleport 5+ targets with one Portal tap", icon:"🎲",xp:90 },
  { id:"particlebomb_tap",   label:"Boom!",            desc:"Tap a Particle Bomb target",              icon:"💥",xp:35 },
  { id:"beacon_tap",         label:"Signal Boost",     desc:"Tap a Beacon target",                     icon:"🔆",xp:30 },
  { id:"beacon_surge",       label:"Beacon Surge",     desc:"Tap Beacon with 4+ nearby targets",       icon:"⚡",xp:80 },
  { id:"shadow_tap",         label:"Shadow Boxer",     desc:"Tap a Shadow target",                     icon:"🕶️",xp:30 },
  { id:"shadow_clone_catch", label:"Clone Catcher",    desc:"Catch a Shadow clone for bonus pts",      icon:"👤",xp:70 },
  { id:"spectral_tap",       label:"Ghost Buster",     desc:"Tap a Spectral target",                   icon:"👻",xp:35 },
  { id:"spectral_perfect",   label:"Perfect Apparition",desc:"Catch Spectral in its visible phase",   icon:"💛",xp:90 },
  { id:"heart_tap",          label:"Heartbreaker",     desc:"Tap a Heart target",                      icon:"❤️",xp:25 },
  { id:"nova_tap",           label:"Supernova!",       desc:"Tap a Nova target to freeze all enemies", icon:"💫",xp:60 },
  { id:"firefly_tap",        label:"Firefly Catcher",  desc:"Tap a Firefly target",                   icon:"✨",xp:25 },
  { id:"firefly_swift",      label:"Swift Fingers",    desc:"Catch a Firefly within 0.8s of spawn",   icon:"⚡",xp:80 },
  { id:"geode_crack",        label:"Rock Breaker",     desc:"Crack open a Geode target",              icon:"🪨",xp:20 },
  { id:"geode_gem",          label:"Gem Collector",    desc:"Collect the gem from a cracked Geode",   icon:"💎",xp:75 },
  { id:"timebomb_defuse",    label:"Bomb Squad",       desc:"Defuse a Time Bomb before detonation",   icon:"💣",xp:40 },
  { id:"timebomb_clutch",    label:"Clutch Defuse",    desc:"Defuse a Time Bomb in the last 0.4s",    icon:"🔥",xp:120 },
  { id:"thunderbolt_tap",    label:"Lightning Reflexes",desc:"Tap a Thunderbolt target",              icon:"⚡",xp:30 },
  { id:"thunderbolt_clutch", label:"Cliff Hanger",     desc:"Catch Thunderbolt in the bottom quarter", icon:"🌩️",xp:100 },
  { id:"gravityorb_tap",     label:"Gravity Surfer",   desc:"Tap a Gravity Orb target",               icon:"🌐",xp:35 },
  { id:"gravityorb_cluster", label:"Black Hole Master",desc:"Tap Gravity Orb with 4+ pulled targets", icon:"🕳️",xp:110 },
  { id:"crystalball_tap",    label:"Fortune Teller",   desc:"Tap a Crystal Ball to see the future",   icon:"🔮",xp:45 },
  { id:"quantum_tap",        label:"Quantum Mechanic", desc:"Hit a Quantum target twice",              icon:"⚛️",xp:30 },
  { id:"quantum_stable",     label:"Wave Collapse",    desc:"Stabilize a Quantum target (3rd hit)",   icon:"🌀",xp:100 },
  { id:"clockwork_tap",      label:"Timekeeper",       desc:"Tap a Clockwork target to slow time",    icon:"⏰",xp:35 },
  { id:"bloom_tap",          label:"Flower Power",     desc:"Tap a Bloom and release the petals",     icon:"🌸",xp:25 },
  { id:"bloom_harvest",      label:"Full Bloom",       desc:"Catch all 5 petals from one Bloom",      icon:"💐",xp:90 },
  { id:"reflector_tap",      label:"Mirror Match",     desc:"Tap a Reflector target",                 icon:"🪞",xp:30 },
  { id:"reflector_chaos",    label:"Chaos Mirror",     desc:"Reverse 5+ targets with one Reflector",  icon:"🌀",xp:85 },
  { id:"chain_collect",      label:"Chain Reaction",   desc:"Tap a Chain Collector target",            icon:"⛓️",xp:30 },
  { id:"chain_triple",       label:"Triple Chain",     desc:"Chain Collector grabs 3 targets at once", icon:"🔗",xp:80 },
  { id:"stardust_tap",       label:"Star Chaser",      desc:"Tap a Stardust target",                  icon:"⭐",xp:25 },
  { id:"stardust_shower",    label:"Star Shower",      desc:"Catch 5+ stars from one Stardust",       icon:"🌟",xp:85 },
  { id:"score_25k",          label:"Quarter Million",  desc:"Score 25,000 pts in a single session",   icon:"💰",xp:150 },
  { id:"solarflare_tap",     label:"Sun Blocker",      desc:"Tap a Solar Flare target",               icon:"☀️",xp:30 },
  { id:"solarflare_early",   label:"Early Bird",       desc:"Catch Solar Flare in first 25% of life", icon:"🌅",xp:95 },
  { id:"magma_tap",          label:"Lava Tamer",       desc:"Defuse a Magma before it destroys a target",icon:"🌋",xp:40 },
  { id:"prismbomb_tap",      label:"Prism Splitter",   desc:"Tap a Prism Bomb target",                icon:"🌈",xp:35 },
  { id:"prismbomb_triple",   label:"Rainbow Fury",     desc:"All 3 Prism Bomb beams hit targets",     icon:"💥",xp:100 },
  { id:"icecage_tap",        label:"Ice Warden",       desc:"Trap targets with an Ice Cage",          icon:"🧊",xp:35 },
  { id:"icecage_full",       label:"Deep Freeze",      desc:"Freeze 3+ targets with one Ice Cage",    icon:"❄️",xp:90 },
  { id:"pinwheel_tap",       label:"Spin Doctor",      desc:"Tap a Pinwheel target",                  icon:"🌀",xp:25 },
  { id:"pinwheel_jackpot",   label:"Top Spin",         desc:"Catch Pinwheel at the jackpot position", icon:"🏆",xp:95 },
  { id:"supercell_tap",      label:"Storm Chaser",     desc:"Tap a Supercell target",                 icon:"⛈️",xp:35 },
  { id:"supercell_storm",    label:"Full Storm",       desc:"Supercell has 3+ electrified targets",   icon:"⚡",xp:90 },
  // Batch 12 — Nebula, Wisp, Meteor, Aura Ring
  { id:"nebula_tap",      label:"Stargazer",          desc:"Tap a Nebula target",                    icon:"🌌",xp:30 },
  { id:"nebula_cluster",  label:"Galaxy Brain",       desc:"Nebula releases 4+ star pieces",         icon:"⭐",xp:90 },
  { id:"wisp_tap",        label:"Wisp Catcher",       desc:"Tap a Wisp target",                      icon:"🕯️",xp:35 },
  { id:"wisp_still",      label:"Perfect Stillness",  desc:"Catch Wisp while it's stationary",       icon:"✨",xp:110 },
  { id:"meteor_tap",      label:"Meteor Shower",      desc:"Tap a Meteor target",                    icon:"☄️",xp:30 },
  { id:"meteor_early",    label:"Comet Catcher",      desc:"Tap Meteor in the first 30% of life",    icon:"🌠",xp:85 },
  { id:"aura_tap",        label:"Aura Reader",        desc:"Tap an Aura Ring target",                icon:"🔆",xp:25 },
  { id:"aura_edge",       label:"Edge Runner",        desc:"Tap Aura Ring's outer zone for jackpot", icon:"🎯",xp:100 },
  // Batch 11 — Vortex, Pixel, Neon Sign, Lantern, Drift + power-ups
  { id:"vortex_tap",         label:"Into the Swirl",   desc:"Tap a Vortex target",                    icon:"🌀",xp:30 },
  { id:"vortex_feast",       label:"Vortex Feast",     desc:"Vortex consumes 3+ targets before you tap", icon:"🕳️",xp:105 },
  { id:"pixel_tap",          label:"Pixel Perfect",    desc:"Tap a Pixel target (tiny hitbox!)",      icon:"🟩",xp:35 },
  { id:"pixel_speedrun",     label:"Pixel Speedrun",   desc:"Catch Pixel within 0.5s of spawn",       icon:"⚡",xp:95 },
  { id:"neon_tap",           label:"Neon Tapper",      desc:"Tap a Neon Sign target",                 icon:"💡",xp:25 },
  { id:"neon_gold",          label:"Jackpot Neon",     desc:"Tap Neon Sign in the gold phase",        icon:"⭐",xp:110 },
  { id:"lantern_tap",        label:"Lantern Festival", desc:"Tap a Lantern target",                   icon:"🏮",xp:25 },
  { id:"lantern_chain",      label:"Chain Reaction",   desc:"Lantern chain reaction hits 2 targets",  icon:"🔗",xp:80 },
  { id:"drift_tap",          label:"Drift King",       desc:"Tap a Drift target",                     icon:"🌊",xp:25 },
  { id:"drift_max",          label:"Full Drift",       desc:"Catch Drift at 90%+ distance bonus",     icon:"🏆",xp:100 },
  { id:"berserker_use",      label:"Berserker!",       desc:"Activate the Berserker power-up",        icon:"⚔️",xp:40 },
  { id:"ghost_mode_use",     label:"Phase Shift",      desc:"Use Ghost Mode to walk through a bomb",  icon:"👻",xp:45 },
  // Batch 13 — Thunderclap, Mirror Shard, Rune Stone
  { id:"thunderclap_tap",    label:"Shockwaver",       desc:"Tap a Thunderclap target",               icon:"💥",xp:35 },
  { id:"thunderclap_surge",  label:"Thunder God",      desc:"Thunderclap shockwave hits 4+ targets",  icon:"⚡",xp:105 },
  { id:"mirror_tap",         label:"Mirror Breaker",   desc:"Tap a Mirror Shard target",              icon:"💎",xp:30 },
  { id:"mirror_triple",      label:"Triple Reflection",desc:"Fully shatter a Mirror Shard (3 taps)",  icon:"🌟",xp:95 },
  { id:"rune_tap",           label:"Rune Reader",      desc:"Tap a Rune Stone target",                icon:"ᚱ",xp:30 },
  { id:"rune_power",         label:"Runic Power",      desc:"Tap Rune Stone in a power rune phase (ᚱ/ᚦ)", icon:"🔮",xp:80 },
  { id:"score_50k",          label:"Half Century",     desc:"Score 50,000 pts in a single session",   icon:"💰",xp:200 },
  { id:"score_100k",         label:"Centurion",        desc:"Score 100,000 pts in a single session",  icon:"👑",xp:300 },
  // Batch 14 — Fractal, Plasma, Timewarp Target
  { id:"fractal_tap",        label:"Fractal Splitter", desc:"Tap a Fractal target",                   icon:"🔷",xp:30 },
  { id:"fractal_cascade",    label:"Fractal Cascade",  desc:"Catch all splits from a Fractal",        icon:"💠",xp:90 },
  { id:"plasma_tap",         label:"Plasma Surfer",    desc:"Tap a Plasma target",                    icon:"⚡",xp:30 },
  { id:"plasma_surge",       label:"Full Charge!",     desc:"Tap Plasma during the surge window (70-90%)", icon:"🌊",xp:100 },
  { id:"timewarp_target_tap",label:"Time Bender",      desc:"Tap a Timewarp target to extend time",   icon:"⏱️",xp:35 },
  { id:"timewarp_hoard",     label:"Time Hoarder",     desc:"Timewarp extends 5+ targets",            icon:"🕰️",xp:85 },
  // Batch 15 — Solar Drone, Crystal Cluster, Swarm, Celestial Orb
  { id:"solar_drone_tap",    label:"Drone Strike",     desc:"Tap a Solar Drone target",               icon:"☀️",xp:30 },
  { id:"solar_drone_orbit",  label:"Sun Surfer",       desc:"Catch Solar Drone while it orbits",      icon:"🛸",xp:70 },
  { id:"cluster_tap",        label:"Crystal Smasher",  desc:"Tap a Crystal Cluster target",           icon:"💠",xp:30 },
  { id:"cluster_rainbow",    label:"Rainbow Smash",    desc:"Shatter all 4 crystals in one tap",      icon:"🌈",xp:80 },
  { id:"swarm_tap",          label:"Swarm Lord",       desc:"Tap a Swarm target",                     icon:"🐝",xp:25 },
  { id:"swarm_master",       label:"Swarm Master",     desc:"Pop 3 Swarm targets in a single game",   icon:"🐝",xp:75 },
  { id:"celestial_tap",      label:"Star Gazer",       desc:"Tap a Celestial Orb target",             icon:"🌠",xp:35 },
  { id:"celestial_legend",   label:"Celestial Legend", desc:"Tap 5 Celestial Orbs in a single game",  icon:"🌌",xp:100 },
  // Batch 16 — Smoke Ring, Chain Bomb, Warp Gate, Mirage
  { id:"smoke_tap",          label:"Smoke Signal",     desc:"Tap a Smoke Ring target",                icon:"💨",xp:25 },
  { id:"smoke_early",        label:"Quick Draw",       desc:"Catch a Smoke Ring before 40% expansion",icon:"⚡",xp:70 },
  { id:"chainbomb_tap",      label:"Chain Defuse",     desc:"Tap a Chain Bomb to defuse both",        icon:"🔗",xp:40 },
  { id:"chainbomb_safe",     label:"Bomb Squad Pro",   desc:"Chain Bomb defuse without losing lives",  icon:"💚",xp:90 },
  { id:"warpgate_tap",       label:"Warp Tapper",      desc:"Tap a Warp Gate target",                 icon:"🌀",xp:30 },
  { id:"warpgate_chaos",     label:"Chaos Warp",       desc:"Warp Gate teleports 5+ targets at once", icon:"🌪️",xp:80 },
  { id:"mirage_tap",         label:"Mirage Buster",    desc:"Spot and tap a Mirage target",           icon:"🏜️",xp:15 },
  { id:"mirage_avoid",       label:"Sharp Eyes",       desc:"Let a Mirage expire without tapping it", icon:"👁️",xp:55 },
  { id:"surgeball_tap",      label:"Arc Striker",      desc:"Tap a Surge Ball target",                icon:"⚡",xp:20 },
  { id:"surgeball_chain",    label:"Chain Reaction",   desc:"Surge Ball chains to 3+ targets at once",icon:"🔗",xp:70 },
  { id:"clock_tap",          label:"Clockwatcher",     desc:"Tap a Ticking Clock target",             icon:"🕐",xp:20 },
  { id:"clock_jackpot",      label:"On the Dot",       desc:"Tap Ticking Clock as hand hits 12",      icon:"🎯",xp:90 },
  { id:"echopulse_tap",      label:"Wave Rider",       desc:"Tap an Echo Pulse target",               icon:"◎",xp:20 },
  { id:"echopulse_bonus",    label:"Ring Collector",   desc:"Collect 3+ rings from one Echo Pulse",   icon:"💜",xp:75 },
  { id:"gravwell_tap",       label:"Event Horizon",    desc:"Tap a Gravity Well target",              icon:"🌀",xp:20 },
  { id:"gravwell_feast",     label:"Tidal Force",      desc:"Gravity Well pulls in 4+ targets",       icon:"🌌",xp:80 },
  { id:"spectrum_tap",       label:"Prismatic",        desc:"Tap a Spectrum Orb target",              icon:"🌈",xp:20 },
  { id:"spectrum_gold",      label:"Golden Spectrum",  desc:"Tap Spectrum Orb during yellow phase",   icon:"✨",xp:65 },
  { id:"shardstorm_tap",     label:"Shard Striker",    desc:"Tap a Shard Storm target",               icon:"❄️",xp:20 },
  { id:"shardstorm_align",   label:"Perfect Alignment","desc":"Tap Shard Storm when shards align",     icon:"💠",xp:85 },
  { id:"pulsenova_tap",      label:"Nova Rider",       desc:"Tap a Pulse Nova target",                icon:"💥",xp:20 },
  { id:"pulsenova_release",  label:"Nova Surfer",      desc:"Tap Pulse Nova during nova release",     icon:"🔥",xp:80 },
  { id:"bubble_tap",         label:"Bubble Popper",    desc:"Tap a Bubble Cluster target",            icon:"🫧",xp:15 },
  { id:"bubble_all",         label:"Full Burst",       desc:"Pop a Bubble Cluster in one tap",        icon:"💨",xp:60 },
  { id:"voidshard_tap",      label:"Void Walker",      desc:"Tap a Void Shard target",                icon:"🌑",xp:20 },
  { id:"voidshard_rich",     label:"Absorbed Wealth",  desc:"Tap Void Shard with 200+ absorbed pts",  icon:"💫",xp:80 },
  { id:"stasis_tap",         label:"Frozen Moment",    desc:"Tap a Stasis Field target",              icon:"❄️",xp:20 },
  { id:"stasis_core",        label:"Core Breach",      desc:"Tap Stasis Field before ring expands",   icon:"🎯",xp:75 },
  { id:"crystalline_tap",    label:"Gem Cutter",       desc:"Tap a Crystalline target",               icon:"💎",xp:20 },
  { id:"crystalline_facets", label:"Full Spectrum",    desc:"Crystalline scored 7-facet bonus",       icon:"✨",xp:70 },
  { id:"qleap_tap",          label:"Phase Catcher",    desc:"Tap a Quantum Leap target",              icon:"🔮",xp:25 },
  { id:"qleap_between",      label:"Blink Window",     desc:"Tap Quantum Leap mid-teleport phase",    icon:"⚡",xp:85 },
  { id:"magma_burst_tap",    label:"Heat Seeker",      desc:"Tap a Magma Burst target",               icon:"🌋",xp:20 },
  { id:"magma_burst_hot",    label:"White Hot",        desc:"Tap Magma Burst at peak heat (70%+)",    icon:"🔥",xp:80 },
  { id:"lightning_rod_tap",  label:"Rod Striker",      desc:"Tap a Lightning Rod target",             icon:"⚡",xp:20 },
  { id:"lightning_rod_surge",label:"Mega Surge",       desc:"Lightning Rod attracts 5+ targets",      icon:"⛈️",xp:85 },
  { id:"prismgate_tap",      label:"Gate Opener",      desc:"Tap a Prism Gate target",                icon:"🔷",xp:20 },
  { id:"prismgate_triple",   label:"Tri-Color",        desc:"Catch all 3 shards from Prism Gate",     icon:"🌈",xp:80 },
  { id:"solarburst_tap",     label:"Star Striker",     desc:"Tap a Solar Burst target",               icon:"☀️",xp:20 },
  { id:"solarburst_aoe",     label:"Sunstrike",        desc:"Solar Burst hits 4+ targets at once",    icon:"🌟",xp:90 },
  { id:"frostcomet_tap",     label:"Ice Chaser",       desc:"Tap a Frost Comet target",               icon:"🌨️",xp:20 },
  { id:"frostcomet_freeze",  label:"Permafrost",       desc:"Frost Comet shard freezes a target",     icon:"🧊",xp:65 },
  { id:"energyweb_tap",      label:"Web Spinner",      desc:"Tap an Energy Web target",               icon:"🕸️",xp:20 },
  { id:"energyweb_nodes",    label:"Full Network",     desc:"Energy Web links 5+ targets",            icon:"🔗",xp:80 },
  { id:"voidrift_tap",       label:"Rift Caller",      desc:"Tap a Void Rift target",                 icon:"🌀",xp:25 },
  { id:"voidrift_feast",     label:"Singularity",      desc:"Void Rift devours 5+ targets",           icon:"💀",xp:95 },
  { id:"lasergrid_tap",      label:"Lock On",          desc:"Tap a Laser Grid target",                icon:"🎯",xp:20 },
  { id:"lasergrid_beam",     label:"Beam Aligned",     desc:"Tap Laser Grid when beams align (top)",  icon:"🔴",xp:80 },
  { id:"bouncing_tap",       label:"Ball Tracker",     desc:"Tap a Bouncing Orb target",              icon:"⚽",xp:20 },
  { id:"bouncing_4x",        label:"Max Bounce",       desc:"Bouncing Orb reaches 8× multiplier",     icon:"💫",xp:90 },
  { id:"timecapsule_tap",    label:"Time Keeper",      desc:"Tap a Time Capsule target",              icon:"⏳",xp:20 },
  { id:"timecapsule_golden", label:"Golden Moment",    desc:"Tap Time Capsule in the golden window",  icon:"🥇",xp:95 },
  { id:"nethershard_tap",    label:"Dark Wielder",     desc:"Tap a Nether Shard target",              icon:"🔮",xp:20 },
  { id:"nethershard_max",    label:"Nether Master",    desc:"Tap Nether Shard at max power (×10)",    icon:"⚫",xp:100 },
  { id:"accumulator_tap",    label:"Stack Master",     desc:"Tap an Accumulator target",              icon:"📈",xp:20 },
  { id:"accumulator_max",    label:"Overflow",         desc:"Tap Accumulator with 8+ stacks",         icon:"💥",xp:90 },
  { id:"jackpot_tap",        label:"Jackpot!",         desc:"Tap the Jackpot Target",                 icon:"🎰",xp:50 },
  { id:"phaseorb_tap",       label:"Phase Rider",      desc:"Tap a Phase Orb target",                 icon:"🔵",xp:20 },
  { id:"phaseorb_bonus",     label:"Golden Phase",     desc:"Tap Phase Orb in the bonus (💰) phase",  icon:"💰",xp:75 },
  { id:"ripplewave_tap",     label:"Wave Caller",      desc:"Tap a Ripple Wave target",               icon:"🌊",xp:20 },
  { id:"ripplewave_chain",   label:"Chain Ripple",     desc:"Ripple Wave ring hits 3+ targets",       icon:"🔗",xp:75 },
  { id:"venomorb_tap",       label:"Venom Breaker",    desc:"Tap a Venom Orb target",                 icon:"☠️",xp:20 },
  { id:"venomorb_antidote",  label:"Quick Antidote",   desc:"Tap Venom Orb in first second (antidote)",icon:"💚",xp:65 },
  { id:"thunderegg_tap",     label:"Egg Cracker",      desc:"Tap a Thunder Egg target",               icon:"🥚",xp:20 },
  { id:"thunderegg_crack",   label:"Lightning Born",   desc:"Tap Thunder Egg at phase 3 (full crack)",icon:"⚡",xp:80 },
  { id:"moonglyph_tap",      label:"Glyph Reader",     desc:"Tap a Moon Glyph target",                icon:"☽",xp:20 },
  { id:"moonglyph_match",    label:"Symbol Matched",   desc:"Tap Moon Glyph when it shows ☽",        icon:"🌙",xp:70 },
  { id:"comettail_tap",      label:"Comet Chaser",     desc:"Tap a Comet Tail target",                icon:"☄️",xp:20 },
  { id:"comettail_early",    label:"Blazing Speed",    desc:"Tap Comet Tail with 50%+ life remaining",icon:"🔥",xp:70 },
  { id:"emberring_tap",      label:"Ember Dancer",     desc:"Tap an Ember Ring target",               icon:"🔥",xp:20 },
  { id:"emberring_max",      label:"Full Blaze",       desc:"Tap Ember Ring at max expansion",        icon:"🌋",xp:80 },
  { id:"supernova_tap",      label:"Nova Starter",     desc:"Tap a Supernova target",                 icon:"💫",xp:20 },
  { id:"supernova_collect",  label:"Star Field",       desc:"Collect 4+ mini-orbs from a Supernova",  icon:"⭐",xp:80 },
  { id:"crystalmatrix_tap",  label:"Matrix Breaker",   desc:"Tap a Crystal Matrix target",            icon:"💎",xp:20 },
  { id:"crystalmatrix_shatter","label":"Full Shatter", desc:"Destroy all 3 Crystal Matrix layers",   icon:"✨",xp:75 },
  { id:"plasmastorm_tap",    label:"Storm Rider",      desc:"Tap a Plasma Storm target",              icon:"⚡",xp:20 },
  { id:"plasmastorm_surge",  label:"Thunder King",     desc:"Plasma Storm electrifies 5+ targets",    icon:"⛈️",xp:85 },
  { id:"nexusgate_tap",      label:"Gate Master",      desc:"Tap a Nexus Gate target",                icon:"🔵",xp:20 },
  { id:"nexusgate_chain",    label:"Portal Chain",     desc:"Nexus Gate teleports 3+ targets",        icon:"🌀",xp:80 },
  { id:"arclight_tap",      label:"Arc Rider",        desc:"Tap an Arc Light target",                icon:"⚡",xp:20 },
  { id:"arclight_web",      label:"Lightning Web",    desc:"Arc Light connects to 4+ targets",       icon:"🕸️",xp:80 },
  { id:"pulsechain_tap",    label:"Chain Starter",    desc:"Tap a Pulse Chain target",               icon:"🔗",xp:20 },
  { id:"pulsechain_burst",  label:"Chain Reaction",   desc:"Pulse Chain destroys 3+ nearby targets", icon:"💥",xp:75 },
  { id:"ghostorb_tap",      label:"Ghost Hunter",     desc:"Tap a Ghost Orb target",                 icon:"👻",xp:20 },
  { id:"ghostorb_phantom",  label:"Phantom Touch",    desc:"Tap Ghost Orb at 90%+ invisible",        icon:"🌫️",xp:85 },
  { id:"timefracture_tap",  label:"Time Breaker",     desc:"Tap a Time Fracture target",             icon:"⏳",xp:20 },
  { id:"timefracture_haul", label:"Temporal Feast",   desc:"5+ targets on screen when fracture triggers",icon:"⏱️",xp:90 },
  { id:"solarorbit_tap",   label:"Orbit Rider",      desc:"Tap a Solar Orbit target",               icon:"🪐",xp:20 },
  { id:"solarorbit_align", label:"Grand Alignment",  desc:"All 3 planets aligned when orbit tapped", icon:"🌌",xp:85 },
  { id:"chroma_tap",       label:"Color Collector",  desc:"Tap a Chroma Shift target",              icon:"🎨",xp:20 },
  { id:"chroma_gold",      label:"Golden Frequency", desc:"Tap Chroma Shift during gold phase",      icon:"🏅",xp:90 },
  { id:"pulsar_tap",       label:"Pulsar Hunter",    desc:"Tap a Neon Pulsar target",               icon:"💡",xp:20 },
  { id:"pulsar_peak",      label:"Peak Power",       desc:"Tap Neon Pulsar at its maximum size",    icon:"⚡",xp:75 },
  { id:"driftmine_tap",    label:"Mine Defuser",     desc:"Tap a Drift Mine target",                icon:"💣",xp:20 },
  { id:"driftmine_clutch", label:"Clutch Defuse",    desc:"Defuse Drift Mine with <600ms remaining",icon:"⏱️",xp:90 },
  { id:"eclipse_tap",      label:"Eclipse Watcher",  desc:"Tap an Eclipse Orb target",              icon:"🌗",xp:20 },
  { id:"eclipse_moon",     label:"Moonshot",         desc:"Tap Eclipse Orb during moon phase",      icon:"🌙",xp:80 },
  { id:"spinbloom_tap",    label:"Bloom Collector",  desc:"Tap a Spin Bloom target",                icon:"🌸",xp:20 },
  { id:"spinbloom_full",   label:"Full Bloom",       desc:"Tap Spin Bloom with 7+ petals intact",   icon:"🌺",xp:80 },
  { id:"teslanode_tap",    label:"Tesla Tapper",     desc:"Tap a Tesla Node target",                icon:"⚡",xp:20 },
  { id:"teslanode_rich",   label:"Power Surge",      desc:"Claim 400+ banked pts from Tesla Node",  icon:"🔋",xp:85 },
  { id:"runecircle_tap",   label:"Rune Scholar",     desc:"Tap a Rune Circle target",               icon:"🔮",xp:20 },
  { id:"runecircle_jackpot",label:"Sacred Rune",     desc:"Tap Rune Circle at the legendary rune",  icon:"✨",xp:90 },
  { id:"starforge_tap",    label:"Forge Starter",    desc:"Tap a Star Forge target",                icon:"⭐",xp:20 },
  { id:"starforge_max",    label:"Full Charge",      desc:"Tap Star Forge at max charge (95%+)",    icon:"💥",xp:95 },
  { id:"magicdice_tap",    label:"Dice Roller",      desc:"Tap a Magic Dice target",                icon:"🎲",xp:20 },
  { id:"magicdice_six",    label:"Lucky Six",        desc:"Tap Magic Dice when it shows 6",         icon:"🎰",xp:80 },
  { id:"wormhole_tap",     label:"Wormhole Rider",   desc:"Tap a Wormhole target",                  icon:"🌀",xp:20 },
  { id:"wormhole_chaos",   label:"Total Chaos",      desc:"Wormhole teleports 6+ targets",          icon:"🌌",xp:85 },
  { id:"shadowpulse_tap",  label:"Shadow Hunter",    desc:"Tap a Shadow Pulse target",              icon:"🌑",xp:20 },
  { id:"shadowpulse_dark", label:"Into the Dark",    desc:"Tap Shadow Pulse in its dark phase",     icon:"🌚",xp:80 },
  { id:"qsplit_tap",       label:"Quantum Splitter",  desc:"Tap a Quantum Split target",             icon:"⚛️",xp:20 },
  { id:"qsplit_collect",   label:"Both Halves",       desc:"Collect both halves of Quantum Split",   icon:"⚛️",xp:80 },
  { id:"frostbolt_tap",    label:"Bolt Catcher",      desc:"Tap a Frost Bolt target",                icon:"❄️",xp:20 },
  { id:"frostbolt_speed",  label:"Speed Freezer",     desc:"Tap Frost Bolt with 70%+ lifetime left", icon:"🧊",xp:80 },
  { id:"vortexstar_tap",   label:"Star Vortex",       desc:"Tap a Vortex Star target",               icon:"🌟",xp:20 },
  { id:"vortexstar_max",   label:"Full Orbit",        desc:"Tap Vortex Star with 7+ trailing stars", icon:"💫",xp:85 },
  { id:"pixelburst_tap",   label:"Pixel Tapper",      desc:"Tap a Pixel Burst target",               icon:"🎮",xp:20 },
  { id:"pixelburst_full",  label:"Full Resolution",   desc:"Fully un-pixelate a Pixel Burst",        icon:"🖥️",xp:80 },
  { id:"neoncrystal_tap",  label:"Crystal Tapper",   desc:"Tap a Neon Crystal target",              icon:"💎",xp:20 },
  { id:"neoncrystal_hot",  label:"Hot Crystal",      desc:"Tap Neon Crystal in its hot phase",      icon:"🔥",xp:80 },
  { id:"sonarping_tap",    label:"Ping Hunter",      desc:"Tap a Sonar Ping target",                icon:"📡",xp:20 },
  { id:"sonarping_cluster",label:"Cluster Signal",   desc:"Sonar Ping hits 4+ targets in its ring", icon:"📶",xp:85 },
  { id:"gravitybomb_tap",  label:"Gravity Defuser",  desc:"Tap a Gravity Bomb target",              icon:"🧲",xp:20 },
  { id:"gravitybomb_early",label:"Caught Mid-Air",   desc:"Defuse Gravity Bomb in top half of screen",icon:"🎯",xp:80 },
  { id:"speednova_tap",    label:"Nova Catcher",     desc:"Tap a Speed Nova target",                icon:"💫",xp:20 },
  { id:"speednova_max",    label:"Full Expansion",   desc:"Tap Speed Nova at maximum size",         icon:"🌟",xp:85 },
  { id:"omega_tap",        label:"Omega Seeker",     desc:"Tap an Omega Orb target",                icon:"Ω",xp:25 },
  { id:"omega_legendary",  label:"Omega Awakened",   desc:"Tap Omega Orb with an active combo 20+", icon:"🔱",xp:100 },
  { id:"thunderball_tap",  label:"Thunder Charger",  desc:"Tap a Thunder Ball target",              icon:"⚡",xp:20 },
  { id:"thunderball_chain",label:"Electrified Chain",desc:"Thunder Ball charges 3+ nearby targets",  icon:"🌩️",xp:80 },
  { id:"cosmicaura_tap",   label:"Aura Absorber",    desc:"Tap a Cosmic Aura target",               icon:"🌌",xp:20 },
  { id:"comborush_tap",    label:"Rush Rider",       desc:"Tap a Combo Rush target",                icon:"🔥",xp:20 },
  { id:"comborush_max",    label:"Rush Frenzy",      desc:"Tap Combo Rush with 30+ streak",         icon:"💥",xp:90 },
  { id:"score_500k",       label:"Half Millionaire", desc:"Score 500,000 pts in a single session",  icon:"💎",xp:500 },
  { id:"score_250k",       label:"Quarter Million",  desc:"Score 250,000 pts in a single session",  icon:"🏅",xp:300 },
  { id:"score_1m",         label:"MILLIONAIRE",      desc:"Score 1,000,000 pts in a single session",icon:"👑",xp:1000},
  { id:"twilight_tap",     label:"Twilight Hunter",  desc:"Tap a Twilight Orb target",              icon:"🌙",xp:20 },
  { id:"twilight_catch",   label:"Shadow Catch",     desc:"Tap Twilight Orb at <15% visible",       icon:"🌑",xp:90 },
  { id:"lifeorb_tap",      label:"Life Bringer",     desc:"Tap a Life Orb target",                  icon:"💚",xp:30 },
  { id:"scoreamp_tap",     label:"Amplifier",        desc:"Tap a Score Amp Orb target",             icon:"📣",xp:20 },
  { id:"scoreamp_multi",   label:"Triple Amp",       desc:"3+ targets collect bonus while amp active",icon:"📢",xp:85 },
  { id:"nightorb_tap",     label:"Dawn Seeker",      desc:"Tap a Night Orb target",                 icon:"🌅",xp:20 },
  { id:"nightorb_bright",  label:"Full Daylight",    desc:"Tap Night Orb at full brightness (95%+)", icon:"☀️",xp:80 },
  { id:"shift_tap",        label:"Phase Shifter",    desc:"Tap a Shifting Target",                  icon:"🔄",xp:20 },
  { id:"shift_legendary",  label:"Legendary Shift",  desc:"Tap Shifting Target at legendary phase", icon:"⚡",xp:90 },
  { id:"luckwheel_tap",    label:"Wheel Turner",     desc:"Tap a Lucky Wheel target",               icon:"🎡",xp:20 },
  { id:"luckwheel_max",    label:"Jackpot Wheel",    desc:"Tap Lucky Wheel at its highest number",  icon:"🎰",xp:90 },
  { id:"abyss_tap",        label:"Abyss Defuser",    desc:"Tap an Abyss Orb before it drains",     icon:"🌑",xp:25 },
  { id:"portal_tap2",      label:"Portal Starter",   desc:"Tap a Cosmic Portal target",             icon:"🌀",xp:20 },
  { id:"portal_bonus2",    label:"Portal Amplified", desc:"Collect a 5× pts target after portal",   icon:"💫",xp:85 },
  { id:"flamering_tap",    label:"Fire Dancer",      desc:"Tap a Flame Ring target",                icon:"🔥",xp:20 },
  { id:"flamering_sweet",  label:"Ring of Fire",     desc:"Tap Flame Ring in the sweet spot",       icon:"💍",xp:80 },
  { id:"iceclock_tap",     label:"Time Freezer",     desc:"Tap an Ice Clock target",                icon:"🧊",xp:20 },
  { id:"iceclock_5",       label:"Full Ice!",        desc:"Tap Ice Clock at 5 seconds remaining",   icon:"⏰",xp:85 },
  { id:"holodecoy_tap",    label:"Holo Hitter",      desc:"Tap a Holo Decoy target",                icon:"👻",xp:20 },
  { id:"holodecoy_real",   label:"Real Deal",        desc:"Tap the real Holo Decoy target",         icon:"✅",xp:60 },
  { id:"mirrorgate_tap",   label:"Gate Opener",      desc:"Tap a Mirror Gate target",               icon:"🪞",xp:20 },
  { id:"mirrorgate_echo",  label:"Echo Collector",   desc:"Collect a Mirror Gate reflected clone",  icon:"🌀",xp:75 },
  { id:"spiralbomb_tap",   label:"Spiral Defuser",   desc:"Tap a Spiral Bomb target",               icon:"🌀",xp:20 },
  { id:"spiralbomb_full",  label:"Full Spiral!",     desc:"Tap Spiral Bomb at full spiral",         icon:"💥",xp:90 },
  { id:"chainorb_tap",     label:"Chain Starter",    desc:"Tap a Chain Orb target",                 icon:"⛓️",xp:20 },
  { id:"chainorb_max",     label:"Chain Master",     desc:"Complete all 4 Chain Orb taps",          icon:"🔗",xp:90 },
  { id:"bolt_tap",         label:"Storm Catcher",    desc:"Tap a Bolt Catcher target",              icon:"⚡",xp:20 },
  { id:"bolt_fast",        label:"Lightning Fast",   desc:"Tap Bolt Catcher within 0.5 seconds",   icon:"🌩️",xp:80 },
  { id:"gemharvest_tap",   label:"Gem Harvester",    desc:"Tap a Gem Harvest target",               icon:"💎",xp:20 },
  { id:"gemharvest_rich",  label:"Gem Tycoon",       desc:"Tap Gem Harvest with 5+ gems collected", icon:"👑",xp:90 },
  { id:"prismpulse_tap",   label:"Prism Player",     desc:"Tap a Prism Pulse target",               icon:"🌈",xp:20 },
  { id:"prismpulse_match", label:"Color Matcher",    desc:"Tap Prism Pulse on the correct color",   icon:"🎯",xp:80 },
  { id:"darkmatter_tap",   label:"Dark Touch",       desc:"Tap a Dark Matter target",               icon:"⚫",xp:25 },
  { id:"darkmatter_blind", label:"Sixth Sense",      desc:"Tap Dark Matter without any reveal",     icon:"🧠",xp:95 },
  { id:"cometblast_tap",   label:"Comet Chaser",     desc:"Tap a Comet Blast target",               icon:"☄️",xp:20 },
  { id:"cometblast_all",   label:"Full Blast",       desc:"Collect all 4 Comet Blast shards",       icon:"💥",xp:80 },
  { id:"timeprison_tap",   label:"Warden",           desc:"Tap a Time Prison target",               icon:"🔒",xp:20 },
  { id:"timeprison_max",   label:"Mass Release",     desc:"Release 3 targets from Time Prison",     icon:"🔓",xp:85 },
  { id:"shadowtwin_tap",   label:"Shadow Hunter",    desc:"Tap the real Shadow Twin target",         icon:"👥",xp:25 },
  { id:"shadowtwin_lucky", label:"Lucky Guess",      desc:"Tap real Shadow Twin on first try",       icon:"🎲",xp:70 },
  { id:"photon_tap",       label:"Light Chaser",     desc:"Tap a Photon Burst target",               icon:"💡",xp:20 },
  { id:"photon_precise",   label:"Flash Master",     desc:"Tap Photon Burst while it's visible",     icon:"⚡",xp:80 },
  { id:"vault_tap",        label:"Vault Cracker",    desc:"Tap a Vault Orb target",                  icon:"🏦",xp:20 },
  { id:"vault_rich",       label:"Bank Breaker",     desc:"Claim 500+ pts from a Vault Orb",         icon:"💰",xp:90 },
  { id:"nanoswarm_tap",    label:"Swarm Commander",  desc:"Tap a Nano Swarm target",                 icon:"🦠",xp:20 },
  { id:"nanoswarm_all",    label:"Full Swarm",       desc:"Collect all 6 nano targets",              icon:"🔬",xp:85 },
  { id:"runechain_tap",    label:"Rune Reader",      desc:"Tap a Rune Chain target",                 icon:"🔮",xp:20 },
  { id:"runechain_max",    label:"Rune Master",      desc:"Complete all 3 rune chain taps in order", icon:"⚗️",xp:90 },
  { id:"plasmaweb_tap",    label:"Web Spinner",      desc:"Tap a Plasma Web target",                 icon:"🕸️",xp:20 },
  { id:"plasmaweb_dense",  label:"Web Tycoon",       desc:"Tap Plasma Web with 5+ connections",      icon:"🌐",xp:85 },
  { id:"astral_tap",       label:"Astral Catcher",   desc:"Tap an Astral Orb target",                icon:"✨",xp:20 },
  { id:"astral_instant",   label:"Instant Catch",    desc:"Tap Astral Orb within 0.3s of teleport",  icon:"⚡",xp:85 },
  { id:"crystal_clock_tap",label:"Crystal Watcher",  desc:"Tap a Crystal Clock target",              icon:"💎",xp:20 },
  { id:"crystal_clock_max",label:"Full Crystal",     desc:"Tap Crystal Clock at all 8 segments",     icon:"👑",xp:90 },
  { id:"sunflower_tap",    label:"Sunflower Picker", desc:"Tap a Sunflower Orb target",              icon:"🌻",xp:20 },
  { id:"sunflower_full",   label:"Full Bloom",       desc:"Tap Sunflower Orb with all 12 petals",   icon:"🌞",xp:90 },
  { id:"toxic_tap",        label:"Hazmat Hero",      desc:"Tap a Toxic Cloud and clear it",         icon:"☣️",xp:25 },
  { id:"toxic_zone",       label:"Danger Zone",      desc:"Have 3 taps penalized by Toxic Cloud",   icon:"⚠️",xp:60 },
  { id:"laserprism_tap",   label:"Prism Catcher",    desc:"Tap a Laser Prism target",               icon:"🔆",xp:20 },
  { id:"laserprism_chain", label:"Laser Chain",      desc:"Tap a laser-charged target for bonus",   icon:"💥",xp:75 },
  { id:"echostar_tap",     label:"Star Echoer",      desc:"Tap an Echo Star target",                icon:"⭐",xp:20 },
  { id:"echostar_all",     label:"Full Echo",        desc:"Collect Echo Star + all 4 echoes",       icon:"🌟",xp:85 },
  { id:"meteor_shower_tap",label:"Meteor Catcher",   desc:"Tap a Meteor Shower core",               icon:"☄️",xp:20 },
  { id:"meteor_shower_all",label:"Full Meteor",      desc:"Collect all 6 meteor shards",            icon:"🌠",xp:85 },
  { id:"golden_gate_tap",  label:"Golden Second",    desc:"Tap the Golden Gate target",             icon:"🏆",xp:150},
  { id:"pulse_ring_tap",   label:"Ring Rider",       desc:"Tap a Pulse Ring target",                icon:"💫",xp:20 },
  { id:"pulse_ring_sweet", label:"Perfect Ring",     desc:"Tap Pulse Ring in the sweet band",       icon:"🎯",xp:85 },
  { id:"clockwork_tap",    label:"Clockwork Tap",    desc:"Tap a Clockwork Bomb target",            icon:"⚙️",xp:20 },
  { id:"clockwork_max",    label:"Gear Master",      desc:"Tap Clockwork Bomb at gear 4",           icon:"💣",xp:90 },
  { id:"nebula_vortex_tap",label:"Nebula Explorer",  desc:"Tap a Nebula Vortex target",             icon:"🌌",xp:20 },
  { id:"nebula_vortex_big",label:"Cosmic Feast",     desc:"Tap Nebula Vortex at max size",          icon:"🪐",xp:90 },
  { id:"frost_hex_tap",    label:"Frost Weaver",     desc:"Tap a Frost Hex target",                 icon:"❄️",xp:20 },
  { id:"frost_hex_freeze", label:"Cold Front",       desc:"Freeze 3 targets with Frost Hex",        icon:"🧊",xp:80 },
  { id:"lightning_orb_tap",label:"Storm Rider",      desc:"Tap a Lightning Orb target",             icon:"⚡",xp:20 },
  { id:"lightning_orb_chain",label:"Chain Reaction", desc:"Chain Lightning Orb to 3+ targets",      icon:"🌩️",xp:85 },
  { id:"starburst_tap",    label:"Star Popper",      desc:"Tap a Star Burst target",                icon:"💫",xp:20 },
  { id:"starburst_full",   label:"Supernova Pop",    desc:"Tap Star Burst at full expansion",       icon:"🌟",xp:90 },
  { id:"crystalspire_tap", label:"Spire Smasher",    desc:"Tap a Crystal Spire target",             icon:"🔷",xp:20 },
  { id:"crystalspire_all", label:"Full Shatter",     desc:"Collect all Crystal Spire shards",       icon:"💎",xp:85 },
  { id:"phasegate_tap",    label:"Phase Walker",     desc:"Tap a Phase Gate while solid",           icon:"🌀",xp:20 },
  { id:"phasegate_clutch", label:"Phase Clutch",     desc:"Tap Phase Gate in last 0.5s of solid",   icon:"⚡",xp:80 },
  { id:"voidpulsar_tap",   label:"Void Catcher",     desc:"Tap a Void Pulsar target",               icon:"🌌",xp:20 },
  { id:"voidpulsar_early", label:"Void Sniper",      desc:"Tap Void Pulsar before it shrinks 30%",  icon:"🎯",xp:80 },
  { id:"novacannon_tap",   label:"Cannon Operator",  desc:"Tap a Nova Cannon target",               icon:"💥",xp:20 },
  { id:"novacannon_mega",  label:"Mega Blast",       desc:"Nova Cannon clears 4+ targets",          icon:"🔥",xp:90 },
  { id:"hexgrid_tap",      label:"Hex Starter",      desc:"Tap the first hex of a Hex Grid",        icon:"🔷",xp:15 },
  { id:"hexgrid_full",     label:"Hex Master",       desc:"Complete all 6 hexes in a Hex Grid",     icon:"👑",xp:95 },
  { id:"qtunnel_tap",      label:"Tunnel Vision",    desc:"Tap a Quantum Tunnel endpoint",           icon:"🔵",xp:20 },
  { id:"qtunnel_both",     label:"Both Ends",        desc:"Tap both A and B Quantum Tunnels",        icon:"🌀",xp:80 },
  { id:"inferno_tap",      label:"Fire Chaser",      desc:"Tap an Inferno Orb target",              icon:"🔥",xp:20 },
  { id:"inferno_max",      label:"Inferno King",     desc:"Tap Inferno Orb at max heat (8 stacks)", icon:"👑",xp:95 },
  { id:"chrono_tap",       label:"Time Bender",      desc:"Tap a Chrono Sphere target",             icon:"⏱",xp:20 },
  { id:"chrono_surge",     label:"Time Warden",      desc:"Chrono Sphere slows 3+ targets",         icon:"⌛",xp:85 },
  { id:"prism_lens_tap",   label:"Lens Catcher",     desc:"Tap a Prismatic Lens target",            icon:"🔮",xp:20 },
  { id:"prism_lens_focus", label:"Perfect Focus",    desc:"Tap Prismatic Lens on the focus color",  icon:"🎯",xp:85 },
  { id:"shadow_mirror_tap",label:"Mirror Gazer",     desc:"Tap a Shadow Mirror target",             icon:"🪞",xp:20 },
  { id:"shadow_mirror_hit",label:"Mirror Master",    desc:"Tap Shadow Mirror while active",         icon:"✨",xp:80 },
  { id:"plasma_comet_tap", label:"Comet Catcher",    desc:"Tap a Plasma Comet target",              icon:"☄️",xp:20 },
  { id:"plasma_comet_apex",label:"Apex Striker",     desc:"Tap Plasma Comet at its apex",           icon:"⭐",xp:90 },
  { id:"abyssal_tap",      label:"Void Tamer",       desc:"Tap an Abyssal Gate target",             icon:"⚫",xp:25 },
  { id:"abyssal_drain",    label:"Abyss Feeder",     desc:"Tap Abyssal Gate after 5+ drains",       icon:"🌑",xp:90 },
  { id:"warpball_tap",     label:"Warp Catcher",     desc:"Tap a Warp Ball target",                 icon:"🌀",xp:20 },
  { id:"warpball_instant", label:"Instant Catch",    desc:"Tap Warp Ball within 0.3s of warp",      icon:"🌟",xp:85 },
  { id:"crystalspike_tap", label:"Spike Defuser",    desc:"Tap Crystal Spike from the safe zone",   icon:"💎",xp:25 },
  { id:"crystalspike_ow",  label:"Ouch!",            desc:"Tap Crystal Spike from the danger zone", icon:"😱",xp:15 },
  { id:"energycore_green", label:"Green Light",      desc:"Tap Energy Core in green phase",         icon:"✅",xp:20 },
  { id:"energycore_red",   label:"Red Alert",        desc:"Tap Energy Core in red phase (risky!)",  icon:"☢️",xp:30 },
  { id:"arcane_tap",       label:"Rune Seeker",      desc:"Tap an Arcane Circle target",            icon:"🔮",xp:20 },
  { id:"arcane_match",     label:"Rune Master",      desc:"Tap Arcane Circle on the correct rune",  icon:"⚡",xp:85 },
  { id:"stellar_tap",      label:"Star Drifter",     desc:"Tap a Stellar Drift target",             icon:"🌠",xp:20 },
  { id:"stellar_far",      label:"Far Traveler",     desc:"Tap Stellar Drift after 200px drift",    icon:"⭐",xp:80 },
  { id:"bubblewave_tap",   label:"Bubble Popper",    desc:"Tap a Bubble Wave before it bursts",     icon:"🫧",xp:20 },
  { id:"bubblewave_clutch",label:"Last Second Pop",  desc:"Pop Bubble Wave at >80% size",           icon:"💥",xp:85 },
  { id:"ironshield_tap",   label:"Shield Breaker",   desc:"Hit an Iron Shield target once",         icon:"🛡️",xp:15 },
  { id:"ironshield_break", label:"Iron Will",        desc:"Fully break an Iron Shield",             icon:"💪",xp:90 },
  { id:"timeecho_tap",     label:"Echo Hunter",      desc:"Tap a Time Echo target",                 icon:"👻",xp:20 },
  { id:"timeecho_perfect", label:"Ghost Catcher",    desc:"Catch 3 Time Echo targets in one game",  icon:"🌀",xp:85 },
  { id:"combo_shield_use", label:"Shield Up",        desc:"Activate the Combo Shield power-up",     icon:"🔒",xp:30 },
  { id:"perfect_aim_use",  label:"Dead Eye",         desc:"Activate the Perfect Aim power-up",      icon:"🎯",xp:30 },
  { id:"gravity2_tap",     label:"Black Hole Tamer", desc:"Tap a Gravity Orb Mk2 target",           icon:"🌑",xp:25 },
  { id:"gravity2_max",     label:"Event Horizon",    desc:"Tap Gravity Orb Mk2 with 5+ pulled",     icon:"⚫",xp:90 },
  { id:"pixelswarm_tap",   label:"Pixel Hunter",     desc:"Tap the Pixel Swarm core",               icon:"📡",xp:20 },
  { id:"pixelswarm_full",  label:"Full Spectrum",    desc:"Collect all 8 pixel units",              icon:"🎨",xp:90 },
  { id:"chain_burst_use",  label:"Chain Reactor",   desc:"Activate the Chain Burst power-up",      icon:"🔥",xp:30 },
  { id:"magnet_burst_use", label:"Mass Collector",  desc:"Activate the Magnet Burst power-up",     icon:"🧲",xp:30 },
  { id:"chrono_tap",       label:"Time Keeper",     desc:"Tap a Chrono Crystal target",            icon:"🔮",xp:25 },
  { id:"chrono_bonus",     label:"Double Click",    desc:"Score the bonus tap on a Chrono Crystal",icon:"⏳",xp:90 },
  { id:"voidseed_tap",     label:"Seed Planter",    desc:"Tap a Void Seed target",                 icon:"🌱",xp:20 },
  { id:"voidseed_max",     label:"Full Bloom",      desc:"Tap a Void Seed at 90%+ growth",         icon:"🌸",xp:90 },
  { id:"score_pulse_use",  label:"Pulse Master",    desc:"Activate the Score Pulse power-up",      icon:"💫",xp:30 },
  { id:"life_shield_use",  label:"Guardian",        desc:"Activate the Life Shield power-up",      icon:"🛡️",xp:30 },
  { id:"prism_shard_tap",  label:"Splitter",        desc:"Tap a Prism Shard target",               icon:"💎",xp:20 },
  { id:"prism_shard_full", label:"Crystal Clear",   desc:"Collect all 3 mini shards from one Prism",icon:"✨",xp:85 },
  { id:"temporal_tap",     label:"Time Bender",     desc:"Tap a Temporal Orb",                     icon:"🌀",xp:25 },
  { id:"temporal_rewind",  label:"Rewind!",         desc:"Tap a Temporal Orb right after it rewinds",icon:"⏪",xp:90 },
  { id:"mult_surge_use",   label:"Surge Activated", desc:"Activate the Multiplier Surge power-up",  icon:"⚡×",xp:30 },
  { id:"bounce_back_use",  label:"Bouncer",         desc:"Activate the Bounce Back power-up",       icon:"🔄",xp:30 },
  { id:"lchain_tap",       label:"Chain Reaction",  desc:"Tap a Lightning Chain target",            icon:"⚡",xp:25 },
  { id:"lchain_max",       label:"Chain Max",       desc:"Chain-kill 3+ targets with Lightning Chain",icon:"🔗",xp:80 },
  { id:"magmaseed_tap",    label:"Lava Tamer",      desc:"Tap a Magma Seed target",                 icon:"🌋",xp:20 },
  { id:"magmaseed_full",   label:"Volcano",         desc:"Collect Magma Seed core and all 5 drops", icon:"🔥",xp:90 },
  { id:"fever_extend_use", label:"Fever Fiend",     desc:"Activate the Fever Extend power-up",      icon:"🌡️",xp:30 },
  { id:"coin_rush_use",    label:"Coin Collector",  desc:"Activate the Coin Rush power-up",         icon:"🪙+",xp:30 },
  { id:"gemini_tap",       label:"Twin Tap",        desc:"Tap one of a Gemini Pair",                icon:"♊",xp:20 },
  { id:"gemini_combo",     label:"Perfect Pair",    desc:"Tap both Gemini targets within 3 seconds",icon:"💫",xp:90 },
  { id:"aurora_ring_tap",  label:"Ring Master",     desc:"Tap an Aurora Ring",                      icon:"🌌",xp:20 },
  { id:"aurora_ring_max",  label:"Aurora Burst",    desc:"Burst an Aurora Ring with 5+ targets inside",icon:"💥",xp:90 },
  { id:"gravity_well_use", label:"Gravity Lord",    desc:"Activate the Gravity Well power-up",      icon:"🌀",xp:30 },
  { id:"clone_army_use",   label:"Clone Maker",    desc:"Activate the Clone Army power-up",         icon:"👥",xp:30 },
  { id:"neonpulse_tap",    label:"Neon Blink",     desc:"Tap a Neon Pulse target",                  icon:"💡",xp:20 },
  { id:"neonpulse_bright", label:"Flash Tap",      desc:"Tap a Neon Pulse at peak brightness for 3×",icon:"✨",xp:80 },
  { id:"mirrortwin_tap",   label:"Twin Mirror",    desc:"Tap a Mirror Twin target",                 icon:"🪞",xp:20 },
  { id:"mirrortwin_both",  label:"Double Vision",  desc:"Tap both Mirror Twins within 4 seconds",   icon:"👁️",xp:90 },
  { id:"rush_mode_use",    label:"Rush Hour",       desc:"Activate the Rush Mode power-up",          icon:"⚡!",xp:30 },
  { id:"invincible_use",   label:"God Mode",        desc:"Activate the Invincible power-up",         icon:"⚡💫",xp:30 },
  { id:"starcluster_tap",  label:"Star Burst",      desc:"Tap a Star Cluster",                       icon:"✨",xp:20 },
  { id:"starcluster_full", label:"Constellation",   desc:"Collect all 5 sparkles from a Star Cluster",icon:"🌌",xp:85 },
  { id:"voidcrystal_tap",  label:"Void Catcher",    desc:"Tap a Void Crystal",                       icon:"💜",xp:20 },
  { id:"voidcrystal_tiny", label:"Tiny Treasure",   desc:"Tap a Void Crystal at under 30% size",     icon:"🔮",xp:90 },
  { id:"healer_use",       label:"First Aid",        desc:"Activate the Healer power-up",            icon:"❤️+",xp:30 },
  { id:"score_guard_use",  label:"Iron Score",       desc:"Activate the Score Guard power-up",       icon:"🔰+",xp:30 },
  { id:"energy_orb_tap",   label:"Energy Tap",       desc:"Tap an Energy Orb target",                icon:"⚡",xp:20 },
  { id:"energy_orb_surge", label:"Full Charge",      desc:"Tap an Energy Orb at 80%+ charge",        icon:"🔋",xp:90 },
  { id:"comettrail_tap",   label:"Comet Chaser",     desc:"Tap a Comet Trail target",                icon:"☄️",xp:20 },
  { id:"comettrail_max",   label:"Tail Blazer",      desc:"Tap a Comet Trail with 4+ trail hits",    icon:"✨",xp:85 },
  { id:"swipe_boost_use",  label:"Speed Freak",      desc:"Activate the Swipe Boost power-up",       icon:"💨",xp:30 },
  { id:"lucky_coin_use",   label:"Lucky Break",      desc:"Activate the Lucky Coin power-up",        icon:"🍀",xp:30 },
  { id:"runeburst_tap",    label:"Rune Mage",        desc:"Tap a Rune Burst target",                 icon:"🔮",xp:25 },
  { id:"runeburst_max",    label:"Five Runes",       desc:"Hit 5 targets with one Rune Burst",       icon:"⭐",xp:85 },
  { id:"golem_tap",        label:"Golem Cracker",    desc:"Land the first hit on a Crystal Golem",   icon:"💎",xp:20 },
  { id:"golem_destroy",    label:"Golem Slayer",     desc:"Destroy a Crystal Golem (3 hits)",        icon:"🏆",xp:90 },
  { id:"echo_shot_use",    label:"Echo Cannon",      desc:"Activate the Echo Shot power-up",         icon:"📡",xp:30 },
  { id:"burst_shield_use", label:"Bomb Proof",       desc:"Activate the Burst Shield power-up",      icon:"💣🛡",xp:30 },
  { id:"freeze_orb_tap",   label:"Cryo Strike",      desc:"Tap a Freeze Orb target",                 icon:"🧊",xp:25 },
  { id:"freeze_orb_bonus", label:"Frozen Loot",      desc:"Score 3 normals while a Freeze Orb is active",icon:"❄️",xp:85 },
  { id:"twincomet_tap",    label:"Double Comet",     desc:"Tap a Twin Comet target",                 icon:"☄️☄️",xp:25 },
  { id:"twincomet_clean",  label:"Twin Kill",        desc:"Kill both Twin Comets with a single tap", icon:"💫",xp:85 },
  { id:"magnet_orbit_use", label:"Orbital Pull",     desc:"Activate the Magnet Orbit power-up",      icon:"🌀🧲",xp:30 },
  { id:"score_echo_use",   label:"Echo Chamber",     desc:"Activate the Score Echo power-up",        icon:"📢",xp:30 },
  { id:"score_echo_proc",  label:"Echo Strike",      desc:"Trigger a Score Echo bonus tap",          icon:"💫📢",xp:60 },
  { id:"plasmavortex_tap", label:"Vortex Surfer",    desc:"Tap a Plasma Vortex target",              icon:"🌀",xp:25 },
  { id:"plasmavortex_peak",label:"Perfect Vortex",   desc:"Tap Plasma Vortex at peak alignment",     icon:"🌀✨",xp:85 },
  { id:"starbomb_tap",     label:"Star Burst",       desc:"Tap a Star Bomb target",                  icon:"💥⭐",xp:40 },
  { id:"phantom_burst_use",label:"Ghost Striker",    desc:"Activate the Phantom Burst power-up",     icon:"👻💥",xp:30 },
  { id:"streak_storm_use", label:"Storm Chaser",     desc:"Activate the Streak Storm power-up",      icon:"⛈️",xp:30 },
  { id:"streak_storm_bonus",label:"Lightning Streak",desc:"Score a Streak Storm bonus at 10× streak",icon:"⚡⛈️",xp:60 },
  { id:"glowshard_tap",    label:"Shard Splitter",   desc:"Tap a Glow Shard target",                 icon:"✨🔀",xp:25 },
  { id:"glowshard_full",   label:"Full Spectrum",    desc:"Collect all 3 Glow Shard fragments",      icon:"✨✨✨",xp:80 },
  { id:"quantumleech_tap", label:"Energy Vampire",   desc:"Tap a Quantum Leech target",              icon:"🧿",xp:40 },
  { id:"time_bubble_use",  label:"Time Bubble",      desc:"Activate the Time Bubble power-up",       icon:"⏱️💫",xp:30 },
  { id:"crit_boost_use",   label:"Critical Hit",     desc:"Activate the Crit Boost power-up",        icon:"💥×3",xp:30 },
  { id:"crit_boost_proc",  label:"Triple Strike",    desc:"Score a Crit Boost 3× critical tap",      icon:"⚡💥",xp:60 },
  { id:"novastar_tap",     label:"Nova Star",        desc:"Tap a Nova Star target",                  icon:"🌟💥",xp:35 },
  { id:"novastar_sweep",   label:"Stellar Sweep",    desc:"Nova Star scores 3+ nearby normals",      icon:"🌟🌟🌟",xp:80 },
  { id:"icephoenix_tap",   label:"Cryo Phoenix",     desc:"Tap an Ice Phoenix target",               icon:"🧊🐦",xp:35 },
  { id:"gold_rush_use",    label:"Gold Rush!",       desc:"Activate the Gold Rush power-up",         icon:"🥇",xp:30 },
  { id:"mirror_maze_use",  label:"Maze Runner",      desc:"Activate the Mirror Maze power-up",       icon:"🪞🌀",xp:30 },
  { id:"spiralorb_tap",    label:"Spiral Catch",     desc:"Tap a Spiral Orb target",                 icon:"🌀⭐",xp:30 },
  { id:"spiralorb_far",    label:"Far Spiral",       desc:"Tap Spiral Orb at 75%+ travel distance",  icon:"🌀💫",xp:75 },
  { id:"darkpulse_tap",    label:"Dark Gamble",      desc:"Tap a Dark Pulse target",                 icon:"🌑",xp:40 },
  { id:"darkpulse_bonus",  label:"Shadow Bonus",     desc:"Score both bonus targets from Dark Pulse", icon:"🌑✨",xp:85 },
  { id:"blast_wave_use",   label:"Blast Wave",       desc:"Activate the Blast Wave power-up",        icon:"💥🌊",xp:35 },
  { id:"blast_wave_sweep", label:"Total Annihilation",desc:"Blast Wave clears 5+ targets at once",   icon:"💥💥💥",xp:90 },
  { id:"tempo_sync_use",   label:"In the Zone",      desc:"Activate the Tempo Sync power-up",        icon:"🎵⚡",xp:30 },
  { id:"lunareclipse_tap", label:"Moon Tap",         desc:"Tap a Lunar Eclipse target",              icon:"🌑🌕",xp:30 },
  { id:"lunareclipse_max", label:"Full Moon!",       desc:"Tap Lunar Eclipse at maximum size",       icon:"🌕💥",xp:80 },
  { id:"chainwall_tap",    label:"Wall Breaker",     desc:"Tap a Chain Wall target",                 icon:"🧱",xp:30 },
  { id:"life_leech_use",   label:"Life Drain",       desc:"Activate the Life Leech power-up",        icon:"❤️🩸",xp:30 },
  { id:"life_leech_proc",  label:"Leech Heal",       desc:"Restore a life via Life Leech",           icon:"❤️+",xp:55 },
  { id:"score_rain_use",   label:"Score Shower",     desc:"Activate the Score Rain power-up",        icon:"🌧️⭐",xp:30 },
  { id:"bounceweb_tap",    label:"Web Weaver",       desc:"Tap a Bounce Web target",                 icon:"🕸️",xp:30 },
  { id:"bounceweb_snare",  label:"Triple Snare",     desc:"Bounce Web snares 3+ moving targets",     icon:"🕸️×3",xp:80 },
  { id:"meteorshower_tap", label:"Meteor Storm",     desc:"Tap a Meteor Shower target",              icon:"☄️🌧️",xp:35 },
  { id:"chain_magnet_use", label:"Magnetic Chain",   desc:"Activate the Chain Magnet power-up",      icon:"🧲⛓️",xp:30 },
  { id:"chain_magnet_pull",label:"Auto-Pull",        desc:"Chain Magnet auto-pulls 5 targets",       icon:"🧲×5",xp:60 },
  { id:"score_surge_use",  label:"Score Surge",      desc:"Activate the Score Surge power-up",       icon:"💹",xp:30 },
  { id:"score_surge_peak", label:"Surge Peak",       desc:"Score Surge multiplier hits ×8",          icon:"💹×8",xp:80 },
  { id:"prismatic_tap",    label:"Prism Tap",        desc:"Tap a Prismatic Orb target",              icon:"🔮",xp:30 },
  { id:"prismatic_bonus",  label:"True Colors",      desc:"Tap a Prismatic Orb on the matching color",icon:"🔮✨",xp:70 },
  { id:"voidtether_tap",   label:"Tether Link",      desc:"Tap a Void Tether target",                icon:"🌑⛓️",xp:35 },
  { id:"orbit_shield_use", label:"Orbital Guard",    desc:"Activate the Orbit Shield power-up",      icon:"🛸🛡",xp:30 },
  { id:"orbit_shield_abs", label:"Bomb Blocked",     desc:"Orbit Shield absorbs a bomb",             icon:"🛸💥",xp:60 },
  { id:"echo_wave_use",    label:"Echo Wave",        desc:"Activate the Echo Wave power-up",         icon:"〰️💥",xp:30 },
  { id:"echo_wave_multi",  label:"Wave Feast",       desc:"Echo Wave scores 3+ targets at once",     icon:"〰️×3",xp:70 },
  { id:"crystalpulsar_tap",label:"Pulsar Hit",       desc:"Tap a Crystal Pulsar target",             icon:"💎⚡",xp:30 },
  { id:"crystalpulsar_peak",label:"Peak Power",      desc:"Tap Crystal Pulsar at peak pulse",        icon:"💎✨",xp:75 },
  { id:"shadowecho_tap",   label:"Shadow Echo",      desc:"Tap a Shadow Echo target",                icon:"🌒💫",xp:35 },
  { id:"combo_burst_use",  label:"Burst Combo",      desc:"Activate the Combo Burst power-up",       icon:"💥⛓️",xp:30 },
  { id:"combo_burst_big",  label:"Big Burst",        desc:"Combo Burst with 15+ streak",             icon:"💥×15",xp:80 },
  { id:"phase_lock_use",   label:"Phase Lock",       desc:"Activate the Phase Lock power-up",        icon:"🔒⏸",xp:30 },
  { id:"phase_lock_haul",  label:"Frozen Haul",      desc:"Score 500+ pts during Phase Lock",        icon:"🔒💰",xp:70 },
  { id:"fluxring_tap",     label:"Ring Tap",         desc:"Tap a Flux Ring target",                  icon:"⭕",xp:30 },
  { id:"fluxring_early",   label:"Ring Rush",        desc:"Tap Flux Ring before it thins out",       icon:"⭕⚡",xp:65 },
  { id:"quantumflare_tap", label:"Quantum Flare",    desc:"Tap a Quantum Flare target",              icon:"🌟🔮",xp:35 },
  { id:"score_leech_use",  label:"Score Leech",      desc:"Activate the Score Leech power-up",       icon:"🔋💰",xp:30 },
  { id:"score_leech_gain", label:"Leech Harvest",    desc:"Gain 150+ pts from Score Leech expiries", icon:"🔋×5",xp:65 },
  { id:"split_shot_use",   label:"Split Shot",       desc:"Activate the Split Shot power-up",        icon:"🎯🎯",xp:30 },
  { id:"split_shot_double",label:"Double Tap",       desc:"Split Shot auto-taps 3+ targets",         icon:"🎯×3",xp:70 },
  { id:"nebulacrystal_tap",label:"Nebula Break",     desc:"Crack a Nebula Crystal target",           icon:"💜💎",xp:30 },
  { id:"nebulacrystal_3",  label:"Crystal Core",     desc:"Crack Nebula Crystal 3 times",            icon:"💜×3",xp:85 },
  { id:"timeshard_tap",    label:"Time Shard",       desc:"Tap a Time Shard target",                 icon:"⏳💫",xp:30 },
  { id:"fever_storm_use",  label:"Fever Storm",      desc:"Activate the Fever Storm power-up",       icon:"🌪️🌡️",xp:35 },
  { id:"rarity_boost_use", label:"Rarity Boost",     desc:"Activate the Rarity Boost power-up",      icon:"✨⬆️",xp:35 },
  { id:"rarity_boost_leg", label:"All Legendary",    desc:"Rarity Boost: tap 3 legendary targets",   icon:"✨×3",xp:80 },
  { id:"gravitynode_tap",  label:"Gravity Node",     desc:"Tap a Gravity Node target",               icon:"🌑🌀",xp:35 },
  { id:"gravitynode_pull", label:"Node Feast",       desc:"Gravity Node releases 4+ pulled targets", icon:"🌑×4",xp:85 },
  { id:"lightningcage_tap",label:"Cage Break",       desc:"Tap a Lightning Cage target",             icon:"⚡🔒",xp:35 },
  { id:"lightningcage_all",label:"Full Cage",        desc:"Lightning Cage traps 5+ targets",         icon:"⚡×5",xp:85 },
  { id:"twin_fever_use",   label:"Twin Fever",       desc:"Activate the Twin Fever power-up",        icon:"🌡️×2",xp:35 },
  { id:"reflect_wave_use", label:"Reflect Wave",     desc:"Activate the Reflect Wave power-up",      icon:"💥🔄",xp:30 },
  { id:"reflect_wave_hit", label:"Bomb Reflect",     desc:"Reflect Wave scores from bomb expiry",    icon:"💥⭐",xp:60 },
  { id:"plasmacomet_tap",  label:"Plasma Comet",     desc:"Tap a Plasma Comet target",               icon:"🔥☄️",xp:35 },
  { id:"plasmacomet_early",label:"Comet Rush",       desc:"Tap Plasma Comet before 30% crossing",    icon:"🔥☄️⚡",xp:75 },
  { id:"crystalweb_tap",   label:"Crystal Web",      desc:"Tap a Crystal Web target",                icon:"🔷🕸",xp:30 },
  { id:"crystalweb_chain", label:"Web Chain",        desc:"Crystal Web 3-tap chain completed",       icon:"🔷×3",xp:80 },
  { id:"nova_boost_use",   label:"Nova Boost",       desc:"Activate the Nova Boost power-up",        icon:"💥🌟",xp:30 },
  { id:"nova_boost_proc",  label:"Nova Strike",      desc:"Trigger a Nova Boost shockwave",          icon:"💥⭐",xp:55 },
  { id:"streak_lock_use",  label:"Streak Lock",      desc:"Activate the Streak Lock power-up",       icon:"🔐⛓️",xp:35 },
  { id:"streak_lock_save", label:"Streak Saved",     desc:"Streak Lock prevents a streak reset",     icon:"🔐✅",xp:60 },
  { id:"arcpulse_tap",     label:"Arc Tap",          desc:"Tap an Arc Pulse target",                 icon:"🌀⚡",xp:30 },
  { id:"arcpulse_arc",     label:"Arc Perfect",      desc:"Tap Arc Pulse in the arc zone for 3× pts",icon:"🌀💥",xp:75 },
  { id:"voidbloom_tap",    label:"Void Bloom",       desc:"Tap a Void Bloom target",                 icon:"🌸🌀",xp:30 },
  { id:"voidbloom_full",   label:"Full Bloom",       desc:"Collect all 4 Void Bloom petals",         icon:"🌸×4",xp:80 },
  { id:"magnet_storm_use", label:"Magnet Storm",     desc:"Activate the Magnet Storm power-up",      icon:"🌪️🧲",xp:30 },
  { id:"magnet_storm_pull",label:"Storm Pull",       desc:"Magnet Storm pulls 5+ targets at once",   icon:"🌪️💥",xp:65 },
  { id:"score_freeze_use", label:"Score Freeze",     desc:"Activate the Score Freeze power-up",      icon:"❄️🔒",xp:35 },
  { id:"score_freeze_save",label:"Freeze Saved",     desc:"Score Freeze prevents a score deduction", icon:"❄️✅",xp:60 },
  { id:"stellarorb_tap",   label:"Stellar Orb",      desc:"Tap a Stellar Orb target",                icon:"⭐🌀",xp:30 },
  { id:"stellarorb_max",   label:"Stellar Max",      desc:"Tap Stellar Orb at maximum size",         icon:"⭐💥",xp:75 },
  { id:"runegate_tap",     label:"Rune Gate",        desc:"Tap a Rune Gate target",                  icon:"🔮⚡",xp:30 },
  { id:"runegate_chain",   label:"Rune Chain",       desc:"Tap both Rune Gate orbs in sequence",     icon:"🔮×2",xp:80 },
  { id:"chain_fever_use",  label:"Chain Fever",      desc:"Activate the Chain Fever power-up",       icon:"⛓️🌡️",xp:30 },
  { id:"chain_fever_proc", label:"Fever Chained",    desc:"Chain Fever triggers instant fever",      icon:"⛓️💥",xp:65 },
  { id:"life_bomb_use",    label:"Life Bomb",        desc:"Activate the Life Bomb power-up",         icon:"💣❤️",xp:35 },
  { id:"life_bomb_proc",   label:"Bomb to Life",     desc:"Life Bomb converts a bomb into a life",   icon:"💣→❤️",xp:70 },
  { id:"novacrystal_tap",  label:"Nova Crystal",     desc:"Tap a Nova Crystal target",               icon:"💎💥",xp:30 },
  { id:"novacrystal_full", label:"Crystal Full",     desc:"Tap Nova Crystal at full charge (stage 3)",icon:"💎🔥",xp:75 },
  { id:"phaseshift_tap",   label:"Phase Shift",      desc:"Tap a Phase Shift target",                icon:"👁️⚡",xp:30 },
  { id:"phaseshift_solid", label:"Phase Solid",      desc:"Tap Phase Shift exactly in solid phase",  icon:"👁️✅",xp:60 },
  { id:"turbo_tap_use",    label:"Turbo Tap",        desc:"Activate the Turbo Tap power-up",         icon:"⚡⚡⚡",xp:30 },
  { id:"turbo_tap_triple", label:"Turbo Triple",     desc:"Score all 3 Turbo Tap charges",           icon:"⚡×3",xp:65 },
  { id:"coin_magnet_use",  label:"Coin Magnet",      desc:"Activate the Coin Magnet power-up",       icon:"🧲🪙",xp:30 },
  { id:"coin_magnet_rich", label:"Coin Haul",        desc:"Coin Magnet collects 20+ coins",          icon:"🧲💰",xp:60 },
  { id:"solarring_tap",    label:"Solar Ring",       desc:"Tap a Solar Ring target",                 icon:"🌞💍",xp:30 },
  { id:"solarring_edge",   label:"Ring Perfect",     desc:"Solar Ring edge-tap for max score",       icon:"🌞🎯",xp:75 },
  { id:"chronovortex_tap", label:"Chrono Vortex",    desc:"Tap a Chrono Vortex target",              icon:"⏳🌀",xp:30 },
  { id:"chronovortex_slow",label:"Vortex Slow",      desc:"Chrono Vortex slows 3+ targets",          icon:"⏳×3",xp:65 },
  { id:"blast_radius_use", label:"Area Blast",       desc:"Activate the Blast Radius power-up",      icon:"💥🌐",xp:30 },
  { id:"blast_radius_aoe", label:"Multi-Hit",        desc:"Blast Radius hits 2+ extra targets",      icon:"💥×2",xp:70 },
  { id:"score_accel_use",  label:"Score Accelerator",desc:"Activate the Score Accelerator power-up", icon:"📈⚡",xp:30 },
  { id:"score_accel_peak", label:"Full Throttle",    desc:"Score Accelerator reaches 8-tap max mult", icon:"📈🔥",xp:75 },
  { id:"prismnova_tap",    label:"Prism Nova",       desc:"Tap a Prism Nova target",                 icon:"🌈💥",xp:30 },
  { id:"prismnova_shards", label:"Shard Jackpot",    desc:"Tap all 3 Prism Nova shards for jackpot", icon:"🌈×3",xp:80 },
  { id:"echoblast_tap",    label:"Echo Blast",       desc:"Tap an Echo Blast target",                icon:"🔊💥",xp:30 },
  { id:"echoblast_ring",   label:"Ring Rider",       desc:"Score inside an Echo Blast ring",         icon:"🔊🎯",xp:65 },
  { id:"life_chain_use",   label:"Life Chain",       desc:"Activate the Life Chain power-up",         icon:"❤️⛓️",xp:30 },
  { id:"life_chain_heal",  label:"Chain Heal",       desc:"Life Chain restores all 3 lives",          icon:"❤️×3",xp:80 },
  { id:"score_mirror_use", label:"Score Mirror",     desc:"Activate the Score Mirror power-up",       icon:"🪞💯",xp:30 },
  { id:"score_mirror_big", label:"Mirror Payout",    desc:"Score Mirror pays out 500+ bonus",         icon:"🪞💰",xp:75 },
  { id:"vortexstar_61_tap",label:"Vortex Star",      desc:"Tap a Vortex Star target",                icon:"⭐🌀",xp:30 },
  { id:"vortexstar_61_out",label:"Outer Ring",       desc:"Hit Vortex Star outer zone for max score", icon:"⭐🎯",xp:70 },
  { id:"qdrift_tap",       label:"Quantum Drift",    desc:"Tap a Quantum Drift target",               icon:"⚛️💨",xp:30 },
  { id:"qdrift_freeze",    label:"Drift Frozen",     desc:"Tap Quantum Drift in frozen window",       icon:"⚛️❄️",xp:65 },
  { id:"fever_lock_use",   label:"Fever Lock",       desc:"Activate the Fever Lock power-up",         icon:"🔒🌡️",xp:30 },
  { id:"fever_lock_save",  label:"Fever Kept",       desc:"Fever Lock prevents fever from expiring",  icon:"🔒✅",xp:70 },
  { id:"streak_surge_use", label:"Streak Surge",     desc:"Activate the Streak Surge power-up",       icon:"🔥⬆️",xp:30 },
  { id:"streak_surge_big", label:"Surge Peak",       desc:"Streak Surge fires at 20+ streak",         icon:"🔥×4",xp:75 },
  { id:"plasmaburst_tap",  label:"Plasma Burst",     desc:"Tap a Plasma Burst target",                icon:"💥⚡",xp:30 },
  { id:"plasmaburst_all",  label:"Full Burst",       desc:"All 4 plasma bolts score from one burst",  icon:"💥×4",xp:80 },
  { id:"starclock_tap",    label:"Star Clock",       desc:"Tap a Star Clock target",                  icon:"⭐⏱️",xp:30 },
  { id:"starclock_zero",   label:"Zero Hour",        desc:"Tap Star Clock exactly at 0 for jackpot",  icon:"⭐🎯",xp:85 },
  { id:"combo_echo_use",   label:"Combo Echo",       desc:"Activate the Combo Echo power-up",         icon:"🔊⛓️",xp:30 },
  { id:"combo_echo_proc",  label:"Echo Strike",      desc:"Combo Echo auto-hits a nearby target",     icon:"🔊💥",xp:65 },
  { id:"gold_shield_use",  label:"Gold Shield",      desc:"Activate the Gold Shield power-up",        icon:"🛡️🥇",xp:30 },
  { id:"gold_shield_proc", label:"Golden Miss",      desc:"Gold Shield converts a missed life to gold",icon:"🛡️🪙",xp:70 },
  { id:"nebulaclock_tap",  label:"Nebula Clock",     desc:"Tap a Nebula Clock target",                icon:"🌌⏱️",xp:30 },
  { id:"nebulaclock_all",  label:"Nebula Jackpot",   desc:"Collect all Nebula Clock dots for jackpot",icon:"🌌×5",xp:85 },
  { id:"iceshatter_tap",   label:"Ice Shatter",      desc:"Tap an Ice Shatter target",                icon:"🧊💥",xp:30 },
  { id:"iceshatter_done",  label:"Shattered",        desc:"Shatter Ice Shatter with 3 taps",          icon:"🧊×3",xp:70 },
  { id:"phantom_life_use", label:"Phantom Life",     desc:"Activate the Phantom Life power-up",       icon:"👻❤️",xp:30 },
  { id:"phantom_life_save",label:"Ghost Saver",      desc:"Phantom Life prevents game over",          icon:"👻✅",xp:90 },
  { id:"score_doubler_use",label:"Score Doubler",    desc:"Activate the Score Doubler power-up",      icon:"×2🎯",xp:30 },
  { id:"score_doubler_tri",label:"Triple Double",    desc:"Score Doubler scores all 3 targets",       icon:"×2×3",xp:75 },
  { id:"hue_orb_tap",      label:"Hue Orb",          desc:"Tap a Hue Orb target",                     icon:"🌈⭕",xp:30 },
  { id:"hue_orb_match",    label:"Color Match",      desc:"Tap Hue Orb when matching world color",    icon:"🌈✅",xp:75 },
  { id:"lchain_64_tap",    label:"Lightning Chain",  desc:"Tap a Lightning Chain target",             icon:"⚡🔗",xp:30 },
  { id:"lchain_64_max",    label:"Full Chain",       desc:"Lightning Chain hits 5 targets",           icon:"⚡×5",xp:80 },
  { id:"magnet_blitz_use", label:"Magnet Blitz",     desc:"Activate the Magnet Blitz power-up",       icon:"🧲⚡",xp:30 },
  { id:"magnet_blitz_pull",label:"Blitz Pull",       desc:"Magnet Blitz auto-collects 10+ targets",   icon:"🧲×10",xp:70 },
  { id:"crit_storm_use",   label:"Crit Storm",       desc:"Activate the Crit Storm power-up",         icon:"💥🌩️",xp:30 },
  { id:"crit_storm_proc",  label:"Storm Crit",       desc:"Crit Storm fires a 3× crit",               icon:"💥×3",xp:65 },
  { id:"arcanerune_tap",   label:"Arcane Rune",      desc:"Tap an Arcane Rune target",                icon:"🔮✨",xp:30 },
  { id:"arcanerune_match", label:"Rune Match",       desc:"Match the Arcane Rune symbol sequence",    icon:"🔮✅",xp:80 },
  { id:"pulsegem_tap",     label:"Pulse Gem",        desc:"Tap a Pulse Gem target",                   icon:"💎💓",xp:30 },
  { id:"pulsegem_peak",    label:"Peak Pulse",       desc:"Tap Pulse Gem at pulse peak for 4× score", icon:"💎🎯",xp:75 },
  { id:"bonus_wave_use",   label:"Bonus Wave",       desc:"Activate the Bonus Wave power-up",         icon:"🌊🎁",xp:30 },
  { id:"bonus_wave_all",   label:"Wave Cleared",     desc:"Collect all 5 Bonus Wave orbs",            icon:"🌊×5",xp:70 },
  { id:"tap_fury_use",     label:"Tap Fury",         desc:"Activate the Tap Fury power-up",           icon:"🔥👊",xp:30 },
  { id:"tap_fury_big",     label:"Fury Score",       desc:"Score 500+ pts during Tap Fury",           icon:"🔥💯",xp:75 },
  { id:"cosmicweb_tap",    label:"Cosmic Web",       desc:"Tap a Cosmic Web center target",           icon:"🕸️✨",xp:30 },
  { id:"cosmicweb_all",    label:"Full Web",         desc:"Tap all 6 Cosmic Web nodes first",         icon:"🕸️×6",xp:85 },
  { id:"warporb_tap",      label:"Warp Orb",         desc:"Tap a Warp Orb target",                    icon:"🌀💥",xp:30 },
  { id:"warporb_crit",     label:"Mid-Warp",         desc:"Tap Warp Orb during a teleport",           icon:"🌀🎯",xp:80 },
  { id:"score_spike_use",  label:"Score Spike",      desc:"Activate the Score Spike power-up",        icon:"📊⚡",xp:30 },
  { id:"score_spike_proc", label:"Spike Hit",        desc:"Score Spike fires a 5× spike",             icon:"📊×5",xp:70 },
  { id:"life_burst_use",   label:"Life Burst",       desc:"Activate the Life Burst power-up",         icon:"❤️💥",xp:30 },
  { id:"life_burst_heal",  label:"Full Burst",       desc:"Life Burst restores 2 lives",              icon:"❤️❤️",xp:65 },
  { id:"darkmirror_tap",   label:"Dark Mirror",      desc:"Tap a Dark Mirror target",                 icon:"🪞🖤",xp:30 },
  { id:"darkmirror_peak",  label:"Mirror Peak",      desc:"Tap Dark Mirror at max opacity",           icon:"🪞✅",xp:75 },
  { id:"stormball_tap",    label:"Storm Ball",       desc:"Tap a Storm Ball target",                  icon:"⚡🔴",xp:30 },
  { id:"stormball_early",  label:"Storm Catch",      desc:"Tap Storm Ball before max speed",          icon:"⚡🏃",xp:70 },
  { id:"phase_burst_use",  label:"Phase Burst",      desc:"Activate the Phase Burst power-up",        icon:"💠⚡",xp:30 },
  { id:"phase_burst_bomb", label:"Phase Through",    desc:"Phase Burst safely scores a bomb",         icon:"💠💣",xp:75 },
  { id:"vortex_tap_use",   label:"Vortex Tap",       desc:"Activate the Vortex Tap power-up",         icon:"🌀👊",xp:30 },
  { id:"vortex_tap_pull",  label:"Vortex Pull",      desc:"Vortex Tap pulls 3+ targets at once",      icon:"🌀🎯",xp:65 },
  { id:"chainnova_tap",    label:"Chain Nova",       desc:"Tap a Chain Nova target",                  icon:"💥⛓️",xp:35 },
  { id:"chainnova_ring",   label:"Nova Ring",        desc:"Chain Nova scores 3+ nearby targets",      icon:"💥🌐",xp:85 },
  { id:"mirrorflash_tap",  label:"Mirror Flash",     desc:"Tap a Mirror Flash target",                icon:"⚡🪞",xp:35 },
  { id:"mirrorflash_true", label:"True Mirror",      desc:"Tap the real Mirror Flash (not decoy)",    icon:"🪞✅",xp:80 },
  { id:"chain_lock_use",   label:"Chain Lock",       desc:"Activate the Chain Lock power-up",         icon:"🔐⛓️",xp:30 },
  { id:"chain_lock_max",   label:"Chain Reaction",   desc:"Chain Lock scores 5 chained taps",         icon:"⛓️×5",xp:75 },
  { id:"nova_storm_use",   label:"Nova Storm",       desc:"Activate the Nova Storm power-up",         icon:"🌩️💥",xp:30 },
  { id:"nova_storm_proc",  label:"Storm Strike",     desc:"Nova Storm auto-detonates a target",       icon:"💥⚡",xp:60 },
  { id:"prismclock_tap",   label:"Prism Clock",      desc:"Tap a Prism Clock target",                 icon:"🕐🌈",xp:35 },
  { id:"prismclock_match", label:"Color Match",      desc:"Hit Prism Clock on matching world color",  icon:"🌈✅",xp:85 },
  { id:"voidpulse_tap",    label:"Void Pulse",       desc:"Tap a Void Pulse target",                  icon:"🌑💫",xp:35 },
  { id:"voidpulse_max",    label:"Max Pulse",        desc:"Tap Void Pulse at maximum size",           icon:"🌑⬆️",xp:80 },
  { id:"echo_chain_use",   label:"Echo Chain",       desc:"Activate the Echo Chain power-up",         icon:"🔊⛓️",xp:30 },
  { id:"echo_chain_proc",  label:"Echo Score",       desc:"Echo Chain re-scores a tap position",      icon:"🔊💰",xp:60 },
  { id:"gravity_burst_use",label:"Gravity Burst",    desc:"Activate the Gravity Burst power-up",      icon:"🌀💥",xp:30 },
  { id:"gravity_burst_big",label:"Gravity Cluster",  desc:"Gravity Burst pulls 4+ targets at once",   icon:"🌀×4",xp:80 },
  { id:"arcbloom_tap",     label:"Arc Bloom",        desc:"Tap an Arc Bloom target",                  icon:"🌸⚡",xp:35 },
  { id:"arcbloom_full",    label:"Full Bloom",       desc:"Tap Arc Bloom when fully open",            icon:"🌸🌟",xp:85 },
  { id:"neonbolt_tap",     label:"Neon Bolt",        desc:"Tap a Neon Bolt target",                   icon:"⚡🌈",xp:35 },
  { id:"neonbolt_fast",    label:"Lightning Catch",  desc:"Tap Neon Bolt before it exits screen",     icon:"⚡🏃",xp:75 },
  { id:"score_flood_use",  label:"Score Flood",      desc:"Activate the Score Flood power-up",        icon:"🌊💰",xp:30 },
  { id:"score_flood_big",  label:"Flood Wave",       desc:"Score Flood awards 500+ bonus score",      icon:"🌊🎉",xp:70 },
  { id:"life_leech_plus",  label:"Life Leech+",      desc:"Activate the Life Leech+ power-up",        icon:"❤️🩸+",xp:30 },
  { id:"life_leech_plus_3",label:"Leeched 3",        desc:"Life Leech+ restores 3 lives in one run",  icon:"❤️❤️❤️",xp:75 },
  { id:"crystalburst_tap", label:"Crystal Burst",    desc:"Tap a Crystal Burst target",               icon:"💎💥",xp:35 },
  { id:"crystalburst_all", label:"All Shards",       desc:"Collect all 4 Crystal Burst shards",       icon:"💎×4",xp:85 },
  { id:"timerift_tap",     label:"Time Rift",        desc:"Tap a Time Rift target",                   icon:"⏱️🌀",xp:35 },
  { id:"timerift_clutch",  label:"Clutch Rift",      desc:"Tap Time Rift in final second",            icon:"⏱️🏃",xp:90 },
  { id:"combo_surge_use",  label:"Combo Surge",      desc:"Activate the Combo Surge power-up",        icon:"🔥⛓️",xp:30 },
  { id:"combo_surge_big",  label:"Surge Hold",       desc:"Combo Surge holds 20+ streak for 5 taps",  icon:"🔥×20",xp:80 },
  { id:"star_forge_use",   label:"Star Forge",       desc:"Activate the Star Forge power-up",         icon:"⭐🔥",xp:30 },
  { id:"star_forge_leg",   label:"Forge Legend",     desc:"Star Forge upgrades a target to legendary",icon:"⭐👑",xp:90 },
  { id:"glitchorb_tap",    label:"Glitch Orb",       desc:"Tap a Glitch Orb target",                  icon:"👾⚡",xp:35 },
  { id:"glitchorb_vis",    label:"Visible Frame",    desc:"Tap Glitch Orb while visible",             icon:"👾✅",xp:80 },
  { id:"solarflare72_tap", label:"Solar Flare",      desc:"Tap a Solar Flare target",                 icon:"☀️⚡",xp:35 },
  { id:"solarflare72_peak",label:"Solar Peak",       desc:"Tap Solar Flare at maximum brightness",    icon:"☀️🌟",xp:85 },
  { id:"pulse_shield_use", label:"Pulse Shield",     desc:"Activate the Pulse Shield power-up",       icon:"💠🛡️",xp:30 },
  { id:"pulse_shield_blk", label:"Shield Block",     desc:"Pulse Shield blocks a miss",               icon:"💠✅",xp:65 },
  { id:"score_amplify_use",label:"Score Amplify",    desc:"Activate the Score Amplify power-up",      icon:"📈🔊",xp:30 },
  { id:"score_amplify_big",label:"Amplify Surge",    desc:"Score Amplify awards 300+ bonus in a run", icon:"📈💥",xp:75 },
  { id:"frostnova_tap",    label:"Frost Nova",       desc:"Tap a Frost Nova target",                  icon:"❄️💥",xp:35 },
  { id:"frostnova_freeze", label:"Mass Freeze",      desc:"Frost Nova freezes 3+ targets",            icon:"❄️×3",xp:80 },
  { id:"lightbomb_tap",    label:"Light Bomb",       desc:"Tap a Light Bomb target",                  icon:"💡💣",xp:30 },
  { id:"lightbomb_chain",  label:"Light Chain",      desc:"Tap 3 Light Bombs in one level",           icon:"💡×3",xp:65 },
  { id:"ricochet_blast_use",label:"Ricochet Blast",  desc:"Activate the Ricochet Blast power-up",     icon:"🎯⛓️",xp:30 },
  { id:"ricochet_blast_nb", label:"Ricochet Hit",    desc:"Ricochet Blast scores a neighbor target",  icon:"🎯💥",xp:65 },
  { id:"fever_fountain_use",label:"Fever Fountain",  desc:"Activate the Fever Fountain power-up",     icon:"🌡️⛲",xp:30 },
  { id:"fever_fountain_ext",label:"Fever Extended",  desc:"Fever Fountain extends active fever",      icon:"🌡️⬆️",xp:70 },
  { id:"omegapulse_tap",   label:"Omega Pulse",      desc:"Tap an Omega Pulse target",                icon:"Ω💫",xp:40 },
  { id:"omegapulse_fast",  label:"Omega Quick",      desc:"Tap Omega Pulse in first 2 seconds",       icon:"Ω⚡",xp:90 },
  { id:"spiralorb74_tap",  label:"Spiral Orb 74",    desc:"Tap a Spiral Orb 74 target",               icon:"🌀⭐",xp:35 },
  { id:"spiralorb74_center",label:"Spiral Center",   desc:"Tap Spiral Orb 74 at center zone",         icon:"🌀🎯",xp:80 },
  { id:"double_fever_use",  label:"Double Fever",    desc:"Activate the Double Fever power-up",       icon:"🌡️×2",xp:30 },
  { id:"double_fever_2nd",  label:"Second Wave",     desc:"Double Fever triggers second fever wave",  icon:"🌡️🌊",xp:75 },
  { id:"bomb_convert_use",  label:"Bomb Convert",    desc:"Activate the Bomb Convert power-up",       icon:"💣💡",xp:30 },
  { id:"bomb_convert_proc", label:"Bomb Converted",  desc:"Bomb Convert changes a bomb to safe",      icon:"💣✅",xp:55 },
  { id:"nexuscore_tap",     label:"Nexus Core",      desc:"Tap a Nexus Core target",                  icon:"💠⭐",xp:35 },
  { id:"nexuscore_edge",    label:"Edge Catch",      desc:"Tap Nexus Core at edge of hit zone",       icon:"💠🎯",xp:75 },
  { id:"blinkstar_tap",     label:"Blink Star",      desc:"Tap a Blink Star target",                  icon:"⭐🌀",xp:35 },
  { id:"blinkstar_crit",    label:"Blink Crit",      desc:"Tap Blink Star just after it teleports",   icon:"⭐💥",xp:85 },
  { id:"score_magnet_use",  label:"Score Magnet",    desc:"Activate the Score Magnet power-up",       icon:"🧲💰",xp:30 },
  { id:"score_magnet_save", label:"Magnet Save",     desc:"Score Magnet scores an expired target",    icon:"🧲✅",xp:60 },
  { id:"life_overflow_use", label:"Life Overflow",   desc:"Activate the Life Overflow power-up",      icon:"❤️⬆️",xp:30 },
  { id:"life_overflow_bonus",label:"Overflow Bonus", desc:"Life Overflow awards overflow bonus pts",  icon:"❤️💰",xp:65 },
  { id:"etherOrb_tap",      label:"Ether Orb",       desc:"Tap an Ether Orb target",                  icon:"🌫️⭐",xp:35 },
  { id:"etherOrb_copies",   label:"Ether Split",     desc:"Collect both Ether Orb copies",            icon:"🌫️×2",xp:80 },
  { id:"qblast_tap",        label:"Quantum Blast",   desc:"Tap a Quantum Blast target",               icon:"⚛️💥",xp:35 },
  { id:"qblast_tiny",       label:"Quantum Tiny",    desc:"Tap Quantum Blast in tiny phase (5×)",     icon:"⚛️🎯",xp:90 },
  { id:"streak_heal_use",   label:"Streak Heal",     desc:"Activate the Streak Heal power-up",        icon:"❤️🔥",xp:30 },
  { id:"streak_heal_full",  label:"Full Heal",       desc:"Streak Heal restores 3 lives in one run",  icon:"❤️❤️❤️",xp:70 },
  { id:"tempo_burst_use",   label:"Tempo Burst",     desc:"Activate the Tempo Burst power-up",        icon:"🎵💥",xp:30 },
  { id:"tempo_burst_peak",  label:"Tempo Peak",      desc:"Tempo Burst scores 1000+ bonus pts",       icon:"🎵🏆",xp:80 },
  { id:"plasmadisk_tap",    label:"Plasma Disk",     desc:"Tap a Plasma Disk target",                 icon:"💿⚡",xp:35 },
  { id:"plasmadisk_edge",   label:"Disk Edge",       desc:"Tap Plasma Disk within edge ring zone",    icon:"💿🎯",xp:65 },
  { id:"voidring_tap",      label:"Void Ring",       desc:"Tap a Void Ring target",                   icon:"⭕🌑",xp:35 },
  { id:"voidring_inner",    label:"Ring Inner",      desc:"Tap Void Ring through the center gap",     icon:"⭕🎯",xp:75 },
  { id:"crit_surge_use",    label:"Crit Surge",      desc:"Activate the Crit Surge power-up",         icon:"💥🔥",xp:30 },
  { id:"crit_surge_big",    label:"Surge Master",    desc:"Crit Surge lands 5+ crits in one run",     icon:"💥🏆",xp:75 },
  { id:"life_pulse_use",    label:"Life Pulse",      desc:"Activate the Life Pulse power-up",         icon:"💓⚡",xp:30 },
  { id:"life_pulse_full",   label:"Pulse Full",      desc:"Life Pulse restores lives at full combo",  icon:"💓💫",xp:65 },
  { id:"sunpetal_tap",      label:"Sun Petal",       desc:"Tap a Sun Petal target",                   icon:"🌸☀️",xp:35 },
  { id:"sunpetal_bloom",    label:"Full Bloom",      desc:"Tap Sun Petal at full bloom = max pts",    icon:"🌸💛",xp:70 },
  { id:"ghostbell_tap",     label:"Ghost Bell",      desc:"Tap a Ghost Bell target",                  icon:"🔔👻",xp:35 },
  { id:"ghostbell_silent",  label:"Silent Bell",     desc:"Tap Ghost Bell in silent phase (3×)",      icon:"🔔🤫",xp:80 },
  { id:"chain_wave_use",    label:"Chain Wave",      desc:"Activate the Chain Wave power-up",         icon:"🌊⛓️",xp:30 },
  { id:"chain_wave_chain",  label:"Wave Chain",      desc:"Chain Wave chains 3+ targets in a row",    icon:"🌊🔗",xp:70 },
  { id:"score_od_use",      label:"Score Overdrive", desc:"Activate the Score Overdrive power-up",    icon:"📈🔥",xp:30 },
  { id:"score_od_peak",     label:"Overdrive Peak",  desc:"Score Overdrive adds 2000+ pts total",     icon:"📈🏆",xp:85 },
  { id:"moon_orb_tap",      label:"Moon Orb",        desc:"Tap a Moon Orb target",                    icon:"🌙⭐",xp:35 },
  { id:"moon_orb_phase",    label:"Moon Phase",      desc:"Tap Moon Orb at full moon phase = 2×",     icon:"🌕🎯",xp:70 },
  { id:"lava_core_tap",     label:"Lava Core",       desc:"Tap a Lava Core target",                   icon:"🌋💥",xp:35 },
  { id:"lava_core_chain",   label:"Lava Chain",      desc:"Tap 3 Lava Cores within 4s",              icon:"🌋🔗",xp:80 },
  { id:"phantom_strike_use",label:"Phantom Strike",  desc:"Activate the Phantom Strike power-up",     icon:"👻⚡",xp:30 },
  { id:"phantom_strike_hit",label:"Ghost Hit",       desc:"Phantom Strike destroys a ghost target",   icon:"👻🎯",xp:65 },
  { id:"coin_surge_use",    label:"Coin Surge",      desc:"Activate the Coin Surge power-up",         icon:"🪙🚀",xp:30 },
  { id:"coin_surge_rich",   label:"Surge Rich",      desc:"Coin Surge earns 50+ coins in one run",    icon:"🪙🏆",xp:80 },
  { id:"arcnode_tap",       label:"Arc Node",        desc:"Tap an Arc Node target",                   icon:"⚡🔵",xp:35 },
  { id:"arcnode_link",      label:"Node Link",       desc:"Arc Node chains to 2+ nearby targets",     icon:"⚡🔗",xp:70 },
  { id:"prismaticburst_tap",label:"Prismatic Burst", desc:"Tap a Prismatic Burst target",             icon:"🌈💥",xp:35 },
  { id:"prismaticburst_all",label:"Full Spectrum",   desc:"Prismatic Burst hits all 5 color tiers",   icon:"🌈🏆",xp:90 },
  { id:"freeze_surge_use",  label:"Freeze Surge",    desc:"Activate the Freeze Surge power-up",       icon:"❄️⚡",xp:30 },
  { id:"freeze_surge_big",  label:"Surge Freeze",    desc:"Freeze Surge scores 5+ frozen targets",    icon:"❄️🏆",xp:70 },
  { id:"lucky_strike_use",  label:"Lucky Strike",    desc:"Activate the Lucky Strike power-up",       icon:"🍀⚡",xp:30 },
  { id:"lucky_strike_jack", label:"Lucky Jackpot",   desc:"Lucky Strike triggers a jackpot",          icon:"🍀💰",xp:85 },
  { id:"crystalwing_tap",   label:"Crystal Wing",    desc:"Tap a Crystal Wing target",                icon:"💎🦋",xp:35 },
  { id:"crystalwing_pair",  label:"Wing Pair",       desc:"Tap both Crystal Wings within 2.5s",       icon:"💎✌️",xp:75 },
  { id:"stormcloud_tap",    label:"Storm Cloud",     desc:"Tap a Storm Cloud target",                 icon:"⛈️💥",xp:35 },
  { id:"stormcloud_bolt",   label:"Storm Bolt",      desc:"Tap Storm Cloud while lightning flashes",  icon:"⛈️⚡",xp:75 },
  { id:"echo_burst_use",    label:"Echo Burst",      desc:"Activate the Echo Burst power-up",         icon:"🔊💥",xp:30 },
  { id:"echo_burst_big",    label:"Echo Big",        desc:"Echo Burst scores 500+ total bonus pts",   icon:"🔊🏆",xp:75 },
  { id:"shield_surge_use",  label:"Shield Surge",    desc:"Activate the Shield Surge power-up",       icon:"🛡️🚀",xp:30 },
  { id:"shield_surge_blk",  label:"Surge Block",     desc:"Shield Surge absorbs 3+ hits in one run",  icon:"🛡️🏆",xp:70 },
  { id:"solarpetal_tap",    label:"Solar Petal",     desc:"Tap a Solar Petal target",                 icon:"🌸☀️",xp:35 },
  { id:"solarpetal_chain",  label:"Petal Chain",     desc:"Tap 3 Solar Petals without missing",       icon:"🌸🔗",xp:70 },
  { id:"cosmicshard_tap",   label:"Cosmic Shard",    desc:"Tap a Cosmic Shard target",                icon:"💠🌌",xp:35 },
  { id:"cosmicshard_align", label:"Shard Align",     desc:"Tap 3 Cosmic Shards to align the set",     icon:"💠✨",xp:85 },
  { id:"score_snap_use",    label:"Score Snap",      desc:"Activate the Score Snap power-up",         icon:"📸💰",xp:30 },
  { id:"score_snap_big",    label:"Snap Big",        desc:"Score Snap saves 500+ pts from misses",    icon:"📸🏆",xp:75 },
  { id:"life_guard_use",    label:"Life Guard",      desc:"Activate the Life Guard power-up",         icon:"❤️🛡️",xp:30 },
  { id:"life_guard_save",   label:"Guard Save",      desc:"Life Guard saves a life on game over",     icon:"❤️✅",xp:80 },
  { id:"novadrift_tap",     label:"Nova Drift",      desc:"Tap a Nova Drift target",                  icon:"💥🌊",xp:35 },
  { id:"novadrift_center",  label:"Drift Center",    desc:"Tap Nova Drift at the center = 2×",        icon:"💥🎯",xp:70 },
  { id:"energypulse_tap",   label:"Energy Pulse",    desc:"Tap an Energy Pulse target",               icon:"⚡💙",xp:35 },
  { id:"energypulse_max",   label:"Max Pulse",       desc:"Tap Energy Pulse at maximum charge",       icon:"⚡🔋",xp:75 },
  { id:"burst_fever_use",   label:"Burst Fever",     desc:"Activate the Burst Fever power-up",        icon:"🌡️💥",xp:30 },
  { id:"burst_fever_peak",  label:"Burst Peak",      desc:"Score 2000+ pts during Burst Fever",       icon:"🌡️🏆",xp:80 },
  { id:"score_trail_use",   label:"Score Trail",     desc:"Activate the Score Trail power-up",        icon:"✨📈",xp:30 },
  { id:"score_trail_big",   label:"Trail Big",       desc:"Score Trail adds 600+ pts in one run",     icon:"✨🏆",xp:75 },
  { id:"icedagger_tap",     label:"Ice Dagger",      desc:"Tap an Ice Dagger target",                 icon:"🗡️❄️",xp:35 },
  { id:"icedagger_tip",     label:"Dagger Tip",      desc:"Tap Ice Dagger at the pointed tip = 3×",   icon:"🗡️🎯",xp:80 },
  { id:"shadowflare_tap",   label:"Shadow Flare",    desc:"Tap a Shadow Flare target",                icon:"🌑🔥",xp:35 },
  { id:"shadowflare_bright",label:"Flare Bright",    desc:"Tap Shadow Flare in bright phase = 2×",    icon:"🌑☀️",xp:70 },
  { id:"chain_snap_use",    label:"Chain Snap",      desc:"Activate the Chain Snap power-up",         icon:"🔗📸",xp:30 },
  { id:"chain_snap_proc",   label:"Snap Proc",       desc:"Chain Snap auto-scores 3 targets in one run", icon:"🔗🏆",xp:75 },
  { id:"life_echo_use",     label:"Life Echo",       desc:"Activate the Life Echo power-up",          icon:"❤️🔊",xp:30 },
  { id:"life_echo_trigger", label:"Echo Trigger",    desc:"Life Echo grants a bonus life",            icon:"❤️✨",xp:70 },
  { id:"plasmaring_tap",    label:"Plasma Ring",     desc:"Tap a Plasma Ring target",                 icon:"💿🔥",xp:35 },
  { id:"plasmaring_inside", label:"Ring Inside",     desc:"Tap inside the Plasma Ring = 2×",         icon:"💿🎯",xp:80 },
  { id:"voidstar_tap",      label:"Void Star",       desc:"Tap a Void Star target",                   icon:"⭐🌑",xp:35 },
  { id:"voidstar_core",     label:"Star Core",       desc:"Tap Void Star core = 3× points",          icon:"⭐💥",xp:80 },
  { id:"score_vault_use",   label:"Score Vault",     desc:"Activate the Score Vault power-up",        icon:"🏦💰",xp:30 },
  { id:"score_vault_big",   label:"Vault Big",       desc:"Score Vault releases 800+ pts",            icon:"🏦🏆",xp:80 },
  { id:"streak_echo_use",   label:"Streak Echo",     desc:"Activate the Streak Echo power-up",        icon:"🔥🔊",xp:30 },
  { id:"streak_echo_peak",  label:"Echo Peak",       desc:"Streak Echo grants 1000+ bonus pts",       icon:"🔥🏆",xp:75 },
  { id:"aurorawave_tap",    label:"Aurora Wave",     desc:"Tap an Aurora Wave target",                icon:"🌊🌈",xp:35 },
  { id:"aurorawave_crest",  label:"Wave Crest",      desc:"Tap Aurora Wave at peak crest = 2×",      icon:"🌊🏔️",xp:80 },
  { id:"crystalprism_tap",  label:"Crystal Prism",   desc:"Tap a Crystal Prism target",               icon:"🔷🌈",xp:35 },
  { id:"crystalprism_split",label:"Prism Split",     desc:"Crystal Prism splits into 3 fragments",   icon:"🔷✨",xp:75 },
  { id:"nova_pulse_use",    label:"Nova Pulse",      desc:"Activate the Nova Pulse power-up",         icon:"💥🫀",xp:30 },
  { id:"nova_pulse_big",    label:"Pulse Big",       desc:"Nova Pulse grants 1500+ bonus pts",        icon:"💥🏆",xp:80 },
  { id:"coin_echo_use",     label:"Coin Echo",       desc:"Activate the Coin Echo power-up",          icon:"🪙🔊",xp:30 },
  { id:"coin_echo_rich",    label:"Echo Rich",       desc:"Coin Echo earns 60+ coins in one run",     icon:"🪙🏆",xp:75 },
  { id:"thunderbead_tap",   label:"Thunder Bead",    desc:"Tap a Thunder Bead target",                icon:"🟡⚡",xp:35 },
  { id:"thunderbead_chain", label:"Bead Chain",      desc:"Tap 3 Thunder Beads in a row",             icon:"🟡⛓️",xp:80 },
  { id:"mirrorstar_tap",    label:"Mirror Star",     desc:"Tap a Mirror Star target",                 icon:"⭐🪞",xp:35 },
  { id:"mirrorstar_match",  label:"Mirror Match",    desc:"Mirror Star reflects for bonus score",     icon:"⭐💡",xp:75 },
  { id:"time_shield_use",   label:"Time Shield",     desc:"Activate the Time Shield power-up",        icon:"⏱️🛡️",xp:30 },
  { id:"time_shield_save",  label:"Shield Save",     desc:"Time Shield extends a target's lifetime",  icon:"⏱️✅",xp:70 },
  { id:"score_blitz_use",   label:"Score Blitz",     desc:"Activate the Score Blitz power-up",        icon:"⚡💯",xp:30 },
  { id:"score_blitz_peak",  label:"Blitz Peak",      desc:"Score Blitz grants 2000+ bonus",           icon:"⚡🏆",xp:80 },
  { id:"frostgem_tap",      label:"Frost Gem",       desc:"Tap a Frost Gem target",                   icon:"💎❄️",xp:35 },
  { id:"frostgem_freeze",   label:"Gem Freeze",      desc:"Frost Gem freezes bombs on tap",           icon:"💎🧊",xp:75 },
  { id:"heatorb_tap",       label:"Heat Orb",        desc:"Tap a Heat Orb target",                    icon:"🔴🔥",xp:35 },
  { id:"heatorb_hot",       label:"Orb Ignite",      desc:"Tap Heat Orb while fever active = 3×",    icon:"🔴☀️",xp:80 },
  { id:"phantom_rush_use",  label:"Phantom Rush",    desc:"Activate the Phantom Rush power-up",       icon:"👻⚡",xp:30 },
  { id:"phantom_rush_big",  label:"Rush Big",        desc:"Phantom Rush grants 1800+ bonus",          icon:"👻🏆",xp:80 },
  { id:"coin_storm_use",    label:"Coin Storm",      desc:"Activate the Coin Storm power-up",         icon:"🪙🌪️",xp:30 },
  { id:"coin_storm_rich",   label:"Storm Rich",      desc:"Coin Storm earns 80+ coins",               icon:"🪙💎",xp:75 },
  { id:"nebuladrop_tap",    label:"Nebula Drop",     desc:"Tap a Nebula Drop target",                 icon:"🌌💧",xp:35 },
  { id:"nebuladrop_collect",label:"Drop Collect",    desc:"Tap 5 Nebula Drops in one run",            icon:"🌌✨",xp:80 },
  { id:"pulsegate_tap",     label:"Pulse Gate",      desc:"Tap a Pulse Gate target",                  icon:"🚪💥",xp:35 },
  { id:"pulsegate_open",    label:"Gate Open",       desc:"Tap Pulse Gate in open phase = 2×",        icon:"🚪🌟",xp:75 },
  { id:"streak_blaze_use",  label:"Streak Blaze",    desc:"Activate the Streak Blaze power-up",       icon:"🔥🌪️",xp:30 },
  { id:"streak_blaze_max",  label:"Blaze Max",       desc:"Streak Blaze peaks at streak 30+",         icon:"🔥🏆",xp:80 },
  { id:"life_bloom_use",    label:"Life Bloom",      desc:"Activate the Life Bloom power-up",         icon:"❤️🌸",xp:30 },
  { id:"life_bloom_max",    label:"Bloom Max",       desc:"Life Bloom restores max lives",            icon:"❤️🌟",xp:70 },
  { id:"spiralbead_tap",    label:"Spiral Bead",     desc:"Tap a Spiral Bead target",                 icon:"🌀💫",xp:35 },
  { id:"spiralbead_center", label:"Bead Center",     desc:"Tap Spiral Bead at center = 3×",          icon:"🌀🎯",xp:80 },
  { id:"quantumflux_tap",   label:"Quantum Flux",    desc:"Tap a Quantum Flux target",                icon:"⚛️💥",xp:35 },
  { id:"quantumflux_stable",label:"Flux Stable",     desc:"Tap Quantum Flux in stable phase = 2×",   icon:"⚛️✅",xp:75 },
  { id:"focus_blast_use",   label:"Focus Blast",     desc:"Activate the Focus Blast power-up",        icon:"🎯💥",xp:30 },
  { id:"focus_blast_big",   label:"Blast Big",       desc:"Focus Blast hits 5 targets in one run",    icon:"🎯🏆",xp:80 },
  { id:"coin_chain_use",    label:"Coin Chain",      desc:"Activate the Coin Chain power-up",         icon:"🪙⛓️",xp:30 },
  { id:"coin_chain_max",    label:"Chain Max",       desc:"Coin Chain accumulates 50+ bonus coins",   icon:"🪙🏆",xp:75 },
  { id:"nebulacore_tap",    label:"Nebula Core",     desc:"Tap a Nebula Core target",                 icon:"🌌💫",xp:35 },
  { id:"nebulacore_center", label:"Core Center",     desc:"Tap Nebula Core dead center = 3×",         icon:"🌌🎯",xp:80 },
  { id:"twilightorb_tap",   label:"Twilight Orb",    desc:"Tap a Twilight Orb target",                icon:"🌅💜",xp:35 },
  { id:"twilightorb_dusk",  label:"Dusk Phase",      desc:"Tap Twilight Orb in dusk phase = 2×",     icon:"🌅🌟",xp:75 },
  { id:"shield_burst_use",  label:"Shield Burst",    desc:"Activate the Shield Burst power-up",       icon:"🛡️💥",xp:30 },
  { id:"shield_burst_tank", label:"Burst Tanker",    desc:"Shield Burst blocks 3 misses in one run",  icon:"🛡️🏆",xp:80 },
  { id:"score_echo_use",    label:"Score Echo",      desc:"Activate the Score Echo power-up",         icon:"📊🔁",xp:30 },
  { id:"score_echo_big",    label:"Echo Big",        desc:"Score Echo replays 500+ total bonus pts",  icon:"📊🏆",xp:75 },
  { id:"flameorb_tap",      label:"Flame Orb",       desc:"Tap a Flame Orb target",                   icon:"🔥🔮",xp:35 },
  { id:"flameorb_super",    label:"Super Flame",     desc:"Tap Flame Orb at peak intensity = 3×",     icon:"🔥🏆",xp:80 },
  { id:"crystalshell_tap",  label:"Crystal Shell",   desc:"Tap a Crystal Shell target",               icon:"💎🐚",xp:35 },
  { id:"crystalshell_core", label:"Shell Core",      desc:"Crack Crystal Shell to the core = 2×",    icon:"💎🎯",xp:75 },
  { id:"combo_surge2_use",  label:"Combo Surge II",  desc:"Activate the Combo Surge II power-up",     icon:"🔥⛓️",xp:30 },
  { id:"combo_surge2_peak", label:"Surge Peak",      desc:"Combo Surge II reaches 15× multiplier",    icon:"🔥🏆",xp:80 },
  { id:"life_spiral_use",   label:"Life Spiral",     desc:"Activate the Life Spiral power-up",        icon:"❤️🌀",xp:30 },
  { id:"life_spiral_max",   label:"Spiral Max",      desc:"Life Spiral restores 3+ lives",            icon:"❤️🏆",xp:75 },
  { id:"starpulse_tap",     label:"Star Pulse",      desc:"Tap a Star Pulse target",                  icon:"⭐💓",xp:35 },
  { id:"starpulse_sync",    label:"Pulse Sync",      desc:"Tap Star Pulse at exact peak = 4×",        icon:"⭐🏆",xp:85 },
  { id:"voidnova_tap",      label:"Void Nova",       desc:"Tap a Void Nova target",                   icon:"🌑💥",xp:35 },
  { id:"voidnova_burst",    label:"Nova Burst",      desc:"Tap Void Nova right before collapse = 3×", icon:"🌑🏆",xp:80 },
  { id:"tempo_echo_use",    label:"Tempo Echo",      desc:"Activate the Tempo Echo power-up",         icon:"🎵🔊",xp:30 },
  { id:"tempo_echo_beat",   label:"Beat Master",     desc:"Tempo Echo triggers 10+ bonus beats",      icon:"🎵🏆",xp:80 },
  { id:"coin_bloom_use",    label:"Coin Bloom",      desc:"Activate the Coin Bloom power-up",         icon:"🪙🌸",xp:30 },
  { id:"coin_bloom_full",   label:"Full Bloom",      desc:"Coin Bloom yields 30+ coins in one run",   icon:"🪙🏆",xp:75 },
  { id:"solarbeam_tap",     label:"Solar Beam",      desc:"Tap a Solar Beam target",                  icon:"☀️🔦",xp:35 },
  { id:"solarbeam_center",  label:"Beam Center",     desc:"Hit Solar Beam dead center = 3×",          icon:"☀️🎯",xp:80 },
  { id:"iceprism_tap",      label:"Ice Prism",       desc:"Tap an Ice Prism target",                  icon:"🧊🔷",xp:35 },
  { id:"iceprism_split",    label:"Prism Split",     desc:"Tap Ice Prism in split phase = 2×",        icon:"🧊🌈",xp:75 },
  { id:"streak_nova_use",   label:"Streak Nova",     desc:"Activate the Streak Nova power-up",        icon:"⛓️💥",xp:30 },
  { id:"streak_nova_peak",  label:"Nova Ignition",   desc:"Streak Nova fires at 20+ streak",          icon:"⛓️🏆",xp:85 },
  { id:"life_storm_use",    label:"Life Storm",      desc:"Activate the Life Storm power-up",         icon:"❤️⛈️",xp:30 },
  { id:"life_storm_max",    label:"Storm Surge",     desc:"Life Storm restores max 5 lives",          icon:"❤️🏆",xp:80 },
  { id:"prismaticeye_tap",  label:"Prismatic Eye",   desc:"Tap a Prismatic Eye target",               icon:"👁️🌈",xp:35 },
  { id:"prismaticeye_gaze", label:"Eye Gaze",        desc:"Hold gaze on Prismatic Eye = 3×",          icon:"👁️🏆",xp:80 },
  { id:"thundercrown_tap",  label:"Thunder Crown",   desc:"Tap a Thunder Crown target",               icon:"⚡👑",xp:35 },
  { id:"thundercrown_zap",  label:"Crown Zap",       desc:"Tap Thunder Crown at full charge = 3×",   icon:"⚡🏆",xp:80 },
  { id:"gold_frenzy_use",   label:"Gold Frenzy",     desc:"Activate the Gold Frenzy power-up",        icon:"🥇🔥",xp:30 },
  { id:"gold_frenzy_rich",  label:"Frenzy Rich",     desc:"Gold Frenzy earns 50+ coins in one run",   icon:"🥇🏆",xp:85 },
  { id:"phantom_coin_use",  label:"Phantom Coin",    desc:"Activate the Phantom Coin power-up",       icon:"👻🪙",xp:30 },
  { id:"phantom_coin_haul", label:"Phantom Haul",    desc:"Phantom Coin collects 5 phantom coins",    icon:"👻🏆",xp:75 },
  { id:"cosmicdust_tap",    label:"Cosmic Dust",     desc:"Tap a Cosmic Dust target",                 icon:"🌌✨",xp:35 },
  { id:"cosmicdust_cloud",  label:"Dust Cloud",      desc:"Tap Cosmic Dust at peak density = 2×",     icon:"🌌🏆",xp:75 },
  { id:"novabell_tap",      label:"Nova Bell",       desc:"Tap a Nova Bell target",                   icon:"🔔💥",xp:35 },
  { id:"novabell_ring",     label:"Bell Ring",       desc:"Tap Nova Bell at resonance peak = 3×",     icon:"🔔🏆",xp:80 },
  { id:"score_frenzy_use",  label:"Score Frenzy",    desc:"Activate the Score Frenzy power-up",       icon:"📊🔥",xp:30 },
  { id:"score_frenzy_big",  label:"Frenzy Big",      desc:"Score Frenzy doubles score 500+ in one run",icon:"📊🏆",xp:85 },
  { id:"life_beacon_use",   label:"Life Beacon",     desc:"Activate the Life Beacon power-up",        icon:"❤️🔦",xp:30 },
  { id:"life_beacon_save",  label:"Beacon Save",     desc:"Life Beacon triggers when at 1 life",      icon:"❤️🏆",xp:80 },
  { id:"warpglyph_tap",     label:"Warp Glyph",      desc:"Tap a Warp Glyph target",                  icon:"🔮✨",xp:35 },
  { id:"warpglyph_portal",  label:"Glyph Portal",    desc:"Tap Warp Glyph while glowing = 3×",        icon:"🔮🏆",xp:80 },
  { id:"etherbolt_tap",     label:"Ether Bolt",      desc:"Tap an Ether Bolt target",                 icon:"⚡🌀",xp:35 },
  { id:"etherbolt_chain",   label:"Bolt Chain",      desc:"Ether Bolt chains 3 consecutive = 2×",     icon:"⚡🏆",xp:75 },
  { id:"blaze_trail_use",   label:"Blaze Trail",     desc:"Activate the Blaze Trail power-up",        icon:"🔥✨",xp:30 },
  { id:"blaze_trail_long",  label:"Trail Blazer",    desc:"Blaze Trail bonus exceeds 300pts total",   icon:"🔥🏆",xp:80 },
  { id:"coin_pulse_use",    label:"Coin Pulse",      desc:"Activate the Coin Pulse power-up",         icon:"🪙💓",xp:30 },
  { id:"coin_pulse_rich",   label:"Pulse Rich",      desc:"Coin Pulse accumulates 40+ coins",         icon:"🪙🏆",xp:75 },
  { id:"lunarrune_tap",     label:"Lunar Rune",      desc:"Tap a Lunar Rune target",                  icon:"🌙🔮",xp:35 },
  { id:"lunarrune_full",    label:"Full Moon Rune",  desc:"Tap Lunar Rune at full moon phase = 3×",   icon:"🌕🏆",xp:80 },
  { id:"stormseed_tap",     label:"Storm Seed",      desc:"Tap a Storm Seed target",                  icon:"🌩️🌱",xp:35 },
  { id:"stormseed_grow",    label:"Storm Grown",     desc:"Tap Storm Seed after it fully grows = 2×", icon:"🌩️🏆",xp:75 },
  { id:"fever_bloom_use",   label:"Fever Bloom",     desc:"Activate the Fever Bloom power-up",        icon:"🌡️🌸",xp:30 },
  { id:"fever_bloom_peak",  label:"Bloom Peak",      desc:"Fever Bloom activates fever mode twice",   icon:"🌡️🏆",xp:85 },
  { id:"coin_cascade_use",  label:"Coin Cascade",    desc:"Activate the Coin Cascade power-up",       icon:"🪙💦",xp:30 },
  { id:"coin_cascade_max",  label:"Cascade Max",     desc:"Coin Cascade total exceeds 60 coins",      icon:"🪙🏆",xp:85 },
  { id:"plasmahalo_tap",    label:"Plasma Halo",     desc:"Tap a Plasma Halo target",                 icon:"💜🔱",xp:35 },
  { id:"plasmahalo_sweet",  label:"Halo Sweetspot",  desc:"Tap Plasma Halo at ring sweetspot = 3×",   icon:"💜🏆",xp:80 },
  { id:"glitchnode_tap",    label:"Glitch Node",     desc:"Tap a Glitch Node target",                 icon:"💾⚡",xp:35 },
  { id:"glitchnode_burst",  label:"Node Burst",      desc:"Tap Glitch Node in burst phase = 2×",      icon:"💾🏆",xp:75 },
  { id:"score_omega_use",   label:"Score Omega",     desc:"Activate the Score Omega power-up",        icon:"Ω💥",xp:30  },
  { id:"score_omega_max",   label:"Omega Maxed",     desc:"Score Omega reaches 10× multiplier",       icon:"Ω🏆",xp:100 },
  { id:"eternal_life_use",  label:"Eternal Life",    desc:"Activate the Eternal Life power-up",       icon:"♾️❤️",xp:30  },
  { id:"eternal_life_hero", label:"Eternal Hero",    desc:"Eternal Life saves you from 0 lives",      icon:"♾️🏆",xp:100 },
  { id:"omegaheart_tap",    label:"Omega Heart",     desc:"Tap an Omega Heart target",                icon:"💜Ω",xp:40  },
  { id:"omegaheart_sync",   label:"Heart Sync",      desc:"Tap Omega Heart at perfect sync = 5×",     icon:"💜🏆",xp:100 },
  { id:"nexusgem_tap",      label:"Nexus Gem",       desc:"Tap a Nexus Gem target",                   icon:"💎🌐",xp:40  },
  { id:"nexusgem_core",     label:"Gem Core",        desc:"Tap Nexus Gem dead center = 4×",           icon:"💎🏆",xp:100 },
  { id:"arc_surge_use",     label:"Arc Surge",       desc:"Activate the Arc Surge power-up",          icon:"⚡🌊",xp:30 },
  { id:"arc_surge_chain",   label:"Surge Chain",     desc:"Arc Surge chains 8+ targets in sequence",  icon:"⚡🏆",xp:80 },
  { id:"relic_pulse_use",   label:"Relic Pulse",     desc:"Activate the Relic Pulse power-up",        icon:"🏺💓",xp:30 },
  { id:"relic_pulse_max",   label:"Relic Max",       desc:"Relic Pulse total bonus exceeds 400pts",   icon:"🏺🏆",xp:80 },
  { id:"shadowcrystal_tap", label:"Shadow Crystal",  desc:"Tap a Shadow Crystal target",              icon:"🖤💎",xp:35 },
  { id:"shadowcrystal_dark",label:"Dark Core",       desc:"Tap Shadow Crystal in dark phase = 3×",    icon:"🖤🏆",xp:80 },
  { id:"tideorb_tap",       label:"Tide Orb",        desc:"Tap a Tide Orb target",                    icon:"🌊🔮",xp:35 },
  { id:"tideorb_crest",     label:"Wave Crest",      desc:"Tap Tide Orb at wave crest = 2×",          icon:"🌊🏆",xp:75 },
  { id:"venom_surge_use",   label:"Venom Surge",     desc:"Activate the Venom Surge power-up",        icon:"☠️⚡",xp:30 },
  { id:"venom_surge_max",   label:"Venom Stacker",   desc:"Venom Surge converts 5+ misses to bonus",  icon:"☠️🏆",xp:80 },
  { id:"lunar_bloom_use",   label:"Lunar Bloom",     desc:"Activate the Lunar Bloom power-up",        icon:"🌙🌸",xp:30 },
  { id:"lunar_bloom_max",   label:"Bloom Harvest",   desc:"Lunar Bloom spawns 10+ ghost targets",     icon:"🌙🏆",xp:75 },
  { id:"crystalpetal_tap",  label:"Crystal Petal",   desc:"Tap a Crystal Petal target",               icon:"🌸💎",xp:35 },
  { id:"crystalpetal_align",label:"Petal Align",     desc:"Tap Crystal Petal when all petals align",  icon:"🌸🏆",xp:85 },
  { id:"twilightshard_tap", label:"Twilight Shard",  desc:"Tap a Twilight Shard target",              icon:"🌌🔮",xp:35 },
  { id:"twilightshard_dusk",label:"Dusk Phase",      desc:"Tap Twilight Shard at twilight phase = 2×",icon:"🌌🏆",xp:75 },
  { id:"star_chain_use",    label:"Star Chain",      desc:"Activate the Star Chain power-up",         icon:"⭐⛓️",xp:30 },
  { id:"star_chain_max",    label:"Star Streak",     desc:"Star Chain creates 6+ consecutive links",  icon:"⭐🏆",xp:80 },
  { id:"void_bloom_use",    label:"Void Bloom",      desc:"Activate the Void Bloom power-up",         icon:"🌑🌸",xp:30 },
  { id:"void_bloom_max",    label:"Void Harvest",    desc:"Void Bloom total bonus exceeds 500pts",    icon:"🌑🏆",xp:80 },
  { id:"novaspire_tap",     label:"Nova Spire",      desc:"Tap a Nova Spire target",                  icon:"💥🗼",xp:35 },
  { id:"novaspire_peak",    label:"Spire Peak",      desc:"Tap Nova Spire at peak energy = 3×",       icon:"💥🏆",xp:85 },
  { id:"echolens_tap",      label:"Echo Lens",       desc:"Tap an Echo Lens target",                  icon:"🔮📡",xp:35 },
  { id:"echolens_focus",    label:"Focused Echo",    desc:"Tap Echo Lens in focus phase = 2×",        icon:"🔮🏆",xp:75 },
  { id:"pulse_wave_use",    label:"Pulse Wave",      desc:"Activate the Pulse Wave power-up",         icon:"〰️💥",xp:30 },
  { id:"pulse_wave_max",    label:"Wave Rider",      desc:"Pulse Wave clears 8+ targets total",       icon:"〰️🏆",xp:80 },
  { id:"coin_vortex_use",   label:"Coin Vortex",     desc:"Activate the Coin Vortex power-up",        icon:"🪙🌀",xp:30 },
  { id:"coin_vortex_max",   label:"Vortex Rich",     desc:"Coin Vortex earns 60+ total coins",        icon:"🪙🏆",xp:80 },
  { id:"frosthalo_tap",     label:"Frost Halo",      desc:"Tap a Frost Halo target",                  icon:"❄️🔱",xp:35 },
  { id:"frosthalo_freeze",  label:"Deep Freeze",     desc:"Tap Frost Halo at freeze peak = 3×",       icon:"❄️🏆",xp:85 },
  { id:"emberclock_tap",    label:"Ember Clock",     desc:"Tap an Ember Clock target",                icon:"🕐🔥",xp:35 },
  { id:"emberclock_clutch", label:"Clutch Burn",     desc:"Tap Ember Clock in final 20% lifetime",    icon:"🕐🏆",xp:80 },
  { id:"gravity_burst2_use",label:"Gravity Burst 2", desc:"Activate the Gravity Burst II power-up",   icon:"🌀💥",xp:30 },
  { id:"gravity_burst2_max",label:"Gravity Crush",   desc:"Gravity Burst II pulls 5+ targets at once",icon:"🌀🏆",xp:85 },
  { id:"score_avalanche_use",label:"Score Avalanche",desc:"Activate the Score Avalanche power-up",    icon:"🏔️💰",xp:30 },
  { id:"score_avalanche_max",label:"Avalanche Peak", desc:"Score Avalanche total bonus exceeds 600pts",icon:"🏔️🏆",xp:85 },
  { id:"prismcoil_tap",     label:"Prism Coil",      desc:"Tap a Prism Coil target",                  icon:"🌈🌀",xp:35 },
  { id:"prismcoil_sync",    label:"Coil Sync",       desc:"Tap Prism Coil at color sync = 3×",        icon:"🌈🏆",xp:85 },
  { id:"starveil_tap",      label:"Star Veil",       desc:"Tap a Star Veil target",                   icon:"⭐🌫️",xp:35 },
  { id:"starveil_reveal",   label:"Veil Reveal",     desc:"Tap Star Veil at reveal moment = 2×",      icon:"⭐🏆",xp:75 },
  { id:"inferno_chain_use", label:"Inferno Chain",   desc:"Activate the Inferno Chain power-up",      icon:"🔥⛓️",xp:30 },
  { id:"inferno_chain_max", label:"Inferno Peak",    desc:"Inferno Chain reaches ×8 multiplier",      icon:"🔥🏆",xp:85 },
  { id:"spectral_coin_use", label:"Spectral Coin",   desc:"Activate the Spectral Coin power-up",      icon:"👻💰",xp:30 },
  { id:"spectral_coin_max", label:"Spectral Rich",   desc:"Spectral Coin earns 80+ total coins",      icon:"👻🏆",xp:80 },
  { id:"driftorb_tap",      label:"Drift Orb",       desc:"Tap a Drift Orb target",                   icon:"🌀🔮",xp:35 },
  { id:"driftorb_still",    label:"Drift Still",     desc:"Tap Drift Orb when it momentarily stops",  icon:"🌀🏆",xp:80 },
  { id:"chronostar_tap",    label:"Chrono Star",     desc:"Tap a Chrono Star target",                 icon:"⭐⏱️",xp:35 },
  { id:"chronostar_apex",   label:"Time Apex",       desc:"Tap Chrono Star at apex phase = 3×",       icon:"⭐🏆",xp:85 },
  { id:"magma_wave_use",    label:"Magma Wave",      desc:"Activate the Magma Wave power-up",         icon:"🌋〰️",xp:30 },
  { id:"magma_wave_max",    label:"Magma Surge",     desc:"Magma Wave total AoE bonus exceeds 400pts",icon:"🌋🏆",xp:80 },
  { id:"cosmic_echo_use",   label:"Cosmic Echo",     desc:"Activate the Cosmic Echo power-up",        icon:"🌌🔊",xp:30 },
  { id:"cosmic_echo_max",   label:"Echo Infinity",   desc:"Cosmic Echo total bonus exceeds 500pts",   icon:"🌌🏆",xp:85 },
  { id:"fluxring2_tap",     label:"Flux Ring 2",     desc:"Tap a Flux Ring 2 target",                 icon:"〰️🔵",xp:35 },
  { id:"fluxring2_inner",   label:"Ring Core",       desc:"Tap Flux Ring 2 inside the inner ring",    icon:"〰️🏆",xp:80 },
  { id:"solarvortex_tap",   label:"Solar Vortex",    desc:"Tap a Solar Vortex target",                icon:"☀️🌀",xp:35 },
  { id:"solarvortex_eye",   label:"Vortex Eye",      desc:"Tap Solar Vortex at the calm center",      icon:"☀️🏆",xp:85 },
  { id:"frost_lance_use",   label:"Frost Lance",     desc:"Activate the Frost Lance power-up",        icon:"❄️🗡️",xp:30 },
  { id:"frost_lance_max",   label:"Lance Mastery",   desc:"Frost Lance pierces 6+ targets total",     icon:"❄️🏆",xp:85 },
  { id:"life_matrix_use",   label:"Life Matrix",     desc:"Activate the Life Matrix power-up",        icon:"❤️🔲",xp:30 },
  { id:"life_matrix_max",   label:"Matrix Full",     desc:"Life Matrix restores 4+ lives total",      icon:"❤️🏆",xp:80 },
  { id:"quantumburst2_tap", label:"Quantum Burst 2", desc:"Tap a Quantum Burst 2 target",             icon:"⚛️💥",xp:35 },
  { id:"quantumburst2_tiny",label:"Quantum Tiny",    desc:"Tap Quantum Burst 2 at smallest phase",    icon:"⚛️🏆",xp:85 },
  { id:"nebularing_tap",    label:"Nebula Ring",     desc:"Tap a Nebula Ring target",                 icon:"🌌🔱",xp:35 },
  { id:"nebularing_outer",  label:"Ring Edge",       desc:"Tap Nebula Ring on the outer edge = 2×",   icon:"🌌🏆",xp:75 },
  { id:"thunder_burst_use", label:"Thunder Burst",   desc:"Activate the Thunder Burst power-up",      icon:"⚡💥",xp:30 },
  { id:"thunder_burst_max", label:"Storm Lord",      desc:"Thunder Burst clears 5+ targets per burst",icon:"⚡🏆",xp:85 },
  { id:"shimmer_coin_use",  label:"Shimmer Coin",    desc:"Activate the Shimmer Coin power-up",       icon:"✨🪙",xp:30 },
  { id:"shimmer_coin_max",  label:"Shimmer Rich",    desc:"Shimmer Coin total bonus earns 100+ coins", icon:"✨🏆",xp:85 },
  { id:"glacierspike_tap",  label:"Glacier Spike",   desc:"Tap a Glacier Spike target",               icon:"🧊⚡",xp:35 },
  { id:"glacierspike_tip",  label:"Spike Tip",       desc:"Tap Glacier Spike at the very tip = 3×",   icon:"🧊🏆",xp:85 },
  { id:"plasmapulse2_tap",  label:"Plasma Pulse 2",  desc:"Tap a Plasma Pulse 2 target",              icon:"💜〰️",xp:35 },
  { id:"plasmapulse2_peak", label:"Pulse Crest",     desc:"Tap Plasma Pulse 2 at pulse peak = 3×",    icon:"💜🏆",xp:85 },
  { id:"score_quake_use",   label:"Score Quake",     desc:"Activate the Score Quake power-up",        icon:"🌍💥",xp:30 },
  { id:"score_quake_max",   label:"Quake Peak",      desc:"Score Quake total bonus exceeds 700pts",   icon:"🌍🏆",xp:90 },
  { id:"echo_life_use",     label:"Echo Life",       desc:"Activate the Echo Life power-up",          icon:"❤️〰️",xp:30 },
  { id:"echo_life_max",     label:"Echo Heal",       desc:"Echo Life restores 5+ lives total",        icon:"❤️🏆",xp:85 },
  { id:"darkpulsar_tap",    label:"Dark Pulsar",     desc:"Tap a Dark Pulsar target",                 icon:"🖤💫",xp:35 },
  { id:"darkpulsar_burst",  label:"Pulsar Burst",    desc:"Tap Dark Pulsar at burst phase = 3×",      icon:"🖤🏆",xp:85 },
  { id:"crystalbomb_tap",   label:"Crystal Bomb",    desc:"Tap a Crystal Bomb target",                icon:"💎💣",xp:35 },
  { id:"crystalbomb_safe",  label:"Safe Defuse",     desc:"Tap Crystal Bomb before it explodes = 2×", icon:"💎🏆",xp:80 },
  { id:"nova_cascade_use",  label:"Nova Cascade",    desc:"Activate the Nova Cascade power-up",       icon:"💥🌊",xp:30 },
  { id:"nova_cascade_max",  label:"Cascade Peak",    desc:"Nova Cascade total bonus exceeds 800pts",  icon:"💥🏆",xp:90 },
  { id:"time_warp2_use",    label:"Time Warp 2",     desc:"Activate the Time Warp 2 power-up",        icon:"⏱️×2",xp:30 },
  { id:"time_warp2_max",    label:"Warp Master",     desc:"Time Warp 2 extends target lifetime 8+ times",icon:"⏱️🏆",xp:85 },
  { id:"astralcore_tap",    label:"Astral Core",     desc:"Tap an Astral Core target",                icon:"🌟🔮",xp:35 },
  { id:"astralcore_align",  label:"Astral Align",    desc:"Tap Astral Core when rings align = 3×",    icon:"🌟🏆",xp:90 },
  { id:"lightbolt_tap",     label:"Light Bolt",      desc:"Tap a Light Bolt target",                  icon:"⚡💡",xp:35 },
  { id:"lightbolt_charged", label:"Full Charge",     desc:"Tap Light Bolt at full charge = 3×",       icon:"⚡🏆",xp:85 },
  { id:"solar_pulse_use",   label:"Solar Pulse",     desc:"Activate the Solar Pulse power-up",        icon:"☀️💓",xp:30 },
  { id:"solar_pulse_max",   label:"Solar Peak",      desc:"Solar Pulse total bonus exceeds 600pts",   icon:"☀️🏆",xp:90 },
  { id:"void_chain_use",    label:"Void Chain",      desc:"Activate the Void Chain power-up",         icon:"🌑⛓️",xp:30 },
  { id:"void_chain_max",    label:"Chain Master",    desc:"Void Chain chains 10+ targets in one run", icon:"🌑🏆",xp:85 },
  { id:"solareye_tap",      label:"Solar Eye",       desc:"Tap a Solar Eye target",                   icon:"☀️👁️",xp:35 },
  { id:"solareye_open",     label:"Eye Open",        desc:"Tap Solar Eye with iris fully open = 3×",  icon:"☀️🏆",xp:90 },
  { id:"voidserpent_tap",   label:"Void Serpent",    desc:"Tap a Void Serpent target",                icon:"🌑🐍",xp:35 },
  { id:"voidserpent_vert",  label:"Serpent Strike",  desc:"Tap Void Serpent when vertical = 2×",      icon:"🌑🏆",xp:85 },
  { id:"ember_wave_use",    label:"Ember Wave",      desc:"Activate the Ember Wave power-up",         icon:"🔥〰️",xp:30 },
  { id:"ember_wave_max",    label:"Ember Surge",     desc:"Ember Wave total bonus exceeds 700pts",    icon:"🔥🏆",xp:90 },
  { id:"crystal_net_use",   label:"Crystal Net",     desc:"Activate the Crystal Net power-up",        icon:"💎🕸️",xp:30 },
  { id:"crystal_net_max",   label:"Net Master",      desc:"Crystal Net slows 8+ targets in one run",  icon:"💎🏆",xp:85 },
  { id:"moonpetal_tap",     label:"Moon Petal",      desc:"Tap a Moon Petal target",                  icon:"🌙🌸",xp:35 },
  { id:"moonpetal_bloom",   label:"Full Bloom",      desc:"Tap Moon Petal when fully bloomed = 3×",   icon:"🌙🏆",xp:90 },
  { id:"stardust2_tap",     label:"Star Dust II",    desc:"Tap a Star Dust II target",                icon:"⭐✨",xp:35 },
  { id:"stardust2_trail",   label:"Dust Trail",      desc:"Tap Star Dust II leaving longest trail",   icon:"⭐🏆",xp:85 },
  { id:"prism_echo_use",    label:"Prism Echo",      desc:"Activate the Prism Echo power-up",         icon:"🌈🔊",xp:30 },
  { id:"prism_echo_max",    label:"Echo Peak",       desc:"Prism Echo duplicates bonus 6+ times",     icon:"🌈🏆",xp:90 },
  { id:"volt_surge_use",    label:"Volt Surge",      desc:"Activate the Volt Surge power-up",         icon:"⚡🔥",xp:30 },
  { id:"volt_surge_max",    label:"Volt Max",        desc:"Volt Surge total bonus exceeds 800pts",    icon:"⚡🏆",xp:85 },
  { id:"icenova2_tap",      label:"Ice Nova II",     desc:"Tap an Ice Nova II target",                icon:"❄️💥",xp:35 },
  { id:"icenova2_freeze",   label:"Deep Freeze",     desc:"Tap Ice Nova II at freeze peak = 3×",      icon:"❄️🏆",xp:90 },
  { id:"arcgem_tap",        label:"Arc Gem",         desc:"Tap an Arc Gem target",                    icon:"💎⚡",xp:35 },
  { id:"arcgem_charged",    label:"Arc Charged",     desc:"Tap Arc Gem at full charge = 3×",          icon:"💎🏆",xp:85 },
  { id:"shadow_bloom_use",  label:"Shadow Bloom",    desc:"Activate the Shadow Bloom power-up",       icon:"🌑🌸",xp:30 },
  { id:"shadow_bloom_max",  label:"Dark Bloom",      desc:"Shadow Bloom absorbs 4+ misses",           icon:"🌑🏆",xp:90 },
  { id:"nexus_wave_use",    label:"Nexus Wave",      desc:"Activate the Nexus Wave power-up",         icon:"🌐〰️",xp:30 },
  { id:"nexus_wave_max",    label:"Wave Master",     desc:"Nexus Wave total bonus exceeds 900pts",    icon:"🌐🏆",xp:85 },
  { id:"flameorb2_tap",     label:"Flame Orb II",    desc:"Tap a Flame Orb II target",                icon:"🔥🔮",xp:35 },
  { id:"flameorb2_peak",    label:"Flame Peak",      desc:"Tap Flame Orb II at peak heat = 3×",       icon:"🔥🏆",xp:90 },
  { id:"thorgem_tap",       label:"Thor Gem",        desc:"Tap a Thor Gem target",                    icon:"⚡💎",xp:35 },
  { id:"thorgem_storm",     label:"Storm Strike",    desc:"Tap Thor Gem during storm phase = 2×",     icon:"⚡🏆",xp:85 },
  { id:"quantum_echo_use",  label:"Quantum Echo",    desc:"Activate the Quantum Echo power-up",       icon:"🔮🔊",xp:30 },
  { id:"quantum_echo_max",  label:"Echo Cascade",    desc:"Quantum Echo creates 8+ echo bonuses",     icon:"🔮🏆",xp:90 },
  { id:"frost_crown_use",   label:"Frost Crown",     desc:"Activate the Frost Crown power-up",        icon:"❄️👑",xp:30 },
  { id:"frost_crown_max",   label:"Crown Max",       desc:"Frost Crown total bonus exceeds 1000pts",  icon:"❄️🏆",xp:85 },
  { id:"cosmiclens_tap",    label:"Cosmic Lens",     desc:"Tap a Cosmic Lens target",                 icon:"🌌🔭",xp:35 },
  { id:"cosmiclens_focus",  label:"Cosmic Focus",    desc:"Tap Cosmic Lens when focused = 3×",        icon:"🌌🏆",xp:90 },
  { id:"nebulaburst2_tap",  label:"Nebula Burst II", desc:"Tap a Nebula Burst II target",             icon:"🌠💥",xp:35 },
  { id:"nebulaburst2_nova", label:"Nova Release",    desc:"Tap Nebula Burst II at nova moment = 3×",  icon:"🌠🏆",xp:85 },
];

const MISSION_TEMPLATES = [
  { id:"tap_30",    desc:"Tap 30 targets! 👆",          key:"tapsTotal",        goal:30  },
  { id:"rare_5",    desc:"Tap 5 Magic+ targets ✨",      key:"rareHits",         goal:5   },
  { id:"combo_15",  desc:"Reach a 15× tap chain 🔥",    key:"bestCombo",        goal:15  },
  { id:"boss_1",    desc:"Defeat a world boss 🐉",       key:"bossKills",        goal:1   },
  { id:"fever_2",   desc:"Activate Magic Mode 2× ⚡",   key:"feverCount",       goal:2   },
  { id:"perfect_5", desc:"5 Perfect Taps 🎯",           key:"perfectTaps",      goal:5   },
  { id:"levels_3",  desc:"Finish 3 levels 🏆",          key:"levelsCompleted",  goal:3   },
  { id:"powerup_3", desc:"Collect 3 power-ups 🔮",      key:"powerupCollected", goal:3   },
  { id:"score_500", desc:"Score 500 points in a level", key:"score",            goal:500 },
];

const SHOP_ITEMS = [
  { id:"extra_life",   name:"Magic Potion 🧪",  desc:"Start with +1 extra life",            cost:60,  icon:"🧪" },
  { id:"head_start",   name:"Lucky Start ⭐",    desc:"+300 bonus points at the start",      cost:80,  icon:"⭐" },
  { id:"shield_start", name:"Magic Shield 🛡️",  desc:"Begin with a Magic Shield",           cost:100, icon:"🛡️" },
  { id:"power_pack",   name:"Power Pack 🔮",     desc:"Start with a random power-up",        cost:120, icon:"🔮" },
  { id:"fever_potion", name:"Fever Potion 🌡️",  desc:"Start in instant Fever Mode!",        cost:90,  icon:"🌡️" },
  { id:"xp_bomb",      name:"XP Bomb ⚡",        desc:"2× XP reward from this level",        cost:70,  icon:"⚡" },
  { id:"streak_saver", name:"Streak Saver 💛",   desc:"Keep streak on next miss (once)",     cost:50,  icon:"💛" },
  { id:"time_warp_start",name:"Time Warp ⏱️",  desc:"Start with Time Warp active (6s slow)", cost:110, icon:"⏱️" },
  { id:"double_spawn", name:"Target Rush 🌊",   desc:"2× target spawn rate from the start",  cost:85,  icon:"🌊" },
];

// ── Prestige cosmetics — unique title + badge per prestige level (12A) ──
const PRESTIGE_TIERS = [
  { level:0, title:null,               badge:null,   color:"#ffffff" },
  { level:1, title:"Dragon Slayer",    badge:"🐉",   color:"#ff6b35",
    desc:"Defeated the Dragon — eternal glory awaits" },
  { level:2, title:"Arcane Scholar",   badge:"🔮",   color:"#b06de8",
    desc:"Mastered the arcane arts beyond mortal limits" },
  { level:3, title:"Frost Warlord",    badge:"❄️",   color:"#6ec0f5",
    desc:"Conquered the Viking Fjords in the frozen north" },
  { level:4, title:"Cosmic Voyager",   badge:"🚀",   color:"#ffd700",
    desc:"Traveled all 10 worlds — the stars themselves bow" },
  { level:5, title:"NEXUS LEGEND",     badge:"🌈",   color:"#ff00ff",
    desc:"All 5 prestiges achieved — a living legend of NexusTap" },
];

// ═══════════════════════════════════════════════════════════════
// STORY DATA
// ═══════════════════════════════════════════════════════════════
const MAIN_STORY = {
  title: "The Rainbow Crystal Quest",
  intro: [
    "Long ago, the Rainbow Dragon kept 10 magical crystals that made the world colourful and happy. ✨",
    "One stormy night a mischievous wizard zapped the crystals away — sending one into each magical world! 🌩️",
    "Now YOU must travel through all 10 worlds, tap your way past creatures and bosses, and bring the crystals home! 🌈",
  ],
};

const BOSS_TAUNTS = {
  phase2: ["You dare challenge me?!","Is that all you've got?","My power grows stronger!","You can't stop me now!","Feel my wrath, tapper!"],
  phase3: ["IMPOSSIBLE! I'm invincible!","You'll regret this!!!","MAXIMUM POWER UNLEASHED!","I won't go down easily!","THIS IS MY FINAL FORM!!!"],
  dying:  ["No... this can't be...","You're... stronger than I thought...","Take the crystal... you earned it...","I'll be back... stronger...","Well played, young tapper..."],
};

const WORLD_STORIES = [
  {
    worldId: 1,
    title: "Dragon's Lair",
    panels: [
      { emoji:"🐉", text:"Deep inside a glowing cave lives Ember the Dragon. He accidentally swallowed a crystal and now sneezes fire everywhere! 🔥" },
      { emoji:"😮", text:"All the little dragon hatchlings are running around in a panic — bouncing off the walls!" },
      { emoji:"👆", text:"Tap the fireballs and gems to calm everything down. Help Ember find peace and the crystal will pop right out!" },
    ],
    bossIntro: { emoji:"🐉🔥", title:"EMBER THE DRAGON!", text:"Ember is on his last sneeze! Tap him gently to help him feel better and release the crystal!" },
    victory: "Ember sneezed out the crystal — and said THANK YOU with a happy roar! 🐉💎 World 2 unlocked!",
  },
  {
    worldId: 2,
    title: "Troll Forest",
    panels: [
      { emoji:"🌿", text:"The Troll Forest is usually a quiet, happy place — but the crystal landed in the mushroom patch and now the trolls are dancing and bouncing EVERYWHERE! 🍄" },
      { emoji:"🧌", text:"Big Bork the Troll Chief is doing the wildest dance of all! He can't stop! The magic crystal is making him wiggle!" },
      { emoji:"👆", text:"Tap the bouncing mushrooms and help the trolls calm their wiggles! The crystal will be yours!" },
    ],
    bossIntro: { emoji:"🧌🍄", title:"BORK THE TROLL CHIEF!", text:"Big Bork is wiggling so hard the whole forest is shaking! Tap him in rhythm to stop the dance!" },
    victory: "Bork finally stopped dancing and gave you the crystal as a thank-you gift! 🍄💎 World 3 unlocked!",
  },
  {
    worldId: 3,
    title: "Elven Kingdom",
    panels: [
      { emoji:"✨", text:"The Elven Kingdom glitters with golden light — but ever since the crystal landed here, the elves have been shooting sparkling arrows in every direction! 🏹" },
      { emoji:"🧝", text:"Queen Elara is trying to cast a calming spell but her wand keeps shooting rainbow stars!" },
      { emoji:"👆", text:"Tap the magical sparks before they disappear — collect enough and you'll earn the crystal!" },
    ],
    bossIntro: { emoji:"🧝‍♀️✨", title:"QUEEN ELARA!", text:"The queen's magic is going wild! Tap her sparkling stars to absorb the excess magic — then the crystal will appear!" },
    victory: "Queen Elara's magic calmed down and the crystal floated right into your hands! ✨💎 World 4 unlocked!",
  },
  {
    worldId: 4,
    title: "Magic Tower",
    panels: [
      { emoji:"🔮", text:"In the tallest tower in the land lives Professor Zap, a very friendly wizard — but his spells went haywire when the crystal crashed through his window! 🪟" },
      { emoji:"🧙", text:"Magic orbs, potion bubbles and sparkle bolts are flying around the tower!" },
      { emoji:"👆", text:"Tap the flying magic orbs before they explode — help Professor Zap clean up his tower!" },
    ],
    bossIntro: { emoji:"🧙‍♂️🔮", title:"PROFESSOR ZAP!", text:"Zap has accidentally turned himself into a giant magic orb! Tap him to bring him back to normal!" },
    victory: "Zap popped back to normal and thanked you with the crystal and a bag of magic sweets! 🔮💎 World 5 unlocked!",
  },
  {
    worldId: 5,
    title: "Viking Fjords",
    panels: [
      { emoji:"❄️", text:"The Viking Fjords are icy and beautiful — but the crystal landed on a frozen lake and now the Vikings are having the biggest snowball fight EVER! ❄️🏔️" },
      { emoji:"⛵", text:"Chief Björn is throwing snowballs so fast that even his reindeer are scared!" },
      { emoji:"👆", text:"Tap the snowballs before they land! Survive the blizzard and Chief Björn will give you the crystal!" },
    ],
    bossIntro: { emoji:"⛵❄️", title:"CHIEF BJÖRN!", text:"Björn is spinning on the ice throwing snowballs in every direction! Tap them all to win the crystal!" },
    victory: "Björn declared you the snowball champion and handed over the crystal with a big Viking smile! ❄️💎 World 6 unlocked!",
  },
  {
    worldId: 6,
    title: "Goblin Mines",
    panels: [
      { emoji:"⛏️", text:"The Goblin Mines are full of glittering gems and happy little goblins — but the crystal fell deep in the mine and now ALL the goblins are super excited, throwing gems everywhere! 💎" },
      { emoji:"🟢", text:"The Goblin Chief is so happy he's spinning in circles, flinging gold coins and crystals in all directions!" },
      { emoji:"👆", text:"Tap the flying gems and gold coins! Collect them all to earn the magical crystal!" },
    ],
    bossIntro: { emoji:"⛏️💰", title:"THE GOBLIN CHIEF!", text:"The Chief is dizzy with excitement! Tap him and his flying treasure to claim the magic crystal!" },
    victory: "The Goblin Chief was SO happy to share his treasure with a brave hero like you! ⛏️💎 World 7 unlocked!",
  },
  {
    worldId: 7,
    title: "Haunted Castle",
    panels: [
      { emoji:"👻", text:"Don't be scared — this is actually a FRIENDLY haunted castle! The ghosts love to play hide-and-seek, but the crystal has made them invisible for real! 🏰" },
      { emoji:"😄", text:"Giggles the Ghost is laughing so hard you can barely see him at all!" },
      { emoji:"👆", text:"Tap the spots where ghosts are hiding before they giggle away! Find Giggles last for the crystal!" },
    ],
    bossIntro: { emoji:"👻🎃", title:"GIGGLES THE GHOST!", text:"Giggles is nearly invisible from all the laughing! Tap him when you see a shimmer to win the crystal!" },
    victory: "Giggles laughed so hard the crystal fell out of his pocket! The nicest ghost ever! 👻💎 World 8 unlocked!",
  },
  {
    worldId: 8,
    title: "Ocean Deep",
    panels: [
      { emoji:"🌊", text:"Under the sparkling ocean live wonderful sea creatures — but the crystal sank to the bottom and now everyone is blowing bubbles in total excitement! 🐠🐙" },
      { emoji:"🐋", text:"Wally the Friendly Whale is blowing so many bubbles that the whole ocean looks like a giant bubble bath!" },
      { emoji:"👆", text:"Pop the colourful bubbles before they float to the surface! Pop them all and Wally will give you the crystal!" },
    ],
    bossIntro: { emoji:"🐋🫧", title:"WALLY THE WHALE!", text:"Wally is the biggest bubble-blower in the whole ocean! Tap his bubbles to win the crystal!" },
    victory: "Wally sang you a happy whale song and pushed the crystal to shore with his nose! 🌊💎 World 9 unlocked!",
  },
  {
    worldId: 9,
    title: "Giant's Peak",
    panels: [
      { emoji:"⛰️", text:"High in the clouds live the gentlest giants you've ever met — but the crystal got stuck in a storm cloud and now boulders are rolling down the mountain! 🪨⚡" },
      { emoji:"🌩️", text:"Grumble the Giant is stomping around trying to catch the boulders but his big feet keep making things worse!" },
      { emoji:"👆", text:"Tap the rolling boulders before they reach the bottom! Help Grumble and he'll fish out the crystal!" },
    ],
    bossIntro: { emoji:"⛰️🌩️", title:"GRUMBLE THE GIANT!", text:"Grumble is stomping through a thunderstorm! Tap his boulders to calm the mountain!" },
    victory: "Grumble finally caught all the boulders and found the crystal in his pocket all along! ⛰️💎 World 10 unlocked!",
  },
  {
    worldId: 10,
    title: "Rainbow Dragon",
    panels: [
      { emoji:"🌈", text:"You've made it to the magical sky palace where the Rainbow Dragon lives! All the other creatures cheered you on every step of the way! 🎉" },
      { emoji:"🐲", text:"The Rainbow Dragon has been waiting for a true hero to return the last crystal — this is your moment!" },
      { emoji:"👆", text:"The Rainbow Dragon will test you with its most amazing rainbow powers! Tap everything you see to prove you are the QUEST MASTER!" },
    ],
    bossIntro: { emoji:"🌈👑", title:"THE RAINBOW DRAGON!", text:"The greatest challenge of the whole adventure! Tap every single rainbow burst to prove your skill!" },
    victory: "🌈 YOU DID IT! 🌈 All 10 crystals returned! The Rainbow Dragon crowned you the greatest hero in all the land! 🎊👑",
  },
];

const DEFAULT_SAVE = {
  highScore:0, xp:0, bestStreak:0, coins:0, totalCoins:0,
  unlockedAchievements:[], themeId:"neon", soundEnabled:true,
  scores:[], lastLoginDate:null, loginStreak:0,
  missionDate:null, missionProgress:{}, missionCompleted:false,
  levelStars:{}, unlockedLevel:1,
  seenWorldStories:[], seenMainStory:false, seenBossIntros:[],
  seenTutorial:false, lastSpinDate:null, mascotId:"dragon",
  unlockedMascots:["dragon","fox"],
  // New feature fields
  prestigeLevel:0,       // 0-5 prestiges, each adds +5% score mult
  mascotXP:{},           // { mascotId: totalXP }
  musicVol:80,           // 0-100
  sfxVol:80,             // 0-100
  hapticEnabled:true,    // vibration on/off
  hapticIntensity:70,    // vibration scale 10-100
  speedMode:1.0,         // 0.7=easy, 1.0=normal, 1.3=hard, 1.6=expert
  colorblindMode:false,  // rarity symbols instead of color-only
  infinityBest:0,        // best score in infinity mode
  infinityScores:[],     // top 10 infinity scores
  gauntletDate:null,     // date string of last gauntlet attempt
  gauntletBest:0,        // bosses defeated in best gauntlet run
  mascotAccessories:{},  // { mascotId: accessoryId } — currently equipped
  ownedAccessories:{},   // { "mascotId:accessoryId": true } — purchased
  weeklyChallengeDate:null, // week key of last weekly attempt
  weeklyChallengeCompleted:false, // true if this week's weekly was beaten
  weeklyChallengeBest:0, // best score on this week's weekly
  tournamentDate:null,    // day key of today's tournament attempt
  tournamentBest:0,       // best score today
  tournamentHistory:[],   // [{date, score, rank}] last 7 days
  bossFragments:0,        // cosmetic fragments from boss loot (10 = 1 accessory unlock)
  bossLootHistory:[],     // [{type, amount, date}] last 20 drops
  skillPoints:0,          // earned via XP milestones, spent in skill tree
  skills:{},              // { skillId: level }
  adaptiveDifficulty:false, // auto-adjusts spawn rate to keep player challenged
  zenBest:0,               // best score in zen mode
  taBest:0,                // best score in time attack mode
  zenBestWorld:1,          // world of best zen run
  lastActiveTime:null,     // timestamp (ms) when player last closed/backgrounded app
  worldBest:{},            // { worldId: bestScore } — per-world records (12B)
  lastWeeklyRecap:null,    // weekKey of last shown weekly recap (12C)
};

// ── Offline coin calculation (max 4 hours, 1 coin per 10s) ──
function calcOfflineCoins(lastActiveMs) {
  if(!lastActiveMs) return 0;
  const elapsed=Date.now()-lastActiveMs;
  const maxMs=4*60*60*1000; // 4 hours
  const cappedMs=Math.min(elapsed,maxMs);
  return Math.floor(cappedMs/10000); // 1 coin every 10 seconds
}

// ═══════════════════════════════════════════════════════════════
// AUDIO ENGINE
// ═══════════════════════════════════════════════════════════════
function createAudio() {
  let ctx = null;
  // Master chain nodes — initialized on first audio context creation
  let comp = null, revConv = null, revWet = null;
  const C = () => {
    if(!ctx) {
      ctx = new(window.AudioContext||window.webkitAudioContext)();
      // Dynamics compressor — adds punch, prevents clipping, makes hits feel physical
      comp = ctx.createDynamicsCompressor();
      comp.threshold.value = -18; comp.knee.value = 12;
      comp.ratio.value = 4; comp.attack.value = 0.003; comp.release.value = 0.28;
      comp.connect(ctx.destination);
      // Programmatic reverb impulse — adds room/space to big moments
      const len = Math.floor(ctx.sampleRate * 1.1);
      const ir = ctx.createBuffer(2, len, ctx.sampleRate);
      for(let ch=0;ch<2;ch++){const d=ir.getChannelData(ch);for(let i=0;i<len;i++)d[i]=(Math.random()*2-1)*Math.pow(1-i/len,2.5);}
      revConv = ctx.createConvolver(); revConv.buffer = ir;
      revWet = ctx.createGain(); revWet.gain.value = 0.22;
      revConv.connect(revWet); revWet.connect(comp);
    }
    return ctx;
  };
  // Dry sound — routes through compressor only
  const t = (freq,type,dur,vol=0.26,delay=0)=>{
    try{
      const c=C(),o=c.createOscillator(),g=c.createGain();
      o.connect(g);g.connect(comp);
      o.type=type;o.frequency.setValueAtTime(freq,c.currentTime+delay);
      g.gain.setValueAtTime(vol,c.currentTime+delay);
      g.gain.exponentialRampToValueAtTime(0.001,c.currentTime+delay+dur);
      o.start(c.currentTime+delay);o.stop(c.currentTime+delay+dur+0.05);
    }catch{}
  };
  // Reverb sound — dry + wet send for big moments (boss kill, legendary, jackpot)
  const tR = (freq,type,dur,vol=0.26,delay=0)=>{
    try{
      const c=C(),o=c.createOscillator(),g=c.createGain(),rv=c.createGain();
      o.connect(g);g.connect(comp);g.connect(rv);rv.gain.value=0.5;rv.connect(revConv);
      o.type=type;o.frequency.setValueAtTime(freq,c.currentTime+delay);
      g.gain.setValueAtTime(vol,c.currentTime+delay);
      g.gain.exponentialRampToValueAtTime(0.001,c.currentTime+delay+dur);
      o.start(c.currentTime+delay);o.stop(c.currentTime+delay+dur+0.05);
    }catch{}
  };
  const chord=(freqs,type,dur,vol,dt=0.06)=>freqs.forEach((f,i)=>t(f,type,dur,vol,i*dt));
  const chordR=(freqs,type,dur,vol,dt=0.06)=>freqs.forEach((f,i)=>tR(f,type,dur,vol,i*dt));
  return{
    tap:        ()=>{t(540,"sine",0.09,0.22);t(810,"sine",0.07,0.1,0.04);},
    miss:       ()=>{t(160,"sawtooth",0.38,0.22);},
    uncommon:   ()=>{t(600,"sine",0.12,0.24);t(900,"sine",0.08,0.16,0.07);},
    rare:       ()=>{chord([660,880,1100],"sine",0.18,0.26,0.07);},
    epic:       ()=>{chord([440,554,659,880,1108],"sine",0.22,0.26,0.065);},
    legendary:  ()=>{chordR([330,440,550,660,880,1100,1320],"sine",0.28,0.28,0.055);},
    boss:       ()=>{t(100,"sawtooth",0.5,0.32);t(140,"square",0.3,0.18,0.1);},
    bossHit:    ()=>{t(200,"sawtooth",0.2,0.28);t(260,"sine",0.12,0.18,0.06);},
    bossKill:   ()=>{chordR([262,330,392,523,659],"sine",0.35,0.3,0.08);},
    bombSpawn:  ()=>{t(110,"sawtooth",0.4,0.28);t(75,"sawtooth",0.22,0.18,0.14);},
    bombHit:    ()=>{t(90,"sawtooth",0.55,0.36);t(55,"sawtooth",0.28,0.26,0.2);},
    powerUp:    ()=>{chord([440,554,659,880],"sine",0.14,0.22,0.055);},
    feverStart: ()=>{chordR([440,554,659,880,1108],"square",0.12,0.18,0.05);},
    feverEnd:   ()=>{chord([880,659,554,440],"sine",0.14,0.18,0.07);},
    levelUp:    ()=>{chord([523,659,784,1047,1319],"sine",0.18,0.28,0.08);},
    levelComplete:()=>{chordR([523,659,784,1047],"sine",0.22,0.3,0.09);},
    perfect:    ()=>{chord([880,1108,1320],"sine",0.14,0.26,0.06);},
    countdown:  ()=>{t(440,"sine",0.15,0.28);},
    go:         ()=>{chord([523,659,784],"sine",0.2,0.3,0.05);},
    gameOver:   ()=>{chord([440,370,294,220],"sawtooth",0.25,0.18,0.1);},
    coin:       ()=>{t(1200,"sine",0.08,0.16);t(1500,"sine",0.05,0.12,0.07);},
    starEarn:   ()=>{tR(880,"sine",0.15,0.25);tR(1108,"sine",0.1,0.2,0.12);},
    unlock:     ()=>{chordR([440,554,659,784],"sine",0.2,0.22,0.07);},
    waveUp:     ()=>{chord([330,415,523,659],"square",0.2,0.2,0.07);},
    // Musical pentatonic scale — each streak note rises chromatically
    // C major pentatonic: C5 D5 E5 G5 A5 C6 D6 E6 G6 A6 C7
    comboNote:  (streak)=>{
      const scale=[523,587,659,784,880,1047,1175,1319,1568,1760,2093,2349];
      const n=scale[Math.min(Math.max(0,streak-1),scale.length-1)];
      const vol=0.13+Math.min(0.09,streak*0.004);
      t(n,"sine",0.09,vol);
      if(streak>=5)t(n*1.498,"sine",0.065,vol*0.4,0.02);  // perfect 5th
      if(streak>=10)t(n*2,"sine",0.045,vol*0.25,0.04);     // octave
      if(streak>=20)t(n*3,"sine",0.03,vol*0.15,0.06);      // 12th
    },
    // Per-mascot combo note — same ratios as comboNote but custom base+wave
    mascotNote: (streak,base,wave)=>{
      const ratios=[1,1.122,1.26,1.498,1.682,2,2.245,2.52,2.996,3.364,4,4.49];
      const n=base*ratios[Math.min(Math.max(0,streak-1),ratios.length-1)];
      const vol=0.13+Math.min(0.09,streak*0.004);
      t(n,wave||"sine",0.09,vol);
      if(streak>=5)t(n*1.498,"sine",0.065,vol*0.4,0.02);
      if(streak>=10)t(n*2,"sine",0.045,vol*0.25,0.04);
      if(streak>=20)t(n*3,"sine",0.03,vol*0.15,0.06);
    },
    chainBonus: ()=>{chord([784,1047,1319],"square",0.1,0.22,0.04);t(1568,"sine",0.08,0.18,0.14);},
    flawless:   ()=>{chordR([523,659,784,1047,1319,1568],"sine",0.24,0.3,0.07);tR(2093,"sine",0.2,0.28,0.6);},
    hotStreak:  ()=>{chord([440,554,659,880],"sawtooth",0.12,0.2,0.055);},
    treasure:   ()=>{chordR([523,659,784,1047,1319,1568],"sine",0.22,0.26,0.06);tR(2093,"sine",0.12,0.2,0.38);},
    jackpot:    ()=>{
      [523,659,784,1047,1319,1568,2093].forEach((f,i)=>tR(f,"sine",0.18,0.28,i*0.055));
      setTimeout(()=>chordR([1047,1319,1568,2093],"sine",0.18,0.24,0.04),500);
    },
    shieldBreak:()=>{t(660,"sine",0.12,0.22);t(440,"sawtooth",0.18,0.16,0.08);},
    lucky:      ()=>{chordR([784,1047,1319,1568],"sine",0.14,0.24,0.06);},
    bossPhase:  ()=>{t(110,"sawtooth",0.4,0.36);t(160,"square",0.3,0.24,0.08);chord([440,330],"sawtooth",0.22,0.18,0.18);},
    prestige:   ()=>{[523,659,784,1047,1319,1568,2093,2349].forEach((f,i)=>tR(f,"sine",0.22,0.32,i*0.07));},
    infinityWin:()=>{chordR([523,659,784,1047,1319],"sine",0.2,0.28,0.06);setTimeout(()=>chordR([1047,1319,1568,2093],"sine",0.18,0.24,0.05),550);},
    nearMiss:   ()=>{chord([262,330,392,523],"sine",0.2,0.2,0.1);},
    // ── Background music ──
    startBgMusic:(wid)=>{
      const WM=[
        [196,220,247,262,294,262,247,220], // Dragon
        [330,370,415,440,494,440,415,370], // Forest
        [440,494,554,587,659,587,554,494], // Elven
        [262,294,330,370,415,370,330,294], // Magic
        [174,196,220,247,262,247,220,196], // Viking
        [220,247,262,294,330,294,262,247], // Goblin
        [196,220,247,262,247,220,196,174], // Haunted
        [294,330,370,415,440,415,370,330], // Ocean
        [262,294,330,370,415,440,415,370], // Giant
        [523,587,659,784,880,784,659,587], // Rainbow
      ];
      if(ctx&&ctx.state==="suspended")ctx.resume().catch(()=>{});
      const mel=WM[Math.max(0,(wid||1)-1)];
      let note=0,tmpo=460,tmr=null,on=true;
      const tick=()=>{
        if(!on)return;
        try{
          const c=C(),o=c.createOscillator(),g=c.createGain();
          o.connect(g);g.connect(comp);
          o.type="triangle";o.frequency.setValueAtTime(mel[note%mel.length],c.currentTime);
          g.gain.setValueAtTime(bgVol,c.currentTime);
          g.gain.exponentialRampToValueAtTime(0.001,c.currentTime+tmpo*0.0008);
          o.start(c.currentTime);o.stop(c.currentTime+tmpo*0.0009);
        }catch{}
        note++;
        tmr=setTimeout(tick,tmpo);
      };
      let bgVol=0.032;
      tick();
      bgMusicCtl={stop:()=>{on=false;if(tmr)clearTimeout(tmr);},setFever:(f)=>{tmpo=f?280:460;},setVol:(v)=>{bgVol=v;}};
    },
    stopBgMusic:()=>{bgMusicCtl&&bgMusicCtl.stop();bgMusicCtl=null;},
    setBgMusicFever:(f)=>{bgMusicCtl&&bgMusicCtl.setFever(f);},
  };
}
let bgMusicCtl=null;
let _bgBlast=null; // {x,y,at:performance.now()} — set on boss kill for bg particle explosion

// ═══════════════════════════════════════════════════════════════
// HELPERS
// ═══════════════════════════════════════════════════════════════
function getRarity(rarityBonus=0) {
  const r=Math.random();
  let cumul=0;
  const entries=Object.values(RARITY);
  // Shift probability toward rarer items
  const adj=entries.map((v,i)=>({...v,adjChance:i<2?Math.max(0.08,v.chance-rarityBonus*(i===0?0.7:0.3)):v.chance+rarityBonus*(i===2?0.4:i===3?0.35:0.25)}));
  for(const v of adj){ cumul+=v.adjChance; if(r<cumul) return v; }
  return RARITY.COMMON;
}

const getLvl   = xp => Math.floor(xp/XP_PER_LVL)+1;
const xpToNext = xp => XP_PER_LVL-(xp%XP_PER_LVL);
function seededRng(seed){let s=seed;return()=>{s=(s*1664525+1013904223)&0xffffffff;return(s>>>0)/0xffffffff;};}
function getDailyMissions(){const d=new Date();const seed=d.getFullYear()*10000+(d.getMonth()+1)*100+d.getDate();const rng=seededRng(seed);return[...MISSION_TEMPLATES].sort(()=>rng()-0.5).slice(0,3);}
function getTodayKey(){const d=new Date();return`${d.getFullYear()}-${d.getMonth()}-${d.getDate()}`;}
function loadSave(){try{const r=localStorage.getItem("nexustap_v5");if(r)return{...DEFAULT_SAVE,...JSON.parse(r)};}catch{}return{...DEFAULT_SAVE};}
// Module-level flags synced from save on game start
let _hapticOn = true;
let _colorblindOn = false;
let _hapticIntensity = 70; // 10-100, scales vibration duration
function vibrate(p){
  try{
    if(!_hapticOn||!navigator.vibrate)return;
    const scale=(_hapticIntensity??70)/70; // 1.0 = default
    const scaled=Array.isArray(p)?p.map(v=>Math.round(v*scale)):Math.round(p*scale);
    navigator.vibrate(scaled);
  }catch{}
}

// ═══════════════════════════════════════════════════════════════
// CANVAS DRAWING — ENHANCED ANIMATED SHAPES
// ═══════════════════════════════════════════════════════════════

// Urgency pulse: blinks when < 25% lifetime remains
function urgencyAlpha(timeLeft, ts) {
  if(timeLeft>0.25) return 1;
  return 0.55+0.45*Math.abs(Math.sin(ts/120));
}

// Draw orbiting dots around a center
function drawOrbiters(ctx, count, orbitR, dotR, color, ts, speed=1) {
  for(let i=0;i<count;i++){
    const a = ts*speed*0.001 + i*(Math.PI*2/count);
    const x = Math.cos(a)*orbitR, y = Math.sin(a)*orbitR;
    ctx.save();
    ctx.fillStyle=color; ctx.shadowColor=color; ctx.shadowBlur=dotR*3;
    ctx.globalAlpha=0.9;
    ctx.beginPath(); ctx.arc(x,y,dotR,0,Math.PI*2); ctx.fill();
    ctx.restore();
  }
}

// Radial light beams from center
function drawBeams(ctx, count, r, color, ts, speed=0.4) {
  const rot = ts*speed*0.001;
  for(let i=0;i<count;i++){
    const a = rot + i*(Math.PI*2/count);
    ctx.save();
    ctx.globalAlpha=0.18;
    ctx.strokeStyle=color;
    ctx.lineWidth=3;
    ctx.beginPath(); ctx.moveTo(0,0); ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r); ctx.stroke();
    ctx.restore();
  }
}

// Electric arc between two points
function drawArc(ctx, x1,y1,x2,y2, color, ts, id=0) {
  const steps=8, amp=4+2*Math.sin(ts*0.01+id);
  ctx.beginPath(); ctx.moveTo(x1,y1);
  for(let i=1;i<steps;i++){
    const p=i/steps;
    const mx=x1+(x2-x1)*p, my=y1+(y2-y1)*p;
    const nx=-(y2-y1)/Math.hypot(x2-x1,y2-y1), ny=(x2-x1)/Math.hypot(x2-x1,y2-y1);
    const jitter=amp*Math.sin(ts*0.015+id*7+i*1.3);
    ctx.lineTo(mx+nx*jitter, my+ny*jitter);
  }
  ctx.lineTo(x2,y2);
  ctx.strokeStyle=color; ctx.lineWidth=1.5; ctx.globalAlpha=0.6;
  ctx.shadowColor=color; ctx.shadowBlur=6;
  ctx.stroke();
}

// ── Common: Animated radar rings ──
function drawCommon(ctx, r, color, glow, ts, timeLeft) {
  const ua=urgencyAlpha(timeLeft,ts);
  ctx.save(); ctx.globalAlpha=ua;
  // Pulsing fill
  const pulse=0.5+0.5*Math.sin(ts*0.003);
  ctx.fillStyle=color; ctx.globalAlpha=ua*(0.08+pulse*0.06);
  ctx.beginPath(); ctx.arc(0,0,r,0,Math.PI*2); ctx.fill();
  // 3 rings rotating at different speeds
  const speeds=[0.5,0.8,-0.6]; const radii=[r*0.98,r*0.72,r*0.46];
  speeds.forEach((sp,i)=>{
    ctx.save(); ctx.rotate(ts*sp*0.001);
    ctx.strokeStyle=color; ctx.lineWidth= i===0?2.5:1.8; ctx.globalAlpha=ua*(0.8-i*0.15);
    ctx.shadowColor=glow; ctx.shadowBlur=8;
    // Dashed for inner rings
    if(i>0) ctx.setLineDash([radii[i]*0.4,radii[i]*0.2]);
    ctx.beginPath(); ctx.arc(0,0,radii[i],0,Math.PI*2); ctx.stroke();
    ctx.setLineDash([]);
    ctx.restore();
  });
  // Scanning laser line
  ctx.save(); ctx.rotate(ts*0.002);
  const scanGrad=ctx.createLinearGradient(0,0,r*0.92,0);
  scanGrad.addColorStop(0,color+"ff"); scanGrad.addColorStop(1,color+"00");
  ctx.strokeStyle=scanGrad; ctx.lineWidth=2; ctx.globalAlpha=ua*0.8;
  ctx.shadowColor=glow; ctx.shadowBlur=12;
  ctx.beginPath(); ctx.moveTo(0,0); ctx.lineTo(r*0.92,0); ctx.stroke();
  ctx.restore();
  // Pulsing center dot
  const dotR=r*0.18*(1+pulse*0.2);
  ctx.fillStyle=color; ctx.globalAlpha=ua*0.9; ctx.shadowColor=glow; ctx.shadowBlur=16;
  ctx.beginPath(); ctx.arc(0,0,dotR,0,Math.PI*2); ctx.fill();
  ctx.restore();
}

// ── Uncommon: Spinning diamond with orbiting sparks ──
function drawUncommon(ctx, r, color, glow, ts, timeLeft) {
  const ua=urgencyAlpha(timeLeft,ts);
  ctx.save(); ctx.globalAlpha=ua;
  // Outer diamond
  ctx.save(); ctx.rotate(ts*0.0008);
  ctx.shadowColor=glow; ctx.shadowBlur=r*1;
  const d=r-2;
  ctx.fillStyle=color+"28"; ctx.strokeStyle=color; ctx.lineWidth=2.5;
  ctx.beginPath(); ctx.moveTo(0,-d); ctx.lineTo(d*0.8,0); ctx.lineTo(0,d); ctx.lineTo(-d*0.8,0); ctx.closePath();
  ctx.fill(); ctx.stroke(); ctx.restore();
  // Inner diamond counter-rotating
  ctx.save(); ctx.rotate(-ts*0.0015);
  const id2=r*0.52;
  ctx.fillStyle=color+"55"; ctx.strokeStyle=color+"aa"; ctx.lineWidth=1.5;
  ctx.beginPath(); ctx.moveTo(0,-id2); ctx.lineTo(id2*0.8,0); ctx.lineTo(0,id2); ctx.lineTo(-id2*0.8,0); ctx.closePath();
  ctx.fill(); ctx.stroke(); ctx.restore();
  // 4 orbiting sparks (speed up when urgent)
  const orbSpeed = timeLeft<0.25?3.5:1.5;
  drawOrbiters(ctx,4,r*0.62,r*0.1,color,ts,orbSpeed);
  // Center
  const pulse=0.5+0.5*Math.sin(ts*0.004);
  ctx.fillStyle=color; ctx.globalAlpha=ua*0.9; ctx.shadowColor=glow; ctx.shadowBlur=14;
  ctx.beginPath(); ctx.arc(0,0,r*0.2*(1+pulse*0.15),0,Math.PI*2); ctx.fill();
  ctx.restore();
}

// ── Rare: 5-star with orbiting micro-stars ──
function drawRare(ctx, r, color, glow, ts, timeLeft) {
  const ua=urgencyAlpha(timeLeft,ts);
  const pulse=0.5+0.5*Math.sin(ts*0.0035);
  ctx.save(); ctx.globalAlpha=ua;
  // Glow aura
  const auraGrad=ctx.createRadialGradient(0,0,r*0.2,0,0,r*1.3);
  auraGrad.addColorStop(0,color+"40"); auraGrad.addColorStop(1,color+"00");
  ctx.fillStyle=auraGrad; ctx.beginPath(); ctx.arc(0,0,r*1.3,0,Math.PI*2); ctx.fill();
  // Star shape (rotating)
  ctx.save(); ctx.rotate(ts*0.0007);
  ctx.shadowColor=glow; ctx.shadowBlur=r*1.2;
  ctx.fillStyle=color+"44"; ctx.strokeStyle=color; ctx.lineWidth=2.5;
  ctx.beginPath();
  for(let i=0;i<10;i++){
    const a=i*Math.PI/5-Math.PI/2;
    const cr=i%2===0?r-2:r*0.42;
    i===0?ctx.moveTo(Math.cos(a)*cr,Math.sin(a)*cr):ctx.lineTo(Math.cos(a)*cr,Math.sin(a)*cr);
  }
  ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.restore();
  // Second star counter-rotating (slightly smaller)
  ctx.save(); ctx.rotate(-ts*0.0012);
  ctx.strokeStyle=color+"66"; ctx.lineWidth=1.5; ctx.fillStyle="transparent";
  ctx.beginPath();
  for(let i=0;i<10;i++){
    const a=i*Math.PI/5-Math.PI/2+Math.PI/10;
    const cr=i%2===0?(r-2)*0.75:r*0.35;
    i===0?ctx.moveTo(Math.cos(a)*cr,Math.sin(a)*cr):ctx.lineTo(Math.cos(a)*cr,Math.sin(a)*cr);
  }
  ctx.closePath(); ctx.stroke(); ctx.restore();
  // 5 orbiting micro-stars
  const orbSpeed=timeLeft<0.25?4:1.8;
  for(let i=0;i<5;i++){
    const a=ts*orbSpeed*0.001+i*(Math.PI*2/5);
    const ox=Math.cos(a)*(r+9), oy=Math.sin(a)*(r+9);
    ctx.save(); ctx.globalAlpha=ua*0.9; ctx.fillStyle=color; ctx.shadowColor=glow; ctx.shadowBlur=6;
    ctx.beginPath(); ctx.arc(ox,oy,r*0.1,0,Math.PI*2); ctx.fill(); ctx.restore();
  }
  // Inner glowing orb
  const coreR=r*0.22*(1+pulse*0.2);
  const coreGrad=ctx.createRadialGradient(0,0,0,0,0,coreR);
  coreGrad.addColorStop(0,"#fff"); coreGrad.addColorStop(0.5,color); coreGrad.addColorStop(1,glow);
  ctx.fillStyle=coreGrad; ctx.globalAlpha=ua*0.95; ctx.shadowColor=glow; ctx.shadowBlur=20;
  ctx.beginPath(); ctx.arc(0,0,coreR,0,Math.PI*2); ctx.fill();
  ctx.restore();
}

// ── Epic: Layered hexagons with electric arcs ──
function drawEpic(ctx, r, color, glow, ts, timeLeft) {
  const ua=urgencyAlpha(timeLeft,ts);
  const pulse=0.5+0.5*Math.sin(ts*0.004);
  ctx.save(); ctx.globalAlpha=ua;
  // Outer glow
  const auraGrad=ctx.createRadialGradient(0,0,r*0.3,0,0,r*1.5);
  auraGrad.addColorStop(0,color+"50"); auraGrad.addColorStop(1,color+"00");
  ctx.fillStyle=auraGrad; ctx.beginPath(); ctx.arc(0,0,r*1.5,0,Math.PI*2); ctx.fill();
  // Outer 6-star rotating
  ctx.save(); ctx.rotate(ts*0.0007);
  ctx.shadowColor=glow; ctx.shadowBlur=r*1.4;
  ctx.fillStyle=color+"35"; ctx.strokeStyle=color; ctx.lineWidth=2.8;
  ctx.beginPath();
  for(let i=0;i<12;i++){
    const a=i*Math.PI/6-Math.PI/2;
    const cr=i%2===0?r-2:r*0.5;
    i===0?ctx.moveTo(Math.cos(a)*cr,Math.sin(a)*cr):ctx.lineTo(Math.cos(a)*cr,Math.sin(a)*cr);
  }
  ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.restore();
  // Inner hexagon counter-rotating
  ctx.save(); ctx.rotate(-ts*0.0018);
  ctx.strokeStyle=color+"88"; ctx.lineWidth=2; ctx.fillStyle=color+"18";
  ctx.beginPath();
  for(let i=0;i<6;i++){
    const a=i*Math.PI/3-Math.PI/6;
    const cr=r*0.52;
    i===0?ctx.moveTo(Math.cos(a)*cr,Math.sin(a)*cr):ctx.lineTo(Math.cos(a)*cr,Math.sin(a)*cr);
  }
  ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.restore();
  // Electric arcs between star tips
  ctx.save(); ctx.rotate(ts*0.0007);
  const tips=Array.from({length:6},(_,i)=>{const a=i*Math.PI/3-Math.PI/2; return{x:Math.cos(a)*(r-2),y:Math.sin(a)*(r-2)};});
  ctx.globalAlpha=ua*0.7;
  for(let i=0;i<6;i++) drawArc(ctx,tips[i].x,tips[i].y,tips[(i+2)%6].x,tips[(i+2)%6].y,color,ts,i);
  ctx.restore();
  // 6 orbiting dots
  drawOrbiters(ctx,6,r*0.68,r*0.095,color,ts,timeLeft<0.25?5:2);
  // Bright center
  const coreGrad=ctx.createRadialGradient(0,0,0,0,0,r*(0.28+pulse*0.05));
  coreGrad.addColorStop(0,"#fff"); coreGrad.addColorStop(0.35,color); coreGrad.addColorStop(1,glow+"00");
  ctx.fillStyle=coreGrad; ctx.globalAlpha=ua; ctx.shadowColor=glow; ctx.shadowBlur=28;
  ctx.beginPath(); ctx.arc(0,0,r*(0.28+pulse*0.05),0,Math.PI*2); ctx.fill();
  ctx.restore();
}

// ── Legendary: Full animated explosion star ──
function drawLegendary(ctx, r, color, glow, ts, timeLeft) {
  const ua=urgencyAlpha(timeLeft,ts);
  const pulse=0.5+0.5*Math.sin(ts*0.005);
  const spin=ts*0.0015;
  ctx.save(); ctx.globalAlpha=ua;
  // Massive aura
  const auraGrad=ctx.createRadialGradient(0,0,0,0,0,r*2.0);
  auraGrad.addColorStop(0,color+"55"); auraGrad.addColorStop(0.5,color+"20"); auraGrad.addColorStop(1,color+"00");
  ctx.fillStyle=auraGrad; ctx.beginPath(); ctx.arc(0,0,r*2.0,0,Math.PI*2); ctx.fill();
  // Radial beams
  drawBeams(ctx,12,r*1.6,color,ts,0.5);
  // Outer rotating ring of dashes
  ctx.save(); ctx.rotate(spin*0.7);
  ctx.strokeStyle=color+"88"; ctx.lineWidth=2; ctx.setLineDash([r*0.3,r*0.15]);
  ctx.beginPath(); ctx.arc(0,0,r+8,0,Math.PI*2); ctx.stroke();
  ctx.setLineDash([]); ctx.restore();
  // 8-point star (main body)
  ctx.save(); ctx.rotate(spin);
  ctx.shadowColor=glow; ctx.shadowBlur=r*1.8;
  ctx.fillStyle=color+"45"; ctx.strokeStyle=color; ctx.lineWidth=3;
  ctx.beginPath();
  for(let i=0;i<16;i++){
    const a=i*Math.PI/8-Math.PI/2;
    const cr=i%2===0?r-2:r*0.45;
    i===0?ctx.moveTo(Math.cos(a)*cr,Math.sin(a)*cr):ctx.lineTo(Math.cos(a)*cr,Math.sin(a)*cr);
  }
  ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.restore();
  // Inner rotating star (counter)
  ctx.save(); ctx.rotate(-spin*1.5);
  ctx.strokeStyle=color+"77"; ctx.lineWidth=2; ctx.fillStyle=color+"22";
  ctx.beginPath();
  for(let i=0;i<16;i++){
    const a=i*Math.PI/8-Math.PI/2+Math.PI/16;
    const cr=i%2===0?(r-2)*0.68:r*0.32;
    i===0?ctx.moveTo(Math.cos(a)*cr,Math.sin(a)*cr):ctx.lineTo(Math.cos(a)*cr,Math.sin(a)*cr);
  }
  ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.restore();
  // 3 orbiting satellites at different radii
  [[r*0.88,3,0.9],[r*1.15,2,1.5],[r*1.35,1.5,2.2]].forEach(([orR,dotR,sp],i)=>{
    const a=ts*sp*0.001+i*(Math.PI*2/3);
    const ox=Math.cos(a)*orR, oy=Math.sin(a)*orR;
    ctx.save(); ctx.globalAlpha=ua*0.95; ctx.fillStyle=color; ctx.shadowColor=glow; ctx.shadowBlur=dotR*5;
    ctx.beginPath(); ctx.arc(ox,oy,dotR,0,Math.PI*2); ctx.fill(); ctx.restore();
  });
  // Radiant core
  const coreR=r*(0.22+pulse*0.06);
  const coreGrad=ctx.createRadialGradient(0,0,0,0,0,coreR);
  coreGrad.addColorStop(0,"#fff"); coreGrad.addColorStop(0.3,"#fff"); coreGrad.addColorStop(0.7,color); coreGrad.addColorStop(1,glow);
  ctx.fillStyle=coreGrad; ctx.globalAlpha=ua; ctx.shadowColor="#fff"; ctx.shadowBlur=40;
  ctx.beginPath(); ctx.arc(0,0,coreR,0,Math.PI*2); ctx.fill();
  ctx.restore();
}

// ── Bomb target ──
function drawBomb(ctx, r, color, glow, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.008);
  ctx.shadowColor="#ef4444"; ctx.shadowBlur=r*(0.8+pulse*1.2);
  // Dark body
  const bodyGrad=ctx.createRadialGradient(-r*0.2,-r*0.2,0,0,0,r);
  bodyGrad.addColorStop(0,"#3a0000"); bodyGrad.addColorStop(1,"#0a0000");
  ctx.fillStyle=bodyGrad; ctx.beginPath(); ctx.arc(0,0,r,0,Math.PI*2); ctx.fill();
  // Red ring
  ctx.strokeStyle="#ef4444"; ctx.lineWidth=3; ctx.beginPath(); ctx.arc(0,0,r,0,Math.PI*2); ctx.stroke();
  // Warning triangle
  ctx.fillStyle="#ef4444"; ctx.globalAlpha=0.9;
  const tw=r*0.55, th=r*0.55;
  ctx.beginPath(); ctx.moveTo(0,-th); ctx.lineTo(tw*0.86,th*0.5); ctx.lineTo(-tw*0.86,th*0.5); ctx.closePath(); ctx.fill();
  ctx.fillStyle="#1a0000"; ctx.font=`bold ${r*0.55}px sans-serif`; ctx.textAlign="center"; ctx.textBaseline="middle";
  ctx.fillText("!",0,r*0.08);
  // Spikes
  ctx.globalAlpha=0.4+pulse*0.5;
  ctx.strokeStyle="#ff6666"; ctx.lineWidth=1.5;
  for(let i=0;i<8;i++){
    const a=i*Math.PI/4, s=r*1.05, e=r*1.28+pulse*6;
    ctx.beginPath(); ctx.moveTo(Math.cos(a)*s,Math.sin(a)*s); ctx.lineTo(Math.cos(a)*e,Math.sin(a)*e); ctx.stroke();
  }
  ctx.globalAlpha=1;
}

// ── Power-up target ──
function drawPowerup(ctx, r, pwrType, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.005);
  ctx.shadowColor="#60a5fa"; ctx.shadowBlur=r*(0.8+pulse*0.6);
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#1a3060"); grad.addColorStop(1,"#060c20");
  ctx.fillStyle=grad; ctx.beginPath(); ctx.arc(0,0,r,0,Math.PI*2); ctx.fill();
  ctx.strokeStyle="#60a5fa"; ctx.lineWidth=2.5; ctx.beginPath(); ctx.arc(0,0,r,0,Math.PI*2); ctx.stroke();
  // Rotating outer ring
  ctx.save(); ctx.rotate(ts*0.002); ctx.strokeStyle="#60a5fa55"; ctx.lineWidth=1.5;
  ctx.setLineDash([r*0.35,r*0.18]); ctx.beginPath(); ctx.arc(0,0,r-4,0,Math.PI*2); ctx.stroke();
  ctx.setLineDash([]); ctx.restore();
  // Icon
  const icons={SHIELD:"🛡",SLOW:"🐢",DOUBLE:"×2",LIFE:"❤️",FREEZE:"❄️",LUCKY:"⭐",MIRROR:"🪞",MULTIPLIER:"×3",COMBO_FREEZE:"🧊",GRAVITY:"🌐",CHAIN_LIGHTNING:"⚡",TIME_WARP:"⏱️",SCORE_BOOST:"×5",LIFE_SURGE:"❤️+2",MAGNET_FIELD:"🧲",OVERCLOCK:"⚡",SHIELD_WALL:"🏰",PHANTOM_TOUCH:"👻",COIN_RAIN:"🪙",BERSERKER:"⚔️",GHOST_MODE:"👻",SCORE_SHIELD:"🔰",DOUBLE_SPAWN:"×2+",STAR_RAIN:"⭐",TIME_STOP:"⏸️",MEGA_TAP:"💥",RAINBOW_SURGE:"🌈",TWIN_STRIKE:"👥",GOLDEN_TOUCH:"✨",LUCK_SURGE:"🍀",FREEZE_ZONE:"❄️+",SCORE_X2:"×2!",MAGNET_PULL:"🧲+",COMBO_SHIELD:"🔒",PERFECT_AIM:"🎯",CHAIN_BURST:"🔥",MAGNET_BURST:"🧲💥",SCORE_PULSE:"💫",LIFE_SHIELD:"🛡️+",MULTIPLIER_SURGE:"⚡×",BOUNCE_BACK:"🔄",FEVER_EXTEND:"🌡️",COIN_RUSH:"🪙+",GRAVITY_WELL:"🌀",CLONE_ARMY:"👥+",RUSH_MODE:"⚡!",INVINCIBLE:"💫",HEALER:"❤️+",SCORE_GUARD:"🔰+",SWIPE_BOOST:"💨",LUCKY_COIN:"🍀",ECHO_SHOT:"📡",BURST_SHIELD:"💣🛡",MAGNET_ORBIT:"🌀🧲",SCORE_ECHO:"📢",PHANTOM_BURST:"👻💥",STREAK_STORM:"⛈️",TIME_BUBBLE:"⏱️💫",CRIT_BOOST:"💥×3",GOLD_RUSH:"🥇",MIRROR_MAZE:"🪞🌀",BLAST_WAVE:"💥🌊",TEMPO_SYNC:"🎵⚡",LIFE_LEECH:"❤️🩸",SCORE_RAIN:"🌧️⭐",CHAIN_MAGNET:"🧲⛓️",SCORE_SURGE:"💹",ORBIT_SHIELD:"🛸🛡",ECHO_WAVE:"〰️💥",COMBO_BURST:"💥⛓️",PHASE_LOCK:"🔒⏸",SCORE_LEECH:"🔋💰",SPLIT_SHOT:"🎯🎯",FEVER_STORM:"🌪️🌡️",RARITY_BOOST:"✨⬆️",TWIN_FEVER:"🌡️×2",REFLECT_WAVE:"💥🔄",NOVA_BOOST:"💥🌟",STREAK_LOCK:"🔐⛓️",MAGNET_STORM:"🌪️🧲",SCORE_FREEZE:"❄️🔒",CHAIN_FEVER:"⛓️🌡️",LIFE_BOMB:"💣❤️",TURBO_TAP:"⚡⚡⚡",COIN_MAGNET:"🧲🪙",BLAST_RADIUS:"💥🌐",SCORE_ACCELERATOR:"📈⚡",LIFE_CHAIN:"❤️⛓️",SCORE_MIRROR:"🪞💯",FEVER_LOCK:"🔒🌡️",STREAK_SURGE:"🔥⬆️",COMBO_ECHO:"🔊⛓️",GOLD_SHIELD:"🛡️🥇",PHANTOM_LIFE:"👻❤️",SCORE_DOUBLER:"×2🎯",MAGNET_BLITZ:"🧲⚡",CRIT_STORM:"💥🌩️",BONUS_WAVE:"🌊🎁",TAP_FURY:"🔥👊",SCORE_SPIKE:"📊⚡",LIFE_BURST:"❤️💥",PHASE_BURST:"💠⚡",VORTEX_TAP:"🌀👊",CHAIN_LOCK:"🔐⛓️",NOVA_STORM:"🌩️💥",ECHO_CHAIN:"🔊⛓️",GRAVITY_BURST:"🌀💥",SCORE_FLOOD:"🌊💰",LIFE_LEECH_PLUS:"❤️🩸+",COMBO_SURGE:"🔥⛓️",STAR_FORGE:"⭐🔥",PULSE_SHIELD:"💠🛡️",SCORE_AMPLIFY:"📈🔊",RICOCHET_BLAST:"🎯⛓️",FEVER_FOUNTAIN:"🌡️⛲",DOUBLE_FEVER:"🌡️×2",BOMB_CONVERT:"💣💡",SCORE_MAGNET:"🧲💰",LIFE_OVERFLOW:"❤️⬆️",STREAK_HEAL:"❤️🔥",TEMPO_BURST:"🎵💥",CRIT_SURGE:"💥🔥",LIFE_PULSE:"💓⚡",CHAIN_WAVE:"🌊⛓️",SCORE_OVERDRIVE:"📈🔥",PHANTOM_STRIKE:"👻⚡",COIN_SURGE:"🪙🚀",FREEZE_SURGE:"❄️⚡",LUCKY_STRIKE:"🍀⚡",ECHO_BURST:"🔊💥",SHIELD_SURGE:"🛡️🚀",SCORE_SNAP:"📸💰",LIFE_GUARD:"❤️🛡️",BURST_FEVER:"🌡️💥",SCORE_TRAIL:"✨📈",CHAIN_SNAP:"🔗📸",LIFE_ECHO:"❤️🔊",SCORE_VAULT:"🏦💰",STREAK_ECHO:"🔥🔊",NOVA_PULSE:"💥🫀",COIN_ECHO:"🪙🔊",TIME_SHIELD:"⏱️🛡️",SCORE_BLITZ:"⚡💯",PHANTOM_RUSH:"👻⚡",COIN_STORM:"🪙🌪️",STREAK_BLAZE:"🔥🌪️",LIFE_BLOOM:"❤️🌸",FOCUS_BLAST:"🎯💥",COIN_CHAIN:"🪙⛓️",SHIELD_BURST:"🛡️💥",SCORE_ECHO2:"📊🔁",COMBO_SURGE2:"🔥⛓️",LIFE_SPIRAL:"❤️🌀",TEMPO_ECHO:"🎵🔊",COIN_BLOOM:"🪙🌸",STREAK_NOVA:"⛓️💥",LIFE_STORM:"❤️⛈️",GOLD_FRENZY:"🥇🔥",PHANTOM_COIN:"👻🪙",SCORE_FRENZY:"📊🔥",LIFE_BEACON:"❤️🔦",BLAZE_TRAIL:"🔥✨",COIN_PULSE:"🪙💓",FEVER_BLOOM:"🌡️🌸",COIN_CASCADE:"🪙💦",SCORE_OMEGA:"Ω💥",ETERNAL_LIFE:"♾️❤️",ARC_SURGE:"⚡🌊",RELIC_PULSE:"🏺💓",VENOM_SURGE:"☠️⚡",LUNAR_BLOOM:"🌙🌸",STAR_CHAIN:"⭐⛓️",VOID_BLOOM:"🌑🌸",PULSE_WAVE:"〰️💥",COIN_VORTEX:"🪙🌀",GRAVITY_BURST2:"🌀💥",SCORE_AVALANCHE:"🏔️💰",INFERNO_CHAIN:"🔥⛓️",SPECTRAL_COIN:"👻💰",MAGMA_WAVE:"🌋〰️",COSMIC_ECHO:"🌌🔊",FROST_LANCE:"❄️🗡️",LIFE_MATRIX:"❤️🔲",THUNDER_BURST:"⚡💥",SHIMMER_COIN:"✨🪙",SCORE_QUAKE:"🌍💥",ECHO_LIFE:"❤️〰️",NOVA_CASCADE:"💥🌊",TIME_WARP2:"⏱️×2",SOLAR_PULSE:"☀️💓",VOID_CHAIN:"🌑⛓️",EMBER_WAVE:"🔥〰️",CRYSTAL_NET:"💎🕸️",PRISM_ECHO:"🌈🔊",VOLT_SURGE:"⚡🔥",SHADOW_BLOOM:"🌑🌸",NEXUS_WAVE:"🌐〰️",QUANTUM_ECHO:"🔮🔊",FROST_CROWN:"❄️👑"};
  if(pwrType==="LUCKY"){ctx.shadowColor="#ffd700";ctx.shadowBlur=r*(1.2+pulse*0.8);}
  const label=icons[pwrType]||"⚡";
  ctx.font=`bold ${r*0.9}px serif`; ctx.textAlign="center"; ctx.textBaseline="middle";
  ctx.fillStyle="#fff"; ctx.globalAlpha=0.95+pulse*0.05;
  ctx.fillText(label,0,r*0.05);
  ctx.globalAlpha=1;
}

// World boss emojis
const BOSS_EMOJIS=["🐉","🌿","✨","🔮","❄️","⛏️","👻","🌊","⛰️","🌈"];

// ── Boss target ──
function drawBoss(ctx, r, hitsLeft, maxHits, ts, worldId=1, rage=false) {
  const phase=(maxHits-hitsLeft)/maxHits;
  const colors=["#ff6030","#f472b6","#ef4444"];
  const c=rage?"#ff0000":colors[Math.min(Math.floor(phase*3),2)];
  const glow=rage?"#ff0000":colors[Math.min(Math.floor(phase*3),2)];
  // Rage pulsing outer ring
  if(rage){
    const rageAlpha=0.4+0.6*Math.abs(Math.sin(ts*0.012));
    ctx.save();
    ctx.strokeStyle=`rgba(255,0,0,${rageAlpha})`;ctx.lineWidth=4;
    ctx.shadowColor="#ff0000";ctx.shadowBlur=30;
    ctx.beginPath();ctx.arc(0,0,r*2.1,0,Math.PI*2);ctx.stroke();
    ctx.beginPath();ctx.arc(0,0,r*1.5,0,Math.PI*2);ctx.stroke();
    ctx.restore();
  }
  const spin=ts*0.001;
  const pulse=0.5+0.5*Math.sin(ts*0.006);
  ctx.save();
  // Outer glow aura
  const auraGrad=ctx.createRadialGradient(0,0,r*0.5,0,0,r*1.8);
  auraGrad.addColorStop(0,c+"50"); auraGrad.addColorStop(1,c+"00");
  ctx.fillStyle=auraGrad; ctx.beginPath(); ctx.arc(0,0,r*1.8,0,Math.PI*2); ctx.fill();
  // Rotating spike ring
  ctx.save(); ctx.rotate(spin*0.7);
  ctx.strokeStyle=c+"99"; ctx.lineWidth=2;
  for(let i=0;i<12;i++){
    const a=i*Math.PI/6; const s=r*1.02,e=r*1.35+pulse*8;
    ctx.shadowColor=glow; ctx.shadowBlur=10;
    ctx.beginPath(); ctx.moveTo(Math.cos(a)*s,Math.sin(a)*s); ctx.lineTo(Math.cos(a)*e,Math.sin(a)*e); ctx.stroke();
  }
  ctx.restore();
  // Main 6-star
  ctx.save(); ctx.rotate(spin);
  ctx.shadowColor=glow; ctx.shadowBlur=r*1.8;
  const fcs=ctx.createRadialGradient(0,0,0,0,0,r);
  fcs.addColorStop(0,c+"88"); fcs.addColorStop(1,c+"22");
  ctx.fillStyle=fcs; ctx.strokeStyle=c; ctx.lineWidth=3;
  ctx.beginPath();
  for(let i=0;i<12;i++){
    const a=i*Math.PI/6-Math.PI/2;
    const cr=i%2===0?r-2:r*0.5;
    i===0?ctx.moveTo(Math.cos(a)*cr,Math.sin(a)*cr):ctx.lineTo(Math.cos(a)*cr,Math.sin(a)*cr);
  }
  ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.restore();
  // Counter-rotating inner
  ctx.save(); ctx.rotate(-spin*2);
  ctx.strokeStyle=c+"66"; ctx.lineWidth=2; ctx.fillStyle="transparent";
  ctx.beginPath();
  for(let i=0;i<12;i++){
    const a=i*Math.PI/6-Math.PI/2+Math.PI/12;
    const cr=i%2===0?(r-2)*0.62:r*0.3;
    i===0?ctx.moveTo(Math.cos(a)*cr,Math.sin(a)*cr):ctx.lineTo(Math.cos(a)*cr,Math.sin(a)*cr);
  }
  ctx.closePath(); ctx.stroke(); ctx.restore();
  // Electric arcs
  ctx.save(); ctx.rotate(spin);
  const tips=Array.from({length:6},(_,i)=>{const a=i*Math.PI/3-Math.PI/2; return{x:Math.cos(a)*(r-2),y:Math.sin(a)*(r-2)};});
  ctx.globalAlpha=0.75;
  for(let i=0;i<6;i++) drawArc(ctx,tips[i].x,tips[i].y,tips[(i+2)%6].x,tips[(i+2)%6].y,c,ts,i);
  ctx.restore();
  // World emoji label
  ctx.save(); ctx.globalAlpha=0.9;
  ctx.font=`${r*0.72}px serif`; ctx.textAlign="center"; ctx.textBaseline="middle";
  ctx.shadowColor="#fff"; ctx.shadowBlur=16;
  ctx.fillText(BOSS_EMOJIS[(worldId||1)-1]||"🐉",0,r*0.06); ctx.restore();
  // HP bar
  const bw=r*2.4, bh=8, by=r+12;
  ctx.fillStyle="#1a1a1a"; ctx.fillRect(-bw/2,by,bw,bh);
  const hpCol=phase<0.5?"#34d399":phase<0.8?"#fbbf24":"#ef4444";
  ctx.fillStyle=hpCol; ctx.shadowColor=hpCol; ctx.shadowBlur=8;
  ctx.fillRect(-bw/2,by,bw*(hitsLeft/maxHits),bh);
  ctx.strokeStyle="#ffffff33"; ctx.lineWidth=1; ctx.shadowBlur=0;
  ctx.strokeRect(-bw/2,by,bw,bh);
  ctx.restore();
}

// ── Treasure chest target ──
function drawTreasure(ctx, r, ts) {
  const bob=Math.sin(ts*0.003)*3;
  const spin=ts*0.0012;
  ctx.save();ctx.translate(0,bob);
  // Outer glow
  const grd=ctx.createRadialGradient(0,0,r*0.3,0,0,r*1.8);
  grd.addColorStop(0,"#ffd70055");grd.addColorStop(1,"transparent");
  ctx.fillStyle=grd;ctx.beginPath();ctx.arc(0,0,r*1.8,0,Math.PI*2);ctx.fill();
  // Chest lid (top half)
  ctx.shadowColor="#ffd700";ctx.shadowBlur=r*0.8;
  ctx.fillStyle="#d4a017";
  ctx.beginPath();ctx.roundRect(-r*0.88,-r*0.72,r*1.76,r*0.82,6);ctx.fill();
  // Chest body (bottom half)
  ctx.fillStyle="#c8860a";
  ctx.beginPath();ctx.roundRect(-r*0.88,-r*0.08,r*1.76,r*0.85,4);ctx.fill();
  // Gold bands
  ctx.strokeStyle="#ffd700";ctx.lineWidth=3;ctx.shadowBlur=8;
  ctx.beginPath();ctx.moveTo(-r*0.88,-r*0.06);ctx.lineTo(r*0.88,-r*0.06);ctx.stroke();
  ctx.strokeStyle="#ffc800";ctx.lineWidth=1.5;
  [-0.62,0.62].forEach(x=>{ctx.beginPath();ctx.moveTo(r*x,-r*0.72);ctx.lineTo(r*x,r*0.77);ctx.stroke();});
  // Keyhole
  ctx.fillStyle="#7a4800";ctx.shadowBlur=0;
  ctx.beginPath();ctx.arc(0,r*0.2,r*0.18,0,Math.PI*2);ctx.fill();
  ctx.fillStyle="#5a3200";
  ctx.beginPath();ctx.rect(-r*0.08,r*0.2,r*0.16,r*0.28);ctx.fill();
  // "?" sparkling on lid
  ctx.fillStyle="#fff";ctx.shadowColor="#fff";ctx.shadowBlur=16;
  ctx.font=`bold ${r*0.68}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("?",0,-r*0.32);
  // Orbiting sparkles
  for(let i=0;i<8;i++){
    const a=i*Math.PI/4+spin;const dist=r*1.4;
    const sx=Math.cos(a)*dist,sy=Math.sin(a)*dist*0.6;
    const pulse=0.5+0.5*Math.abs(Math.sin(ts*0.006+i*0.8));
    ctx.save();ctx.translate(sx,sy);ctx.globalAlpha=pulse*0.9;
    ctx.fillStyle=i%2===0?"#ffd700":"#fff";ctx.shadowColor=ctx.fillStyle;ctx.shadowBlur=8;
    const ss=r*0.18*pulse;
    ctx.beginPath();
    for(let p=0;p<4;p++){const pa=p*Math.PI/2-Math.PI/4;ctx.lineTo(Math.cos(pa)*ss,Math.sin(pa)*ss);}
    ctx.closePath();ctx.fill();
    ctx.restore();
  }
  ctx.restore();
}

// ── Mystery Box target ──
function drawMimic(ctx, r, ts, mimicColor="#c8c8c8", mimicGlow="#ffffff") {
  // Mimic — silvery shape-shifting target with rotating "?" glyph
  const pulse=0.5+0.5*Math.sin(ts*0.005);
  const spin=ts*0.0014;
  ctx.save();
  // Outer halo
  const halo=ctx.createRadialGradient(0,0,r*0.2,0,0,r*2.1);
  halo.addColorStop(0,mimicColor+"55");halo.addColorStop(0.6,mimicColor+"15");halo.addColorStop(1,"transparent");
  ctx.fillStyle=halo;ctx.beginPath();ctx.arc(0,0,r*2.1,0,Math.PI*2);ctx.fill();
  // Pulsing outer ring
  ctx.save();ctx.rotate(spin*0.6);
  ctx.strokeStyle=mimicGlow;ctx.globalAlpha=0.5+pulse*0.4;ctx.lineWidth=2;
  ctx.shadowColor=mimicGlow;ctx.shadowBlur=18;
  ctx.setLineDash([5,8]);
  ctx.beginPath();ctx.arc(0,0,r*1.35,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);ctx.restore();
  // Body — shifting prismatic look
  ctx.shadowColor=mimicGlow;ctx.shadowBlur=r*(0.7+pulse*0.5);
  const body=ctx.createRadialGradient(-r*0.3,-r*0.3,0,0,0,r);
  body.addColorStop(0,"#f5f5f5");body.addColorStop(0.4,mimicColor);body.addColorStop(1,mimicGlow);
  ctx.fillStyle=body;ctx.beginPath();ctx.arc(0,0,r*(0.92+pulse*0.08),0,Math.PI*2);ctx.fill();
  // "?" glyph
  ctx.fillStyle="#111";ctx.shadowColor="transparent";ctx.shadowBlur=0;
  ctx.font=`bold ${Math.round(r*1.3)}px 'Segoe UI',sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("?",0,r*0.05);
  // Sparkle satellites
  for(let i=0;i<5;i++){
    const a=i*Math.PI*0.4+spin*2;const d=r*1.55;
    const op=0.3+0.7*Math.abs(Math.sin(ts*0.008+i*1.3));
    ctx.save();ctx.translate(Math.cos(a)*d,Math.sin(a)*d);ctx.globalAlpha=op;
    ctx.fillStyle="#ffffff";ctx.shadowColor=mimicGlow;ctx.shadowBlur=10;
    ctx.beginPath();ctx.arc(0,0,r*0.08,0,Math.PI*2);ctx.fill();ctx.restore();
  }
  ctx.restore();
}

function drawMystery(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.004);
  const spin=ts*0.0018;
  const bob=Math.sin(ts*0.0035)*4;
  ctx.save();ctx.translate(0,bob);
  // Outer glow ring
  const grd=ctx.createRadialGradient(0,0,r*0.2,0,0,r*2);
  grd.addColorStop(0,"#ffd70066");grd.addColorStop(0.6,"#ffd70022");grd.addColorStop(1,"transparent");
  ctx.fillStyle=grd;ctx.beginPath();ctx.arc(0,0,r*2,0,Math.PI*2);ctx.fill();
  // Rotating dashed ring
  ctx.save();ctx.rotate(spin);
  ctx.strokeStyle=`rgba(255,215,0,${0.5+pulse*0.5})`;ctx.lineWidth=3;
  ctx.shadowColor="#ffd700";ctx.shadowBlur=14;
  ctx.setLineDash([8,6]);ctx.beginPath();ctx.arc(0,0,r*1.22,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);ctx.restore();
  // Main body
  ctx.shadowColor="#ffd700";ctx.shadowBlur=r*(0.6+pulse*0.4);
  const bodyGrd=ctx.createRadialGradient(-r*0.3,-r*0.3,0,0,0,r);
  bodyGrd.addColorStop(0,"#ffe566");bodyGrd.addColorStop(0.5,"#ffd700");bodyGrd.addColorStop(1,"#c8a000");
  ctx.fillStyle=bodyGrd;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Gift ribbon vertical
  ctx.strokeStyle="#ff6030";ctx.lineWidth=r*0.18;ctx.shadowColor="#ff6030";ctx.shadowBlur=8;
  ctx.beginPath();ctx.moveTo(0,-r*0.95);ctx.lineTo(0,r*0.95);ctx.stroke();
  // Gift ribbon horizontal
  ctx.beginPath();ctx.moveTo(-r*0.95,0);ctx.lineTo(r*0.95,0);ctx.stroke();
  // Bow center
  ctx.fillStyle="#ff6030";ctx.shadowBlur=12;
  ctx.beginPath();ctx.arc(0,0,r*0.22,0,Math.PI*2);ctx.fill();
  ctx.fillStyle="#fff";ctx.shadowColor="#fff";ctx.shadowBlur=8;
  ctx.beginPath();ctx.arc(0,0,r*0.1,0,Math.PI*2);ctx.fill();
  // Orbiting stars
  for(let i=0;i<6;i++){
    const a=i*Math.PI/3+spin*1.5;const d=r*1.55;
    const op=0.4+0.6*Math.abs(Math.sin(ts*0.005+i));
    ctx.save();ctx.translate(Math.cos(a)*d,Math.sin(a)*d);ctx.globalAlpha=op;
    ctx.fillStyle=i%2===0?"#ffd700":"#fff";ctx.shadowColor=ctx.fillStyle;ctx.shadowBlur=6;
    ctx.font=`${r*0.28}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText(["⭐","✨","💎","⭐","✨","💎"][i],0,0);ctx.restore();
  }
  ctx.restore();
}

// ── Poison target — avoid it; if it expires, next target gives 50% score ──
function drawPoison(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.009);
  const drip=Math.sin(ts*0.015)*2;
  ctx.save();
  // Toxic aura
  ctx.globalAlpha=0.18+pulse*0.18;ctx.fillStyle="#4ade80";ctx.shadowColor="#16a34a";ctx.shadowBlur=22+pulse*14;
  ctx.beginPath();ctx.arc(0,0,r*1.28,0,Math.PI*2);ctx.fill();
  // Core — sickly green
  ctx.globalAlpha=1;
  const pg=ctx.createRadialGradient(0,0,0,0,0,r);
  pg.addColorStop(0,"rgba(187,247,208,0.92)");pg.addColorStop(0.5,"rgba(74,222,128,0.88)");pg.addColorStop(1,"rgba(22,101,52,0.82)");
  ctx.fillStyle=pg;ctx.shadowColor="#22c55e";ctx.shadowBlur=14;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Biohazard-ish symbol (3 arcs)
  ctx.globalAlpha=0.6+pulse*0.2;ctx.strokeStyle="#14532d";ctx.lineWidth=2.5;ctx.shadowBlur=0;
  ctx.beginPath();ctx.arc(0,0,r*0.28,0,Math.PI*2);ctx.stroke();
  [0,Math.PI*2/3,Math.PI*4/3].forEach(a=>{
    ctx.beginPath();ctx.arc(Math.cos(a)*r*0.45,Math.sin(a)*r*0.45,r*0.22,a-0.3,a+Math.PI*0.65);ctx.stroke();
  });
  // Drip at bottom
  ctx.globalAlpha=0.7+pulse*0.2;ctx.translate(0,drip);
  ctx.fillStyle="#bbf7d0";
  ctx.beginPath();ctx.arc(0,r*0.68,r*0.1,0,Math.PI*2);ctx.fill();
  ctx.beginPath();ctx.arc(r*0.3,r*0.62,r*0.07,0,Math.PI*2);ctx.fill();
  ctx.restore();
}

function drawAnchor(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.0035);
  const spin=ts*0.0008;
  ctx.save();
  // Outer glow
  const grd=ctx.createRadialGradient(0,0,r*0.2,0,0,r*2);
  grd.addColorStop(0,"#06b6d466");grd.addColorStop(0.6,"#06b6d422");grd.addColorStop(1,"transparent");
  ctx.fillStyle=grd;ctx.beginPath();ctx.arc(0,0,r*2,0,Math.PI*2);ctx.fill();
  // Rotating ring
  ctx.save();ctx.rotate(spin);
  ctx.strokeStyle=`rgba(6,182,212,${0.5+pulse*0.4})`;ctx.lineWidth=2.5;
  ctx.shadowColor="#06b6d4";ctx.shadowBlur=12;
  ctx.setLineDash([6,5]);ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);ctx.restore();
  // Body
  ctx.shadowColor="#06b6d4";ctx.shadowBlur=r*(0.5+pulse*0.5);
  const body=ctx.createRadialGradient(-r*0.3,-r*0.3,0,0,0,r);
  body.addColorStop(0,"#67e8f9");body.addColorStop(0.5,"#06b6d4");body.addColorStop(1,"#0e7490");
  ctx.fillStyle=body;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Anchor symbol
  ctx.strokeStyle="#ffffff";ctx.lineWidth=r*0.13;ctx.lineCap="round";ctx.lineJoin="round";
  ctx.shadowColor="#ffffff";ctx.shadowBlur=6;
  // Ring at top
  ctx.beginPath();ctx.arc(0,-r*0.52,r*0.2,0,Math.PI*2);ctx.stroke();
  // Vertical stem
  ctx.beginPath();ctx.moveTo(0,-r*0.32);ctx.lineTo(0,r*0.52);ctx.stroke();
  // Crossbar
  ctx.beginPath();ctx.moveTo(-r*0.38,-r*0.05);ctx.lineTo(r*0.38,-r*0.05);ctx.stroke();
  // Flukes
  ctx.beginPath();ctx.moveTo(-r*0.38,r*0.52);ctx.quadraticCurveTo(-r*0.5,r*0.72,0,r*0.62);ctx.stroke();
  ctx.beginPath();ctx.moveTo(r*0.38,r*0.52);ctx.quadraticCurveTo(r*0.5,r*0.72,0,r*0.62);ctx.stroke();
  ctx.restore();
}

// ── Divider target — large orb with crack; splits into 2 medium targets ──
function drawDivider(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.005);
  const crackShift=Math.sin(ts*0.015)*1.2; // crack vibrates
  ctx.save();
  // Outer halo
  ctx.globalAlpha=0.18+pulse*0.14;ctx.fillStyle="#fb923c";ctx.shadowColor="#ea580c";ctx.shadowBlur=28;
  ctx.beginPath();ctx.arc(0,0,r*1.28,0,Math.PI*2);ctx.fill();
  // Main body
  ctx.globalAlpha=1;
  const dg=ctx.createRadialGradient(0,0,0,0,0,r);
  dg.addColorStop(0,"rgba(253,186,116,0.95)");dg.addColorStop(0.55,"rgba(251,146,60,0.9)");dg.addColorStop(1,"rgba(234,88,12,0.85)");
  ctx.fillStyle=dg;ctx.shadowColor="#f97316";ctx.shadowBlur=16;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Crack line through center
  ctx.globalAlpha=0.85+pulse*0.12;ctx.strokeStyle="#fef3c7";ctx.lineWidth=2.5;
  ctx.shadowColor="#fbbf24";ctx.shadowBlur=8;
  ctx.beginPath();
  ctx.moveTo(-r*0.05+crackShift,-r*0.85);
  ctx.lineTo(r*0.12+crackShift,-r*0.25);
  ctx.lineTo(-r*0.08+crackShift,r*0.15);
  ctx.lineTo(r*0.1+crackShift,r*0.82);
  ctx.stroke();
  // ÷ symbol hint
  ctx.globalAlpha=0.65+pulse*0.2;ctx.fillStyle="#fff7ed";ctx.shadowBlur=4;
  ctx.font=`bold ${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("÷",-r*0.2,0);
  ctx.restore();
}

function drawSplitter(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.005);
  const spin=ts*0.0022;
  ctx.save();
  // Outer glow
  const grd=ctx.createRadialGradient(0,0,r*0.2,0,0,r*2);
  grd.addColorStop(0,"#f97316aa");grd.addColorStop(0.6,"#f9731633");grd.addColorStop(1,"transparent");
  ctx.fillStyle=grd;ctx.beginPath();ctx.arc(0,0,r*2,0,Math.PI*2);ctx.fill();
  // Spinning triangular ring
  ctx.save();ctx.rotate(spin);
  ctx.strokeStyle=`rgba(251,146,60,${0.6+pulse*0.4})`;ctx.lineWidth=2.5;
  ctx.shadowColor="#fb923c";ctx.shadowBlur=14;
  ctx.beginPath();
  for(let i=0;i<3;i++){const a=i*Math.PI*2/3;ctx.lineTo(Math.cos(a)*r*1.25,Math.sin(a)*r*1.25);}
  ctx.closePath();ctx.stroke();ctx.restore();
  // Body — hexagonal-ish glow
  ctx.shadowColor="#f97316";ctx.shadowBlur=r*(0.7+pulse*0.4);
  const body=ctx.createRadialGradient(-r*0.3,-r*0.3,0,0,0,r);
  body.addColorStop(0,"#fed7aa");body.addColorStop(0.5,"#f97316");body.addColorStop(1,"#c2410c");
  ctx.fillStyle=body;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Split arrows (3 outward arrows suggesting the split)
  for(let i=0;i<3;i++){
    const a=i*Math.PI*2/3+Math.PI/6;
    ctx.save();ctx.rotate(a);
    ctx.strokeStyle="#ffffff";ctx.lineWidth=r*0.1;ctx.lineCap="round";ctx.shadowColor="#fff";ctx.shadowBlur=4;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(0,-r*0.55);ctx.stroke();
    ctx.beginPath();ctx.moveTo(-r*0.15,-r*0.35);ctx.lineTo(0,-r*0.55);ctx.lineTo(r*0.15,-r*0.35);ctx.stroke();
    ctx.restore();
  }
  ctx.restore();
}

// ── Magnet target — pulls nearby targets toward it ──
function drawMagnet(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.006);
  const spin=ts*0.0018;
  ctx.save();
  // Outer magnetic field rings
  for(let i=1;i<=3;i++){
    ctx.save();
    ctx.globalAlpha=(0.12+pulse*0.08)/i;
    ctx.strokeStyle="#ec4899";ctx.lineWidth=1.5;
    ctx.setLineDash([4+i*2,4+i*2]);ctx.lineDashOffset=ts*0.03*i;
    ctx.beginPath();ctx.arc(0,0,r*(1.4+i*0.35),0,Math.PI*2);ctx.stroke();
    ctx.setLineDash([]);ctx.restore();
  }
  // Glow
  const grd=ctx.createRadialGradient(0,0,r*0.2,0,0,r*2);
  grd.addColorStop(0,"#ec489944");grd.addColorStop(0.6,"#ec489911");grd.addColorStop(1,"transparent");
  ctx.fillStyle=grd;ctx.beginPath();ctx.arc(0,0,r*2,0,Math.PI*2);ctx.fill();
  // Body — hot pink
  ctx.shadowColor="#ec4899";ctx.shadowBlur=r*(0.8+pulse*0.4);
  const body=ctx.createRadialGradient(-r*0.3,-r*0.3,0,0,0,r);
  body.addColorStop(0,"#fce7f3");body.addColorStop(0.5,"#ec4899");body.addColorStop(1,"#9d174d");
  ctx.fillStyle=body;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Horseshoe magnet U shape
  ctx.save();ctx.rotate(spin*0.5);
  ctx.strokeStyle="rgba(255,255,255,0.9)";ctx.lineWidth=r*0.14;ctx.lineCap="round";
  ctx.shadowColor="#fff";ctx.shadowBlur=6;
  ctx.beginPath();ctx.arc(0,0,r*0.45,Math.PI,0,false);ctx.stroke();
  ctx.beginPath();ctx.moveTo(-r*0.45,-0.1);ctx.lineTo(-r*0.45,r*0.22);ctx.stroke();
  ctx.beginPath();ctx.moveTo(r*0.45,-0.1);ctx.lineTo(r*0.45,r*0.22);ctx.stroke();
  ctx.restore();
  ctx.restore();
}

// ── Ice Comet target — frozen streaker; freezes nearby targets on expiry ──
function drawIceComet(ctx, r, ts, trail) {
  const pulse=0.7+0.3*Math.sin(ts*0.01);
  // Icy trail
  if(trail&&trail.length>1){
    for(let i=1;i<trail.length;i++){
      const alpha=(i/trail.length)*0.4*pulse;
      const sz=r*(i/trail.length)*0.6;
      ctx.save();
      ctx.globalAlpha=alpha;
      ctx.fillStyle=`hsl(${195+i*4},100%,${75-i*2}%)`;
      ctx.translate(trail[i].x-trail[trail.length-1].x,trail[i].y-trail[trail.length-1].y);
      ctx.beginPath();ctx.arc(0,0,sz,0,Math.PI*2);ctx.fill();
      ctx.restore();
    }
  }
  // Ice crystals around head
  ctx.save();
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.001;
    ctx.fillStyle="#bae6fd";ctx.globalAlpha=0.5*pulse;
    const px=Math.cos(a)*(r+6),py=Math.sin(a)*(r+6);
    ctx.beginPath();ctx.moveTo(px,py);ctx.lineTo(px+3,py-4);ctx.lineTo(px-3,py-4);ctx.closePath();ctx.fill();
  }
  // Core
  ctx.globalAlpha=1;
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#ffffff");grad.addColorStop(0.3,"#bae6fd");grad.addColorStop(0.7,"#0ea5e9");grad.addColorStop(1,"#075985");
  ctx.fillStyle=grad;ctx.shadowColor="#38bdf8";ctx.shadowBlur=20+pulse*12;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  ctx.fillStyle="#ffffff";ctx.shadowBlur=0;
  ctx.font=`${Math.floor(r*0.9)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("❄️",0,1);
  ctx.restore();
}

// ── Phoenix target — fiery bird; when expired it respawns once as Epic ──
function drawPhoenix(ctx, r, ts, isRisen) {
  const pulse=0.6+0.4*Math.sin(ts*0.012);
  const spin=ts*0.0005;
  const col1=isRisen?"#c084fc":"#f97316";
  const col2=isRisen?"#7c3aed":"#dc2626";
  // Flame aura
  const grad=ctx.createRadialGradient(0,0,r*0.2,0,0,r*1.6);
  grad.addColorStop(0,col1+"cc");grad.addColorStop(0.5,col1+"44");grad.addColorStop(1,"transparent");
  ctx.save();ctx.globalAlpha=0.55*pulse;ctx.fillStyle=grad;
  ctx.beginPath();ctx.arc(0,0,r*1.6,0,Math.PI*2);ctx.fill();
  // Flickering flames (5 spikes radiating up)
  ctx.globalAlpha=0.75*pulse;ctx.fillStyle=col1;
  for(let i=0;i<5;i++){
    const a=-Math.PI/2+(i-2)*0.3+Math.sin(ts*0.018+i)*0.15;
    const fl=r*(0.8+Math.random()*0.4);
    ctx.save();ctx.rotate(a);
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(-4,-fl);ctx.lineTo(4,-fl);ctx.closePath();ctx.fill();
    ctx.restore();
  }
  // Body
  ctx.globalAlpha=1;ctx.rotate(spin);
  const bg=ctx.createRadialGradient(0,0,0,0,0,r);
  bg.addColorStop(0,"#fff5f5");bg.addColorStop(0.35,col1);bg.addColorStop(1,col2);
  ctx.fillStyle=bg;ctx.shadowColor=col1;ctx.shadowBlur=14+pulse*12;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Bird emoji or phoenix emoji
  ctx.fillStyle="#ffffff";ctx.shadowBlur=0;
  ctx.font=`${Math.floor(r*1.0)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isRisen?"🦋":"🔥",0,1);
  ctx.restore();
}

// ── Nexus target — ultra-rare cosmic core; 2000+ pts; orbiting planets ──
function drawNexus(ctx, r, ts) {
  const pulse=0.65+0.35*Math.sin(ts*0.006);
  const spin=ts*0.0007;
  // Outer galaxy nebula
  const neb=ctx.createRadialGradient(0,0,r*0.3,0,0,r*2.5);
  neb.addColorStop(0,"#ffd70055");neb.addColorStop(0.4,"#a78bfa33");neb.addColorStop(0.7,"#60a5fa18");neb.addColorStop(1,"transparent");
  ctx.save();ctx.globalAlpha=0.7*pulse;ctx.fillStyle=neb;
  ctx.beginPath();ctx.arc(0,0,r*2.5,0,Math.PI*2);ctx.fill();
  // Orbiting planets
  const planets=[{r2:r*1.5,sz:r*0.22,col:"#60a5fa",spd:0.0014},{r2:r*2.0,sz:r*0.16,col:"#f472b6",spd:-0.0009},{r2:r*1.2,sz:r*0.14,col:"#34d399",spd:0.002}];
  planets.forEach(p=>{
    const ang=spin*p.spd/0.0007+ts*p.spd;
    ctx.globalAlpha=0.9;ctx.fillStyle=p.col;ctx.shadowColor=p.col;ctx.shadowBlur=10;
    ctx.beginPath();ctx.arc(Math.cos(ang)*p.r2,Math.sin(ang)*p.r2,p.sz,0,Math.PI*2);ctx.fill();
  });
  // Inner core — layered gradients
  ctx.globalAlpha=1;ctx.rotate(spin*2);
  const core=ctx.createRadialGradient(0,0,0,0,0,r);
  core.addColorStop(0,"#ffffff");core.addColorStop(0.2,"#fde68a");core.addColorStop(0.5,"#ffd700");core.addColorStop(0.8,"#b45309");core.addColorStop(1,"#78350f");
  ctx.fillStyle=core;ctx.shadowColor="#ffd700";ctx.shadowBlur=28+pulse*20;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Star burst rays
  ctx.strokeStyle="#fde68a";ctx.lineWidth=1.5;ctx.globalAlpha=0.5*pulse;
  for(let i=0;i<8;i++){
    const a=i*Math.PI/4;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.8,Math.sin(a)*r*0.8);ctx.lineTo(Math.cos(a)*r*1.6,Math.sin(a)*r*1.6);ctx.stroke();
  }
  // "N" logo in center
  ctx.globalAlpha=1;ctx.fillStyle="#ffffff";ctx.shadowBlur=0;
  ctx.font=`black ${Math.floor(r*0.9)}px 'Exo 2',sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("N",0,0);
  ctx.restore();
}

// ── Mirror Ball target — disco ball; tapping spawns 3 mini copies ──
function drawMirrorBall(ctx, r, ts) {
  const spin=ts*0.002;
  const pulse=0.6+0.4*Math.sin(ts*0.01);
  ctx.save();ctx.rotate(spin);
  // Outer shimmer
  const numFacets=12;
  for(let i=0;i<numFacets;i++){
    const angle=(i/numFacets)*Math.PI*2;
    const fx=Math.cos(angle)*r*0.75;const fy=Math.sin(angle)*r*0.75;
    const hue=(i/numFacets)*360+(ts*0.05)%360;
    const sz=r*0.22;
    ctx.fillStyle=`hsl(${hue},100%,70%)`;
    ctx.globalAlpha=(0.5+0.5*Math.sin(angle*3+ts*0.008))*0.85;
    ctx.beginPath();ctx.arc(fx,fy,sz,0,Math.PI*2);ctx.fill();
  }
  // Main silvery body
  ctx.globalAlpha=1;
  const grad=ctx.createRadialGradient(-r*0.25,-r*0.25,0,0,0,r);
  grad.addColorStop(0,"#ffffff");grad.addColorStop(0.3,"#e2e8f0");grad.addColorStop(0.7,"#94a3b8");grad.addColorStop(1,"#1e293b");
  ctx.fillStyle=grad;ctx.shadowColor="#ffffff";ctx.shadowBlur=14+pulse*10;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Grid lines
  ctx.strokeStyle="#ffffff33";ctx.lineWidth=0.8;ctx.shadowBlur=0;
  for(let i=0;i<8;i++){
    const a=i*Math.PI/4;
    ctx.beginPath();ctx.moveTo(-r,Math.cos(a)*r*0.7);ctx.lineTo(r,Math.cos(a)*r*0.7);ctx.stroke();
  }
  for(let i=0;i<8;i++){
    const a=i*Math.PI/4;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.7,-r);ctx.lineTo(Math.cos(a)*r*0.7,r);ctx.stroke();
  }
  ctx.restore();
}

// ── Comet target — streaks across the screen in a straight line; short lifetime ──
function drawComet(ctx, r, ts, trail) {
  const pulse=0.7+0.3*Math.sin(ts*0.015);
  // Draw trail history
  if(trail&&trail.length>1){
    for(let i=1;i<trail.length;i++){
      const alpha=(i/trail.length)*0.55*pulse;
      const size=r*(i/trail.length)*0.7;
      const dx=trail[i].x-trail[i-1].x;const dy=trail[i].y-trail[i-1].y;
      ctx.save();
      ctx.globalAlpha=alpha;
      ctx.fillStyle=`hsl(${45+i*8},100%,${65+i*2}%)`;
      ctx.translate(trail[i].x-trail[trail.length-1].x,trail[i].y-trail[trail.length-1].y);
      ctx.beginPath();ctx.arc(0,0,size,0,Math.PI*2);ctx.fill();
      ctx.restore();
    }
  }
  // Head — bright white-gold core
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#ffffff");grad.addColorStop(0.3,"#fde68a");grad.addColorStop(0.7,"#f59e0b");grad.addColorStop(1,"#92400e");
  ctx.save();ctx.fillStyle=grad;ctx.shadowColor="#fde68a";ctx.shadowBlur=18+pulse*12;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Star symbol ★
  ctx.fillStyle="#ffffff";ctx.shadowBlur=0;
  ctx.font=`bold ${Math.floor(r*0.9)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("★",0,1);
  ctx.restore();
}

// ── Prism target — refracts light into 3 rainbow beams when tapped ──
function drawPrism(ctx, r, ts) {
  const spin=ts*0.0009;
  const pulse=0.7+0.3*Math.sin(ts*0.008);
  // Rainbow shimmer aura
  const hue1=(ts*0.04)%360;
  const aura=ctx.createRadialGradient(0,0,r*0.3,0,0,r*1.6);
  aura.addColorStop(0,`hsla(${hue1},100%,75%,0.5)`);
  aura.addColorStop(0.5,`hsla(${(hue1+120)%360},100%,60%,0.25)`);
  aura.addColorStop(1,"transparent");
  ctx.save();ctx.globalAlpha=0.55*pulse;ctx.fillStyle=aura;
  ctx.beginPath();ctx.arc(0,0,r*1.6,0,Math.PI*2);ctx.fill();
  // Main prism body — elongated diamond shape
  ctx.globalAlpha=1;
  ctx.rotate(spin);
  const pts3=[[0,-r*1.1],[r*0.65,0],[0,r*1.1],[-r*0.65,0]];
  const prismGrad=ctx.createLinearGradient(-r,0,r,0);
  for(let i=0;i<7;i++) prismGrad.addColorStop(i/6,`hsl(${(hue1+i*52)%360},100%,65%)`);
  ctx.fillStyle=prismGrad;ctx.shadowColor="#ffffff";ctx.shadowBlur=12+pulse*10;
  ctx.beginPath();pts3.forEach(([px,py],i)=>i===0?ctx.moveTo(px,py):ctx.lineTo(px,py));ctx.closePath();ctx.fill();
  // Inner facet lines
  ctx.strokeStyle="#ffffff88";ctx.lineWidth=1;ctx.shadowBlur=0;
  ctx.beginPath();ctx.moveTo(-r*0.65,0);ctx.lineTo(0,-r*1.1);ctx.lineTo(r*0.65,0);ctx.lineTo(0,r*1.1);ctx.stroke();
  ctx.restore();
}

// ── Glitch target — corrupted pixel art; teleports every 0.9s; catch mid-teleport for bonus ──
function drawGlitch(ctx, r, ts) {
  const blinkRate=300;const blink=Math.floor(ts/blinkRate)%2===0;
  const jitter=blink?0:Math.random()*3-1.5; // position jitter when "glitching"
  ctx.save();ctx.translate(jitter,jitter*0.6);
  // Digital corruption bars
  const numBars=5;
  for(let i=0;i<numBars;i++){
    const barY=-r+i*(r*2/numBars);
    const barH=r*2/numBars*0.8;
    const shift=Math.sin(ts*0.02+i*1.4)*r*0.4*(blink?1:0.2);
    const hue=(i*72+ts*0.1)%360;
    ctx.fillStyle=`hsl(${hue},100%,60%)`;
    ctx.globalAlpha=0.75;
    ctx.fillRect(-r+shift,barY,r*2,barH);
  }
  // Main body — dark with scanlines
  ctx.globalAlpha=0.92;
  const grad=ctx.createRadialGradient(0,0,r*0.1,0,0,r);
  grad.addColorStop(0,"#0f172a");grad.addColorStop(0.7,"#1e293b");grad.addColorStop(1,"#334155");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Scanlines
  ctx.globalAlpha=0.2;ctx.strokeStyle="#ffffff";ctx.lineWidth=0.8;
  for(let y=-r;y<r;y+=4){
    ctx.beginPath();ctx.moveTo(-Math.sqrt(Math.max(0,r*r-y*y)),y);
    ctx.lineTo(Math.sqrt(Math.max(0,r*r-y*y)),y);ctx.stroke();
  }
  // Glitch text
  ctx.globalAlpha=blink?1:0.4;
  ctx.fillStyle=blink?"#00ff88":"#ff0080";
  ctx.font=`bold ${Math.floor(r*0.75)}px monospace`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.shadowColor=blink?"#00ff88":"#ff0080";ctx.shadowBlur=10;
  ctx.fillText("ERR",0,0);
  ctx.restore();
}

// ── Siphon target — dark vortex that drains 1 life on expiry; tap it for huge reward ──
function drawSiphon(ctx, r, ts, lifeLeft) {
  const pulse=0.6+0.4*Math.sin(ts*0.014);
  const dangerpct=1-lifeLeft;
  const warnCol=dangerpct>0.6?"#ef4444":"#dc2626";
  // Outer vortex ring
  const spin=-ts*0.006; // counter-clockwise, sinister
  ctx.save();
  ctx.rotate(spin);
  // Pulsing dark aura
  const aura=ctx.createRadialGradient(0,0,r*0.4,0,0,r*1.6);
  aura.addColorStop(0,"#7f1d1d88");aura.addColorStop(0.55,warnCol+"44");aura.addColorStop(1,"transparent");
  ctx.globalAlpha=0.6*pulse;ctx.fillStyle=aura;
  ctx.beginPath();ctx.arc(0,0,r*1.6,0,Math.PI*2);ctx.fill();
  // Dark spiral arms (vortex look)
  ctx.globalAlpha=0.8;
  for(let i=0;i<3;i++){
    const armA=(i*(Math.PI*2/3));
    ctx.strokeStyle=warnCol;ctx.lineWidth=2;
    ctx.shadowColor=warnCol;ctx.shadowBlur=8;
    ctx.beginPath();
    for(let s=0;s<12;s++){
      const ang=armA+s*0.22;
      const rad=r*(0.2+s*0.065);
      s===0?ctx.moveTo(Math.cos(ang)*rad,Math.sin(ang)*rad):ctx.lineTo(Math.cos(ang)*rad,Math.sin(ang)*rad);
    }
    ctx.stroke();
  }
  ctx.restore();
  // Body
  const bg=ctx.createRadialGradient(0,0,0,0,0,r);
  bg.addColorStop(0,"#450a0a");bg.addColorStop(0.5,"#991b1b");bg.addColorStop(1,"#450a0a");
  ctx.save();ctx.fillStyle=bg;ctx.shadowColor=warnCol;ctx.shadowBlur=14+pulse*12;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Warning symbol
  ctx.fillStyle="#fff";ctx.shadowBlur=0;
  ctx.font=`bold ${Math.floor(r*1.1)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.7+0.3*pulse;
  ctx.fillText("⚠",0,1);
  ctx.restore();
}

// ── Conductor target — musical note theme; when tapped boosts spawn for 5s ──
function drawConductor(ctx, r, ts) {
  const pulse=0.7+0.3*Math.sin(ts*0.009);
  const beat=0.5+0.5*Math.sin(ts*0.016); // faster "heartbeat" to music
  // Outer resonance ring
  const grad=ctx.createRadialGradient(0,0,r*0.2,0,0,r*1.5);
  grad.addColorStop(0,"#fb923c99");grad.addColorStop(0.5,"#f9780433");grad.addColorStop(1,"transparent");
  ctx.save();ctx.globalAlpha=0.5*pulse;ctx.fillStyle=grad;
  ctx.beginPath();ctx.arc(0,0,r*1.5,0,Math.PI*2);ctx.fill();
  // Body
  ctx.globalAlpha=1;
  const bg=ctx.createRadialGradient(-r*0.2,-r*0.2,0,0,0,r);
  bg.addColorStop(0,"#fed7aa");bg.addColorStop(0.45,"#f97316");bg.addColorStop(1,"#7c2d12");
  ctx.fillStyle=bg;ctx.shadowColor="#f97316";ctx.shadowBlur=12+beat*10;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Musical note symbol ♪
  ctx.fillStyle="#fff";ctx.shadowBlur=0;ctx.shadowColor="transparent";
  ctx.font=`bold ${Math.floor(r*1.1)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("♪",0,0);
  // Orbiting music dots
  for(let i=0;i<3;i++){
    const angle=ts*0.003+i*(Math.PI*2/3);
    const ox=Math.cos(angle)*(r+7);const oy=Math.sin(angle)*(r+7);
    ctx.globalAlpha=0.7*pulse;ctx.fillStyle=["#fde68a","#fca5a5","#86efac"][i];
    ctx.beginPath();ctx.arc(ox,oy,3,0,Math.PI*2);ctx.fill();
  }
  ctx.restore();
}

// ── Vanishing target — blinks in/out of visibility every ~0.8s ──
function drawVanishing(ctx, r, ts) {
  // Blink cycle: visible for 40% of cycle, invisible for 60%
  const cycleLen=800; // ms
  const phase=(ts%cycleLen)/cycleLen;
  const visible=phase<0.4; // visible 40% of the time
  const fadeFrac=visible?(phase<0.05?phase/0.05:(0.4-phase<0.05?(0.4-phase)/0.05:1)):0;
  if(fadeFrac<=0)return; // fully invisible — still hittable though!
  const pulse=0.5+0.5*Math.sin(ts*0.008);
  ctx.save();
  ctx.globalAlpha=fadeFrac*(0.7+pulse*0.2);
  // Ghostly white/cyan body
  const vg=ctx.createRadialGradient(0,0,0,0,0,r);
  vg.addColorStop(0,"rgba(240,255,255,0.92)");vg.addColorStop(0.5,"rgba(103,232,249,0.8)");vg.addColorStop(1,"rgba(14,165,233,0.6)");
  ctx.fillStyle=vg;ctx.shadowColor="#67e8f9";ctx.shadowBlur=20+pulse*14;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Blink ring
  ctx.globalAlpha=fadeFrac*0.7;ctx.strokeStyle="#bae6fd";ctx.lineWidth=2.5;ctx.shadowBlur=8;
  ctx.beginPath();ctx.arc(0,0,r*1.18,0,Math.PI*2);ctx.stroke();
  // ? symbol — what is it?
  ctx.globalAlpha=fadeFrac*(0.7+pulse*0.25);ctx.fillStyle="#ffffff";ctx.shadowBlur=0;
  ctx.font=`bold ${Math.round(r*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("?",0,0);
  ctx.restore();
}

// ── Ninja target — nearly invisible, materializes in last 20% of lifetime ──
function drawNinja(ctx, r, ts, lifeRatio) {
  // Only fully visible in last 20%; faint shimmer hint for 80-40%; invisible for 40-20%
  let alpha;
  if(lifeRatio>0.6) alpha=0.04+0.06*Math.sin(ts*0.004); // barely visible shimmer hint
  else if(lifeRatio>0.2) alpha=0.0; // completely invisible
  else alpha=Math.pow(1-lifeRatio/0.2,2)*0.95; // materializes rapidly in final 20%
  if(alpha<=0.01) return; // skip draw
  ctx.save();ctx.globalAlpha=alpha;
  // Dark smoke body
  const bg=ctx.createRadialGradient(0,0,0,0,0,r);
  bg.addColorStop(0,"#9ca3af");bg.addColorStop(0.5,"#6b7280");bg.addColorStop(1,"#111827");
  ctx.fillStyle=bg;ctx.shadowColor="#374151";ctx.shadowBlur=12;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Ninja mask eyes
  ctx.fillStyle="#ffffff";ctx.shadowBlur=0;
  ctx.beginPath();ctx.arc(-r*0.25,-r*0.1,r*0.13,0,Math.PI*2);ctx.fill();
  ctx.beginPath();ctx.arc(r*0.25,-r*0.1,r*0.13,0,Math.PI*2);ctx.fill();
  // Shuriken star shape
  if(lifeRatio<0.2){
    const spin=ts*0.012;ctx.strokeStyle="#e2e8f0";ctx.lineWidth=1.5;
    for(let i=0;i<4;i++){
      const a=i*Math.PI/2+spin;
      ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.6,Math.sin(a)*r*0.6);
      ctx.lineTo(Math.cos(a+Math.PI/4)*r*0.25,Math.sin(a+Math.PI/4)*r*0.25);
      ctx.stroke();
    }
  }
  ctx.restore();
}

// ── Homing target — drifts toward screen center; arrows show direction ──
function drawHoming(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.007);
  const spin=ts*0.0022;
  ctx.save();
  // Outer orbit ring with arrow markers
  ctx.globalAlpha=0.22+pulse*0.18;ctx.strokeStyle="#34d399";ctx.lineWidth=2.5;
  ctx.shadowColor="#059669";ctx.shadowBlur=18+pulse*12;
  ctx.setLineDash([6,5]);ctx.lineDashOffset=-spin*80;
  ctx.beginPath();ctx.arc(0,0,r*1.38,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);
  // Direction arrows on orbit ring (pointing inward = target moves to center)
  ctx.globalAlpha=0.6+pulse*0.25;ctx.fillStyle="#4ade80";ctx.shadowBlur=6;
  [0,Math.PI*0.66,Math.PI*1.32].forEach(a=>{
    const rx=Math.cos(a+spin)*r*1.38,ry=Math.sin(a+spin)*r*1.38;
    ctx.save();ctx.translate(rx,ry);ctx.rotate(a+spin+Math.PI); // pointing inward
    ctx.beginPath();ctx.moveTo(0,-4.5);ctx.lineTo(3.5,3);ctx.lineTo(-3.5,3);ctx.closePath();ctx.fill();
    ctx.restore();
  });
  ctx.globalAlpha=1;
  // Core — green gradient
  const hg=ctx.createRadialGradient(0,0,0,0,0,r);
  hg.addColorStop(0,"rgba(134,239,172,0.95)");hg.addColorStop(0.6,"rgba(52,211,153,0.88)");hg.addColorStop(1,"rgba(5,150,105,0.8)");
  ctx.fillStyle=hg;ctx.shadowColor="#10b981";ctx.shadowBlur=14;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Target crosshair
  ctx.globalAlpha=0.7+pulse*0.2;ctx.strokeStyle="#d1fae5";ctx.lineWidth=1.5;ctx.shadowBlur=4;
  ctx.beginPath();ctx.moveTo(-r*0.55,0);ctx.lineTo(r*0.55,0);ctx.stroke();
  ctx.beginPath();ctx.moveTo(0,-r*0.55);ctx.lineTo(0,r*0.55);ctx.stroke();
  ctx.beginPath();ctx.arc(0,0,r*0.3,0,Math.PI*2);ctx.stroke();
  ctx.restore();
}

// ── Bouncy target — fast-moving elastic ball with squish trails ──
function drawBouncy(ctx, r, ts) {
  const bounce=0.5+0.5*Math.sin(ts*0.014);
  const squish=1+bounce*0.12;
  ctx.save();
  // Elastic squish transform
  ctx.scale(squish,1/squish);
  // Outer glow
  ctx.globalAlpha=0.4+bounce*0.3;
  ctx.fillStyle="#fef08a";ctx.shadowColor="#eab308";ctx.shadowBlur=20+bounce*10;
  ctx.beginPath();ctx.arc(0,0,r*1.4,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;
  // Gradient body
  const bg=ctx.createRadialGradient(-r*0.28,-r*0.3,0,0,0,r);
  bg.addColorStop(0,"#fef9c3");bg.addColorStop(0.4,"#fde047");bg.addColorStop(0.8,"#eab308");bg.addColorStop(1,"#a16207");
  ctx.fillStyle=bg;ctx.shadowColor="#eab308";ctx.shadowBlur=12+bounce*6;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Shine highlight
  ctx.fillStyle="#fffbeb66";
  ctx.beginPath();ctx.ellipse(-r*0.22,-r*0.3,r*0.3,r*0.18,Math.PI*0.3,0,Math.PI*2);ctx.fill();
  // Speed lines (3 arcs)
  ctx.globalAlpha=0.45;ctx.strokeStyle="#fbbf24";ctx.lineWidth=1.5;
  for(let i=0;i<3;i++){
    ctx.beginPath();ctx.arc(0,0,r*(1.1+i*0.12),Math.PI*0.6,Math.PI*1.4);ctx.stroke();
  }
  ctx.restore();
}

// ── Frozen target — smaller tap zone, 3× points, crystalline ice ──
function drawFrozen(ctx, r, ts) {
  const sparkle=0.5+0.5*Math.sin(ts*0.012);
  const spin=ts*0.0008;
  ctx.save();
  // Ice crystal outer ring with snowflake spokes
  ctx.globalAlpha=0.5+sparkle*0.3;ctx.strokeStyle="#e0f2fe";ctx.lineWidth=1.5;ctx.shadowColor="#93c5fd";ctx.shadowBlur=18;
  for(let i=0;i<6;i++){
    const a=i*Math.PI/3+spin;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*1.35,Math.sin(a)*r*1.35);ctx.stroke();
    // Small crossbar
    const mx=Math.cos(a)*r*0.8,my=Math.sin(a)*r*0.8;
    const bx=Math.cos(a+Math.PI/2)*5,by=Math.sin(a+Math.PI/2)*5;
    ctx.beginPath();ctx.moveTo(mx-bx,my-by);ctx.lineTo(mx+bx,my+by);ctx.stroke();
  }
  // Icy body
  ctx.globalAlpha=1;
  const bg=ctx.createRadialGradient(0,0,0,0,0,r);
  bg.addColorStop(0,"#f0f9ff");bg.addColorStop(0.4,"#bfdbfe");bg.addColorStop(0.75,"#60a5fa");bg.addColorStop(1,"#2563eb");
  ctx.fillStyle=bg;ctx.shadowColor="#93c5fd";ctx.shadowBlur=14+sparkle*8;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // ×3 label
  ctx.fillStyle="#ffffff";ctx.font=`black ${r*0.6}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.shadowBlur=0;
  ctx.fillText("×3",0,0);
  // Reduce effective tap radius visual indicator (dotted inner circle at 70%)
  ctx.globalAlpha=0.35;ctx.setLineDash([3,3]);ctx.strokeStyle="#ff0000";ctx.lineWidth=1.5;
  ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);
  ctx.restore();
}

// ── Rainbow target — cycles rarity every ~2s, score based on current tier ──
const RAINBOW_CYCLE=[
  {color:"#a0a0a0",glow:"#c0c0c0",name:"common",mult:1},
  {color:"#4ecb71",glow:"#22c55e",name:"uncommon",mult:2},
  {color:"#60a5fa",glow:"#3b82f6",name:"rare",mult:3},
  {color:"#b06de8",glow:"#9333ea",name:"epic",mult:5},
  {color:"#ffd700",glow:"#f59e0b",name:"legendary",mult:10},
];
function getRainbowTier(ts){return RAINBOW_CYCLE[Math.floor(ts/2000)%RAINBOW_CYCLE.length];}
function drawRainbow(ctx, r, ts) {
  const tier=getRainbowTier(ts);
  const frac=(ts%2000)/2000; // 0→1 within current cycle
  const pulse=0.5+0.5*Math.sin(ts*0.01);
  const spin=ts*0.001;
  ctx.save();
  // Rainbow ring that rotates with hue shift
  ctx.globalAlpha=0.6;ctx.lineWidth=3;ctx.shadowColor=tier.glow;ctx.shadowBlur=18;
  const grad=ctx.createConicalGradient?.(0,0,0)||null;
  ctx.strokeStyle=tier.color;
  ctx.beginPath();ctx.arc(0,0,r*1.4,0,Math.PI*2);ctx.stroke();
  // Body with current tier color
  ctx.globalAlpha=1;
  const bg=ctx.createRadialGradient(0,0,0,0,0,r);
  bg.addColorStop(0,"#ffffff88");bg.addColorStop(0.4,tier.color);bg.addColorStop(1,tier.glow);
  ctx.fillStyle=bg;ctx.shadowColor=tier.glow;ctx.shadowBlur=16+pulse*10;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Tier label in center
  ctx.fillStyle="#fff";ctx.font=`bold ${r*0.5}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.shadowBlur=0;
  ctx.fillText(["C","U","R","E","L"][RAINBOW_CYCLE.indexOf(tier)]||"?",0,0);
  // Progress arc for next cycle
  ctx.globalAlpha=0.45;ctx.strokeStyle="#fff";ctx.lineWidth=2;
  ctx.beginPath();ctx.arc(0,0,r+4,-Math.PI/2,-Math.PI/2+frac*Math.PI*2);ctx.stroke();
  ctx.restore();
}

// ── Twin target — golden amber, tapping one scores both ──
function drawTwin(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.006);
  const spin=ts*0.0012;
  ctx.save();
  // Outer glow
  ctx.globalAlpha=0.3+pulse*0.2;
  ctx.strokeStyle="#f59e0b";ctx.lineWidth=3;ctx.shadowColor="#fbbf24";ctx.shadowBlur=20;
  ctx.setLineDash([6,4]);ctx.lineDashOffset=spin*50;
  ctx.beginPath();ctx.arc(0,0,r*1.45,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);ctx.globalAlpha=1;
  // Body
  const bg=ctx.createRadialGradient(0,0,0,0,0,r);
  bg.addColorStop(0,"#fde68a");bg.addColorStop(0.5,"#f59e0b");bg.addColorStop(1,"#b45309");
  ctx.fillStyle=bg;ctx.shadowColor="#f59e0b";ctx.shadowBlur=14+pulse*8;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // ∞ or ✦ symbol
  ctx.fillStyle="#ffffff";ctx.font=`bold ${r*0.8}px serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.shadowBlur=0;
  ctx.fillText("✦",0,0);
  // Two small orbiting dots
  for(let i=0;i<2;i++){
    const a=spin*3+i*Math.PI;const d=r*1.25;
    ctx.fillStyle="#fde68a";ctx.globalAlpha=0.8;
    ctx.beginPath();ctx.arc(Math.cos(a)*d,Math.sin(a)*d,3.5,0,Math.PI*2);ctx.fill();
  }
  ctx.restore();
}

// ── Morph target — cycles through rarity tiers; catch at legendary for jackpot ──
const MORPH_CYCLE=[
  {color:"#4ade80",glow:"#16a34a",label:"C"},  // common  0-1.4s
  {color:"#60a5fa",glow:"#2563eb",label:"U"},  // uncommon
  {color:"#f472b6",glow:"#db2777",label:"R"},  // rare
  {color:"#c084fc",glow:"#7c3aed",label:"E"},  // epic
  {color:"#ffd700",glow:"#b8860b",label:"L"},  // legendary
];
function drawMorph(ctx, r, ts, spawnedAt) {
  const cycleMs=1400;
  const elapsed=Date.now()-spawnedAt;
  const phase=Math.floor((elapsed/cycleMs)%MORPH_CYCLE.length);
  const phasePct=(elapsed%cycleMs)/cycleMs;
  const tier=MORPH_CYCLE[phase];
  const nextTier=MORPH_CYCLE[(phase+1)%MORPH_CYCLE.length];
  // Blend color between phases
  const pulse=0.75+0.25*Math.sin(ts*0.012);
  const spin=ts*0.0014;
  // Outer morphing aura
  const grad=ctx.createRadialGradient(0,0,r*0.1,0,0,r*1.4);
  grad.addColorStop(0,tier.color+"cc");
  grad.addColorStop(0.6,tier.color+"55");
  grad.addColorStop(1,"transparent");
  ctx.save();
  ctx.globalAlpha=0.45*pulse;
  ctx.fillStyle=grad;
  ctx.beginPath();ctx.arc(0,0,r*1.4,0,Math.PI*2);ctx.fill();
  // Inner body — star with 5 points
  ctx.globalAlpha=1;
  ctx.rotate(spin);
  const bg=ctx.createRadialGradient(0,0,0,0,0,r);
  bg.addColorStop(0,"#ffffff");
  bg.addColorStop(0.35,tier.color);
  bg.addColorStop(1,tier.glow+"aa");
  ctx.fillStyle=bg;
  ctx.beginPath();
  const pts=5,outer=r,inner=r*0.48;
  for(let i=0;i<pts*2;i++){
    const rad=i%2===0?outer:inner;
    const ang=(i*Math.PI/pts)-Math.PI/2;
    i===0?ctx.moveTo(Math.cos(ang)*rad,Math.sin(ang)*rad):ctx.lineTo(Math.cos(ang)*rad,Math.sin(ang)*rad);
  }
  ctx.closePath();ctx.fill();
  ctx.strokeStyle=tier.color;ctx.lineWidth=1.5;ctx.shadowColor=tier.glow;ctx.shadowBlur=10+pulse*10;ctx.stroke();
  ctx.restore();
  // Tier label
  ctx.save();
  ctx.font=`bold ${Math.floor(r*0.7)}px 'Exo 2',sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillStyle="#ffffff";ctx.shadowColor=tier.glow;ctx.shadowBlur=8;
  ctx.fillText(tier.label,0,0);
  // Phase progress ring
  ctx.restore();
  ctx.save();
  const ringR=r+3.5,circ=2*Math.PI*ringR;
  ctx.strokeStyle=nextTier.color;ctx.lineWidth=2;ctx.globalAlpha=0.6;
  ctx.shadowColor=nextTier.glow;ctx.shadowBlur=6;
  ctx.beginPath();ctx.arc(0,0,ringR,-Math.PI/2,-Math.PI/2+phasePct*Math.PI*2);ctx.stroke();
  ctx.restore();
}

// ── Gemstone target — prismatic gem with rainbow light refraction ──
function drawGemstone(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.006);
  const spin=ts*0.0008;
  // Rainbow color cycling
  const hue=(ts*0.05)%360;
  const hue2=(hue+120)%360;
  const hue3=(hue+240)%360;
  ctx.save();
  // Outer rainbow aura
  ctx.globalAlpha=0.2+pulse*0.2;
  const ag=ctx.createConicalGradient?null:null; // fallback — use simple glow
  ctx.strokeStyle=`hsl(${hue},100%,70%)`;ctx.lineWidth=4.5;
  ctx.shadowColor=`hsl(${hue},100%,60%)`;ctx.shadowBlur=28+pulse*18;
  ctx.beginPath();ctx.arc(0,0,r*1.3,0,Math.PI*2);ctx.stroke();
  // Diamond octagon shape
  ctx.globalAlpha=1;
  const sides=8;
  const gg=ctx.createLinearGradient(-r,-r,r,r);
  gg.addColorStop(0,`hsl(${hue},90%,75%)`);gg.addColorStop(0.33,`hsl(${hue2},90%,70%)`);
  gg.addColorStop(0.66,`hsl(${hue3},90%,75%)`);gg.addColorStop(1,`hsl(${hue},90%,80%)`);
  ctx.fillStyle=gg;ctx.shadowColor=`hsl(${hue2},100%,65%)`;ctx.shadowBlur=18;
  ctx.beginPath();
  for(let i=0;i<sides;i++){
    const a=spin+i*(Math.PI*2/sides);const scaledR=r*(i%2===0?1.0:0.72);
    if(i===0)ctx.moveTo(Math.cos(a)*scaledR,Math.sin(a)*scaledR);
    else ctx.lineTo(Math.cos(a)*scaledR,Math.sin(a)*scaledR);
  }
  ctx.closePath();ctx.fill();
  // Inner facet lines
  ctx.globalAlpha=0.45+pulse*0.2;ctx.strokeStyle="#ffffff";ctx.lineWidth=1;ctx.shadowBlur=0;
  for(let i=0;i<4;i++){
    const a=spin+i*(Math.PI/2);
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*0.75,Math.sin(a)*r*0.75);ctx.stroke();
  }
  // Bright center glint
  ctx.globalAlpha=0.85+pulse*0.12;ctx.fillStyle="#ffffff";ctx.shadowColor="#fff";ctx.shadowBlur=12;
  ctx.beginPath();ctx.arc(-r*0.2,-r*0.25,r*0.12,0,Math.PI*2);ctx.fill();
  ctx.restore();
}

// ── Loot Chest — golden chest with coin sparkles, drops 3-5 coins ──
function drawLootChest(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.007);
  const shake=Math.sin(ts*0.022)*1.5;
  ctx.save();
  ctx.translate(shake,0);
  // Outer golden glow
  ctx.globalAlpha=0.35+pulse*0.3;
  ctx.fillStyle="#ffd700";ctx.shadowColor="#ffd700";ctx.shadowBlur=22+pulse*12;
  ctx.beginPath();ctx.arc(0,0,r*1.45,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;
  // Chest body (rounded rect)
  const bw=r*1.4,bh=r*1.0;
  ctx.shadowColor="#b45309";ctx.shadowBlur=8;
  const bg=ctx.createLinearGradient(0,-bh/2,0,bh/2);
  bg.addColorStop(0,"#fde68a");bg.addColorStop(0.45,"#f59e0b");bg.addColorStop(1,"#92400e");
  ctx.fillStyle=bg;
  ctx.beginPath();ctx.roundRect(-bw/2,-bh/2,bw,bh,4);ctx.fill();
  // Lid
  const lidGrad=ctx.createLinearGradient(0,-bh/2,0,-bh/2+bh*0.35);
  lidGrad.addColorStop(0,"#fef3c7");lidGrad.addColorStop(1,"#d97706");
  ctx.fillStyle=lidGrad;ctx.beginPath();ctx.roundRect(-bw/2,-bh/2,bw,bh*0.35,4);ctx.fill();
  // Lid line
  ctx.strokeStyle="#7c2d12";ctx.lineWidth=1.5;ctx.shadowBlur=0;
  ctx.beginPath();ctx.moveTo(-bw/2,-bh*0.15);ctx.lineTo(bw/2,-bh*0.15);ctx.stroke();
  // Lock clasp
  ctx.fillStyle="#ffd700";ctx.shadowColor="#ffd700";ctx.shadowBlur=6+pulse*4;
  ctx.beginPath();ctx.arc(0,-bh*0.13,4,0,Math.PI*2);ctx.fill();
  ctx.strokeStyle="#b45309";ctx.lineWidth=1.5;ctx.shadowBlur=0;
  ctx.beginPath();ctx.arc(0,-bh*0.13,4,0,Math.PI*2);ctx.stroke();
  // Coin sparkles
  const coinAngles=[0,Math.PI*0.6,Math.PI*1.4];
  coinAngles.forEach((a,i)=>{
    const oa=a+ts*0.002+i*0.8;
    const od=r*(0.9+0.3*Math.sin(ts*0.006+i));
    const px=Math.cos(oa)*od,py=Math.sin(oa)*od;
    ctx.globalAlpha=(0.5+0.5*Math.sin(ts*0.008+i))*0.8;
    ctx.fillStyle="#ffd700";ctx.shadowColor="#ffd700";ctx.shadowBlur=8;
    ctx.font=`bold ${r*0.38}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText("🪙",px,py);
  });
  ctx.restore();
}

// ── Bubble target — large translucent soap bubble, generous hit zone ──
function drawBubble(ctx, r, ts) {
  const shimmer=0.5+0.5*Math.sin(ts*0.006);
  const drift=Math.sin(ts*0.0045)*2;
  ctx.save();
  ctx.translate(0,drift);
  // Outer translucent bubble
  ctx.globalAlpha=0.12+shimmer*0.08;
  ctx.fillStyle="#e0f7ff";ctx.shadowColor="#67e8f9";ctx.shadowBlur=20;
  ctx.beginPath();ctx.arc(0,0,r*1.25,0,Math.PI*2);ctx.fill();
  // Iridescent rim ring
  ctx.globalAlpha=0.5+shimmer*0.3;
  const rimGrad=ctx.createLinearGradient(-r,-r,r,r);
  rimGrad.addColorStop(0,"#f472b6");rimGrad.addColorStop(0.25,"#67e8f9");
  rimGrad.addColorStop(0.5,"#c084fc");rimGrad.addColorStop(0.75,"#4ade80");rimGrad.addColorStop(1,"#fbbf24");
  ctx.strokeStyle=rimGrad;ctx.lineWidth=3.5;ctx.shadowColor="#60a5fa";ctx.shadowBlur=10;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();
  // Inner sheen (half-moon highlight)
  ctx.globalAlpha=0.35+shimmer*0.2;
  ctx.fillStyle="#ffffff";ctx.shadowBlur=0;
  ctx.beginPath();ctx.ellipse(-r*0.28,-r*0.32,r*0.38,r*0.22,Math.PI*0.35,0,Math.PI*2);ctx.fill();
  // Small secondary highlight
  ctx.globalAlpha=0.25;
  ctx.beginPath();ctx.ellipse(r*0.2,r*0.3,r*0.12,r*0.07,Math.PI*0.1,0,Math.PI*2);ctx.fill();
  ctx.restore();
}

// ── Rage target — grows angrier (red, faster) the longer it lives ──
function drawRage(ctx, r, ts, rageLevel) {
  const rl=Math.min(1,rageLevel||0); // 0=calm, 1=max rage
  const pulse=0.5+0.5*Math.sin(ts*0.01*(1+rl*3));
  const shake=rl>0.5?Math.sin(ts*0.04)*2.5*rl:0;
  ctx.save();ctx.translate(shake,0);
  // Outer aura — grows redder with rage
  ctx.globalAlpha=0.2+rl*0.35;
  const auraColor=rl<0.5?`rgba(251,146,60,${0.5+pulse*0.3})`:`rgba(239,68,68,${0.5+pulse*0.3})`;
  ctx.fillStyle=auraColor;ctx.shadowColor=rl<0.5?"#fb923c":"#ef4444";ctx.shadowBlur=20+rl*28;
  ctx.beginPath();ctx.arc(0,0,r*(1.2+rl*0.35),0,Math.PI*2);ctx.fill();
  // Core body — orange to red gradient
  ctx.globalAlpha=1;
  const cg=ctx.createRadialGradient(0,0,0,0,0,r);
  const innerColor=rl<0.5?`rgba(251,113,133,${0.9+pulse*0.08})`:`rgba(220,38,38,${0.9+pulse*0.08})`;
  const outerColor=rl<0.5?"rgba(239,68,68,0.85)":"rgba(127,29,29,0.85)";
  cg.addColorStop(0,innerColor);cg.addColorStop(1,outerColor);
  ctx.fillStyle=cg;ctx.shadowColor=rl<0.5?"#f97316":"#dc2626";ctx.shadowBlur=12+rl*12;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Rage veins (jagged lines radiating out)
  if(rl>0.3){
    ctx.globalAlpha=(rl-0.3)*0.8+0.1;ctx.strokeStyle="#fca5a5";ctx.lineWidth=1.2;ctx.shadowBlur=4;
    for(let i=0;i<Math.floor(3+rl*5);i++){
      const angle=(i/8)*Math.PI*2+ts*0.002;
      const inner=r*0.45;const outer=r*0.82;
      ctx.beginPath();ctx.moveTo(Math.cos(angle)*inner,Math.sin(angle)*inner);
      ctx.lineTo(Math.cos(angle+0.25)*outer,Math.sin(angle+0.25)*outer);ctx.stroke();
    }
  }
  // Rage face (👿 emoji approximation)
  ctx.globalAlpha=0.85+rl*0.1;ctx.fillStyle="#fff";ctx.shadowBlur=0;
  ctx.font=`bold ${Math.round(r*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(rl>0.6?"😡":rl>0.3?"😠":"😤",0,0);
  ctx.restore();
}

// ── Crystal target — hexagonal gem that shatters into 3 scoreable shards ──
function drawCrystalTarget(ctx, r, ts) {
  const spin=ts*0.0012;const pulse=0.5+0.5*Math.sin(ts*0.006);
  ctx.save();
  // Outer glow halo
  ctx.globalAlpha=0.2+pulse*0.18;ctx.shadowColor="#60a5fa";ctx.shadowBlur=28+pulse*18;
  ctx.strokeStyle="#93c5fd";ctx.lineWidth=3.5;
  ctx.beginPath();for(let i=0;i<6;i++){const a=spin+i*Math.PI/3;const x2=Math.cos(a)*(r*1.35),y2=Math.sin(a)*(r*1.35);i===0?ctx.moveTo(x2,y2):ctx.lineTo(x2,y2);}ctx.closePath();ctx.stroke();
  // Crystal body hexagon
  ctx.globalAlpha=1;
  const hexGrad=ctx.createLinearGradient(-r,-r,r,r);
  hexGrad.addColorStop(0,"rgba(191,219,254,0.95)");hexGrad.addColorStop(0.4,"rgba(96,165,250,0.85)");hexGrad.addColorStop(1,"rgba(37,99,235,0.8)");
  ctx.fillStyle=hexGrad;ctx.shadowColor="#60a5fa";ctx.shadowBlur=16;
  ctx.beginPath();for(let i=0;i<6;i++){const a=spin+i*Math.PI/3;const x2=Math.cos(a)*r*1.0,y2=Math.sin(a)*r*1.0;i===0?ctx.moveTo(x2,y2):ctx.lineTo(x2,y2);}ctx.closePath();ctx.fill();
  // Facet lines — inner reflections
  ctx.globalAlpha=0.35+pulse*0.2;ctx.strokeStyle="#dbeafe";ctx.lineWidth=1;ctx.shadowBlur=0;
  for(let i=0;i<6;i++){const a=spin+i*Math.PI/3;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*0.85,Math.sin(a)*r*0.85);ctx.stroke();}
  // Bright glint
  ctx.globalAlpha=0.7+pulse*0.25;ctx.fillStyle="#eff6ff";ctx.shadowColor="#ffffff";ctx.shadowBlur=8;
  ctx.beginPath();ctx.arc(-r*0.25,-r*0.3,r*0.15,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
function drawCrystalShard(ctx, r, ts, bornAt) {
  const age=performance.now()-(bornAt||performance.now());
  const life=Math.max(0,1-age/2000);if(life<=0)return;
  const spin=ts*0.004;
  ctx.save();ctx.globalAlpha=life*0.9;
  const sg=ctx.createLinearGradient(-r,-r,r,r);
  sg.addColorStop(0,"rgba(219,234,254,0.95)");sg.addColorStop(1,"rgba(96,165,250,0.8)");
  ctx.fillStyle=sg;ctx.shadowColor="#60a5fa";ctx.shadowBlur=10*life;
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(r*0.6,r*0.6);ctx.lineTo(-r*0.6,r*0.6);ctx.closePath();ctx.fill();
  ctx.globalAlpha=life*0.55;ctx.strokeStyle="#bfdbfe";ctx.lineWidth=1.2;ctx.stroke();
  ctx.restore();
}

// ── Echo target — leaves a ghost echo on tap; tap the echo for bonus ──
function drawEcho(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.008);
  const spin=ts*0.0018;
  ctx.save();
  // Outer echo rings
  for(let ring=0;ring<3;ring++){
    const rr=r*(0.6+ring*0.35);
    const phase=ring*(Math.PI*2/3)+spin;
    ctx.globalAlpha=(0.18+pulse*0.14)*(1-ring*0.25);
    ctx.strokeStyle="#a78bfa";ctx.lineWidth=2.5-ring*0.5;ctx.shadowColor="#7c3aed";ctx.shadowBlur=12;
    ctx.setLineDash([Math.PI*rr/4,Math.PI*rr/4]);ctx.lineDashOffset=phase*rr;
    ctx.beginPath();ctx.arc(0,0,rr,0,Math.PI*2);ctx.stroke();
  }
  ctx.setLineDash([]);ctx.globalAlpha=1;
  // Core — translucent violet orb
  const cg=ctx.createRadialGradient(0,0,0,0,0,r*0.52);
  cg.addColorStop(0,`rgba(196,181,253,${0.85+pulse*0.12})`);
  cg.addColorStop(0.6,`rgba(139,92,246,${0.7+pulse*0.1})`);
  cg.addColorStop(1,"rgba(91,33,182,0.55)");
  ctx.fillStyle=cg;ctx.shadowColor="#7c3aed";ctx.shadowBlur=22+pulse*14;
  ctx.beginPath();ctx.arc(0,0,r*0.52,0,Math.PI*2);ctx.fill();
  // Echo symbol — two overlapping circles
  ctx.globalAlpha=0.55+pulse*0.25;ctx.strokeStyle="#e9d5ff";ctx.lineWidth=1.5;ctx.shadowBlur=4;
  ctx.beginPath();ctx.arc(-r*0.14,0,r*0.22,0,Math.PI*2);ctx.stroke();
  ctx.beginPath();ctx.arc(r*0.14,0,r*0.22,0,Math.PI*2);ctx.stroke();
  ctx.restore();
}
// ── Echo Ghost — fading afterimage of an echo tap ──
function drawEchoGhost(ctx, r, ts, bornAt) {
  const age=ts-(bornAt||ts);
  const life=Math.max(0,1-age/1500);
  if(life<=0)return;
  ctx.save();
  ctx.globalAlpha=life*0.55;
  ctx.strokeStyle="#c4b5fd";ctx.lineWidth=3;ctx.shadowColor="#7c3aed";ctx.shadowBlur=18*life;
  ctx.setLineDash([8,6]);
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);
  ctx.globalAlpha=life*0.3;
  ctx.fillStyle="#a78bfa";
  ctx.beginPath();ctx.arc(0,0,r*0.5,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=life*0.75;ctx.fillStyle="#e9d5ff";ctx.font=`bold ${Math.round(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("◎",0,0);
  ctx.restore();
}

// ── Tornado target — blue spinning vortex, scrambles nearby target positions ──
function drawTornado(ctx, r, ts) {
  const spin=ts*0.003;
  const pulse=0.5+0.5*Math.sin(ts*0.007);
  ctx.save();
  // Outer wind halo
  ctx.globalAlpha=0.22+pulse*0.2;
  ctx.strokeStyle="#67e8f9";ctx.lineWidth=4;ctx.shadowColor="#06b6d4";ctx.shadowBlur=22+pulse*14;
  ctx.beginPath();ctx.arc(0,0,r*1.55,0,Math.PI*2);ctx.stroke();
  ctx.globalAlpha=1;
  // Spiral arms (3)
  for(let arm=0;arm<3;arm++){
    const armAngle=spin+arm*(Math.PI*2/3);
    ctx.globalAlpha=0.7;ctx.strokeStyle="#67e8f9";ctx.lineWidth=2;
    ctx.shadowColor="#06b6d4";ctx.shadowBlur=10;
    ctx.beginPath();
    for(let i=0;i<24;i++){
      const t2=i/23;
      const a=armAngle+t2*Math.PI*1.8;
      const rd=r*0.12+t2*r*0.82;
      if(i===0)ctx.moveTo(Math.cos(a)*rd,Math.sin(a)*rd);
      else ctx.lineTo(Math.cos(a)*rd,Math.sin(a)*rd);
    }
    ctx.stroke();
  }
  ctx.globalAlpha=1;
  // Core
  const cg=ctx.createRadialGradient(0,0,0,0,0,r*0.55);
  cg.addColorStop(0,"#e0f7ff");cg.addColorStop(0.4,"#67e8f9");cg.addColorStop(1,"#0891b2");
  ctx.fillStyle=cg;ctx.shadowColor="#06b6d4";ctx.shadowBlur=14;
  ctx.beginPath();ctx.arc(0,0,r*0.55,0,Math.PI*2);ctx.fill();
  // Eye symbol
  ctx.fillStyle="#ffffff";ctx.font=`bold ${r*0.55}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.shadowBlur=0;ctx.globalAlpha=0.85;
  ctx.fillText("🌀",0,1);
  ctx.restore();
}

// ── Healer target — glowing green cross, restores 1 life when tapped ──
function drawHealer(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.005);
  ctx.save();
  // Outer healing glow
  const grad=ctx.createRadialGradient(0,0,r*0.4,0,0,r*1.6);
  grad.addColorStop(0,"#34d39944");grad.addColorStop(1,"transparent");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*1.6,0,Math.PI*2);ctx.fill();
  // Body
  const bg=ctx.createRadialGradient(0,0,0,0,0,r);
  bg.addColorStop(0,"#6ee7b7");bg.addColorStop(0.55,"#34d399");bg.addColorStop(1,"#059669");
  ctx.fillStyle=bg;ctx.shadowColor="#34d399";ctx.shadowBlur=18+pulse*10;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Cross symbol
  ctx.fillStyle="#ffffff";ctx.shadowBlur=0;
  const arm=r*0.45,thick=r*0.22;
  ctx.beginPath();ctx.roundRect(-thick/2,-arm,thick,arm*2,thick/4);ctx.fill();
  ctx.beginPath();ctx.roundRect(-arm,-thick/2,arm*2,thick,thick/4);ctx.fill();
  // Sparkle particles around it
  for(let i=0;i<4;i++){
    const a=(i/4)*Math.PI*2+ts*0.002;const d=r*1.2;
    ctx.fillStyle="#a7f3d0";ctx.globalAlpha=0.6+0.4*Math.sin(ts*0.008+i);
    ctx.beginPath();ctx.arc(Math.cos(a)*d,Math.sin(a)*d,2.5,0,Math.PI*2);ctx.fill();
  }
  ctx.restore();
}

// ── Volatile target — pulsing bomb-like, explodes on expiry ──
function drawVolatile(ctx, r, ts, lifeRatio) {
  const urgency=1-lifeRatio; // 0=fresh, 1=about to explode
  const pulse=0.5+0.5*Math.sin(ts*0.008*(1+urgency*4));
  const sz=r*(1+urgency*0.25+pulse*0.08);
  ctx.save();
  // Outer explosive ring
  ctx.globalAlpha=0.4+urgency*0.4;
  ctx.strokeStyle=`hsl(${20-urgency*20},100%,50%)`;
  ctx.lineWidth=2+urgency*2;ctx.shadowColor="#ff4500";ctx.shadowBlur=18+urgency*20;
  ctx.beginPath();ctx.arc(0,0,sz*1.4,0,Math.PI*2);ctx.stroke();
  // Body gradient
  ctx.globalAlpha=1;
  const bg=ctx.createRadialGradient(0,0,0,0,0,sz);
  const hue=20-urgency*20;
  bg.addColorStop(0,`hsl(${hue+30},100%,70%)`);
  bg.addColorStop(0.5,`hsl(${hue},100%,50%)`);
  bg.addColorStop(1,`hsl(${hue-15},100%,30%)`);
  ctx.fillStyle=bg;ctx.shadowColor=`hsl(${hue},100%,50%)`;ctx.shadowBlur=16+urgency*12;
  ctx.beginPath();ctx.arc(0,0,sz,0,Math.PI*2);ctx.fill();
  // ⚠ warning icon
  ctx.fillStyle="#fff";ctx.font=`bold ${sz*0.85}px serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💣",0,0);
  // Timer ring
  ctx.globalAlpha=0.7;ctx.strokeStyle="#ffffff";ctx.lineWidth=2.5;
  ctx.shadowBlur=0;ctx.beginPath();
  ctx.arc(0,0,sz+4,-Math.PI/2,-Math.PI/2+lifeRatio*Math.PI*2);ctx.stroke();
  ctx.restore();
}

// ── Phantom target — ultra-short lifetime, huge points, ghostly flicker ──
function drawPhantom(ctx, r, ts, lifeRatio) {
  // Flicker faster as time runs out
  const flicker=lifeRatio>0.4?1:0.4+0.6*Math.abs(Math.sin(ts*0.04*(1-lifeRatio+0.5)));
  const pulse=0.4+0.6*Math.sin(ts*0.009);
  ctx.save();
  ctx.globalAlpha=flicker*0.85*(0.5+0.5*lifeRatio);
  // Outer glow ring
  const grad=ctx.createRadialGradient(0,0,r*0.3,0,0,r*1.5);
  grad.addColorStop(0,"#e879f988");grad.addColorStop(1,"transparent");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*1.5,0,Math.PI*2);ctx.fill();
  // Body
  const bg=ctx.createRadialGradient(0,0,0,0,0,r);
  bg.addColorStop(0,"#f0abfc");bg.addColorStop(0.6,"#e879f9");bg.addColorStop(1,"#a21caf");
  ctx.fillStyle=bg;
  ctx.shadowColor="#e879f9";ctx.shadowBlur=20+pulse*12;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Ghost symbol (∞)
  ctx.globalAlpha*=0.9;ctx.fillStyle="#ffffff";ctx.font=`bold ${r*0.8}px serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("👻",0,0);
  ctx.restore();
}

// ── Shielded target — 2 taps (shield absorbs first) ──
function drawShielded(ctx, r, shieldUp, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.004);
  const spin=ts*0.0015;
  ctx.save();
  // Outer glow
  const grd=ctx.createRadialGradient(0,0,r*0.3,0,0,r*2.1);
  grd.addColorStop(0,"#818cf855");grd.addColorStop(0.6,"#818cf822");grd.addColorStop(1,"transparent");
  ctx.fillStyle=grd;ctx.beginPath();ctx.arc(0,0,r*2.1,0,Math.PI*2);ctx.fill();
  // Target body (indigo)
  ctx.shadowColor="#6366f1";ctx.shadowBlur=r*(0.5+pulse*0.3);
  const body=ctx.createRadialGradient(-r*0.3,-r*0.3,0,0,0,r);
  body.addColorStop(0,"#c7d2fe");body.addColorStop(0.5,"#6366f1");body.addColorStop(1,"#3730a3");
  ctx.fillStyle=body;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Shield ring (hexagonal spinning)
  if(shieldUp){
    ctx.save();ctx.rotate(spin);
    ctx.strokeStyle=`rgba(165,180,252,${0.7+pulse*0.3})`;ctx.lineWidth=3;
    ctx.shadowColor="#a5b4fc";ctx.shadowBlur=12+pulse*8;
    ctx.beginPath();
    for(let i=0;i<6;i++){
      const a=i*Math.PI/3;const nr=(i+1)*Math.PI/3;
      ctx.lineTo(Math.cos(a)*r*1.5,Math.sin(a)*r*1.5);
    }
    ctx.closePath();ctx.stroke();
    // Shield "S" icon
    ctx.font=`bold ${r*0.55}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillStyle="rgba(255,255,255,0.9)";ctx.shadowBlur=6;ctx.shadowColor="#a5b4fc";
    ctx.fillText("🛡",0,0);
    ctx.restore();
  } else {
    // Cracked shield (shield broken — one more hit)
    ctx.save();
    ctx.strokeStyle=`rgba(253,186,116,${0.5+pulse*0.3})`;ctx.lineWidth=1.5;
    ctx.setLineDash([4,4]);ctx.lineDashOffset=ts*0.02;
    ctx.beginPath();ctx.arc(0,0,r*1.45,0,Math.PI*2);ctx.stroke();
    ctx.setLineDash([]);
    ctx.font=`bold ${r*0.55}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillStyle="rgba(255,200,100,0.8)";ctx.shadowColor="#fb923c";ctx.shadowBlur=4;
    ctx.fillText("💥",0,0);
    ctx.restore();
  }
  ctx.restore();
}

// ── Voltage target — electric orb; chain-zaps 3 nearby targets when tapped ──
function drawVoltage(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.009);
  // Outer electric field
  const outerGrd=ctx.createRadialGradient(0,0,r*0.4,0,0,r*1.6);
  outerGrd.addColorStop(0,"#facc1544");outerGrd.addColorStop(1,"transparent");
  ctx.save();ctx.globalAlpha=0.5*pulse;ctx.fillStyle=outerGrd;
  ctx.beginPath();ctx.arc(0,0,r*1.6,0,Math.PI*2);ctx.fill();ctx.restore();
  // Lightning arcs around border
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.003;
    const jitter=(Math.sin(ts*0.04+i*1.7)*0.25);
    const x1=Math.cos(a)*r*0.7,y1=Math.sin(a)*r*0.7;
    const x2=Math.cos(a+jitter)*r*1.1,y2=Math.sin(a+jitter)*r*1.1;
    ctx.save();ctx.strokeStyle="#facc15";ctx.lineWidth=1.5;ctx.globalAlpha=0.7*pulse;
    ctx.shadowColor="#fbbf24";ctx.shadowBlur=8;
    ctx.beginPath();ctx.moveTo(x1,y1);ctx.lineTo(x2,y2);ctx.stroke();ctx.restore();
  }
  // Core gradient
  const grd=ctx.createRadialGradient(0,0,0,0,0,r);
  grd.addColorStop(0,"#fef08a");grd.addColorStop(0.5,"#fbbf24");grd.addColorStop(1,"#b45309");
  ctx.save();ctx.shadowColor="#fbbf24";ctx.shadowBlur=18+10*pulse;
  ctx.fillStyle=grd;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // ⚡ symbol
  ctx.fillStyle="#fff";ctx.font=`bold ${Math.round(r*0.9)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.95;
  ctx.fillText("⚡",0,1);ctx.restore();
}

// ── Lucky Clover — 4-leaf clover; gives random 1-10× score multiplier on tap ──
function drawLuckyClover(ctx, r, ts) {
  const pulse=0.8+0.2*Math.sin(ts*0.008);
  // Glow aura
  const auraGrd=ctx.createRadialGradient(0,0,r*0.3,0,0,r*1.5);
  auraGrd.addColorStop(0,"#4ade8033");auraGrd.addColorStop(1,"transparent");
  ctx.save();ctx.globalAlpha=pulse;ctx.fillStyle=auraGrd;
  ctx.beginPath();ctx.arc(0,0,r*1.5,0,Math.PI*2);ctx.fill();ctx.restore();
  // 4 leaf circles
  const leafColors=["#22c55e","#16a34a","#4ade80","#15803d"];
  const angles=[0,Math.PI/2,Math.PI,3*Math.PI/2];
  const leafR=r*0.48;const leafOff=r*0.38;
  angles.forEach((a,i)=>{
    const lx=Math.cos(a)*leafOff,ly=Math.sin(a)*leafOff;
    ctx.save();ctx.translate(lx,ly);
    const grd=ctx.createRadialGradient(0,0,0,0,0,leafR);
    grd.addColorStop(0,"#86efac");grd.addColorStop(1,leafColors[i]);
    ctx.fillStyle=grd;ctx.shadowColor="#22c55e";ctx.shadowBlur=8*pulse;
    ctx.beginPath();ctx.arc(0,0,leafR,0,Math.PI*2);ctx.fill();
    ctx.restore();
  });
  // Center hub
  ctx.save();ctx.fillStyle="#166534";ctx.shadowColor="#15803d";ctx.shadowBlur=6;
  ctx.beginPath();ctx.arc(0,0,r*0.18,0,Math.PI*2);ctx.fill();
  // Lucky sparkles
  for(let i=0;i<3;i++){
    const sa=(i/3)*Math.PI*2+ts*0.005;
    const sd=r*(0.9+0.15*Math.sin(ts*0.012+i));
    ctx.fillStyle=`hsl(${140+i*20},90%,70%)`;ctx.globalAlpha=0.7*pulse;
    ctx.beginPath();ctx.arc(Math.cos(sa)*sd,Math.sin(sa)*sd,2.5,0,Math.PI*2);ctx.fill();
  }
  ctx.restore();
  ctx.save();ctx.fillStyle="#fff";ctx.font=`bold ${Math.round(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.9;
  ctx.shadowColor="#22c55e";ctx.shadowBlur=6;
  ctx.fillText("🍀",0,1);ctx.restore();
}

// ── Void target — swirling dark orb; absorbs nearby targets for bonus pts ──
function drawVoid(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.007);
  // Gravity distortion rings
  for(let i=3;i>0;i--){
    ctx.save();ctx.strokeStyle=`hsl(${270+i*10},80%,${30+i*8}%)`;
    ctx.lineWidth=1;ctx.globalAlpha=(0.1+i*0.07)*pulse;
    ctx.beginPath();ctx.arc(0,0,r*(0.9+i*0.35),0,Math.PI*2);ctx.stroke();ctx.restore();
  }
  // Swirling spiral arms
  for(let arm=0;arm<3;arm++){
    ctx.save();ctx.strokeStyle=`hsl(${280+arm*30},90%,70%)`;
    ctx.lineWidth=2;ctx.globalAlpha=0.5*pulse;ctx.shadowColor="#a855f7";ctx.shadowBlur=8;
    ctx.beginPath();
    for(let s=0;s<40;s++){
      const angle=(s/40)*Math.PI*2*1.8+(arm/3)*Math.PI*2-ts*0.005;
      const rad=(s/40)*r*0.85;
      if(s===0)ctx.moveTo(Math.cos(angle)*rad,Math.sin(angle)*rad);
      else ctx.lineTo(Math.cos(angle)*rad,Math.sin(angle)*rad);
    }
    ctx.stroke();ctx.restore();
  }
  // Dark core
  const grd=ctx.createRadialGradient(0,0,0,0,0,r);
  grd.addColorStop(0,"#1e1b4b");grd.addColorStop(0.6,"#4c1d95");grd.addColorStop(1,"#7c3aed88");
  ctx.save();ctx.shadowColor="#a855f7";ctx.shadowBlur=20+8*pulse;
  ctx.fillStyle=grd;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // ◈ eye symbol
  ctx.fillStyle="#e9d5ff";ctx.font=`bold ${Math.round(r*0.85)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.9;
  ctx.fillText("🌀",0,1);ctx.restore();
}

// ── Pinwheel — spins around; top-scoring when caught at "12 o'clock" position ──
function drawPinwheel(ctx,r,ts){
  const spin=ts*0.003;
  ctx.save();
  ctx.rotate(spin);
  ctx.shadowBlur=10;ctx.shadowColor="#fbbf24";
  const blades=4;
  for(let i=0;i<blades;i++){
    const ba=(i/blades)*Math.PI*2;
    ctx.save();ctx.rotate(ba);
    ctx.fillStyle=i%2===0?"#fbbf24":"#f97316";
    ctx.beginPath();ctx.ellipse(r*0.48,0,r*0.58,r*0.22,0,0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
  ctx.fillStyle="#ffffff";ctx.shadowBlur=0;ctx.beginPath();ctx.arc(0,0,r*0.2,0,Math.PI*2);ctx.fill();
  ctx.restore();
  // Orbit score zone indicator (faint arc at top)
  ctx.save();ctx.strokeStyle="#fbbf2440";ctx.lineWidth=3;
  ctx.beginPath();ctx.arc(0,-r*0.0,r*1.15,-Math.PI*0.25,Math.PI*0.25);ctx.stroke();
  ctx.restore();
}
// ── Supercell — storm cloud; electrifies nearby targets (makes them score 2×) ──
function drawSupercell(ctx,r,ts){
  const flicker=0.7+0.3*Math.abs(Math.sin(ts*0.018));
  ctx.save();
  ctx.shadowBlur=16*flicker;ctx.shadowColor="#7dd3fc";
  const grad=ctx.createRadialGradient(0,-r*0.2,0,0,0,r);
  grad.addColorStop(0,"#bae6fd");grad.addColorStop(0.5,"#38bdf8");grad.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,-r*0.1,r,0,Math.PI*2);ctx.fill();
  // Lightning bolt downward
  ctx.strokeStyle="#fde68a";ctx.lineWidth=2.5;ctx.globalAlpha=flicker*0.9;
  ctx.beginPath();ctx.moveTo(r*0.05,-r*0.3);ctx.lineTo(-r*0.15,r*0.1);ctx.lineTo(r*0.1,r*0.1);ctx.lineTo(-r*0.05,r*0.55);ctx.stroke();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.82)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⛈️",0,0);ctx.restore();
}
// ── Vortex — dark swirling portal; pulls nearby targets ──
function drawVortex(ctx,r,ts){
  ctx.save();
  const spin=ts*0.003;
  ctx.shadowBlur=22;ctx.shadowColor="#7c3aed";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#1e1b4b");grad.addColorStop(0.5,"#4c1d95");grad.addColorStop(1,"#7c3aed");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Spiral arms
  ctx.strokeStyle="#a78bfa";ctx.lineWidth=1.8;
  for(let arm=0;arm<3;arm++){
    const ao=spin+(arm*Math.PI*2/3);
    ctx.beginPath();
    for(let t2=0;t2<Math.PI*2;t2+=0.08){
      const rr=r*(0.12+t2/Math.PI/2*0.8);
      const x=Math.cos(ao+t2)*rr,y=Math.sin(ao+t2)*rr;
      t2===0?ctx.moveTo(x,y):ctx.lineTo(x,y);
    }
    ctx.globalAlpha=0.7;ctx.stroke();
  }
  ctx.globalAlpha=1;
  // Center eye
  ctx.shadowBlur=12;ctx.shadowColor="#c4b5fd";
  ctx.fillStyle="#c4b5fd";ctx.beginPath();ctx.arc(0,0,r*0.2,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Pixel — tiny retro 8-bit square target; tiny hitbox, huge pts ──
function drawPixel(ctx,r,ts){
  ctx.save();
  const blink=Math.floor(ts*0.007)%2===0;
  ctx.shadowBlur=blink?20:8;ctx.shadowColor="#00ff88";
  // Outer glow ring
  ctx.strokeStyle=blink?"#00ff88":"#00cc66";ctx.lineWidth=2;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();
  // Pixel art squares
  const sq=Math.round(r*0.38);
  const palette=["#00ff88","#00cc66","#00ff44","#88ffaa","#ffffff"];
  const grid=3;const off=-sq*grid*0.5+sq*0.5;
  const pattern=[[1,0,1],[0,1,0],[1,0,1]]; // pixel cross
  for(let row=0;row<grid;row++){
    for(let col=0;col<grid;col++){
      if(pattern[row][col]){
        ctx.fillStyle=palette[(row*grid+col)%palette.length];
        ctx.fillRect(off+col*sq-sq/2,off+row*sq-sq/2,sq-1,sq-1);
      }
    }
  }
  // Scanline shimmer
  ctx.globalAlpha=0.3;ctx.fillStyle="#ffffff";
  ctx.fillRect(-r,-r*0.05,r*2,r*0.1);
  ctx.globalAlpha=1;ctx.restore();
}
// ── Neon Sign — flashes between 3 colors; bonus based on color when tapped ──
function drawNeonSign(ctx,r,ts,colorPhase){
  ctx.save();
  const colors=["#ff2244","#00ccff","#ffcc00"]; // red=normal, cyan=2×, gold=3×
  const c=colors[colorPhase%3];
  ctx.shadowBlur=22;ctx.shadowColor=c;
  // Sign background
  const grad=ctx.createLinearGradient(-r,-r,r,r);
  grad.addColorStop(0,"#1a1a2e");grad.addColorStop(1,"#16213e");
  ctx.fillStyle=grad;
  ctx.beginPath();
  const w=r*1.7,h=r*1.1;
  ctx.roundRect(-w/2,-h/2,w,h,r*0.2);
  ctx.fill();
  // Neon border
  ctx.strokeStyle=c;ctx.lineWidth=2.5;
  ctx.shadowBlur=16;ctx.shadowColor=c;
  ctx.beginPath();ctx.roundRect(-w/2+3,-h/2+3,w-6,h-6,r*0.15);ctx.stroke();
  // Neon text glow
  ctx.fillStyle=c;ctx.font=`bold ${Math.round(r*0.55)}px monospace`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  const labels=["TAP","!!","★★★"];
  ctx.shadowBlur=20;ctx.shadowColor=c;
  ctx.fillText(labels[colorPhase%3],0,0);
  ctx.restore();
}
// ── Lantern — paper lantern; chain reaction with nearest targets on tap ──
function drawLantern(ctx,r,ts){
  ctx.save();
  const sway=Math.sin(ts*0.004)*0.12;
  ctx.rotate(sway);
  ctx.shadowBlur=18;ctx.shadowColor="#fbbf24";
  // Body gradient
  const grad=ctx.createRadialGradient(0,-r*0.1,0,0,0,r);
  grad.addColorStop(0,"#fffde7");grad.addColorStop(0.4,"#fbbf24");
  grad.addColorStop(0.85,"#f97316");grad.addColorStop(1,"#b45309");
  ctx.fillStyle=grad;
  // Lantern body (ellipse)
  ctx.beginPath();ctx.ellipse(0,0,r*0.7,r*0.95,0,0,Math.PI*2);ctx.fill();
  // Ribs
  ctx.strokeStyle="#b45309";ctx.lineWidth=1.2;
  for(let i=-2;i<=2;i++){
    const x=i*(r*0.25);ctx.beginPath();
    ctx.ellipse(x,0,r*0.05,r*0.95,0,0,Math.PI*2);ctx.stroke();
  }
  // Top/bottom caps
  ctx.fillStyle="#b45309";
  ctx.fillRect(-r*0.5,-r*0.97,r*1.0,r*0.18);
  ctx.fillRect(-r*0.5,r*0.8,r*1.0,r*0.18);
  // String
  ctx.strokeStyle="#b45309";ctx.lineWidth=1.5;
  ctx.beginPath();ctx.moveTo(0,-r*0.97);ctx.lineTo(0,-r*1.3);ctx.stroke();
  // Glow center flame
  ctx.shadowBlur=24;ctx.shadowColor="#fffde7";
  ctx.fillStyle="#fffde7";ctx.beginPath();ctx.arc(0,0,r*0.2,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Drift — wavy drifting target; more pts the further from center ──
function drawDrift(ctx,r,ts,distPct){
  ctx.save();
  const hue=180+distPct*120; // cyan→magenta as it drifts further
  ctx.shadowBlur=16;ctx.shadowColor=`hsl(${hue},90%,60%)`;
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,`hsl(${hue},100%,85%)`);
  grad.addColorStop(0.5,`hsl(${hue},80%,55%)`);
  grad.addColorStop(1,`hsl(${hue},60%,30%)`);
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Wave ripples
  for(let w2=0;w2<3;w2++){
    const wr=(r*(0.35+w2*0.25)+Math.sin(ts*0.008+w2)*r*0.06);
    ctx.strokeStyle=`hsla(${hue},90%,75%,${0.5-w2*0.12})`;
    ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(0,0,wr,0,Math.PI*2);ctx.stroke();
  }
  // Distance indicator bar at top
  if(distPct>0.3){
    const bw=r*1.1,bh=r*0.18;
    ctx.fillStyle="#00000044";ctx.fillRect(-bw/2,-r-bh-2,bw,bh);
    ctx.fillStyle=`hsl(${hue},90%,65%)`;
    ctx.fillRect(-bw/2,-r-bh-2,bw*distPct,bh);
    ctx.font=`bold ${Math.round(r*0.3)}px sans-serif`;ctx.textAlign="center";
    ctx.fillStyle="#fff";ctx.fillText(`${Math.round(distPct*100)}%`,-0,-r-bh/2-2+bh/2);
  }
  ctx.restore();
}
// ── Nebula — cosmic gas cloud; grows 3 star_piece collectibles when tapped ──
function drawNebula(ctx,r,ts){
  ctx.save();
  const pulse=0.88+0.12*Math.sin(ts*0.005);
  ctx.shadowBlur=20;ctx.shadowColor="#818cf8";
  // Layered cloud blobs
  const layers=[{x:-r*0.15,y:-r*0.1,r:r*0.85,c:"#4c1d95"},{x:r*0.15,y:r*0.1,r:r*0.7,c:"#6d28d9"},{x:0,y:-r*0.2,r:r*0.6,c:"#7c3aed"}];
  layers.forEach(l=>{
    const g=ctx.createRadialGradient(l.x,l.y,0,l.x,l.y,l.r*pulse);
    g.addColorStop(0,`${l.c}ee`);g.addColorStop(1,`${l.c}00`);
    ctx.fillStyle=g;ctx.beginPath();ctx.arc(l.x,l.y,l.r*pulse,0,Math.PI*2);ctx.fill();
  });
  // Star specks
  ctx.fillStyle="#e0e7ff";
  for(let i=0;i<7;i++){const a=(i/7)*Math.PI*2+ts*0.001;const rr=r*(0.25+0.55*(i%3)*0.33);ctx.beginPath();ctx.arc(Math.cos(a)*rr,Math.sin(a)*rr,1.5,0,Math.PI*2);ctx.fill();}
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.7)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌌",0,0);ctx.restore();
}
// ── Wisp — ethereal light; invisible unless moving; huge pts if caught still ──
function drawWisp(ctx,r,ts,isMoving){
  ctx.save();
  const flicker=0.5+0.5*Math.abs(Math.sin(ts*0.012));
  ctx.globalAlpha=isMoving?0.15*flicker:0.9*flicker;
  ctx.shadowBlur=isMoving?6:24;ctx.shadowColor="#e0f2fe";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#ffffff");grad.addColorStop(0.4,"#bae6fd");grad.addColorStop(1,"#0ea5e900");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Trailing wisps
  if(!isMoving){
    for(let i=0;i<5;i++){const a=(i/5)*Math.PI*2+ts*0.004;ctx.globalAlpha=0.3-i*0.05;ctx.fillStyle="#bae6fd";ctx.beginPath();ctx.arc(Math.cos(a)*r*0.5,Math.sin(a)*r*0.5,r*0.15,0,Math.PI*2);ctx.fill();}
  }
  ctx.globalAlpha=1;ctx.restore();
}
// ── Meteor — fast diagonally-falling rock; breaks into shards on canvas edge hit ──
function drawMeteor(ctx,r,ts,trail){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#f97316";
  // Draw trail
  if(trail&&trail.length>1){
    for(let i=1;i<trail.length;i++){
      const a=i/trail.length;ctx.globalAlpha=a*0.4;ctx.strokeStyle="#f97316";ctx.lineWidth=r*0.5*a;
      ctx.beginPath();ctx.moveTo(trail[i-1].x-ctx.currentTransform?.e||0,trail[i-1].y-ctx.currentTransform?.f||0);
    }
  }
  ctx.globalAlpha=1;
  const grad=ctx.createRadialGradient(-r*0.2,-r*0.2,0,0,0,r);
  grad.addColorStop(0,"#fde68a");grad.addColorStop(0.4,"#f97316");grad.addColorStop(1,"#1c0700");
  ctx.fillStyle=grad;
  // Asteroid silhouette
  ctx.beginPath();
  const pts2=[[r,0],[r*0.5,-r*0.9],[-r*0.3,-r*0.85],[-r*0.9,-r*0.3],[-r*0.85,r*0.4],[-r*0.2,r*0.9],[r*0.6,r*0.6]];
  ctx.moveTo(pts2[0][0],pts2[0][1]);pts2.forEach(p=>ctx.lineTo(p[0],p[1]));ctx.closePath();ctx.fill();
  // Glow dot at front
  ctx.shadowBlur=10;ctx.shadowColor="#fde68a";ctx.fillStyle="#fde68a";
  ctx.beginPath();ctx.arc(r*0.45,-r*0.3,r*0.18,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Aura Ring — pulsing ring of energy; tap center for base pts, outer edge for jackpot ──
function drawAuraRing(ctx,r,ts){
  ctx.save();
  const pulse=0.5+0.5*Math.sin(ts*0.007);
  const hue=(ts*0.08)%360;
  ctx.shadowBlur=14;ctx.shadowColor=`hsl(${hue},90%,65%)`;
  // Outer ring
  ctx.strokeStyle=`hsl(${hue},90%,65%)`;ctx.lineWidth=r*0.22;
  ctx.globalAlpha=0.7+0.3*pulse;ctx.beginPath();ctx.arc(0,0,r*0.72,0,Math.PI*2);ctx.stroke();
  // Center core
  ctx.globalAlpha=1;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.36);
  g.addColorStop(0,`hsl(${hue},100%,90%)`);g.addColorStop(1,`hsl(${hue},80%,40%)`);
  ctx.fillStyle=g;ctx.shadowBlur=18;ctx.beginPath();ctx.arc(0,0,r*0.36,0,Math.PI*2);ctx.fill();
  // Orbit dots
  for(let i=0;i<4;i++){
    const a=(i/4)*Math.PI*2+ts*0.006;
    ctx.fillStyle=`hsl(${(hue+i*60)%360},100%,80%)`;
    ctx.globalAlpha=0.8;ctx.beginPath();ctx.arc(Math.cos(a)*r*0.72,Math.sin(a)*r*0.72,r*0.1,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.restore();
}
// ── Supernova — explodes on tap into 5 mini-orbs that scatter and score 100pts each ──
function drawSupernova(ctx,r,ts,agePct){
  ctx.save();
  const pulse=0.85+0.15*Math.abs(Math.sin(ts*0.01));
  const heat=Math.min(1,agePct*1.2);
  const hue=55-heat*55;
  ctx.shadowBlur=20+heat*12;ctx.shadowColor=`hsl(${hue},100%,60%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.25,`hsl(${hue},100%,75%)`);
  g.addColorStop(0.6,`hsl(${hue},95%,45%)`);g.addColorStop(1,"#0a0000");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Corona flares
  const flares=8;
  for(let i=0;i<flares;i++){
    const a=(i/flares)*Math.PI*2+ts*0.008;
    const fl=r*(0.9+0.2*Math.sin(ts*0.02+i));
    ctx.strokeStyle=`hsl(${hue},90%,70%)`;ctx.lineWidth=2;ctx.globalAlpha=0.55;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.7,Math.sin(a)*r*0.7);ctx.lineTo(Math.cos(a)*fl,Math.sin(a)*fl);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#fff";
  ctx.font=`${Math.round(r*0.4)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💫",0,0);ctx.restore();
}
// ── Crystal Matrix — 3-tap 3×3 grid of mini crystals; shatter row for bonus ──
function drawCrystalMatrix(ctx,r,ts,tapsLeft){
  ctx.save();
  ctx.shadowBlur=10;ctx.shadowColor="#a5f3fc";
  const grid=3;const spacing=r*0.55;
  const hitCount=3-tapsLeft;
  for(let row=0;row<grid;row++){
    for(let col=0;col<grid;col++){
      const cx=(col-1)*spacing;const cy=(row-1)*spacing;
      const idx=row*3+col;const isBroken=idx<hitCount*3;
      ctx.save();ctx.translate(cx,cy);
      if(!isBroken){
        ctx.shadowBlur=8+4*Math.sin(ts*0.01+idx);ctx.shadowColor="#38bdf8";
        ctx.fillStyle=`hsl(${195+idx*10},80%,${60+idx*3}%)`;
        ctx.beginPath();ctx.rect(-spacing*0.3,-spacing*0.3,spacing*0.6,spacing*0.6);ctx.fill();
        ctx.strokeStyle="#7dd3fc";ctx.lineWidth=1;ctx.beginPath();ctx.rect(-spacing*0.3,-spacing*0.3,spacing*0.6,spacing*0.6);ctx.stroke();
      } else {
        ctx.globalAlpha=0.2;ctx.fillStyle="#38bdf8";
        ctx.beginPath();ctx.rect(-spacing*0.3,-spacing*0.3,spacing*0.6,spacing*0.6);ctx.fill();
      }
      ctx.restore();
    }
  }
  // HP indicator at center
  ctx.globalAlpha=1;ctx.fillStyle="#fff";
  ctx.font=`bold ${Math.round(r*0.25)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(`${tapsLeft}HP`,0,0);ctx.restore();
}
// ── Plasma Storm — electrifies all nearby normal targets briefly; score × electrified count ──
function drawPlasmaStorm(ctx,r,ts){
  ctx.save();
  const pulse=0.88+0.12*Math.sin(ts*0.016);
  ctx.shadowBlur=20;ctx.shadowColor="#00ffcc";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#cffafe");g.addColorStop(0.3,"#22d3ee");g.addColorStop(0.7,"#0891b2");g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Storm arcs — chaotic zigzag bolts
  for(let i=0;i<6;i++){
    const a1=(i/6)*Math.PI*2+ts*0.009;
    ctx.strokeStyle=`rgba(0,255,204,${0.5+0.3*Math.sin(ts*0.02+i)})`;ctx.lineWidth=1.5;ctx.globalAlpha=0.8;
    ctx.beginPath();ctx.moveTo(Math.cos(a1)*r*0.5,Math.sin(a1)*r*0.5);
    const mx=Math.cos(a1+0.4)*r*(1.0+Math.random()*0.3);
    const my=Math.sin(a1+0.4)*r*(1.0+Math.random()*0.3);
    ctx.lineTo(mx,my);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#cffafe";
  ctx.font=`${Math.round(r*0.38)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,0);ctx.restore();
}
// ── Nexus Gate — dual-portal system; tapping one teleports nearby targets through the other ──
function drawNexusGate(ctx,r,ts,isSecondary){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.011+(isSecondary?Math.PI:0));
  const col=isSecondary?"#f43f5e":"#6366f1";const gCol=isSecondary?"#fda4af":"#a5b4fc";
  ctx.shadowBlur=18;ctx.shadowColor=col;
  // Portal ring
  for(let i=0;i<3;i++){
    ctx.strokeStyle=i===0?col:gCol;ctx.lineWidth=3-i;ctx.globalAlpha=0.7-i*0.15;
    ctx.beginPath();ctx.arc(0,0,r*(0.85+i*0.18)*pulse,0,Math.PI*2);ctx.stroke();
  }
  // Inner vortex
  ctx.globalAlpha=0.8;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.7*pulse);
  g.addColorStop(0,isSecondary?"#fff1f2":"#eef2ff");g.addColorStop(0.4,col);g.addColorStop(1,"#000");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.7*pulse,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;ctx.fillStyle="#fff";
  ctx.font=`${Math.round(r*0.5)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isSecondary?"🔴":"🔵",0,0);ctx.restore();
}
// ── Venom Orb — poisonous; degrades score if not tapped quickly; antidote = tap within 1s ──
function drawVenomOrb(ctx,r,ts,agePct){
  ctx.save();
  const toxicity=Math.min(1,agePct*1.5);
  const pulse=0.88+0.12*Math.sin(ts*0.014);
  const hue=120-toxicity*80;// green → yellow → red
  ctx.shadowBlur=16;ctx.shadowColor=`hsl(${hue},90%,50%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,`hsl(${hue},80%,75%)`);g.addColorStop(0.4,`hsl(${hue},85%,45%)`);g.addColorStop(1,"#0a0a00");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Bubble drips
  for(let i=0;i<5;i++){
    const a=(i/5)*Math.PI*2+ts*0.006;const dist=r*(0.55+toxicity*0.2);
    ctx.fillStyle=`hsla(${hue},90%,60%,${0.5+toxicity*0.3})`;ctx.globalAlpha=0.75;
    ctx.beginPath();ctx.arc(Math.cos(a)*dist,Math.sin(a)*dist,r*(0.09+toxicity*0.04),0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#f0fdf4";
  ctx.font=`${Math.round(r*0.38)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("☠️",0,0);ctx.restore();
}
// ── Thunder Egg — cracks open in 3 phases; final crack = lightning burst + bonus pts ──
function drawThunderEgg(ctx,r,ts,phase){
  ctx.save();
  ctx.shadowBlur=12+phase*4;ctx.shadowColor="#fef08a";
  // Egg body
  const g=ctx.createRadialGradient(-r*0.2,-r*0.3,0,0,0,r*0.9);
  g.addColorStop(0,"#fefce8");g.addColorStop(0.4,"#fbbf24");g.addColorStop(0.85,"#78350f");g.addColorStop(1,"#1c0a00");
  ctx.fillStyle=g;ctx.beginPath();ctx.ellipse(0,0,r*0.72,r,0,0,Math.PI*2);ctx.fill();
  // Cracks based on phase
  ctx.strokeStyle="#fef08a";ctx.lineWidth=1.5;ctx.globalAlpha=0.8;
  if(phase>=1){ctx.beginPath();ctx.moveTo(0,-r*0.6);ctx.lineTo(r*0.2,-r*0.1);ctx.lineTo(-r*0.1,r*0.3);ctx.stroke();}
  if(phase>=2){ctx.beginPath();ctx.moveTo(-r*0.3,-r*0.2);ctx.lineTo(-r*0.1,r*0.1);ctx.lineTo(r*0.15,r*0.5);ctx.stroke();}
  if(phase>=3){
    ctx.strokeStyle="#fff";ctx.lineWidth=3;
    ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(r*0.35,-r*0.2);ctx.lineTo(-r*0.2,r*0.4);ctx.lineTo(r*0.1,r*0.95);ctx.stroke();
    ctx.shadowBlur=22;ctx.shadowColor="#ffd700";
  }
  ctx.globalAlpha=1;ctx.restore();
}
// ── Moon Glyph — ancient rune that cycles symbols; tap matching symbol = jackpot ──
function drawMoonGlyph(ctx,r,ts,symbolIdx){
  ctx.save();
  const symbols=["☽","✦","⊕","◈","⟡"];
  const pulse=0.9+0.1*Math.sin(ts*0.008);
  ctx.shadowBlur=16;ctx.shadowColor="#818cf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#1e1b4b");g.addColorStop(0.5,"#312e81");g.addColorStop(1,"#0f0e1a");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Concentric star rings
  for(let i=0;i<3;i++){
    ctx.strokeStyle=`rgba(129,140,248,${0.4-i*0.1})`;ctx.lineWidth=1.2;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(0,0,r*(0.65+i*0.2),0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#c7d2fe";
  ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(symbols[symbolIdx%symbols.length],0,r*0.04);ctx.restore();
}
// ── Comet Tail — fast moving; score bonus per 0.1s of life remaining when tapped ──
function drawCometTail(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor="#fbbf24";
  // Head
  const g=ctx.createRadialGradient(-r*0.15,-r*0.15,0,0,0,r*0.6);
  g.addColorStop(0,"#fef9c3");g.addColorStop(0.5,"#fbbf24");g.addColorStop(1,"#d97706");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.6,0,Math.PI*2);ctx.fill();
  // Tail flame streaks
  for(let i=0;i<5;i++){
    const stretch=1.2+i*0.5;const fade=0.7-i*0.12;
    ctx.globalAlpha=fade;ctx.fillStyle=i<3?"#fbbf24":"#ef4444";
    ctx.beginPath();ctx.ellipse(-r*stretch*0.55,0,r*(0.35-i*0.04),r*(0.2-i*0.025),0,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.restore();
}
// ── Ember Ring — pulsing fire ring; must be tapped at max expansion for jackpot ──
function drawEmberRing(ctx,r,ts,agePct){
  ctx.save();
  const expand=0.3+agePct*0.7;
  const ringR=r*expand;
  const isMax=agePct>=0.85;
  ctx.shadowBlur=isMax?24:12;ctx.shadowColor=isMax?"#ffd700":"#f97316";
  // Outer ring
  ctx.strokeStyle=isMax?"#ffd700":`hsl(${20+agePct*20},90%,60%)`;ctx.lineWidth=isMax?4:2.5;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
  // Inner ember dots
  const embers=8;
  for(let i=0;i<embers;i++){
    const a=(i/embers)*Math.PI*2+ts*0.009;
    const er=ringR*(0.88+0.12*Math.sin(ts*0.018+i));
    ctx.fillStyle=`hsl(${25+agePct*15},100%,${55+agePct*20}%)`;ctx.globalAlpha=0.75;
    ctx.beginPath();ctx.arc(Math.cos(a)*er,Math.sin(a)*er,r*0.1,0,Math.PI*2);ctx.fill();
  }
  // Center glow
  ctx.globalAlpha=1;ctx.shadowBlur=isMax?18:8;ctx.fillStyle=isMax?"#ffd700":"#fbbf24";
  ctx.beginPath();ctx.arc(0,0,r*0.22,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Twilight Orb — fades to near-invisible; catchphantom tap for jackpot ──
function drawTwilightOrb(ctx,r,ts,agePct){
  const visibility=Math.max(0.04,1-agePct*0.95);
  ctx.save();
  ctx.shadowBlur=14*visibility;ctx.shadowColor="#818cf8";
  const pulse=0.9+0.1*Math.sin(ts*0.011);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#e0e7ff");g.addColorStop(0.5,"#6366f1");g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=visibility;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  ctx.strokeStyle="#818cf8";ctx.lineWidth=2;ctx.globalAlpha=visibility*0.4;
  ctx.setLineDash([4,6]);ctx.beginPath();ctx.arc(0,0,r*1.1,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);
  ctx.restore();
}
// ── Life Orb — gives +1 life when tapped (rare bonus target) ──
function drawLifeOrb(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=22;ctx.shadowColor="#4ade80";
  const pulse=0.88+0.12*Math.sin(ts*0.013);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#dcfce7");g.addColorStop(0.4,"#22c55e");g.addColorStop(1,"#14532d");
  ctx.fillStyle=g;ctx.globalAlpha=0.95;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Heartbeat rings
  for(let i=0;i<2;i++){
    const pr=0.65+i*0.25+0.08*Math.sin(ts*0.018-i*0.8);
    ctx.strokeStyle="#4ade80";ctx.lineWidth=2-i*0.5;ctx.globalAlpha=0.4-i*0.15;
    ctx.beginPath();ctx.arc(0,0,r*pr,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.65)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("❤️",0,1);
  ctx.restore();
}
// ── Score Amp Orb — while alive, nearby taps score 2× for its duration ──
function drawScoreAmpOrb(ctx,r,ts,secsLeft){
  ctx.save();
  ctx.shadowBlur=20;ctx.shadowColor="#f43f5e";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#ffe4e6");g.addColorStop(0.4,"#f43f5e");g.addColorStop(1,"#4c0519");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Amplifier rings (expanding)
  for(let i=0;i<3;i++){
    const phase=(ts*0.005+i/3)%1;
    const pr=0.6+phase*0.8;
    ctx.strokeStyle="#f43f5e";ctx.lineWidth=2;ctx.globalAlpha=(1-phase)*0.55;
    ctx.beginPath();ctx.arc(0,0,r*pr,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.38)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.shadowBlur=4;ctx.shadowColor="#f43f5e";
  ctx.fillText(`×2!`,0,-r*0.12);ctx.font=`${Math.floor(r*0.26)}px sans-serif`;ctx.fillText(`${secsLeft}s`,0,r*0.3);
  ctx.restore();
}
// ── Night Orb — starts dark, brightens over time; peak brightness = jackpot ──
function drawNightOrb(ctx,r,ts,agePct){
  const brightness=agePct;
  const isPeak=agePct>=0.95;
  ctx.save();
  ctx.shadowBlur=isPeak?28:8+brightness*14;ctx.shadowColor=isPeak?"#ffd700":"#a78bfa";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  const h1=isPeak?"#fef9c3":"#" + Math.round(brightness*0x1c + 0x03).toString(16).padStart(2,"0").repeat(3);
  g.addColorStop(0,isPeak?"#fff":h1);
  g.addColorStop(0.5,isPeak?"#ffd700":`hsl(${255-brightness*55},${60+brightness*30}%,${10+brightness*50}%)`);
  g.addColorStop(1,isPeak?"#92400e":"#030011");
  ctx.fillStyle=g;ctx.globalAlpha=0.15+brightness*0.8;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  if(isPeak){for(let i=0;i<8;i++){const a=(i/8)*Math.PI*2+ts*0.01;
    ctx.strokeStyle="#ffd700";ctx.lineWidth=1.5;ctx.globalAlpha=0.6;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.9,Math.sin(a)*r*0.9);ctx.lineTo(Math.cos(a)*r*1.3,Math.sin(a)*r*1.3);ctx.stroke();
  }}
  ctx.globalAlpha=0.1+brightness*0.9;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(isPeak?"☀️":"🌅",0,1);
  ctx.restore();
}
// ── Shifting Target — cycles 5 phases every 600ms ──
function drawShiftTarget(ctx,r,ts,phase){
  const colors=["#a3e635","#38bdf8","#c084fc","#f472b6","#ffd700"];
  const labels=["N","R","E","L","B"];
  const glows=["#65a30d","#0284c7","#7e22ce","#db2777","#b45309"];
  ctx.save();
  ctx.shadowBlur=14+phase*4;ctx.shadowColor=glows[phase];
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,colors[phase]);g.addColorStop(1,"#111");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();
  const sides=3+phase; // 3→7 sides as phase increases
  for(let i=0;i<sides;i++){const a=(i/sides)*Math.PI*2-Math.PI/2+ts*0.003*(phase%2===0?1:-1);ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);}
  ctx.closePath();ctx.fill();
  // Phase label
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.5)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(labels[phase],0,1);
  // Rotating outer ring
  ctx.strokeStyle=colors[phase];ctx.lineWidth=2;ctx.globalAlpha=0.6;
  ctx.beginPath();ctx.arc(0,0,r*1.15,0,Math.PI*2);ctx.stroke();
  ctx.restore();
}
// ── Lucky Wheel — spinning number display; pts = 100 × number showing ──
function drawLuckyWheel(ctx,r,ts,num){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#f59e0b";
  // Wheel body
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fef9c3");g.addColorStop(0.5,"#f59e0b");g.addColorStop(1,"#78350f");
  ctx.fillStyle=g;ctx.globalAlpha=0.93;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Spinning segments
  for(let i=0;i<8;i++){
    const a=(i/8)*Math.PI*2+ts*0.012;
    ctx.strokeStyle=i%2===0?"#fcd34d":"#92400e";ctx.lineWidth=1.5;ctx.globalAlpha=0.4;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);ctx.stroke();
  }
  // Number display
  ctx.globalAlpha=1;ctx.fillStyle="#1c1917";ctx.font=`bold ${Math.floor(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(num,0,1);
  // Outer ring
  ctx.strokeStyle="#fcd34d";ctx.lineWidth=3;ctx.globalAlpha=0.85;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();
  ctx.restore();
}
// ── Abyss Orb — penalty; drains 200pts if not tapped; tap for 300pts ──
function drawAbyssOrb(ctx,r,ts,agePct){
  ctx.save();
  const drain=agePct; // 0→1; gets more dangerous
  ctx.shadowBlur=8+drain*20;ctx.shadowColor=drain>0.7?"#ef4444":"#7c3aed";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  const inner=drain>0.7?"#1f2937":"#0f0720";
  g.addColorStop(0,drain>0.7?"#374151":inner);
  g.addColorStop(0.5,drain>0.7?"#7f1d1d":"#4c1d95");
  g.addColorStop(1,"#000");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Swirling vortex lines
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.008;const ir=r*0.3;const or=r*0.85;
    ctx.strokeStyle=drain>0.7?"#ef4444":"#8b5cf6";ctx.lineWidth=1.5;ctx.globalAlpha=0.5-drain*0.2;
    ctx.beginPath();
    ctx.arc(0,0,ir+(or-ir)*((i%3)/2),a,a+Math.PI*0.7);ctx.stroke();
  }
  // Warning exclamation if draining fast
  ctx.globalAlpha=0.9;ctx.font=`bold ${Math.floor(r*0.6)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle=drain>0.7?"#ef4444":"#a78bfa";
  ctx.fillText(drain>0.7?"⚠️":"🌑",0,1);
  ctx.restore();
}
// ── Cosmic Portal — next tap after this gives 5× pts ──
function drawCosmicPortal(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=22;ctx.shadowColor="#a78bfa";
  // Portal swirl body
  const g=ctx.createRadialGradient(0,0,r*0.2,0,0,r);
  g.addColorStop(0,"#000");g.addColorStop(0.4,"#4c1d95");g.addColorStop(0.8,"#7c3aed");g.addColorStop(1,"#c4b5fd");
  ctx.fillStyle=g;ctx.globalAlpha=0.93;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Rotating arms
  for(let i=0;i<4;i++){
    const a=(i/4)*Math.PI*2+ts*0.010;
    ctx.save();ctx.rotate(a);
    ctx.strokeStyle="#e9d5ff";ctx.lineWidth=2.5;ctx.globalAlpha=0.5;
    ctx.beginPath();ctx.moveTo(r*0.2,0);ctx.bezierCurveTo(r*0.5,-r*0.3,r*0.8,-r*0.1,r,0);ctx.stroke();
    ctx.restore();
  }
  // 5× label
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.42)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.shadowBlur=8;ctx.shadowColor="#ffd700";ctx.fillStyle="#ffd700";
  ctx.fillText("5×",0,1);
  ctx.restore();
}
// ── Flame Ring — shrinking ring of fire; sweet spot at 0.4-0.7 size ──
function drawFlameRing(ctx,r,ts,ringPct){
  ctx.save();
  const rr=r*(0.3+ringPct*0.7); // ring radius shrinks over time
  const inSweet=ringPct>0.3&&ringPct<0.6;
  ctx.shadowBlur=inSweet?28:12;ctx.shadowColor=inSweet?"#ffd700":"#f97316";
  // Ring body
  ctx.strokeStyle=inSweet?"#ffd700":"#f97316";ctx.lineWidth=inSweet?8:5;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,rr,0,Math.PI*2);ctx.stroke();
  // Flame particles on ring
  for(let i=0;i<8;i++){
    const a=(i/8)*Math.PI*2+ts*0.015;
    const fx=Math.cos(a)*rr,fy=Math.sin(a)*rr;
    ctx.fillStyle=i%2===0?"#f97316":"#fbbf24";ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(fx,fy,inSweet?4:2.5,0,Math.PI*2);ctx.fill();
  }
  // Center target dot
  ctx.fillStyle=inSweet?"#ffd700":"#fb923c";ctx.globalAlpha=0.8;ctx.shadowBlur=inSweet?16:6;
  ctx.beginPath();ctx.arc(0,0,r*0.18,0,Math.PI*2);ctx.fill();
  if(inSweet){ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.35)}px sans-serif`;
    ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.9;ctx.fillText("!",0,1);}
  ctx.restore();
}
// ── Ice Clock — countdown 5→0; pts = 200 × secs remaining ──
function drawIceClock(ctx,r,ts,secsLeft){
  ctx.save();
  const danger=secsLeft<=2;
  ctx.shadowBlur=danger?22:10;ctx.shadowColor=danger?"#ef4444":"#7dd3fc";
  // Clock body
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#f0f9ff");g.addColorStop(0.5,danger?"#fca5a5":"#bae6fd");g.addColorStop(1,danger?"#450a0a":"#0c4a6e");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Clock face tick marks
  for(let i=0;i<12;i++){
    const a=(i/12)*Math.PI*2;ctx.strokeStyle=danger?"#ef4444":"#0ea5e9";ctx.lineWidth=i%3===0?2:1;ctx.globalAlpha=0.5;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.8,Math.sin(a)*r*0.8);ctx.lineTo(Math.cos(a)*r*0.95,Math.sin(a)*r*0.95);ctx.stroke();
  }
  // Clock hand
  const angle=(secsLeft/5)*Math.PI*2-Math.PI/2;
  ctx.strokeStyle=danger?"#ef4444":"#0284c7";ctx.lineWidth=3;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(angle)*r*0.65,Math.sin(angle)*r*0.65);ctx.stroke();
  // Seconds display
  ctx.globalAlpha=1;ctx.fillStyle=danger?"#ef4444":"#0c4a6e";ctx.font=`bold ${Math.floor(r*0.5)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(secsLeft,0,1);
  ctx.restore();
}
// ── Holo Decoy — real (60%) gives 400pts; decoy (40%) costs -100pts ──
function drawHoloDecoy(ctx,r,ts,isReal){
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor=isReal?"#22d3ee":"#f472b6";
  // Flickering hologram effect
  const flicker=0.7+0.3*Math.sin(ts*0.08+Math.random()*0.1);
  ctx.globalAlpha=flicker*0.88;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,isReal?"#e0f2fe":"#fdf4ff");
  g.addColorStop(0.5,isReal?"#22d3ee":"#e879f9");
  g.addColorStop(1,isReal?"#0c4a6e":"#4a044e");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Scan lines
  for(let y=-r;y<r;y+=5){
    ctx.strokeStyle=isReal?"#0ea5e9":"#d946ef";ctx.lineWidth=0.5;ctx.globalAlpha=0.15;
    ctx.beginPath();ctx.moveTo(-Math.sqrt(r*r-y*y)||0,y);ctx.lineTo(Math.sqrt(r*r-y*y)||0,y);ctx.stroke();
  }
  // ? symbol — player doesn't know if real
  ctx.globalAlpha=0.9;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.6)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("?",0,1);
  ctx.restore();
}
// ── Mirror Gate — activates reflection mode; next tap clones that target ──
function drawMirrorGate(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#818cf8";
  const spin=ts*0.008;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#f0f9ff");g.addColorStop(0.4,"#818cf8");g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Mirror reflection lines
  ctx.strokeStyle="#c7d2fe";ctx.lineWidth=2;ctx.globalAlpha=0.7;
  ctx.beginPath();ctx.moveTo(-r*0.85,0);ctx.lineTo(r*0.85,0);ctx.stroke();
  // Rotating reflection shards
  for(let i=0;i<6;i++){
    const a=spin+(i/6)*Math.PI*2;
    ctx.save();ctx.rotate(a);
    ctx.fillStyle="#e0e7ff";ctx.globalAlpha=0.4;
    ctx.beginPath();ctx.moveTo(r*0.35,0);ctx.lineTo(r*0.7,-r*0.1);ctx.lineTo(r*0.7,r*0.1);ctx.closePath();ctx.fill();
    ctx.restore();
  }
  // Mirror icon
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🪞",0,1);
  ctx.restore();
}
// ── Spiral Bomb — fills spiral; jackpot at full (1.0) ──
function drawSpiralBomb(ctx,r,ts,fillPct){
  ctx.save();
  const isDanger=fillPct>0.8;
  ctx.shadowBlur=isDanger?22:10;ctx.shadowColor=isDanger?"#ffd700":"#f97316";
  // Central core
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.4);
  g.addColorStop(0,"#fff");g.addColorStop(1,isDanger?"#ffd700":"#f97316");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*0.35,0,Math.PI*2);ctx.fill();
  // Spiral trail
  const totalAngle=fillPct*Math.PI*8; // 4 full rotations at 100%
  ctx.strokeStyle=isDanger?"#ffd700":"#f97316";ctx.lineWidth=2.5;ctx.globalAlpha=0.8;
  ctx.beginPath();
  for(let i=0;i<200;i++){
    const pct=(i/200)*fillPct;
    const a=pct*Math.PI*8;const rad=r*0.35+pct*r*0.5;
    const x=Math.cos(a)*rad,y=Math.sin(a)*rad;
    i===0?ctx.moveTo(x,y):ctx.lineTo(x,y);
  }
  ctx.stroke();
  // Jackpot indicator
  if(isDanger){
    ctx.globalAlpha=0.8;ctx.font=`bold ${Math.floor(r*0.38)}px sans-serif`;
    ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#ffd700";ctx.fillText("NOW!",0,1);}
  ctx.restore();
}
// ── Chain Orb — 4-tap chain: 100/200/400/800pts per tap ──
function drawChainOrb(ctx,r,ts,tapsLeft){
  ctx.save();
  const progress=(4-tapsLeft)/4;
  ctx.shadowBlur=8+progress*18;ctx.shadowColor=`hsl(${200+progress*160},90%,55%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");
  g.addColorStop(0.4,`hsl(${200+progress*160},90%,55%)`);
  g.addColorStop(1,"#111");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Chain links indicator
  for(let i=0;i<4;i++){
    const filled=i<(4-tapsLeft);
    ctx.fillStyle=filled?"#ffd700":"#374151";ctx.globalAlpha=0.85;
    const lx=-r*0.5+i*r*0.32,ly=r*0.55;
    ctx.beginPath();ctx.arc(lx,ly,r*0.12,0,Math.PI*2);ctx.fill();
  }
  // Tap count
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.5)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`×${4-tapsLeft+1}`,0,-r*0.08);
  ctx.restore();
}
// ── Bolt Catcher — spawns at edge; must tap within 1.5s ──
function drawBoltCatcher(ctx,r,ts,urgencyPct){
  ctx.save();
  const danger=urgencyPct>0.7;
  ctx.shadowBlur=danger?28:12;ctx.shadowColor=danger?"#ef4444":"#fbbf24";
  // Electric body
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fef9c3");g.addColorStop(0.5,danger?"#ef4444":"#f59e0b");g.addColorStop(1,danger?"#450a0a":"#78350f");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Lightning bolt symbol
  ctx.fillStyle=danger?"#fff":"#fef9c3";ctx.font=`${Math.floor(r*0.65)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("⚡",0,1);
  // Urgency ring pulsing
  ctx.strokeStyle=danger?"#ef4444":"#fbbf24";ctx.lineWidth=3;ctx.globalAlpha=0.5+0.5*Math.abs(Math.sin(ts*0.02));
  ctx.beginPath();ctx.arc(0,0,r*1.15,0,Math.PI*2);ctx.stroke();
  ctx.restore();
}
// ── Gem Harvest — collects nearby targets; bonus per gem ──
function drawGemHarvest(ctx,r,ts,gemCount){
  ctx.save();
  const hasGems=gemCount>0;
  ctx.shadowBlur=hasGems?20:10;ctx.shadowColor=hasGems?"#34d399":"#059669";
  // Body
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#f0fdf4");g.addColorStop(0.5,hasGems?"#34d399":"#059669");g.addColorStop(1,"#052e16");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Orbiting gem dots
  for(let i=0;i<Math.min(gemCount,8);i++){
    const a=(i/Math.max(1,gemCount))*Math.PI*2+ts*0.012;
    const gx=Math.cos(a)*r*1.1,gy=Math.sin(a)*r*1.1;
    ctx.fillStyle="#34d399";ctx.globalAlpha=0.9;ctx.shadowBlur=6;ctx.shadowColor="#34d399";
    ctx.beginPath();ctx.arc(gx,gy,4,0,Math.PI*2);ctx.fill();
  }
  // Gem count
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.45)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`💎${gemCount}`,0,1);
  ctx.restore();
}
// ── Prism Pulse — cycles rainbow; match target color for jackpot ──
function drawPrismPulse(ctx,r,ts,colorIdx,targetIdx){
  const colors=["#ef4444","#f97316","#facc15","#22c55e","#3b82f6","#a855f7"];
  const color=colors[colorIdx];
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor=color;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.45,color);g.addColorStop(1,"#111");
  ctx.fillStyle=g;ctx.globalAlpha=0.93;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Rotating rainbow outer ring
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.010;
    ctx.fillStyle=colors[i];ctx.globalAlpha=colorIdx===i?0.9:0.3;
    ctx.beginPath();ctx.arc(Math.cos(a)*r*1.2,Math.sin(a)*r*1.2,4,0,Math.PI*2);ctx.fill();
  }
  // Target color indicator dot at top
  ctx.fillStyle=colors[targetIdx];ctx.globalAlpha=0.8;ctx.shadowBlur=10;ctx.shadowColor=colors[targetIdx];
  ctx.beginPath();ctx.arc(0,-r*0.55,6,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Dark Matter — invisible; reveals on touch; 800pts blind, 300pts visible ──
function drawDarkMatter(ctx,r,ts,revealed){
  ctx.save();
  if(revealed){
    ctx.shadowBlur=20;ctx.shadowColor="#6d28d9";
    const g=ctx.createRadialGradient(0,0,0,0,0,r);
    g.addColorStop(0,"#f5f3ff");g.addColorStop(0.5,"#7c3aed");g.addColorStop(1,"#000");
    ctx.fillStyle=g;ctx.globalAlpha=0.8;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
    ctx.globalAlpha=0.7;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;
    ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("⚫",0,1);
  } else {
    // Almost invisible — just a very faint shimmer
    ctx.globalAlpha=0.08+0.04*Math.sin(ts*0.012);
    ctx.fillStyle="#6d28d9";ctx.shadowBlur=5;ctx.shadowColor="#6d28d9";
    ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  }
  ctx.restore();
}
// ── Comet Blast — fast mover; tapping spawns 4 blast shards ──
function drawCometBlast(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=20;ctx.shadowColor="#f97316";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#fbbf24");g.addColorStop(1,"#9a3412");
  ctx.fillStyle=g;ctx.globalAlpha=0.95;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Trailing flame effect
  for(let i=0;i<5;i++){
    const a=Math.PI+(i-2)*0.2;const d=r*(1+i*0.25);
    ctx.fillStyle=i<2?"#f97316":"#7f1d1d";ctx.globalAlpha=0.4-i*0.07;
    ctx.beginPath();ctx.arc(Math.cos(a)*d,Math.sin(a)*d,r*(0.5-i*0.08),0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.6)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("☄️",0,1);
  ctx.restore();
}
// ── Time Prison — freezes 3 nearby targets; release them all on tap ──
function drawTimePrison(ctx,r,ts,cagedCount){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#0ea5e9";
  // Prison bars background
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#e0f2fe");g.addColorStop(0.6,"#0ea5e9");g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Prison bars
  ctx.strokeStyle="#fff";ctx.lineWidth=2;ctx.globalAlpha=0.5;
  for(let i=-2;i<=2;i++){
    ctx.beginPath();ctx.moveTo(i*r*0.3,-r);ctx.lineTo(i*r*0.3,r);ctx.stroke();
  }
  // Caged count
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.45)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`🔒×${cagedCount}`,0,1);
  ctx.restore();
}
// ── Shadow Twin — real + shadow; tap real for 400pts, shadow costs -50pts ──
function drawShadowTwin(ctx,r,ts,isShadow){
  ctx.save();
  ctx.shadowBlur=isShadow?8:16;ctx.shadowColor=isShadow?"#374151":"#f472b6";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  if(isShadow){
    g.addColorStop(0,"#4b5563");g.addColorStop(0.6,"#1f2937");g.addColorStop(1,"#000");
    ctx.fillStyle=g;ctx.globalAlpha=0.6;
  } else {
    g.addColorStop(0,"#fdf2f8");g.addColorStop(0.5,"#f472b6");g.addColorStop(1,"#831843");
    ctx.fillStyle=g;ctx.globalAlpha=0.93;
  }
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=isShadow?0.5:1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(isShadow?"👤":"👥",0,1);
  // Subtle shimmer on real to distinguish (player must observe carefully)
  if(!isShadow){
    ctx.strokeStyle="#f9a8d4";ctx.lineWidth=2;ctx.globalAlpha=0.4+0.3*Math.abs(Math.sin(ts*0.012));
    ctx.beginPath();ctx.arc(0,0,r*1.1,0,Math.PI*2);ctx.stroke();
  }
  ctx.restore();
}
// ── Photon Burst — flashes every 150ms; 600pts visible, 0pts invisible ──
function drawPhotonBurst(ctx,r,ts,isOn){
  ctx.save();
  if(!isOn){ctx.restore();return;}// completely invisible when off
  ctx.shadowBlur=24;ctx.shadowColor="#facc15";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,"#fef9c3");g.addColorStop(0.7,"#facc15");g.addColorStop(1,"#78350f");
  ctx.fillStyle=g;ctx.globalAlpha=0.97;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Radial flash rays
  for(let i=0;i<12;i++){
    const a=(i/12)*Math.PI*2;
    ctx.strokeStyle="#fef9c3";ctx.lineWidth=1.5;ctx.globalAlpha=0.6;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r,Math.sin(a)*r);ctx.lineTo(Math.cos(a)*r*1.4,Math.sin(a)*r*1.4);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.6)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("💡",0,1);
  ctx.restore();
}
// ── Vault Orb — banks 100pts/s; tap to claim; more time = more pts ──
function drawVaultOrb(ctx,r,ts,bankedPts){
  ctx.save();
  const isRich=bankedPts>=400;
  ctx.shadowBlur=isRich?24:10;ctx.shadowColor=isRich?"#ffd700":"#4ade80";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#f0fdf4");g.addColorStop(0.5,isRich?"#ffd700":"#4ade80");g.addColorStop(1,isRich?"#78350f":"#052e16");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Vault door circles
  ctx.strokeStyle=isRich?"#fcd34d":"#86efac";ctx.lineWidth=2;ctx.globalAlpha=0.6;
  ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.stroke();
  ctx.strokeStyle=isRich?"#fbbf24":"#4ade80";ctx.lineWidth=1.5;ctx.globalAlpha=0.4;
  ctx.beginPath();ctx.arc(0,0,r*0.5,0,Math.PI*2);ctx.stroke();
  // Banked pts display
  ctx.globalAlpha=1;ctx.fillStyle=isRich?"#ffd700":"#fff";ctx.font=`bold ${Math.floor(r*0.38)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`+${bankedPts}`,0,1);
  ctx.restore();
}
// ── Nano Swarm — splits into 6 tiny targets on tap ──
function drawNanoSwarm(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor="#6ee7b7";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#f0fdf4");g.addColorStop(0.5,"#34d399");g.addColorStop(1,"#064e3b");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Tiny orbiting dots representing the nano units
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.014;
    const nx=Math.cos(a)*r*0.65,ny=Math.sin(a)*r*0.65;
    ctx.fillStyle="#6ee7b7";ctx.globalAlpha=0.8;ctx.beginPath();ctx.arc(nx,ny,3,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🦠",0,1);
  ctx.restore();
}
// ── Rune Chain — 3 sequential runes appear; tap in order for max pts ──
function drawRuneChain(ctx,r,ts,runeIdx,nextRune){
  const runeSyms=["ᚠ","ᚢ","ᚦ"];const runeColors=["#a78bfa","#f472b6","#fbbf24"];
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor=runeColors[runeIdx]||"#a78bfa";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#f5f3ff");g.addColorStop(0.5,runeColors[runeIdx]||"#a78bfa");g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Rune progress indicators
  for(let i=0;i<3;i++){
    ctx.fillStyle=i<=runeIdx?runeColors[i]:"#374151";ctx.globalAlpha=i<=runeIdx?0.9:0.3;
    const px=-r*0.45+i*r*0.45,py=r*0.6;
    ctx.beginPath();ctx.arc(px,py,r*0.1,0,Math.PI*2);ctx.fill();
  }
  // Current rune symbol
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.6)}px serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(runeSyms[runeIdx]||"ᚠ",0,-r*0.05);
  ctx.restore();
}
// ── Plasma Web — scores 80pts per web connection ──
function drawPlasmaWeb(ctx,r,ts,connections){
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor="#7c3aed";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#faf5ff");g.addColorStop(0.5,"#7c3aed");g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Web indicator count
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.42)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`🕸️×${connections}`,0,1);
  ctx.restore();
}
// ── Astral Orb — teleports every 1s; tap within 0.3s for jackpot ──
function drawAstralOrb(ctx,r,ts,freshTeleport){
  ctx.save();
  ctx.shadowBlur=freshTeleport?28:14;ctx.shadowColor=freshTeleport?"#ffd700":"#818cf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,freshTeleport?"#ffd700":"#818cf8");g.addColorStop(1,freshTeleport?"#78350f":"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=freshTeleport?1:0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Teleport shimmer rings
  if(freshTeleport){
    for(let i=1;i<=3;i++){
      ctx.strokeStyle="#ffd700";ctx.lineWidth=1.5;ctx.globalAlpha=0.5-i*0.15;
      ctx.beginPath();ctx.arc(0,0,r*(1+i*0.15),0,Math.PI*2);ctx.stroke();
    }
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("✨",0,1);
  ctx.restore();
}
// ── Crystal Clock — fills 8 segments over lifetime; pts = 100 × segments ──
function drawCrystalClock(ctx,r,ts,segments){
  ctx.save();
  ctx.shadowBlur=10+segments*1.5;ctx.shadowColor="#67e8f9";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#f0f9ff");g.addColorStop(0.5,"#0ea5e9");g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // 8 crystal segments
  for(let i=0;i<8;i++){
    const a=(i/8)*Math.PI*2-Math.PI/2;
    const filled=i<segments;
    ctx.strokeStyle=filled?"#67e8f9":"#164e63";ctx.lineWidth=filled?3:1.5;ctx.globalAlpha=filled?0.9:0.3;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.5,Math.sin(a)*r*0.5);ctx.lineTo(Math.cos(a)*r*0.9,Math.sin(a)*r*0.9);ctx.stroke();
  }
  // Segment count
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.4)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`${segments}/8`,0,1);
  ctx.restore();
}
// ── Sunflower Orb — rotating petals; lose 1 per 300ms; pts = 50 × petals ──
function drawSunflowerOrb(ctx,r,ts,petals){
  ctx.save();
  ctx.shadowBlur=12+petals;ctx.shadowColor="#fbbf24";
  // Center circle
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.35);
  g.addColorStop(0,"#fef9c3");g.addColorStop(1,"#f59e0b");
  ctx.fillStyle=g;ctx.globalAlpha=0.95;ctx.beginPath();ctx.arc(0,0,r*0.35,0,Math.PI*2);ctx.fill();
  // Petals
  for(let i=0;i<petals;i++){
    const a=(i/12)*Math.PI*2+ts*0.008;
    ctx.save();ctx.rotate(a);
    ctx.fillStyle=i<petals?"#facc15":"#374151";ctx.globalAlpha=0.85;
    ctx.beginPath();ctx.ellipse(0,-r*0.65,r*0.18,r*0.32,0,0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
  // Petal count
  ctx.globalAlpha=1;ctx.fillStyle="#78350f";ctx.font=`bold ${Math.floor(r*0.35)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`×${petals}`,0,1);
  ctx.restore();
}
// ── Toxic Cloud — penalty zone; nearby taps penalized; tap to clear ──
function drawToxicCloud(ctx,r,ts,agePct){
  ctx.save();
  const pulse=0.85+0.15*Math.sin(ts*0.014);
  ctx.shadowBlur=12+agePct*10;ctx.shadowColor="#4ade80";
  // Cloud body — amorphous shape
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.003;const cr=r*(0.5+0.2*Math.sin(i*1.3));
    const cx2=Math.cos(a)*r*0.35,cy2=Math.sin(a)*r*0.35;
    ctx.fillStyle=`rgba(74,222,128,${0.25-agePct*0.1})`;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(cx2,cy2,cr*pulse,0,Math.PI*2);ctx.fill();
  }
  // Center toxic symbol
  ctx.globalAlpha=0.85;ctx.font=`${Math.floor(r*0.6)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("☣️",0,1);
  ctx.restore();
}
// ── Laser Prism — rotating laser charges nearby targets ──
function drawLaserPrism(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#f43f5e";
  // Prism body (triangle)
  ctx.fillStyle="#fce7f3";ctx.globalAlpha=0.9;
  ctx.beginPath();
  for(let i=0;i<3;i++){const a=(i/3)*Math.PI*2-Math.PI/6+ts*0.006;ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);}
  ctx.closePath();ctx.fill();
  // Rainbow refraction
  const laserAngle=ts*0.012;
  const rainbowColors=["#ef4444","#f97316","#facc15","#4ade80","#3b82f6","#a855f7"];
  for(let i=0;i<6;i++){
    const a=laserAngle+i*0.18;
    ctx.strokeStyle=rainbowColors[i];ctx.lineWidth=1.5;ctx.globalAlpha=0.5;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*2,Math.sin(a)*r*2);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.5)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🔆",0,1);
  ctx.restore();
}
// ── Echo Star — fading echoes drift outward; tap original or echoes ──
function drawEchoStar(ctx,r,ts,isEcho,alpha){
  ctx.save();
  ctx.globalAlpha=(isEcho?alpha:1)*0.9;
  ctx.shadowBlur=isEcho?8:18;ctx.shadowColor=isEcho?"#fbbf24":"#ffd700";
  // 5-point star
  ctx.fillStyle=isEcho?"#fbbf24":"#ffd700";
  ctx.beginPath();
  for(let i=0;i<10;i++){
    const a=(i/10)*Math.PI*2-Math.PI/2;const sr=r*(i%2===0?1:0.45);
    i===0?ctx.moveTo(Math.cos(a)*sr,Math.sin(a)*sr):ctx.lineTo(Math.cos(a)*sr,Math.sin(a)*sr);
  }
  ctx.closePath();ctx.fill();
  if(!isEcho){
    ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.4)}px sans-serif`;
    ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("⭐",0,1);
  }
  ctx.restore();
}
// ── Meteor Shower — core target spawns 6 shower shards on tap ──
function drawMeteorShower(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#fb923c";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff7ed");g.addColorStop(0.5,"#fb923c");g.addColorStop(1,"#7c2d12");
  ctx.fillStyle=g;ctx.globalAlpha=0.93;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Small orbiting meteor dots
  for(let i=0;i<4;i++){
    const a=(i/4)*Math.PI*2+ts*0.018;
    ctx.fillStyle="#fbbf24";ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(Math.cos(a)*r*1.15,Math.sin(a)*r*1.15,4,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.6)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("☄️",0,1);
  ctx.restore();
}
// ── Golden Gate — only 0.8s visible; 2000pts ──
function drawGoldenGate(ctx,r,ts,agePct){
  ctx.save();
  const flash=Math.abs(Math.sin(ts*0.06));
  ctx.shadowBlur=30+flash*20;ctx.shadowColor="#ffd700";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,"#fef9c3");g.addColorStop(0.7,"#ffd700");g.addColorStop(1,"#92400e");
  ctx.fillStyle=g;ctx.globalAlpha=Math.max(0.5,1-agePct*1.5);ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Gate arch decoration
  ctx.strokeStyle="#fbbf24";ctx.lineWidth=3;ctx.globalAlpha=0.6;
  ctx.beginPath();ctx.arc(0,0,r*0.75,-Math.PI,0);ctx.stroke();
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.42)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";
  ctx.shadowBlur=8;ctx.shadowColor="#ffd700";ctx.fillText("2000",0,1);
  ctx.restore();
}
// ── Pulse Ring — expanding ring; sweet spot between 0.4r and 0.8r ──
function drawPulseRing(ctx,r,ts,ringPct){
  ctx.save();
  const inSweet=ringPct>0.35&&ringPct<0.65;
  const ringR=r*0.15+ringPct*r*0.85;
  ctx.shadowBlur=inSweet?22:8;ctx.shadowColor=inSweet?"#ffd700":"#818cf8";
  // Background circle
  ctx.fillStyle="#1e1b4b";ctx.globalAlpha=0.6;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Sweet spot band
  ctx.strokeStyle=inSweet?"#ffd700":"#c7d2fe";ctx.lineWidth=inSweet?6:3;ctx.globalAlpha=0.85;
  ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
  // Inner ring boundary
  ctx.strokeStyle="#374151";ctx.lineWidth=1.5;ctx.globalAlpha=0.4;
  ctx.beginPath();ctx.arc(0,0,r*0.35,0,Math.PI*2);ctx.stroke();
  ctx.beginPath();ctx.arc(0,0,r*0.65,0,Math.PI*2);ctx.stroke();
  // Center dot
  ctx.fillStyle="#818cf8";ctx.globalAlpha=0.8;ctx.beginPath();ctx.arc(0,0,r*0.12,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Clockwork Bomb — 5 gears; each tap advances gear; gear 5 = -1 life ──
function drawClockworkBomb(ctx,r,ts,gear){
  ctx.save();
  const gearColors=["#6b7280","#60a5fa","#4ade80","#fbbf24","#ef4444"];
  const gc=gearColors[Math.min(gear,4)];
  ctx.shadowBlur=8+gear*4;ctx.shadowColor=gc;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#f9fafb");g.addColorStop(0.5,gc);g.addColorStop(1,"#111");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Gear teeth
  const spin=ts*0.006*(gear+1);
  for(let i=0;i<12;i++){
    const a=spin+(i/12)*Math.PI*2;
    ctx.fillStyle=gc;ctx.globalAlpha=0.5;
    ctx.beginPath();ctx.rect(Math.cos(a)*r*0.9-3,Math.sin(a)*r*0.9-3,6,6);ctx.fill();
  }
  // Gear number
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`⚙️${gear+1}`,0,1);
  ctx.restore();
}
// ── Nebula Vortex — swirling gas cloud; grows over time; pts scale with size ──
function drawNebulaVortex(ctx,r,ts,growPct){
  ctx.save();
  const effectR=r*(0.6+growPct*0.8);
  ctx.shadowBlur=22;ctx.shadowColor="#818cf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,effectR);
  g.addColorStop(0,"rgba(165,180,252,0.95)");g.addColorStop(0.4,"rgba(139,92,246,0.7)");g.addColorStop(0.75,"rgba(79,70,229,0.4)");g.addColorStop(1,"rgba(17,24,39,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,effectR,0,Math.PI*2);ctx.fill();
  // Swirl arms
  ctx.globalAlpha=0.35;
  for(let arm=0;arm<3;arm++){
    const armOff=(arm/3)*Math.PI*2;
    ctx.beginPath();
    for(let i=0;i<=20;i++){
      const t2=i/20;const a=armOff+t2*Math.PI*3+ts*0.012;const sr=t2*effectR*0.9;
      i===0?ctx.moveTo(Math.cos(a)*sr,Math.sin(a)*sr):ctx.lineTo(Math.cos(a)*sr,Math.sin(a)*sr);
    }
    ctx.strokeStyle=["#c7d2fe","#ddd6fe","#ede9fe"][arm];ctx.lineWidth=2.5;ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🌌",0,1);
  ctx.restore();
}
// ── Frost Hex — hexagonal ice; freezes nearby moving targets ──
function drawFrostHex(ctx,r,ts,frozenCount){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#7dd3fc";
  const pulse=0.92+0.08*Math.sin(ts*0.014);
  const hexR=r*pulse;
  const g=ctx.createRadialGradient(0,0,0,0,0,hexR);
  g.addColorStop(0,"#f0f9ff");g.addColorStop(0.5,"#bae6fd");g.addColorStop(1,"#0369a1");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();
  for(let i=0;i<6;i++){const a=(i/6)*Math.PI*2-Math.PI/6;ctx.lineTo(Math.cos(a)*hexR,Math.sin(a)*hexR);}
  ctx.closePath();ctx.fill();
  // Ice crystal lines
  ctx.strokeStyle="#e0f2fe";ctx.lineWidth=1.5;ctx.globalAlpha=0.6;
  for(let i=0;i<6;i++){const a=(i/6)*Math.PI*2-Math.PI/6;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*hexR*0.85,Math.sin(a)*hexR*0.85);ctx.stroke();}
  // Frozen count indicator
  if(frozenCount>0){
    ctx.globalAlpha=0.9;ctx.fillStyle="#0ea5e9";ctx.font=`bold ${Math.floor(r*0.38)}px sans-serif`;
    ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`❄️×${frozenCount}`,0,1);
  } else {
    ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("❄️",0,1);
  }
  ctx.restore();
}
// ── Lightning Orb — chains sparks to nearby targets on tap ──
function drawLightningOrb(ctx,r,ts,nearbyCount){
  ctx.save();
  const crackle=Math.sin(ts*0.08)*0.5+0.5;
  ctx.shadowBlur=20+crackle*15;ctx.shadowColor="#facc15";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,"#fde68a");g.addColorStop(0.7,"#f59e0b");g.addColorStop(1,"#78350f");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Lightning sparks around edge
  ctx.strokeStyle="#fbbf24";ctx.lineWidth=2;ctx.globalAlpha=0.7+crackle*0.3;
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.02;const jitter=(Math.sin(ts*0.1+i*2.3))*5;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.7,Math.sin(a)*r*0.7);
    ctx.lineTo(Math.cos(a)*(r+jitter),Math.sin(a)*(r+jitter));ctx.stroke();
  }
  // Nearby chain count badge
  if(nearbyCount>0){ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`⚡${nearbyCount}`,0,1);}
  else{ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("⚡",0,1);}
  ctx.restore();
}
// ── Star Burst — expands over time; tap at full size for max pts ──
function drawStarBurst(ctx,r,ts,expandPct){
  ctx.save();
  const starR=r*(0.4+expandPct*0.7);
  const isFull=expandPct>=0.85;
  ctx.shadowBlur=isFull?40:14;ctx.shadowColor=isFull?"#ffd700":"#fbbf24";
  // 8-point star
  ctx.fillStyle=isFull?"#ffd700":"#fbbf24";ctx.globalAlpha=0.9;
  ctx.beginPath();
  for(let i=0;i<16;i++){
    const a=(i/16)*Math.PI*2-Math.PI/2;const sr=starR*(i%2===0?1:0.45);
    i===0?ctx.moveTo(Math.cos(a)*sr,Math.sin(a)*sr):ctx.lineTo(Math.cos(a)*sr,Math.sin(a)*sr);
  }
  ctx.closePath();ctx.fill();
  // Pulsing ring at full
  if(isFull){
    const ringPulse=0.8+0.2*Math.sin(ts*0.04);
    ctx.strokeStyle="#fff";ctx.lineWidth=3;ctx.globalAlpha=ringPulse;
    ctx.beginPath();ctx.arc(0,0,starR*1.15,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.45)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("💫",0,1);
  ctx.restore();
}
// ── Crystal Spire — tall crystal; shatters into shards on tap ──
function drawCrystalSpire(ctx,r,ts,chargePct){
  ctx.save();
  const pulse=0.95+0.05*Math.sin(ts*0.02);
  ctx.shadowBlur=14+chargePct*20;ctx.shadowColor="#67e8f9";
  // Crystal body — elongated hexagon
  const h=r*(1.6+chargePct*0.4);const w=r*0.7;
  ctx.fillStyle=`rgba(103,232,249,${0.7+chargePct*0.25})`;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.moveTo(0,-h);ctx.lineTo(w,-h*0.3);ctx.lineTo(w,h*0.3);ctx.lineTo(0,h);ctx.lineTo(-w,h*0.3);ctx.lineTo(-w,-h*0.3);ctx.closePath();ctx.fill();
  // Inner shine
  ctx.fillStyle="rgba(255,255,255,0.35)";ctx.beginPath();ctx.moveTo(0,-h*0.9);ctx.lineTo(w*0.4,-h*0.2);ctx.lineTo(0,-h*0.1);ctx.closePath();ctx.fill();
  // Charge glow rings
  if(chargePct>0.5){
    ctx.strokeStyle="#e0f2fe";ctx.lineWidth=2;ctx.globalAlpha=chargePct-0.5;
    ctx.beginPath();ctx.ellipse(0,0,w*1.3*pulse,h*0.5*pulse,0,0,Math.PI*2);ctx.stroke();
  }
  ctx.restore();
}
// ── Phase Gate — cycles solid/ghost; only tappable when solid ──
function drawPhaseGate(ctx,r,ts,isSolid){
  ctx.save();
  ctx.shadowBlur=isSolid?20:5;ctx.shadowColor=isSolid?"#22d3ee":"#6b7280";
  ctx.globalAlpha=isSolid?0.95:0.25;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,isSolid?"#a5f3fc":"#e5e7eb");g.addColorStop(0.5,isSolid?"#0ea5e9":"#9ca3af");g.addColorStop(1,isSolid?"#0c4a6e":"#374151");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Portal ring
  ctx.strokeStyle=isSolid?"#38bdf8":"#6b7280";ctx.lineWidth=3;ctx.globalAlpha=(isSolid?0.9:0.2);
  const spin=ts*0.018*(isSolid?1:-0.5);
  for(let i=0;i<8;i++){
    const a=spin+(i/8)*Math.PI*2;ctx.beginPath();
    ctx.moveTo(Math.cos(a)*r*0.6,Math.sin(a)*r*0.6);ctx.lineTo(Math.cos(a)*r*0.9,Math.sin(a)*r*0.9);ctx.stroke();
  }
  ctx.globalAlpha=isSolid?1:0.3;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(isSolid?"🌀":"👻",0,1);
  ctx.restore();
}
// ── Void Pulsar — shrinks over time; miss window costs pts ──
function drawVoidPulsar(ctx,r,ts,shrinkPct){
  ctx.save();
  const effectR=r*(1-shrinkPct*0.65);
  const danger=shrinkPct>0.6;
  ctx.shadowBlur=danger?25:12;ctx.shadowColor=danger?"#ef4444":"#7c3aed";
  const g=ctx.createRadialGradient(0,0,0,0,0,effectR);
  g.addColorStop(0,danger?"#fca5a5":"#ddd6fe");g.addColorStop(0.5,danger?"#ef4444":"#7c3aed");g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,effectR,0,Math.PI*2);ctx.fill();
  // Pulse ring
  const pulsing=0.7+0.3*Math.sin(ts*0.025*(1+shrinkPct*2));
  ctx.strokeStyle=danger?"#ef4444":"#a78bfa";ctx.lineWidth=2.5;ctx.globalAlpha=pulsing;
  ctx.beginPath();ctx.arc(0,0,effectR*1.2,0,Math.PI*2);ctx.stroke();
  ctx.globalAlpha=1;ctx.font=`${Math.floor(effectR*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(danger?"💀":"🌌",0,1);
  ctx.restore();
}
// ── Nova Cannon — charges up; fires shockwave that clears nearby targets ──
function drawNovaCannon(ctx,r,ts,chargePct){
  ctx.save();
  const isCharged=chargePct>=0.8;
  ctx.shadowBlur=isCharged?40:10+chargePct*20;ctx.shadowColor=isCharged?"#ffd700":"#f97316";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(chargePct*0.5,"#fed7aa");g.addColorStop(Math.min(0.9,chargePct),"#f97316");g.addColorStop(1,"#7c2d12");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Charging arcs
  if(chargePct>0.2){
    const arcCount=Math.floor(chargePct*6);
    for(let i=0;i<arcCount;i++){
      const a=((i/arcCount)*Math.PI*2)+ts*0.025;
      ctx.strokeStyle=isCharged?"#ffd700":"#fbbf24";ctx.lineWidth=2;ctx.globalAlpha=0.7*chargePct;
      ctx.beginPath();ctx.arc(0,0,r*0.8,a,a+0.6);ctx.stroke();
    }
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(isCharged?"🔥":"💥",0,1);
  ctx.restore();
}
// ── Hex Grid — 6 hex cells in a honeycomb; tap all for jackpot ──
function drawHexGrid(ctx,r,ts,tappedMask){
  ctx.save();
  ctx.shadowBlur=12;ctx.shadowColor="#818cf8";
  const hr=r*0.38;const positions=[];
  for(let i=0;i<6;i++){const a=(i/6)*Math.PI*2-Math.PI/6;positions.push([Math.cos(a)*hr*1.55,Math.sin(a)*hr*1.55]);}
  positions.forEach(([px,py],i)=>{
    const isTapped=!!(tappedMask&(1<<i));
    ctx.save();ctx.translate(px,py);
    ctx.fillStyle=isTapped?"#ffd700":"#818cf8";ctx.globalAlpha=isTapped?0.9:0.6;
    ctx.strokeStyle="#e0e7ff";ctx.lineWidth=1.5;
    ctx.beginPath();for(let j=0;j<6;j++){const a2=(j/6)*Math.PI*2-Math.PI/6;ctx.lineTo(Math.cos(a2)*hr,Math.sin(a2)*hr);}
    ctx.closePath();ctx.fill();ctx.stroke();ctx.restore();
  });
  // Center gem
  const allTapped=tappedMask===63;
  ctx.fillStyle=allTapped?"#ffd700":"#c7d2fe";ctx.globalAlpha=0.9;
  ctx.font=`${Math.floor(r*0.4)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(allTapped?"👑":"💜",0,1);
  ctx.restore();
}
// ── Quantum Tunnel — dual endpoints; teleports between them ──
function drawQuantumTunnel(ctx,r,ts,isPortalA){
  ctx.save();
  const colA="#22d3ee";const colB="#f472b6";
  const col=isPortalA?colA:colB;
  const spin=ts*0.022*(isPortalA?1:-1);
  ctx.shadowBlur=18;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"#0f172a");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Spiral arms
  ctx.globalAlpha=0.5;
  for(let i=0;i<3;i++){
    const aOff=(i/3)*Math.PI*2;
    ctx.beginPath();
    for(let j=0;j<=15;j++){const t2=j/15;const a=aOff+t2*Math.PI*2+spin;const sr=t2*r*0.85;ctx.lineTo(Math.cos(a)*sr,Math.sin(a)*sr);}
    ctx.strokeStyle=col;ctx.lineWidth=2;ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(isPortalA?"🔵":"🔴",0,1);
  ctx.restore();
}
// ── Inferno Orb — gains heat stacks per second; max = legendary ──
function drawInfernoOrb(ctx,r,ts,heatStacks){
  ctx.save();
  const maxHeat=8;const heatPct=Math.min(1,heatStacks/maxHeat);
  const r2=r*(1+heatPct*0.35);
  ctx.shadowBlur=10+heatPct*30;ctx.shadowColor=heatPct>=0.8?"#ffd700":"#f97316";
  const g=ctx.createRadialGradient(0,0,0,0,0,r2);
  g.addColorStop(0,"#fff");
  g.addColorStop(0.3,heatPct>=0.8?"#ffd700":"#fbbf24");
  g.addColorStop(0.7,heatPct>=0.8?"#f97316":"#dc2626");
  g.addColorStop(1,"#1c0a00");
  ctx.fillStyle=g;ctx.globalAlpha=0.93;ctx.beginPath();ctx.arc(0,0,r2,0,Math.PI*2);ctx.fill();
  // Flames
  for(let i=0;i<4;i++){
    const a=(i/4)*Math.PI*2+ts*0.02;
    ctx.fillStyle="#f97316";ctx.globalAlpha=0.4*heatPct;
    ctx.beginPath();ctx.arc(Math.cos(a)*r2*0.7,Math.sin(a)*r2*0.7,r2*0.25,0,Math.PI*2);ctx.fill();
  }
  // Heat stack indicator
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`🔥${heatStacks}`,0,1);
  ctx.restore();
}
// ── Chrono Sphere — slows nearby targets; bonus pts for each slowed ──
function drawChronoSphere(ctx,r,ts,slowedCount){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#6366f1";
  const pulse=0.93+0.07*Math.sin(ts*0.015);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#e0e7ff");g.addColorStop(0.4,"#818cf8");g.addColorStop(0.8,"#4f46e5");g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Clock face lines
  ctx.strokeStyle="#c7d2fe";ctx.lineWidth=1.5;ctx.globalAlpha=0.5;
  for(let i=0;i<12;i++){
    const a=(i/12)*Math.PI*2;const ir=r*0.6;const or=r*0.8;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*ir,Math.sin(a)*ir);ctx.lineTo(Math.cos(a)*or,Math.sin(a)*or);ctx.stroke();
  }
  // Slow aura ring
  ctx.strokeStyle="#818cf8";ctx.lineWidth=2;ctx.globalAlpha=0.35;
  ctx.setLineDash([5,5]);ctx.beginPath();ctx.arc(0,0,r*1.6,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);
  // Slowed count
  if(slowedCount>0){
    ctx.globalAlpha=0.9;ctx.fillStyle="#e0e7ff";ctx.font=`bold ${Math.floor(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`⏱×${slowedCount}`,0,1);
  } else {
    ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("⏱",0,1);
  }
  ctx.restore();
}
// ── Prismatic Lens — 6-color cycle; tap on focus color for bonus ──
function drawPrismaticLens(ctx,r,ts,colorIdx,focusIdx){
  ctx.save();
  const colors=["#ef4444","#f97316","#facc15","#4ade80","#22d3ee","#818cf8"];
  const isFocus=colorIdx===focusIdx;
  ctx.shadowBlur=isFocus?30:12;ctx.shadowColor=isFocus?"#ffd700":colors[colorIdx];
  // Prism body — triangle base
  ctx.fillStyle=colors[colorIdx];ctx.globalAlpha=0.88;
  ctx.beginPath();
  ctx.moveTo(0,-r);ctx.lineTo(r*0.87,r*0.5);ctx.lineTo(-r*0.87,r*0.5);ctx.closePath();ctx.fill();
  // Spinning rainbow arc
  if(isFocus){
    ctx.strokeStyle="#ffd700";ctx.lineWidth=3;ctx.globalAlpha=0.9;
    ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2);ctx.stroke();
  }
  // Color spectrum at bottom edge
  for(let i=0;i<6;i++){
    const dx=-r*0.7+i*(r*1.4/5.5);
    ctx.fillStyle=colors[i];ctx.globalAlpha=i===focusIdx?0.9:0.4;
    ctx.beginPath();ctx.arc(dx,r*0.6,r*0.1,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.42)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(isFocus?"🎯":"🔮",0,-r*0.15);
  ctx.restore();
}
// ── Shadow Mirror — reflects last tap; tap the mirror position ──
function drawShadowMirror(ctx,r,ts,isActive){
  ctx.save();
  ctx.shadowBlur=isActive?22:10;ctx.shadowColor=isActive?"#f472b6":"#6b7280";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,isActive?"#fdf4ff":"#1f2937");g.addColorStop(0.4,isActive?"#e879f9":"#6b7280");g.addColorStop(1,"#0f172a");
  ctx.fillStyle=g;ctx.globalAlpha=isActive?0.92:0.55;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Mirror reflection lines
  ctx.strokeStyle=isActive?"#f0abfc":"#4b5563";ctx.lineWidth=1.5;ctx.globalAlpha=0.6;
  for(let i=0;i<5;i++){const y=-r+i*(r*2/4);ctx.beginPath();ctx.moveTo(-r*0.8,y);ctx.lineTo(r*0.8,y);ctx.stroke();}
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(isActive?"🪞":"🌑",0,1);
  ctx.restore();
}
// ── Plasma Comet — parabolic arc; apex = best pts ──
function drawPlasmaComet(ctx,r,ts,apexPct){
  ctx.save();
  const isApex=apexPct>0.4&&apexPct<0.65;
  ctx.shadowBlur=isApex?35:14;ctx.shadowColor=isApex?"#ffd700":"#c084fc";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,isApex?"#ffd700":"#e879f9");g.addColorStop(0.7,"#7c3aed");g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Comet tail — backward trailing sparks
  ctx.globalAlpha=0.35;
  for(let i=1;i<=5;i++){
    const tailX=i*r*0.3;const tailR=r*(1-i*0.15);
    ctx.fillStyle=isApex?"#fcd34d":"#c084fc";ctx.beginPath();ctx.arc(-tailX,0,tailR,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(isApex?"⭐":"☄️",0,1);
  ctx.restore();
}
// ── Abyssal Gate — drains nearby normals; high reward on tap ──
function drawAbyssalGate(ctx,r,ts,drainCount){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.02);
  ctx.shadowBlur=25;ctx.shadowColor="#6d28d9";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#1e1b4b");g.addColorStop(0.3,"#4c1d95");g.addColorStop(0.7,"#7c3aed");g.addColorStop(1,"rgba(109,40,217,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Rotating absorption rings
  const spin=ts*0.014;
  for(let ring=0;ring<3;ring++){
    ctx.strokeStyle=["#7c3aed","#a78bfa","#ddd6fe"][ring];ctx.lineWidth=2.5-ring*0.5;
    ctx.globalAlpha=0.5-ring*0.1;ctx.setLineDash([8,8]);ctx.lineDashOffset=spin*(ring+1)*20;
    ctx.beginPath();ctx.arc(0,0,r*(0.5+ring*0.15)*pulse,0,Math.PI*2);ctx.stroke();
  }
  ctx.setLineDash([]);
  // Drain count badge
  if(drainCount>0){
    ctx.globalAlpha=0.9;ctx.fillStyle="#ede9fe";ctx.font=`bold ${Math.floor(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`⚫×${drainCount}`,0,1);
  } else {
    ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("⚫",0,1);
  }
  ctx.restore();
}
// ── Warp Ball — teleports rapidly; 800pts if caught within 0.3s of warp ──
function drawWarpBall(ctx,r,ts,justWarped){
  ctx.save();
  const pulse=0.88+0.12*Math.sin(ts*0.03);
  ctx.shadowBlur=justWarped?40:14;ctx.shadowColor=justWarped?"#ffd700":"#06b6d4";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,justWarped?"#fff":"#e0f2fe");g.addColorStop(0.4,justWarped?"#ffd700":"#0ea5e9");g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Warp distortion rings
  const warpRings=justWarped?3:1;
  for(let i=0;i<warpRings;i++){
    ctx.strokeStyle=justWarped?"#ffd700":"#38bdf8";ctx.lineWidth=2;ctx.globalAlpha=(0.7-i*0.2)*pulse;
    ctx.beginPath();ctx.arc(0,0,r*(1.1+i*0.2)*pulse,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(justWarped?"🌟":"🌀",0,1);
  ctx.restore();
}
// ── Crystal Spike — spiked; only safe from top quadrant ──
function drawCrystalSpike(ctx,r,ts,dangerArc){
  ctx.save();
  ctx.shadowBlur=12;ctx.shadowColor="#a5f3fc";
  // Main body
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#e0f2fe");g.addColorStop(0.5,"#0ea5e9");g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Danger spikes (3 spikes in danger arc)
  ctx.fillStyle="#ef4444";ctx.globalAlpha=0.8;ctx.shadowColor="#ef4444";
  for(let i=0;i<3;i++){
    const a=dangerArc+(i-1)*0.7;
    ctx.save();ctx.rotate(a);
    ctx.beginPath();ctx.moveTo(0,-r*0.85);ctx.lineTo(-r*0.18,-r*1.3);ctx.lineTo(r*0.18,-r*1.3);ctx.closePath();ctx.fill();
    ctx.restore();
  }
  // Safe zone arc highlight
  ctx.strokeStyle="#4ade80";ctx.lineWidth=3;ctx.globalAlpha=0.6;
  const safeA=dangerArc+Math.PI;
  ctx.beginPath();ctx.arc(0,0,r*1.1,safeA-0.6,safeA+0.6);ctx.stroke();
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.48)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("💎",0,1);
  ctx.restore();
}
// ── Energy Core — 3 phases (green/yellow/red); red = miss penalty ──
function drawEnergyCore(ctx,r,ts,phase){
  ctx.save();
  const colors=["#4ade80","#facc15","#ef4444"];
  const emoji=["✅","⚠️","☢️"];
  const col=colors[phase];
  ctx.shadowBlur=20;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"#1a1a2e");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Phase rings
  ctx.strokeStyle=col;ctx.lineWidth=3;ctx.globalAlpha=0.5+0.3*Math.sin(ts*0.04);
  ctx.beginPath();ctx.arc(0,0,r*1.12,0,Math.PI*2);ctx.stroke();
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(emoji[phase],0,1);
  ctx.restore();
}
// ── Arcane Circle — 5 rune symbols; tap when correct shown ──
function drawArcaneCircle(ctx,r,ts,runeIdx,targetRuneIdx){
  ctx.save();
  const runes=["🜁","🜂","🜃","🜄","🜅"];// element symbols
  const emojis=["🔥","💧","🌍","💨","⚡"];
  const isMatch=runeIdx===targetRuneIdx;
  ctx.shadowBlur=isMatch?30:10;ctx.shadowColor=isMatch?"#ffd700":"#818cf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,isMatch?"#fef9c3":"#e0e7ff");g.addColorStop(0.5,isMatch?"#fbbf24":"#818cf8");g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Rotating rune ring
  const spin=ts*0.014;
  for(let i=0;i<5;i++){
    const a=(i/5)*Math.PI*2+spin;const isTarget=i===targetRuneIdx;
    ctx.globalAlpha=isTarget?0.9:0.3;ctx.font=`${Math.floor(r*0.28)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText(emojis[i],Math.cos(a)*r*0.72,Math.sin(a)*r*0.72);
  }
  // Center active rune
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.48)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(emojis[runeIdx],0,1);
  ctx.restore();
}
// ── Stellar Drift — moves slowly; pts scale with distance from spawn ──
function drawStellarDrift(ctx,r,ts,driftDist){
  ctx.save();
  const driftPct=Math.min(1,driftDist/300);
  ctx.shadowBlur=10+driftPct*20;ctx.shadowColor=driftPct>0.5?"#ffd700":"#a5f3fc";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,driftPct>0.5?"#fcd34d":"#67e8f9");g.addColorStop(0.8,driftPct>0.5?"#f59e0b":"#0284c7");g.addColorStop(1,"#0f172a");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Drift trail sparkles
  for(let i=0;i<4;i++){
    const a=(i/4)*Math.PI*2+ts*0.01;const tr=r*(0.9+0.2*Math.sin(ts*0.03+i));
    ctx.fillStyle=driftPct>0.5?"#fcd34d":"#67e8f9";ctx.globalAlpha=0.4*driftPct;
    ctx.beginPath();ctx.arc(Math.cos(a)*tr,Math.sin(a)*tr,r*0.12,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(driftPct>0.5?"⭐":"🌠",0,1);
  ctx.restore();
}
// ── Bubble Wave — grows over time; pop before burst = pts; burst = -1 life ──
function drawBubbleWave(ctx,r,ts,growPct){
  ctx.save();
  const effR=r*(0.4+growPct*0.8);
  const isDanger=growPct>0.85;
  ctx.shadowBlur=isDanger?25:10;ctx.shadowColor=isDanger?"#ef4444":"#38bdf8";
  // Outer shell
  ctx.strokeStyle=isDanger?"#ef4444":"#38bdf8";ctx.lineWidth=3;ctx.globalAlpha=0.75;
  ctx.beginPath();ctx.arc(0,0,effR,0,Math.PI*2);ctx.stroke();
  // Inner fill
  ctx.fillStyle=isDanger?"rgba(239,68,68,0.12)":"rgba(56,189,248,0.12)";ctx.globalAlpha=0.8;
  ctx.beginPath();ctx.arc(0,0,effR*0.9,0,Math.PI*2);ctx.fill();
  // Bubble shine
  ctx.fillStyle="#fff";ctx.globalAlpha=0.35;ctx.beginPath();ctx.ellipse(-effR*0.3,-effR*0.35,effR*0.2,effR*0.12,-Math.PI/5,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;ctx.font=`${Math.floor(Math.max(14,r*0.4))}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(isDanger?"💥":"🫧",0,1);
  ctx.restore();
}
// ── Iron Shield — 3-hit target; each hit shows breaking shield ──
function drawIronShield(ctx,r,ts,hitsLeft){
  ctx.save();
  const integrity=hitsLeft/3;
  ctx.shadowBlur=8+integrity*12;ctx.shadowColor=integrity>0.5?"#94a3b8":"#f87171";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,integrity>0.5?"#e2e8f0":"#fecaca");g.addColorStop(0.5,integrity>0.5?"#64748b":"#ef4444");g.addColorStop(1,"#1e293b");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Shield icon with cracks based on hits
  if(hitsLeft<3){
    ctx.strokeStyle="#f87171";ctx.lineWidth=2.5;ctx.globalAlpha=0.8;
    const cracks=3-hitsLeft;
    for(let i=0;i<cracks;i++){const a=(i/cracks)*Math.PI*2;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*0.85,Math.sin(a)*r*0.85);ctx.stroke();}
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.52)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`🛡️`,0,1);
  // Hits remaining counter
  ctx.fillStyle="#fff";ctx.font=`bold ${Math.floor(r*0.35)}px sans-serif`;ctx.fillText(`×${hitsLeft}`,-r*0.05,r*0.42);
  ctx.restore();
}
// ── Time Echo — ghost replay of a previous target's path ──
function drawTimeEcho(ctx,r,ts,opacity){
  ctx.save();
  ctx.globalAlpha=opacity*0.65;ctx.shadowBlur=14;ctx.shadowColor="#a78bfa";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"rgba(255,255,255,0.6)");g.addColorStop(0.5,"rgba(167,139,250,0.5)");g.addColorStop(1,"rgba(79,70,229,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Phase shift distortion
  ctx.strokeStyle="#a78bfa";ctx.lineWidth=2;ctx.globalAlpha=opacity*0.4;
  ctx.beginPath();ctx.arc(0,0,r*1.15,ts*0.015,ts*0.015+Math.PI*1.5);ctx.stroke();
  ctx.globalAlpha=opacity;ctx.font=`${Math.floor(r*0.52)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("👻",0,1);
  ctx.restore();
}
// ── Gravity Orb Mk2 — black hole; pulls all targets in; huge pts ──
function drawGravityOrb2(ctx,r,ts,pullCount){
  ctx.save();
  const warp=0.88+0.12*Math.sin(ts*0.025);
  ctx.shadowBlur=30+pullCount*5;ctx.shadowColor=pullCount>=5?"#ffd700":"#4c1d95";
  // Event horizon
  const g=ctx.createRadialGradient(0,0,0,0,0,r*warp);
  g.addColorStop(0,"#000");g.addColorStop(0.3,"#1e1b4b");g.addColorStop(0.6,"#4c1d95");g.addColorStop(1,"rgba(109,40,217,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.95;ctx.beginPath();ctx.arc(0,0,r*warp,0,Math.PI*2);ctx.fill();
  // Accretion disc
  ctx.strokeStyle=pullCount>=5?"#ffd700":"#7c3aed";ctx.lineWidth=3;ctx.globalAlpha=0.65;
  ctx.save();ctx.scale(1,0.3);ctx.beginPath();ctx.arc(0,0,r*1.4,0,Math.PI*2);ctx.stroke();ctx.restore();
  // Pull count
  if(pullCount>0){ctx.fillStyle="#ede9fe";ctx.font=`bold ${Math.floor(r*0.4)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`⚫×${pullCount}`,0,1);}
  else{ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🌑",0,1);}
  ctx.restore();
}
// ── Pixel Swarm — 8 tiny pixels scattered around; collect all ──
function drawPixelSwarmCore(ctx,r,ts){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.02);
  ctx.shadowBlur=14;ctx.shadowColor="#f472b6";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#f9a8d4");g.addColorStop(1,"#9d174d");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("📡",0,1);
  ctx.restore();
}
function drawPixelUnit(ctx,r,ts,idx){
  ctx.save();
  const pixColors=["#f472b6","#fb923c","#facc15","#4ade80","#22d3ee","#818cf8","#c084fc","#f9fafb"];
  ctx.shadowBlur=8;ctx.shadowColor=pixColors[idx%8];
  ctx.fillStyle=pixColors[idx%8];ctx.globalAlpha=0.9;
  const side=r*1.6;
  ctx.beginPath();ctx.rect(-side/2,-side/2,side,side);ctx.fill();
  ctx.restore();
}
// ── Chrono Crystal — tap within 3s to freeze; tap again in freeze = 2× pts ──
function drawChronoCrystal(ctx,r,ts,phase,frozenPct){
  ctx.save();
  const col=phase==="frozen"?"#7dd3fc":"#818cf8";
  ctx.shadowBlur=18;ctx.shadowColor=col;
  const spin=ts*(phase==="frozen"?0.002:0.007);
  // Hexagonal crystal body
  ctx.beginPath();
  for(let i=0;i<6;i++){
    const a=spin+i*Math.PI/3;
    i===0?ctx.moveTo(Math.cos(a)*r,Math.sin(a)*r):ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);
  }
  ctx.closePath();
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  if(phase==="frozen"){
    g.addColorStop(0,"#e0f2fe");g.addColorStop(0.5,"#7dd3fc");g.addColorStop(1,"#0369a1");
  } else {
    g.addColorStop(0,"#e0e7ff");g.addColorStop(0.5,"#818cf8");g.addColorStop(1,"#4338ca");
  }
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.fill();
  // Clock hands
  ctx.strokeStyle="#ffffff";ctx.lineWidth=1.5;ctx.globalAlpha=0.8;
  const handAngle=ts*0.006*(phase==="frozen"?0:1);
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(handAngle)*r*0.55,Math.sin(handAngle)*r*0.55);ctx.stroke();
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(handAngle*2)*r*0.35,Math.sin(handAngle*2)*r*0.35);ctx.stroke();
  // Frozen frost ring
  if(phase==="frozen"){
    ctx.strokeStyle="#bae6fd";ctx.lineWidth=2;ctx.globalAlpha=0.6+0.4*Math.sin(ts*0.015);
    ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2);ctx.stroke();
    ctx.font=`bold ${r*0.55}px sans-serif`;ctx.fillStyle="#fff";ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.globalAlpha=0.9;ctx.fillText("⏳",0,0);
  }
  ctx.restore();
}
// ── Void Seed — grows over time; max size → detonates ──
function drawVoidSeed(ctx,r,ts,growPct){
  ctx.save();
  const pulse=0.93+0.07*Math.sin(ts*0.014+growPct*Math.PI);
  const col=`hsl(${270+growPct*60},80%,${40+growPct*20}%)`;
  ctx.shadowBlur=12+growPct*20;ctx.shadowColor=col;
  // Core
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#f9a8d4");g.addColorStop(0.4,col);g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Growth ring spikes
  const spikes=Math.round(4+growPct*8);
  ctx.strokeStyle=col;ctx.lineWidth=1.5;ctx.globalAlpha=0.5;
  for(let i=0;i<spikes;i++){
    const a=ts*0.003+i*(Math.PI*2/spikes);
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.9,Math.sin(a)*r*0.9);
    ctx.lineTo(Math.cos(a)*r*1.35,Math.sin(a)*r*1.35);ctx.stroke();
  }
  // Growth % label
  if(growPct>0.5){
    ctx.font=`bold ${r*0.6}px sans-serif`;ctx.fillStyle="#fde68a";ctx.textAlign="center";
    ctx.textBaseline="middle";ctx.globalAlpha=0.9;
    ctx.fillText(`${Math.round(growPct*100)}%`,0,0);
  }
  ctx.restore();
}
// ── Prism Shard — splits into 3 mini shards on tap ──
function drawPrismShard(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#f0abfc";
  const spin=ts*0.008;
  // 3 rotated triangles
  for(let i=0;i<3;i++){
    const a=spin+i*(Math.PI*2/3);
    const hue=300+i*40;
    ctx.fillStyle=`hsl(${hue},80%,65%)`;ctx.globalAlpha=0.85;
    ctx.beginPath();
    for(let j=0;j<3;j++){
      const pa=a+j*(Math.PI*2/3);
      j===0?ctx.moveTo(Math.cos(pa)*r,Math.sin(pa)*r):ctx.lineTo(Math.cos(pa)*r,Math.sin(pa)*r);
    }
    ctx.closePath();ctx.fill();
  }
  // Bright center gem
  ctx.globalAlpha=0.9;ctx.fillStyle="#ffffff";ctx.shadowBlur=8;
  ctx.beginPath();ctx.arc(0,0,r*0.25,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Prism Mini Shard ──
function drawPrismMiniShard(ctx,r,ts,idx){
  ctx.save();
  const hue=300+idx*40;
  ctx.shadowBlur=10;ctx.shadowColor=`hsl(${hue},80%,65%)`;
  ctx.fillStyle=`hsl(${hue},80%,65%)`;ctx.globalAlpha=0.9;
  const spin=ts*0.012+idx*Math.PI*2/3;
  ctx.beginPath();
  for(let j=0;j<3;j++){
    const pa=spin+j*(Math.PI*2/3);
    j===0?ctx.moveTo(Math.cos(pa)*r,Math.sin(pa)*r):ctx.lineTo(Math.cos(pa)*r,Math.sin(pa)*r);
  }
  ctx.closePath();ctx.fill();
  ctx.restore();
}
// ── Temporal Orb — rewinds position every 2s ──
function drawTemporalOrb(ctx,r,ts,rewindPct){
  ctx.save();
  const col=rewindPct>0.8?"#f59e0b":"#38bdf8";
  ctx.shadowBlur=16;ctx.shadowColor=col;
  const pulse=0.92+0.08*Math.sin(ts*0.012);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#e0f2fe");g.addColorStop(0.45,col);g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Clockwise rotating ring
  const ringAngle=ts*0.006*(rewindPct>0.8?-3:1);
  ctx.strokeStyle=col;ctx.lineWidth=2;ctx.globalAlpha=0.6;
  ctx.setLineDash([8,5]);ctx.lineDashOffset=-ts*0.04;
  ctx.beginPath();ctx.arc(0,0,r*1.25,ringAngle,ringAngle+Math.PI*1.7);ctx.stroke();
  ctx.setLineDash([]);
  // Rewind flash
  if(rewindPct>0.8){
    ctx.globalAlpha=(rewindPct-0.8)/0.2*0.8;
    ctx.fillStyle="#fef3c7";
    ctx.beginPath();ctx.arc(0,0,r*1.4,0,Math.PI*2);ctx.fill();
    ctx.font=`bold ${r*0.8}px sans-serif`;ctx.fillStyle="#f59e0b";
    ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=1;ctx.fillText("⏪",0,0);
  }
  ctx.restore();
}
// ── Lightning Chain — chains to 3 nearest normals on tap ──
function drawLightningChain(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=20;ctx.shadowColor="#fbbf24";
  const spin=ts*0.01;
  // Core orb
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fffbeb");g.addColorStop(0.4,"#fbbf24");g.addColorStop(1,"#92400e");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Lightning arcs radiating outward
  ctx.strokeStyle="#fde68a";ctx.lineWidth=2;ctx.globalAlpha=0.75;
  for(let i=0;i<4;i++){
    const a=spin+i*Math.PI/2;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.7,Math.sin(a)*r*0.7);
    const mid1x=Math.cos(a+0.3)*r*1.1,mid1y=Math.sin(a+0.3)*r*1.1;
    const endx=Math.cos(a)*r*1.6,endy=Math.sin(a)*r*1.6;
    ctx.quadraticCurveTo(mid1x,mid1y,endx,endy);ctx.stroke();
  }
  // Chain link icon
  ctx.font=`bold ${r*0.9}px sans-serif`;ctx.fillStyle="#fff";ctx.textAlign="center";
  ctx.textBaseline="middle";ctx.globalAlpha=0.85;ctx.fillText("⚡",0,0);
  ctx.restore();
}
// ── Magma Seed — orbited by 5 lava drops ──
function drawMagmaSeed(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#ef4444";
  const pulse=0.92+0.08*Math.sin(ts*0.013);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fef9c3");g.addColorStop(0.35,"#f97316");g.addColorStop(0.7,"#ef4444");g.addColorStop(1,"#1c1917");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Lava cracks
  ctx.strokeStyle="#fcd34d";ctx.lineWidth=1.5;ctx.globalAlpha=0.7;
  for(let i=0;i<5;i++){
    const a=ts*0.005+i*(Math.PI*2/5);
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*0.75,Math.sin(a)*r*0.75);ctx.stroke();
  }
  ctx.font=`bold ${r*0.75}px sans-serif`;ctx.fillStyle="#fff";ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText("🌋",0,0);
  ctx.restore();
}
// ── Lava Drop — orbits magma seed ──
function drawLavaDrop(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=10;ctx.shadowColor="#f97316";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fef9c3");g.addColorStop(0.5,"#f97316");g.addColorStop(1,"#7c2d12");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Gemini Pair — two linked targets; tap both within 3s for bonus ──
function drawGeminiPair(ctx,r,ts,tapped){
  ctx.save();
  const col=tapped?"#fde68a":"#a78bfa";
  ctx.shadowBlur=16;ctx.shadowColor=col;
  const pulse=0.92+0.08*Math.sin(ts*0.011);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#f5f3ff");g.addColorStop(0.5,col);g.addColorStop(1,"#4c1d95");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Gemini symbol or "♊"
  ctx.font=`bold ${r*0.9}px sans-serif`;ctx.fillStyle="#fff";ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText(tapped?"⭐":"♊",0,0);
  // Tapped ring
  if(tapped){
    ctx.strokeStyle="#fde68a";ctx.lineWidth=2;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(0,0,r*1.3+Math.sin(ts*0.02)*3,0,Math.PI*2);ctx.stroke();
  }
  ctx.restore();
}
// ── Aurora Ring — expanding ring; tap to burst nearby targets ──
function drawAuroraRing(ctx,r,ts,expandPct){
  ctx.save();
  const ringR=r*(1+expandPct*3.5);
  const col=`hsl(${200+expandPct*100},80%,65%)`;
  ctx.shadowBlur=18;ctx.shadowColor=col;
  // Center dot
  ctx.fillStyle=col;ctx.globalAlpha=0.85;
  ctx.beginPath();ctx.arc(0,0,r*0.4,0,Math.PI*2);ctx.fill();
  // Expanding ring
  ctx.strokeStyle=col;ctx.lineWidth=3+expandPct*4;ctx.globalAlpha=0.8-expandPct*0.4;
  ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
  // Secondary ring faint
  ctx.lineWidth=1.5;ctx.globalAlpha=0.3;
  ctx.beginPath();ctx.arc(0,0,ringR*1.15,0,Math.PI*2);ctx.stroke();
  // Stars in ring
  if(expandPct>0.3){
    ctx.globalAlpha=0.7;ctx.fillStyle="#fff";
    for(let i=0;i<6;i++){
      const a=ts*0.005+i*(Math.PI/3);
      ctx.beginPath();ctx.arc(Math.cos(a)*ringR,Math.sin(a)*ringR,2,0,Math.PI*2);ctx.fill();
    }
  }
  ctx.restore();
}
// ── Neon Pulse — alternates bright/dim; bright tap = 3× pts ──
function drawNeonPulse(ctx,r,ts,brightPct){
  ctx.save();
  const hue=200+brightPct*120;
  const glow=brightPct>0.7?30:8;
  ctx.shadowBlur=glow;ctx.shadowColor=`hsl(${hue},90%,60%)`;
  const col=`hsl(${hue},90%,${40+brightPct*30}%)`;
  ctx.fillStyle=col;ctx.globalAlpha=0.5+brightPct*0.45;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Bright inner ring
  if(brightPct>0.6){
    ctx.strokeStyle="#ffffff";ctx.lineWidth=2+brightPct*2;ctx.globalAlpha=brightPct*0.8;
    ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.stroke();
  }
  // Dimmer state indicator
  ctx.font=`bold ${r*0.8}px sans-serif`;ctx.fillStyle="#fff";ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.7+brightPct*0.25;
  ctx.fillText(brightPct>0.65?"💡":"🔅",0,0);
  ctx.restore();
}
// ── Mirror Twin — spawns a mirrored copy; tap both within 4s for bonus ──
function drawMirrorTwin(ctx,r,ts,isMirror){
  ctx.save();
  const col=isMirror?"#f9a8d4":"#34d399";
  ctx.shadowBlur=14;ctx.shadowColor=col;
  const pulse=0.93+0.07*Math.sin(ts*0.01);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"#064e3b");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Mirror sheen
  ctx.strokeStyle="#ffffff";ctx.lineWidth=1.5;ctx.globalAlpha=0.4;
  ctx.beginPath();ctx.arc(0,0,r*0.85,Math.PI*1.1,Math.PI*1.9);ctx.stroke();
  ctx.font=`bold ${r*0.8}px sans-serif`;ctx.fillStyle="#fff";ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.85;ctx.fillText(isMirror?"🪞":"🌿",0,0);
  ctx.restore();
}
// ── Star Cluster — explodes into 5 sparkle mini-targets ──
function drawStarCluster(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=20;ctx.shadowColor="#fbbf24";
  const spin=ts*0.007;
  // 5 orbiting stars
  for(let i=0;i<5;i++){
    const a=spin+i*(Math.PI*2/5);
    const sr=r*0.55;
    ctx.fillStyle="#fde68a";ctx.globalAlpha=0.9;
    ctx.font=`bold ${sr*0.9}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText("⭐",Math.cos(a)*r*0.7,Math.sin(a)*r*0.7);
  }
  // Core
  ctx.globalAlpha=0.95;ctx.shadowColor="#fbbf24";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.35);
  g.addColorStop(0,"#fff");g.addColorStop(1,"#fbbf24");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.35,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Star Sparkle — mini orbiting star from cluster ──
function drawStarSparkle(ctx,r,ts,idx){
  ctx.save();
  const hue=40+idx*25;
  ctx.shadowBlur=10;ctx.shadowColor=`hsl(${hue},90%,65%)`;
  ctx.font=`bold ${r*1.8}px sans-serif`;ctx.fillStyle=`hsl(${hue},90%,75%)`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.9;ctx.fillText("✨",0,0);
  ctx.restore();
}
// ── Void Crystal — shrinks over lifetime; smaller = more pts ──
function drawVoidCrystal(ctx,r,ts,sizePct){
  ctx.save();
  const col=`hsl(${270+sizePct*60},75%,${35+sizePct*15}%)`;
  ctx.shadowBlur=12+sizePct*12;ctx.shadowColor=col;
  const spin=ts*0.006*(2-sizePct);
  ctx.fillStyle=col;ctx.globalAlpha=0.85+sizePct*0.1;
  // 8-pointed crystal
  ctx.beginPath();
  for(let i=0;i<8;i++){
    const a=spin+i*Math.PI/4;
    const ri=i%2===0?r:r*0.4;
    i===0?ctx.moveTo(Math.cos(a)*ri,Math.sin(a)*ri):ctx.lineTo(Math.cos(a)*ri,Math.sin(a)*ri);
  }
  ctx.closePath();ctx.fill();
  // Inner gem
  const gInner=ctx.createRadialGradient(0,0,0,0,0,r*0.4);
  gInner.addColorStop(0,"#e9d5ff");gInner.addColorStop(1,col);
  ctx.fillStyle=gInner;ctx.globalAlpha=0.8;
  ctx.beginPath();ctx.arc(0,0,r*0.4,0,Math.PI*2);ctx.fill();
  // Size% label
  ctx.font=`bold ${r*0.7}px sans-serif`;ctx.fillStyle="#fde68a";ctx.textAlign="center";
  ctx.textBaseline="middle";ctx.globalAlpha=0.9;
  ctx.fillText(`${Math.round(sizePct*100)}%`,0,0);
  ctx.restore();
}
// ── Energy Orb — charges over 6s; at 80%+ tap = shockwave boost ──
function drawEnergyOrb(ctx,r,ts,chargePct){
  ctx.save();
  const col=chargePct>0.8?`hsl(${40+chargePct*60},90%,60%)`:`hsl(${180+chargePct*60},80%,50%)`;
  ctx.shadowBlur=12+chargePct*18;ctx.shadowColor=col;
  // Charge fill arc
  ctx.strokeStyle=col;ctx.lineWidth=4;ctx.globalAlpha=0.85;
  ctx.beginPath();ctx.arc(0,0,r,(-Math.PI/2),((-Math.PI/2)+chargePct*Math.PI*2));ctx.stroke();
  // Core
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.7);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;
  ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.fill();
  // Charge label
  ctx.font=`bold ${r*0.65}px sans-serif`;ctx.fillStyle="#fff";ctx.textAlign="center";
  ctx.textBaseline="middle";ctx.globalAlpha=0.9;
  ctx.fillText(chargePct>=0.8?"⚡":`${Math.round(chargePct*100)}%`,0,0);
  ctx.restore();
}
// ── Comet Trail — fast-moving with trail; trail near tap = bonus ──
function drawCometTrail(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor="#f97316";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fef9c3");g.addColorStop(0.5,"#f97316");g.addColorStop(1,"#7c2d12");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  ctx.font=`bold ${r*0.85}px sans-serif`;ctx.fillStyle="#fff";ctx.textAlign="center";
  ctx.textBaseline="middle";ctx.globalAlpha=0.85;ctx.fillText("☄️",0,0);
  ctx.restore();
}
// ── Rune Burst — fires 5 rune blasts at nearby normals on tap ──
function drawRuneBurst(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#c084fc";
  const spin=ts*0.008;
  // Pentagram rune lines
  ctx.strokeStyle="#c084fc";ctx.lineWidth=1.8;ctx.globalAlpha=0.6;
  for(let i=0;i<5;i++){
    const a=spin+i*(Math.PI*2/5);
    const b=spin+(i+2)*(Math.PI*2/5);
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.85,Math.sin(a)*r*0.85);
    ctx.lineTo(Math.cos(b)*r*0.85,Math.sin(b)*r*0.85);ctx.stroke();
  }
  // Core
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.55);
  g.addColorStop(0,"#f5f3ff");g.addColorStop(0.5,"#c084fc");g.addColorStop(1,"#581c87");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*0.55,0,Math.PI*2);ctx.fill();
  ctx.font=`bold ${r*0.75}px sans-serif`;ctx.fillStyle="#fff";ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.85;ctx.fillText("🔮",0,0);
  ctx.restore();
}
// ── Crystal Golem — 3 HP; layers reveal gems; 3rd hit = full bonus ──
function drawCrystalGolem(ctx,r,ts,hitsLeft){
  ctx.save();
  const cols=["#f9fafb","#86efac","#67e8f9"];
  const col=cols[hitsLeft-1]||"#f9fafb";
  ctx.shadowBlur=16;ctx.shadowColor=col;
  const pulse=0.92+0.08*Math.sin(ts*0.011);
  // Blocky hexagon body
  ctx.fillStyle=col;ctx.globalAlpha=0.88;
  ctx.beginPath();
  for(let i=0;i<6;i++){
    const a=i*Math.PI/3;
    i===0?ctx.moveTo(Math.cos(a)*r*pulse,Math.sin(a)*r*pulse):ctx.lineTo(Math.cos(a)*r*pulse,Math.sin(a)*r*pulse);
  }
  ctx.closePath();ctx.fill();
  // Crystal cracks (shown as lines)
  if(hitsLeft<3){
    ctx.strokeStyle="#1e1b4b";ctx.lineWidth=2;ctx.globalAlpha=0.5;
    ctx.beginPath();ctx.moveTo(-r*0.3,-r*0.5);ctx.lineTo(r*0.2,r*0.3);ctx.stroke();
  }
  if(hitsLeft===1){
    ctx.beginPath();ctx.moveTo(r*0.3,-r*0.3);ctx.lineTo(-r*0.2,r*0.4);ctx.stroke();
  }
  // HP indicator
  ctx.font=`bold ${r*0.75}px sans-serif`;ctx.fillStyle="#fff";ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;
  const emojis=["💎","🟢","⚪"];ctx.fillText(emojis[hitsLeft-1]||"⚪",0,0);
  ctx.restore();
}
// ── Freeze Orb — icy pulsing orb; tapping freezes all normals 3s ──
function drawFreezeOrb(ctx,r,ts,pulsePct){
  ctx.save();
  const pulse=0.82+0.18*Math.sin(pulsePct*Math.PI*2);
  ctx.shadowBlur=22*pulse;ctx.shadowColor="#a5f3fc";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"rgba(255,255,255,0.95)");
  grad.addColorStop(0.45,"rgba(165,243,252,0.88)");
  grad.addColorStop(1,"rgba(6,182,212,0.6)");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Ice crystal spikes
  ctx.strokeStyle="rgba(224,242,254,0.85)";ctx.lineWidth=1.8;
  for(let i=0;i<6;i++){
    const angle=i*(Math.PI/3)+ts*0.0008;
    const len=r*(0.85+0.15*Math.sin(pulsePct*Math.PI*2+i*1.1));
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(angle)*len,Math.sin(angle)*len);ctx.stroke();
    ctx.fillStyle="rgba(255,255,255,0.95)";ctx.beginPath();ctx.arc(Math.cos(angle)*len,Math.sin(angle)*len,2.2,0,Math.PI*2);ctx.fill();
  }
  ctx.fillStyle="#ffffff";ctx.beginPath();ctx.arc(0,0,r*0.22,0,Math.PI*2);ctx.fill();
  ctx.font=`${Math.round(r*0.75)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText("🧊",0,0);
  ctx.restore();
}
// ── Twin Comet — pair of fast comets; tap one to kill both ──
function drawTwinComet(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#f97316";
  const grad=ctx.createRadialGradient(-r*0.15,-r*0.15,0,0,0,r);
  grad.addColorStop(0,"rgba(255,255,255,0.95)");
  grad.addColorStop(0.4,"rgba(251,146,60,0.9)");
  grad.addColorStop(1,"rgba(239,68,68,0.65)");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  ctx.strokeStyle="rgba(254,215,170,0.6)";ctx.lineWidth=2;
  ctx.beginPath();ctx.arc(0,0,r*1.22,0,Math.PI*2);ctx.stroke();
  ctx.font=`${Math.round(r*0.88)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.88;ctx.fillText("☄️",0,0);
  ctx.restore();
}
// ── Plasma Vortex — spinning ring with inner alignment window ──
function drawPlasmaVortex(ctx,r,ts,phase){
  ctx.save();
  const spd=ts*0.003;
  ctx.shadowBlur=18;ctx.shadowColor="#c084fc";
  // Outer spinning ring
  for(let i=0;i<12;i++){
    const a=i*(Math.PI/6)+spd;
    const rc=r*(0.85+0.15*Math.sin(spd*2+i));
    ctx.fillStyle=i%2===0?"rgba(192,132,252,0.85)":"rgba(139,92,246,0.7)";
    ctx.beginPath();ctx.arc(Math.cos(a)*rc,Math.sin(a)*rc,r*0.18,0,Math.PI*2);ctx.fill();
  }
  // Inner alignment ring (pulses in-out)
  const alignR=r*(0.35+0.45*phase);
  const alignAlpha=phase>0.75?0.9:0.4;
  ctx.strokeStyle=`rgba(253,224,71,${alignAlpha})`;ctx.lineWidth=phase>0.75?3.5:2;
  ctx.beginPath();ctx.arc(0,0,alignR,0,Math.PI*2);ctx.stroke();
  // Center
  ctx.fillStyle="#ffffff";ctx.beginPath();ctx.arc(0,0,r*0.18,0,Math.PI*2);ctx.fill();
  ctx.font=`${Math.round(r*0.75)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.85;ctx.fillText("🌀",0,0);
  ctx.restore();
}
// ── Star Bomb — exploding star; tap early or let it auto-detonate ──
function drawStarBomb(ctx,r,ts,fuse){
  ctx.save();
  ctx.shadowBlur=20+8*Math.sin(ts*0.01);ctx.shadowColor="#fbbf24";
  // Pulsing star shape
  const n=8,outerR=r,innerR=r*0.45;
  ctx.fillStyle=fuse<0.3?"#ef4444":fuse<0.6?"#f97316":"#fbbf24";
  ctx.beginPath();
  for(let i=0;i<n*2;i++){
    const a=i*Math.PI/n-Math.PI/2;
    const rad=i%2===0?outerR:innerR;
    i===0?ctx.moveTo(Math.cos(a)*rad,Math.sin(a)*rad):ctx.lineTo(Math.cos(a)*rad,Math.sin(a)*rad);
  }
  ctx.closePath();ctx.fill();
  // Fuse glow
  const fuseC=fuse<0.3?"#ef4444":fuse<0.6?"#fb923c":"#fde68a";
  ctx.strokeStyle=fuseC;ctx.lineWidth=2.5;
  ctx.beginPath();ctx.arc(0,0,r*1.15,0,Math.PI*2*(1-fuse));ctx.stroke();
  ctx.font=`${Math.round(r*0.72)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText("💥",0,0);
  ctx.restore();
}
// ── Glow Shard — splits into 3 glowing shards on tap ──
function drawGlowShard(ctx,r,ts,isFragment,colorStr){
  ctx.save();
  const col=colorStr||"#f0abfc";
  const spin=ts*(isFragment?0.006:0.004);
  ctx.shadowBlur=16;ctx.shadowColor=col;
  ctx.fillStyle=col;
  // Diamond shape
  ctx.beginPath();
  ctx.moveTo(0,-r);ctx.lineTo(r*0.6,0);ctx.lineTo(0,r);ctx.lineTo(-r*0.6,0);
  ctx.closePath();ctx.fill();
  // Rotating sparkle lines
  for(let i=0;i<4;i++){
    const a=i*Math.PI/2+spin;
    ctx.strokeStyle=`rgba(255,255,255,0.6)`;ctx.lineWidth=1.5;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.4,Math.sin(a)*r*0.4);ctx.lineTo(Math.cos(a)*r*1.1,Math.sin(a)*r*1.1);ctx.stroke();
  }
  ctx.font=`${Math.round(r*0.75)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.85;ctx.fillText(isFragment?"✨":"💎",0,0);
  ctx.restore();
}
// ── Quantum Leech — drains score slowly; tap to reclaim ──
function drawQuantumLeech(ctx,r,ts,drainPct){
  ctx.save();
  const pulse=0.88+0.12*Math.sin(ts*0.009);
  ctx.shadowBlur=18*pulse;ctx.shadowColor="#4ade80";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"rgba(255,255,255,0.9)");
  grad.addColorStop(0.35,"rgba(74,222,128,0.85)");
  grad.addColorStop(1,"rgba(22,163,74,0.65)");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Drain ring (shows how much has been drained)
  ctx.strokeStyle="#ef4444";ctx.lineWidth=3;ctx.globalAlpha=0.75;
  ctx.beginPath();ctx.arc(0,0,r*1.1,-Math.PI/2,-Math.PI/2+drainPct*Math.PI*2,false);ctx.stroke();
  ctx.globalAlpha=1;
  ctx.font=`${Math.round(r*0.75)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🧿",0,0);
  ctx.restore();
}
// ── Nova Star — large pulsing star; sweeps nearby normals on tap ──
function drawNovaStar(ctx,r,ts,dwellPct){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.008);
  const glow=`rgba(253,224,71,${0.5+0.5*dwellPct})`;
  ctx.shadowBlur=22*pulse+8*dwellPct;ctx.shadowColor=glow;
  const n=6,outerR=r*pulse,innerR=r*0.45*pulse;
  ctx.fillStyle=dwellPct>0.7?"#fef08a":"#fbbf24";
  ctx.beginPath();
  for(let i=0;i<n*2;i++){
    const a=i*Math.PI/n+ts*0.002;
    const rad=i%2===0?outerR:innerR;
    i===0?ctx.moveTo(Math.cos(a)*rad,Math.sin(a)*rad):ctx.lineTo(Math.cos(a)*rad,Math.sin(a)*rad);
  }
  ctx.closePath();ctx.fill();
  // Sweep ring when almost ready
  if(dwellPct>0.6){
    ctx.strokeStyle=`rgba(254,240,138,${(dwellPct-0.6)/0.4})`;ctx.lineWidth=3;
    ctx.beginPath();ctx.arc(0,0,r*1.4,0,Math.PI*2);ctx.stroke();
  }
  ctx.font=`${Math.round(r*0.8)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText("🌟",0,0);
  ctx.restore();
}
// ── Ice Phoenix — hatch mechanic: tap before 2.5s for freeze+bonus ──
function drawIcePhoenix(ctx,r,ts,hatchPct){
  ctx.save();
  const isHatched=hatchPct>=1;
  ctx.shadowBlur=18;ctx.shadowColor=isHatched?"#60a5fa":"#a5f3fc";
  const grad=ctx.createRadialGradient(-r*0.1,-r*0.2,0,0,0,r);
  grad.addColorStop(0,"rgba(255,255,255,0.95)");
  grad.addColorStop(0.4,isHatched?"rgba(96,165,250,0.85)":"rgba(165,243,252,0.85)");
  grad.addColorStop(1,isHatched?"rgba(37,99,235,0.65)":"rgba(6,182,212,0.65)");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Hatch ring
  ctx.strokeStyle=isHatched?"#fbbf24":"#a5f3fc";ctx.lineWidth=2.5;
  ctx.beginPath();ctx.arc(0,0,r*1.15,0,Math.PI*2*Math.min(1,hatchPct));ctx.stroke();
  ctx.font=`${Math.round(r*0.8)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText(isHatched?"🐦":"🧊",0,0);
  ctx.restore();
}
// ── Spiral Orb — spiraling away from center; bonus based on travel ──
function drawSpiralOrb(ctx,r,ts,spiralPct){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#34d399";
  const pulse=0.9+0.1*Math.sin(ts*0.009);
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"rgba(255,255,255,0.95)");
  grad.addColorStop(0.4,"rgba(52,211,153,0.88)");
  grad.addColorStop(1,"rgba(5,150,105,0.65)");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Spiral trail ring
  ctx.strokeStyle=`rgba(167,243,208,${0.3+spiralPct*0.5})`;ctx.lineWidth=2;
  ctx.beginPath();ctx.arc(0,0,r*1.3,0,Math.PI*2*spiralPct);ctx.stroke();
  ctx.font=`${Math.round(r*0.78)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.88;ctx.fillText("🌀",0,0);
  ctx.restore();
}
// ── Dark Pulse — anti-target: tap to pay -20pts, get 2 bonus normals ──
function drawDarkPulse(ctx,r,ts){
  ctx.save();
  const pulse=0.88+0.12*Math.sin(ts*0.012);
  ctx.shadowBlur=20*pulse;ctx.shadowColor="#581c87";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"rgba(88,28,135,0.95)");
  grad.addColorStop(0.5,"rgba(67,20,99,0.85)");
  grad.addColorStop(1,"rgba(15,0,30,0.9)");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Rotating dark ring
  ctx.strokeStyle="rgba(167,139,250,0.7)";ctx.lineWidth=2.5;
  ctx.beginPath();ctx.arc(0,0,r*1.2,ts*0.003,ts*0.003+Math.PI*1.5);ctx.stroke();
  ctx.strokeStyle="rgba(91,33,182,0.5)";ctx.lineWidth=1.5;
  ctx.beginPath();ctx.arc(0,0,r*1.5,-ts*0.002,-ts*0.002+Math.PI*1.2);ctx.stroke();
  ctx.font=`${Math.round(r*0.78)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText("🌑",0,0);
  ctx.restore();
}
// ── Lunar Eclipse — grows then shrinks; max size = legendary pts ──
function drawLunarEclipse(ctx,r,ts,sizePct){
  ctx.save();
  const col=sizePct>0.8?"#fbbf24":sizePct>0.5?"#94a3b8":"#475569";
  ctx.shadowBlur=16+8*sizePct;ctx.shadowColor=col;
  ctx.fillStyle=col;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Moon crescent overlay
  ctx.fillStyle="#1e293b";ctx.globalAlpha=0.85;
  ctx.beginPath();ctx.arc(r*0.25,0,r*0.8,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;
  // Glow ring
  ctx.strokeStyle=`rgba(251,191,36,${sizePct*0.8})`;ctx.lineWidth=2.5;
  ctx.beginPath();ctx.arc(0,0,r*1.15,0,Math.PI*2);ctx.stroke();
  ctx.font=`${Math.round(r*0.72)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(sizePct>0.8?"🌕":"🌑",0,0);
  ctx.restore();
}
// ── Chain Wall — tap within 1.5s to preserve streak; else resets ──
function drawChainWall(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#92400e";
  // Brick pattern fill
  ctx.fillStyle="#78350f";ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Grid lines for "brick wall" look
  ctx.strokeStyle="rgba(161,98,7,0.7)";ctx.lineWidth=1.5;
  for(let i=-1;i<=1;i++){
    ctx.beginPath();ctx.moveTo(-r,i*r*0.4);ctx.lineTo(r,i*r*0.4);ctx.stroke();
    ctx.beginPath();ctx.moveTo(i*r*0.5,-r);ctx.lineTo(i*r*0.5+r*0.25,r);ctx.stroke();
  }
  ctx.font=`${Math.round(r*0.78)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText("🧱",0,0);
  ctx.restore();
}
// ── Prismatic Orb — shifts through hue cycle; bonus if tapped on target hue ──
function drawPrismaticOrb(ctx,r,ts,targetHue){
  ctx.save();
  const hue=Math.floor(ts*0.08)%360;
  ctx.shadowBlur=18;ctx.shadowColor=`hsl(${hue},100%,65%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,`hsl(${hue},100%,70%)`);
  g.addColorStop(1,`hsl(${(hue+180)%360},100%,35%)`);
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Target hue indicator arc
  ctx.strokeStyle=`hsl(${targetHue},100%,75%)`;ctx.lineWidth=3;ctx.globalAlpha=0.9;
  ctx.shadowBlur=10;ctx.shadowColor=`hsl(${targetHue},100%,75%)`;
  ctx.beginPath();ctx.arc(0,0,r*0.75,-Math.PI*0.3,Math.PI*0.3);ctx.stroke();
  // Highlight ring
  ctx.strokeStyle=`hsl(${hue},80%,90%)`;ctx.lineWidth=1.5;ctx.globalAlpha=0.5;
  ctx.beginPath();ctx.arc(0,0,r*0.9,0,Math.PI*2);ctx.stroke();
  ctx.font=`${Math.round(r*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText("🔮",0,1);
  ctx.restore();
}
// ── Void Tether — two linked orbs; tap one pulls the other to same spot ──
function drawVoidTether(ctx,r,ts,isLinked){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#6d28d9";
  const pulse=0.93+0.07*Math.sin(ts*0.013);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#1e1b4b");g.addColorStop(0.5,"#4c1d95");g.addColorStop(1,"rgba(109,40,217,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Tether indicator ring (dashed if linked)
  ctx.strokeStyle="#a78bfa";ctx.lineWidth=2;ctx.setLineDash(isLinked?[4,4]:[]);
  ctx.globalAlpha=0.8;ctx.beginPath();ctx.arc(0,0,r*0.85,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);
  ctx.font=`${Math.round(r*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText("🌑",0,1);
  ctx.restore();
}
// ── Crystal Web — 3 linked nodes; tap all 3 within 1.5s for jackpot ──
function drawCrystalWeb(ctx,r,ts,isNode,chainPct){
  ctx.save();
  const pulse=0.93+0.07*Math.sin(ts*0.015);
  ctx.shadowBlur=14;ctx.shadowColor=isNode?"#67e8f9":"#06b6d4";
  const col=isNode?"#22d3ee":"#06b6d4";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#ecfeff");g.addColorStop(0.6,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  if(chainPct>0){
    ctx.strokeStyle="#fbbf24";ctx.lineWidth=2;ctx.globalAlpha=chainPct;
    ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2*chainPct);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isNode?"🔷":"💎",0,1);
  ctx.restore();
}
// ── Arc Pulse — rotating arc; tap when arc faces sweetspot for 3× pts ──
function drawArcPulse(ctx,r,ts,arcAngle,inZone){
  ctx.save();
  ctx.shadowBlur=inZone?28:14;ctx.shadowColor=inZone?"#fbbf24":"#38bdf8";
  ctx.strokeStyle=inZone?"#fbbf24":"#0ea5e9";ctx.lineWidth=1.5;ctx.globalAlpha=0.35;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();
  const arcSpan=Math.PI*0.7;
  ctx.strokeStyle=inZone?"#fde68a":"#38bdf8";ctx.lineWidth=5;ctx.globalAlpha=0.9;ctx.lineCap="round";
  ctx.beginPath();ctx.arc(0,0,r,arcAngle,arcAngle+arcSpan);ctx.stroke();
  ctx.lineCap="butt";
  if(inZone){ctx.strokeStyle="#fbbf24";ctx.lineWidth=2;ctx.globalAlpha=0.5;ctx.setLineDash([3,3]);
    ctx.beginPath();ctx.arc(0,0,r*1.25,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);}
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(inZone?"⚡":"💠",0,1);
  ctx.restore();
}
// ── Void Bloom — core blooms into 4 petals; tap all for jackpot ──
function drawVoidBloom(ctx,r,ts,isPetal,petalPct){
  ctx.save();
  if(isPetal){
    const pulse=0.92+0.08*Math.sin(ts*0.018);
    ctx.shadowBlur=12;ctx.shadowColor="#a855f7";
    const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
    g.addColorStop(0,"#fae8ff");g.addColorStop(0.6,"#a855f7");g.addColorStop(1,"rgba(0,0,0,0)");
    ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
    ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.8)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText("🌸",0,1);
  } else {
    const pulse=0.93+0.07*Math.sin(ts*0.012);
    ctx.shadowBlur=22;ctx.shadowColor="#7c3aed";
    const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
    g.addColorStop(0,"#fff");g.addColorStop(0.4,"#c084fc");g.addColorStop(0.8,"#6d28d9");g.addColorStop(1,"rgba(0,0,0,0)");
    ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
    if(petalPct>0){
      ctx.strokeStyle="#fbbf24";ctx.lineWidth=2.5;ctx.globalAlpha=petalPct;
      ctx.beginPath();ctx.arc(0,0,r*1.25,0,Math.PI*2*petalPct);ctx.stroke();
    }
    ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText("🌀",0,1);
  }
  ctx.restore();
}
// ── Stellar Orb — expands slowly over lifetime; max size = legendary pts ──
function drawStellarOrb(ctx,r,ts,sizePct){
  ctx.save();
  const isMax=sizePct>0.85;
  ctx.shadowBlur=isMax?35:18;ctx.shadowColor=isMax?"#ffd700":"#fbbf24";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,isMax?"#ffd700":"#fbbf24");g.addColorStop(0.7,isMax?"#f97316":"#a16207");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Orbit ring
  const spin=ts*0.006;
  ctx.strokeStyle=isMax?"#fde68a":"rgba(251,191,36,0.5)";ctx.lineWidth=isMax?2.5:1.5;ctx.globalAlpha=0.7;ctx.setLineDash([5,5]);ctx.lineDashOffset=spin*20;
  ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);
  if(isMax){
    ctx.strokeStyle="#fff";ctx.lineWidth=1;ctx.globalAlpha=0.4;
    ctx.beginPath();ctx.arc(0,0,r*1.4,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isMax?"🌟":"⭐",0,1);
  ctx.restore();
}
// ── Rune Gate — two paired runes; tap A then B within 2s for jackpot ──
function drawRuneGate(ctx,r,ts,isSecond,primed){
  ctx.save();
  const pulse=0.93+0.07*Math.sin(ts*0.016);
  const col=isSecond?"#f472b6":"#818cf8";
  ctx.shadowBlur=primed?28:14;ctx.shadowColor=primed?"#fbbf24":col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Spinning rune marks
  const spin57=ts*0.009*(isSecond?-1:1);
  for(let i=0;i<4;i++){
    ctx.save();ctx.rotate(spin57+(i/4)*Math.PI*2);
    ctx.fillStyle=primed?"#fde68a":col;ctx.globalAlpha=0.6;
    ctx.font=`${Math.round(r*0.28)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText(["ᚠ","ᚢ","ᚦ","ᚱ"][i],r*0.65,0);
    ctx.restore();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isSecond?"🔮":"✨",0,1);
  ctx.restore();
}
// ── Nova Crystal — 3 charge stages; full charge = explosive bonus ──
function drawNovaCrystal(ctx,r,ts,stage){
  ctx.save();
  const colors=["#60a5fa","#a78bfa","#fbbf24"];
  const col=colors[Math.min(2,stage)];
  const pulse=0.92+0.08*Math.sin(ts*0.016*(1+stage*0.3));
  ctx.shadowBlur=10+stage*12;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Stage indicator facets
  for(let i=0;i<3;i++){
    ctx.strokeStyle=i<=stage?"#fff":"rgba(255,255,255,0.2)";ctx.lineWidth=i<=stage?2:1;ctx.globalAlpha=i<=stage?0.7:0.2;
    ctx.beginPath();ctx.arc(0,0,r*(0.5+i*0.18),0,Math.PI*2*((i+1)/3));ctx.stroke();
  }
  if(stage>=2){
    ctx.strokeStyle="#ffd700";ctx.lineWidth=2;ctx.globalAlpha=0.6;ctx.setLineDash([4,4]);
    ctx.beginPath();ctx.arc(0,0,r*1.3,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(["💙","💜","💎"][stage],0,1);
  ctx.restore();
}
// ── Phase Shift — cycles solid/ghost every 1.2s; only tappable when solid ──
function drawPhaseShift(ctx,r,ts,solidPct,isSolid){
  ctx.save();
  ctx.globalAlpha=isSolid?0.9:(0.15+0.1*Math.sin(ts*0.02));
  ctx.shadowBlur=isSolid?20:5;ctx.shadowColor=isSolid?"#f0abfc":"#818cf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,isSolid?"#fae8ff":"#e0e7ff");g.addColorStop(0.5,isSolid?"#e879f9":"#818cf8");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  if(isSolid){
    ctx.strokeStyle="#f0abfc";ctx.lineWidth=2;ctx.globalAlpha=solidPct;
    ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2*solidPct);ctx.stroke();
  }
  ctx.globalAlpha=isSolid?0.9:0.4;
  ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isSolid?"👁️":"👻",0,1);
  ctx.restore();
}
// ── Solar Ring — expanding ring; tap near ring edge for max pts ──
function drawSolarRing(ctx,r,ts,ringPct){
  ctx.save();
  const ringR=r*ringPct*2.5;
  const glow59=ringPct>0.7?1:ringPct/0.7;
  ctx.strokeStyle=`rgba(251,191,36,${0.5+glow59*0.4})`;ctx.lineWidth=ringPct>0.7?5:3;
  ctx.shadowBlur=ringPct>0.7?28:12;ctx.shadowColor="#fbbf24";
  ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
  // Center core
  ctx.globalAlpha=0.8;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.45);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,"#fbbf24");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.45,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("☀️",0,1);
  ctx.restore();
}
// ── Chrono Vortex — slows nearby targets; tap = big time-freeze bonus ──
function drawChronoVortex(ctx,r,ts,slowedCount){
  ctx.save();
  const spin=ts*0.005;
  ctx.shadowBlur=18+slowedCount*3;ctx.shadowColor="#38bdf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,"#38bdf8");g.addColorStop(0.7,"#0369a1");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Clockwork rings
  for(let ri=1;ri<=3;ri++){
    ctx.strokeStyle=`rgba(56,189,248,${0.5-ri*0.1})`;ctx.lineWidth=1.5;ctx.setLineDash([4+ri*2,4]);
    ctx.lineDashOffset=spin*40*(ri%2===0?1:-1);
    ctx.beginPath();ctx.arc(0,0,r*(0.45+ri*0.2),0,Math.PI*2);ctx.stroke();
  }
  ctx.setLineDash([]);
  if(slowedCount>0){
    ctx.fillStyle="#fbbf24";ctx.globalAlpha=0.9;ctx.font=`bold ${Math.round(r*0.35)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText(`⏱×${slowedCount}`,0,r*0.55);
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⏳",0,-r*0.15);
  ctx.restore();
}
// ── Prism Nova — rainbow burst target that splits into 3 colored shards ──
function drawPrismNova(ctx,r,ts,stage){
  ctx.save();
  const pulse=0.92+0.08*Math.sin(ts*0.012);
  const hue=(ts*0.15)%360;
  ctx.shadowBlur=22+stage*6;ctx.shadowColor=`hsl(${hue},100%,60%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");
  g.addColorStop(0.3,`hsl(${hue},100%,65%)`);
  g.addColorStop(0.7,`hsl(${(hue+120)%360},100%,55%)`);
  g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Rainbow spin rings
  for(let i=0;i<3;i++){
    const angle=(ts*0.008+i*Math.PI*2/3);
    ctx.strokeStyle=`hsl(${(hue+i*120)%360},100%,70%)`;ctx.lineWidth=2;ctx.globalAlpha=0.5;
    ctx.beginPath();ctx.arc(0,0,r*(0.65+i*0.12),angle,angle+Math.PI*1.2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.62)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌈",0,0);
  ctx.restore();
}
// ── Prism Nova Shard — colored diamond child shard from Prism Nova split ──
function drawPrismNovaShard(ctx,r,ts,shardHue){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.018);
  ctx.shadowBlur=14;ctx.shadowColor=`hsl(${shardHue},100%,60%)`;
  ctx.fillStyle=`hsl(${shardHue},100%,60%)`;ctx.globalAlpha=0.88;
  ctx.beginPath();ctx.moveTo(0,-r*pulse);ctx.lineTo(r*0.55*pulse,0);ctx.lineTo(0,r*pulse);ctx.lineTo(-r*0.55*pulse,0);ctx.closePath();ctx.fill();
  ctx.globalAlpha=0.6;ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💎",0,0);
  ctx.restore();
}
// ── Echo Blast — tap creates a scoring ring that lingers on canvas ──
function drawEchoBlast(ctx,r,ts,charged){
  ctx.save();
  const pulse=0.92+0.08*Math.sin(ts*0.01);
  const col=charged?"#f59e0b":"#8b5cf6";
  ctx.shadowBlur=20;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.35,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Ripple lines
  for(let i=1;i<=3;i++){
    ctx.strokeStyle=col;ctx.lineWidth=1.5;ctx.globalAlpha=0.25+i*0.1;
    ctx.beginPath();ctx.arc(0,0,r*(0.5+i*0.18),0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🔊",0,0);
  ctx.restore();
}
// ── Vortex Star 61 — spinning star with 3 concentric score zones ──
function drawVortexStar61(ctx,r,ts,zone){
  ctx.save();
  const spin=ts*0.006;
  const pulse=0.94+0.06*Math.sin(ts*0.013);
  const zoneColors=["#ef4444","#f59e0b","#4ade80"]; // inner/mid/outer
  ctx.shadowBlur=18+zone*4;ctx.shadowColor=zoneColors[zone]||"#fbbf24";
  // Outer zone ring
  ctx.strokeStyle="#4ade80";ctx.lineWidth=2.5;ctx.globalAlpha=0.35;
  ctx.beginPath();ctx.arc(0,0,r*2,0,Math.PI*2);ctx.stroke();
  // Mid zone ring
  ctx.strokeStyle="#f59e0b";ctx.lineWidth=2;ctx.globalAlpha=0.4;
  ctx.beginPath();ctx.arc(0,0,r*1.4,0,Math.PI*2);ctx.stroke();
  // Core gradient
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#fbbf24");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Spinning star points
  ctx.globalAlpha=0.85;ctx.fillStyle="#fde68a";
  ctx.beginPath();
  for(let i=0;i<5;i++){
    const a=spin+i*(Math.PI*2/5)-Math.PI/2;
    const b=spin+(i+0.5)*(Math.PI*2/5)-Math.PI/2;
    i===0?ctx.moveTo(Math.cos(a)*r,Math.sin(a)*r):ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);
    ctx.lineTo(Math.cos(b)*r*0.4,Math.sin(b)*r*0.4);
  }
  ctx.closePath();ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.52)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⭐",0,0);
  ctx.restore();
}
// ── Quantum Drift — drifts randomly; tap freezes for 2s bonus window ──
function drawQuantumDrift(ctx,r,ts,frozen){
  ctx.save();
  const col=frozen?"#38bdf8":"#a78bfa";
  const pulse=0.9+0.1*Math.sin(ts*(frozen?0.008:0.02));
  ctx.shadowBlur=frozen?24:16;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  if(frozen){
    // Ice crystal overlay
    for(let i=0;i<6;i++){
      const a=i*Math.PI/3;
      ctx.strokeStyle="#e0f2fe";ctx.lineWidth=1.5;ctx.globalAlpha=0.5;
      ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*0.8,Math.sin(a)*r*0.8);ctx.stroke();
    }
    ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText("❄️",0,0);
  } else {
    ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.58)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText("⚛️",0,0);
  }
  ctx.restore();
}
// ── Plasma Burst — explosive target releasing 4 directional bolts ──
function drawPlasmaBurst(ctx,r,ts,charged){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.014);
  const col=charged?"#ef4444":"#f97316";
  ctx.shadowBlur=22;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.35,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Bolt charge lines
  for(let i=0;i<4;i++){
    const a=i*Math.PI/2;const len=r*(0.7+0.3*Math.sin(ts*0.015+i));
    ctx.strokeStyle=col;ctx.lineWidth=2;ctx.globalAlpha=0.5;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*len,Math.sin(a)*len);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💥",0,0);
  ctx.restore();
}
// ── Star Clock — 5-second countdown; tap at 0 = jackpot ──
function drawStarClock(ctx,r,ts,countdown){
  ctx.save();
  const frac=countdown/5;
  const col=countdown<=1?"#ef4444":countdown<=2?"#f59e0b":"#38bdf8";
  ctx.shadowBlur=18+Math.max(0,2-countdown)*8;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Countdown arc
  ctx.strokeStyle=col;ctx.lineWidth=3;ctx.globalAlpha=0.7;
  ctx.beginPath();ctx.arc(0,0,r*0.85,-Math.PI/2,-Math.PI/2+Math.PI*2*frac);ctx.stroke();
  // Countdown number
  ctx.globalAlpha=1;ctx.fillStyle=countdown<=1?"#ef4444":"#fff";
  ctx.font=`bold ${Math.round(r*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(countdown<=0?"0!":countdown.toString(),0,0);
  ctx.restore();
}
// ── Nebula Clock — orbiting countdown dots; collect all = jackpot ──
function drawNebulaClock(ctx,r,ts,dotCount){
  ctx.save();
  const pulse=0.93+0.07*Math.sin(ts*0.011);
  ctx.shadowBlur=18;ctx.shadowColor="#818cf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#818cf8");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.58)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌌",0,0);
  // Remaining dot count badge
  if(dotCount>0){
    ctx.fillStyle="#fbbf24";ctx.globalAlpha=0.9;ctx.font=`bold ${Math.round(r*0.38)}px sans-serif`;
    ctx.fillText(`×${dotCount}`,r*0.5,-r*0.55);
  }
  ctx.restore();
}
// ── Nebula Dot — one of 5 orbiting dots from Nebula Clock ──
function drawNebulaDot(ctx,r,ts){
  ctx.save();
  const hue=(ts*0.2)%360;
  ctx.shadowBlur=12;ctx.shadowColor=`hsl(${hue},100%,65%)`;
  ctx.fillStyle=`hsl(${hue},100%,65%)`;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Ice Shatter — frozen target needs 3 hits to shatter ──
function drawIceShatter(ctx,r,ts,hitsLeft){
  ctx.save();
  const crackPct=1-(hitsLeft/3);
  ctx.shadowBlur=16+crackPct*8;ctx.shadowColor="#67e8f9";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#67e8f9");g.addColorStop(0.8,"#0891b2");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Crack lines by hit count
  ctx.strokeStyle="#0e7490";ctx.globalAlpha=crackPct*0.8;
  for(let ci=0;ci<Math.round(crackPct*6);ci++){
    const a=ci*Math.PI/3+ts*0.001;
    ctx.lineWidth=1+crackPct;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.56)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(hitsLeft<=0?"💥":"🧊",0,0);
  if(hitsLeft>0){
    ctx.fillStyle="#fbbf24";ctx.globalAlpha=0.9;ctx.font=`bold ${Math.round(r*0.35)}px sans-serif`;
    ctx.fillText(`${hitsLeft}`,r*0.52,-r*0.52);
  }
  ctx.restore();
}
// ── Hue Orb — color-cycling orb; match world color for bonus ──
function drawHueOrb(ctx,r,ts,hue,matched){
  ctx.save();
  const pulse=0.92+0.08*Math.sin(ts*0.012);
  ctx.shadowBlur=matched?28:16;ctx.shadowColor=`hsl(${hue},100%,60%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.35,`hsl(${hue},100%,60%)`);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  if(matched){
    ctx.strokeStyle="#fbbf24";ctx.lineWidth=2.5;ctx.globalAlpha=0.6;
    ctx.beginPath();ctx.arc(0,0,r*1.25,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(matched?"✨":"🌑",0,0);
  ctx.restore();
}
// ── Lightning Chain 64 — tap triggers chain to up to 5 nearest targets ──
function drawLightningChain64(ctx,r,ts,chainCount){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.015);
  ctx.shadowBlur=20+chainCount*3;ctx.shadowColor="#facc15";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#facc15");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Electric arc lines
  for(let li=0;li<5;li++){
    const a=(ts*0.01+li*Math.PI*0.4)%(Math.PI*2);
    ctx.strokeStyle="#fef9c3";ctx.lineWidth=1;ctx.globalAlpha=0.3;
    ctx.beginPath();ctx.moveTo(0,0);
    ctx.lineTo(Math.cos(a)*r*0.7+Math.random()*6-3,Math.sin(a)*r*0.7+Math.random()*6-3);
    ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,0);
  if(chainCount>0){
    ctx.fillStyle="#fbbf24";ctx.globalAlpha=0.9;ctx.font=`bold ${Math.round(r*0.35)}px sans-serif`;
    ctx.fillText(`×${chainCount}`,r*0.52,-r*0.52);
  }
  ctx.restore();
}
// ── Arcane Rune — symbol target; tap matching follow-up symbol for jackpot ──
function drawArcaneRune(ctx,r,ts,symbol,primed){
  ctx.save();
  const pulse=0.92+0.08*Math.sin(ts*0.013);
  const col=primed?"#fbbf24":"#a78bfa";
  ctx.shadowBlur=primed?28:18;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Rune border
  ctx.strokeStyle=col;ctx.lineWidth=2.5;ctx.globalAlpha=0.5;
  const spin=ts*0.004;
  ctx.save();ctx.rotate(spin);
  ctx.beginPath();
  for(let i=0;i<6;i++){const a=i*Math.PI/3;ctx.lineTo(Math.cos(a)*r*0.85,Math.sin(a)*r*0.85);}
  ctx.closePath();ctx.stroke();
  ctx.restore();
  const symbols65=["🔥","💧","🌿","⚡"];
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.62)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(symbols65[symbol%4]||"🔮",0,0);
  if(primed){
    ctx.fillStyle="#fbbf24";ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.3)}px sans-serif`;
    ctx.fillText("TAP!",0,r*0.6);
  }
  ctx.restore();
}
// ── Pulse Gem — pulsing gem; tap at peak = 4× score ──
function drawPulseGem(ctx,r,ts,pulsePct){
  ctx.save();
  const scale65=0.7+0.3*pulsePct;
  const isPeak65=pulsePct>0.85;
  const col=isPeak65?"#fbbf24":"#f472b6";
  ctx.shadowBlur=isPeak65?32:18;ctx.shadowColor=col;
  ctx.scale(scale65,scale65);
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.35,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  // Diamond shape
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(r*0.6,0);ctx.lineTo(0,r);ctx.lineTo(-r*0.6,0);ctx.closePath();ctx.fill();
  ctx.scale(1/scale65,1/scale65);
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.58)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isPeak65?"✨":"💎",0,0);
  if(isPeak65){
    ctx.fillStyle="#fbbf24";ctx.globalAlpha=0.9;ctx.font=`bold ${Math.round(r*0.32)}px sans-serif`;
    ctx.fillText("×4!",r*0.5,-r*0.55);
  }
  ctx.restore();
}
// ── Cosmic Web — spider web with 6 nodes; tap all = jackpot on center ──
function drawCosmicWeb(ctx,r,ts,nodesLeft,isCenter){
  ctx.save();
  const spin=ts*0.004;
  ctx.shadowBlur=isCenter?(20+nodesLeft*2):14;ctx.shadowColor=isCenter?"#fbbf24":"#06b6d4";
  if(isCenter){
    // Center hub
    const g=ctx.createRadialGradient(0,0,0,0,0,r);
    g.addColorStop(0,"#fff");g.addColorStop(0.4,nodesLeft===0?"#fbbf24":"#06b6d4");g.addColorStop(1,"rgba(0,0,0,0)");
    ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
    ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText(nodesLeft===0?"✨":"🕸️",0,0);
    if(nodesLeft>0){
      ctx.fillStyle="#fbbf24";ctx.globalAlpha=0.9;ctx.font=`bold ${Math.round(r*0.35)}px sans-serif`;
      ctx.fillText(`${nodesLeft}`,r*0.52,-r*0.52);
    }
  } else {
    // Web node
    ctx.strokeStyle="#06b6d4";ctx.lineWidth=1.5;ctx.globalAlpha=0.3;
    for(let i=0;i<3;i++){const a=spin+i*Math.PI/3;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*1.5,Math.sin(a)*r*1.5);ctx.stroke();}
    ctx.fillStyle="#38bdf8";ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
    ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🔵",0,0);
  }
  ctx.restore();
}
// ── Warp Orb — teleports every 1.5s; tap mid-warp = crit ──
function drawWarpOrb(ctx,r,ts,isWarping){
  ctx.save();
  const pulse=0.85+0.15*Math.sin(ts*0.016);
  const col=isWarping?"#fbbf24":"#6366f1";
  ctx.shadowBlur=isWarping?30:18;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=isWarping?0.95:0.88;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  if(isWarping){
    ctx.strokeStyle="#fbbf24";ctx.lineWidth=3;ctx.globalAlpha=0.5;
    ctx.beginPath();ctx.arc(0,0,r*1.35,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isWarping?"💫":"🌀",0,0);
  ctx.restore();
}
// ── Dark Mirror — semi-transparent; opacity cycles; tap at peak = bonus ──
function drawDarkMirror(ctx,r,ts,opacityPct){
  ctx.save();
  const isPeak67=opacityPct>0.85;
  const alpha67=0.2+opacityPct*0.75;
  ctx.shadowBlur=isPeak67?24:12;ctx.shadowColor=isPeak67?"#a78bfa":"#4c1d95";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"rgba(255,255,255,0.9)");g.addColorStop(0.35,"rgba(167,139,250,0.8)");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=alpha67;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Mirror glare
  if(isPeak67){
    ctx.strokeStyle="#fbbf24";ctx.lineWidth=2;ctx.globalAlpha=0.4;
    ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=alpha67;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isPeak67?"🪞":"🖤",0,0);
  ctx.restore();
}
// ── Storm Ball — accelerating moving target; catch early = bonus ──
function drawStormBall(ctx,r,ts,speedPct){
  ctx.save();
  const col=speedPct>0.8?"#ef4444":speedPct>0.5?"#f97316":"#fbbf24";
  const pulse=0.9+0.1*Math.sin(ts*(0.01+speedPct*0.02));
  ctx.shadowBlur=14+speedPct*12;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Speed lines
  if(speedPct>0.3){
    ctx.strokeStyle=col;ctx.lineWidth=1.5;ctx.globalAlpha=0.3*speedPct;
    for(let si=0;si<4;si++){
      const a=si*Math.PI/2+ts*0.02;
      ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.5,Math.sin(a)*r*0.5);ctx.lineTo(Math.cos(a)*r*1.3,Math.sin(a)*r*1.3);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,0);
  if(speedPct>0.5){
    ctx.fillStyle=col;ctx.globalAlpha=0.9;ctx.font=`bold ${Math.round(r*0.32)}px sans-serif`;
    ctx.fillText(`${Math.round(speedPct*100)}%`,r*0.5,-r*0.52);
  }
  ctx.restore();
}
// ── Chain Nova — explodes in a ring scoring nearby targets ──
function drawChainNova(ctx,r,ts,primed){
  ctx.save();
  const hue68=(ts*0.2)%360;
  const col=`hsl(${hue68},90%,60%)`;
  const pulse=0.9+0.1*Math.sin(ts*0.015);
  ctx.shadowBlur=18;ctx.shadowColor=col;
  // Core
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.95;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Orbiting ring sparks
  ctx.globalAlpha=0.7;ctx.fillStyle=col;
  for(let oi=0;oi<6;oi++){
    const a=oi*Math.PI/3+ts*0.008;
    const rx=Math.cos(a)*r*1.25;const ry=Math.sin(a)*r*1.25;
    ctx.beginPath();ctx.arc(rx,ry,2.5,0,Math.PI*2);ctx.fill();
  }
  if(primed){
    ctx.strokeStyle="#fff";ctx.lineWidth=2;ctx.globalAlpha=0.5+0.4*Math.sin(ts*0.025);
    ctx.beginPath();ctx.arc(0,0,r*1.5,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.95;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💥",0,0);
  ctx.restore();
}
// ── Mirror Flash — flashes between real and decoy; tap real = big score ──
function drawMirrorFlash(ctx,r,ts,isReal,opacity){
  ctx.save();
  ctx.globalAlpha=opacity;
  const col=isReal?"#f0abfc":"#6366f1";
  const pulse=0.92+0.08*Math.sin(ts*(isReal?0.012:0.009));
  ctx.shadowBlur=16;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Mirror glint
  if(isReal){
    ctx.strokeStyle="#fff";ctx.lineWidth=1.5;ctx.globalAlpha=opacity*0.6;
    ctx.beginPath();ctx.moveTo(-r*0.3,-r*0.5);ctx.lineTo(r*0.3,r*0.5);ctx.stroke();
  }
  ctx.globalAlpha=opacity;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isReal?"🪞":"👁️",0,0);
  ctx.restore();
}
// ── Prism Clock — 3 rotating color segments; tap matching world color = 3× ──
function drawPrismClock(ctx,r,ts,segment,matched){
  ctx.save();
  const hues=[0,120,240]; // red, green, blue segments
  const rot=ts*0.004;
  for(let si=0;si<3;si++){
    const startA=rot+si*(Math.PI*2/3);const endA=startA+Math.PI*2/3-0.06;
    ctx.beginPath();ctx.moveTo(0,0);ctx.arc(0,0,r,startA,endA);ctx.closePath();
    const isMatch=si===segment;
    ctx.fillStyle=isMatch&&matched?`hsl(${hues[si]},100%,70%)`:`hsl(${hues[si]},70%,50%)`;
    ctx.globalAlpha=isMatch&&matched?0.95:0.7;
    ctx.fill();
    if(isMatch){ctx.strokeStyle="#fff";ctx.lineWidth=2;ctx.globalAlpha=0.6;ctx.stroke();}
  }
  // Center hub
  ctx.globalAlpha=1;ctx.shadowBlur=12;ctx.shadowColor="#fff";
  ctx.fillStyle="#fff";ctx.beginPath();ctx.arc(0,0,r*0.25,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.35)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🕐",0,0);
  if(matched){
    ctx.strokeStyle="#fff";ctx.lineWidth=2;ctx.globalAlpha=0.4+0.3*Math.sin(ts*0.02);
    ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2);ctx.stroke();
  }
  ctx.restore();
}
// ── Void Pulse — grows then shrinks in pulse cycle; tap at max = 3× ──
function drawVoidPulse(ctx,r,ts,scalePct){
  ctx.save();
  const scale=0.5+0.5*scalePct; // 0.5 to 1.0
  const col=scalePct>0.85?"#a855f7":scalePct>0.5?"#7c3aed":"#4c1d95";
  ctx.shadowBlur=8+scalePct*20;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*scale);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.7+0.25*scalePct;
  ctx.beginPath();ctx.arc(0,0,r*scale,0,Math.PI*2);ctx.fill();
  // Outer ring at peak
  if(scalePct>0.8){
    ctx.strokeStyle="#c4b5fd";ctx.lineWidth=2;ctx.globalAlpha=0.7*(scalePct-0.8)/0.2;
    ctx.beginPath();ctx.arc(0,0,r*scale*1.3,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*scale*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌑",0,0);
  ctx.restore();
}
// ── Arc Bloom — petals unfurl over 3s; tap when fully open = 3× ──
function drawArcBloom(ctx,r,ts,bloomPct){
  ctx.save();
  const petals=6;const rot=ts*0.002;
  // Petals grow with bloomPct
  const petalLen=r*(0.5+bloomPct*0.7);
  const petalW=r*0.3*Math.max(0.2,bloomPct);
  for(let pi=0;pi<petals;pi++){
    const a=rot+pi*(Math.PI*2/petals);
    const hue=pi*60;
    ctx.save();ctx.rotate(a);ctx.fillStyle=`hsl(${hue},80%,65%)`;
    ctx.globalAlpha=0.6+0.3*bloomPct;
    ctx.shadowBlur=8;ctx.shadowColor=`hsl(${hue},80%,65%)`;
    ctx.beginPath();ctx.ellipse(petalLen/2,0,petalLen/2,petalW/2,0,0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
  // Center
  ctx.globalAlpha=1;ctx.shadowBlur=12;ctx.shadowColor="#fde68a";
  ctx.fillStyle=bloomPct>0.9?"#fbbf24":"#f9a8d4";
  ctx.beginPath();ctx.arc(0,0,r*0.35,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.4)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌸",0,0);
  if(bloomPct>0.85){
    ctx.strokeStyle="#fbbf24";ctx.lineWidth=2;ctx.globalAlpha=0.5+0.3*Math.sin(ts*0.03);
    ctx.beginPath();ctx.arc(0,0,r*1.3,0,Math.PI*2);ctx.stroke();
  }
  ctx.restore();
}
// ── Neon Bolt — zips across screen; tap before exit = score ──
function drawNeonBolt(ctx,r,ts,speedPct){
  ctx.save();
  const hue70=(ts*0.5)%360;
  const col=`hsl(${hue70},100%,60%)`;
  ctx.shadowBlur=18;ctx.shadowColor=col;
  ctx.fillStyle=col;ctx.globalAlpha=0.95;
  // Elongated bolt shape
  ctx.save();ctx.rotate(-Math.PI/6);
  ctx.beginPath();ctx.ellipse(0,0,r*1.4,r*0.7,0,0,Math.PI*2);ctx.fill();
  ctx.restore();
  // Electric arcs
  ctx.strokeStyle="#fff";ctx.lineWidth=1.5;ctx.globalAlpha=0.7;
  for(let ai=0;ai<3;ai++){
    const startX=-r+ai*r*0.8;
    ctx.beginPath();ctx.moveTo(startX,-r*0.3+Math.random()*r*0.6);
    ctx.lineTo(startX+r*0.5,0);ctx.lineTo(startX+r*0.8,-r*0.3+Math.random()*r*0.6);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,0);
  ctx.restore();
}
// ── Crystal Burst — shatters into 4 shards on tap; each shard = child target ──
function drawCrystalBurst(ctx,r,ts){
  ctx.save();
  const rot=ts*0.006;
  ctx.shadowBlur=16;ctx.shadowColor="#67e8f9";
  // 4 crystal facets
  for(let fi=0;fi<4;fi++){
    const a=rot+fi*(Math.PI/2);
    const x1=Math.cos(a)*r*0.5;const y1=Math.sin(a)*r*0.5;
    const x2=Math.cos(a+Math.PI/4)*r*0.9;const y2=Math.sin(a+Math.PI/4)*r*0.9;
    const x3=Math.cos(a-Math.PI/4)*r*0.9;const y3=Math.sin(a-Math.PI/4)*r*0.9;
    ctx.fillStyle=`hsl(${fi*90+ts*0.1%360},80%,65%)`;ctx.globalAlpha=0.85;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(x1,y1);ctx.lineTo(x2,y2);ctx.lineTo(x3,y3);ctx.closePath();ctx.fill();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#fff";ctx.beginPath();ctx.arc(0,0,r*0.2,0,Math.PI*2);ctx.fill();
  ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("💎",0,0);
  ctx.restore();
}
// ── Crystal Burst Shard — small colored crystal shard child target ──
function drawCrystalShard71(ctx,r,ts,hue){
  ctx.save();
  ctx.shadowBlur=10;ctx.shadowColor=`hsl(${hue},80%,70%)`;
  ctx.fillStyle=`hsl(${hue},80%,65%)`;ctx.globalAlpha=0.9;
  ctx.save();ctx.rotate(ts*0.01);
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(r*0.6,r*0.5);ctx.lineTo(-r*0.6,r*0.5);ctx.closePath();ctx.fill();
  ctx.restore();
  ctx.restore();
}
// ── Time Rift — slows own countdown to half; tap in final 1s = jackpot ──
function drawTimeRift(ctx,r,ts,timeLeft,lifetime){
  ctx.save();
  const urgency=Math.max(0,1-timeLeft/1000); // 0 normal → 1 at final second
  const col=urgency>0.5?`hsl(${(1-urgency)*60},100%,55%)`:"#818cf8";
  const pulse=0.9+0.1*Math.sin(ts*(0.01+urgency*0.04));
  ctx.shadowBlur=12+urgency*18;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Clock arc
  const arcPct=Math.max(0,timeLeft/lifetime);
  ctx.strokeStyle="#fff";ctx.lineWidth=2.5;ctx.globalAlpha=0.7;
  ctx.beginPath();ctx.arc(0,0,r*0.85,-Math.PI/2,-Math.PI/2+arcPct*Math.PI*2);ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(urgency>0.5?"⏱️":"🌀",0,0);
  if(urgency>0.7){
    ctx.fillStyle="#fff";ctx.globalAlpha=0.8;ctx.font=`bold ${Math.round(r*0.32)}px sans-serif`;
    ctx.fillText(`${(timeLeft/1000).toFixed(1)}s`,r*0.5,-r*0.55);
  }
  ctx.restore();
}
// ── Glitch Orb — flickers visible/invisible randomly; tap while visible = 2× ──
function drawGlitchOrb(ctx,r,ts,visible){
  if(!visible)return;
  ctx.save();
  const glitchHue=(ts*0.8)%360;
  ctx.shadowBlur=14;ctx.shadowColor=`hsl(${glitchHue},100%,60%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,`hsl(${glitchHue},100%,60%)`);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Glitch scan lines
  ctx.globalAlpha=0.3;ctx.strokeStyle="#fff";ctx.lineWidth=1;
  for(let gi=0;gi<3;gi++){
    const gy=-r+gi*r*0.65;ctx.beginPath();ctx.moveTo(-r,gy);ctx.lineTo(r,gy);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("👾",0,0);
  ctx.restore();
}
// ── Solar Flare72 — brightens to peak then fades; tap at peak = 3× ──
function drawSolarFlare72(ctx,r,ts,brightPct){
  ctx.save();
  const col=`hsl(${45-brightPct*30},100%,${55+brightPct*30}%)`;
  const pulse=0.85+0.15*brightPct*Math.sin(ts*0.02);
  ctx.shadowBlur=10+brightPct*25;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.7+0.25*brightPct;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Rays at peak
  if(brightPct>0.7){
    ctx.strokeStyle=col;ctx.lineWidth=1.5;ctx.globalAlpha=0.6*brightPct;
    for(let ri=0;ri<8;ri++){
      const a=ri*Math.PI/4;const rayLen=r*0.6*brightPct;
      ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.85,Math.sin(a)*r*0.85);
      ctx.lineTo(Math.cos(a)*(r*0.85+rayLen),Math.sin(a)*(r*0.85+rayLen));ctx.stroke();
    }
  }
  ctx.globalAlpha=0.95;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("☀️",0,0);
  ctx.restore();
}
// ── Nexus Core — large easy target with wide hit zone ──
function drawNexusCore(ctx,r,ts){
  ctx.save();
  const pulse=0.95+0.05*Math.sin(ts*0.009);
  ctx.shadowBlur=20;ctx.shadowColor="#38bdf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#38bdf8");g.addColorStop(0.8,"#0369a1");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Extended hit zone indicator
  ctx.strokeStyle="#38bdf8";ctx.lineWidth=1.5;ctx.globalAlpha=0.25+0.1*Math.sin(ts*0.01);
  ctx.beginPath();ctx.arc(0,0,r*1.5,0,Math.PI*2);ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`bold ${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillStyle="#fff";ctx.fillText("💠",0,0);
  ctx.restore();
}
// ── Blink Star — teleports every 0.7s; tap just after blink = ×3 crit ──
function drawBlinkStar(ctx,r,ts,justBlinked){
  ctx.save();
  const col=justBlinked?"#fde68a":"#f472b6";
  ctx.shadowBlur=justBlinked?24:14;ctx.shadowColor=col;
  const pulse=0.9+0.1*Math.sin(ts*0.018);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=justBlinked?0.95:0.85;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  if(justBlinked){
    ctx.strokeStyle="#fde68a";ctx.lineWidth=2.5;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(0,0,r*1.3,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⭐",0,0);
  ctx.restore();
}
// ── Ether Orb — teal translucent orb that splits into 2 copies on tap ──
function drawEtherOrb(ctx,r,ts){
  ctx.save();
  const pulse=0.88+0.12*Math.sin(ts*0.012);
  ctx.shadowBlur=16;ctx.shadowColor="#2dd4bf";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"rgba(255,255,255,0.9)");g.addColorStop(0.4,"rgba(45,212,191,0.7)");g.addColorStop(0.85,"rgba(13,148,136,0.4)");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.82;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Translucent outer ring — signals "split" mechanic
  ctx.strokeStyle="#2dd4bf";ctx.lineWidth=2;ctx.globalAlpha=0.4+0.15*Math.sin(ts*0.015);
  ctx.beginPath();ctx.arc(0,0,r*1.25,0,Math.PI*2);ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌫️",0,0);
  ctx.restore();
}
// ── Ether Copy — small child spawned when Ether Orb is tapped ──
function drawEtherCopy(ctx,r,ts,hue){
  ctx.save();
  const col=`hsl(${hue||175},70%,65%)`;
  const pulse=0.85+0.15*Math.sin(ts*0.02+hue||0);
  ctx.shadowBlur=10;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.75;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.8;ctx.font=`${Math.round(r*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("✨",0,0);
  ctx.restore();
}
// ── Quantum Blast — pulses huge↔tiny; tap in tiny phase for 5× jackpot ──
function drawQuantumBlast(ctx,r,ts,scalePct){
  ctx.save();
  const isTiny=scalePct<0.2;
  const col=isTiny?"#fbbf24":"#c084fc";
  const displayR=r*(0.4+scalePct*0.6); // shrinks to 40% at tiny phase
  ctx.shadowBlur=isTiny?28:14;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,displayR);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=isTiny?0.98:0.85;ctx.beginPath();ctx.arc(0,0,displayR,0,Math.PI*2);ctx.fill();
  if(isTiny){
    // Gold ring hint
    ctx.strokeStyle="#fde68a";ctx.lineWidth=2.5;ctx.globalAlpha=0.8+0.2*Math.sin(ts*0.04);
    ctx.beginPath();ctx.arc(0,0,displayR*1.4,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`bold ${Math.round(displayR*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillStyle="#fff";ctx.fillText(isTiny?"⚛️":"💜",0,0);
  ctx.restore();
}
// ── Nova Drift — drifting nova; hit the bright center = 2× ──
function drawNovaDrift(ctx,r,ts,age){
  ctx.save();
  const pulse=0.88+0.12*Math.sin(ts*0.01);
  const driftR=r*(1+age*0.4); // expands slightly
  ctx.shadowBlur=20;ctx.shadowColor="#f43f5e";
  const g=ctx.createRadialGradient(0,0,0,0,0,driftR);
  g.addColorStop(0,"#fff");g.addColorStop(0.25,"#fb7185");g.addColorStop(0.6,"#f43f5e");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=Math.max(0.3,0.92-age*0.4);
  ctx.beginPath();ctx.arc(0,0,driftR,0,Math.PI*2);ctx.fill();
  // Center bright spot
  ctx.fillStyle="#fff";ctx.globalAlpha=0.85-age*0.3;ctx.shadowBlur=14;ctx.shadowColor="#fff";
  ctx.beginPath();ctx.arc(0,0,r*0.25*(1-age*0.6),0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.52)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💥",0,0);
  ctx.restore();
}
// ── Ice Dagger — sharp ice crystal pointing up; tap tip zone = 3× ──
function drawIceDagger(ctx,r,ts,tipAngle){
  ctx.save();
  ctx.rotate(tipAngle);
  ctx.shadowBlur=18;ctx.shadowColor="#93c5fd";
  // Blade gradient
  const gB=ctx.createLinearGradient(0,-r,0,r*0.6);
  gB.addColorStop(0,"#fff");gB.addColorStop(0.2,"#bfdbfe");gB.addColorStop(0.7,"#3b82f6");gB.addColorStop(1,"#1e3a5f");
  ctx.fillStyle=gB;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(r*0.35,r*0.4);ctx.lineTo(0,r*0.6);ctx.lineTo(-r*0.35,r*0.4);ctx.closePath();ctx.fill();
  // Ice facet lines
  ctx.strokeStyle="#e0f2fe";ctx.lineWidth=1;ctx.globalAlpha=0.5;
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(r*0.1,r*0.1);ctx.stroke();
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(-r*0.1,r*0.1);ctx.stroke();
  // Glowing tip indicator
  const tipPulse=0.7+0.3*Math.sin(ts*0.018);
  ctx.globalAlpha=tipPulse;ctx.shadowBlur=16;ctx.shadowColor="#fff";
  ctx.fillStyle="#fff";ctx.beginPath();ctx.arc(0,-r*0.82,r*0.13,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Shadow Flare — dark orb that flares bright cyclically; bright phase = 2× ──
function drawShadowFlare(ctx,r,ts,brightPct){
  ctx.save();
  const col=brightPct>0.75?"#fbbf24":"#1c1917";
  const glow=brightPct>0.75?"#fef3c7":"#44403c";
  ctx.shadowBlur=brightPct>0.75?28:12;ctx.shadowColor=glow;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  if(brightPct>0.75){
    g.addColorStop(0,"#fff");g.addColorStop(0.3,"#fef08a");g.addColorStop(0.7,"#f59e0b");g.addColorStop(1,"rgba(0,0,0,0)");
  } else {
    g.addColorStop(0,"#292524");g.addColorStop(0.5,"#1c1917");g.addColorStop(1,"rgba(0,0,0,0)");
  }
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Flare rays in bright phase
  if(brightPct>0.75){
    ctx.strokeStyle="#fde68a";ctx.lineWidth=1.5;ctx.globalAlpha=(brightPct-0.75)*4;
    for(let ray=0;ray<6;ray++){
      const angle=ray*Math.PI/3+ts*0.008;
      ctx.beginPath();ctx.moveTo(Math.cos(angle)*r*0.4,Math.sin(angle)*r*0.4);ctx.lineTo(Math.cos(angle)*r*1.1,Math.sin(angle)*r*1.1);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.52)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(brightPct>0.75?"☀️":"🌑",0,0);
  ctx.restore();
}
// ── Nebula Core — dense nebula center; dead center = 3× ──
function drawNebulaCore(ctx,r,ts){
  ctx.save();
  const hue=(ts*0.06)%360;
  ctx.shadowBlur=22;ctx.shadowColor=`hsl(${hue},80%,55%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.15,`hsl(${hue},90%,85%)`);g.addColorStop(0.5,`hsl(${hue+30},70%,50%)`);g.addColorStop(0.85,`hsl(${hue+60},60%,25%)`);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Nebula wisps
  ctx.globalAlpha=0.3;ctx.strokeStyle=`hsl(${hue+15},90%,75%)`;ctx.lineWidth=r*0.08;
  for(let w=0;w<4;w++){
    const wa=w*Math.PI/2+ts*0.005;
    ctx.beginPath();ctx.moveTo(0,0);ctx.quadraticCurveTo(Math.cos(wa+0.5)*r*0.5,Math.sin(wa+0.5)*r*0.5,Math.cos(wa)*r*0.85,Math.sin(wa)*r*0.85);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌌",0,0);
  ctx.restore();
}
// ── Dusk Sphere — cycles dawn→dusk phases; dusk phase = 2× ──
function drawDuskSphere(ctx,r,ts,phasePct){
  ctx.save();
  const isDusk=phasePct>0.65;
  const hue=isDusk?280:30; // purple at dusk, orange at dawn
  ctx.shadowBlur=isDusk?22:14;ctx.shadowColor=`hsl(${hue},80%,60%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,`hsl(${hue},80%,75%)`);g.addColorStop(0.7,`hsl(${hue+15},70%,45%)`);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Horizon line
  ctx.strokeStyle=isDusk?"#c4b5fd":"#fed7aa";ctx.lineWidth=r*0.06;ctx.globalAlpha=0.5;
  ctx.beginPath();ctx.moveTo(-r*0.8,0);ctx.lineTo(r*0.8,0);ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.52)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isDusk?"🌅":"🌄",0,0);
  ctx.restore();
}
// ── Spiral Bead — tight spiral target; center tap = 3× ──
function drawSpiralBead(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#f0abfc";
  // Spiral arms
  ctx.strokeStyle="#e879f9";ctx.lineWidth=r*0.08;ctx.globalAlpha=0.8;
  ctx.beginPath();
  for(let a=0;a<Math.PI*6;a+=0.15){
    const ra=r*(a/(Math.PI*6));
    const px=Math.cos(a+ts*0.008)*ra;const py=Math.sin(a+ts*0.008)*ra;
    if(a===0)ctx.moveTo(px,py);else ctx.lineTo(px,py);
  }
  ctx.stroke();
  // Center bright spot
  ctx.globalAlpha=0.9;ctx.shadowBlur=14;ctx.shadowColor="#f5d0fe";
  const gS=ctx.createRadialGradient(0,0,0,0,0,r*0.2);
  gS.addColorStop(0,"#fff");gS.addColorStop(1,"#e879f9");
  ctx.fillStyle=gS;ctx.beginPath();ctx.arc(0,0,r*0.2,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Quantum Flux — unstable energy; stable phase (low pulse) = 2× ──
function drawQuantumFlux(ctx,r,ts,fluxPct){
  ctx.save();
  const isStable=fluxPct<0.35;
  ctx.shadowBlur=isStable?12:22;ctx.shadowColor=isStable?"#6ee7b7":"#f43f5e";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*(0.8+fluxPct*0.4));
  if(isStable){
    g.addColorStop(0,"#d1fae5");g.addColorStop(0.5,"#34d399");g.addColorStop(1,"rgba(0,0,0,0)");
  } else {
    g.addColorStop(0,"#fef2f2");g.addColorStop(0.4,"#ef4444");g.addColorStop(0.8,"#7f1d1d");g.addColorStop(1,"rgba(0,0,0,0)");
  }
  ctx.fillStyle=g;ctx.globalAlpha=0.88;
  ctx.beginPath();ctx.arc(0,0,r*(0.8+fluxPct*0.4),0,Math.PI*2);ctx.fill();
  // Instability lines
  if(!isStable){
    ctx.strokeStyle="#fca5a5";ctx.lineWidth=1;ctx.globalAlpha=0.4*fluxPct;
    for(let q=0;q<3;q++){
      const qa=q*Math.PI*2/3+ts*0.02;
      ctx.beginPath();ctx.moveTo(Math.cos(qa)*r*0.3,Math.sin(qa)*r*0.3);ctx.lineTo(Math.cos(qa)*r*1.1,Math.sin(qa)*r*1.1);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.52)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isStable?"⚛️":"💥",0,0);
  ctx.restore();
}
// ── Flame Orb — pulsing inferno; peak intensity = 3× ──
function drawFlameOrb(ctx,r,ts,intensityPct){
  ctx.save();
  const isPeak=intensityPct>0.8;
  const scale=0.85+intensityPct*0.3;
  ctx.shadowBlur=isPeak?28:16;ctx.shadowColor=isPeak?"#ef4444":"#f97316";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*scale);
  g.addColorStop(0,"#fff7ed");g.addColorStop(0.25,isPeak?"#fef08a":"#fed7aa");
  g.addColorStop(0.6,isPeak?"#ef4444":"#f97316");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*scale,0,Math.PI*2);ctx.fill();
  // Flame flickers
  for(let f=0;f<4;f++){
    const fa=f*Math.PI/2+ts*0.01;const fl=r*(0.6+intensityPct*0.4);
    ctx.globalAlpha=0.35*intensityPct;ctx.strokeStyle="#fbbf24";ctx.lineWidth=r*0.05;
    ctx.beginPath();ctx.moveTo(Math.cos(fa)*r*0.4,Math.sin(fa)*r*0.4);
    ctx.lineTo(Math.cos(fa+0.3)*fl,Math.sin(fa+0.3)*fl);ctx.stroke();
  }
  ctx.globalAlpha=0.92;ctx.font=`${Math.round(r*0.52)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isPeak?"🔥":"🌡️",0,0);
  ctx.restore();
}
// ── Crystal Shell — layered crystal; crack to core = 2× ──
function drawCrystalShell(ctx,r,ts,crackPct){
  ctx.save();
  const isCore=crackPct>0.7;
  ctx.shadowBlur=isCore?22:14;ctx.shadowColor=isCore?"#818cf8":"#c4b5fd";
  // Outer shell rings
  for(let ring=3;ring>=1;ring--){
    const rr=r*(0.45+ring*0.17);
    const alpha=isCore?0.15:0.5-ring*0.1;
    ctx.globalAlpha=Math.max(0.1,alpha);
    ctx.strokeStyle=`hsl(${250+ring*15},70%,${60+ring*5}%)`;
    ctx.lineWidth=r*0.05;
    ctx.beginPath();ctx.arc(0,0,rr,0,Math.PI*2);ctx.stroke();
  }
  // Inner core
  const gc=ctx.createRadialGradient(0,0,0,0,0,r*0.4);
  gc.addColorStop(0,"#fff");gc.addColorStop(0.4,isCore?"#818cf8":"#e9d5ff");gc.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gc;ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,r*0.4,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.92;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isCore?"💎":"🐚",0,0);
  ctx.restore();
}
// ── Omega Heart — pulsing legendary heart; perfect sync = 5× ──
function drawOmegaHeart(ctx,r,ts,syncPct){
  ctx.save();
  const isSync=syncPct>0.9;
  const scale=0.85+syncPct*0.25;
  ctx.shadowBlur=isSync?32:18;ctx.shadowColor=isSync?"#ec4899":"#f9a8d4";
  // Heart shape
  ctx.beginPath();
  const hs=r*scale*0.6;
  ctx.moveTo(0,hs*0.5);
  ctx.bezierCurveTo(-hs*2,0,-hs*2,-hs*1.5,0,-hs*0.7);
  ctx.bezierCurveTo(hs*2,-hs*1.5,hs*2,0,0,hs*0.5);
  ctx.closePath();
  const gH=ctx.createRadialGradient(0,0,0,0,0,r*scale);
  gH.addColorStop(0,"#fff");gH.addColorStop(0.4,isSync?"#ec4899":"#f9a8d4");gH.addColorStop(1,"#be185d");
  ctx.fillStyle=gH;ctx.globalAlpha=0.9;ctx.fill();
  // Omega symbol overlay
  if(isSync){
    ctx.globalAlpha=0.6;ctx.strokeStyle="#fff";ctx.lineWidth=r*0.05;
    ctx.font=`bold ${Math.round(r*0.45)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";
    ctx.fillText("Ω",0,-r*0.05);
  }
  // Pulse rings
  for(let pring=0;pring<2;pring++){
    ctx.globalAlpha=0.25-pring*0.08;ctx.strokeStyle="#ec4899";ctx.lineWidth=r*0.04;
    ctx.beginPath();ctx.arc(0,0,r*(scale+0.2+pring*0.25),0,Math.PI*2);ctx.stroke();
  }
  ctx.restore();
}
// ── Nexus Gem — multi-faceted gem; dead center = 4× ──
function drawNexusGem(ctx,r,ts){
  ctx.save();
  const rot=ts*0.003;
  ctx.rotate(rot);
  ctx.shadowBlur=24;ctx.shadowColor="#38bdf8";
  // 8-facet gem
  const facets=8;
  ctx.beginPath();
  for(let f=0;f<facets;f++){
    const a=f*Math.PI*2/facets;const ri=f%2===0?r:r*0.6;
    if(f===0)ctx.moveTo(Math.cos(a)*ri,Math.sin(a)*ri);
    else ctx.lineTo(Math.cos(a)*ri,Math.sin(a)*ri);
  }
  ctx.closePath();
  const gGem=ctx.createRadialGradient(0,0,0,0,0,r);
  gGem.addColorStop(0,"#fff");gGem.addColorStop(0.3,"#7dd3fc");gGem.addColorStop(0.7,"#0284c7");gGem.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=gGem;ctx.globalAlpha=0.88;ctx.fill();
  ctx.strokeStyle="#bae6fd";ctx.lineWidth=r*0.04;ctx.globalAlpha=0.5;ctx.stroke();
  // Sparkle center
  ctx.globalAlpha=0.95;ctx.fillStyle="#fff";ctx.beginPath();ctx.arc(0,0,r*0.12,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Plasma Halo — glowing ring target; sweetspot on ring edge = 3× ──
function drawPlasmaHalo(ctx,r,ts){
  ctx.save();
  const spin=ts*0.005;
  ctx.shadowBlur=18;ctx.shadowColor="#a855f7";
  // Outer halo ring
  const gRing=ctx.createRadialGradient(0,0,r*0.6,0,0,r*0.9);
  gRing.addColorStop(0,"rgba(0,0,0,0)");gRing.addColorStop(0.5,"rgba(168,85,247,0.8)");gRing.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gRing;ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,r*0.9,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0;ctx.beginPath();ctx.arc(0,0,r*0.6,0,Math.PI*2);ctx.fill();
  // Spinning plasma nodes on the ring
  for(let n=0;n<4;n++){
    const na=n*Math.PI/2+spin;const nx=Math.cos(na)*r*0.75;const ny=Math.sin(na)*r*0.75;
    ctx.globalAlpha=0.8;ctx.fillStyle="#e879f9";
    ctx.shadowBlur=10;ctx.shadowColor="#e879f9";
    ctx.beginPath();ctx.arc(nx,ny,r*0.1,0,Math.PI*2);ctx.fill();
  }
  ctx.restore();
}
// ── Glitch Node — corrupted pixel target; burst phase = 2× ──
function drawGlitchNode(ctx,r,ts,burstPct){
  ctx.save();
  const isBurst=burstPct>0.6&&burstPct<0.8;
  ctx.shadowBlur=isBurst?20:10;ctx.shadowColor=isBurst?"#4ade80":"#86efac";
  // Base square grid
  const cellSize=r*0.22;const cols=4;
  const off=r*0.45;
  for(let gy=0;gy<cols;gy++){for(let gx=0;gx<cols;gx++){
    const glitch=Math.sin((gx+gy+ts*0.02)*2.1)>0.3;
    ctx.globalAlpha=glitch?0.8:0.2;
    ctx.fillStyle=isBurst?(gx%2===0?"#4ade80":"#86efac"):(gx%2===0?"#34d399":"#6ee7b7");
    ctx.fillRect(-off+gx*cellSize,-off+gy*cellSize,cellSize-1,cellSize-1);
  }}
  // Burst overlay
  if(isBurst){
    ctx.globalAlpha=0.4;ctx.fillStyle="#f0fdf4";ctx.beginPath();ctx.arc(0,0,r*0.8,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.48)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isBurst?"💾":"⚡",0,0);
  ctx.restore();
}
// ── Astral Core — nested rings; rings aligned = 3× ──
function drawAstralCore(ctx,r,ts,alignPct){
  ctx.save();
  const isAlign=alignPct>0.8&&alignPct<0.92;
  ctx.shadowBlur=isAlign?26:14;ctx.shadowColor=isAlign?"#818cf8":"#a5b4fc";
  // 3 rotating concentric rings
  const ringRates=[0.003,0.005,0.002];
  const ringColors=["#818cf8","#a5b4fc","#c7d2fe"];
  for(let ri=0;ri<3;ri++){
    const rAngle=ts*ringRates[ri]*(ri%2===0?1:-1);
    ctx.globalAlpha=isAlign?0.8:0.5;ctx.strokeStyle=ringColors[ri];ctx.lineWidth=r*0.05;
    ctx.beginPath();ctx.arc(Math.cos(rAngle)*r*0.05,Math.sin(rAngle)*r*0.05,r*(0.3+ri*0.22),0,Math.PI*2);ctx.stroke();
  }
  // Alignment glow at center
  if(isAlign){
    const gAlign=ctx.createRadialGradient(0,0,0,0,0,r*0.3);
    gAlign.addColorStop(0,"#fff");gAlign.addColorStop(1,"#818cf8");
    ctx.fillStyle=gAlign;ctx.globalAlpha=0.7;ctx.beginPath();ctx.arc(0,0,r*0.3,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isAlign?"🌟":"🔮",0,0);
  ctx.restore();
}
// ── Light Bolt — building electric charge; full charge = 3× ──
function drawLightBolt(ctx,r,ts,chargePct){
  ctx.save();
  const isCharged=chargePct>0.88;
  ctx.shadowBlur=isCharged?26:12;ctx.shadowColor=isCharged?"#fef08a":"#fde68a";
  // Charge ring builds up
  ctx.strokeStyle=isCharged?"#fef08a":"#fde68a";ctx.lineWidth=r*0.06;ctx.globalAlpha=0.6;
  ctx.beginPath();ctx.arc(0,0,r*0.85,0,Math.PI*2*chargePct);ctx.stroke();
  // Outer glow disc
  const gCharge=ctx.createRadialGradient(0,0,0,0,0,r*0.7);
  gCharge.addColorStop(0,"#fef9c3");gCharge.addColorStop(0.5,isCharged?"#eab308":"#fde68a");gCharge.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gCharge;ctx.globalAlpha=isCharged?0.85:0.55;ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.fill();
  // Lightning bolt icon
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.56)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isCharged?"⚡":"💡",0,0);
  ctx.restore();
}
// ── Cosmic Lens — refracting rings; focused = 3× ──
function drawCosmicLens(ctx,r,ts,focusPct){
  ctx.save();
  const isFocus=focusPct>0.87;
  ctx.shadowBlur=isFocus?28:14;ctx.shadowColor=isFocus?"#818cf8":"#6366f1";
  // Lens body — nested rings converging at focus
  for(let cl=0;cl<4;cl++){
    const lr=r*(0.8-cl*0.15)*((isFocus?focusPct:0.6)+0.1);
    ctx.strokeStyle=`rgba(${isFocus?[129,140,248]:[99,102,241]},${0.9-cl*0.2})`;
    ctx.lineWidth=r*0.06;ctx.globalAlpha=0.75-cl*0.15;
    ctx.beginPath();ctx.arc(0,0,lr,0,Math.PI*2);ctx.stroke();
  }
  // Refraction cross
  if(isFocus){
    [0,Math.PI/2,Math.PI,Math.PI*1.5].forEach(angle=>{
      ctx.strokeStyle="#e0e7ff";ctx.lineWidth=r*0.05;ctx.globalAlpha=0.6;
      ctx.beginPath();ctx.moveTo(Math.cos(angle)*r*0.1,Math.sin(angle)*r*0.1);ctx.lineTo(Math.cos(angle)*r*0.75,Math.sin(angle)*r*0.75);ctx.stroke();
    });
  }
  ctx.globalAlpha=0.95;ctx.font=`${Math.round(r*0.44)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isFocus?"🔭":"🌌",0,0);
  ctx.restore();
}
// ── Nebula Burst II — expanding cloud; nova moment = 3× ──
function drawNebulaBurst2(ctx,r,ts,novaPct){
  ctx.save();
  const isNova=novaPct>0.88;
  ctx.shadowBlur=isNova?30:14;ctx.shadowColor=isNova?"#c084fc":"#a855f7";
  // Cloud layers
  for(let nl=0;nl<3;nl++){
    const nr2=r*(0.4+nl*0.2+(isNova?novaPct*0.1:0));
    const gNeb=ctx.createRadialGradient(0,0,0,0,0,nr2);
    gNeb.addColorStop(0,`rgba(${isNova?[192,132,252]:[168,85,247]},0.6)`);gNeb.addColorStop(0.6,`rgba(${isNova?[216,180,254]:[192,132,252]},0.3)`);gNeb.addColorStop(1,"rgba(0,0,0,0)");
    ctx.fillStyle=gNeb;ctx.globalAlpha=0.6-nl*0.15;
    ctx.save();ctx.rotate(nl*Math.PI/3+ts*0.002);ctx.beginPath();ctx.arc(0,0,nr2,0,Math.PI*2);ctx.fill();ctx.restore();
  }
  // Star burst
  if(isNova){
    for(let ns=0;ns<6;ns++){
      const nsa=ns*Math.PI/3+ts*0.004;
      ctx.strokeStyle="#f3e8ff";ctx.lineWidth=r*0.06;ctx.globalAlpha=0.6;
      ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(nsa)*r*0.8,Math.sin(nsa)*r*0.8);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.44)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isNova?"🌠":"💫",0,0);
  ctx.restore();
}
// ── Flame Orb II — heat builds; peak heat = 3× ──
function drawFlameOrb2(ctx,r,ts,heatPct){
  ctx.save();
  const isPeak=heatPct>0.88;
  ctx.shadowBlur=isPeak?30:14;ctx.shadowColor=isPeak?"#ef4444":"#f97316";
  // Core flame gradient
  const gFlame2=ctx.createRadialGradient(0,0,0,0,0,r*0.78);
  gFlame2.addColorStop(0,isPeak?"#fff":"#fef3c7");gFlame2.addColorStop(0.35,isPeak?"#ef4444":"#f97316");gFlame2.addColorStop(0.7,isPeak?"#991b1b":"#92400e");gFlame2.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gFlame2;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*0.78,0,Math.PI*2);ctx.fill();
  // Flickering corona
  for(let fc=0;fc<5;fc++){
    const fa=fc*Math.PI*2/5+ts*0.007;
    const fr=r*(0.72+0.12*heatPct*Math.sin(ts*0.02+fc));
    ctx.globalAlpha=0.3*(isPeak?1.5:1);ctx.fillStyle=isPeak?"#fca5a5":"#fed7aa";
    ctx.beginPath();ctx.arc(Math.cos(fa)*fr*0.3,Math.sin(fa)*fr*0.3,r*0.12,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=0.95;ctx.font=`${Math.round(r*0.44)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isPeak?"🔥":"🌡️",0,0);
  ctx.restore();
}
// ── Thor Gem — gem crackles; storm phase = 2× ──
function drawThorGem(ctx,r,ts,stormPct){
  ctx.save();
  const isStorm=stormPct>0.75&&stormPct<0.92;
  ctx.shadowBlur=isStorm?26:12;ctx.shadowColor=isStorm?"#fde68a":"#fbbf24";
  // Diamond gem body
  ctx.save();ctx.rotate(Math.PI/4);
  const tgSize=r*(0.55+0.1*stormPct);
  ctx.beginPath();ctx.rect(-tgSize*0.7,-tgSize*0.7,tgSize*1.4,tgSize*1.4);
  const gThor=ctx.createLinearGradient(-tgSize,-tgSize,tgSize,tgSize);
  gThor.addColorStop(0,isStorm?"#fef3c7":"#fef9c3");gThor.addColorStop(0.5,isStorm?"#f59e0b":"#fbbf24");gThor.addColorStop(1,isStorm?"#d97706":"#b45309");
  ctx.fillStyle=gThor;ctx.globalAlpha=0.9;ctx.fill();
  ctx.restore();
  // Lightning crackling in storm
  if(isStorm){
    for(let tl=0;tl<3;tl++){
      const lx1=(Math.random()-0.5)*r*0.8;const ly1=(Math.random()-0.5)*r*0.8;
      const lx2=lx1+(Math.random()-0.5)*r*0.5;const ly2=ly1+(Math.random()-0.5)*r*0.5;
      ctx.strokeStyle="#fffbeb";ctx.lineWidth=r*0.04;ctx.globalAlpha=0.7;
      ctx.beginPath();ctx.moveTo(lx1,ly1);ctx.lineTo(lx2,ly2);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.42)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isStorm?"⚡":"💎",0,0);
  ctx.restore();
}
// ── Ice Nova II — icy burst rings; freeze peak = 3× ──
function drawIceNova2(ctx,r,ts,freezePct){
  ctx.save();
  const isFreeze=freezePct>0.88;
  ctx.shadowBlur=isFreeze?28:14;ctx.shadowColor=isFreeze?"#bfdbfe":"#93c5fd";
  // Frozen background
  ctx.fillStyle=isFreeze?"#eff6ff":"#dbeafe";ctx.globalAlpha=0.15;ctx.beginPath();ctx.arc(0,0,r*0.85,0,Math.PI*2);ctx.fill();
  // Ice shard rings
  for(let ii=0;ii<3;ii++){
    const iRad=r*(0.3+ii*0.2)*freezePct;
    ctx.strokeStyle=isFreeze?`rgba(191,219,254,${0.9-ii*0.2})`:`rgba(147,197,253,${0.7-ii*0.2})`;
    ctx.lineWidth=r*0.06;ctx.globalAlpha=0.8;
    ctx.beginPath();ctx.arc(0,0,iRad,0,Math.PI*2);ctx.stroke();
    // 6 spikes per ring
    for(let sp=0;sp<6;sp++){
      const sa=sp*Math.PI/3+ii*0.4+ts*0.002;
      const sr=iRad+r*0.06*freezePct;
      ctx.globalAlpha=0.5;ctx.strokeStyle=isFreeze?"#bfdbfe":"#93c5fd";ctx.lineWidth=r*0.04;
      ctx.beginPath();ctx.moveTo(Math.cos(sa)*iRad,Math.sin(sa)*iRad);ctx.lineTo(Math.cos(sa)*sr,Math.sin(sa)*sr);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.92;ctx.font=`${Math.round(r*0.46)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isFreeze?"💎":"❄️",0,0);
  ctx.restore();
}
// ── Arc Gem — gem charges up; full charge = 3× ──
function drawArcGem(ctx,r,ts,chargePct){
  ctx.save();
  const isCharged=chargePct>0.88;
  ctx.shadowBlur=isCharged?28:12;ctx.shadowColor=isCharged?"#6ee7b7":"#34d399";
  // Gem body
  ctx.save();ctx.rotate(ts*0.004);
  const gemSides=6;ctx.beginPath();
  for(let gs6=0;gs6<gemSides;gs6++){
    const ga=gs6*Math.PI*2/gemSides-Math.PI/2;
    const gr2=r*(0.55+0.2*chargePct);
    if(gs6===0)ctx.moveTo(Math.cos(ga)*gr2,Math.sin(ga)*gr2);else ctx.lineTo(Math.cos(ga)*gr2,Math.sin(ga)*gr2);
  }
  ctx.closePath();
  const gGem=ctx.createLinearGradient(-r*0.5,-r*0.5,r*0.5,r*0.5);
  gGem.addColorStop(0,isCharged?"#a7f3d0":"#d1fae5");gGem.addColorStop(0.5,isCharged?"#10b981":"#34d399");gGem.addColorStop(1,isCharged?"#047857":"#059669");
  ctx.fillStyle=gGem;ctx.globalAlpha=0.9;ctx.fill();
  ctx.strokeStyle=isCharged?"#6ee7b7":"#34d399";ctx.lineWidth=r*0.07;ctx.globalAlpha=0.8;ctx.stroke();
  ctx.restore();
  // Charge arc
  ctx.strokeStyle=isCharged?"#6ee7b7":"#a7f3d0";ctx.lineWidth=r*0.07;ctx.globalAlpha=0.65;
  ctx.beginPath();ctx.arc(0,0,r*0.78,0,Math.PI*2*chargePct);ctx.stroke();
  ctx.globalAlpha=0.95;ctx.font=`${Math.round(r*0.4)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isCharged?"⚡":"💎",0,0);
  ctx.restore();
}
// ── Moon Petal — petals bloom outward; fully bloomed = 3× ──
function drawMoonPetal(ctx,r,ts,bloomPct){
  ctx.save();
  const isBloom=bloomPct>0.88;
  ctx.shadowBlur=isBloom?26:12;ctx.shadowColor=isBloom?"#c4b5fd":"#a78bfa";
  // Background crescent
  ctx.fillStyle="#1e1b4b";ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.fill();
  // Petals
  const petalCount=6;
  for(let pi=0;pi<petalCount;pi++){
    const pa=pi*Math.PI*2/petalCount+ts*0.002;
    const petalR=r*0.35*bloomPct;
    const px=Math.cos(pa)*r*0.48;const py=Math.sin(pa)*r*0.48;
    const gPetal=ctx.createRadialGradient(px,py,0,px,py,petalR);
    gPetal.addColorStop(0,isBloom?"#ede9fe":"#ddd6fe");gPetal.addColorStop(1,"rgba(0,0,0,0)");
    ctx.fillStyle=gPetal;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(px,py,petalR,0,Math.PI*2);ctx.fill();
  }
  // Moon icon center
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.44)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isBloom?"🌸":"🌙",0,0);
  ctx.restore();
}
// ── Star Dust II — sparkling trail; max trail length = special ──
function drawStarDust2(ctx,r,ts,trailLen){
  ctx.save();
  const hasTrail=trailLen>0.75;
  ctx.shadowBlur=hasTrail?22:10;ctx.shadowColor=hasTrail?"#fde68a":"#fef3c7";
  // Sparkle body
  const gStar2=ctx.createRadialGradient(0,0,0,0,0,r*0.65);
  gStar2.addColorStop(0,"#fffbeb");gStar2.addColorStop(0.5,hasTrail?"#f59e0b":"#fbbf24");gStar2.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gStar2;ctx.globalAlpha=0.88;ctx.beginPath();ctx.arc(0,0,r*0.65,0,Math.PI*2);ctx.fill();
  // Rotating sparkle points
  for(let si2=0;si2<5;si2++){
    const sa2=si2*Math.PI*2/5+ts*0.005;
    const sx2=Math.cos(sa2)*r*0.55;const sy2=Math.sin(sa2)*r*0.55;
    ctx.fillStyle="#fff";ctx.globalAlpha=0.6+0.4*Math.sin(ts*0.01+si2);
    ctx.beginPath();ctx.arc(sx2,sy2,r*0.1,0,Math.PI*2);ctx.fill();
  }
  // Center icon
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.42)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(hasTrail?"✨":"⭐",0,0);
  ctx.restore();
}
// ── Solar Eye — iris opens/closes; fully open = 3× ──
function drawSolarEye(ctx,r,ts,openPct){
  ctx.save();
  const isOpen=openPct>0.88;
  ctx.shadowBlur=isOpen?28:14;ctx.shadowColor=isOpen?"#fbbf24":"#f59e0b";
  // Outer sclera
  const gEye=ctx.createRadialGradient(0,0,r*0.1,0,0,r*0.8);
  gEye.addColorStop(0,isOpen?"#fff9c4":"#fffbeb");gEye.addColorStop(0.7,"#fbbf24");gEye.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gEye;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*0.8,0,Math.PI*2);ctx.fill();
  // Iris ring (scales with openPct)
  const irisR=r*0.52*openPct;
  ctx.strokeStyle=isOpen?"#d97706":"#b45309";ctx.lineWidth=r*0.1;ctx.globalAlpha=0.8;
  ctx.beginPath();ctx.arc(0,0,irisR,0,Math.PI*2);ctx.stroke();
  // Pupil
  ctx.fillStyle=isOpen?"#000":"#1c1917";ctx.globalAlpha=0.9;
  const pupR=irisR*0.5;
  ctx.beginPath();ctx.arc(0,0,pupR,0,Math.PI*2);ctx.fill();
  // Open indicator text
  if(isOpen){ctx.globalAlpha=0.95;ctx.font=`${Math.round(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.fillText("☀️",0,0);}
  ctx.restore();
}
// ── Void Serpent — sinuous wave; vertical posture = 2× ──
function drawVoidSerpent(ctx,r,ts,vertPct){
  ctx.save();
  const isVert=vertPct>0.85;
  ctx.shadowBlur=isVert?28:14;ctx.shadowColor=isVert?"#a78bfa":"#7c3aed";
  // Background disc
  ctx.fillStyle="#1e1b4b";ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,r*0.85,0,Math.PI*2);ctx.fill();
  // Serpentine wave path
  const segments=8;ctx.strokeStyle=isVert?"#c4b5fd":"#8b5cf6";ctx.lineWidth=r*0.1;ctx.globalAlpha=0.9;
  ctx.beginPath();
  for(let si=0;si<=segments;si++){
    const ty=-r*0.7+si*(r*1.4/segments);
    const tx=Math.sin(si*Math.PI+ts*0.004*(isVert?0.1:1))*r*0.3*(1-vertPct*0.7);
    if(si===0)ctx.moveTo(tx,ty);else ctx.lineTo(tx,ty);
  }
  ctx.stroke();
  // Head dot
  const headA=ts*0.004*(isVert?0.1:1);
  const hx=Math.sin(headA)*r*0.3*(1-vertPct*0.7);
  ctx.fillStyle=isVert?"#ede9fe":"#ddd6fe";ctx.globalAlpha=0.95;
  ctx.beginPath();ctx.arc(hx,-r*0.7,r*0.18,0,Math.PI*2);ctx.fill();
  if(isVert){ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.32)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🌑",0,0);}
  ctx.restore();
}
// ── Dark Pulsar — pulsating dark star; burst phase = 3× ──
function drawDarkPulsar(ctx,r,ts,burstPct){
  ctx.save();
  const isBurst=burstPct>0.82&&burstPct<0.95;
  ctx.shadowBlur=isBurst?28:14;ctx.shadowColor=isBurst?"#a855f7":"#6d28d9";
  // Dark background disc
  ctx.fillStyle=isBurst?"#1e1b4b":"#0f0a26";ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*0.8,0,Math.PI*2);ctx.fill();
  // Pulsar beams at burst
  if(isBurst){
    for(let pb=0;pb<4;pb++){
      const pba=pb*Math.PI/2+ts*0.008;
      ctx.globalAlpha=0.4;ctx.strokeStyle="#c084fc";ctx.lineWidth=r*0.08;
      ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(pba)*r,Math.sin(pba)*r);ctx.stroke();
    }
  }
  // Core glow
  const gPulsar=ctx.createRadialGradient(0,0,0,0,0,r*0.35);
  gPulsar.addColorStop(0,"#fff");gPulsar.addColorStop(0.4,isBurst?"#c084fc":"#7c3aed");gPulsar.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gPulsar;ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,r*0.35,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.42)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isBurst?"💫":"🖤",0,0);
  ctx.restore();
}
// ── Crystal Bomb — bomb coated in crystal; defuse before explosion = 2× ──
function drawCrystalBomb(ctx,r,ts,lifePct){
  ctx.save();
  const isSafe=lifePct<0.7;
  ctx.shadowBlur=isSafe?12:24;ctx.shadowColor=isSafe?"#86efac":"#ef4444";
  // Bomb body with crystal coating
  const gBomb=ctx.createRadialGradient(0,0,0,0,0,r*0.75);
  gBomb.addColorStop(0,isSafe?"#d1fae5":"#fee2e2");
  gBomb.addColorStop(0.5,isSafe?"#34d399":"#f87171");
  gBomb.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gBomb;ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,r*0.75,0,Math.PI*2);ctx.fill();
  // Crystal facets
  ctx.globalAlpha=0.35;ctx.strokeStyle=isSafe?"#a7f3d0":"#fca5a5";ctx.lineWidth=r*0.04;
  for(let cf=0;cf<6;cf++){
    const cfa=cf*Math.PI/3;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(cfa)*r*0.7,Math.sin(cfa)*r*0.7);ctx.stroke();
  }
  // Fuse countdown
  const fuseLen=r*0.5*(1-lifePct);
  ctx.globalAlpha=0.9;ctx.strokeStyle=isSafe?"#fbbf24":"#ef4444";ctx.lineWidth=r*0.06;
  ctx.beginPath();ctx.moveTo(r*0.55,-r*0.55);ctx.lineTo(r*0.55+fuseLen,-r*0.55-fuseLen);ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.4)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isSafe?"💎":"💣",0,0);
  ctx.restore();
}
// ── Glacier Spike — ice spike; very tip = 3× ──
function drawGlacierSpike(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#93c5fd";
  // Ice spike body
  const gSpike=ctx.createLinearGradient(0,-r*0.95,0,r*0.75);
  gSpike.addColorStop(0,"#e0f2fe");gSpike.addColorStop(0.4,"#7dd3fc");gSpike.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gSpike;ctx.globalAlpha=0.85;
  ctx.beginPath();ctx.moveTo(0,-r*0.95);ctx.lineTo(r*0.28,r*0.75);ctx.lineTo(-r*0.28,r*0.75);ctx.closePath();ctx.fill();
  // Ice facets
  ctx.globalAlpha=0.4;ctx.strokeStyle="#bfdbfe";ctx.lineWidth=r*0.04;
  ctx.beginPath();ctx.moveTo(0,-r*0.95);ctx.lineTo(r*0.1,r*0.0);ctx.stroke();
  ctx.beginPath();ctx.moveTo(0,-r*0.95);ctx.lineTo(-r*0.1,r*0.1);ctx.stroke();
  // Glinting tip
  ctx.globalAlpha=0.8;ctx.fillStyle="#fff";
  ctx.beginPath();ctx.arc(0,-r*0.88,r*0.05,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.36)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🧊",0,r*0.3);
  ctx.restore();
}
// ── Plasma Pulse 2 — oscillating plasma; pulse peak = 3× ──
function drawPlasmaPulse2(ctx,r,ts,peakPct){
  ctx.save();
  const isPeak=peakPct>0.88&&peakPct<0.97;
  const scale109=0.5+peakPct*0.6;
  ctx.shadowBlur=isPeak?26:14;ctx.shadowColor=isPeak?"#e879f9":"#a855f7";
  const gPP2=ctx.createRadialGradient(0,0,0,0,0,r*scale109);
  gPP2.addColorStop(0,"#fdf4ff");gPP2.addColorStop(0.4,isPeak?"#e879f9":"#c084fc");gPP2.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gPP2;ctx.globalAlpha=isPeak?0.92:0.7;
  ctx.beginPath();ctx.arc(0,0,r*scale109,0,Math.PI*2);ctx.fill();
  // Wave rings emanating outward at peak
  if(isPeak){
    for(let pp=1;pp<=3;pp++){
      ctx.globalAlpha=0.2;ctx.strokeStyle="#f5d0fe";ctx.lineWidth=r*0.04;
      ctx.beginPath();ctx.arc(0,0,r*scale109*(1+pp*0.2),0,Math.PI*2);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*scale109*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isPeak?"💜":"〰️",0,0);
  ctx.restore();
}
// ── Quantum Burst 2 — pulsating quantum; smallest phase = 3× ──
function drawQuantumBurst2(ctx,r,ts,pulsePct){
  ctx.save();
  const isTiny=pulsePct<0.15||pulsePct>0.92;
  const scale108=isTiny?0.45:(0.45+pulsePct*0.7);
  ctx.shadowBlur=isTiny?26:14;ctx.shadowColor=isTiny?"#a78bfa":"#7c3aed";
  const gQb2=ctx.createRadialGradient(0,0,0,0,0,r*scale108);
  gQb2.addColorStop(0,"#f5f3ff");gQb2.addColorStop(0.4,isTiny?"#7c3aed":"#a855f7");gQb2.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gQb2;ctx.globalAlpha=isTiny?0.95:0.75;
  ctx.beginPath();ctx.arc(0,0,r*scale108,0,Math.PI*2);ctx.fill();
  // Quantum rings
  for(let qr=1;qr<=2;qr++){
    ctx.globalAlpha=0.3;ctx.strokeStyle="#c4b5fd";ctx.lineWidth=r*0.04;
    ctx.beginPath();ctx.arc(0,0,r*scale108*(0.5+qr*0.25),0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*scale108*0.8)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isTiny?"⚛️":"💥",0,0);
  ctx.restore();
}
// ── Nebula Ring — misty ring; outer edge = 2× ──
function drawNebulaRing(ctx,r,ts){
  ctx.save();
  const spin108=ts*0.004;
  ctx.shadowBlur=16;ctx.shadowColor="#818cf8";
  // Nebula gradient ring
  const gRing108=ctx.createRadialGradient(0,0,r*0.5,0,0,r*0.95);
  gRing108.addColorStop(0,"rgba(0,0,0,0)");gRing108.addColorStop(0.4,"rgba(129,140,248,0.6)");gRing108.addColorStop(0.8,"rgba(99,102,241,0.8)");gRing108.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gRing108;ctx.globalAlpha=0.8;ctx.beginPath();ctx.arc(0,0,r*0.95,0,Math.PI*2);ctx.fill();
  // Nebula stars scattered in ring
  for(let ns=0;ns<6;ns++){
    const na=ns*Math.PI/3+spin108;const nr=r*(0.6+Math.random()*0.25);
    ctx.globalAlpha=0.6;ctx.fillStyle="#c7d2fe";
    ctx.beginPath();ctx.arc(Math.cos(na)*nr,Math.sin(na)*nr,r*0.05,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌌",0,0);
  ctx.restore();
}
// ── Flux Ring 2 — double ring; inside inner = 2× ──
function drawFluxRing2(ctx,r,ts){
  ctx.save();
  const spin107=ts*0.006;
  ctx.shadowBlur=16;ctx.shadowColor="#3b82f6";
  // Outer ring
  ctx.strokeStyle="#93c5fd";ctx.lineWidth=r*0.07;ctx.globalAlpha=0.65;
  ctx.beginPath();ctx.arc(0,0,r*0.9,0,Math.PI*2);ctx.stroke();
  // Inner ring (smaller)
  ctx.strokeStyle="#3b82f6";ctx.lineWidth=r*0.06;ctx.globalAlpha=0.8;
  ctx.beginPath();ctx.arc(0,0,r*0.5,0,Math.PI*2);ctx.stroke();
  // Rotating nodes on outer ring
  for(let fn=0;fn<4;fn++){
    const fa=fn*Math.PI/2+spin107;
    ctx.globalAlpha=0.7;ctx.fillStyle="#60a5fa";
    ctx.beginPath();ctx.arc(Math.cos(fa)*r*0.9,Math.sin(fa)*r*0.9,r*0.08,0,Math.PI*2);ctx.fill();
  }
  // Inner fill
  const gInner107=ctx.createRadialGradient(0,0,0,0,0,r*0.5);
  gInner107.addColorStop(0,"#dbeafe");gInner107.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gInner107;ctx.globalAlpha=0.35;ctx.beginPath();ctx.arc(0,0,r*0.5,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.4)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("〰️",0,0);
  ctx.restore();
}
// ── Solar Vortex — spinning sun vortex; calm center = 3× ──
function drawSolarVortex(ctx,r,ts){
  ctx.save();
  const spin107=ts*0.007;
  ctx.shadowBlur=18;ctx.shadowColor="#f59e0b";
  // Outer spinning arms
  for(let sa=0;sa<4;sa++){
    const armA=sa*Math.PI/2+spin107;
    const ax=Math.cos(armA)*r*0.6;const ay=Math.sin(armA)*r*0.6;
    const gArm=ctx.createLinearGradient(0,0,ax,ay);
    gArm.addColorStop(0,"rgba(251,191,36,0.9)");gArm.addColorStop(1,"rgba(0,0,0,0)");
    ctx.strokeStyle=gArm;ctx.lineWidth=r*0.14;ctx.globalAlpha=0.6;
    ctx.beginPath();ctx.arc(0,0,r*0.6,armA,armA+Math.PI*0.5);ctx.stroke();
  }
  // Calm center
  const gCenter107=ctx.createRadialGradient(0,0,0,0,0,r*0.25);
  gCenter107.addColorStop(0,"#fef9c3");gCenter107.addColorStop(1,"#fbbf24");
  ctx.fillStyle=gCenter107;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*0.25,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.36)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("☀️",0,0);
  ctx.restore();
}
// ── Drift Orb — drifting orb; momentarily still = 2× ──
function drawDriftOrb(ctx,r,ts,stillPct){
  ctx.save();
  const isStill=stillPct>0.47&&stillPct<0.53;
  ctx.shadowBlur=isStill?22:10;ctx.shadowColor=isStill?"#818cf8":"#a5b4fc";
  // Drifting trail pattern
  for(let dt=0;dt<5;dt++){
    const dta=ts*0.01+dt*0.3;const drad=r*(0.9-dt*0.1);
    ctx.globalAlpha=(0.15+dt*0.1)*(isStill?1.4:1);
    ctx.fillStyle=isStill?"#818cf8":"#a5b4fc";
    ctx.beginPath();ctx.arc(Math.cos(dta)*r*0.18,Math.sin(dta)*r*0.18,drad,0,Math.PI*2);ctx.fill();
  }
  // Core orb
  const gDrift=ctx.createRadialGradient(0,0,0,0,0,r*0.65);
  gDrift.addColorStop(0,"#e0e7ff");gDrift.addColorStop(0.5,isStill?"#6366f1":"#818cf8");gDrift.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gDrift;ctx.globalAlpha=0.8;ctx.beginPath();ctx.arc(0,0,r*0.65,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.42)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isStill?"⏸️":"🌀",0,0);
  ctx.restore();
}
// ── Chrono Star — time star; apex phase = 3× ──
function drawChronoStar(ctx,r,ts,apexPct){
  ctx.save();
  const isApex=apexPct>0.85&&apexPct<0.97;
  ctx.shadowBlur=isApex?26:14;ctx.shadowColor=isApex?"#f0abfc":"#c084fc";
  // 6-point star
  const starR=r*0.75;const starInner=r*0.35;
  ctx.globalAlpha=isApex?0.9:0.65;
  const gStar=ctx.createRadialGradient(0,0,0,0,0,starR);
  gStar.addColorStop(0,"#fff");gStar.addColorStop(0.4,isApex?"#f0abfc":"#c084fc");gStar.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gStar;
  ctx.beginPath();
  for(let cs=0;cs<6;cs++){
    const oA=cs*Math.PI/3-Math.PI/2+ts*(isApex?0.006:0.003);
    const iA=(cs+0.5)*Math.PI/3-Math.PI/2+ts*(isApex?0.006:0.003);
    cs===0?ctx.moveTo(Math.cos(oA)*starR,Math.sin(oA)*starR):ctx.lineTo(Math.cos(oA)*starR,Math.sin(oA)*starR);
    ctx.lineTo(Math.cos(iA)*starInner,Math.sin(iA)*starInner);
  }
  ctx.closePath();ctx.fill();
  // Time rings at apex
  if(isApex){
    for(let tr=1;tr<=2;tr++){
      ctx.globalAlpha=0.25;ctx.strokeStyle="#fae8ff";ctx.lineWidth=r*0.04;
      ctx.beginPath();ctx.arc(0,0,r*tr*0.3,0,Math.PI*2);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isApex?"⭐":"⏱️",0,0);
  ctx.restore();
}
// ── Prism Coil — spiraling prism; color sync = 3× ──
function drawPrismCoil(ctx,r,ts,syncPct){
  ctx.save();
  const isSync=syncPct>0.75&&syncPct<0.9;
  ctx.shadowBlur=isSync?24:12;ctx.shadowColor=isSync?"#e879f9":"#a78bfa";
  // Coil as spiraling arcs in rainbow colors
  const colors105=["#ef4444","#f97316","#eab308","#22c55e","#3b82f6","#8b5cf6"];
  for(let ci=0;ci<6;ci++){
    const ca=ci*Math.PI/3+ts*(isSync?0.008:0.004);
    const sr=r*(0.3+ci*0.08);
    ctx.globalAlpha=isSync?0.8:0.5;ctx.strokeStyle=colors105[ci];ctx.lineWidth=r*0.05;
    ctx.beginPath();ctx.arc(0,0,sr,ca,ca+Math.PI*1.2);ctx.stroke();
  }
  // Center bright orb when in sync
  if(isSync){
    const gOrb=ctx.createRadialGradient(0,0,0,0,0,r*0.22);
    gOrb.addColorStop(0,"#fff");gOrb.addColorStop(1,"#e879f9");
    ctx.fillStyle=gOrb;ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,r*0.22,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.42)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isSync?"🌈":"🌀",0,0);
  ctx.restore();
}
// ── Star Veil — shrouded star; reveal moment = 2× ──
function drawStarVeil(ctx,r,ts,revealPct){
  ctx.save();
  const isReveal=revealPct>0.55&&revealPct<0.72;
  ctx.shadowBlur=isReveal?20:8;ctx.shadowColor=isReveal?"#fef08a":"#fde68a";
  // Veil/fog layer
  ctx.globalAlpha=isReveal?0.2:0.6;
  const gVeil=ctx.createRadialGradient(0,0,0,0,0,r);
  gVeil.addColorStop(0,"rgba(148,163,184,0.5)");gVeil.addColorStop(0.5,"rgba(100,116,139,0.8)");gVeil.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gVeil;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Star beneath the veil
  ctx.globalAlpha=isReveal?0.9:0.3;
  const gStar=ctx.createRadialGradient(0,0,0,0,0,r*0.65);
  gStar.addColorStop(0,"#fff");gStar.addColorStop(0.4,"#fef08a");gStar.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gStar;ctx.beginPath();ctx.arc(0,0,r*0.65,0,Math.PI*2);ctx.fill();
  // 5-point star shape
  ctx.globalAlpha=isReveal?0.85:0.2;ctx.strokeStyle="#fde68a";ctx.lineWidth=r*0.04;
  ctx.beginPath();
  for(let sv=0;sv<5;sv++){
    const soa=sv*Math.PI*2/5-Math.PI/2;const sia=(sv+0.5)*Math.PI*2/5-Math.PI/2;
    sv===0?ctx.moveTo(Math.cos(soa)*r*0.6,Math.sin(soa)*r*0.6):ctx.lineTo(Math.cos(soa)*r*0.6,Math.sin(soa)*r*0.6);
    ctx.lineTo(Math.cos(sia)*r*0.28,Math.sin(sia)*r*0.28);
  }
  ctx.closePath();ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.42)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isReveal?"⭐":"🌫️",0,0);
  ctx.restore();
}
// ── Frost Halo — icy ring; freeze peak = 3× ──
function drawFrostHalo(ctx,r,ts,freezePct){
  ctx.save();
  const isFreeze=freezePct>0.78&&freezePct<0.93;
  ctx.shadowBlur=isFreeze?24:12;ctx.shadowColor=isFreeze?"#bfdbfe":"#dbeafe";
  // Outer icy ring
  ctx.strokeStyle=isFreeze?"#93c5fd":"#bfdbfe";ctx.lineWidth=r*0.08;ctx.globalAlpha=isFreeze?0.85:0.55;
  ctx.beginPath();ctx.arc(0,0,r*0.85,0,Math.PI*2);ctx.stroke();
  // Ice crystal spikes around ring
  for(let ic=0;ic<8;ic++){
    const ia=ic*Math.PI/4+ts*0.002;const ix=Math.cos(ia)*r*0.85;const iy=Math.sin(ia)*r*0.85;
    ctx.globalAlpha=isFreeze?0.7:0.35;ctx.strokeStyle="#e0f2fe";ctx.lineWidth=r*0.04;
    ctx.beginPath();ctx.moveTo(ix,iy);ctx.lineTo(Math.cos(ia)*r*(isFreeze?1.05:0.95),Math.sin(ia)*r*(isFreeze?1.05:0.95));ctx.stroke();
  }
  // Inner gradient
  const gFrost=ctx.createRadialGradient(0,0,0,0,0,r*0.6);
  gFrost.addColorStop(0,"#e0f2fe");gFrost.addColorStop(1,isFreeze?"rgba(147,197,253,0.5)":"rgba(219,234,254,0.3)");
  ctx.fillStyle=gFrost;ctx.globalAlpha=0.4;ctx.beginPath();ctx.arc(0,0,r*0.6,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.42)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isFreeze?"❄️":"🔱",0,0);
  ctx.restore();
}
// ── Ember Clock — countdown clock; final 20% lifetime = 2× ──
function drawEmberClock(ctx,r,ts,lifePct){
  ctx.save();
  const isFinal=lifePct>0.8;
  ctx.shadowBlur=isFinal?22:10;ctx.shadowColor=isFinal?"#ef4444":"#f97316";
  // Clock face
  const gClock=ctx.createRadialGradient(0,0,0,0,0,r*0.8);
  gClock.addColorStop(0,isFinal?"#fee2e2":"#ffedd5");
  gClock.addColorStop(0.6,isFinal?"#ef4444":"#f97316");
  gClock.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gClock;ctx.globalAlpha=0.75;ctx.beginPath();ctx.arc(0,0,r*0.8,0,Math.PI*2);ctx.fill();
  // Clock border
  ctx.strokeStyle=isFinal?"#ef4444":"#f97316";ctx.lineWidth=r*0.06;ctx.globalAlpha=0.8;
  ctx.beginPath();ctx.arc(0,0,r*0.8,0,Math.PI*2);ctx.stroke();
  // Clock hand — spins faster as time runs out
  const handSpeed=isFinal?0.015:0.006;
  const handA=ts*handSpeed;
  ctx.globalAlpha=0.85;ctx.strokeStyle=isFinal?"#fff":"#fef3c7";ctx.lineWidth=r*0.05;
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(handA-Math.PI/2)*r*0.55,Math.sin(handA-Math.PI/2)*r*0.55);ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isFinal?"🔥":"🕐",0,0);
  ctx.restore();
}
// ── Nova Spire — energy spire; peak energy = 3× ──
function drawNovaSpire(ctx,r,ts,peakPct){
  ctx.save();
  const isPeak=peakPct>0.82&&peakPct<0.96;
  ctx.shadowBlur=isPeak?28:14;ctx.shadowColor=isPeak?"#f59e0b":"#fbbf24";
  // Spire triangle
  const gSpire=ctx.createLinearGradient(0,-r*0.95,0,r*0.8);
  gSpire.addColorStop(0,isPeak?"#fef9c3":"#fef3c7");
  gSpire.addColorStop(0.4,isPeak?"#f59e0b":"#fbbf24");
  gSpire.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gSpire;ctx.globalAlpha=isPeak?0.92:0.72;
  ctx.beginPath();ctx.moveTo(0,-r*0.95);ctx.lineTo(r*0.35,r*0.8);ctx.lineTo(-r*0.35,r*0.8);ctx.closePath();ctx.fill();
  // Energy rings at peak
  if(isPeak){
    for(let er=1;er<=3;er++){
      const ey=-r*0.95+er*r*0.5;
      ctx.globalAlpha=0.35;ctx.strokeStyle="#fef9c3";ctx.lineWidth=r*0.04;
      ctx.beginPath();ctx.arc(0,ey,r*0.1+er*r*0.05,0,Math.PI*2);ctx.stroke();
    }
  }
  // Glow orb at tip
  const gTip=ctx.createRadialGradient(0,-r*0.85,0,0,-r*0.85,r*0.2);
  gTip.addColorStop(0,"#fff");gTip.addColorStop(1,isPeak?"#f59e0b":"#fbbf24");
  ctx.fillStyle=gTip;ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,-r*0.85,r*0.12,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.36)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isPeak?"💥":"🗼",0,r*0.3);
  ctx.restore();
}
// ── Echo Lens — prismatic lens; focus phase = 2× ──
function drawEchoLens(ctx,r,ts,focusPct){
  ctx.save();
  const isFocus=focusPct>0.6&&focusPct<0.8;
  ctx.shadowBlur=isFocus?20:10;ctx.shadowColor=isFocus?"#22d3ee":"#67e8f9";
  // Outer lens ring
  ctx.strokeStyle=isFocus?"#22d3ee":"#67e8f9";ctx.lineWidth=r*0.07;ctx.globalAlpha=0.75;
  ctx.beginPath();ctx.arc(0,0,r*0.82,0,Math.PI*2);ctx.stroke();
  // Inner lens gradient
  const gLens=ctx.createRadialGradient(0,0,0,0,0,r*0.7);
  gLens.addColorStop(0,isFocus?"#e0f2fe":"#cffafe");
  gLens.addColorStop(0.5,isFocus?"#22d3ee":"#38bdf8");
  gLens.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gLens;ctx.globalAlpha=0.45;ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.fill();
  // Crosshair lines
  ctx.globalAlpha=isFocus?0.7:0.35;ctx.strokeStyle=isFocus?"#fff":"#bae6fd";ctx.lineWidth=r*0.04;
  ctx.beginPath();ctx.moveTo(-r*0.6,0);ctx.lineTo(r*0.6,0);ctx.stroke();
  ctx.beginPath();ctx.moveTo(0,-r*0.6);ctx.lineTo(0,r*0.6);ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.4)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isFocus?"📡":"🔮",0,0);
  ctx.restore();
}
// ── Crystal Petal — rotating petal flower; all aligned = 3× ──
function drawCrystalPetal(ctx,r,ts,alignPct){
  ctx.save();
  const isAlign=alignPct>0.8&&alignPct<0.95;
  ctx.shadowBlur=isAlign?22:12;ctx.shadowColor=isAlign?"#f9a8d4":"#fbcfe8";
  // Center disc
  const gCenter=ctx.createRadialGradient(0,0,0,0,0,r*0.3);
  gCenter.addColorStop(0,"#fff");gCenter.addColorStop(1,isAlign?"#f43f5e":"#ec4899");
  ctx.fillStyle=gCenter;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*0.3,0,Math.PI*2);ctx.fill();
  // 6 rotating petals
  const spinA=ts*0.003;
  for(let p=0;p<6;p++){
    const pa=p*Math.PI/3+spinA;
    const px=Math.cos(pa)*r*0.55;const py=Math.sin(pa)*r*0.55;
    ctx.globalAlpha=isAlign?0.85:0.55;
    ctx.fillStyle=isAlign?(p%2===0?"#f9a8d4":"#fda4af"):(p%2===0?"#fbcfe8":"#fce7f3");
    ctx.beginPath();
    ctx.ellipse(px,py,r*0.18,r*0.28,pa,0,Math.PI*2);ctx.fill();
    // Crystal facet line
    ctx.globalAlpha=0.3;ctx.strokeStyle="#fff";ctx.lineWidth=r*0.03;
    ctx.beginPath();ctx.moveTo(px-Math.cos(pa)*r*0.1,py-Math.sin(pa)*r*0.1);ctx.lineTo(px+Math.cos(pa)*r*0.1,py+Math.sin(pa)*r*0.1);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isAlign?"🌸":"💎",0,0);
  ctx.restore();
}
// ── Twilight Shard — midnight shard; twilight phase = 2× ──
function drawTwilightShard(ctx,r,ts,duskPct){
  ctx.save();
  const isDusk=duskPct>0.5&&duskPct<0.75;
  ctx.shadowBlur=isDusk?20:10;ctx.shadowColor=isDusk?"#818cf8":"#4f46e5";
  // Shard body — irregular triangle
  const gShard=ctx.createLinearGradient(-r*0.6,-r*0.8,r*0.4,r*0.8);
  gShard.addColorStop(0,isDusk?"#a5b4fc":"#6366f1");
  gShard.addColorStop(0.5,isDusk?"#818cf8":"#4f46e5");
  gShard.addColorStop(1,isDusk?"#c7d2fe":"#3730a3");
  ctx.fillStyle=gShard;ctx.globalAlpha=isDusk?0.88:0.65;
  ctx.beginPath();ctx.moveTo(0,-r*0.9);ctx.lineTo(r*0.45,r*0.6);ctx.lineTo(-r*0.5,r*0.4);ctx.lineTo(-r*0.2,-r*0.3);ctx.closePath();ctx.fill();
  // Dusk glow rings
  if(isDusk){
    ctx.globalAlpha=0.3;
    for(let di=1;di<=2;di++){
      ctx.strokeStyle="#e0e7ff";ctx.lineWidth=r*0.04;
      ctx.beginPath();ctx.arc(0,0,r*di*0.4,0,Math.PI*2);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.42)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isDusk?"🌌":"🔮",0,r*0.05);
  ctx.restore();
}
// ── Shadow Crystal — dark crystal; dark phase = 3× ──
function drawShadowCrystal(ctx,r,ts,darkPct){
  ctx.save();
  const isDark=darkPct>0.65&&darkPct<0.85;
  ctx.shadowBlur=isDark?26:14;ctx.shadowColor=isDark?"#7c3aed":"#4c1d95";
  // Crystal body — diamond shape
  const gCrystal=ctx.createRadialGradient(0,-r*0.2,0,0,0,r*0.8);
  gCrystal.addColorStop(0,isDark?"#c4b5fd":"#6d28d9");
  gCrystal.addColorStop(0.5,isDark?"#7c3aed":"#4c1d95");
  gCrystal.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gCrystal;ctx.globalAlpha=isDark?0.9:0.65;
  ctx.beginPath();ctx.moveTo(0,-r*0.9);ctx.lineTo(r*0.55,0);ctx.lineTo(0,r*0.9);ctx.lineTo(-r*0.55,0);ctx.closePath();ctx.fill();
  // Dark core pulse
  if(isDark){
    const gCore=ctx.createRadialGradient(0,0,0,0,0,r*0.35);
    gCore.addColorStop(0,"#fff");gCore.addColorStop(0.6,"#7c3aed");gCore.addColorStop(1,"rgba(0,0,0,0)");
    ctx.fillStyle=gCore;ctx.globalAlpha=0.75;ctx.beginPath();ctx.arc(0,0,r*0.35,0,Math.PI*2);ctx.fill();
  }
  // Facet highlight
  ctx.globalAlpha=0.35;ctx.strokeStyle="#e9d5ff";ctx.lineWidth=r*0.04;
  ctx.beginPath();ctx.moveTo(0,-r*0.9);ctx.lineTo(r*0.55,0);ctx.lineTo(0,r*0.9);ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.46)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isDark?"💜":"🖤",0,0);
  ctx.restore();
}
// ── Tide Orb — tidal wave orb; wave crest = 2× ──
function drawTideOrb(ctx,r,ts,crestPct){
  ctx.save();
  const isCrest=crestPct>0.75&&crestPct<0.9;
  ctx.shadowBlur=isCrest?22:12;ctx.shadowColor=isCrest?"#22d3ee":"#67e8f9";
  // Ocean gradient body
  const gTide=ctx.createRadialGradient(0,0,0,0,0,r*0.85);
  gTide.addColorStop(0,"#e0f2fe");gTide.addColorStop(0.4,isCrest?"#0ea5e9":"#38bdf8");gTide.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gTide;ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,r*0.85,0,Math.PI*2);ctx.fill();
  // Wave crests drawn as sine curves
  const waveOff=ts*0.006;
  for(let wi=0;wi<3;wi++){
    const wy=-r*0.15+wi*r*0.15;
    ctx.globalAlpha=isCrest?0.55:0.3;ctx.strokeStyle=isCrest?"#fff":"#bae6fd";ctx.lineWidth=r*0.05;
    ctx.beginPath();
    for(let wx=-r*0.7;wx<=r*0.7;wx+=2){
      const waveY=wy+Math.sin((wx/r)*Math.PI*2+waveOff+wi*1.2)*r*0.12;
      wx===-r*0.7?ctx.moveTo(wx,waveY):ctx.lineTo(wx,waveY);
    }
    ctx.stroke();
  }
  // Crest splash
  if(isCrest){
    ctx.globalAlpha=0.35;ctx.fillStyle="#e0f2fe";
    for(let sp=0;sp<5;sp++){const sa=sp*Math.PI*0.4-Math.PI*0.8;ctx.beginPath();ctx.arc(Math.cos(sa)*r*0.6,Math.sin(sa)*r*0.6,r*0.07,0,Math.PI*2);ctx.fill();}
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.46)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isCrest?"🌊":"🔮",0,0);
  ctx.restore();
}
// ── Lunar Rune — moon phase rune; full moon = 3× ──
function drawLunarRune(ctx,r,ts,moonPct){
  ctx.save();
  const isFull=moonPct>0.85;
  ctx.shadowBlur=isFull?24:14;ctx.shadowColor=isFull?"#fde68a":"#fef3c7";
  // Moon disc
  const gMoon=ctx.createRadialGradient(-r*0.1,-r*0.1,0,0,0,r*0.7);
  gMoon.addColorStop(0,"#fff");gMoon.addColorStop(0.5,isFull?"#fef08a":"#fde68a");gMoon.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gMoon;ctx.globalAlpha=0.85;ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.fill();
  // Craters
  [[r*0.2,r*0.1,r*0.08],[r*(-0.25),r*(-0.1),r*0.06]].forEach(([cx,cy,cr])=>{
    ctx.globalAlpha=0.2;ctx.fillStyle="#c8a45a";ctx.beginPath();ctx.arc(cx,cy,cr,0,Math.PI*2);ctx.fill();
  });
  // Rune marks
  ctx.globalAlpha=0.5;ctx.strokeStyle=isFull?"#a16207":"#d97706";ctx.lineWidth=r*0.04;
  ctx.beginPath();ctx.moveTo(-r*0.3,r*0.4);ctx.lineTo(0,-r*0.4);ctx.lineTo(r*0.3,r*0.4);ctx.closePath();ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.42)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isFull?"🌕":"🌙",0,r*0.1);
  ctx.restore();
}
// ── Storm Seed — grows into full storm; fully grown = 2× ──
function drawStormSeed(ctx,r,ts,growPct){
  ctx.save();
  const isGrown=growPct>0.85;
  const scale=0.5+growPct*0.6;
  ctx.shadowBlur=isGrown?22:12;ctx.shadowColor=isGrown?"#60a5fa":"#93c5fd";
  const gs98=ctx.createRadialGradient(0,0,0,0,0,r*scale);
  gs98.addColorStop(0,"#dbeafe");gs98.addColorStop(0.5,isGrown?"#2563eb":"#60a5fa");gs98.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gs98;ctx.globalAlpha=0.85;
  ctx.beginPath();ctx.arc(0,0,r*scale,0,Math.PI*2);ctx.fill();
  // Lightning tendrils when grown
  if(isGrown){
    for(let lt=0;lt<4;lt++){
      const la=lt*Math.PI/2+ts*0.01;const ll=r*0.8;
      ctx.globalAlpha=0.4;ctx.strokeStyle="#93c5fd";ctx.lineWidth=r*0.04;
      ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(la)*ll*0.5,Math.sin(la)*ll*0.5);ctx.lineTo(Math.cos(la+0.3)*ll,Math.sin(la+0.3)*ll);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*scale*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isGrown?"🌩️":"🌱",0,0);
  ctx.restore();
}
// ── Warp Glyph — glowing rune; active glow phase = 3× ──
function drawWarpGlyph(ctx,r,ts,glowPct){
  ctx.save();
  const isGlow=glowPct>0.7;
  ctx.shadowBlur=isGlow?24:12;ctx.shadowColor=isGlow?"#c084fc":"#a78bfa";
  // Outer hex
  ctx.strokeStyle=isGlow?"#e879f9":"#a78bfa";ctx.lineWidth=r*0.06;ctx.globalAlpha=0.7;
  ctx.beginPath();
  for(let h=0;h<6;h++){const ha=h*Math.PI/3+ts*0.004;ctx.lineTo(Math.cos(ha)*r*0.8,Math.sin(ha)*r*0.8);}
  ctx.closePath();ctx.stroke();
  // Inner rune lines
  ctx.globalAlpha=0.5+glowPct*0.4;ctx.strokeStyle=isGlow?"#f0abfc":"#c4b5fd";ctx.lineWidth=r*0.04;
  [[0,0.5],[Math.PI/3,0.4],[2*Math.PI/3,0.4]].forEach(([a,rd])=>{
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*rd,Math.sin(a)*r*rd);ctx.stroke();
  });
  // Center orb
  const gg=ctx.createRadialGradient(0,0,0,0,0,r*0.25);
  gg.addColorStop(0,"#fff");gg.addColorStop(1,isGlow?"#e879f9":"#a78bfa");
  ctx.fillStyle=gg;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*0.25,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Ether Bolt — fast electric arc; tap 3 in a row = chain = 2× ──
function drawEtherBolt(ctx,r,ts,chainCount){
  ctx.save();
  const isChain=chainCount>=2;
  ctx.shadowBlur=isChain?22:14;ctx.shadowColor=isChain?"#38bdf8":"#67e8f9";
  // Electric arc body
  ctx.globalAlpha=0.85;
  const gBolt=ctx.createRadialGradient(0,0,0,0,0,r);
  gBolt.addColorStop(0,"#fff");gBolt.addColorStop(0.3,isChain?"#38bdf8":"#7dd3fc");gBolt.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gBolt;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Zigzag bolts
  for(let zz=0;zz<3;zz++){
    const za=zz*Math.PI*2/3+ts*0.02;
    ctx.globalAlpha=0.5;ctx.strokeStyle=isChain?"#7dd3fc":"#bae6fd";ctx.lineWidth=r*0.05;
    ctx.beginPath();ctx.moveTo(0,0);
    ctx.lineTo(Math.cos(za)*r*0.4,Math.sin(za)*r*0.4);
    ctx.lineTo(Math.cos(za+0.4)*r*0.7,Math.sin(za+0.4)*r*0.7);
    ctx.lineTo(Math.cos(za+0.2)*r*0.9,Math.sin(za+0.2)*r*0.9);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.48)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isChain?"⚡":"🌀",0,0);
  ctx.restore();
}
// ── Cosmic Dust — swirling dust cloud; peak density = 2× ──
function drawCosmicDust(ctx,r,ts,densityPct){
  ctx.save();
  const isDense=densityPct>0.7;
  ctx.shadowBlur=isDense?18:10;ctx.shadowColor=isDense?"#818cf8":"#c7d2fe";
  // Swirling dust particles
  const count96=isDense?12:6;
  for(let d=0;d<count96;d++){
    const da=(d/count96)*Math.PI*2+ts*0.007;const dr=r*(0.3+d%3*0.2);
    ctx.globalAlpha=(0.3+densityPct*0.5)*(1-d/count96);
    ctx.fillStyle=d%3===0?"#818cf8":d%3===1?"#c4b5fd":"#ddd6fe";
    ctx.beginPath();ctx.arc(Math.cos(da)*dr,Math.sin(da)*dr,r*0.08,0,Math.PI*2);ctx.fill();
  }
  // Core glow
  const gc=ctx.createRadialGradient(0,0,0,0,0,r*0.35);
  gc.addColorStop(0,"rgba(129,140,248,0.7)");gc.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gc;ctx.globalAlpha=0.6+densityPct*0.3;ctx.beginPath();ctx.arc(0,0,r*0.35,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.45)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isDense?"✨":"🌌",0,0);
  ctx.restore();
}
// ── Nova Bell — resonating bell; ring at resonance peak = 3× ──
function drawNovaBell(ctx,r,ts,resonancePct){
  ctx.save();
  const isRing=resonancePct>0.75;
  ctx.shadowBlur=isRing?24:14;ctx.shadowColor=isRing?"#fbbf24":"#fde68a";
  // Bell silhouette
  ctx.fillStyle=isRing?"#fef3c7":"#fef9c3";ctx.globalAlpha=0.85;
  ctx.beginPath();ctx.arc(0,-r*0.1,r*0.6,Math.PI,0);ctx.lineTo(r*0.3,r*0.4);ctx.arc(0,r*0.4,r*0.3,0,Math.PI);ctx.closePath();ctx.fill();
  // Resonance rings
  if(isRing){
    for(let ring96=1;ring96<=3;ring96++){
      ctx.globalAlpha=0.4-ring96*0.1;ctx.strokeStyle="#fbbf24";ctx.lineWidth=r*0.04;
      ctx.beginPath();ctx.arc(0,0,r*(0.8+ring96*0.2),0,Math.PI*2);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.48)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isRing?"🔔":"🔕",0,0);
  ctx.restore();
}
// ── Prismatic Eye — rotating iris with rainbow gaze; stare = 3× ──
function drawPrismaticEye(ctx,r,ts,gazePct){
  ctx.save();
  const isGaze=gazePct>0.7;
  ctx.shadowBlur=isGaze?24:14;ctx.shadowColor=isGaze?"#e879f9":"#c084fc";
  // Outer iris ring
  const colors95=["#ef4444","#f97316","#fbbf24","#4ade80","#38bdf8","#818cf8","#e879f9"];
  colors95.forEach((c,i)=>{
    const a=i*Math.PI*2/colors95.length+ts*0.006;
    ctx.globalAlpha=0.5;ctx.strokeStyle=c;ctx.lineWidth=r*0.09;
    ctx.beginPath();ctx.arc(Math.cos(a)*r*0.3,Math.sin(a)*r*0.3,r*0.12,0,Math.PI*2);ctx.stroke();
  });
  // Pupil
  ctx.globalAlpha=0.9;ctx.fillStyle=isGaze?"#7c3aed":"#1e1b4b";
  ctx.beginPath();ctx.arc(0,0,r*0.22,0,Math.PI*2);ctx.fill();
  // Highlight
  ctx.globalAlpha=0.7;ctx.fillStyle="#fff";
  ctx.beginPath();ctx.arc(-r*0.07,-r*0.07,r*0.07,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Thunder Crown — charging crown; full charge = 3× ──
function drawThunderCrown(ctx,r,ts,chargePct){
  ctx.save();
  const isFull=chargePct>0.85;
  ctx.shadowBlur=isFull?28:14;ctx.shadowColor=isFull?"#fef08a":"#fbbf24";
  // Crown shape (5 peaks)
  ctx.strokeStyle=isFull?"#fef08a":"#fbbf24";ctx.lineWidth=r*0.07;ctx.globalAlpha=0.9;
  ctx.fillStyle=`rgba(251,191,36,${0.25+chargePct*0.4})`;
  ctx.beginPath();
  const pts95=5;
  for(let c=0;c<pts95;c++){
    const a1=c*Math.PI*2/pts95-Math.PI/2;const a2=(c+0.5)*Math.PI*2/pts95-Math.PI/2;
    const x1=Math.cos(a1)*r*0.7;const y1=Math.sin(a1)*r*0.7;
    const x2=Math.cos(a2)*r*(0.3+chargePct*0.5);const y2=Math.sin(a2)*r*(0.3+chargePct*0.5);
    if(c===0)ctx.moveTo(x1,y1); else ctx.lineTo(x1,y1);
    ctx.lineTo(x2,y2);
  }
  ctx.closePath();ctx.fill();ctx.stroke();
  // Charge sparks
  if(chargePct>0.5){
    for(let s=0;s<3;s++){
      const sa=s*Math.PI*2/3+ts*0.02;const sl=r*(0.5+chargePct*0.5);
      ctx.globalAlpha=0.5*chargePct;ctx.strokeStyle="#fef08a";ctx.lineWidth=r*0.03;
      ctx.beginPath();ctx.moveTo(Math.cos(sa)*r*0.4,Math.sin(sa)*r*0.4);ctx.lineTo(Math.cos(sa)*sl,Math.sin(sa)*sl);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.45)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isFull?"⚡":"👑",0,0);
  ctx.restore();
}
// ── Solar Beam — collimated light beam; center dead-zone = 3× ──
function drawSolarBeam(ctx,r,ts){
  ctx.save();
  const angle=ts*0.004;
  ctx.rotate(angle);
  ctx.shadowBlur=20;ctx.shadowColor="#fef08a";
  // Core bright center
  const gc=ctx.createRadialGradient(0,0,0,0,0,r*0.25);
  gc.addColorStop(0,"#fff");gc.addColorStop(0.5,"#fef08a");gc.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gc;ctx.globalAlpha=0.95;ctx.beginPath();ctx.arc(0,0,r*0.25,0,Math.PI*2);ctx.fill();
  // Beam rays
  for(let b=0;b<6;b++){
    const ba=b*Math.PI/3;
    const grad=ctx.createLinearGradient(0,0,Math.cos(ba)*r,Math.sin(ba)*r);
    grad.addColorStop(0,"rgba(254,240,138,0.9)");grad.addColorStop(1,"rgba(254,240,138,0)");
    ctx.globalAlpha=0.5;ctx.strokeStyle=grad;ctx.lineWidth=r*0.07;
    ctx.beginPath();ctx.moveTo(Math.cos(ba)*r*0.25,Math.sin(ba)*r*0.25);ctx.lineTo(Math.cos(ba)*r,Math.sin(ba)*r);ctx.stroke();
  }
  ctx.restore();
}
// ── Ice Prism — diamond ice; tap in split phase (0.5–0.7) = 2× ──
function drawIcePrism(ctx,r,ts,splitPct){
  ctx.save();
  const isSplit=splitPct>0.5&&splitPct<0.7;
  ctx.shadowBlur=isSplit?22:14;ctx.shadowColor=isSplit?"#7dd3fc":"#bae6fd";
  // Diamond shape (rotated square)
  ctx.save();ctx.rotate(Math.PI/4+ts*0.005);
  const gp=ctx.createLinearGradient(-r,-r,r,r);
  gp.addColorStop(0,"#e0f2fe");gp.addColorStop(0.5,isSplit?"#38bdf8":"#bae6fd");gp.addColorStop(1,"#0369a1");
  ctx.fillStyle=gp;ctx.globalAlpha=0.85;
  ctx.fillRect(-r*0.7,-r*0.7,r*1.4,r*1.4);
  ctx.restore();
  // Rainbow split rays when in split phase
  if(isSplit){
    const colors=["#ef4444","#f97316","#fbbf24","#4ade80","#38bdf8","#818cf8"];
    colors.forEach((c,i)=>{
      const a=i*Math.PI/3+ts*0.003;
      ctx.globalAlpha=0.4;ctx.strokeStyle=c;ctx.lineWidth=r*0.06;
      ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*1.3,Math.sin(a)*r*1.3);ctx.stroke();
    });
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.45)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isSplit?"🌈":"🧊",0,0);
  ctx.restore();
}
// ── Star Pulse — rhythmic star that peaks in brightness; sync tap = 4× ──
function drawStarPulse(ctx,r,ts,pulsePct){
  ctx.save();
  const isPeak=pulsePct>0.88;
  const size=r*(0.7+pulsePct*0.4);
  ctx.shadowBlur=isPeak?30:14;ctx.shadowColor=isPeak?"#fef08a":"#fde68a";
  // 5-pointed star
  const pts93=5;
  ctx.beginPath();
  for(let s=0;s<pts93*2;s++){
    const a=s*Math.PI/pts93-Math.PI/2;
    const rad=s%2===0?size:size*0.4;
    if(s===0)ctx.moveTo(Math.cos(a)*rad,Math.sin(a)*rad);
    else ctx.lineTo(Math.cos(a)*rad,Math.sin(a)*rad);
  }
  ctx.closePath();
  const gStar=ctx.createRadialGradient(0,0,0,0,0,size);
  gStar.addColorStop(0,"#fff");gStar.addColorStop(0.5,isPeak?"#fef08a":"#fde68a");gStar.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gStar;ctx.globalAlpha=0.85+pulsePct*0.15;ctx.fill();
  if(isPeak){
    ctx.strokeStyle="#fbbf24";ctx.lineWidth=r*0.04;ctx.globalAlpha=0.6;ctx.stroke();
  }
  ctx.restore();
}
// ── Void Nova — collapsing star; tap before full collapse = 3× ──
function drawVoidNova(ctx,r,ts,collapsePct){
  ctx.save();
  const isBurst=collapsePct>0.75;
  const rr=r*(1.0-collapsePct*0.5); // shrinks toward center
  ctx.shadowBlur=isBurst?26:18;ctx.shadowColor=isBurst?"#6d28d9":"#a78bfa";
  const g=ctx.createRadialGradient(0,0,0,0,0,rr);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,isBurst?"#4c1d95":"#7c3aed");g.addColorStop(0.8,"#1e1b4b");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;
  ctx.beginPath();ctx.arc(0,0,rr,0,Math.PI*2);ctx.fill();
  // Collapse rings
  for(let rv=0;rv<3;rv++){
    const ringR=rr*(1.3+rv*0.3);
    ctx.globalAlpha=0.25-rv*0.05;ctx.strokeStyle="#a78bfa";ctx.lineWidth=r*0.04;
    ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(rr*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isBurst?"💥":"🌑",0,0);
  ctx.restore();
}
// ── Nebula Drop — small teardrop of nebula material; collect 5 for bonus ──
function drawNebulaDrop(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#818cf8";
  const hue=((ts*0.08)%360);
  const g=ctx.createRadialGradient(0,-r*0.1,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.35,`hsl(${hue},80%,75%)`);g.addColorStop(0.8,`hsl(${hue+30},70%,45%)`);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;
  // Teardrop shape
  ctx.beginPath();ctx.moveTo(0,-r);ctx.bezierCurveTo(r*0.7,-r*0.3,r*0.7,r*0.5,0,r);ctx.bezierCurveTo(-r*0.7,r*0.5,-r*0.7,-r*0.3,0,-r);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌌",0,r*0.05);
  ctx.restore();
}
// ── Pulse Gate — pulsing portal frame; open phase tap = 2× ──
function drawPulseGate(ctx,r,ts,openPct){
  ctx.save();
  const col=openPct>0.6?"#22d3ee":"#0f172a";
  ctx.shadowBlur=openPct>0.6?24:10;ctx.shadowColor=openPct>0.6?"#67e8f9":"#334155";
  // Gate frame (rectangle outline)
  const hw=r*0.75;const hh=r;
  ctx.strokeStyle=openPct>0.6?"#67e8f9":"#475569";
  ctx.lineWidth=r*0.18;ctx.globalAlpha=0.9;
  ctx.strokeRect(-hw,-hh,hw*2,hh*2);
  // Inner fill
  if(openPct>0.6){
    const gP=ctx.createRadialGradient(0,0,0,0,0,r*0.6);
    gP.addColorStop(0,`rgba(34,211,238,${(openPct-0.6)*2.5*0.7})`);gP.addColorStop(1,"rgba(0,0,0,0)");
    ctx.fillStyle=gP;ctx.globalAlpha=0.8;ctx.fillRect(-hw*0.8,-hh*0.8,hw*1.6,hh*1.6);
  }
  // Scanlines
  ctx.strokeStyle=openPct>0.6?"#a5f3fc":"#334155";ctx.lineWidth=1;ctx.globalAlpha=0.3;
  for(let l=-3;l<=3;l++){
    ctx.beginPath();ctx.moveTo(-hw,l*r*0.28);ctx.lineTo(hw,l*r*0.28);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(openPct>0.6?"🚪":"🌑",0,0);
  ctx.restore();
}
// ── Frost Gem — ice crystal gem; freezes nearby bombs on tap ──
function drawFrostGem(ctx,r,ts){
  ctx.save();
  ctx.rotate(ts*0.004);
  ctx.shadowBlur=18;ctx.shadowColor="#93c5fd";
  // Diamond shape
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(r*0.7,0);ctx.lineTo(0,r);ctx.lineTo(-r*0.7,0);ctx.closePath();
  const gFG=ctx.createLinearGradient(0,-r,0,r);
  gFG.addColorStop(0,"#fff");gFG.addColorStop(0.3,"#bfdbfe");gFG.addColorStop(0.7,"#3b82f6");gFG.addColorStop(1,"#1e40af");
  ctx.fillStyle=gFG;ctx.globalAlpha=0.9;ctx.fill();
  // Ice facets
  ctx.strokeStyle="#e0f2fe";ctx.lineWidth=1;ctx.globalAlpha=0.4;
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(0,r);ctx.stroke();
  ctx.beginPath();ctx.moveTo(-r*0.7,0);ctx.lineTo(r*0.7,0);ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💎",0,0);
  ctx.restore();
}
// ── Heat Orb — molten orb; during fever = 3× ──
function drawHeatOrb(ctx,r,ts,isFever){
  ctx.save();
  ctx.shadowBlur=isFever?28:14;ctx.shadowColor=isFever?"#ff6b6b":"#f97316";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  if(isFever){
    g.addColorStop(0,"#fff");g.addColorStop(0.25,"#fef08a");g.addColorStop(0.55,"#ef4444");g.addColorStop(0.85,"#7f1d1d");g.addColorStop(1,"rgba(0,0,0,0)");
  } else {
    g.addColorStop(0,"#fef08a");g.addColorStop(0.4,"#f97316");g.addColorStop(0.8,"#7c2d12");g.addColorStop(1,"rgba(0,0,0,0)");
  }
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Heat shimmer
  if(isFever){
    ctx.strokeStyle="#fde68a";ctx.lineWidth=1;ctx.globalAlpha=0.4+0.3*Math.sin(ts*0.03);
    for(let h=0;h<4;h++){
      const ha=h*(Math.PI/2)+ts*0.01;
      ctx.beginPath();ctx.moveTo(Math.cos(ha)*r*0.5,Math.sin(ha)*r*0.5);ctx.lineTo(Math.cos(ha)*r*1.05,Math.sin(ha)*r*1.05);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.52)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isFever?"🌟":"🔴",0,0);
  ctx.restore();
}
// ── Thunder Bead — electrified orb; 3 in a row = chain bonus ──
function drawThunderBead(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=20;ctx.shadowColor="#fde047";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.35,"#fef08a");g.addColorStop(0.75,"#eab308");g.addColorStop(1,"#713f12");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Electric zap lines
  ctx.strokeStyle="#fff";ctx.lineWidth=1.5;ctx.globalAlpha=0.7+0.3*Math.sin(ts*0.03);
  for(let z=0;z<3;z++){
    const a=z*(Math.PI*2/3)+ts*0.015;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*0.85,Math.sin(a)*r*0.85);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,0);
  ctx.restore();
}
// ── Mirror Star — 6-pointed star; reflects for bonus if streak ≥ 5 ──
function drawMirrorStar(ctx,r,ts,reflectPct){
  ctx.save();
  ctx.rotate(ts*0.006);
  ctx.shadowBlur=18;ctx.shadowColor=reflectPct>0?"#e2e8f0":"#cbd5e1";
  // 6-pointed star
  const outerR6=r;const innerR6=r*0.45;
  ctx.beginPath();
  for(let i=0;i<12;i++){
    const angle=i*(Math.PI/6)-Math.PI/2;
    const rad=i%2===0?outerR6:innerR6;
    if(i===0)ctx.moveTo(Math.cos(angle)*rad,Math.sin(angle)*rad);
    else ctx.lineTo(Math.cos(angle)*rad,Math.sin(angle)*rad);
  }
  ctx.closePath();
  const gS=ctx.createRadialGradient(0,0,0,0,0,r);
  if(reflectPct>0){
    gS.addColorStop(0,"#fff");gS.addColorStop(0.4,"#e2e8f0");gS.addColorStop(0.85,"#94a3b8");
  } else {
    gS.addColorStop(0,"#f1f5f9");gS.addColorStop(0.5,"#cbd5e1");gS.addColorStop(1,"#475569");
  }
  ctx.fillStyle=gS;ctx.globalAlpha=0.88;ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(reflectPct>0?"🪞":"⭐",0,0);
  ctx.restore();
}
// ── Aurora Wave — shimmering wave shape; crest peak = 2× ──
function drawAuroraWave(ctx,r,ts,crestPct){
  ctx.save();
  const isCrest=crestPct>0.75;
  ctx.shadowBlur=isCrest?24:12;ctx.shadowColor=isCrest?"#86efac":"#4ade80";
  // Wave body
  ctx.beginPath();
  const steps=20;
  for(let i=0;i<=steps;i++){
    const fx=(i/steps)*r*2-r;
    const wave=Math.sin((i/steps)*Math.PI*2+ts*0.015)*r*0.35*crestPct;
    if(i===0)ctx.moveTo(fx,wave);else ctx.lineTo(fx,wave);
  }
  for(let i=steps;i>=0;i--){
    const fx=(i/steps)*r*2-r;
    const wave=Math.sin((i/steps)*Math.PI*2+ts*0.015)*r*0.35*crestPct-r*0.3;
    ctx.lineTo(fx,wave);
  }
  ctx.closePath();
  const g=ctx.createLinearGradient(0,-r,0,r);
  g.addColorStop(0,isCrest?"#fff":"#86efac");g.addColorStop(0.5,isCrest?"#4ade80":"#22c55e");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.85;ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isCrest?"🌊":"〰️",0,-r*0.08);
  ctx.restore();
}
// ── Crystal Prism — triangular prism; tap = splits into 3 score fragments ──
function drawCrystalPrism(ctx,r,ts,fragCount){
  ctx.save();
  ctx.rotate(ts*0.005);
  ctx.shadowBlur=18;ctx.shadowColor="#67e8f9";
  // Triangle prism
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(r*0.85,r*0.5);ctx.lineTo(-r*0.85,r*0.5);ctx.closePath();
  const g=ctx.createLinearGradient(0,-r,0,r*0.5);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#a5f3fc");g.addColorStop(0.8,"#06b6d4");g.addColorStop(1,"#0e7490");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;ctx.fill();
  // Rainbow refraction lines
  const colors=["#f43f5e","#fb923c","#fbbf24","#4ade80","#60a5fa","#a78bfa"];
  ctx.lineWidth=1.5;ctx.globalAlpha=0.5;
  colors.forEach((c,i)=>{
    ctx.strokeStyle=c;ctx.beginPath();
    ctx.moveTo(r*0.85,r*0.5);ctx.lineTo((r*(1.1+i*0.15)),r*(0.5+i*0.18));ctx.stroke();
  });
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🔷",0,r*0.1);
  ctx.restore();
}
// ── Plasma Ring — spinning colored ring; inner zone tap = 2× ──
function drawPlasmaRing(ctx,r,ts,spinAngle){
  ctx.save();
  ctx.rotate(spinAngle);
  const hue=((ts*0.15)%360);
  ctx.shadowBlur=20;ctx.shadowColor=`hsl(${hue},90%,60%)`;
  // Outer ring arc
  ctx.strokeStyle=`hsl(${hue},90%,65%)`;ctx.lineWidth=r*0.22;ctx.globalAlpha=0.88;
  ctx.beginPath();ctx.arc(0,0,r*0.72,0,Math.PI*2);ctx.stroke();
  // Inner glow area
  const gI=ctx.createRadialGradient(0,0,0,0,0,r*0.45);
  gI.addColorStop(0,`hsla(${hue},80%,80%,0.5)`);gI.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=gI;ctx.globalAlpha=0.6;ctx.beginPath();ctx.arc(0,0,r*0.45,0,Math.PI*2);ctx.fill();
  // Small center dot
  ctx.fillStyle=`hsl(${hue},100%,85%)`;ctx.globalAlpha=0.9;ctx.shadowBlur=10;
  ctx.beginPath();ctx.arc(0,0,r*0.14,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Void Star — 5-pointed star; core center tap = 3× ──
function drawVoidStar(ctx,r,ts){
  ctx.save();
  ctx.rotate(ts*0.004);
  ctx.shadowBlur=16;ctx.shadowColor="#7c3aed";
  // Star shape
  const outerR=r;const innerR=r*0.42;const pts5=5;
  ctx.beginPath();
  for(let i=0;i<pts5*2;i++){
    const angle=(i*Math.PI/pts5)-Math.PI/2;
    const rad=i%2===0?outerR:innerR;
    if(i===0)ctx.moveTo(Math.cos(angle)*rad,Math.sin(angle)*rad);
    else ctx.lineTo(Math.cos(angle)*rad,Math.sin(angle)*rad);
  }
  ctx.closePath();
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#c4b5fd");g.addColorStop(0.5,"#7c3aed");g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;ctx.fill();
  // Core pulse
  const cp=0.7+0.3*Math.sin(ts*0.016);
  ctx.globalAlpha=cp;ctx.fillStyle="#fff";ctx.shadowBlur=18;ctx.shadowColor="#fff";
  ctx.beginPath();ctx.arc(0,0,r*0.16,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Energy Pulse — charges up over time; max charge = max pts ──
function drawEnergyPulse(ctx,r,ts,chargePct){
  ctx.save();
  const col=`hsl(${200+chargePct*40},90%,${55+chargePct*15}%)`;
  ctx.shadowBlur=10+chargePct*18;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.7+chargePct*0.25;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Charge ring
  if(chargePct>0.05){
    ctx.strokeStyle=col;ctx.lineWidth=3;ctx.globalAlpha=chargePct;
    ctx.beginPath();ctx.arc(0,0,r*1.15,0,Math.PI*2*chargePct);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,0);
  ctx.restore();
}
// ── Solar Petal — spinning sun-ray petals; always worth tapping, chains for +pts ──
function drawSolarPetal(ctx,r,ts){
  ctx.save();
  const RAYS=8;
  ctx.shadowBlur=20;ctx.shadowColor="#fbbf24";
  for(let i=0;i<RAYS;i++){
    const angle=(i/RAYS)*Math.PI*2+ts*0.006;
    const len=r*(0.7+0.3*Math.abs(Math.sin(ts*0.02+i)));
    ctx.strokeStyle=`hsl(${40+i*5},100%,65%)`;ctx.lineWidth=3;ctx.globalAlpha=0.6;
    ctx.beginPath();ctx.moveTo(Math.cos(angle)*r*0.3,Math.sin(angle)*r*0.3);
    ctx.lineTo(Math.cos(angle)*len,Math.sin(angle)*len);ctx.stroke();
  }
  const cg=ctx.createRadialGradient(0,0,0,0,0,r*0.4);
  cg.addColorStop(0,"#fff");cg.addColorStop(1,"#fbbf24");
  ctx.fillStyle=cg;ctx.globalAlpha=0.95;ctx.beginPath();ctx.arc(0,0,r*0.4,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.4)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("☀️",0,0);
  ctx.restore();
}
// ── Cosmic Shard — cycling through 3 color states; tap all 3 = align bonus ──
function drawCosmicShard(ctx,r,ts,colorIdx){
  ctx.save();
  const COLS=["#f43f5e","#8b5cf6","#0ea5e9"];
  const col=COLS[colorIdx%3];
  ctx.shadowBlur=16;ctx.shadowColor=col;
  // Diamond shape
  ctx.save();ctx.rotate(ts*0.005);
  const sg=ctx.createLinearGradient(-r,-r,r,r);
  sg.addColorStop(0,"#fff");sg.addColorStop(0.5,col);sg.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=sg;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(r*0.6,0);ctx.lineTo(0,r);ctx.lineTo(-r*0.6,0);ctx.closePath();ctx.fill();
  ctx.restore();
  // Color indicator dots
  for(let c=0;c<3;c++){
    ctx.fillStyle=COLS[c];ctx.globalAlpha=c===colorIdx%3?1:0.3;
    ctx.beginPath();ctx.arc((c-1)*r*0.45,r*1.15,3,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=0.85;ctx.font=`${Math.round(r*0.48)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💠",0,0);
  ctx.restore();
}
// ── Crystal Wing — butterfly wings; left+right pair must be tapped within 2.5s ──
function drawCrystalWing(ctx,r,ts,isRight,partnerTapped){
  ctx.save();
  const col=isRight?"#818cf8":"#38bdf8";
  const pulse=0.9+0.1*Math.sin(ts*0.014+(isRight?Math.PI:0));
  ctx.shadowBlur=14;ctx.shadowColor=col;
  // Wing shape (ellipse tilted)
  ctx.save();ctx.rotate(isRight?0.5:-0.5);
  const wg=ctx.createRadialGradient(0,0,0,0,0,r);
  wg.addColorStop(0,"rgba(255,255,255,0.9)");wg.addColorStop(0.5,col+"cc");wg.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=wg;ctx.globalAlpha=0.85*pulse;
  ctx.beginPath();ctx.ellipse(isRight?r*0.2:-r*0.2,0,r*0.6,r*0.9,0,0,Math.PI*2);ctx.fill();
  ctx.restore();
  // Facet lines
  ctx.strokeStyle="#fff";ctx.lineWidth=1;ctx.globalAlpha=0.25;
  for(let f=0;f<3;f++){
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(f*1.2+(isRight?0:Math.PI))*r*0.7,Math.sin(f*1.2)*r*0.7);ctx.stroke();
  }
  ctx.globalAlpha=0.85;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💎",0,0);
  ctx.restore();
}
// ── Storm Cloud — thunderstorm target; bolt flash every ~1.5s = 2× ──
function drawStormCloud(ctx,r,ts,boltActive){
  ctx.save();
  const col=boltActive?"#fbbf24":"#64748b";
  ctx.shadowBlur=boltActive?22:10;ctx.shadowColor=boltActive?"#fde68a":"#94a3b8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,boltActive?"#fff":"#e2e8f0");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Rain drops
  if(!boltActive){
    ctx.strokeStyle="#93c5fd";ctx.lineWidth=1;ctx.globalAlpha=0.4;
    for(let d=0;d<4;d++){
      const dx=(d-1.5)*r*0.35;
      ctx.beginPath();ctx.moveTo(dx,r*0.2);ctx.lineTo(dx-r*0.08,r*0.55);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(boltActive?"⚡":"⛈️",0,0);
  ctx.restore();
}
// ── Arc Node — electric tap target; arcs to nearby targets on tap ──
function drawArcNode(ctx,r,ts){
  ctx.save();
  const col="#3b82f6";
  ctx.shadowBlur=18;ctx.shadowColor="#93c5fd";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#60a5fa");g.addColorStop(0.85,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Electric arcs
  ctx.strokeStyle="#bfdbfe";ctx.lineWidth=1.5;ctx.globalAlpha=0.6+0.3*Math.sin(ts*0.025);
  for(let a=0;a<4;a++){
    const angle=a*Math.PI/2+ts*0.01;
    ctx.beginPath();ctx.moveTo(0,0);
    ctx.lineTo(Math.cos(angle)*r*0.85+Math.sin(ts*0.1+a)*4,Math.sin(angle)*r*0.85+Math.cos(ts*0.1+a)*4);
    ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,0);
  ctx.restore();
}
// ── Prismatic Burst — 5 sequential tap zones (rainbow colors); hit all = jackpot ──
function drawPrismaticBurst(ctx,r,ts,phase){
  ctx.save();
  const COLORS=["#ef4444","#f97316","#eab308","#22c55e","#8b5cf6"];
  const col=COLORS[phase%5];
  ctx.shadowBlur=16;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9+0.08*Math.sin(ts*0.02);
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Phase indicator dots
  for(let p=0;p<5;p++){
    const a=-Math.PI/2+p*(Math.PI*2/5);
    ctx.fillStyle=p===phase%5?"#fff":COLORS[p];ctx.globalAlpha=p===phase%5?1:0.35;
    ctx.beginPath();ctx.arc(Math.cos(a)*r*1.2,Math.sin(a)*r*1.2,3,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌈",0,0);
  ctx.restore();
}
// ── Moon Orb — cycles through moon phases; full moon = 2× score ──
function drawMoonOrb(ctx,r,ts,phasePct){
  ctx.save();
  const isFull=phasePct>0.85;
  const col=isFull?"#fde68a":"#94a3b8";
  const glow=isFull?"#fef3c7":"#cbd5e1";
  ctx.shadowBlur=isFull?22:10;ctx.shadowColor=glow;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.5+phasePct*0.45;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Moon phase shadow overlay
  if(!isFull){
    const shadowX=r*(1-2*phasePct); // shifts right→left as it "fills"
    ctx.fillStyle="#0f172a";ctx.globalAlpha=0.55;
    ctx.save();ctx.beginPath();ctx.arc(shadowX,0,r*0.95,0,Math.PI*2);ctx.fill();ctx.restore();
  }
  ctx.globalAlpha=isFull?1:0.8;ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isFull?"🌕":"🌙",0,0);
  ctx.restore();
}
// ── Lava Core — glowing molten target; chain 3 in sequence for bonus ──
function drawLavaCore(ctx,r,ts){
  ctx.save();
  const heat=0.7+0.3*Math.sin(ts*0.016);
  const col=`hsl(${10+heat*20},100%,${48+heat*12}%)`;
  ctx.shadowBlur=16+heat*12;ctx.shadowColor="#f97316";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff7ed");g.addColorStop(0.3,"#fbbf24");g.addColorStop(0.7,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Lava cracks
  ctx.strokeStyle="#ef4444";ctx.lineWidth=1.5;ctx.globalAlpha=0.4+0.2*Math.sin(ts*0.02);
  for(let c=0;c<4;c++){
    const ca=c*Math.PI/2+ts*0.003;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(ca)*r*0.7,Math.sin(ca)*r*0.7);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌋",0,0);
  ctx.restore();
}
// ── Sun Petal — flowering target; bloom level determines score tier ──
function drawSunPetal(ctx,r,ts,bloomPct){
  ctx.save();
  const petalCol=`hsl(${40+bloomPct*20},100%,${60+bloomPct*10}%)`;
  const centerCol=`hsl(${50},100%,75%)`;
  ctx.shadowBlur=14+bloomPct*12;ctx.shadowColor=petalCol;
  const PETALS=6;
  ctx.globalAlpha=0.85;
  for(let p=0;p<PETALS;p++){
    const angle=(p/PETALS)*Math.PI*2+ts*0.004;
    const pr=r*(0.5+bloomPct*0.45);
    ctx.save();ctx.rotate(angle);
    const pg=ctx.createRadialGradient(0,-pr*0.5,0,0,-pr*0.5,pr*0.6);
    pg.addColorStop(0,centerCol);pg.addColorStop(1,"rgba(0,0,0,0)");
    ctx.fillStyle=pg;ctx.beginPath();ctx.ellipse(0,-pr*0.5,pr*0.28,pr*0.55,0,0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
  // center
  const cg=ctx.createRadialGradient(0,0,0,0,0,r*0.35);
  cg.addColorStop(0,"#fff");cg.addColorStop(1,centerCol);
  ctx.fillStyle=cg;ctx.globalAlpha=0.95;ctx.beginPath();ctx.arc(0,0,r*0.35,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=bloomPct>0.85?1:0.7;ctx.font=`${Math.round(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌸",0,0);
  ctx.restore();
}
// ── Ghost Bell — rings out; silent phase (low alpha) = 3× crit ──
function drawGhostBell(ctx,r,ts,alpha,isSilent){
  ctx.save();
  const col=isSilent?"#94a3b8":"#fde68a";
  ctx.shadowBlur=isSilent?6:16;ctx.shadowColor=col;
  ctx.globalAlpha=(isSilent?0.35:0.88)*alpha;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  if(isSilent){
    ctx.strokeStyle="#94a3b8";ctx.lineWidth=2;ctx.globalAlpha=0.45*alpha;
    ctx.beginPath();ctx.arc(0,0,r*1.25,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=(isSilent?0.5:0.9)*alpha;
  ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🔔",0,0);
  ctx.restore();
}
// ── Plasma Disk — spinning disc with hot edge ring; edge tap = 2× ──
function drawPlasmaDisk(ctx,r,ts,spinAngle){
  ctx.save();
  const col="#f97316";const edgeCol="#fde68a";
  ctx.rotate(spinAngle);
  ctx.shadowBlur=18;ctx.shadowColor=col;
  // Disk body
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff8f0");g.addColorStop(0.5,"#fb923c");g.addColorStop(0.85,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Hot edge ring
  ctx.strokeStyle=edgeCol;ctx.lineWidth=3.5;ctx.globalAlpha=0.75+0.2*Math.abs(Math.sin(ts*0.02));
  ctx.shadowColor=edgeCol;ctx.shadowBlur=14;
  ctx.beginPath();ctx.arc(0,0,r*0.9,0,Math.PI*2);ctx.stroke();
  // Center icon
  ctx.rotate(-spinAngle);
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💿",0,0);
  ctx.restore();
}
// ── Void Ring — ring-shaped target; hit through center gap = 2× ──
function drawVoidRing(ctx,r,ts,ringPct){
  ctx.save();
  const col="#7c3aed";const innerCol="#1e1b4b";
  const pulse=0.93+0.07*Math.sin(ts*0.01);
  ctx.shadowBlur=16;ctx.shadowColor=col;
  // Outer ring body (donut)
  ctx.strokeStyle=col;ctx.lineWidth=r*0.45;ctx.globalAlpha=0.88;
  ctx.shadowBlur=18;
  ctx.beginPath();ctx.arc(0,0,r*0.65*pulse,0,Math.PI*2);ctx.stroke();
  // Inner void (dark center)
  ctx.fillStyle=innerCol;ctx.globalAlpha=0.95;
  ctx.beginPath();ctx.arc(0,0,r*0.28*pulse,0,Math.PI*2);ctx.fill();
  // Orbiting spark
  const sparkA=ts*0.014;
  ctx.fillStyle="#a78bfa";ctx.globalAlpha=0.8;ctx.shadowBlur=8;ctx.shadowColor="#a78bfa";
  ctx.beginPath();ctx.arc(Math.cos(sparkA)*r*0.65,Math.sin(sparkA)*r*0.65,3.5,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.4)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⭕",0,0);
  ctx.restore();
}
// ── Omega Pulse — giant slow vanishing target; massive points if caught ──
function drawOmegaPulse(ctx,r,ts,alphaFade){
  ctx.save();
  const pulse=0.95+0.05*Math.sin(ts*0.008);
  ctx.shadowBlur=30*alphaFade;ctx.shadowColor="#7c3aed";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,"#a78bfa");g.addColorStop(0.7,"#7c3aed");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9*alphaFade;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Ω symbol
  ctx.globalAlpha=alphaFade;ctx.fillStyle="#fff";
  ctx.font=`bold ${Math.round(r*0.7)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("Ω",0,0);
  // Pulsing rings
  for(let ri=0;ri<3;ri++){
    const ringR=r*(1.1+ri*0.15);
    ctx.strokeStyle="#a78bfa";ctx.lineWidth=1.5;ctx.globalAlpha=alphaFade*0.3;
    ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
  }
  ctx.restore();
}
// ── Spiral Orb74 — moves in a spiral; tap at center zone = 2× ──
function drawSpiralOrb74(ctx,r,ts,atCenter){
  ctx.save();
  const col=atCenter?"#fbbf24":"#22d3ee";
  const pulse=0.9+0.1*Math.sin(ts*0.015);
  ctx.shadowBlur=14;ctx.shadowColor=col;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.5,col);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Spiral trail hint
  if(atCenter){
    ctx.strokeStyle="#fde68a";ctx.lineWidth=2;ctx.globalAlpha=0.6;
    ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌀",0,0);
  ctx.restore();
}
// ── Frost Nova — freezes all targets in AoE when tapped ──
function drawFrostNova(ctx,r,ts){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.012);
  ctx.shadowBlur=18;ctx.shadowColor="#93c5fd";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#93c5fd");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Ice crystal spikes
  ctx.strokeStyle="#bfdbfe";ctx.lineWidth=1.5;ctx.globalAlpha=0.7;
  for(let ii=0;ii<6;ii++){
    const a=ii*(Math.PI/3)+ts*0.004;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*0.9,Math.sin(a)*r*0.9);ctx.stroke();
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.5,Math.sin(a)*r*0.5);ctx.lineTo(Math.cos(a+0.3)*r*0.7,Math.sin(a+0.3)*r*0.7);ctx.stroke();
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("❄️",0,0);
  ctx.restore();
}
// ── Light Bomb — safe bomb variant; always scoreable, never dangerous ──
function drawLightBomb(ctx,r,ts){
  ctx.save();
  const pulse=0.92+0.08*Math.sin(ts*0.015);
  ctx.shadowBlur=16;ctx.shadowColor="#fde68a";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,"#fbbf24");g.addColorStop(0.8,"#f59e0b");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Soft glow rings
  ctx.strokeStyle="#fde68a";ctx.lineWidth=1.5;ctx.globalAlpha=0.4+0.2*Math.sin(ts*0.018);
  ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2);ctx.stroke();
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.6)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💡",0,0);
  ctx.restore();
}
// ── Gravity Node — dark singularity that pulls nearby targets; release = score all ──
function drawGravityNode(ctx,r,ts,pulledCount){
  ctx.save();
  const pulse=0.92+0.08*Math.sin(ts*0.014);
  ctx.shadowBlur=24+pulledCount*4;ctx.shadowColor="#1e1b4b";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#4c1d95");g.addColorStop(0.5,"#1e1b4b");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Pull rings
  for(let ring=1;ring<=3;ring++){
    const rr=r*(0.9+ring*0.35)*pulse;
    ctx.strokeStyle=`rgba(139,92,246,${0.4-ring*0.1})`;ctx.lineWidth=1;ctx.setLineDash([3,4]);
    ctx.beginPath();ctx.arc(0,0,rr,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);
  }
  if(pulledCount>0){
    ctx.fillStyle="#fbbf24";ctx.font=`bold ${Math.round(r*0.35)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText(`×${pulledCount}`,0,r*0.5);
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌑",0,-r*0.1);
  ctx.restore();
}
// ── Lightning Cage — electric perimeter; score per target inside ──
function drawLightningCage(ctx,r,ts,trapped){
  ctx.save();
  const spin=ts*0.007;
  ctx.shadowBlur=18;ctx.shadowColor="#fbbf24";
  // Outer cage ring
  ctx.strokeStyle="#fbbf24";ctx.lineWidth=2.5;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();
  // Lightning bolts on perimeter
  for(let i=0;i<6;i++){
    const a=spin+(i/6)*Math.PI*2;
    ctx.save();ctx.rotate(a);
    ctx.strokeStyle="#fde68a";ctx.lineWidth=1.5;ctx.globalAlpha=0.6+0.3*Math.sin(ts*0.02+i);
    ctx.beginPath();ctx.moveTo(r*0.75,0);ctx.lineTo(r*0.85,-r*0.08);ctx.lineTo(r*0.9,0);ctx.lineTo(r*0.85,r*0.08);ctx.lineTo(r,0);ctx.stroke();
    ctx.restore();
  }
  // Inner indicator
  ctx.fillStyle="#1c1917";ctx.globalAlpha=0.6;ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.fill();
  if(trapped>0){
    ctx.fillStyle="#fbbf24";ctx.globalAlpha=1;ctx.font=`bold ${Math.round(r*0.38)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText(`×${trapped}`,0,r*0.22);
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.55)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,-r*0.15);
  ctx.restore();
}
// ── Nebula Crystal — 3-phase crack target; each hit dims; 3rd hit = jackpot ──
function drawNebulaCrystal(ctx,r,ts,phase){
  ctx.save();
  const colors53=["#7e22ce","#5b21b6","#3730a3"];
  const col53=colors53[Math.min(phase,2)];
  ctx.shadowBlur=22-phase*5;ctx.shadowColor=col53;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#faf5ff");g.addColorStop(0.4,col53);g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.globalAlpha=1-phase*0.25;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Crack lines per phase
  ctx.strokeStyle=`rgba(255,255,255,${0.6-phase*0.15})`;ctx.lineWidth=1.5;
  for(let i=0;i<phase+1;i++){
    const a=(i/3)*Math.PI*2+Math.PI*0.1*i;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);ctx.stroke();
  }
  const emojis=["💜","💙","⚡"];
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(emojis[Math.min(phase,2)],0,1);
  ctx.restore();
}
// ── Time Shard — blinks visible/invisible; tap during visible = 2× pts ──
function drawTimeShard(ctx,r,ts,visiblePct){
  ctx.save();
  ctx.globalAlpha=visiblePct;
  ctx.shadowBlur=16*visiblePct;ctx.shadowColor="#818cf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#e0e7ff");g.addColorStop(0.5,"#818cf8");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Clock tick marks
  ctx.strokeStyle="rgba(199,210,254,0.6)";ctx.lineWidth=1.5;
  for(let i=0;i<12;i++){
    const a=(i/12)*Math.PI*2;
    const inner=i%3===0?r*0.6:r*0.75;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*inner,Math.sin(a)*inner);ctx.lineTo(Math.cos(a)*r*0.9,Math.sin(a)*r*0.9);ctx.stroke();
  }
  ctx.font=`${Math.round(r*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⏳",0,1);
  ctx.restore();
}
// ── Flux Ring — expanding ring; thick=high pts, thin=low; tap early! ──
function drawFluxRing(ctx,r,ts,agePct){
  ctx.save();
  const thickness=Math.max(2,(1-agePct)*r*0.6+2);
  const outerR=r*(0.4+agePct*0.8);
  ctx.shadowBlur=12+thickness;ctx.shadowColor="#06b6d4";
  ctx.strokeStyle=agePct<0.5?"#67e8f9":"#0e7490";
  ctx.lineWidth=thickness;ctx.globalAlpha=Math.max(0.3,1-agePct*0.8);
  ctx.beginPath();ctx.arc(0,0,outerR,0,Math.PI*2);ctx.stroke();
  // Inner highlight ring
  ctx.strokeStyle="rgba(224,242,254,0.5)";ctx.lineWidth=1;ctx.shadowBlur=6;
  ctx.beginPath();ctx.arc(0,0,outerR*0.8,0,Math.PI*2);ctx.stroke();
  // Early tap pulse indicator
  if(agePct<0.3){
    ctx.strokeStyle="#fde68a";ctx.lineWidth=2;ctx.globalAlpha=0.7*(1-agePct/0.3);
    ctx.setLineDash([4,4]);ctx.beginPath();ctx.arc(0,0,outerR*1.15,0,Math.PI*2);ctx.stroke();
    ctx.setLineDash([]);
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.5)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⭕",0,1);
  ctx.restore();
}
// ── Quantum Flare — spawns 3 decoys on tap; only real one earns full pts ──
function drawQuantumFlare(ctx,r,ts,isDecoy){
  ctx.save();
  const spin=ts*0.011;
  const pulse=0.92+0.08*Math.sin(ts*0.016);
  if(isDecoy){
    ctx.globalAlpha=0.45;ctx.shadowBlur=8;ctx.shadowColor="#818cf8";
    const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
    g.addColorStop(0,"#c7d2fe");g.addColorStop(1,"rgba(99,102,241,0)");
    ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
    ctx.strokeStyle="rgba(165,180,252,0.5)";ctx.lineWidth=1.5;
    ctx.beginPath();ctx.arc(0,0,r*0.85,0,Math.PI*2);ctx.stroke();
  } else {
    ctx.shadowBlur=20;ctx.shadowColor="#f0abfc";
    const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
    g.addColorStop(0,"#fff");g.addColorStop(0.35,"#f0abfc");g.addColorStop(0.8,"#7e22ce");g.addColorStop(1,"rgba(0,0,0,0)");
    ctx.fillStyle=g;ctx.globalAlpha=0.95;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
    for(let i=0;i<5;i++){
      const a=spin+(i/5)*Math.PI*2;
      ctx.fillStyle="#f0abfc";ctx.globalAlpha=0.65;ctx.shadowBlur=6;
      ctx.beginPath();ctx.arc(Math.cos(a)*r*1.2,Math.sin(a)*r*1.2,r*0.15,0,Math.PI*2);ctx.fill();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isDecoy?"❓":"🌟",0,1);
  ctx.restore();
}
// ── Crystal Pulsar — pulses outward rhythmically; peak pulse = legendary pts ──
function drawCrystalPulsar(ctx,r,ts,pulsePct){
  ctx.save();
  const pulse=0.8+0.4*Math.sin(ts*0.022);
  ctx.shadowBlur=20+pulse*14;ctx.shadowColor="#38bdf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#e0f2fe");g.addColorStop(0.4,"#38bdf8");g.addColorStop(0.8,"#0c4a6e");g.addColorStop(1,"rgba(0,0,0,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Outer pulse ring
  ctx.strokeStyle=`rgba(56,189,248,${0.5+pulsePct*0.5})`;ctx.lineWidth=2+pulsePct*3;
  ctx.globalAlpha=0.8;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.stroke();
  // 6 crystal spikes at peak
  if(pulsePct>0.7){
    ctx.globalAlpha=pulsePct;
    for(let i=0;i<6;i++){
      const a=(i/6)*Math.PI*2+ts*0.005;
      ctx.strokeStyle="#e0f2fe";ctx.lineWidth=1.5;
      ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.5,Math.sin(a)*r*0.5);
      ctx.lineTo(Math.cos(a)*r*1.3*pulse,Math.sin(a)*r*1.3*pulse);ctx.stroke();
    }
  }
  ctx.globalAlpha=0.9;ctx.font=`${Math.round(r*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💎",0,1);
  ctx.restore();
}
// ── Shadow Echo — dark orb; 1.5s after spawn releases a bright echo ──
function drawShadowEcho(ctx,r,ts,isEcho){
  ctx.save();
  if(isEcho){
    ctx.shadowBlur=22;ctx.shadowColor="#fbbf24";
    const g=ctx.createRadialGradient(0,0,0,0,0,r);
    g.addColorStop(0,"#fff");g.addColorStop(0.5,"#fbbf24");g.addColorStop(1,"rgba(251,191,36,0)");
    ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
    ctx.font=`${Math.round(r*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.globalAlpha=0.9;ctx.fillText("🌕",0,1);
  } else {
    ctx.shadowBlur=14;ctx.shadowColor="#1e1b4b";
    const g=ctx.createRadialGradient(0,0,0,0,0,r);
    g.addColorStop(0,"#312e81");g.addColorStop(0.6,"#0f172a");g.addColorStop(1,"rgba(0,0,0,0)");
    ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
    ctx.strokeStyle="rgba(99,102,241,0.6)";ctx.lineWidth=1.5;
    ctx.beginPath();ctx.arc(0,0,r*0.8,0,Math.PI*2);ctx.stroke();
    ctx.font=`${Math.round(r*0.7)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.globalAlpha=0.9;ctx.fillText("🌒",0,1);
  }
  ctx.restore();
}
// ── Bounce Web — purple sticky web snares nearby moving targets ──
function drawBounceWeb(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor="#a855f7";
  const pulse=0.93+0.07*Math.sin(ts*0.012);
  // Web base circle
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#3b1a5a");g.addColorStop(0.6,"#6b21a8");g.addColorStop(1,"rgba(88,28,135,0)");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Spoke lines
  ctx.strokeStyle="rgba(192,132,252,0.75)";ctx.lineWidth=1.5;
  for(let i=0;i<8;i++){
    const a=(i/8)*Math.PI*2;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*pulse,Math.sin(a)*r*pulse);ctx.stroke();
  }
  // Concentric web rings
  for(let ring=1;ring<=3;ring++){
    const rr=r*pulse*(ring/3);
    ctx.beginPath();ctx.strokeStyle=`rgba(192,132,252,${0.6-ring*0.15})`;ctx.lineWidth=1;
    for(let i=0;i<8;i++){
      const a0=(i/8)*Math.PI*2,a1=((i+1)/8)*Math.PI*2;
      i===0?ctx.moveTo(Math.cos(a0)*rr,Math.sin(a0)*rr):null;
      ctx.lineTo(Math.cos(a0)*rr,Math.sin(a0)*rr);
      ctx.bezierCurveTo(Math.cos((a0+a1)*0.5)*rr*1.1,Math.sin((a0+a1)*0.5)*rr*1.1,
        Math.cos((a0+a1)*0.5)*rr*1.1,Math.sin((a0+a1)*0.5)*rr*1.1,
        Math.cos(a1)*rr,Math.sin(a1)*rr);
    }
    ctx.closePath();ctx.stroke();
  }
  ctx.font=`${Math.round(r*0.65)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.9;ctx.fillText("🕸️",0,1);
  ctx.restore();
}
// ── Omega Orb — rare, always legendary; visual spectacle + 3000pts ──
function drawOmegaOrb(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=30;ctx.shadowColor="#ffd700";
  const spin=ts*0.009;
  const pulse=0.9+0.1*Math.sin(ts*0.015);
  // Multi-layer gradient body
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.2,"#fef9c3");g.addColorStop(0.5,"#ffd700");
  g.addColorStop(0.8,"#b45309");g.addColorStop(1,"#1c1c1c");
  ctx.fillStyle=g;ctx.globalAlpha=0.95;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Outer rotating ring of diamonds
  for(let i=0;i<8;i++){
    const a=spin+(i/8)*Math.PI*2;
    const dx=Math.cos(a)*r*1.18,dy=Math.sin(a)*r*1.18;
    ctx.save();ctx.translate(dx,dy);ctx.rotate(a+Math.PI/4);
    ctx.fillStyle="#ffd700";ctx.globalAlpha=0.8;ctx.shadowBlur=8;ctx.shadowColor="#ffd700";
    ctx.beginPath();ctx.rect(-r*0.1,-r*0.1,r*0.2,r*0.2);ctx.fill();
    ctx.restore();
  }
  // Inner rotating rings
  for(let ring=0;ring<2;ring++){
    ctx.strokeStyle=ring===0?"#ffd700":"#fff";ctx.lineWidth=ring===0?3:1.5;
    ctx.globalAlpha=0.5-ring*0.15;ctx.shadowBlur=10;ctx.shadowColor="#ffd700";
    ctx.beginPath();ctx.arc(0,0,r*(0.7-ring*0.2),0,Math.PI*2);ctx.stroke();
  }
  // Omega symbol
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.65)}px serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.shadowBlur=8;ctx.shadowColor="#ffd700";
  ctx.fillText("Ω",0,2);
  ctx.restore();
}
// ── Thunder Ball — charges nearby targets; charged targets give 2× for 2s ──
function drawThunderBall(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=20;ctx.shadowColor="#fbbf24";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fef9c3");g.addColorStop(0.4,"#f59e0b");g.addColorStop(1,"#451a03");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Lightning bolts
  for(let i=0;i<3;i++){
    const a=(i/3)*Math.PI*2+ts*0.005;
    ctx.save();ctx.rotate(a);ctx.strokeStyle="#fff";ctx.lineWidth=2;ctx.globalAlpha=0.8;ctx.shadowBlur=8;ctx.shadowColor="#fbbf24";
    ctx.beginPath();ctx.moveTo(0,-r*0.9);ctx.lineTo(r*0.1,-r*0.45);ctx.lineTo(-r*0.1,-r*0.45);ctx.lineTo(0,0);ctx.stroke();
    ctx.restore();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.6)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("⚡",0,1);
  ctx.restore();
}
// ── Cosmic Aura — large aura target; tap = 300pts + LUCKY 5s ──
function drawCosmicAura(ctx,r,ts){
  ctx.save();
  const pulse=0.88+0.12*Math.sin(ts*0.008);
  ctx.shadowBlur=22;ctx.shadowColor="#818cf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#e0e7ff");g.addColorStop(0.3,"#6366f1");g.addColorStop(0.7,"#1e1b4b");g.addColorStop(1,"#000");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Aura rings
  for(let i=0;i<3;i++){
    const rr=r*(1.1+i*0.15)+Math.sin(ts*0.008+i*1.2)*r*0.06;
    ctx.strokeStyle=`hsl(${240-i*20},80%,${65+i*10}%)`;ctx.lineWidth=1.5-i*0.3;ctx.globalAlpha=0.35-i*0.08;
    ctx.beginPath();ctx.arc(0,0,rr,0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.6)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#e0e7ff";ctx.shadowBlur=6;ctx.shadowColor="#818cf8";
  ctx.fillText("🌌",0,1);
  ctx.restore();
}
// ── Combo Rush — pts directly tied to current combo streak ──
function drawComboRush(ctx,r,ts,streakVal){
  const isRush=streakVal>=30;
  ctx.save();
  ctx.shadowBlur=isRush?28:14;ctx.shadowColor=isRush?"#ef4444":"#fb923c";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,isRush?"#fef2f2":"#fff7ed");
  g.addColorStop(0.4,isRush?"#ef4444":"#f97316");
  g.addColorStop(1,isRush?"#7f1d1d":"#431407");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Streak value display
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.38)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.shadowBlur=6;ctx.shadowColor=isRush?"#ef4444":"#fb923c";
  ctx.fillText(`×${Math.max(1,streakVal)}`,0,-r*0.12);
  ctx.font=`${Math.floor(r*0.28)}px sans-serif`;ctx.globalAlpha=0.8;
  ctx.fillText("COMBO",0,r*0.3);
  ctx.restore();
}
// ── Neon Crystal — 3 temperature phases cycling; hot phase = jackpot ──
function drawNeonCrystal(ctx,r,ts,phase){
  // phase 0=cold(cyan), 1=warm(orange), 2=hot(red)
  const phaseData=[[["#67e8f9","#0e7490"],"COLD"],[["#fb923c","#c2410c"],"WARM"],[["#ffd700","#dc2626"],"HOT"]];
  const [[c1,c2],label]=phaseData[phase];
  ctx.save();
  ctx.shadowBlur=phase===2?28:16;ctx.shadowColor=c1;
  // Crystal shape (hexagon)
  ctx.beginPath();
  for(let i=0;i<6;i++){const a=(i/6)*Math.PI*2-Math.PI/6;ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);}
  ctx.closePath();
  const g=ctx.createLinearGradient(-r,-r,r,r);
  g.addColorStop(0,c1);g.addColorStop(1,c2);
  ctx.fillStyle=g;ctx.globalAlpha=0.92;ctx.fill();
  ctx.strokeStyle="#fff";ctx.lineWidth=2;ctx.globalAlpha=0.4;ctx.stroke();
  // Internal facets
  for(let i=0;i<3;i++){const a=(i/3)*Math.PI*2;
    ctx.strokeStyle=c1;ctx.lineWidth=1.5;ctx.globalAlpha=0.35;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*0.85,Math.sin(a)*r*0.85);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.4)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.shadowBlur=6;ctx.shadowColor=c1;
  ctx.fillText(label,0,1);
  ctx.restore();
}
// ── Sonar Ping — emits expanding rings; bonus per target inside ring at tap ──
function drawSonarPing(ctx,r,ts,rings){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#4ade80";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.55);
  g.addColorStop(0,"#dcfce7");g.addColorStop(0.5,"#16a34a");g.addColorStop(1,"#14532d");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*0.55,0,Math.PI*2);ctx.fill();
  // Expanding ping rings
  rings.forEach(ringPct=>{
    const ringR=r*0.6+ringPct*(r*4);
    ctx.strokeStyle="#4ade80";ctx.lineWidth=2;ctx.globalAlpha=(1-ringPct)*0.7;ctx.shadowBlur=8;ctx.shadowColor="#4ade80";
    ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
  });
  // Sweep line
  const sweep=(ts*0.009)%(Math.PI*2);
  ctx.save();ctx.rotate(sweep);
  const sg=ctx.createLinearGradient(0,0,r*3,0);
  sg.addColorStop(0,"#4ade80");sg.addColorStop(1,"transparent");
  ctx.strokeStyle=sg;ctx.lineWidth=1.5;ctx.globalAlpha=0.6;
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(r*3,0);ctx.stroke();
  ctx.restore();
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.45)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("📡",0,1);
  ctx.restore();
}
// ── Gravity Bomb — falls toward bottom; explodes on floor, defuse before then ──
function drawGravityBomb(ctx,r,ts,fallPct){
  const isDanger=fallPct>0.7;
  ctx.save();
  ctx.shadowBlur=isDanger?24:14;ctx.shadowColor=isDanger?"#ef4444":"#f97316";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,isDanger?"#fca5a5":"#fcd34d");g.addColorStop(0.5,isDanger?"#ef4444":"#f97316");g.addColorStop(1,"#431407");
  ctx.fillStyle=g;ctx.globalAlpha=0.93;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Fuse (wiggling line going up)
  ctx.strokeStyle="#fbbf24";ctx.lineWidth=2.5;ctx.globalAlpha=0.85;
  ctx.beginPath();ctx.moveTo(0,-r);
  for(let i=1;i<=4;i++){const yo=-r-i*8;const xo=Math.sin(ts*0.04+i*2.1)*5;ctx.lineTo(xo,yo);}
  ctx.stroke();
  // Spark at fuse tip
  if(Math.sin(ts*0.05)>0.3){ctx.fillStyle="#ffd700";ctx.globalAlpha=1;ctx.beginPath();ctx.arc(Math.sin(ts*0.04+4*2.1)*5,-r-36,3,0,Math.PI*2);ctx.fill();}
  // Weight indicator (downward arrow)
  ctx.fillStyle="#fff";ctx.globalAlpha=0.7;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🧲",0,1);
  ctx.restore();
}
// ── Speed Nova — starts tiny, grows to full size; pts scale with current size ──
function drawSpeedNova(ctx,r,ts,sizePct){
  const scale=0.2+sizePct*0.8;
  const isPeak=sizePct>=0.9;
  ctx.save();
  ctx.shadowBlur=isPeak?28:14;ctx.shadowColor=isPeak?"#ffd700":"#c084fc";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*scale);
  g.addColorStop(0,isPeak?"#fef9c3":"#fdf4ff");
  g.addColorStop(0.5,isPeak?"#ffd700":"#c084fc");
  g.addColorStop(1,isPeak?"#92400e":"#581c87");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*scale,0,Math.PI*2);ctx.fill();
  // Nova rays at peak
  if(sizePct>0.5){for(let i=0;i<8;i++){
    const a=(i/8)*Math.PI*2+ts*0.007;
    const len=r*scale*(0.4+sizePct*0.6);
    ctx.strokeStyle=isPeak?"#ffd700":"#e9d5ff";ctx.lineWidth=1.5;ctx.globalAlpha=sizePct*0.5;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*scale*0.85,Math.sin(a)*r*scale*0.85);
    ctx.lineTo(Math.cos(a)*len,Math.sin(a)*len);ctx.stroke();
  }}
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*scale*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillStyle="#fff";ctx.shadowBlur=4;ctx.shadowColor="#c084fc";
  ctx.fillText("💫",0,1);
  ctx.restore();
}
// ── Quantum Split — splits into 2 halves on first tap; collect both for bonus ──
function drawQuantumSplit(ctx,r,ts,splitFrac){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#60a5fa";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#dbeafe");g.addColorStop(0.5,"#3b82f6");g.addColorStop(1,"#1e3a8a");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Quantum interference rings
  const rings=3;
  for(let i=0;i<rings;i++){
    const pr=(i+1)/rings;
    ctx.strokeStyle="#93c5fd";ctx.lineWidth=1;ctx.globalAlpha=0.4*(1-pr);
    ctx.setLineDash([3+i*2,5]);ctx.lineDashOffset=ts*0.025;
    ctx.beginPath();ctx.arc(0,0,r*pr,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);
  }
  // ⚛ symbol
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.65)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.shadowBlur=6;ctx.shadowColor="#60a5fa";
  ctx.fillText("⚛️",0,1);
  ctx.restore();
}
function drawQuantumHalf(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=12;ctx.shadowColor="#60a5fa";
  ctx.globalAlpha=0.85;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#bfdbfe");g.addColorStop(1,"#1d4ed8");
  ctx.fillStyle=g;
  // Half-circle
  ctx.beginPath();ctx.arc(0,0,r,-Math.PI/2,Math.PI/2);ctx.closePath();ctx.fill();
  ctx.strokeStyle="#93c5fd";ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(0,r);ctx.stroke();
  ctx.globalAlpha=0.7;ctx.font=`bold ${Math.floor(r*0.5)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.fillText("½",r*0.3,0);
  ctx.restore();
}
// ── Frost Bolt — ultrafast; early tap (>70% lifetime) = 3× pts ──
function drawFrostBolt(ctx,r,ts,speedFx){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#67e8f9";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#ecfeff");g.addColorStop(0.4,"#22d3ee");g.addColorStop(1,"#0e7490");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Frost crystal spokes
  for(let i=0;i<6;i++){const a=(i/6)*Math.PI*2+ts*0.025;
    ctx.save();ctx.rotate(a);ctx.strokeStyle="#a5f3fc";ctx.lineWidth=2;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(r*0.8,0);ctx.stroke();
    ctx.beginPath();ctx.moveTo(r*0.5,-r*0.15);ctx.lineTo(r*0.5,r*0.15);ctx.stroke();
    ctx.restore();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("❄️",0,1);
  ctx.restore();
}
// ── Vortex Star — collects trailing stars over time; pts = 50 × star count ──
function drawVortexStar(ctx,r,ts,starCount){
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor="#fbbf24";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.7);
  g.addColorStop(0,"#fef9c3");g.addColorStop(0.5,"#f59e0b");g.addColorStop(1,"#78350f");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.fill();
  // Orbiting stars
  for(let i=0;i<Math.min(8,starCount);i++){
    const a=(i/8)*Math.PI*2+ts*0.01*(1+i*0.1);
    const sr=r*(0.85+0.15*(i%2));
    ctx.save();ctx.globalAlpha=0.9;ctx.font=`${Math.floor(r*0.28)}px sans-serif`;
    ctx.textAlign="center";ctx.textBaseline="middle";ctx.shadowBlur=8;ctx.shadowColor="#ffd700";
    ctx.fillText("⭐",Math.cos(a)*sr,Math.sin(a)*sr);
    ctx.restore();
  }
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.4)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.shadowBlur=4;ctx.shadowColor="#f59e0b";
  ctx.fillText(`×${starCount}`,0,1);
  ctx.restore();
}
// ── Pixel Burst — 3-tap target; each tap un-pixelates it ──
function drawPixelBurst(ctx,r,ts,hitsLeft){
  ctx.save();
  const pixelSize=hitsLeft===3?10:hitsLeft===2?5:2;
  const col=hitsLeft===3?"#6366f1":hitsLeft===2?"#a855f7":"#f472b6";
  ctx.shadowBlur=14;ctx.shadowColor=col;
  // Pixelated fill
  const cells=Math.ceil(r*2/pixelSize);
  for(let px=0;px<cells;px++){for(let py=0;py<cells;py++){
    const cx=-r+px*pixelSize+pixelSize/2,cy=-r+py*pixelSize+pixelSize/2;
    if(Math.hypot(cx,cy)>r)continue;
    const hue=200+px*8+py*12+ts*0.01;
    ctx.fillStyle=`hsl(${hue%360},70%,60%)`;ctx.globalAlpha=0.85;
    ctx.fillRect(cx-pixelSize/2,cy-pixelSize/2,pixelSize-1,pixelSize-1);
  }}
  // Tap count indicator
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.45)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.shadowBlur=6;ctx.shadowColor=col;
  ctx.fillText(`${4-hitsLeft}/3`,0,1);
  ctx.restore();
}
// ── Star Forge — charges over lifetime; pts tier depends on charge level ──
function drawStarForge(ctx,r,ts,chargePct){
  const tier=chargePct>=0.95?2:chargePct>=0.8?1:0;
  const tierCols=[["#6366f1","#a5b4fc"],["#f59e0b","#fde68a"],["#ffd700","#fff"]];
  const [c1,c2]=tierCols[tier];
  ctx.save();
  ctx.shadowBlur=12+chargePct*20;ctx.shadowColor=c2;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,c2);g.addColorStop(0.5,c1);g.addColorStop(1,"#0f0f2a");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Charge arc
  ctx.strokeStyle=c2;ctx.lineWidth=3.5;ctx.globalAlpha=0.85;ctx.shadowBlur=10;ctx.shadowColor=c2;
  ctx.beginPath();ctx.arc(0,0,r*0.85,-Math.PI/2,-Math.PI/2+chargePct*Math.PI*2);ctx.stroke();
  // Star points at high charge
  if(tier>=1){
    for(let i=0;i<5;i++){const a=(i/5)*Math.PI*2+ts*0.01;
      ctx.save();ctx.rotate(a);ctx.fillStyle=c2;ctx.globalAlpha=chargePct*0.8;
      ctx.beginPath();ctx.moveTo(0,-r*0.9);ctx.lineTo(r*0.08,-r*0.6);ctx.lineTo(-r*0.08,-r*0.6);ctx.closePath();ctx.fill();
      ctx.restore();
    }
  }
  // Tier label
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.36)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.shadowBlur=4;ctx.shadowColor=c1;
  ctx.fillText(tier===2?"⚡MAX":tier===1?"🔥HOT":"⭐",0,1);
  ctx.restore();
}
// ── Magic Dice — re-rolls every 600ms; pts = 100 × face value ──
function drawMagicDice(ctx,r,ts,face){
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor="#a855f7";
  // Die body
  const g=ctx.createRadialGradient(-r*0.2,-r*0.2,0,0,0,r);
  g.addColorStop(0,"#f3e8ff");g.addColorStop(0.4,"#a855f7");g.addColorStop(1,"#4c1d95");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  const s=r*0.85;
  ctx.beginPath();ctx.roundRect(-s,-s,s*2,s*2,r*0.22);ctx.fill();
  // Die dots
  const dotPos={1:[[0,0]],2:[[-0.45,-0.45],[0.45,0.45]],3:[[-0.45,-0.45],[0,0],[0.45,0.45]],
    4:[[-0.45,-0.45],[-0.45,0.45],[0.45,-0.45],[0.45,0.45]],
    5:[[-0.45,-0.45],[-0.45,0.45],[0,0],[0.45,-0.45],[0.45,0.45]],
    6:[[-0.45,-0.45],[-0.45,0],[-0.45,0.45],[0.45,-0.45],[0.45,0],[0.45,0.45]]};
  const dots=dotPos[face]||dotPos[1];
  ctx.fillStyle="#fff";ctx.globalAlpha=1;ctx.shadowBlur=0;
  dots.forEach(([dx,dy])=>{ctx.beginPath();ctx.arc(dx*r*0.75,dy*r*0.75,r*0.14,0,Math.PI*2);ctx.fill();});
  ctx.restore();
}
// ── Wormhole Target — teleports all other targets on tap; bonus per target moved ──
function drawWormholeTgt(ctx,r,ts){
  ctx.save();
  const spin=ts*0.011;
  ctx.shadowBlur=22;ctx.shadowColor="#38bdf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#ecfeff");g.addColorStop(0.35,"#06b6d4");g.addColorStop(0.7,"#1e3a5f");g.addColorStop(1,"#000");
  ctx.fillStyle=g;ctx.globalAlpha=0.95;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Spiral arms
  for(let arm=0;arm<3;arm++){
    ctx.strokeStyle=`hsl(${180+arm*30},80%,70%)`;ctx.lineWidth=2;ctx.globalAlpha=0.55;ctx.shadowBlur=8;ctx.shadowColor="#06b6d4";
    ctx.beginPath();
    for(let i=0;i<60;i++){const t2=i/60;const a=spin+arm*(Math.PI*2/3)+t2*Math.PI*4;const ri=t2*r*0.9;
      if(i===0)ctx.moveTo(Math.cos(a)*ri,Math.sin(a)*ri);
      else ctx.lineTo(Math.cos(a)*ri,Math.sin(a)*ri);
    }
    ctx.stroke();
  }
  ctx.restore();
}
// ── Shadow Pulse — cycles dark/bright every 800ms; dark phase = 3× pts ──
function drawShadowPulse(ctx,r,ts,isDark){
  ctx.save();
  const pulse=0.88+0.12*Math.sin(ts*0.015);
  ctx.shadowBlur=isDark?8:22;ctx.shadowColor=isDark?"#1e1e2e":"#8b5cf6";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  if(isDark){
    g.addColorStop(0,"#2d2d44");g.addColorStop(0.6,"#1e1e2e");g.addColorStop(1,"#0a0a14");
  } else {
    g.addColorStop(0,"#f5f3ff");g.addColorStop(0.5,"#8b5cf6");g.addColorStop(1,"#4c1d95");
  }
  ctx.fillStyle=g;ctx.globalAlpha=isDark?0.65:0.95;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Faint ring hint
  ctx.strokeStyle=isDark?"#3f3f60":"#c4b5fd";ctx.lineWidth=isDark?1:2;ctx.globalAlpha=isDark?0.3:0.6;
  ctx.setLineDash([4,6]);
  ctx.beginPath();ctx.arc(0,0,r*1.08,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);
  if(!isDark){ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.45)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("👁️",0,1);}
  ctx.restore();
}
// ── Eclipse Orb — cycles sun/moon every 800ms; moon phase = 3× pts ──
function drawEclipseOrb(ctx,r,ts,isMoon){
  ctx.save();
  ctx.shadowBlur=isMoon?22:18;ctx.shadowColor=isMoon?"#818cf8":"#f59e0b";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  if(isMoon){
    g.addColorStop(0,"#e0e7ff");g.addColorStop(0.5,"#6366f1");g.addColorStop(1,"#1e1b4b");
  } else {
    g.addColorStop(0,"#fef9c3");g.addColorStop(0.5,"#f59e0b");g.addColorStop(1,"#92400e");
  }
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Eclipse crescent (shadow arc for moon phase)
  if(isMoon){
    ctx.globalAlpha=0.35;ctx.fillStyle="#000";
    ctx.beginPath();ctx.arc(r*0.25,0,r*0.85,0,Math.PI*2);ctx.fill();
  } else {
    // Sun rays
    for(let i=0;i<8;i++){const a=(i/8)*Math.PI*2+ts*0.004;
      ctx.save();ctx.rotate(a);ctx.fillStyle="#fde68a";ctx.globalAlpha=0.5;
      ctx.fillRect(r*0.95,r*-0.06,r*0.3,r*0.12);ctx.restore();
    }
  }
  ctx.globalAlpha=1;ctx.font=`${Math.floor(r*0.7)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(isMoon?"🌙":"☀️",0,2);
  ctx.restore();
}
// ── Spin Bloom — flower with petals that fade; pts per petal remaining ──
function drawSpinBloom(ctx,r,ts,petalsLeft){
  ctx.save();
  const petalCols=["#f472b6","#fb923c","#facc15","#4ade80","#60a5fa","#a78bfa","#f472b6","#34d399"];
  const spin=ts*0.006;
  // Petals
  for(let i=0;i<8;i++){
    const alive=i<petalsLeft;
    if(!alive){ctx.save();ctx.rotate(spin+(i/8)*Math.PI*2);ctx.globalAlpha=0.08;ctx.fillStyle="#888";ctx.beginPath();ctx.ellipse(r*0.7,0,r*0.28,r*0.16,0,0,Math.PI*2);ctx.fill();ctx.restore();continue;}
    const a=spin+(i/8)*Math.PI*2;
    ctx.save();ctx.rotate(a);
    ctx.globalAlpha=0.88;ctx.fillStyle=petalCols[i%petalCols.length];ctx.shadowBlur=8;ctx.shadowColor=petalCols[i%petalCols.length];
    ctx.beginPath();ctx.ellipse(r*0.68,0,r*0.29,r*0.16,0,0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
  // Center
  ctx.shadowBlur=12;ctx.shadowColor="#fbbf24";ctx.fillStyle="#fef9c3";ctx.globalAlpha=0.95;
  ctx.beginPath();ctx.arc(0,0,r*0.3,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Tesla Node — auto-zaps nearest target every 1.2s; tap to claim banked pts ──
function drawTeslaNode(ctx,r,ts,bankPts){
  ctx.save();
  const charge=Math.min(1,bankPts/400);
  ctx.shadowBlur=16+charge*12;ctx.shadowColor="#38bdf8";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#e0f2fe");g.addColorStop(0.5,"#0284c7");g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Electric sparks at high charge
  if(charge>0.3){
    for(let i=0;i<4;i++){const a=ts*0.014+i*Math.PI/2;const er=r*(0.7+0.3*Math.sin(ts*0.02+i*2.1));
      ctx.strokeStyle="#7dd3fc";ctx.lineWidth=1.2;ctx.globalAlpha=charge*0.7;
      ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*er,Math.sin(a)*er);ctx.stroke();
    }
  }
  // Bank indicator text
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.36)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillStyle="#fff";ctx.shadowBlur=4;ctx.shadowColor="#38bdf8";
  ctx.fillText(bankPts>0?`+${bankPts}`:"⚡",0,1);
  ctx.restore();
}
// ── Rune Circle — 6 runes rotate; legendary rune at top = jackpot ──
function drawRuneCircle(ctx,r,ts,runeIdx){
  const runes=["🔮","🌟","⚗️","📿","🔯","✨"];
  const pts=[100,200,350,500,750,1000];
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#a78bfa";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.6);
  g.addColorStop(0,"#f5f3ff");g.addColorStop(0.5,"#7c3aed");g.addColorStop(1,"#2e1065");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*0.6,0,Math.PI*2);ctx.fill();
  // Rune orbit
  ctx.setLineDash([3,5]);ctx.strokeStyle="#a78bfa";ctx.lineWidth=1;ctx.globalAlpha=0.3;
  ctx.beginPath();ctx.arc(0,0,r*1.0,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);
  // Draw all 6 runes
  const baseAngle=ts*0.0055;
  runes.forEach((rune,i)=>{
    const a=baseAngle+(i/6)*Math.PI*2;
    const rx=Math.cos(a)*r*1.0,ry=Math.sin(a)*r*1.0;
    const isActive=i===runeIdx;
    ctx.save();ctx.globalAlpha=isActive?1:0.45;
    if(isActive){ctx.shadowBlur=14;ctx.shadowColor="#ffd700";}
    ctx.font=`${Math.floor(r*(isActive?0.42:0.3))}px sans-serif`;
    ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(rune,rx,ry);
    ctx.restore();
  });
  ctx.restore();
}
// ── Solar Orbit — 3 mini-planets orbit it; bonus per planet in alignment arc ──
function drawSolarOrbit(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor="#f59e0b";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.55);
  g.addColorStop(0,"#fef3c7");g.addColorStop(0.6,"#f59e0b");g.addColorStop(1,"#b45309");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r*0.55,0,Math.PI*2);ctx.fill();
  // Orbit ring
  ctx.strokeStyle="#fbbf24";ctx.lineWidth=1;ctx.globalAlpha=0.3;ctx.shadowBlur=0;
  ctx.setLineDash([3,5]);
  ctx.beginPath();ctx.arc(0,0,r*1.05,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);
  // 3 planets
  const speeds=[0.0055,0.0038,0.0071];
  const planetColors=["#60a5fa","#f87171","#34d399"];
  const planetR=[0.17,0.13,0.15];
  speeds.forEach((sp,i)=>{
    const a=ts*sp+(i*(Math.PI*2/3));
    const px=Math.cos(a)*r*1.05,py=Math.sin(a)*r*1.05;
    ctx.shadowBlur=8;ctx.shadowColor=planetColors[i];
    ctx.fillStyle=planetColors[i];ctx.globalAlpha=0.9;
    ctx.beginPath();ctx.arc(px,py,r*planetR[i],0,Math.PI*2);ctx.fill();
  });
  ctx.restore();
}
// ── Chroma Shift — cycles 6 colors every 400ms; pts depend on color at tap ──
function drawChromaShift(ctx,r,ts,colorIdx){
  const cols=["#ef4444","#3b82f6","#22c55e","#a855f7","#06b6d4","#ffd700"];
  const names=["RED","BLUE","GREEN","PURPLE","CYAN","GOLD"];
  const c=cols[colorIdx];
  ctx.save();
  ctx.shadowBlur=22;ctx.shadowColor=c;
  const pulse=0.9+0.1*Math.sin(ts*0.014);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,c);g.addColorStop(1,c+"88");
  ctx.fillStyle=g;ctx.globalAlpha=0.92;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Color name label
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.38)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillStyle="#fff";ctx.shadowBlur=4;ctx.shadowColor="#000";
  ctx.fillText(names[colorIdx],0,1);
  // Spinning outer ring
  ctx.strokeStyle=c;ctx.lineWidth=2.5;ctx.globalAlpha=0.55;ctx.shadowBlur=10;ctx.shadowColor=c;
  ctx.setLineDash([6,6]);ctx.lineDashOffset=ts*0.03;
  ctx.beginPath();ctx.arc(0,0,r*1.05,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);
  ctx.restore();
}
// ── Neon Pulsar — pulses between small and large; bonus at peak size ──
function drawNeonPulsar(ctx,r,ts,phasePct){
  const sizeMult=0.7+0.5*phasePct; // 0.7→1.2
  const isPeak=phasePct>0.88;
  ctx.save();
  ctx.shadowBlur=isPeak?28:14;ctx.shadowColor=isPeak?"#ffd700":"#e879f9";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*sizeMult);
  g.addColorStop(0,isPeak?"#fef9c3":"#fdf4ff");
  g.addColorStop(0.5,isPeak?"#ffd700":"#e879f9");
  g.addColorStop(1,isPeak?"#ca8a04":"#9333ea");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*sizeMult,0,Math.PI*2);ctx.fill();
  if(isPeak){
    ctx.strokeStyle="#ffd700";ctx.lineWidth=3;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(0,0,r*sizeMult*1.18,0,Math.PI*2);ctx.stroke();
  }
  // Center dot
  ctx.fillStyle="#fff";ctx.globalAlpha=1;ctx.shadowBlur=6;ctx.shadowColor="#fff";
  ctx.beginPath();ctx.arc(0,0,r*0.15,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Drift Mine — slow-moving hazard; explodes on expiry, defuse for pts ──
function drawDriftMine(ctx,r,ts,timeLeftPct){
  ctx.save();
  const danger=timeLeftPct<0.25;
  const flash=danger&&Math.sin(ts*0.03)>0;
  ctx.shadowBlur=danger?24:14;ctx.shadowColor=flash?"#fff":"#ef4444";
  // Body
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,flash?"#fff":"#fca5a5");g.addColorStop(0.5,"#ef4444");g.addColorStop(1,"#7f1d1d");
  ctx.fillStyle=g;ctx.globalAlpha=0.95;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Spike arms
  for(let i=0;i<8;i++){
    const a=(i/8)*Math.PI*2+ts*0.002;
    ctx.save();ctx.rotate(a);
    ctx.fillStyle=danger?"#fff":"#fca5a5";ctx.globalAlpha=0.8;
    ctx.beginPath();ctx.moveTo(r*0.9,0);ctx.lineTo(r*1.35,r*0.08);ctx.lineTo(r*1.35,-r*0.08);ctx.closePath();ctx.fill();
    ctx.restore();
  }
  // Warning symbol
  ctx.fillStyle="#fff";ctx.globalAlpha=1;
  ctx.font=`bold ${Math.floor(r*0.7)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚠️",0,1);
  ctx.restore();
}
// ── Arc Light — electric node; shoots arcs to nearby targets, bonus per arc ──
function drawArcLight(ctx,r,ts,arcTargets){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#facc15";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fef9c3");g.addColorStop(0.5,"#facc15");g.addColorStop(1,"#ca8a04");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*0.55,0,Math.PI*2);ctx.fill();
  if(arcTargets&&arcTargets.length>0){
    arcTargets.forEach(({dx,dy})=>{
      ctx.strokeStyle="#facc15";ctx.lineWidth=1.5;ctx.globalAlpha=0.65;ctx.shadowBlur=8;ctx.shadowColor="#facc15";
      ctx.beginPath();ctx.moveTo(0,0);
      const steps=6;
      for(let i=1;i<steps;i++){const t=i/steps;const j=(Math.sin(ts*0.04+i*1.3)*10);ctx.lineTo(dx*t+j,dy*t+j);}
      ctx.lineTo(dx,dy);ctx.stroke();
    });
  }
  ctx.globalAlpha=0.7;ctx.strokeStyle="#fbbf24";ctx.lineWidth=2;ctx.shadowBlur=10;ctx.shadowColor="#facc15";
  ctx.setLineDash([4,6]);
  ctx.beginPath();ctx.arc(0,0,r*(0.85+0.08*Math.sin(ts*0.009)),0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);
  ctx.restore();
}
// ── Pulse Chain — chain reaction; destroys nearby normals on tap ──
function drawPulseChain(ctx,r,ts){
  ctx.save();
  const pulse=0.88+0.12*Math.sin(ts*0.013);
  ctx.shadowBlur=20;ctx.shadowColor="#f97316";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fff7ed");g.addColorStop(0.4,"#f97316");g.addColorStop(1,"#c2410c");
  ctx.fillStyle=g;ctx.globalAlpha=0.9;
  ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.007;
    const lx=Math.cos(a)*r*0.72,ly=Math.sin(a)*r*0.72;
    ctx.save();ctx.translate(lx,ly);ctx.rotate(a+Math.PI/4);
    ctx.strokeStyle="#fb923c";ctx.lineWidth=2;ctx.globalAlpha=0.8;ctx.shadowBlur=6;ctx.shadowColor="#f97316";
    ctx.beginPath();ctx.arc(0,0,r*0.15,0,Math.PI*2);ctx.stroke();
    ctx.restore();
  }
  ctx.globalAlpha=1;ctx.font=`bold ${Math.floor(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillStyle="#fff";ctx.shadowBlur=6;ctx.shadowColor="#f97316";
  ctx.fillText("🔗",0,1);
  ctx.restore();
}
// ── Ghost Orb — fades invisible over lifetime; phantom tap at 90%+ for 3× pts ──
function drawGhostOrb(ctx,r,ts,agePct){
  ctx.save();
  const visibility=Math.max(0.06,1-agePct*0.9);
  const pulse=0.90+0.1*Math.sin(ts*0.012);
  ctx.globalAlpha=visibility;ctx.shadowBlur=20*visibility;ctx.shadowColor="#c4b5fd";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#f5f3ff");g.addColorStop(0.5,"#8b5cf6");g.addColorStop(1,"#4c1d95");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  for(let i=0;i<3;i++){
    const wa=(i/3)*Math.PI*2+ts*0.005;
    const wr=r*(0.6+0.2*Math.sin(ts*0.008+i));
    ctx.globalAlpha=visibility*0.5;ctx.fillStyle="#c4b5fd";
    ctx.beginPath();ctx.arc(Math.cos(wa)*wr,Math.sin(wa)*wr,r*0.12,0,Math.PI*2);ctx.fill();
  }
  ctx.restore();
}
// ── Time Fracture — cracked clock; freezes all targets for 2s when tapped ──
function drawTimeFracture(ctx,r,ts){
  ctx.save();
  const spin=ts*0.008;
  ctx.shadowBlur=22;ctx.shadowColor="#06b6d4";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#ecfeff");g.addColorStop(0.5,"#06b6d4");g.addColorStop(1,"#0e7490");
  ctx.fillStyle=g;ctx.globalAlpha=0.88;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  const crackAngles=[0.5,2.5,4.0,5.5];
  crackAngles.forEach(ca=>{
    ctx.strokeStyle="#fff";ctx.lineWidth=1.5;ctx.globalAlpha=0.7;ctx.shadowBlur=0;
    ctx.beginPath();ctx.moveTo(0,0);
    ctx.lineTo(Math.cos(ca)*r*0.85,Math.sin(ca)*r*0.85);ctx.stroke();
  });
  ctx.strokeStyle="#fff";ctx.lineWidth=2.5;ctx.globalAlpha=0.9;ctx.lineCap="round";ctx.shadowBlur=4;ctx.shadowColor="#fff";
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(spin-Math.PI/2)*r*0.5,Math.sin(spin-Math.PI/2)*r*0.5);ctx.stroke();
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(spin*4-Math.PI/2)*r*0.72,Math.sin(spin*4-Math.PI/2)*r*0.72);ctx.stroke();
  ctx.fillStyle="#fff";ctx.globalAlpha=1;ctx.beginPath();ctx.arc(0,0,r*0.12,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Accumulator — grows in size as other targets expire; score scales with radius ──
function drawAccumulator(ctx,r,ts,stacks){
  ctx.save();
  const stackMult=Math.min(8,1+stacks);
  const pulse=0.92+0.08*Math.sin(ts*0.01);
  const hue=stacks===0?200:stacks<4?280:stacks<7?30:0;
  ctx.shadowBlur=12+stacks*3;ctx.shadowColor=`hsl(${hue},85%,60%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,`hsl(${hue},70%,80%)`);g.addColorStop(0.5,`hsl(${hue},80%,50%)`);g.addColorStop(1,"#000");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Stack rings
  for(let i=0;i<Math.min(stacks,6);i++){
    ctx.strokeStyle=`hsla(${hue},80%,65%,${0.5-i*0.07})`;ctx.lineWidth=1.5;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(0,0,r*(0.75+i*0.15),0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.fillStyle=`hsl(${hue},60%,85%)`;
  ctx.font=`bold ${Math.round(r*0.33)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(`×${stackMult}`,0,0);ctx.restore();
}
// ── Jackpot Target — rare once-per-level giant; guaranteed high pts but moves erratically ──
function drawJackpotTarget(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=28;ctx.shadowColor="#ffd700";
  // Spinning rainbow body
  const hue=(ts*0.25)%360;
  const pulse=0.88+0.12*Math.sin(ts*0.009);
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.3,`hsl(${hue},100%,70%)`);
  g.addColorStop(0.7,`hsl(${(hue+120)%360},100%,50%)`);g.addColorStop(1,"#1a0a00");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Gold star overlay
  ctx.globalAlpha=0.8;
  for(let i=0;i<8;i++){
    const a=(i/8)*Math.PI*2+ts*0.007;
    ctx.fillStyle=`hsl(${50+i*8},100%,65%)`;
    ctx.beginPath();ctx.arc(Math.cos(a)*r*0.78,Math.sin(a)*r*0.78,r*0.1,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#fff";
  ctx.font=`${Math.round(r*0.55)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🎰",0,0);ctx.restore();
}
// ── Phase Orb — cycles through 3 phases (attack/defense/bonus); best tap = bonus phase ──
function drawPhaseOrb(ctx,r,ts,phase){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.012);
  const phaseColors=[["#ef4444","#fca5a5"],["#3b82f6","#93c5fd"],["#ffd700","#fef08a"]];
  const phaseLabels=["⚔️","🛡","💰"];
  const [mainCol,glowCol]=phaseColors[phase%3];
  ctx.shadowBlur=18;ctx.shadowColor=glowCol;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fff");g.addColorStop(0.4,mainCol);g.addColorStop(1,"#0a0a0a");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Phase indicator segments
  for(let i=0;i<3;i++){
    const active=i===phase%3;
    const a=(i/3)*Math.PI*2-Math.PI/2;
    ctx.fillStyle=phaseColors[i][0];ctx.globalAlpha=active?0.85:0.2;
    ctx.beginPath();ctx.moveTo(0,0);ctx.arc(0,0,r*0.88,a,a+Math.PI*0.6);ctx.closePath();ctx.fill();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#fff";
  ctx.font=`${Math.round(r*0.4)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(phaseLabels[phase%3],0,0);ctx.restore();
}
// ── Ripple Wave — radiates damage rings outward; tap when ring passes a specific target ──
function drawRippleWave(ctx,r,ts,ringAges){
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor="#06b6d4";
  // Core node
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.5);
  g.addColorStop(0,"#cffafe");g.addColorStop(0.5,"#06b6d4");g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.5,0,Math.PI*2);ctx.fill();
  // Expanding rings
  (ringAges||[]).forEach((age,i)=>{
    const ring=age*r*2.5;const alpha=1-age;
    ctx.strokeStyle=`rgba(6,182,212,${alpha*0.6})`;ctx.lineWidth=2-age*1.5;ctx.globalAlpha=alpha;
    ctx.beginPath();ctx.arc(0,0,ring,0,Math.PI*2);ctx.stroke();
  });
  ctx.globalAlpha=1;ctx.restore();
}
// ── Laser Grid — geometric reticle with rotating beams; beam alignment = jackpot ──
function drawLaserGrid(ctx,r,ts){
  ctx.save();
  const rot=ts*0.014;
  ctx.shadowBlur=16;ctx.shadowColor="#f43f5e";
  // Reticle rings
  [0.55,0.8,1.0].forEach((frac,i)=>{
    ctx.strokeStyle=`rgba(244,63,94,${0.6-i*0.15})`;ctx.lineWidth=1.4;ctx.globalAlpha=0.85;
    ctx.beginPath();ctx.arc(0,0,r*frac,0,Math.PI*2);ctx.stroke();
  });
  // 4 rotating laser beams
  for(let i=0;i<4;i++){
    const a=rot+(i/4)*Math.PI*2;
    ctx.globalAlpha=0.7;ctx.strokeStyle="#f43f5e";ctx.lineWidth=1.8;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.35,Math.sin(a)*r*0.35);ctx.lineTo(Math.cos(a)*r*1.05,Math.sin(a)*r*1.05);ctx.stroke();
  }
  // Cross-hairs
  ctx.globalAlpha=0.55;ctx.strokeStyle="#fda4af";ctx.lineWidth=1;
  ctx.beginPath();ctx.moveTo(-r*0.3,0);ctx.lineTo(r*0.3,0);ctx.stroke();
  ctx.beginPath();ctx.moveTo(0,-r*0.3);ctx.lineTo(0,r*0.3);ctx.stroke();
  ctx.globalAlpha=1;ctx.restore();
}
// ── Bouncing Orb — bounces off walls; each bounce doubles score multiplier (max 8×) ──
function drawBouncingOrb(ctx,r,ts,bounces){
  ctx.save();
  const mult=Math.min(8,1+bounces);
  const hue=120-bounces*15;
  const pulse=0.9+0.1*Math.sin(ts*0.014+bounces*0.5);
  ctx.shadowBlur=14+bounces*2;ctx.shadowColor=`hsl(${hue},90%,60%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,`hsl(${hue},80%,85%)`);g.addColorStop(0.5,`hsl(${hue},80%,55%)`);g.addColorStop(1,`hsl(${hue},70%,20%)`);
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // bounce ripple rings
  for(let i=0;i<bounces&&i<4;i++){
    ctx.strokeStyle=`hsla(${hue},90%,70%,${0.4-i*0.08})`;ctx.lineWidth=1.5;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(0,0,r*(0.85+i*0.2),0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#fff";
  ctx.font=`bold ${Math.round(r*0.32)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(`×${mult}`,0,0);ctx.restore();
}
// ── Time Capsule — frozen in amber; score 100pts normally or 1000pts at exact 3.0s mark ──
function drawTimeCapsule(ctx,r,ts,agePct){
  ctx.save();
  const isGolden=agePct>=0.595&&agePct<=0.705; // sweet spot near 50% of 5s lifetime = 2.5-3.5s
  ctx.shadowBlur=isGolden?24:12;ctx.shadowColor=isGolden?"#ffd700":"#d97706";
  // Amber body
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,isGolden?"#fffbeb":"#fef3c7");g.addColorStop(0.35,isGolden?"#ffd700":"#f59e0b");
  g.addColorStop(0.7,"#b45309");g.addColorStop(1,"#451a03");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Time freeze effect — concentric amber rings
  if(isGolden){
    for(let i=0;i<3;i++){
      ctx.strokeStyle=`rgba(255,215,0,${0.5-i*0.12})`;ctx.lineWidth=2;ctx.globalAlpha=0.7;
      ctx.beginPath();ctx.arc(0,0,r*(1.1+i*0.2),0,Math.PI*2);ctx.stroke();
    }
  }
  // Analog progress arc showing the "golden window"
  ctx.globalAlpha=1;ctx.strokeStyle=isGolden?"#ffd700":"#f59e0b";ctx.lineWidth=3;ctx.lineCap="round";
  ctx.beginPath();ctx.arc(0,0,r*0.88,-Math.PI/2,-Math.PI/2+agePct*Math.PI*2);ctx.stroke();
  ctx.fillStyle=isGolden?"#451a03":"#fef3c7";
  ctx.font=`${Math.round(r*0.35)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⏳",0,0);ctx.restore();
}
// ── Nether Shard — grows darker and more powerful the longer it lives; infinite pts potential ──
function drawNetherShard(ctx,r,ts,agePct){
  ctx.save();
  const power=Math.min(1,agePct);
  const shardR=r*(0.7+power*0.5);
  ctx.shadowBlur=14+power*20;ctx.shadowColor=`hsl(${270-power*30},80%,${50+power*20}%)`;
  // Body gets darker then lights up at peak
  const coreAlpha=power>0.7?0.6+power*0.4:0.9-power*0.3;
  ctx.fillStyle=`rgba(${Math.round(15+power*40)},${Math.round(8+power*10)},${Math.round(40+power*60)},${coreAlpha})`;
  // Hexagonal shard shape
  ctx.beginPath();
  for(let i=0;i<6;i++){const a=(i/6)*Math.PI*2-Math.PI/6;ctx.lineTo(Math.cos(a)*shardR,Math.sin(a)*shardR);}
  ctx.closePath();ctx.fill();
  // Power cracks light up
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.006;
    ctx.strokeStyle=`hsla(${270+power*60},90%,${60+power*30}%,${power*0.7})`;ctx.lineWidth=1.5;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*shardR*0.82,Math.sin(a)*shardR*0.82);ctx.stroke();
  }
  // Power multiplier
  const mult=Math.round(1+power*9);
  ctx.globalAlpha=1;ctx.fillStyle=`hsl(${270+power*60},80%,80%)`;
  ctx.font=`bold ${Math.round(r*0.32)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(`×${mult}`,0,0);ctx.restore();
}
// ── Solar Burst — radiant sun; AoE hit scores all targets within 1.2× radius on tap ──
function drawSolarBurst(ctx,r,ts){
  ctx.save();
  const pulse=0.88+0.12*Math.abs(Math.sin(ts*0.009));
  ctx.shadowBlur=22;ctx.shadowColor="#fbbf24";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fef9c3");g.addColorStop(0.3,"#fbbf24");g.addColorStop(0.7,"#d97706");g.addColorStop(1,"#7c2d12");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Solar rays
  const rays=8;
  for(let i=0;i<rays;i++){
    const a=(i/rays)*Math.PI*2+ts*0.005;
    const inner=r*(0.85+0.05*Math.sin(ts*0.02+i));
    const outer=r*(1.35+0.1*Math.cos(ts*0.018+i));
    ctx.strokeStyle=`rgba(251,191,36,${0.5+0.3*Math.sin(ts*0.01+i)})`;ctx.lineWidth=3;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*inner,Math.sin(a)*inner);ctx.lineTo(Math.cos(a)*outer,Math.sin(a)*outer);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#fef9c3";
  ctx.font=`${Math.round(r*0.38)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("☀️",0,0);ctx.restore();
}
// ── Frost Comet — icy comet; on tap splits into 3 ice shards that scatter and freeze ──
function drawFrostComet(ctx,r,ts){
  ctx.save();
  const pulse=0.92+0.08*Math.sin(ts*0.012);
  ctx.shadowBlur=18;ctx.shadowColor="#7dd3fc";
  // Comet core
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.7*pulse);
  g.addColorStop(0,"#f0f9ff");g.addColorStop(0.4,"#7dd3fc");g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.7*pulse,0,Math.PI*2);ctx.fill();
  // Ice tail
  for(let i=0;i<4;i++){
    const tailAlpha=0.6-i*0.12;
    ctx.globalAlpha=tailAlpha;ctx.fillStyle="#bae6fd";
    ctx.beginPath();ctx.ellipse(-r*(0.7+i*0.5),0,r*(0.25-i*0.05),r*0.18,0,0,Math.PI*2);ctx.fill();
  }
  // Frost sparkles
  ctx.globalAlpha=1;
  for(let i=0;i<5;i++){
    const a=(i/5)*Math.PI*2+ts*0.008;
    ctx.fillStyle=`rgba(186,230,253,${0.6+0.3*Math.sin(ts*0.02+i)})`;
    ctx.beginPath();ctx.arc(Math.cos(a)*r*0.82,Math.sin(a)*r*0.82,r*0.08,0,Math.PI*2);ctx.fill();
  }
  ctx.fillStyle="#f0f9ff";ctx.font=`${Math.round(r*0.4)}px serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("❄️",0,0);ctx.restore();
}
// ── Energy Web — draws connecting lines to nearby targets; score = nodes in web on tap ──
function drawEnergyWeb(ctx,r,ts,webPts){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.011);
  ctx.shadowBlur=16;ctx.shadowColor="#34d399";
  // Core
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.6*pulse);
  g.addColorStop(0,"#d1fae5");g.addColorStop(0.5,"#34d399");g.addColorStop(1,"#064e3b");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.6*pulse,0,Math.PI*2);ctx.fill();
  // Web strands
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.004;
    ctx.strokeStyle=`rgba(52,211,153,${0.4+0.3*Math.sin(ts*0.009+i)})`;ctx.lineWidth=1.2;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*1.1,Math.sin(a)*r*1.1);ctx.stroke();
  }
  // Node count
  if(webPts>0){
    ctx.globalAlpha=1;ctx.fillStyle="#34d399";
    ctx.font=`bold ${Math.round(r*0.28)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText(`${webPts}×`,0,r*0.55);
  }
  ctx.restore();
}
// ── Void Rift — black hole that grows and devours nearby targets; massive pts per devoured ──
function drawVoidRift(ctx,r,ts,agePct,devouredCount){
  ctx.save();
  const expandR=r*(0.6+agePct*0.6);
  ctx.shadowBlur=24+agePct*10;ctx.shadowColor="#7c3aed";
  // Pitch black core
  ctx.fillStyle="#000000";ctx.beginPath();ctx.arc(0,0,expandR*0.55,0,Math.PI*2);ctx.fill();
  // Accretion disk
  for(let ring=0;ring<3;ring++){
    const rFrac=0.7+ring*0.18;
    const ringAlpha=0.6-ring*0.15;
    ctx.globalAlpha=ringAlpha*(0.7+0.3*Math.sin(ts*0.015+ring*1.1));
    ctx.strokeStyle=ring===0?"#a78bfa":ring===1?"#7c3aed":"#4c1d95";ctx.lineWidth=2.5-ring*0.5;
    ctx.beginPath();ctx.arc(0,0,expandR*rFrac,0,Math.PI*2);ctx.stroke();
  }
  // Devoured count badge
  if(devouredCount>0){
    ctx.globalAlpha=1;ctx.fillStyle="#c4b5fd";
    ctx.font=`bold ${Math.round(r*0.3)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText(`×${devouredCount}`,0,0);
  }
  // Gravity distortion lines
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+ts*0.009;
    ctx.globalAlpha=0.3+agePct*0.2;ctx.strokeStyle="#a78bfa";ctx.lineWidth=1;
    ctx.beginPath();
    ctx.moveTo(Math.cos(a)*expandR,Math.sin(a)*expandR);
    ctx.quadraticCurveTo(Math.cos(a+0.3)*expandR*0.7,Math.sin(a+0.3)*expandR*0.7,0,0);
    ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.restore();
}
// ── Quantum Leap — teleports to random position every 1.2s; trail ghost shows last position ──
function drawQuantumLeap(ctx,r,ts,trailAlpha){
  ctx.save();
  const pulse=0.85+0.15*Math.sin(ts*0.018);
  ctx.shadowBlur=18;ctx.shadowColor="#818cf8";
  // Ghost trail (faint previous position shown by caller at trailAlpha)
  if(trailAlpha>0){
    ctx.globalAlpha=trailAlpha*0.35;ctx.fillStyle="#818cf8";
    ctx.beginPath();ctx.arc(0,0,r*0.9,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#e0e7ff");g.addColorStop(0.4,"#818cf8");g.addColorStop(0.8,"#4338ca");g.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Phase-shift rings
  for(let i=0;i<3;i++){
    const drift=(ts*0.015+i*1.2)%(Math.PI*2);
    ctx.strokeStyle=`rgba(129,140,248,${0.5-i*0.12})`;ctx.lineWidth=1.5;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(0,0,r*(0.7+i*0.18)*pulse,drift,drift+Math.PI*1.4);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#e0e7ff";
  ctx.font=`${Math.round(r*0.45)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🔮",0,0);ctx.restore();
}
// ── Magma Burst — heats up over time; max pts when glowing white-hot (agePct>0.7) ──
function drawMagmaBurst(ctx,r,ts,agePct){
  ctx.save();
  const heat=Math.min(1,agePct);
  const pulse=0.9+0.1*Math.sin(ts*0.014);
  // Heat color: dark red → orange → yellow → white
  const hotColor=heat<0.4?`hsl(${heat*30},90%,35%)`:heat<0.7?`hsl(${20+heat*40},95%,55%)`:heat<0.9?`hsl(${45+heat*15},100%,70%)`:"#fefce8";
  ctx.shadowBlur=14+heat*14;ctx.shadowColor=hotColor;
  const g=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  g.addColorStop(0,"#fefce8");g.addColorStop(0.25,hotColor);
  g.addColorStop(0.6,heat<0.5?"#7f1d1d":"#92400e");g.addColorStop(1,"#0c0a09");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Magma cracks
  for(let i=0;i<5;i++){
    const a=(i/5)*Math.PI*2+ts*0.004;
    ctx.strokeStyle=`rgba(253,186,116,${0.3+heat*0.5})`;ctx.lineWidth=1.3;ctx.globalAlpha=heat;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*0.75,Math.sin(a)*r*0.75);ctx.stroke();
  }
  // Heat indicator ring
  ctx.globalAlpha=1;ctx.strokeStyle=hotColor;ctx.lineWidth=2;
  ctx.beginPath();ctx.arc(0,0,r*1.08,0,Math.PI*2*heat);ctx.stroke();
  ctx.restore();
}
// ── Lightning Rod — static; attracts nearby targets; scores 60pts per attracted target on tap ──
function drawLightningRod(ctx,r,ts,attractCount){
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#fef08a";
  // Rod body — tall thin rectangle
  ctx.fillStyle="#1c1917";ctx.beginPath();ctx.roundRect(-r*0.12,-r*0.88,r*0.24,r*1.7,r*0.05);ctx.fill();
  // Metal sheen
  ctx.fillStyle="#92400e";ctx.beginPath();ctx.roundRect(-r*0.08,-r*0.82,r*0.16,r*1.55,r*0.04);ctx.fill();
  // Electric crown at top
  const crownFlash=Math.sin(ts*0.03)>0.3;
  ctx.shadowBlur=crownFlash?24:10;ctx.shadowColor="#fef08a";
  ctx.fillStyle=crownFlash?"#fef08a":"#fbbf24";
  ctx.beginPath();ctx.arc(0,-r*0.82,r*0.2,0,Math.PI*2);ctx.fill();
  // Lightning bolts outward
  if(crownFlash){
    for(let i=0;i<4;i++){
      const a=(i/4)*Math.PI*2+(ts*0.02);
      ctx.strokeStyle="#fef08a";ctx.lineWidth=1.5;ctx.globalAlpha=0.85;
      ctx.beginPath();ctx.moveTo(0,-r*0.82);ctx.lineTo(Math.cos(a)*r*(0.7+Math.random()*0.5),Math.sin(a)*r*(0.7+Math.random()*0.5)-r*0.82);ctx.stroke();
    }
  }
  // Count badge
  if(attractCount>0){
    ctx.globalAlpha=1;ctx.fillStyle="#fbbf24";
    ctx.font=`bold ${Math.round(r*0.3)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText(`+${attractCount}`,r*0.55,r*0.3);
  }
  ctx.restore();
}
// ── Prism Gate — tap to release 3 colored gem shards that scatter; catch them for bonus ──
function drawPrismGate(ctx,r,ts){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.01);
  ctx.shadowBlur=18;ctx.shadowColor="#67e8f9";
  // Triangle prism body
  const h=r*1.1*pulse;
  ctx.fillStyle="#082f49";
  ctx.beginPath();ctx.moveTo(0,-h*0.6);ctx.lineTo(h*0.5,h*0.4);ctx.lineTo(-h*0.5,h*0.4);ctx.closePath();ctx.fill();
  // Rainbow facets
  [["#f87171","#ef4444"],["#4ade80","#16a34a"],["#60a5fa","#2563eb"]].forEach(([c1,c2],i)=>{
    const ai=(i/3)*Math.PI*2;
    ctx.fillStyle=c1;ctx.globalAlpha=0.6;
    ctx.beginPath();ctx.moveTo(0,0);
    ctx.lineTo(Math.cos(ai)*r*0.75,Math.sin(ai)*r*0.75);
    ctx.lineTo(Math.cos(ai+Math.PI*0.65)*r*0.75,Math.sin(ai+Math.PI*0.65)*r*0.75);
    ctx.closePath();ctx.fill();
  });
  // Light beam (white line from top)
  ctx.globalAlpha=0.7;ctx.strokeStyle="#fff";ctx.lineWidth=2;
  ctx.beginPath();ctx.moveTo(0,-h*0.55);ctx.lineTo(0,-h*0.2);ctx.stroke();
  ctx.globalAlpha=1;ctx.restore();
}
// ── Bubble Cluster — 5 linked soap bubbles; pop them all in one tap for bonus ──
function drawBubbleCluster(ctx,r,ts,popped){
  ctx.save();
  const positions=[{x:0,y:0},{x:r*0.52,y:-r*0.42},{x:-r*0.5,y:-r*0.4},{x:r*0.46,y:r*0.44},{x:-r*0.48,y:r*0.44}];
  positions.forEach((p,i)=>{
    if(popped&&popped[i])return;
    const br=r*(i===0?0.42:0.32);
    const hue=(i*72+ts*0.12)%360;
    ctx.save();ctx.translate(p.x,p.y);
    ctx.globalAlpha=0.55;ctx.fillStyle=`hsl(${hue},80%,75%)`;
    ctx.beginPath();ctx.arc(0,0,br,0,Math.PI*2);ctx.fill();
    ctx.globalAlpha=0.85;ctx.strokeStyle=`hsl(${hue},90%,85%)`;ctx.lineWidth=1.5;
    ctx.beginPath();ctx.arc(0,0,br,0,Math.PI*2);ctx.stroke();
    ctx.globalAlpha=0.55;ctx.fillStyle="#fff";
    ctx.beginPath();ctx.arc(-br*0.3,-br*0.3,br*0.22,0,Math.PI*2);ctx.fill();
    ctx.restore();
  });
  ctx.globalAlpha=1;ctx.restore();
}
// ── Void Shard — dark matter shard; absorbs pts from nearby targets while alive; tap to reclaim ──
function drawVoidShard(ctx,r,ts,absorbedPts){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.013);
  ctx.shadowBlur=20;ctx.shadowColor="#0ea5e9";
  // dark shard body
  ctx.fillStyle="#020617";ctx.globalAlpha=0.95;
  ctx.beginPath();
  ctx.moveTo(0,-r*0.9*pulse);ctx.lineTo(r*0.5,r*0.25);ctx.lineTo(r*0.2,r*0.9);
  ctx.lineTo(-r*0.2,r*0.9);ctx.lineTo(-r*0.5,r*0.25);ctx.closePath();ctx.fill();
  // energy crack lines
  ctx.globalAlpha=0.9;ctx.strokeStyle="#38bdf8";ctx.lineWidth=1.8;
  ctx.beginPath();ctx.moveTo(0,-r*0.6);ctx.lineTo(r*0.1,r*0.2);ctx.lineTo(-r*0.05,r*0.6);ctx.stroke();
  ctx.beginPath();ctx.moveTo(r*0.2,-r*0.1);ctx.lineTo(-r*0.2,r*0.4);ctx.stroke();
  // absorbed pts counter
  ctx.globalAlpha=1;ctx.fillStyle="#38bdf8";
  ctx.font=`bold ${Math.round(r*0.32)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText(`+${absorbedPts||0}`,0,0);ctx.restore();
}
// ── Stasis Field — freezes ring expands outward from center; tap inside ring for jackpot ──
function drawStasisField(ctx,r,ts,agePct){
  ctx.save();
  // Frozen core
  ctx.shadowBlur=14;ctx.shadowColor="#bae6fd";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.6);
  g.addColorStop(0,"#e0f2fe");g.addColorStop(0.5,"#38bdf8");g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.6,0,Math.PI*2);ctx.fill();
  // Ice crystal pattern on core
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2;
    ctx.strokeStyle="#e0f2fe";ctx.lineWidth=1.5;ctx.globalAlpha=0.6;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*0.5,Math.sin(a)*r*0.5);ctx.stroke();
  }
  // Expanding freeze ring
  const ringR=r*0.7+agePct*r*1.5;
  ctx.globalAlpha=Math.max(0,0.9-agePct*0.7);
  ctx.strokeStyle="#38bdf8";ctx.lineWidth=3;
  ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
  ctx.globalAlpha=1;ctx.restore();
}
// ── Crystalline — multi-faceted gem; score depends on how many facets are visible (angle) ──
function drawCrystalline(ctx,r,ts){
  ctx.save();
  const rot=ts*0.008;
  ctx.shadowBlur=18;ctx.shadowColor="#c084fc";
  const facets=7;
  for(let i=0;i<facets;i++){
    const a1=rot+(i/facets)*Math.PI*2;
    const a2=rot+((i+1)/facets)*Math.PI*2;
    const hue=250+i*15;
    ctx.fillStyle=`hsl(${hue},75%,${60+i*4}%)`;ctx.globalAlpha=0.8;
    ctx.beginPath();ctx.moveTo(0,0);
    ctx.lineTo(Math.cos(a1)*r,Math.sin(a1)*r);
    ctx.lineTo(Math.cos((a1+a2)/2)*r*0.7,Math.sin((a1+a2)/2)*r*0.7);
    ctx.lineTo(Math.cos(a2)*r,Math.sin(a2)*r);
    ctx.closePath();ctx.fill();
    ctx.strokeStyle="#e9d5ff";ctx.lineWidth=0.8;ctx.globalAlpha=0.5;ctx.stroke();
  }
  // inner glow
  ctx.globalAlpha=1;ctx.shadowBlur=24;ctx.shadowColor="#f0abfc";
  ctx.fillStyle="#fff";ctx.beginPath();ctx.arc(0,0,r*0.2,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Gravity Well — dark sphere that warps space; distortion ring grows with time ──
function drawGravityWell(ctx,r,ts,agePct){
  ctx.save();
  const pulse=0.95+0.05*Math.sin(ts*0.009);
  ctx.shadowBlur=18;ctx.shadowColor="#7c3aed";
  // Dark core
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.75);
  g.addColorStop(0,"#020617");g.addColorStop(0.5,"#1e1b4b");g.addColorStop(1,"#4c1d95");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.75*pulse,0,Math.PI*2);ctx.fill();
  // Distortion rings at 3 radii
  [0.85,1.05,1.25].forEach((frac,i)=>{
    const ringAlpha=0.5-i*0.12;
    ctx.globalAlpha=ringAlpha*(0.6+0.4*Math.sin(ts*0.008+i*1.2));
    ctx.strokeStyle="#a78bfa";ctx.lineWidth=1.5;
    ctx.beginPath();ctx.arc(0,0,r*frac,0,Math.PI*2);ctx.stroke();
  });
  // Warping dots orbiting the core
  ctx.globalAlpha=1;
  for(let i=0;i<5;i++){
    const a=(i/5)*Math.PI*2+ts*0.007*((i%2===0)?1:-1);
    const dr=r*(0.88+agePct*0.15);
    ctx.fillStyle=`hsl(${265+i*12},80%,75%)`;
    ctx.beginPath();ctx.arc(Math.cos(a)*dr,Math.sin(a)*dr,r*0.07,0,Math.PI*2);ctx.fill();
  }
  ctx.restore();
}
// ── Spectrum Orb — cycles hue through the full rainbow every 1.5s; base pts × hue multiplier ──
function drawSpectrumOrb(ctx,r,ts){
  ctx.save();
  const hue=(ts*0.22)%360;
  const pulse=0.92+0.08*Math.sin(ts*0.013);
  ctx.shadowBlur=20;ctx.shadowColor=`hsl(${hue},100%,60%)`;
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,`hsl(${hue},100%,85%)`);
  g.addColorStop(0.5,`hsl(${(hue+60)%360},90%,55%)`);
  g.addColorStop(1,`hsl(${(hue+120)%360},80%,25%)`);
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // rainbow rim
  for(let i=0;i<12;i++){
    const a=(i/12)*Math.PI*2;
    ctx.fillStyle=`hsl(${(hue+i*30)%360},100%,70%)`;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.arc(Math.cos(a)*r*0.88,Math.sin(a)*r*0.88,r*0.09,0,Math.PI*2);ctx.fill();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#fff";
  ctx.font=`bold ${Math.round(r*0.35)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌈",0,0);ctx.restore();
}
// ── Shard Storm — rotating crystal shards; tap when shards align north for jackpot ──
function drawShardStorm(ctx,r,ts){
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor="#67e8f9";
  // body
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.65);
  g.addColorStop(0,"#ecfeff");g.addColorStop(0.6,"#22d3ee");g.addColorStop(1,"#0c4a6e");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.65,0,Math.PI*2);ctx.fill();
  // rotating crystal shards
  const rotSpeed=ts*0.012;
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+rotSpeed;
    ctx.save();ctx.rotate(a);ctx.translate(r*0.82,0);ctx.rotate(a*0.3);
    ctx.fillStyle=`rgba(103,232,249,${0.7+0.3*Math.sin(ts*0.01+i)})`;
    ctx.beginPath();ctx.moveTo(0,-r*0.16);ctx.lineTo(r*0.07,r*0.06);ctx.lineTo(-r*0.07,r*0.06);ctx.closePath();ctx.fill();
    ctx.restore();
  }
  // alignment indicator — glow at top when shards near 12 o'clock
  const alignScore=Math.abs(Math.sin(rotSpeed));
  if(alignScore>0.92){
    ctx.shadowBlur=22;ctx.shadowColor="#ffd700";ctx.globalAlpha=alignScore;
    ctx.strokeStyle="#ffd700";ctx.lineWidth=2.5;ctx.beginPath();ctx.arc(0,0,r*1.08,0,Math.PI*2);ctx.stroke();
    ctx.globalAlpha=1;
  }
  ctx.restore();
}
// ── Pulse Nova — charges then releases a shockwave pulse; hit during release for max pts ──
function drawPulseNova(ctx,r,ts,chargePct){
  ctx.save();
  const isReleasing=chargePct>=1.0;
  const expandR=isReleasing?r*(1.0+(ts%600)/600*1.8):r*0.6;
  ctx.shadowBlur=isReleasing?28:12;ctx.shadowColor=isReleasing?"#fb7185":"#e11d48";
  // core
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.7);
  g.addColorStop(0,isReleasing?"#fff":"#fda4af");
  g.addColorStop(0.4,isReleasing?"#fb7185":"#e11d48");
  g.addColorStop(1,"#4c0519");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*(isReleasing?0.6:0.4+chargePct*0.3)*0.9,0,Math.PI*2);ctx.fill();
  if(isReleasing){
    // nova ring expands
    const ringAlpha=1-(ts%600)/600;
    ctx.globalAlpha=ringAlpha*0.7;ctx.strokeStyle="#fb7185";ctx.lineWidth=3;
    ctx.beginPath();ctx.arc(0,0,expandR,0,Math.PI*2);ctx.stroke();
    ctx.globalAlpha=1;
  } else {
    // charge indicator ring
    ctx.strokeStyle=`rgba(251,113,133,${0.3+chargePct*0.5})`;ctx.lineWidth=2;
    ctx.beginPath();ctx.arc(0,0,r*1.05,0,Math.PI*2*chargePct);ctx.stroke();
  }
  ctx.restore();
}
// ── Surge Ball — charged electric sphere; creates a chain of lightning on tap ──
function drawSurgeBall(ctx,r,ts){
  ctx.save();
  const pulse=0.8+0.2*Math.abs(Math.sin(ts*0.011));
  ctx.shadowBlur=22*pulse;ctx.shadowColor="#facc15";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#fefce8");grad.addColorStop(0.35,"#facc15");
  grad.addColorStop(0.7,"#ca8a04");grad.addColorStop(1,"#1a0a00");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Electric arcs
  for(let i=0;i<4;i++){
    const a1=(i/4)*Math.PI*2+ts*0.006;
    const a2=a1+Math.PI*0.35;
    ctx.strokeStyle="#fef08a";ctx.lineWidth=1.5;ctx.globalAlpha=0.8*pulse;
    ctx.beginPath();
    ctx.moveTo(Math.cos(a1)*r*0.55,Math.sin(a1)*r*0.55);
    const mx=Math.cos((a1+a2)/2)*r*(0.95+Math.random()*0.2);
    const my=Math.sin((a1+a2)/2)*r*(0.95+Math.random()*0.2);
    ctx.quadraticCurveTo(mx,my,Math.cos(a2)*r*0.55,Math.sin(a2)*r*0.55);
    ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.fillStyle="#fefce8";
  ctx.font=`${Math.round(r*0.6)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,0);ctx.restore();
}
// ── Ticking Clock — counts down; tap as clock hand hits 12 for jackpot ──
function drawTickingClock(ctx,r,ts,secsLeft,totalSecs){
  ctx.save();
  const handAngle=((totalSecs-secsLeft)/totalSecs)*Math.PI*2-Math.PI/2;
  const isNearTop=Math.abs(secsLeft-totalSecs)<0.3||secsLeft<0.3;
  ctx.shadowBlur=isNearTop?22:10;ctx.shadowColor=isNearTop?"#ffd700":"#94a3b8";
  // Clock body
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#1e293b");grad.addColorStop(0.7,"#0f172a");grad.addColorStop(1,"#020617");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Clock rim
  ctx.strokeStyle=isNearTop?"#ffd700":"#475569";ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();
  // Hour markers
  for(let i=0;i<12;i++){
    const a=(i/12)*Math.PI*2-Math.PI/2;
    const big=i%3===0;
    ctx.fillStyle=big?"#94a3b8":"#475569";
    ctx.beginPath();ctx.arc(Math.cos(a)*r*0.82,Math.sin(a)*r*0.82,big?r*0.06:r*0.04,0,Math.PI*2);ctx.fill();
  }
  // Clock hand (sweeps from 12 as time passes)
  ctx.strokeStyle=isNearTop?"#ffd700":"#f1f5f9";ctx.lineWidth=2.5;ctx.lineCap="round";
  ctx.shadowBlur=isNearTop?12:0;ctx.shadowColor="#ffd700";
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(handAngle)*r*0.68,Math.sin(handAngle)*r*0.68);ctx.stroke();
  // Seconds left
  ctx.fillStyle=isNearTop?"#ffd700":"#94a3b8";ctx.font=`bold ${Math.round(r*0.3)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`${Math.ceil(secsLeft)}s`,0,r*0.22);
  ctx.restore();
}
// ── Echo Pulse — emits rings that grow outward; each ring is a scoreable tap ──
function drawEchoPulse(ctx,r,ts,rings){
  ctx.save();
  // Center orb
  ctx.shadowBlur=14;ctx.shadowColor="#c084fc";
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.6);
  g.addColorStop(0,"#e879f9");g.addColorStop(1,"#7e22ce");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.55,0,Math.PI*2);ctx.fill();
  // Pulse rings at expanding radii
  rings.forEach((ring,i)=>{
    const ringR=ring.r||0;const alpha=(ring.maxR-ringR)/ring.maxR;
    ctx.strokeStyle=`rgba(192,132,252,${alpha*0.7})`;ctx.lineWidth=2.5-alpha*1;
    ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
  });
  ctx.fillStyle="#f5d0fe";ctx.font=`bold ${Math.round(r*0.38)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("◎",0,-r*0.02);ctx.restore();
}
// ── Smoke Ring — expands outward; tap it before it fades for pts based on ring size ──
function drawSmokeRing(ctx,r,ts,expandPct){
  ctx.save();
  const rr=r*(0.4+expandPct*1.2);
  const alpha=1-expandPct*0.85;
  ctx.globalAlpha=Math.max(0,alpha);
  ctx.shadowBlur=10;ctx.shadowColor="#94a3b8";
  ctx.strokeStyle="#94a3b8";ctx.lineWidth=3-expandPct*1.8;
  ctx.beginPath();ctx.arc(0,0,rr,0,Math.PI*2);ctx.stroke();
  // Inner glow
  const g=ctx.createRadialGradient(0,0,rr*0.5,0,0,rr);
  g.addColorStop(0,"#e2e8f044");g.addColorStop(1,"#e2e8f000");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,rr,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;ctx.restore();
}
// ── Chain Bomb — connects to nearest bomb; tap it to safely defuse both; risky but 1500pts ──
function drawChainBomb(ctx,r,ts){
  ctx.save();
  const pulse=0.85+0.15*Math.abs(Math.sin(ts*0.012));
  ctx.shadowBlur=18;ctx.shadowColor="#ff6700";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#fde68a");grad.addColorStop(0.4,"#f97316");grad.addColorStop(1,"#7f1d1d");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Chain links decoration
  ctx.strokeStyle="#f97316";ctx.lineWidth=2;
  for(let i=0;i<4;i++){
    const a=(i/4)*Math.PI*2;const d=r*0.75;
    ctx.beginPath();ctx.arc(Math.cos(a)*d,Math.sin(a)*d,r*0.15,0,Math.PI*2);ctx.stroke();
  }
  ctx.fillStyle="#fff";ctx.font=`bold ${Math.round(r*0.65)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💣",0,0);ctx.restore();
}
// ── Warp Gate — small teleporter; tap and player jumps all OTHER targets to random positions ──
function drawWarpGate(ctx,r,ts){
  ctx.save();
  const spin=ts*0.006;
  ctx.shadowBlur=20;ctx.shadowColor="#06b6d4";
  // Outer ring
  ctx.strokeStyle="#06b6d4";ctx.lineWidth=2.5;ctx.globalAlpha=0.8;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();
  ctx.globalAlpha=1;
  // Inner vortex
  const g=ctx.createRadialGradient(0,0,0,0,0,r*0.65);
  g.addColorStop(0,"#e0f9ff");g.addColorStop(0.4,"#06b6d4");g.addColorStop(1,"#083344");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r*0.65,0,Math.PI*2);ctx.fill();
  // Animated dashes around ring
  ctx.setLineDash([8,6]);ctx.lineDashOffset=-spin*40;
  ctx.strokeStyle="#67e8f9";ctx.lineWidth=1.5;ctx.globalAlpha=0.6;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);ctx.globalAlpha=1;
  ctx.font=`${Math.round(r*0.7)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌀",0,0);ctx.restore();
}
// ── Mirage — appears as a normal legendary but is actually just 50pts (decoy); rare miss if tapped ──
function drawMirage(ctx,r,ts){
  ctx.save();
  const shimmer=0.6+0.4*Math.abs(Math.sin(ts*0.009));
  // Fake legendary appearance
  ctx.shadowBlur=20*shimmer;ctx.shadowColor="#ffd700";
  const g=ctx.createRadialGradient(0,0,0,0,0,r);
  g.addColorStop(0,"#fffde7");g.addColorStop(0.3,"#fbbf24");g.addColorStop(0.7,"#b45309");g.addColorStop(1,"#78350f");
  ctx.fillStyle=g;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Heat haze distortion hint
  for(let i=0;i<3;i++){
    const offset=Math.sin(ts*0.015+i*1.4)*r*0.12;
    ctx.strokeStyle=`rgba(255,220,100,${0.12+i*0.05})`;ctx.lineWidth=1.2;
    ctx.beginPath();ctx.arc(offset,0,r*(0.55+i*0.12),0,Math.PI*2);ctx.stroke();
  }
  ctx.globalAlpha=shimmer;
  ctx.font=`${Math.round(r*0.75)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌟",0,0);ctx.globalAlpha=1;ctx.restore();
}
// ── Solar Drone — small sun-like bot that circles the player area, 2× pts in orbit ──
function drawSolarDrone(ctx,r,ts){
  ctx.save();
  const spin=ts*0.005;
  ctx.shadowBlur=16;ctx.shadowColor="#fbbf24";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#fffde7");grad.addColorStop(0.45,"#fbbf24");grad.addColorStop(0.8,"#b45309");
  grad.addColorStop(1,"#78350f");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Solar panel wings
  ctx.save();ctx.rotate(spin);
  for(let w=0;w<2;w++){
    ctx.save();ctx.rotate(w*Math.PI);
    ctx.fillStyle="#475569";ctx.fillRect(r*0.7,-r*0.18,r*0.7,r*0.36);
    ctx.fillStyle="#0ea5e9";ctx.fillRect(r*0.72,-r*0.16,r*0.65,r*0.32);
    ctx.restore();
  }
  ctx.restore();
  // Corona rays
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2+spin*0.5;
    ctx.strokeStyle="#fde68a";ctx.lineWidth=1.5;ctx.globalAlpha=0.7;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*1.0,Math.sin(a)*r*1.0);
    ctx.lineTo(Math.cos(a)*r*1.35,Math.sin(a)*r*1.35);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.restore();
}
// ── Crystal Cluster — group of 4 mini crystals; tap to shatter all at once ──
function drawCrystalCluster(ctx,r,ts){
  ctx.save();
  const pulse=0.9+0.1*Math.sin(ts*0.007);
  const offsets=[[0,-r*0.55],[r*0.5,r*0.3],[-r*0.5,r*0.3],[0,r*0.65]];
  const colors=["#38bdf8","#818cf8","#34d399","#f472b6"];
  offsets.forEach(([ox,oy],i)=>{
    ctx.save();ctx.translate(ox*pulse,oy*pulse);
    ctx.shadowBlur=10;ctx.shadowColor=colors[i];ctx.fillStyle=colors[i];
    // Crystal shard shape
    ctx.beginPath();ctx.moveTo(0,-r*0.32);ctx.lineTo(r*0.18,0);ctx.lineTo(0,r*0.32);ctx.lineTo(-r*0.18,0);ctx.closePath();
    ctx.fill();
    // Shine facet
    ctx.strokeStyle="#ffffff55";ctx.lineWidth=0.8;
    ctx.beginPath();ctx.moveTo(0,-r*0.32);ctx.lineTo(r*0.18,0);ctx.stroke();
    ctx.restore();
  });
  // Center connector
  ctx.shadowBlur=8;ctx.shadowColor="#ffffff";ctx.fillStyle="#ffffff";
  ctx.beginPath();ctx.arc(0,0,r*0.12,0,Math.PI*2);ctx.fill();
  ctx.restore();
}
// ── Swarm — tiny target surrounded by 6 orbiting dots; pop any dot to score all ──
function drawSwarm(ctx,r,ts){
  ctx.save();
  const spin=ts*0.008;
  ctx.shadowBlur=12;ctx.shadowColor="#f97316";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r*0.55);
  grad.addColorStop(0,"#fed7aa");grad.addColorStop(1,"#ea580c");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*0.5,0,Math.PI*2);ctx.fill();
  // Orbiting swarm dots
  for(let i=0;i<6;i++){
    const a=spin+(i/6)*Math.PI*2;
    const dx=Math.cos(a)*r*0.82,dy=Math.sin(a)*r*0.82;
    ctx.shadowBlur=8;ctx.shadowColor="#fb923c";
    ctx.fillStyle="#fb923c";ctx.beginPath();ctx.arc(dx,dy,r*0.18,0,Math.PI*2);ctx.fill();
  }
  ctx.fillStyle="#fff7ed";ctx.font=`bold ${Math.round(r*0.38)}px serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🐝",0,0);
  ctx.restore();
}
// ── Celestial Orb — large slow target; worth pts based on which constellation it shows ──
function drawCelestialOrb(ctx,r,ts){
  ctx.save();
  const twinkle=0.75+0.25*Math.abs(Math.sin(ts*0.006));
  ctx.shadowBlur=22;ctx.shadowColor="#1e40af";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#1e3a8a");grad.addColorStop(0.5,"#1e40af");grad.addColorStop(1,"#0c1445");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Star field
  ctx.fillStyle="#e0e7ff";
  const starPos=[[0,-r*0.6],[r*0.4,-r*0.3],[r*0.5,r*0.2],[-r*0.3,r*0.4],[-r*0.5,-r*0.1],[r*0.15,r*0.55]];
  starPos.forEach(([sx,sy])=>{ctx.globalAlpha=twinkle*0.9;ctx.beginPath();ctx.arc(sx,sy,1.6,0,Math.PI*2);ctx.fill();});
  // Connect constellation lines
  ctx.strokeStyle="#93c5fd";ctx.lineWidth=0.7;ctx.globalAlpha=0.4;
  ctx.beginPath();ctx.moveTo(starPos[0][0],starPos[0][1]);
  starPos.slice(1).forEach(p=>ctx.lineTo(p[0],p[1]));ctx.stroke();
  ctx.globalAlpha=1;
  ctx.font=`${Math.round(r*0.52)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌠",0,-r*0.05);ctx.restore();
}
// ── Fractal — geometric burst; splits into 2 smaller fractals on tap ──
function drawFractal(ctx,r,ts,depth){
  ctx.save();
  const spin=(ts*0.002*(depth===0?1:2))%(Math.PI*2);
  ctx.rotate(spin);
  const hue=(ts*0.06+depth*60)%360;
  ctx.shadowBlur=14;ctx.shadowColor=`hsl(${hue},90%,65%)`;
  // Draw fractal triangle
  const vertices=3+depth;
  ctx.beginPath();
  for(let i=0;i<vertices;i++){const a=(i/vertices)*Math.PI*2;ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);}
  ctx.closePath();
  const grad=ctx.createLinearGradient(-r,-r,r,r);
  grad.addColorStop(0,`hsl(${hue},100%,70%)`);
  grad.addColorStop(1,`hsl(${(hue+120)%360},80%,40%)`);
  ctx.fillStyle=grad;ctx.fill();
  // Inner fractal pattern
  if(depth===0){
    ctx.rotate(-spin);ctx.scale(0.55,0.55);ctx.rotate(spin*1.6);
    ctx.beginPath();
    for(let i=0;i<6;i++){const a=(i/6)*Math.PI*2;ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);}
    ctx.closePath();
    ctx.strokeStyle=`hsla(${(hue+60)%360},100%,85%,0.6)`;ctx.lineWidth=1.5;ctx.stroke();
  }
  ctx.restore();
}
// ── Plasma — charges up over lifetime; top pts at 70-90% charge ──
function drawPlasma(ctx,r,ts,chargePct){
  ctx.save();
  const surge=chargePct>0.7&&chargePct<0.9;
  ctx.shadowBlur=surge?28:12;ctx.shadowColor=surge?"#00ffcc":"#22d3ee";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,surge?"#ccfff5":"#a5f3fc");
  grad.addColorStop(0.4,surge?"#00ffcc":"#22d3ee");
  grad.addColorStop(1,surge?"#0e7490":"#164e63");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*(0.8+chargePct*0.25),0,Math.PI*2);ctx.fill();
  // Charge arcs
  for(let i=0;i<5;i++){
    const a=(i/5)*Math.PI*2+ts*0.004*(i%2===0?1:-1);
    const arcLen=Math.PI*2*chargePct*(0.3+i*0.06);
    ctx.strokeStyle=`hsla(${180+i*15},100%,75%,${0.4+chargePct*0.4})`;
    ctx.lineWidth=surge?2.5:1.5;ctx.shadowBlur=surge?16:8;
    ctx.beginPath();ctx.arc(0,0,r*(0.55+i*0.06),a,a+arcLen);ctx.stroke();
  }
  // Charge % indicator at top
  const bw=r*1.4,bh=r*0.2;
  ctx.fillStyle="#00000055";ctx.fillRect(-bw/2,-r-bh-4,bw,bh);
  ctx.fillStyle=surge?"#00ffcc":"#22d3ee";ctx.shadowBlur=surge?12:0;
  ctx.fillRect(-bw/2,-r-bh-4,bw*chargePct,bh);
  ctx.globalAlpha=1;ctx.restore();
}
// ── Timewarp Target — holographic clock; extends all target lifetimes when tapped ──
function drawTimewarpTarget(ctx,r,ts){
  ctx.save();
  const pulse=0.85+0.15*Math.abs(Math.sin(ts*0.008));
  ctx.shadowBlur=18;ctx.shadowColor="#818cf8";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#e0e7ff");grad.addColorStop(0.5,"#6366f1");grad.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  // Clock face
  ctx.strokeStyle="#c7d2fe";ctx.lineWidth=1;ctx.beginPath();ctx.arc(0,0,r*0.75,0,Math.PI*2);ctx.stroke();
  // Clock hands (animating backward = time reversal)
  const hs=(ts*-0.003)%(Math.PI*2);const ms=(ts*-0.012)%(Math.PI*2);
  ctx.strokeStyle="#ffffff";ctx.lineWidth=2;ctx.lineCap="round";
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(hs-Math.PI/2)*r*0.48,Math.sin(hs-Math.PI/2)*r*0.48);ctx.stroke();
  ctx.lineWidth=1.5;
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(ms-Math.PI/2)*r*0.64,Math.sin(ms-Math.PI/2)*r*0.64);ctx.stroke();
  // +2s label
  ctx.fillStyle="#fde68a";ctx.font=`bold ${Math.round(r*0.3)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("+2s",0,r*0.12);ctx.restore();
}
// ── Thunderclap — dark storm orb; shockwave on tap that knocks back all targets ──
function drawThunderclap(ctx,r,ts){
  ctx.save();
  const jitter=Math.random()<0.08?2:0;
  ctx.shadowBlur=20+jitter*8;ctx.shadowColor="#facc15";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#1e1b4b");grad.addColorStop(0.4,"#312e81");grad.addColorStop(0.8,"#4338ca");
  grad.addColorStop(1,"#6366f1");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Concentric rings
  for(let i=1;i<=3;i++){
    ctx.strokeStyle=`rgba(253,224,71,${0.4-i*0.1})`;ctx.lineWidth=1.2;
    ctx.beginPath();ctx.arc(0,0,r*(0.35+i*0.2),0,Math.PI*2);ctx.stroke();
  }
  // Lightning bolt
  ctx.strokeStyle="#fde047";ctx.lineWidth=2;ctx.shadowBlur=10;
  const flicker=now=>0.7+0.3*Math.abs(Math.sin(ts*0.02));
  ctx.globalAlpha=flicker(ts)*0.9;
  ctx.beginPath();ctx.moveTo(-r*0.1,-r*0.5);ctx.lineTo(r*0.2,-r*0.05);ctx.lineTo(-r*0.05,-r*0.05);ctx.lineTo(r*0.1,r*0.5);ctx.stroke();
  ctx.globalAlpha=1;
  ctx.fillStyle="#fde047";ctx.font=`${Math.round(r*0.55)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,0);ctx.restore();
}
// ── Mirror Shard — small reflective diamond; worth more each tap (up to 3 taps) ──
function drawMirrorShard(ctx,r,ts,tapsLeft,maxTaps){
  ctx.save();
  const phase=(maxTaps-tapsLeft)/maxTaps;
  const brightness=0.5+phase*0.5;
  ctx.shadowBlur=12+phase*16;ctx.shadowColor=`rgba(200,220,255,${0.5+phase*0.5})`;
  // Diamond shape
  const spin=(ts*0.002)*phase*2;ctx.rotate(spin);
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(r*0.6,0);ctx.lineTo(0,r);ctx.lineTo(-r*0.6,0);ctx.closePath();
  const grad=ctx.createLinearGradient(-r*0.6,-r,r*0.6,r);
  grad.addColorStop(0,`rgba(${Math.round(200+phase*55)},240,255,0.95)`);
  grad.addColorStop(0.35,`rgba(100,180,${Math.round(200+phase*55)},0.8)`);
  grad.addColorStop(1,`rgba(${Math.round(60+phase*80)},120,200,0.9)`);
  ctx.fillStyle=grad;ctx.fill();
  // Facet lines
  ctx.strokeStyle=`rgba(255,255,255,${0.3+phase*0.5})`;ctx.lineWidth=0.8;
  ctx.beginPath();ctx.moveTo(0,-r);ctx.lineTo(0,r);ctx.stroke();
  ctx.beginPath();ctx.moveTo(-r*0.6,0);ctx.lineTo(r*0.6,0);ctx.stroke();
  // Tap counter
  ctx.rotate(-spin);
  ctx.fillStyle=tapsLeft===1?"#ffd700":"#e0f2fe";ctx.font=`bold ${Math.round(r*0.4)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`×${tapsLeft}`,0,0);
  ctx.restore();
}
// ── Rune Stone — ancient glowing stone; power rune changes every 1.5s ──
function drawRuneStone(ctx,r,ts,runePhase){
  ctx.save();
  const runes=["ᚱ","ᚦ","ᚨ","ᚲ","ᚷ","ᛁ","ᛃ","ᛗ"];
  const rune=runes[runePhase%runes.length];
  ctx.shadowBlur=16;ctx.shadowColor="#a855f7";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#2d1b69");grad.addColorStop(0.6,"#4c1d95");grad.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=grad;
  // Stone polygon (hexagonal rock shape)
  ctx.beginPath();
  for(let i=0;i<6;i++){const a=i/6*Math.PI*2;ctx.lineTo(Math.cos(a)*r*(0.85+0.08*Math.sin(i)),Math.sin(a)*r*(0.85+0.08*Math.cos(i)));}
  ctx.closePath();ctx.fill();
  // Rune glow
  ctx.shadowBlur=20;ctx.shadowColor="#d8b4fe";ctx.fillStyle="#d8b4fe";
  ctx.font=`bold ${Math.round(r*0.72)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.globalAlpha=0.7+0.3*Math.abs(Math.sin(ts*0.008));
  ctx.fillText(rune,0,0);ctx.globalAlpha=1;ctx.restore();
}
// ── Prism Bomb — triangular prism; sends 3 rainbow beams on tap ──
function drawPrismBomb(ctx,r,ts){
  const hue=(ts*0.1)%360;
  ctx.save();
  ctx.shadowBlur=16;ctx.shadowColor=`hsl(${hue},100%,70%)`;
  // Triangle prism shape
  ctx.fillStyle=`hsl(${hue},80%,60%)`;
  ctx.beginPath();
  for(let i=0;i<3;i++){const a=(i/3)*Math.PI*2-Math.PI/2;ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);}
  ctx.closePath();ctx.fill();
  // Inner light beam glow
  ctx.fillStyle=`hsla(${(hue+180)%360},100%,90%,0.7)`;ctx.globalAlpha=0.6;
  ctx.beginPath();
  for(let i=0;i<3;i++){const a=(i/3)*Math.PI*2-Math.PI/2;ctx.lineTo(Math.cos(a)*r*0.5,Math.sin(a)*r*0.5);}
  ctx.closePath();ctx.fill();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.72)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌈",0,0);ctx.restore();
}
// ── Ice Cage — freezes nearby targets; tap to collect all at once ──
function drawIceCage(ctx,r,ts){
  const pulse=0.8+0.2*Math.sin(ts*0.007);
  ctx.save();
  ctx.shadowBlur=16*pulse;ctx.shadowColor="#bae6fd";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"rgba(255,255,255,0.9)");grad.addColorStop(0.5,"#bae6fd");grad.addColorStop(1,"#0c4a6e80");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Ice crystal bars
  ctx.strokeStyle="#7dd3fc";ctx.lineWidth=2;ctx.globalAlpha=0.7;
  for(let i=0;i<6;i++){
    const a=(i/6)*Math.PI*2;ctx.beginPath();
    ctx.moveTo(Math.cos(a)*r*0.3,Math.sin(a)*r*0.3);
    ctx.lineTo(Math.cos(a)*r*0.9,Math.sin(a)*r*0.9);ctx.stroke();
  }
  ctx.globalAlpha=0.4;ctx.strokeStyle="#e0f2fe";ctx.lineWidth=1;
  ctx.beginPath();ctx.arc(0,0,r*0.6,0,Math.PI*2);ctx.stroke();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.82)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🧊",0,0);ctx.restore();
}
// ── Solar Flare — expands outward; smaller = more points ──
function drawSolarFlare(ctx,r,ts,growPct){
  const pct=growPct||0;
  const heat=0.6+0.4*Math.sin(ts*0.016);
  const hue=15+pct*25; // orange → yellow as it grows
  ctx.save();
  ctx.shadowBlur=16+pct*20;ctx.shadowColor=`hsl(${hue},100%,60%)`;
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#fff7ed");grad.addColorStop(0.4,`hsl(${hue},100%,65%)`);
  grad.addColorStop(0.8,`hsl(${hue+20},100%,45%)`);
  grad.addColorStop(1,"transparent");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Coronal rays
  const rays=6+Math.floor(pct*4);
  ctx.strokeStyle=`hsl(${hue},100%,75%)`;ctx.lineWidth=1.5;ctx.globalAlpha=0.4*heat;
  for(let i=0;i<rays;i++){
    const a=(i/rays)*Math.PI*2+ts*0.0006;const len=r*(0.25+Math.sin(ts*0.012+i)*0.15);
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.8,Math.sin(a)*r*0.8);ctx.lineTo(Math.cos(a)*(r*0.8+len),Math.sin(a)*(r*0.8+len));ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.62)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("☀️",0,0);ctx.restore();
}
// ── Magma — hunts the nearest normal target; defuse before it destroys one ──
function drawMagma(ctx,r,ts){
  const drip=Math.sin(ts*0.011)*0.1;
  ctx.save();
  ctx.shadowBlur=18;ctx.shadowColor="#dc2626";
  const grad=ctx.createRadialGradient(0,r*0.1,0,0,0,r);
  grad.addColorStop(0,"#fde68a");grad.addColorStop(0.3,"#f97316");
  grad.addColorStop(0.7,"#dc2626");grad.addColorStop(1,"#450a0a");
  ctx.fillStyle=grad;ctx.beginPath();
  // Lava drop shape
  ctx.moveTo(0,-r*(0.9+drip));
  ctx.bezierCurveTo(r*0.85,-r*0.4,r*0.85,r*0.6,0,r*(0.9-drip));
  ctx.bezierCurveTo(-r*0.85,r*0.6,-r*0.85,-r*0.4,0,-r*(0.9+drip));
  ctx.fill();
  // Cracks
  ctx.strokeStyle="#fde68a";ctx.lineWidth=1;ctx.globalAlpha=0.5;
  ctx.beginPath();ctx.moveTo(-r*0.2,-r*0.3);ctx.lineTo(r*0.1,r*0.2);ctx.lineTo(-r*0.1,r*0.5);ctx.stroke();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.82)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌋",0,0);ctx.restore();
}
// ── Chain Collector — auto-grabs 3 nearest targets on tap ──
function drawChainCollector(ctx,r,ts){
  const pulse=0.8+0.2*Math.sin(ts*0.009);
  ctx.save();
  ctx.shadowBlur=16*pulse;ctx.shadowColor="#34d399";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#a7f3d0");grad.addColorStop(0.5,"#10b981");grad.addColorStop(1,"#064e3b");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Chain links
  ctx.strokeStyle="#34d399";ctx.lineWidth=1.8;ctx.globalAlpha=0.7;
  const links=4;
  for(let i=0;i<links;i++){
    const a=(i/links)*Math.PI*2+ts*0.001;
    const lx=Math.cos(a)*r*0.72,ly=Math.sin(a)*r*0.72;
    ctx.save();ctx.translate(lx,ly);ctx.rotate(a);
    ctx.beginPath();ctx.ellipse(0,0,r*0.18,r*0.1,0,0,Math.PI*2);ctx.stroke();
    ctx.restore();
  }
  // Connection beams
  ctx.globalAlpha=0.3*pulse;ctx.strokeStyle="#6ee7b7";ctx.lineWidth=1;
  for(let i=0;i<3;i++){
    const a=(i/3)*Math.PI*2+ts*0.0015;
    ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r*1.3,Math.sin(a)*r*1.3);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.82)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⛓️",0,0);ctx.restore();
}
// ── Stardust — tap to release star shower (8 collectible stars) ──
function drawStardust(ctx,r,ts){
  const twinkle=0.6+0.4*Math.sin(ts*0.014);
  ctx.save();
  ctx.shadowBlur=18*twinkle;ctx.shadowColor="#fde68a";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#fffbeb");grad.addColorStop(0.4,"#fde68a");grad.addColorStop(1,"#92400e00");
  ctx.globalAlpha=twinkle;ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Star shape
  ctx.save();ctx.rotate(ts*0.0005);ctx.fillStyle="#fbbf24";ctx.globalAlpha=0.8*twinkle;
  ctx.beginPath();
  for(let i=0;i<10;i++){
    const a=(i/10)*Math.PI*2-Math.PI/2;const rr=i%2===0?r*0.9:r*0.4;
    i===0?ctx.moveTo(Math.cos(a)*rr,Math.sin(a)*rr):ctx.lineTo(Math.cos(a)*rr,Math.sin(a)*rr);
  }
  ctx.closePath();ctx.fill();ctx.restore();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.72)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⭐",0,0);ctx.restore();
}
// ── Bloom — flower that splits into 5 scoreable petals on tap ──
function drawBloom(ctx,r,ts){
  const sway=Math.sin(ts*0.006)*0.12;
  ctx.save();
  ctx.rotate(sway);
  const petalColors=["#ff6b9d","#ff9e9e","#ffd1dc","#ffb347","#ff85c1"];
  // Petals
  for(let i=0;i<5;i++){
    const a=(i/5)*Math.PI*2-Math.PI/2;
    ctx.save();ctx.rotate(a);ctx.translate(0,-r*0.42);
    ctx.fillStyle=petalColors[i];ctx.shadowColor=petalColors[i];ctx.shadowBlur=8;
    ctx.beginPath();ctx.ellipse(0,0,r*0.36,r*0.5,0,0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
  // Center
  ctx.shadowBlur=12;ctx.shadowColor="#ffd700";
  ctx.fillStyle="#ffd700";ctx.beginPath();ctx.arc(0,0,r*0.32,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.55)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌸",0,0);ctx.restore();
}
// ── Reflector — reverses all moving targets' direction on tap ──
function drawReflector(ctx,r,ts){
  const rot=ts*0.001;
  ctx.save();ctx.rotate(rot);
  ctx.shadowBlur=14;ctx.shadowColor="#67e8f9";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#e0f7ff");grad.addColorStop(0.5,"#22d3ee");grad.addColorStop(1,"#0e7490");
  ctx.fillStyle=grad;
  // Hexagon shape
  ctx.beginPath();
  for(let i=0;i<6;i++){const a=(i/6)*Math.PI*2-Math.PI/6;ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);}
  ctx.closePath();ctx.fill();
  // Mirror lines
  ctx.strokeStyle="#ffffff";ctx.lineWidth=1.5;ctx.globalAlpha=0.6;
  ctx.beginPath();ctx.moveTo(-r*0.55,0);ctx.lineTo(r*0.55,0);ctx.stroke();
  ctx.beginPath();ctx.moveTo(-r*0.28,-r*0.48);ctx.lineTo(r*0.28,r*0.48);ctx.stroke();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.75)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🪞",0,0);ctx.restore();
}
// ── Quantum — flickers between positions; needs 3 taps to stabilize and score ──
function drawQuantum(ctx,r,ts,hitsLeft,maxHits){
  const flicker=0.4+0.6*Math.abs(Math.sin(ts*0.025));
  const hue=(ts*0.09)%360;
  ctx.save();
  ctx.globalAlpha=flicker;
  ctx.shadowBlur=16;ctx.shadowColor=`hsl(${hue},100%,70%)`;
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#ffffff");
  grad.addColorStop(0.4,`hsl(${hue},100%,70%)`);
  grad.addColorStop(1,`hsl(${(hue+180)%360},100%,30%)`);
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Quantum uncertainty rings
  ctx.strokeStyle=`hsl(${hue},100%,80%)`;ctx.lineWidth=1.5;
  ctx.globalAlpha=0.3*flicker;
  ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2);ctx.stroke();
  ctx.beginPath();ctx.arc(0,0,r*1.5,0,Math.PI*2);ctx.stroke();
  ctx.globalAlpha=1;
  // Tap counter
  const remaining=hitsLeft||1;
  ctx.fillStyle="#ffffff";ctx.font=`bold ${Math.round(r*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText(`${remaining}`,0,0);
  ctx.restore();
}
// ── Clockwork — slows time when tapped; gear-like design ──
function drawClockwork(ctx,r,ts){
  const rot=ts*0.0008;
  ctx.save();
  ctx.shadowBlur=14;ctx.shadowColor="#94a3b8";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#e2e8f0");grad.addColorStop(0.6,"#64748b");grad.addColorStop(1,"#1e293b");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Gear teeth
  ctx.save();ctx.rotate(rot);ctx.strokeStyle="#94a3b8";ctx.lineWidth=r*0.22;
  const teeth=8;
  for(let i=0;i<teeth;i++){
    const a=(i/teeth)*Math.PI*2;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.78,Math.sin(a)*r*0.78);ctx.lineTo(Math.cos(a)*r*1.08,Math.sin(a)*r*1.08);ctx.stroke();
  }
  ctx.restore();
  // Clock hands
  ctx.strokeStyle="#e2e8f0";ctx.lineWidth=1.8;ctx.lineCap="round";
  const sec=ts/500;
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(sec)*r*0.55,Math.sin(sec)*r*0.55);ctx.stroke();
  ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(sec*0.0833)*r*0.38,Math.sin(sec*0.0833)*r*0.38);ctx.stroke();
  // Center dot
  ctx.fillStyle="#ffffff";ctx.beginPath();ctx.arc(0,0,r*0.08,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.82)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⏰",0,0);ctx.restore();
}
// ── Thunderbolt — lightning bolt falling from top; tap fast or lose a life ──
function drawThunderbolt(ctx,r,ts){
  const pulse=0.7+0.3*Math.abs(Math.sin(ts*0.02));
  ctx.save();
  ctx.shadowBlur=20*pulse;ctx.shadowColor="#facc15";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#fef08a");grad.addColorStop(0.5,"#facc15");grad.addColorStop(1,"#78350f");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Lightning bolt shape
  ctx.strokeStyle="#ffffff";ctx.lineWidth=2;ctx.globalAlpha=0.9*pulse;
  ctx.beginPath();
  ctx.moveTo(r*0.15,-r*0.6);ctx.lineTo(-r*0.1,-r*0.05);ctx.lineTo(r*0.25,-r*0.05);
  ctx.lineTo(-r*0.15,r*0.6);ctx.lineTo(r*0.05,r*0.1);ctx.lineTo(-r*0.2,r*0.1);
  ctx.closePath();ctx.fillStyle="#fef9c3";ctx.fill();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.78)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("⚡",0,0);ctx.restore();
}
// ── Gravity Orb — pulls normal targets; tap to score + release them ──
function drawGravityOrb(ctx,r,ts){
  const spin=ts*0.0015;
  ctx.save();
  ctx.shadowBlur=24;ctx.shadowColor="#7c3aed";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#a78bfa");grad.addColorStop(0.6,"#6d28d9");grad.addColorStop(1,"#1e1b4b");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Orbiting rings
  for(let i=0;i<2;i++){
    ctx.save();ctx.rotate(spin+(i*Math.PI));
    ctx.strokeStyle=`rgba(167,139,250,${0.35+i*0.1})`;ctx.lineWidth=1.5;
    ctx.beginPath();ctx.ellipse(0,0,r*0.92,r*0.3,0,0,Math.PI*2);ctx.stroke();
    ctx.restore();
  }
  // Gravity waves
  ctx.globalAlpha=0.25;ctx.strokeStyle="#a78bfa";ctx.lineWidth=1;
  for(let i=1;i<=3;i++){ctx.beginPath();ctx.arc(0,0,r*(0.6+i*0.22),0,Math.PI*2);ctx.stroke();}
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.88)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🌐",0,0);ctx.restore();
}
// ── Crystal Ball — predicts next target rarity; guaranteed lucky spawn after tap ──
function drawCrystalBall(ctx,r,ts){
  const glow=0.5+0.5*Math.sin(ts*0.005);
  const hue=(ts*0.04)%360;
  ctx.save();
  ctx.shadowBlur=20+10*glow;ctx.shadowColor=`hsl(${hue},100%,70%)`;
  const grad=ctx.createRadialGradient(-r*0.25,-r*0.25,r*0.05,0,0,r);
  grad.addColorStop(0,"rgba(255,255,255,0.9)");
  grad.addColorStop(0.3,`hsla(${hue},80%,70%,0.6)`);
  grad.addColorStop(0.7,`hsla(${(hue+60)%360},100%,40%,0.7)`);
  grad.addColorStop(1,`hsla(${(hue+120)%360},100%,20%,0.8)`);
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Swirling inner mist
  ctx.save();ctx.rotate(ts*0.0008);
  ctx.globalAlpha=0.3*glow;ctx.strokeStyle=`hsl(${hue},100%,90%)`;ctx.lineWidth=1.5;
  ctx.beginPath();ctx.arc(0,0,r*0.45,0,Math.PI*1.5);ctx.stroke();
  ctx.restore();
  // Stand base
  ctx.globalAlpha=0.5;ctx.fillStyle="#4a3728";
  ctx.beginPath();ctx.ellipse(0,r*0.85,r*0.5,r*0.12,0,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.82)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("🔮",0,0);ctx.restore();
}
// ── Firefly — tiny, fast, flickering; hard to hit, big points ──
function drawFirefly(ctx,r,ts){
  const flicker=0.65+0.35*Math.sin(ts*0.012);
  ctx.save();
  ctx.shadowBlur=18*flicker;ctx.shadowColor="#84ef63";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r);
  grad.addColorStop(0,"#ffffff");grad.addColorStop(0.3,"#bbf7d0");grad.addColorStop(1,"#22c55e00");
  ctx.globalAlpha=flicker;ctx.fillStyle=grad;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  const wf=Math.sin(ts*0.018)*0.35;
  ctx.globalAlpha=0.35*flicker;ctx.fillStyle="#bbf7d0";
  ctx.beginPath();ctx.ellipse(-r*0.55,-r*0.1,r*0.65,r*0.25+wf*r*0.15,-0.4,0,Math.PI*2);ctx.fill();
  ctx.beginPath();ctx.ellipse(r*0.55,-r*0.1,r*0.65,r*0.25+wf*r*0.15,0.4,0,Math.PI*2);ctx.fill();
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.88)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("✨",0,0);ctx.restore();
}
// ── Geode — rocky shell; first tap cracks open, second collects the gem ──
function drawGeode(ctx,r,ts,cracked){
  ctx.save();
  if(!cracked){
    ctx.shadowBlur=8;ctx.shadowColor="#94a3b8";ctx.fillStyle="#475569";
    ctx.beginPath();
    const sides=7;
    for(let i=0;i<sides;i++){
      const a=(i/sides)*Math.PI*2;const j=0.78+0.22*Math.sin(i*3.7+ts*0.001);
      const px=Math.cos(a)*r*j,py=Math.sin(a)*r*j;
      i===0?ctx.moveTo(px,py):ctx.lineTo(px,py);
    }
    ctx.closePath();ctx.fill();
    ctx.strokeStyle="#94a3b8";ctx.lineWidth=1.5;ctx.globalAlpha=0.6;
    ctx.beginPath();ctx.moveTo(-r*0.3,-r*0.5);ctx.lineTo(r*0.1,r*0.2);ctx.lineTo(-r*0.1,r*0.6);ctx.stroke();
    ctx.beginPath();ctx.moveTo(r*0.2,-r*0.4);ctx.lineTo(-r*0.1,r*0.1);ctx.stroke();
    ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.78)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText("🪨",0,0);
  } else {
    const hue=(ts*0.08)%360;
    ctx.shadowBlur=22;ctx.shadowColor=`hsl(${hue},100%,70%)`;
    const grad=ctx.createRadialGradient(0,0,0,0,0,r);
    grad.addColorStop(0,"#ffffff");grad.addColorStop(0.4,`hsl(${hue},100%,75%)`);
    grad.addColorStop(1,`hsl(${(hue+120)%360},100%,40%)`);
    ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
    ctx.strokeStyle="#ffffff";ctx.lineWidth=1;ctx.globalAlpha=0.45;
    for(let i=0;i<6;i++){const a=(i/6)*Math.PI*2;ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(Math.cos(a)*r,Math.sin(a)*r);ctx.stroke();}
    ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.88)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText("💎",0,0);
  }
  ctx.restore();
}
// ── Time Bomb — ticking target; tap to defuse or it detonates (−2 lives) ──
function drawTimeBomb(ctx,r,ts,secondsLeft){
  const urgency=secondsLeft!==undefined?Math.max(0,Math.min(1,1-secondsLeft/3)):0.5;
  const pulse=0.85+0.15*Math.sin(ts*(0.008+urgency*0.025));
  ctx.save();
  ctx.shadowBlur=12+urgency*22;ctx.shadowColor=urgency>0.6?"#ef4444":"#f97316";
  const grad=ctx.createRadialGradient(0,0,0,0,0,r*pulse);
  grad.addColorStop(0,urgency>0.6?"#fca5a5":"#fed7aa");
  grad.addColorStop(0.5,urgency>0.6?"#ef4444":"#f97316");
  grad.addColorStop(1,"#111111");
  ctx.fillStyle=grad;ctx.beginPath();ctx.arc(0,0,r*pulse,0,Math.PI*2);ctx.fill();
  const segs=Math.max(1,Math.round(secondsLeft!==undefined?secondsLeft*3:8));
  ctx.strokeStyle=urgency>0.6?"#ef4444":"#fbbf24";ctx.lineWidth=2.5;ctx.globalAlpha=0.8;
  for(let i=0;i<segs;i++){
    const a=(i/9)*Math.PI*2-Math.PI/2;const a2=((i+0.72)/9)*Math.PI*2-Math.PI/2;
    ctx.beginPath();ctx.arc(0,0,r*0.82,a,a2);ctx.stroke();
  }
  ctx.globalAlpha=1;ctx.font=`${Math.round(r*0.88)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";
  ctx.fillText("💣",0,0);ctx.restore();
}
// ── Motion trail + ghost afterimages for moving targets ──
// ── Spectral target — phasing ghost orb; massive pts if caught while visible ──
function drawSpectral(ctx, r, ts) {
  const cycle=1600;const phase=(ts%cycle)/cycle; // 0→1 fade in, then 0 fade out
  const vis=phase<0.5?phase*2:2-phase*2; // triangle wave: 0→1→0
  const pulse=0.5+0.5*Math.sin(ts*0.008);
  // Spectral shimmer aura
  ctx.save();ctx.globalAlpha=vis*0.3*pulse;
  ctx.fillStyle="#e0e7ff";ctx.shadowColor="#818cf8";ctx.shadowBlur=20;
  ctx.beginPath();ctx.arc(0,0,r*1.5,0,Math.PI*2);ctx.fill();
  // Body — fades in and out
  const grd=ctx.createRadialGradient(0,0,0,0,0,r);
  grd.addColorStop(0,"#e0e7ff");grd.addColorStop(0.4,"#a5b4fc");grd.addColorStop(1,"#3730a3");
  ctx.globalAlpha=vis*0.9;ctx.fillStyle=grd;ctx.shadowColor="#818cf8";ctx.shadowBlur=14+8*pulse;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Visible indicator ring (full opacity ring shows when visible)
  if(vis>0.6){
    ctx.globalAlpha=(vis-0.6)*2.5*pulse;ctx.strokeStyle="#fbbf24";ctx.lineWidth=2;
    ctx.shadowColor="#fbbf24";ctx.shadowBlur=10;
    ctx.beginPath();ctx.arc(0,0,r*1.2,0,Math.PI*2);ctx.stroke();
  }
  ctx.fillStyle="#c7d2fe";ctx.font=`bold ${Math.round(r*0.75)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=vis*0.9;
  ctx.fillText("👻",0,1);ctx.restore();
}

// ── Heart target — beats like a heart; tap for +1 life AND coin bonus ──
function drawHeart(ctx, r, ts) {
  const beat=0.5+0.5*Math.sin(ts*0.012);
  const sz=r*(1+beat*0.12);
  ctx.save();ctx.shadowColor="#f43f5e";ctx.shadowBlur=16+10*beat;
  ctx.fillStyle=`hsl(${350+beat*5},90%,${55+beat*5}%)`;
  // Heart shape using bezier curves
  ctx.beginPath();
  ctx.moveTo(0,-sz*0.55);
  ctx.bezierCurveTo(sz*0.55,-sz*0.95,sz*1.1,-sz*0.3,0,sz*0.55);
  ctx.bezierCurveTo(-sz*1.1,-sz*0.3,-sz*0.55,-sz*0.95,0,-sz*0.55);
  ctx.fill();
  // Highlight
  ctx.fillStyle="#ffffff44";ctx.beginPath();
  ctx.ellipse(-sz*0.3,-sz*0.55,sz*0.22,sz*0.15,Math.PI*0.4,0,Math.PI*2);ctx.fill();
  // Label
  ctx.fillStyle="#fff";ctx.font=`bold ${Math.round(sz*0.55)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.9;
  ctx.fillText("+1",0,sz*0.1);ctx.restore();
}

// ── Nova target — pulsing supernova; tap to freeze ALL targets for 2s ──
function drawNova(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.009);
  const boom=0.7+0.3*Math.sin(ts*0.006);
  // Outer corona
  const coronaColors=["#fde68a","#fbbf24","#f59e0b","#d97706"];
  for(let i=3;i>=0;i--){
    const cr=r*(1.2+i*0.35)*boom;
    ctx.save();ctx.globalAlpha=(0.06+i*0.04)*pulse;
    ctx.fillStyle=coronaColors[i]||"#fbbf24";ctx.shadowColor="#fbbf24";ctx.shadowBlur=8;
    ctx.beginPath();ctx.arc(0,0,cr,0,Math.PI*2);ctx.fill();ctx.restore();
  }
  // Star burst rays
  for(let i=0;i<8;i++){
    const a=(i/8)*Math.PI*2+ts*0.003;
    const rl=r*(0.85+0.2*Math.sin(ts*0.007+i));
    ctx.save();ctx.strokeStyle="#fde68a";ctx.lineWidth=2;ctx.globalAlpha=0.6*pulse;
    ctx.shadowColor="#fbbf24";ctx.shadowBlur=10;
    ctx.beginPath();ctx.moveTo(Math.cos(a)*r*0.6,Math.sin(a)*r*0.6);
    ctx.lineTo(Math.cos(a)*rl,Math.sin(a)*rl);ctx.stroke();ctx.restore();
  }
  // Core
  const grd=ctx.createRadialGradient(0,0,0,0,0,r*0.85);
  grd.addColorStop(0,"#fef9c3");grd.addColorStop(0.4,"#fbbf24");grd.addColorStop(1,"#92400e");
  ctx.save();ctx.fillStyle=grd;ctx.shadowColor="#fbbf24";ctx.shadowBlur=20+10*pulse;
  ctx.beginPath();ctx.arc(0,0,r*0.85,0,Math.PI*2);ctx.fill();
  ctx.fillStyle="#fff";ctx.font=`bold ${Math.round(r*0.8)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.95;
  ctx.fillText("💫",0,1);ctx.restore();
}

// ── Beacon target — stationary golden lighthouse; nearby taps score double while it lives ──
function drawBeacon(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.007);
  // Radiating rings (influence area indicator)
  for(let i=3;i>0;i--){
    ctx.save();ctx.strokeStyle="#fbbf24";ctx.lineWidth=1;ctx.globalAlpha=(0.06+i*0.03)*pulse;
    ctx.shadowColor="#fbbf24";ctx.shadowBlur=4;
    ctx.beginPath();ctx.arc(0,0,r*(1.5+i*0.6),0,Math.PI*2);ctx.stroke();ctx.restore();
  }
  // Core golden orb
  const grd=ctx.createRadialGradient(-r*0.2,-r*0.2,0,0,0,r);
  grd.addColorStop(0,"#fef9c3");grd.addColorStop(0.4,"#fbbf24");grd.addColorStop(1,"#b45309");
  ctx.save();ctx.fillStyle=grd;ctx.shadowColor="#fbbf24";ctx.shadowBlur=18+8*pulse;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Beacon beam upward
  const beamH=r*2.5;
  const beamGrd=ctx.createLinearGradient(0,-r,0,-r-beamH);
  beamGrd.addColorStop(0,"#fbbf2488");beamGrd.addColorStop(1,"transparent");
  ctx.globalAlpha=0.4+0.3*pulse;ctx.fillStyle=beamGrd;
  ctx.beginPath();ctx.moveTo(-r*0.3,-r);ctx.lineTo(r*0.3,-r);
  ctx.lineTo(r*0.6,-r-beamH);ctx.lineTo(-r*0.6,-r-beamH);ctx.closePath();ctx.fill();
  ctx.fillStyle="#fff";ctx.font=`bold ${Math.round(r*0.8)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.95;
  ctx.fillText("🔆",0,1);ctx.restore();
}

// ── Shadow target — leaves a clone shadow on tap; tap shadow for bonus pts ──
function drawShadow(ctx, r, ts, isShadowClone) {
  const pulse=0.5+0.5*Math.sin(ts*0.009);
  if(isShadowClone){
    // Shadow clone: ghostly dark silhouette
    ctx.save();ctx.globalAlpha=0.45*pulse;
    ctx.fillStyle="#1e1b4b";ctx.shadowColor="#818cf8";ctx.shadowBlur=12;
    ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
    ctx.fillStyle="#818cf8";ctx.font=`bold ${Math.round(r*0.7)}px sans-serif`;
    ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.7*pulse;
    ctx.fillText("👤",0,1);ctx.restore();
    return;
  }
  // Main shadow target: deep indigo with mirror effect
  const grd=ctx.createRadialGradient(0,0,0,0,0,r);
  grd.addColorStop(0,"#4338ca");grd.addColorStop(0.5,"#3730a3");grd.addColorStop(1,"#1e1b4b");
  ctx.save();ctx.fillStyle=grd;ctx.shadowColor="#6366f1";ctx.shadowBlur=16+6*pulse;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Dark shimmer waves
  for(let i=0;i<3;i++){
    const a=(i/3)*Math.PI*2+ts*0.004;
    ctx.strokeStyle="#818cf8";ctx.lineWidth=1;ctx.globalAlpha=0.3*pulse;
    ctx.beginPath();ctx.arc(Math.cos(a)*r*0.3,Math.sin(a)*r*0.3,r*0.4,0,Math.PI*2);ctx.stroke();
  }
  ctx.fillStyle="#c7d2fe";ctx.font=`bold ${Math.round(r*0.8)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.95;
  ctx.fillText("🕶️",0,1);ctx.restore();
}

// ── Portal target — swirling warp gate; teleports all targets when tapped ──
function drawPortal(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.008);
  // Outer ring of portal energy
  for(let ring=3;ring>0;ring--){
    const ringR=r*(0.7+ring*0.22);
    const hue=(ts*0.06+ring*60)%360;
    ctx.save();ctx.globalAlpha=(0.1+ring*0.08)*pulse;
    ctx.strokeStyle=`hsl(${hue},85%,65%)`;ctx.lineWidth=3-ring*0.5;
    ctx.shadowColor=`hsl(${hue},85%,65%)`;ctx.shadowBlur=10;
    ctx.setLineDash([5,4]);ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
    ctx.setLineDash([]);ctx.restore();
  }
  // Spinning inner warp ring
  const spinAngle=ts*0.005;
  for(let i=0;i<8;i++){
    const a=spinAngle+(i/8)*Math.PI*2;
    const hue2=(i*45+ts*0.08)%360;
    ctx.save();ctx.globalAlpha=0.7*pulse;
    ctx.fillStyle=`hsl(${hue2},90%,65%)`;ctx.shadowColor=`hsl(${hue2},90%,65%)`;ctx.shadowBlur=8;
    ctx.beginPath();ctx.arc(Math.cos(a)*r*0.58,Math.sin(a)*r*0.58,3,0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
  // Dark core with ∞ symbol
  const coreGrd=ctx.createRadialGradient(0,0,0,0,0,r*0.7);
  coreGrd.addColorStop(0,"#0f0f2a");coreGrd.addColorStop(0.6,"#1e1b4b");coreGrd.addColorStop(1,"#312e81cc");
  ctx.save();ctx.fillStyle=coreGrd;ctx.shadowColor="#818cf8";ctx.shadowBlur=16*pulse;
  ctx.beginPath();ctx.arc(0,0,r*0.7,0,Math.PI*2);ctx.fill();
  ctx.fillStyle="#818cf8";ctx.font=`bold ${Math.round(r*0.7)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.9;
  ctx.fillText("🌀",0,1);ctx.restore();
}

// ── Particle Bomb target — explodes into scoreable spark particles on tap ──
function drawParticleBomb(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.009);
  // Pulsing danger ring
  ctx.save();ctx.strokeStyle="#ff4500";ctx.lineWidth=2.5+pulse;ctx.globalAlpha=0.5+0.4*pulse;
  ctx.shadowColor="#ff4500";ctx.shadowBlur=18;
  ctx.beginPath();ctx.arc(0,0,r*1.3+pulse*4,0,Math.PI*2);ctx.stroke();
  // Core orange-red gradient
  const grd=ctx.createRadialGradient(0,0,0,0,0,r);
  grd.addColorStop(0,"#ffed4a");grd.addColorStop(0.4,"#ff8c00");grd.addColorStop(0.8,"#ff4500");grd.addColorStop(1,"#dc2626");
  ctx.globalAlpha=1;ctx.fillStyle=grd;ctx.shadowColor="#ff4500";ctx.shadowBlur=20+pulse*8;
  ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Particle sparks orbiting
  for(let i=0;i<5;i++){
    const a=(i/5)*Math.PI*2+ts*0.006;
    const osc=r*(1.0+0.15*Math.sin(ts*0.012+i));
    ctx.fillStyle=i%2===0?"#ffed4a":"#ff8c00";ctx.globalAlpha=0.8*pulse;
    ctx.beginPath();ctx.arc(Math.cos(a)*osc,Math.sin(a)*osc,3,0,Math.PI*2);ctx.fill();
  }
  ctx.fillStyle="#fff";ctx.font=`bold ${Math.round(r*0.8)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.95;
  ctx.fillText("💥",0,1);ctx.restore();
}

// ── Ricochet target — bouncing silver orb; tap to ricochet-kill 2 nearby targets ──
function drawRicochet(ctx, r, ts) {
  const pulse=0.7+0.3*Math.sin(ts*0.01);
  // Outer bounce ring
  ctx.save();ctx.strokeStyle="#e2e8f0";ctx.lineWidth=2;ctx.globalAlpha=0.5*pulse;
  ctx.shadowColor="#94a3b8";ctx.shadowBlur=10;
  ctx.setLineDash([6,4]);ctx.beginPath();ctx.arc(0,0,r*1.3,0,Math.PI*2);ctx.stroke();
  ctx.setLineDash([]);ctx.restore();
  // Core silver body
  const grd=ctx.createRadialGradient(-r*0.25,-r*0.25,0,0,0,r);
  grd.addColorStop(0,"#f8fafc");grd.addColorStop(0.5,"#94a3b8");grd.addColorStop(1,"#475569");
  ctx.save();ctx.shadowColor="#cbd5e1";ctx.shadowBlur=14+6*pulse;
  ctx.fillStyle=grd;ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fill();
  // Reflection highlight
  ctx.fillStyle="#ffffff66";ctx.beginPath();ctx.arc(-r*0.28,-r*0.3,r*0.28,0,Math.PI*2);ctx.fill();
  // Bounce arrows indicating ricochet
  const arrowAngs=[Math.PI*0.2,Math.PI*0.7,Math.PI*1.2];
  arrowAngs.forEach(a=>{
    const ax=Math.cos(a)*r*0.55,ay=Math.sin(a)*r*0.55;
    ctx.save();ctx.translate(ax,ay);ctx.rotate(a+Math.PI/2);
    ctx.strokeStyle="#fff";ctx.lineWidth=1.5;ctx.globalAlpha=0.7*pulse;
    ctx.beginPath();ctx.moveTo(0,-4);ctx.lineTo(0,4);ctx.lineTo(-3,1);ctx.moveTo(0,4);ctx.lineTo(3,1);
    ctx.stroke();ctx.restore();
  });
  ctx.restore();
}

// ── Aurora target — shimmering northern lights; rhythm-based tap multiplier ──
function drawAurora(ctx, r, ts) {
  const pulse=0.5+0.5*Math.sin(ts*0.007);
  // Aurora wave bands
  const bands=5;
  for(let i=0;i<bands;i++){
    const hue=(i/bands)*120+180+ts*0.04; // shifting blue-green-purple
    const wave=Math.sin(ts*0.006+i*1.2)*r*0.3;
    const bandR=r*(0.6+i*0.18);
    ctx.save();ctx.globalAlpha=(0.12+i*0.04)*pulse;
    ctx.strokeStyle=`hsl(${hue},90%,65%)`;ctx.lineWidth=6-i;
    ctx.shadowColor=`hsl(${hue},90%,65%)`;ctx.shadowBlur=12;
    ctx.beginPath();
    for(let a=0;a<=Math.PI*2;a+=0.1){
      const wr=bandR+wave*Math.sin(a*3+ts*0.003);
      const px=Math.cos(a)*wr,py=Math.sin(a)*wr;
      a===0?ctx.moveTo(px,py):ctx.lineTo(px,py);
    }
    ctx.closePath();ctx.stroke();ctx.restore();
  }
  // Core gem
  const grd=ctx.createRadialGradient(0,0,0,0,0,r*0.85);
  grd.addColorStop(0,"#e0f2fe");grd.addColorStop(0.4,"#38bdf8");grd.addColorStop(0.8,"#0284c7");grd.addColorStop(1,"#0c4a6e");
  ctx.save();ctx.shadowColor="#38bdf8";ctx.shadowBlur=20+10*pulse;
  ctx.fillStyle=grd;ctx.beginPath();ctx.arc(0,0,r*0.85,0,Math.PI*2);ctx.fill();
  ctx.fillStyle="#ffffff88";ctx.beginPath();ctx.arc(-r*0.22,-r*0.25,r*0.22,0,Math.PI*2);ctx.fill();
  ctx.fillStyle="#e0f2fe";ctx.font=`bold ${Math.round(r*0.7)}px sans-serif`;
  ctx.textAlign="center";ctx.textBaseline="middle";ctx.globalAlpha=0.9;
  ctx.fillText("🌌",0,1);ctx.restore();
}

function drawTrail(ctx, t) {
  if(!t.trail||t.trail.length<2) return;
  // Ghost afterimages every 3 frames
  const step=Math.max(1,Math.floor(t.trail.length/4));
  for(let i=0;i<t.trail.length-step;i+=step){
    const tp=t.trail[i];
    const af=i/t.trail.length;
    ctx.save();ctx.globalAlpha=af*0.18;
    ctx.fillStyle=t.color;ctx.shadowColor=t.glow||t.color;ctx.shadowBlur=t.radius*af;
    ctx.beginPath();ctx.arc(tp.x,tp.y,t.radius*(0.4+af*0.5),0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
  // Line trail
  for(let i=1;i<t.trail.length;i++){
    const a=i/t.trail.length;
    ctx.save(); ctx.globalAlpha=a*0.25;
    ctx.strokeStyle=t.color; ctx.lineWidth=t.radius*1.4*a; ctx.lineCap="round";
    ctx.shadowColor=t.color; ctx.shadowBlur=t.radius*a*0.7;
    ctx.beginPath(); ctx.moveTo(t.trail[i-1].x,t.trail[i-1].y); ctx.lineTo(t.trail[i].x,t.trail[i].y);
    ctx.stroke(); ctx.restore();
  }
}

// ── Direction arrow for moving targets ──
function drawArrow(ctx, vx, vy, r, color) {
  const len=Math.hypot(vx,vy); if(len<0.01) return;
  const angle=Math.atan2(vy,vx);
  const ar=r+14, aw=6;
  ctx.save();
  ctx.translate(Math.cos(angle)*ar, Math.sin(angle)*ar);
  ctx.rotate(angle);
  ctx.fillStyle=color; ctx.globalAlpha=0.6; ctx.shadowColor=color; ctx.shadowBlur=6;
  ctx.beginPath(); ctx.moveTo(aw,0); ctx.lineTo(-aw,-aw*0.6); ctx.lineTo(-aw,aw*0.6); ctx.closePath();
  ctx.fill(); ctx.restore();
}

// ── Master target draw dispatcher ──
function drawTarget(ctx, t, ts) {
  const now=Date.now();
  const timeLeft=Math.max(0,1-(now-t.spawnedAt)/t.lifetime);
  const agePn=performance.now()-t.born;
  // Anticipation ring: show telegraph pulse before target fully appears
  const anticipateMs=t.anticipateMs||0;
  if(anticipateMs>0&&agePn<anticipateMs){
    const pct=agePn/anticipateMs;
    ctx.save();
    ctx.globalAlpha=0.25+pct*0.55;
    ctx.strokeStyle=t.color||"#ffffff";ctx.shadowColor=t.glow||t.color||"#ffffff";ctx.shadowBlur=18;
    ctx.lineWidth=3*(1-pct*0.6);
    ctx.beginPath();ctx.arc(t.x,t.y,t.radius*(2.2-pct*0.9),0,Math.PI*2);ctx.stroke();
    ctx.globalAlpha=(0.18+pct*0.3)*(0.4+0.6*Math.abs(Math.sin(ts/60)));
    ctx.lineWidth=1.5;
    ctx.beginPath();ctx.arc(t.x,t.y,t.radius*(1.5-pct*0.4),0,Math.PI*2);ctx.stroke();
    ctx.restore();
    return; // don't draw full target during anticipation
  }
  const spawnScale=agePn<200?Math.min(1.08,agePn/200*1.08):1;
  let ghostAlpha=1;
  if(t.ghost) ghostAlpha=0.3+0.7*(0.5+0.5*Math.sin(ts/200));
  // Haunted Castle (W7) ghost wave — temporary translucency
  if(t.waveGhost&&Date.now()<t.waveGhost){
    const remaining=(t.waveGhost-Date.now())/1400;
    ghostAlpha*=0.35+0.65*(1-remaining); // dips then recovers
  }

  if(t.moving) drawTrail(ctx,t);

  // Timing ring — shrinks toward target, turns gold in perfect zone
  if(t.type==="normal" && t.rarity){
    const tl=Math.max(0,1-(now-t.spawnedAt)/t.lifetime);
    const ringR=t.radius*(1.18+tl*1.5);
    const inPerfZone=tl>0.36&&tl<0.67;
    ctx.save();ctx.translate(t.x,t.y);
    ctx.globalAlpha=ghostAlpha*(inPerfZone?0.7:0.28+tl*0.22);
    ctx.strokeStyle=inPerfZone?"#ffd700":t.color;
    ctx.lineWidth=inPerfZone?3:1.5;
    if(inPerfZone){ctx.shadowColor="#ffd700";ctx.shadowBlur=12;}
    ctx.beginPath();ctx.arc(0,0,ringR,0,Math.PI*2);ctx.stroke();
    // Second inner ring pulsing in perfect zone
    if(inPerfZone){
      ctx.globalAlpha=0.35+0.3*Math.abs(Math.sin(ts/80));
      ctx.lineWidth=1.5;
      ctx.beginPath();ctx.arc(0,0,t.radius*1.08,0,Math.PI*2);ctx.stroke();
    }
    ctx.restore();
  }

  ctx.save();
  ctx.globalAlpha=ghostAlpha;
  ctx.translate(t.x,t.y);
  ctx.scale(spawnScale,spawnScale);

  if     (t.type==="bomb")     drawBomb(ctx,t.radius,t.color,t.glow,ts);
  else if(t.type==="powerup")  drawPowerup(ctx,t.radius,t.pwrType,ts);
  else if(t.type==="boss")     drawBoss(ctx,t.radius,t.hitsLeft,t.maxHits,ts,t.worldId||1,t.rage||false);
  else if(t.type==="treasure") drawTreasure(ctx,t.radius,ts);
  else if(t.type==="mystery")  drawMystery(ctx,t.radius,ts);
  else if(t.type==="mimic")    drawMimic(ctx,t.radius,ts);
  else if(t.type==="anchor")   drawAnchor(ctx,t.radius,ts);
  else if(t.type==="splitter") drawSplitter(ctx,t.radius,ts);
  else if(t.type==="shielded") drawShielded(ctx,t.radius,t.hitsLeft>=2,ts);
  else if(t.type==="magnet")   drawMagnet(ctx,t.radius,ts);
  else if(t.type==="phantom")  drawPhantom(ctx,t.radius,ts,timeLeft);
  else if(t.type==="volatile") drawVolatile(ctx,t.radius,ts,timeLeft);
  else if(t.type==="healer")   drawHealer(ctx,t.radius,ts);
  else if(t.type==="twin")     drawTwin(ctx,t.radius,ts);
  else if(t.type==="rainbow")  drawRainbow(ctx,t.radius,ts);
  else if(t.type==="frozen")   drawFrozen(ctx,t.radius,ts);
  else if(t.type==="bouncy")   drawBouncy(ctx,t.radius,ts);
  else if(t.type==="ninja")    drawNinja(ctx,t.radius,ts,timeLeft);
  else if(t.type==="lootchest") drawLootChest(ctx,t.radius,ts);
  else if(t.type==="tornado")  drawTornado(ctx,t.radius,ts);
  else if(t.type==="bubble")   drawBubble(ctx,t.radius,ts);
  else if(t.type==="echo")     drawEcho(ctx,t.radius,ts);
  else if(t.type==="echo_ghost") drawEchoGhost(ctx,t.radius,ts,t._ghostBorn);
  else if(t.type==="crystal")  drawCrystalTarget(ctx,t.radius,ts);
  else if(t.type==="crystal_shard") drawCrystalShard(ctx,t.radius,ts,t._shardBorn);
  else if(t.type==="rage")     {const rl=Math.min(1,(now-t.spawnedAt)/(t.lifetime*0.9));drawRage(ctx,t.radius,ts,rl);}
  else if(t.type==="divider")  drawDivider(ctx,t.radius,ts);
  else if(t.type==="vanishing") drawVanishing(ctx,t.radius,ts);
  else if(t.type==="homing")   drawHoming(ctx,t.radius,ts);
  else if(t.type==="gemstone") drawGemstone(ctx,t.radius,ts);
  else if(t.type==="poison")   drawPoison(ctx,t.radius,ts);
  else if(t.type==="morph")      drawMorph(ctx,t.radius,ts,t.spawnedAt);
  else if(t.type==="conductor")  drawConductor(ctx,t.radius,ts);
  else if(t.type==="siphon")     drawSiphon(ctx,t.radius,ts,1-(now-t.spawnedAt)/t.lifetime);
  else if(t.type==="glitch")     drawGlitch(ctx,t.radius,ts);
  else if(t.type==="prism")      drawPrism(ctx,t.radius,ts);
  else if(t.type==="comet")      drawComet(ctx,t.radius,ts,t.trail);
  else if(t.type==="mirrorball") drawMirrorBall(ctx,t.radius,ts);
  else if(t.type==="nexus")      drawNexus(ctx,t.radius,ts);
  else if(t.type==="icecomet")   drawIceComet(ctx,t.radius,ts,t.trail);
  else if(t.type==="phoenix")    drawPhoenix(ctx,t.radius,ts,false);
  else if(t.type==="phoenix2")   drawPhoenix(ctx,t.radius,ts,true);
  else if(t.type==="voltage")    drawVoltage(ctx,t.radius,ts);
  else if(t.type==="void")       drawVoid(ctx,t.radius,ts);
  else if(t.type==="clover")     drawLuckyClover(ctx,t.radius,ts);
  else if(t.type==="ricochet")   drawRicochet(ctx,t.radius,ts);
  else if(t.type==="aurora")     drawAurora(ctx,t.radius,ts);
  else if(t.type==="portal")     drawPortal(ctx,t.radius,ts);
  else if(t.type==="particlebomb") drawParticleBomb(ctx,t.radius,ts);
  else if(t.type==="beacon")     drawBeacon(ctx,t.radius,ts);
  else if(t.type==="shadow")     drawShadow(ctx,t.radius,ts,false);
  else if(t.type==="shadow_clone") drawShadow(ctx,t.radius,ts,true);
  else if(t.type==="spectral")   drawSpectral(ctx,t.radius,ts);
  else if(t.type==="heart")      drawHeart(ctx,t.radius,ts);
  else if(t.type==="nova")       drawNova(ctx,t.radius,ts);
  else if(t.type==="firefly")    drawFirefly(ctx,t.radius,ts);
  else if(t.type==="geode")      drawGeode(ctx,t.radius,ts,t._geodeCracked);
  else if(t.type==="timebomb"){const secsLeft=Math.max(0,(t.spawnedAt+t.lifetime-Date.now())/1000);drawTimeBomb(ctx,t.radius,ts,secsLeft);}
  else if(t.type==="thunderbolt") drawThunderbolt(ctx,t.radius,ts);
  else if(t.type==="gravityorb")  drawGravityOrb(ctx,t.radius,ts);
  else if(t.type==="crystalball") drawCrystalBall(ctx,t.radius,ts);
  else if(t.type==="quantum")     drawQuantum(ctx,t.radius,ts,t.hitsLeft,t.maxHits);
  else if(t.type==="clockwork")   drawClockwork(ctx,t.radius,ts);
  else if(t.type==="solarflare"){const gpct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime*0.9));drawSolarFlare(ctx,t.radius,ts,gpct);}
  else if(t.type==="magma")       drawMagma(ctx,t.radius,ts);
  else if(t.type==="prismbomb")   drawPrismBomb(ctx,t.radius,ts);
  else if(t.type==="icecage")     drawIceCage(ctx,t.radius,ts);
  else if(t.type==="pinwheel")    drawPinwheel(ctx,t.radius,ts);
  else if(t.type==="supercell")   drawSupercell(ctx,t.radius,ts);
  else if(t.type==="chaincollector") drawChainCollector(ctx,t.radius,ts);
  else if(t.type==="stardust")    drawStardust(ctx,t.radius,ts);
  else if(t.type==="star_piece")  {ctx.save();ctx.shadowBlur=10;ctx.shadowColor="#fde68a";ctx.fillStyle="#fbbf24";ctx.beginPath();ctx.arc(0,0,t.radius,0,Math.PI*2);ctx.fill();ctx.globalAlpha=1;ctx.font=`${Math.round(t.radius*0.9)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("⭐",0,0);ctx.restore();}
  else if(t.type==="bloom")       drawBloom(ctx,t.radius,ts);
  else if(t.type==="petal")       {ctx.save();ctx.shadowBlur=8;ctx.shadowColor="#ff6b9d";ctx.fillStyle="#ff6b9d";ctx.beginPath();ctx.arc(0,0,t.radius,0,Math.PI*2);ctx.fill();ctx.globalAlpha=1;ctx.font=`${Math.round(t.radius*0.9)}px serif`;ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🌸",0,0);ctx.restore();}
  else if(t.type==="reflector")   drawReflector(ctx,t.radius,ts);
  else if(t.type==="vortex")      drawVortex(ctx,t.radius,ts);
  else if(t.type==="pixel")       drawPixel(ctx,t.radius,ts);
  else if(t.type==="neon"){const phase=Math.floor((Date.now()-t.spawnedAt)/900)%3;drawNeonSign(ctx,t.radius,ts,phase);}
  else if(t.type==="lantern")     drawLantern(ctx,t.radius,ts);
  else if(t.type==="drift"){const cw5=canvasRef.current?.width||390,ch5=canvasRef.current?.height||700;const dist=Math.hypot(t.x-cw5/2,t.y-ch5/2)/Math.hypot(cw5/2,ch5/2);drawDrift(ctx,t.radius,ts,Math.min(1,dist));}
  else if(t.type==="nebula")     drawNebula(ctx,t.radius,ts);
  else if(t.type==="wisp")       drawWisp(ctx,t.radius,ts,t.moving&&(Math.hypot(t.vx||0,t.vy||0)>0.1));
  else if(t.type==="meteor")     drawMeteor(ctx,t.radius,ts,t.trail);
  else if(t.type==="aura")       drawAuraRing(ctx,t.radius,ts);
  else if(t.type==="smoke_ring"){const exp=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||3000));drawSmokeRing(ctx,t.radius,ts,exp);}
  else if(t.type==="chain_bomb")    drawChainBomb(ctx,t.radius,ts);
  else if(t.type==="warp_gate")     drawWarpGate(ctx,t.radius,ts);
  else if(t.type==="mirage")        drawMirage(ctx,t.radius,ts);
  else if(t.type==="prism_shard"){
    ctx.save();ctx.shadowBlur=12;ctx.shadowColor=t.color||"#67e8f9";
    ctx.fillStyle=t.color||"#67e8f9";ctx.globalAlpha=0.85;
    ctx.beginPath();const pr=t.radius;
    ctx.moveTo(0,-pr);ctx.lineTo(pr*0.6,pr*0.5);ctx.lineTo(-pr*0.6,pr*0.5);ctx.closePath();ctx.fill();
    ctx.restore();
  }
  else if(t.type==="supernova"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||3800));
    drawSupernova(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="supernova_mini"){
    ctx.save();ctx.shadowBlur=10;ctx.shadowColor="#fbbf24";
    const hue=(ts*0.2+t._hue||0)%360;
    ctx.fillStyle=`hsl(${hue},90%,65%)`;ctx.beginPath();ctx.arc(0,0,t.radius,0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
  else if(t.type==="crystal_matrix") drawCrystalMatrix(ctx,t.radius,ts,t.hitsLeft||1);
  else if(t.type==="plasma_storm")   drawPlasmaStorm(ctx,t.radius,ts);
  else if(t.type==="nexus_gate")     drawNexusGate(ctx,t.radius,ts,t._nexusSecondary||false);
  else if(t.type==="twilight_orb"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4500));
    drawTwilightOrb(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="life_orb")      drawLifeOrb(ctx,t.radius,ts);
  else if(t.type==="score_amp"){
    const secsLeft=Math.max(0,Math.ceil(((t.spawnedAt+t.lifetime)-Date.now())/1000));
    drawScoreAmpOrb(ctx,t.radius,ts,secsLeft);
  }
  else if(t.type==="night_orb"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4500));
    drawNightOrb(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="shift_target"){
    const phase=Math.floor((Date.now()-t.spawnedAt)/600)%5;
    drawShiftTarget(ctx,t.radius,ts,phase);
  }
  else if(t.type==="lucky_wheel"){
    const num=1+Math.floor((Date.now()-t.spawnedAt)/100)%20;
    drawLuckyWheel(ctx,t.radius,ts,num);
  }
  else if(t.type==="abyss_orb"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4000));
    drawAbyssOrb(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="cosmic_portal") drawCosmicPortal(ctx,t.radius,ts);
  else if(t.type==="flame_ring"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4000));
    const ringPct=1-agePct; // ring starts big, shrinks
    drawFlameRing(ctx,t.radius,ts,ringPct);
  }
  else if(t.type==="ice_clock"){
    const elapsed=(Date.now()-t.spawnedAt)/1000;
    const secsLeft=Math.max(0,Math.ceil(5-elapsed));
    drawIceClock(ctx,t.radius,ts,secsLeft);
  }
  else if(t.type==="holo_decoy") drawHoloDecoy(ctx,t.radius,ts,t._holoReal||false);
  else if(t.type==="mirror_gate") drawMirrorGate(ctx,t.radius,ts);
  else if(t.type==="prism_pulse"){
    const colorIdx=Math.floor((Date.now()-t.spawnedAt)/300)%6;
    drawPrismPulse(ctx,t.radius,ts,colorIdx,t._prismTargetIdx||0);
  }
  else if(t.type==="dark_matter"){
    const revealed=(Date.now()-t.spawnedAt)>2000;
    drawDarkMatter(ctx,t.radius,ts,revealed);
  }
  else if(t.type==="comet_blast") drawCometBlast(ctx,t.radius,ts);
  else if(t.type==="comet_shard"){
    ctx.save();ctx.shadowBlur=12;ctx.shadowColor="#fbbf24";ctx.fillStyle="#fbbf24";ctx.globalAlpha=0.9;
    ctx.beginPath();const csr=t.radius;const csn=5;
    for(let i=0;i<csn*2;i++){const a=(i/csn/2)*Math.PI*2-Math.PI/2;const cr=csr*(i%2===0?1:0.45);ctx.lineTo(Math.cos(a)*cr,Math.sin(a)*cr);}
    ctx.closePath();ctx.fill();ctx.restore();
  }
  else if(t.type==="time_prison"){
    // Cage nearby normals in draw loop
    if(!t._caged)t._caged=[];
    const now3=Date.now();
    if(!t._lastCageCheck||now3-t._lastCageCheck>600){
      t._lastCageCheck=now3;
      const candidates=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<150&&!t._caged.includes(o.id));
      candidates.slice(0,3-t._caged.length).forEach(o=>{t._caged.push(o.id);o._imprisoned=t.id;o.moving=false;o.vx=0;o.vy=0;});
    }
    drawTimePrison(ctx,t.radius,ts,t._caged.length);
  }
  else if(t.type==="shadow_twin")   drawShadowTwin(ctx,t.radius,ts,false);
  else if(t.type==="shadow_clone2") drawShadowTwin(ctx,t.radius,ts,true);
  else if(t.type==="photon_burst"){
    const cycleMs=300;const isOn=Math.floor((Date.now()-t.spawnedAt)/cycleMs)%2===0;
    drawPhotonBurst(ctx,t.radius,ts,isOn);
  }
  else if(t.type==="vault_orb"){
    const secsAlive=(Date.now()-t.spawnedAt)/1000;
    const banked=Math.floor(secsAlive*100);
    drawVaultOrb(ctx,t.radius,ts,banked);
  }
  else if(t.type==="nano_swarm")    drawNanoSwarm(ctx,t.radius,ts);
  else if(t.type==="rune_chain"){
    const runeIdx=Math.max(0,t.maxHits-(t.hitsLeft||1));
    drawRuneChain(ctx,t.radius,ts,runeIdx,0);
  }
  else if(t.type==="plasma_web"){
    const WEB_R=170;
    const webCount=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<WEB_R).length;
    t._webCount=webCount;
    drawPlasmaWeb(ctx,t.radius,ts,webCount);
  }
  else if(t.type==="astral_orb"){
    // Teleport every 1s
    const now4=Date.now();
    if(!t._lastTeleport||now4-t._lastTeleport>1000){
      if(t._lastTeleport){
        const cw5=canvasRef.current?.width||360;const ch5=canvasRef.current?.height||640;
        t.x=60+Math.random()*(cw5-120);t.y=60+Math.random()*(ch5-200);
        t.cx=t.x;t.cy=t.y;t._justTeleported=now4;
      }
      t._lastTeleport=now4;
    }
    const fresh=t._justTeleported&&(now4-t._justTeleported)<300;
    drawAstralOrb(ctx,t.radius,ts,fresh||false);
  }
  else if(t.type==="crystal_clock"){
    const segments=Math.min(8,Math.floor((Date.now()-t.spawnedAt)/(t.lifetime||6000)*8));
    drawCrystalClock(ctx,t.radius,ts,segments);
  }
  else if(t.type==="sunflower_orb"){
    const petals=Math.max(1,12-Math.floor((Date.now()-t.spawnedAt)/300));
    t._petals=petals;
    drawSunflowerOrb(ctx,t.radius,ts,petals);
  }
  else if(t.type==="toxic_cloud"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
    drawToxicCloud(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="laser_prism"){
    // Charge nearby normals with laser every 800ms
    const now5=Date.now();
    if(!t._lastLaser||now5-t._lastLaser>800){
      t._lastLaser=now5;
      const laserTarget=targetsRef.current.find(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<220);
      if(laserTarget){laserTarget._laserCharged=now5;laserTarget._laserExpiry=now5+2000;}
    }
    drawLaserPrism(ctx,t.radius,ts);
  }
  else if(t.type==="echo_star"){
    // Spawn echoes on first tick
    if(!t._echoesSpawned){
      t._echoesSpawned=true;t._echoGroup=t.id;
      const cw6=canvasRef.current?.width||360;const ch6=canvasRef.current?.height||640;
      for(let i=0;i<4;i++){
        const ea=(i/4)*Math.PI*2;const ed=70+Math.random()*30;
        const ex=Math.max(25,Math.min(cw6-25,t.x+Math.cos(ea)*ed));
        const ey=Math.max(25,Math.min(ch6-25,t.y+Math.sin(ea)*ed));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"echo_point",
          rarity:null,x:ex,y:ey,radius:18,color:"#fbbf24",glow:"#fef9c3",
          lifetime:2000,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
          worldId:t.worldId,worldColor:t.worldColor,bossPattern:null,bossPhase:1,cx:ex,cy:ey,
          _echoGroup:t.id,_echoIdx:i});
      }
    }
    drawEchoStar(ctx,t.radius,ts,false,1);
  }
  else if(t.type==="echo_point"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||2000));
    const alpha=Math.max(0.1,1-agePct);
    drawEchoStar(ctx,t.radius,ts,true,alpha);
  }
  else if(t.type==="meteor_shower") drawMeteorShower(ctx,t.radius,ts);
  else if(t.type==="meteor_shard"){
    ctx.save();ctx.shadowBlur=14;ctx.shadowColor="#f97316";ctx.fillStyle="#fed7aa";ctx.globalAlpha=0.9;
    ctx.beginPath();ctx.arc(0,0,t.radius,0,Math.PI*2);ctx.fill();
    ctx.font=`${Math.floor(t.radius*0.9)}px sans-serif`;ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillText("🔥",0,1);ctx.restore();
  }
  else if(t.type==="golden_gate"){
    const agePct2=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||800));
    drawGoldenGate(ctx,t.radius,ts,agePct2);
  }
  else if(t.type==="pulse_ring"){
    const ringPct=(Date.now()-t.spawnedAt)%(t.lifetime||4000)/(t.lifetime||4000);
    drawPulseRing(ctx,t.radius,ts,ringPct);
  }
  else if(t.type==="clockwork_bomb"){
    const cwGear=t.maxHits-(t.hitsLeft||1);
    drawClockworkBomb(ctx,t.radius,ts,cwGear);
  }
  else if(t.type==="nebula_vortex"){
    const nvGrow=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
    drawNebulaVortex(ctx,t.radius,ts,nvGrow);
  }
  else if(t.type==="frost_hex"){
    // Freeze nearby moving targets every 1.2s
    const nowFH=Date.now();
    if(!t._lastFrostTick||nowFH-t._lastFrostTick>1200){
      t._lastFrostTick=nowFH;
      const FROST_R=160;
      const victims=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.moving&&Math.hypot(o.x-t.x,o.y-t.y)<FROST_R).slice(0,3);
      victims.forEach(o=>{o._frostedBy=t.id;o._frostUntil=nowFH+1500;o.vx*=0.1;o.vy*=0.1;});
      t._frozenCount=(t._frozenCount||0)+victims.length;
    }
    drawFrostHex(ctx,t.radius,ts,t._frozenCount||0);
  }
  else if(t.type==="lightning_orb"){
    // Count nearby normal targets for chain display
    const nearbyLO=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<200).length;
    drawLightningOrb(ctx,t.radius,ts,nearbyLO);
  }
  else if(t.type==="star_burst"){
    const sbExpand=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4500));
    drawStarBurst(ctx,t.radius,ts,sbExpand);
  }
  else if(t.type==="crystal_spire"){
    const csCharge=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4000));
    drawCrystalSpire(ctx,t.radius,ts,csCharge);
  }
  else if(t.type==="crystal_spire_shard"){
    ctx.save();ctx.shadowBlur=10;ctx.shadowColor="#67e8f9";ctx.fillStyle="#a5f3fc";ctx.globalAlpha=0.85;
    ctx.beginPath();ctx.moveTo(0,-t.radius*1.4);ctx.lineTo(t.radius*0.55,0);ctx.lineTo(0,t.radius*0.8);ctx.lineTo(-t.radius*0.55,0);ctx.closePath();ctx.fill();ctx.restore();
  }
  else if(t.type==="phase_gate"){
    const cycleMs=1800;const solidMs=900;
    const phase=(Date.now()-t.spawnedAt)%cycleMs;
    const isSolid=phase<solidMs;
    drawPhaseGate(ctx,t.radius,ts,isSolid);
  }
  else if(t.type==="void_pulsar"){
    const vpShrink=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||3500));
    drawVoidPulsar(ctx,t.radius,ts,vpShrink);
  }
  else if(t.type==="nova_cannon"){
    const ncCharge=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4500));
    drawNovaCannon(ctx,t.radius,ts,ncCharge);
  }
  else if(t.type==="hex_grid"){
    drawHexGrid(ctx,t.radius,ts,t._tappedMask||0);
  }
  else if(t.type==="quantum_tunnel_a"||t.type==="quantum_tunnel_b"){
    const isA=t.type==="quantum_tunnel_a";
    // Teleport: swap positions with partner every 1.5s
    const nowQT=Date.now();
    if(!t._lastTeleportQT||nowQT-t._lastTeleportQT>1500){
      t._lastTeleportQT=nowQT;
      const partner=targetsRef.current.find(o=>o.id===t._partnerId&&!o.dying);
      if(partner){
        const tmpX=t.x,tmpY=t.y;t.x=partner.x;t.y=partner.y;partner.x=tmpX;partner.y=tmpY;
        t.cx=t.x;t.cy=t.y;partner.cx=partner.x;partner.cy=partner.y;
      }
    }
    drawQuantumTunnel(ctx,t.radius,ts,isA);
  }
  else if(t.type==="inferno_orb"){
    // Gain heat stack every 1s
    const nowIO=Date.now();
    if(!t._lastHeatTick||nowIO-t._lastHeatTick>1000){
      t._lastHeatTick=nowIO;
      t._heatStacks=Math.min(8,(t._heatStacks||0)+1);
    }
    drawInfernoOrb(ctx,t.radius,ts,t._heatStacks||0);
  }
  else if(t.type==="chrono_sphere"){
    // Slow nearby moving targets
    const nowCS=Date.now();
    if(!t._lastSlowTick||nowCS-t._lastSlowTick>600){
      t._lastSlowTick=nowCS;
      const SLOW_R=200;
      const affected=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.moving&&Math.hypot(o.x-t.x,o.y-t.y)<SLOW_R);
      affected.forEach(o=>{o.vx*=0.85;o.vy*=0.85;});
      t._slowedCount=(t._slowedCount||0)+affected.length;
    }
    drawChronoSphere(ctx,t.radius,ts,t._slowedCount||0);
  }
  else if(t.type==="prismatic_lens"){
    const colorIdx=Math.floor((Date.now()-t.spawnedAt)/300)%6;
    if(!t._focusIdx)t._focusIdx=Math.floor(Math.random()*6);
    drawPrismaticLens(ctx,t.radius,ts,colorIdx,t._focusIdx);
  }
  else if(t.type==="shadow_mirror"){
    const gs2=gsRef.current;
    const hasLastTap=gs2?._lastTapX!==undefined;
    drawShadowMirror(ctx,t.radius,ts,hasLastTap);
  }
  else if(t.type==="plasma_comet"){
    // Parabolic arc: compute apex progress via vy direction (starts negative = going up)
    const age=(Date.now()-t.spawnedAt)/(t.lifetime||2500);
    drawPlasmaComet(ctx,t.radius,ts,age);
  }
  else if(t.type==="abyssal_gate"){
    // Drain nearby normal targets every 1.5s
    const nowAG=Date.now();
    if(!t._lastDrainTick||nowAG-t._lastDrainTick>1500){
      t._lastDrainTick=nowAG;
      const DRAIN_R=200;
      const victims=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<DRAIN_R).slice(0,3);
      victims.forEach(o=>{o.dying=performance.now();});
      t._drainCount=(t._drainCount||0)+victims.length;
    }
    drawAbyssalGate(ctx,t.radius,ts,t._drainCount||0);
  }
  else if(t.type==="warp_ball"){
    // Teleport every 0.8s to a random canvas position
    const nowWB=Date.now();
    if(!t._lastWarp||nowWB-t._lastWarp>800){
      t._lastWarp=nowWB;t._justWarped=nowWB;
      const cw10=canvasRef.current?.width||360;const ch10=canvasRef.current?.height||640;
      t.x=40+Math.random()*(cw10-80);t.y=80+Math.random()*(ch10-120);
      t.cx=t.x;t.cy=t.y;
    }
    const justWarped=t._justWarped&&(nowWB-t._justWarped)<300;
    drawWarpBall(ctx,t.radius,ts,!!justWarped);
  }
  else if(t.type==="crystal_spike"){
    if(!t._dangerArc)t._dangerArc=Math.random()*Math.PI*2;
    drawCrystalSpike(ctx,t.radius,ts,t._dangerArc);
  }
  else if(t.type==="energy_core"){
    const cycleMs=1400;
    const phase=Math.floor((Date.now()-t.spawnedAt)/cycleMs)%3;
    drawEnergyCore(ctx,t.radius,ts,phase);
  }
  else if(t.type==="arcane_circle"){
    const cycleMs=500;
    const runeIdx=Math.floor((Date.now()-t.spawnedAt)/cycleMs)%5;
    if(!t._targetRune)t._targetRune=Math.floor(Math.random()*5);
    drawArcaneCircle(ctx,t.radius,ts,runeIdx,t._targetRune);
  }
  else if(t.type==="stellar_drift"){
    // Track distance drifted from spawn
    if(!t._spawnX){t._spawnX=t.x;t._spawnY=t.y;}
    const driftDist=Math.hypot(t.x-t._spawnX,t.y-t._spawnY);
    drawStellarDrift(ctx,t.radius,ts,driftDist);
  }
  else if(t.type==="bubble_wave"){
    const bwGrow=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4000));
    drawBubbleWave(ctx,t.radius,ts,bwGrow);
  }
  else if(t.type==="iron_shield")  drawIronShield(ctx,t.radius,ts,t.hitsLeft||3);
  else if(t.type==="time_echo"){
    const teAge=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||2500));
    const teOpacity=Math.max(0,1-teAge);
    drawTimeEcho(ctx,t.radius,ts,teOpacity);
  }
  else if(t.type==="gravity_orb2"){
    // Pull all non-boss targets toward this black hole every 0.5s
    const nowGO2=Date.now();
    if(!t._lastGO2Pull||nowGO2-t._lastGO2Pull>500){
      t._lastGO2Pull=nowGO2;
      const PULL_R2=250;
      const victims2=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<PULL_R2);
      victims2.forEach(o=>{
        const dx=t.x-o.x;const dy=t.y-o.y;const d=Math.hypot(dx,dy)||1;
        const pull=6*(1-d/PULL_R2);o.x+=dx/d*pull;o.y+=dy/d*pull;
        if(!t._pulledIds)t._pulledIds=new Set();t._pulledIds.add(o.id);
      });
      t._pullCount=(t._pulledIds?.size||0);
    }
    drawGravityOrb2(ctx,t.radius,ts,t._pullCount||0);
  }
  else if(t.type==="pixel_swarm"){
    // Spawn 8 pixel units on first tick
    if(!t._pixelsSpawned){
      t._pixelsSpawned=true;t._pixelGroup=t.id;
      const cw11=canvasRef.current?.width||360;const ch11=canvasRef.current?.height||640;
      for(let i=0;i<8;i++){
        const pa=(i/8)*Math.PI*2;const pd=70+Math.random()*40;
        const px=Math.max(20,Math.min(cw11-20,t.x+Math.cos(pa)*pd));
        const py=Math.max(20,Math.min(ch11-20,t.y+Math.sin(pa)*pd));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"pixel_unit",
          rarity:null,x:px,y:py,radius:14,color:"#f472b6",glow:"#fce7f3",
          lifetime:5000,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
          worldId:t.worldId,worldColor:t.worldColor,bossPattern:null,bossPhase:1,cx:px,cy:py,
          _pixelGroup:t.id,_pixelIdx:i});
      }
    }
    drawPixelSwarmCore(ctx,t.radius,ts);
  }
  else if(t.type==="pixel_unit"){
    drawPixelUnit(ctx,t.radius,ts,t._pixelIdx||0);
  }
  else if(t.type==="chrono_crystal"){
    const phase=t._chronoPhase||"active";
    drawChronoCrystal(ctx,t.radius,ts,phase,0);
  }
  else if(t.type==="rune_burst"){
    drawRuneBurst(ctx,t.radius,ts);
  }
  else if(t.type==="crystal_golem"){
    drawCrystalGolem(ctx,t.radius,ts,t.hitsLeft||3);
  }
  else if(t.type==="freeze_orb"){
    const pulsePct43=(Date.now()-t.spawnedAt)%1500/1500;
    drawFreezeOrb(ctx,t.radius,ts,pulsePct43);
  }
  else if(t.type==="twin_comet"){
    // Lazy-spawn mirrored twin on first draw tick
    if(!t._twinSpawned){
      t._twinSpawned=true;
      const cw43=canvasRef.current?.width||360;const ch43=canvasRef.current?.height||640;
      const grpId43=t.id;t._twinGroup=grpId43;
      const tx43=Math.max(30,Math.min(cw43-30,cw43-t.x));
      const ty43=Math.max(30,Math.min(ch43-30,t.y));
      const twin43={id:Math.random().toString(36).slice(2),type:"twin_comet",
        rarity:t.rarity,x:tx43,y:ty43,radius:t.radius,color:t.color,glow:t.glow,
        lifetime:t.lifetime,spawnedAt:t.spawnedAt,born:performance.now(),anticipateMs:0,
        moving:true,ghost:false,vx:-t.vx,vy:-t.vy,trail:[],
        hitsLeft:1,maxHits:1,dying:null,_twinGroup:grpId43,_twinSpawned:true,
        cx:tx43,cy:ty43,worldId:t.worldId,worldColor:t.worldColor,bossPattern:null,bossPhase:1,pwrType:null};
      targetsRef.current.push(twin43);
    }
    drawTwinComet(ctx,t.radius,ts);
  }
  else if(t.type==="plasma_vortex"){
    // phase: inner ring pulses 0→1 over 2s cycle
    const phase44=(Date.now()-t.spawnedAt)%2000/2000;
    t._vortexPhase=phase44;
    drawPlasmaVortex(ctx,t.radius,ts,phase44);
  }
  else if(t.type==="nova_star"){
    const dwellPct46=Math.min(1,(Date.now()-t.spawnedAt)/2000);
    t._dwellPct=dwellPct46;
    drawNovaStar(ctx,t.radius,ts,dwellPct46);
  }
  else if(t.type==="spiral_orb"){
    drawSpiralOrb(ctx,t.radius,ts,t._spiralPct||0);
  }
  else if(t.type==="dark_pulse"){
    drawDarkPulse(ctx,t.radius,ts);
  }
  else if(t.type==="lunar_eclipse"){
    // Grows from 0→1 in first half, shrinks 1→0 in second half
    const age48=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
    const sizePct48=age48<0.5?age48*2:1-(age48-0.5)*2;
    t.radius=BASE_R*1.3*Math.max(0.2,sizePct48);
    t._sizePct=sizePct48;
    drawLunarEclipse(ctx,t.radius,ts,sizePct48);
  }
  else if(t.type==="chain_wall"){
    drawChainWall(ctx,t.radius,ts);
  }
  else if(t.type==="bounce_web"){
    drawBounceWeb(ctx,t.radius,ts);
  }
  else if(t.type==="meteor_shower"){
    drawMeteorShower(ctx,t.radius,ts);
  }
  else if(t.type==="prismatic_orb"){
    if(!t._targetHue)t._targetHue=Math.floor(Math.random()*360);
    const curHue=Math.floor(ts*0.08)%360;
    t._curHue=curHue;
    drawPrismaticOrb(ctx,t.radius,ts,t._targetHue);
  }
  else if(t.type==="void_tether"){
    drawVoidTether(ctx,t.radius,ts,!!t._tetherGroup);
  }
  else if(t.type==="crystal_pulsar"){
    const age51=Date.now()-t.spawnedAt;
    const pulsePct51=0.5+0.5*Math.sin(age51*0.0065);
    t._pulsePct=pulsePct51;
    drawCrystalPulsar(ctx,t.radius,ts,pulsePct51);
  }
  else if(t.type==="shadow_echo"||t.type==="shadow_echo_bright"){
    drawShadowEcho(ctx,t.radius,ts,t.type==="shadow_echo_bright");
  }
  else if(t.type==="flux_ring"){
    const age52=(Date.now()-t.spawnedAt)/(t.lifetime||3000);
    t._agePct=Math.min(1,age52);
    drawFluxRing(ctx,t.radius,ts,t._agePct);
  }
  else if(t.type==="quantum_flare"||t.type==="quantum_flare_decoy"){
    drawQuantumFlare(ctx,t.radius,ts,t.type==="quantum_flare_decoy");
  }
  else if(t.type==="nebula_crystal"){
    const phase53=(t.maxHits||3)-(t.hitsLeft||1);
    drawNebulaCrystal(ctx,t.radius,ts,phase53);
  }
  else if(t.type==="time_shard"){
    const visAge53=(Date.now()-t.spawnedAt);
    const visOsc53=0.5+0.5*Math.sin(visAge53*0.005);
    t._visPct=visOsc53;
    drawTimeShard(ctx,t.radius,ts,visOsc53);
  }
  else if(t.type==="gravity_node"){
    const pulled54=t._pulledTargets?.length||0;
    drawGravityNode(ctx,t.radius,ts,pulled54);
  }
  else if(t.type==="lightning_cage"){
    const cageR54=(t._cageRadius||t.radius*2.5);
    const trapped54=targetsRef.current.filter(o=>o.id!==t.id&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<cageR54).length;
    t._trapped=trapped54;
    ctx.save();ctx.translate(-(t.x-t.x),0); // already in translated coords via drawTarget
    drawLightningCage(ctx,t.radius,ts,trapped54);
    ctx.restore();
  }
  else if(t.type==="cosmic_web"){
    const nodesLeft66=t._nodesLeft!==undefined?t._nodesLeft:6;
    drawCosmicWeb(ctx,t.radius,ts,nodesLeft66,true);
  }
  else if(t.type==="cosmic_node"){
    drawCosmicWeb(ctx,t.radius,ts,0,false);
  }
  else if(t.type==="warp_orb"){
    const isWarping66=!!(t._warping);
    drawWarpOrb(ctx,t.radius,ts,isWarping66);
  }
  else if(t.type==="omega_heart"){
    const ohSync100=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._ohSync100=ohSync100;
    drawOmegaHeart(ctx,t.radius,ts,ohSync100);
  }
  else if(t.type==="nexus_gem"){
    drawNexusGem(ctx,t.radius,ts);
  }
  else if(t.type==="plasma_halo"){
    drawPlasmaHalo(ctx,t.radius,ts);
  }
  else if(t.type==="glitch_node"){
    const gnBurst99=(Math.sin((Date.now()-t.spawnedAt)*0.005)+1)/2;
    t._gnBurst99=gnBurst99;
    drawGlitchNode(ctx,t.radius,ts,gnBurst99);
  }
  else if(t.type==="astral_core"){
    const acAlign111=(Math.sin((Date.now()-t.spawnedAt)*0.004)+1)/2;
    t._acAlign111=acAlign111;
    drawAstralCore(ctx,t.radius,ts,acAlign111);
  }
  else if(t.type==="light_bolt"){
    const lbCharge111=Math.min(1,(Date.now()-t.spawnedAt)/4500);
    t._lbCharge111=lbCharge111;
    drawLightBolt(ctx,t.radius,ts,lbCharge111);
  }
  else if(t.type==="solar_eye"){
    const seOpen112=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._seOpen112=seOpen112;
    drawSolarEye(ctx,t.radius,ts,seOpen112);
  }
  else if(t.type==="void_serpent"){
    const vsVert112=(Math.sin((Date.now()-t.spawnedAt)*0.0025+Math.PI/2)+1)/2;
    t._vsVert112=vsVert112;
    drawVoidSerpent(ctx,t.radius,ts,vsVert112);
  }
  else if(t.type==="moon_petal"){
    const mpBloom113=(Math.sin((Date.now()-t.spawnedAt)*0.0028)+1)/2;
    t._mpBloom113=mpBloom113;
    drawMoonPetal(ctx,t.radius,ts,mpBloom113);
  }
  else if(t.type==="star_dust2"){
    const sd2Trail113=Math.min(1,(Date.now()-t.spawnedAt)/4000);
    t._sd2Trail113=sd2Trail113;
    drawStarDust2(ctx,t.radius,ts,sd2Trail113);
  }
  else if(t.type==="ice_nova2"){
    const in2Frz114=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._in2Frz114=in2Frz114;
    drawIceNova2(ctx,t.radius,ts,in2Frz114);
  }
  else if(t.type==="arc_gem"){
    const agCharge114=Math.min(1,(Date.now()-t.spawnedAt)/4500);
    t._agCharge114=agCharge114;
    drawArcGem(ctx,t.radius,ts,agCharge114);
  }
  else if(t.type==="flame_orb2"){
    const fo2Heat115=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._fo2Heat115=fo2Heat115;
    drawFlameOrb2(ctx,t.radius,ts,fo2Heat115);
  }
  else if(t.type==="thor_gem"){
    const tgStorm115=(Math.sin((Date.now()-t.spawnedAt)*0.0035)+1)/2;
    t._tgStorm115=tgStorm115;
    drawThorGem(ctx,t.radius,ts,tgStorm115);
  }
  else if(t.type==="cosmic_lens"){
    const clFocus116=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._clFocus116=clFocus116;
    drawCosmicLens(ctx,t.radius,ts,clFocus116);
  }
  else if(t.type==="nebula_burst2"){
    const nb2Nova116=(Math.sin((Date.now()-t.spawnedAt)*0.0032)+1)/2;
    t._nb2Nova116=nb2Nova116;
    drawNebulaBurst2(ctx,t.radius,ts,nb2Nova116);
  }
  else if(t.type==="dark_pulsar"){
    const dpBurst110=(Math.sin((Date.now()-t.spawnedAt)*0.005)+1)/2;
    t._dpBurst110=dpBurst110;
    drawDarkPulsar(ctx,t.radius,ts,dpBurst110);
  }
  else if(t.type==="crystal_bomb"){
    const cbLife110=Math.min(1,(Date.now()-t.spawnedAt)/t.lifetime);
    t._cbLife110=cbLife110;
    drawCrystalBomb(ctx,t.radius,ts,cbLife110);
  }
  else if(t.type==="glacier_spike"){
    drawGlacierSpike(ctx,t.radius,ts);
  }
  else if(t.type==="plasma_pulse2"){
    const pp2Peak109=(Math.sin((Date.now()-t.spawnedAt)*0.007)+1)/2;
    t._pp2Peak109=pp2Peak109;
    drawPlasmaPulse2(ctx,t.radius,ts,pp2Peak109);
  }
  else if(t.type==="quantum_burst2"){
    const qb2Pulse108=(Math.sin((Date.now()-t.spawnedAt)*0.006)+1)/2;
    t._qb2Pulse108=qb2Pulse108;
    drawQuantumBurst2(ctx,t.radius,ts,qb2Pulse108);
  }
  else if(t.type==="nebula_ring"){
    drawNebulaRing(ctx,t.radius,ts);
  }
  else if(t.type==="flux_ring2"){
    drawFluxRing2(ctx,t.radius,ts);
  }
  else if(t.type==="solar_vortex"){
    drawSolarVortex(ctx,t.radius,ts);
  }
  else if(t.type==="drift_orb"){
    const doStill106=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._doStill106=doStill106;
    drawDriftOrb(ctx,t.radius,ts,doStill106);
  }
  else if(t.type==="chrono_star"){
    const csApex106=(Math.sin((Date.now()-t.spawnedAt)*0.005)+1)/2;
    t._csApex106=csApex106;
    drawChronoStar(ctx,t.radius,ts,csApex106);
  }
  else if(t.type==="prism_coil"){
    const pcSync105=(Math.sin((Date.now()-t.spawnedAt)*0.005)+1)/2;
    t._pcSync105=pcSync105;
    drawPrismCoil(ctx,t.radius,ts,pcSync105);
  }
  else if(t.type==="star_veil"){
    const svReveal105=(Math.sin((Date.now()-t.spawnedAt)*0.004)+1)/2;
    t._svReveal105=svReveal105;
    drawStarVeil(ctx,t.radius,ts,svReveal105);
  }
  else if(t.type==="frost_halo"){
    const fhFreeze104=(Math.sin((Date.now()-t.spawnedAt)*0.004)+1)/2;
    t._fhFreeze104=fhFreeze104;
    drawFrostHalo(ctx,t.radius,ts,fhFreeze104);
  }
  else if(t.type==="ember_clock"){
    const ecLife104=Math.min(1,(Date.now()-t.spawnedAt)/t.lifetime);
    t._ecLife104=ecLife104;
    drawEmberClock(ctx,t.radius,ts,ecLife104);
  }
  else if(t.type==="nova_spire"){
    const nsPeak103=(Math.sin((Date.now()-t.spawnedAt)*0.004)+1)/2;
    t._nsPeak103=nsPeak103;
    drawNovaSpire(ctx,t.radius,ts,nsPeak103);
  }
  else if(t.type==="echo_lens"){
    const elFocus103=(Math.sin((Date.now()-t.spawnedAt)*0.005)+1)/2;
    t._elFocus103=elFocus103;
    drawEchoLens(ctx,t.radius,ts,elFocus103);
  }
  else if(t.type==="crystal_petal"){
    const cpAlign102=(Math.sin((Date.now()-t.spawnedAt)*0.004)+1)/2;
    t._cpAlign102=cpAlign102;
    drawCrystalPetal(ctx,t.radius,ts,cpAlign102);
  }
  else if(t.type==="twilight_shard"){
    const tsDusk102=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._tsDusk102=tsDusk102;
    drawTwilightShard(ctx,t.radius,ts,tsDusk102);
  }
  else if(t.type==="shadow_crystal"){
    const scDark101=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._scDark101=scDark101;
    drawShadowCrystal(ctx,t.radius,ts,scDark101);
  }
  else if(t.type==="tide_orb"){
    const toCrest101=(Math.sin((Date.now()-t.spawnedAt)*0.004)+1)/2;
    t._toCrest101=toCrest101;
    drawTideOrb(ctx,t.radius,ts,toCrest101);
  }
  else if(t.type==="lunar_rune"){
    const lrMoon98=Math.min(1,(Date.now()-t.spawnedAt)/5000);
    t._lrMoon98=lrMoon98;
    drawLunarRune(ctx,t.radius,ts,lrMoon98);
  }
  else if(t.type==="storm_seed"){
    const ssGrow98=Math.min(1,(Date.now()-t.spawnedAt)/4500);
    t._ssGrow98=ssGrow98;
    drawStormSeed(ctx,t.radius,ts,ssGrow98);
  }
  else if(t.type==="warp_glyph"){
    const wgGlow97=(Math.sin((Date.now()-t.spawnedAt)*0.004)+1)/2;
    t._wgGlow97=wgGlow97;
    drawWarpGlyph(ctx,t.radius,ts,wgGlow97);
  }
  else if(t.type==="ether_bolt"){
    const ebChain97=t._ebChain97||0;
    drawEtherBolt(ctx,t.radius,ts,ebChain97);
  }
  else if(t.type==="cosmic_dust"){
    const cdDensity96=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._cdDensity96=cdDensity96;
    drawCosmicDust(ctx,t.radius,ts,cdDensity96);
  }
  else if(t.type==="nova_bell"){
    const nbRes96=(Math.sin((Date.now()-t.spawnedAt)*0.004)+1)/2;
    t._nbRes96=nbRes96;
    drawNovaBell(ctx,t.radius,ts,nbRes96);
  }
  else if(t.type==="prismatic_eye"){
    const peg95=Math.min(1,(Date.now()-t.spawnedAt)/3500);
    t._peg95=peg95;
    drawPrismaticEye(ctx,t.radius,ts,peg95);
  }
  else if(t.type==="thunder_crown"){
    const tcCharge95=Math.min(1,(Date.now()-t.spawnedAt)/4000);
    t._tcCharge95=tcCharge95;
    drawThunderCrown(ctx,t.radius,ts,tcCharge95);
  }
  else if(t.type==="solar_beam"){
    drawSolarBeam(ctx,t.radius,ts);
  }
  else if(t.type==="ice_prism"){
    const ipSplit94=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._ipSplit94=ipSplit94;
    drawIcePrism(ctx,t.radius,ts,ipSplit94);
  }
  else if(t.type==="star_pulse"){
    const spPulse93=(Math.sin((Date.now()-t.spawnedAt)*0.004)+1)/2;
    t._spPulse93=spPulse93;
    drawStarPulse(ctx,t.radius,ts,spPulse93);
  }
  else if(t.type==="void_nova"){
    const vnCollapse93=Math.min(1,(Date.now()-t.spawnedAt)/4000);
    t._vnCollapse93=vnCollapse93;
    drawVoidNova(ctx,t.radius,ts,vnCollapse93);
  }
  else if(t.type==="flame_orb"){
    const foIntensity92=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._foIntensity92=foIntensity92;
    drawFlameOrb(ctx,t.radius,ts,foIntensity92);
  }
  else if(t.type==="crystal_shell"){
    const cscrack92=Math.min(1,(Date.now()-t.spawnedAt)/4500);
    t._cscrack92=cscrack92;
    drawCrystalShell(ctx,t.radius,ts,cscrack92);
  }
  else if(t.type==="nebula_core"){
    drawNebulaCore(ctx,t.radius,ts);
  }
  else if(t.type==="dusk_sphere"){
    const toPhase91=Math.min(1,(Date.now()-t.spawnedAt)/5000);
    t._toPhase91=toPhase91;
    drawDuskSphere(ctx,t.radius,ts,toPhase91);
  }
  else if(t.type==="spiral_bead"){
    drawSpiralBead(ctx,t.radius,ts);
  }
  else if(t.type==="quantum_flux"){
    const qfFlux90=(Math.sin((Date.now()-t.spawnedAt)*0.005)+1)/2;
    t._qfFlux90=qfFlux90;
    drawQuantumFlux(ctx,t.radius,ts,qfFlux90);
  }
  else if(t.type==="nebula_drop"){
    drawNebulaDrop(ctx,t.radius,ts);
  }
  else if(t.type==="pulse_gate"){
    const pgOpen89=(Math.sin((Date.now()-t.spawnedAt)*0.004)+1)/2;
    t._pgOpen89=pgOpen89;
    drawPulseGate(ctx,t.radius,ts,pgOpen89);
  }
  else if(t.type==="frost_gem"){
    drawFrostGem(ctx,t.radius,ts);
  }
  else if(t.type==="heat_orb"){
    const gsHO88=gsRef.current;
    drawHeatOrb(ctx,t.radius,ts,!!(gsHO88?.feverActive));
  }
  else if(t.type==="thunder_bead"){
    drawThunderBead(ctx,t.radius,ts);
  }
  else if(t.type==="mirror_star"){
    const msReflect87=(t._msStreak87||0)>=5?1:0;
    drawMirrorStar(ctx,t.radius,ts,msReflect87);
  }
  else if(t.type==="aurora_wave"){
    const awCrest86=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._awCrest86=awCrest86;
    drawAuroraWave(ctx,t.radius,ts,awCrest86);
  }
  else if(t.type==="crystal_prism"){
    drawCrystalPrism(ctx,t.radius,ts,t._cpFrags86||0);
  }
  else if(t.type==="plasma_ring"){
    if(!t._prSpin85)t._prSpin85=0;
    t._prSpin85+=0.025+(Math.floor((t.spawnedAt||0)%3))*0.005;
    drawPlasmaRing(ctx,t.radius,ts,t._prSpin85);
  }
  else if(t.type==="void_star"){
    drawVoidStar(ctx,t.radius,ts);
  }
  else if(t.type==="ice_dagger"){
    if(t._idTipAngle84===undefined)t._idTipAngle84=0;
    t._idTipAngle84+=(Math.random()<0.002?0.08:0); // slight wobble
    drawIceDagger(ctx,t.radius,ts,t._idTipAngle84);
  }
  else if(t.type==="shadow_flare"){
    const sfAge84=(Date.now()-t.spawnedAt)/1200;
    const sfBright84=(Math.sin(sfAge84*Math.PI)+1)/2; // 0→1 cycle
    t._sfBright84=sfBright84;
    drawShadowFlare(ctx,t.radius,ts,sfBright84);
  }
  else if(t.type==="nova_drift"){
    const ndAge83=Math.min(1,(Date.now()-t.spawnedAt)/3500);
    t._ndAge83=ndAge83;
    drawNovaDrift(ctx,t.radius,ts,ndAge83);
    if(ndAge83>=1&&!t.dying)t.dying=performance.now();
  }
  else if(t.type==="energy_pulse"){
    const epCharge83=Math.min(1,(Date.now()-t.spawnedAt)/5500);
    t._epCharge83=epCharge83;
    drawEnergyPulse(ctx,t.radius,ts,epCharge83);
  }
  else if(t.type==="solar_petal"){
    drawSolarPetal(ctx,t.radius,ts);
  }
  else if(t.type==="cosmic_shard"){
    const csColorIdx82=Math.floor((Date.now()-t.spawnedAt)/1500)%3;
    t._csColor82=csColorIdx82;
    drawCosmicShard(ctx,t.radius,ts,csColorIdx82);
  }
  else if(t.type==="crystal_wing_l"||t.type==="crystal_wing_r"){
    const isRight81=t.type==="crystal_wing_r";
    drawCrystalWing(ctx,t.radius,ts,isRight81,!!(t._cwTapTime81));
  }
  else if(t.type==="storm_cloud"){
    const scBolt81=Math.floor(Date.now()/1500)%4===0; // 1 out of 4 phase = bolt
    t._scBolt81=scBolt81;
    drawStormCloud(ctx,t.radius,ts,scBolt81);
  }
  else if(t.type==="arc_node"){
    drawArcNode(ctx,t.radius,ts);
  }
  else if(t.type==="prismatic_burst"){
    const pbPhase80=Math.floor((Date.now()-t.spawnedAt)/1200)%5;
    t._pbPhase80=pbPhase80;
    t._pbHitPhases80=t._pbHitPhases80||new Set();
    drawPrismaticBurst(ctx,t.radius,ts,pbPhase80);
  }
  else if(t.type==="moon_orb"){
    const moPct79=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||6000));
    t._moonPct79=moPct79;
    drawMoonOrb(ctx,t.radius,ts,moPct79);
  }
  else if(t.type==="lava_core"){
    drawLavaCore(ctx,t.radius,ts);
  }
  else if(t.type==="sun_petal"){
    const spBloom78=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
    t._bloomPct78=spBloom78;
    drawSunPetal(ctx,t.radius,ts,spBloom78);
  }
  else if(t.type==="ghost_bell"){
    const gbAge78=(Date.now()-t.spawnedAt)/1200;
    const gbAlpha78=(Math.sin(gbAge78*Math.PI*2)*0.5+0.5); // 0→1 oscillation
    const gbSilent78=gbAlpha78<0.25; // silent phase when very faint
    t._gbSilent78=gbSilent78;
    drawGhostBell(ctx,t.radius,ts,Math.max(0.15,gbAlpha78),gbSilent78);
  }
  else if(t.type==="plasma_disk"){
    if(!t._spinAngle77)t._spinAngle77=0;
    t._spinAngle77+=(0.025+0.005*Math.floor((t.spawnedAt||0)%5))*( (Date.now()-t.spawnedAt)/t.lifetime>0.5?1.3:1 );
    drawPlasmaDisk(ctx,t.radius,ts,t._spinAngle77);
  }
  else if(t.type==="void_ring"){
    const vrRingPct77=(Date.now()-t.spawnedAt)%(t.lifetime||4200)/(t.lifetime||4200);
    drawVoidRing(ctx,t.radius,ts,vrRingPct77);
  }
  else if(t.type==="ether_orb"){
    drawEtherOrb(ctx,t.radius,ts);
  }
  else if(t.type==="ether_copy"){
    drawEtherCopy(ctx,t.radius,ts,t._ethHue76||175);
  }
  else if(t.type==="quantum_blast"){
    const qbScale76=(Math.sin(ts*0.015)+1)/2;
    t._qbScale76=qbScale76;
    drawQuantumBlast(ctx,t.radius,ts,qbScale76);
  }
  else if(t.type==="nexus_core"){
    drawNexusCore(ctx,t.radius,ts);
  }
  else if(t.type==="blink_star"){
    drawBlinkStar(ctx,t.radius,ts,!!(t._justBlinked75));
  }
  else if(t.type==="omega_pulse"){
    const opAlpha74=Math.max(0,1-(Date.now()-t.spawnedAt)/3000);
    t._alphaFade74=opAlpha74;
    drawOmegaPulse(ctx,t.radius,ts,opAlpha74);
    if(opAlpha74<=0&&!t.dying)t.dying=performance.now();
  }
  else if(t.type==="spiral_orb74"){
    drawSpiralOrb74(ctx,t.radius,ts,!!(t._atCenter74));
  }
  else if(t.type==="frost_nova"){
    drawFrostNova(ctx,t.radius,ts);
  }
  else if(t.type==="light_bomb"){
    drawLightBomb(ctx,t.radius,ts);
  }
  else if(t.type==="glitch_orb"){
    const flickerPeriod72=400; // ms between flicker toggles
    const flickerPhase72=Math.floor(Date.now()/flickerPeriod72);
    const visible72=flickerPhase72%2===0; // on/off alternating
    t._visible72=visible72;
    if(visible72)drawGlitchOrb(ctx,t.radius,ts,true);
  }
  else if(t.type==="solar_flare72"){
    const sfLifetime72=t.lifetime||5000;
    const sfElapsed72=Date.now()-t.spawnedAt;
    // Bright arc: peaks at 60% of lifetime, then fades
    const sfT72=sfElapsed72/sfLifetime72;
    const brightPct72=sfT72<0.6?sfT72/0.6:Math.max(0,1-(sfT72-0.6)/0.4);
    t._brightPct72=brightPct72;
    drawSolarFlare72(ctx,t.radius,ts,brightPct72);
  }
  else if(t.type==="crystal_burst"){
    drawCrystalBurst(ctx,t.radius,ts);
  }
  else if(t.type==="crystal_shard71"){
    drawCrystalShard71(ctx,t.radius,ts,t._shardHue||180);
  }
  else if(t.type==="time_rift"){
    // Time rift counts down at half speed
    const trLifetime71=t.lifetime||8000;
    const trElapsed71=(Date.now()-t.spawnedAt)*0.5; // half speed
    const trLeft71=Math.max(0,trLifetime71-trElapsed71);
    t._effectiveTimeLeft=trLeft71;
    drawTimeRift(ctx,t.radius,ts,trLeft71,trLifetime71);
    // Expire at half-speed
    if(trLeft71<=0&&!t.dying)t.dying=performance.now();
  }
  else if(t.type==="arc_bloom"){
    const bloomPct70=Math.min(1,(Date.now()-t.spawnedAt)/3000);
    t._bloomPct=bloomPct70;
    drawArcBloom(ctx,t.radius,ts,bloomPct70);
  }
  else if(t.type==="neon_bolt"){
    const boltSpd70=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||3000));
    drawNeonBolt(ctx,t.radius,ts,boltSpd70);
  }
  else if(t.type==="prism_clock"){
    // Determine which segment is active (rotates every 2s)
    const segCycle69=Math.floor((Date.now()-t.spawnedAt)/2000)%3;
    t._activeSeg=segCycle69;
    // Check if world color matches (simplified: use world index mod 3)
    const worldSeg69=(t._worldSegHint||0);
    const matched69=segCycle69===worldSeg69;
    t._matched=matched69;
    drawPrismClock(ctx,t.radius,ts,segCycle69,matched69);
  }
  else if(t.type==="void_pulse"){
    const pulseCycle69=(Math.sin((Date.now()-t.spawnedAt)*0.003)+1)/2;
    t._scalePct=pulseCycle69;
    drawVoidPulse(ctx,t.radius,ts,pulseCycle69);
  }
  else if(t.type==="chain_nova"){
    const primed68=!!(t._primed);
    drawChainNova(ctx,t.radius,ts,primed68);
  }
  else if(t.type==="mirror_flash"){
    const flashOp68=0.6+0.4*Math.abs(Math.sin(ts*0.006));
    t._opacity=flashOp68;
    drawMirrorFlash(ctx,t.radius,ts,true,flashOp68);
  }
  else if(t.type==="mirror_decoy"){
    const decoyOp68=0.4+0.3*Math.abs(Math.sin(ts*0.006+0.8));
    drawMirrorFlash(ctx,t.radius,ts,false,decoyOp68);
  }
  else if(t.type==="dark_mirror"){
    const opacityPct67=(Math.sin(ts*0.008)+1)/2;
    t._opacityPct=opacityPct67;
    drawDarkMirror(ctx,t.radius,ts,opacityPct67);
  }
  else if(t.type==="storm_ball"){
    const speedPct67=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
    t._speedPct=speedPct67;
    drawStormBall(ctx,t.radius,ts,speedPct67);
  }
  else if(t.type==="heal_orb"){
    const healPct67=Math.min(1,(Date.now()-t.spawnedAt)/3000);
    drawHueOrb(ctx,t.radius,ts,120,healPct67>0.5);
  }
  else if(t.type==="bonus_orb"){
    // Reuse stellar orb at small size
    const boPct=Math.min(1,(Date.now()-t.spawnedAt)/1000);
    drawStellarOrb(ctx,t.radius,ts,boPct);
  }
  else if(t.type==="arcane_rune"){
    const sym65=t._symbol||0;
    const primed65=!!(t._primed);
    drawArcaneRune(ctx,t.radius,ts,sym65,primed65);
  }
  else if(t.type==="pulse_gem"){
    const pulsePct65=(Math.sin(ts*0.012)+1)/2; // 0-1 oscillates
    t._pulsePct=pulsePct65;
    drawPulseGem(ctx,t.radius,ts,pulsePct65);
  }
  else if(t.type==="hue_orb"){
    const cycleMs64=2000;
    const hue64=((Date.now()-t.spawnedAt)/cycleMs64*360)%360;
    t._currentHue=hue64;
    const worldHue64=t._worldHue||0;
    const hueDiff64=Math.abs(hue64-worldHue64)%360;
    const matched64=hueDiff64<30||hueDiff64>330;
    t._matched=matched64;
    drawHueOrb(ctx,t.radius,ts,hue64,matched64);
  }
  else if(t.type==="lightning_chain_64"){
    const chainCount64=t._chainCount||0;
    drawLightningChain64(ctx,t.radius,ts,chainCount64);
  }
  else if(t.type==="nebula_clock"){
    const dotCount63=t._dotsLeft!==undefined?t._dotsLeft:5;
    drawNebulaClock(ctx,t.radius,ts,dotCount63);
  }
  else if(t.type==="nebula_dot"){
    drawNebulaDot(ctx,t.radius,ts);
  }
  else if(t.type==="ice_shatter"){
    drawIceShatter(ctx,t.radius,ts,t.hitsLeft||0);
  }
  else if(t.type==="plasma_burst"){
    drawPlasmaBurst(ctx,t.radius,ts,!!(t._boltsFired));
  }
  else if(t.type==="plasma_bolt"){
    // Reuse plasma burst draw at small size with charged=true
    drawPlasmaBurst(ctx,t.radius,ts,true);
  }
  else if(t.type==="star_clock"){
    const elapsed62=(Date.now()-t.spawnedAt)/1000;
    const countdown62=Math.max(0,Math.round(5-elapsed62));
    t._countdown=countdown62;
    drawStarClock(ctx,t.radius,ts,countdown62);
  }
  else if(t.type==="vortex_star_61"){
    const tapDist61=t._lastTapDist||0;
    const zone61=tapDist61>t.radius*2?2:tapDist61>t.radius*1.4?1:0;
    drawVortexStar61(ctx,t.radius,ts,zone61);
  }
  else if(t.type==="quantum_drift"){
    const frozen61=!!(t._frozenUntil&&Date.now()<t._frozenUntil);
    drawQuantumDrift(ctx,t.radius,ts,frozen61);
  }
  else if(t.type==="prism_nova"){
    const pnStage60=t._shardsTapped||0;
    drawPrismNova(ctx,t.radius,ts,pnStage60);
  }
  else if(t.type==="prism_shard"){
    const shardHue60=t._shardHue||0;
    drawPrismNovaShard(ctx,t.radius,ts,shardHue60);
  }
  else if(t.type==="echo_blast"){
    const ebCharged60=!!(t._ringSpawned);
    drawEchoBlast(ctx,t.radius,ts,ebCharged60);
    // Draw echo ring if active
    if(t._echoRing&&t._echoRingR){
      const echoAge60=(Date.now()-t._echoRingSpawnAt)/1500;
      if(echoAge60<1){
        ctx.save();ctx.globalAlpha=(1-echoAge60)*0.5;
        ctx.strokeStyle="#f59e0b";ctx.lineWidth=3+echoAge60*2;
        ctx.shadowColor="#f59e0b";ctx.shadowBlur=12;
        ctx.beginPath();ctx.arc(0,0,t._echoRingR*(0.5+echoAge60*1.2),0,Math.PI*2);ctx.stroke();
        ctx.restore();
      }
    }
  }
  else if(t.type==="solar_ring"){
    const ringPct59=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4000));
    t._ringPct=ringPct59;
    // Compute ring radius for hit detection in tap handler
    t._ringR=t.radius*ringPct59*2.5;
    drawSolarRing(ctx,t.radius,ts,ringPct59);
  }
  else if(t.type==="chrono_vortex"){
    const slowedCount59=t._slowedCount||0;
    drawChronoVortex(ctx,t.radius,ts,slowedCount59);
  }
  else if(t.type==="nova_crystal"){
    const agePct58=(Date.now()-t.spawnedAt)/(t.lifetime||5000);
    const stage58=Math.min(2,Math.floor(agePct58*3));
    t._stage=stage58;
    drawNovaCrystal(ctx,t.radius,ts,stage58);
  }
  else if(t.type==="phase_shift"){
    const cyclePct58=((Date.now()-t.spawnedAt)%1200)/1200;
    const isSolid58=cyclePct58<0.5;
    t._isSolid=isSolid58;t._cyclePct=cyclePct58;
    drawPhaseShift(ctx,t.radius,ts,Math.min(1,cyclePct58*2),isSolid58);
  }
  else if(t.type==="stellar_orb"){
    const age57s=Date.now()-t.spawnedAt;
    const sizePct57=(age57s/(t.lifetime||6000));
    const scaledR57=t.radius*(0.3+0.7*Math.min(1,sizePct57));
    t._sizePct=sizePct57;
    ctx.scale(0.3+0.7*Math.min(1,sizePct57),0.3+0.7*Math.min(1,sizePct57));
    drawStellarOrb(ctx,t.radius,ts,sizePct57);
    ctx.scale(1/(0.3+0.7*Math.min(1,sizePct57)),1/(0.3+0.7*Math.min(1,sizePct57)));
  }
  else if(t.type==="rune_gate_a"||t.type==="rune_gate_b"){
    const isSecond57=t.type==="rune_gate_b";
    const primed57=t._primed||false;
    drawRuneGate(ctx,t.radius,ts,isSecond57,primed57);
    // Draw dashed line to partner
    const partner57=targetsRef.current.find(o=>o.id===t._partnerId&&!o.dying);
    if(partner57){
      ctx.save();
      ctx.strokeStyle=primed57?"#fde68a":"rgba(129,140,248,0.4)";
      ctx.lineWidth=1.5;ctx.setLineDash([5,5]);
      ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(partner57.x-t.x,partner57.y-t.y);ctx.stroke();ctx.setLineDash([]);
      ctx.restore();
    }
  }
  else if(t.type==="arc_pulse"){
    const arcAngle56=((performance.now()|0)*0.0015)%(Math.PI*2);
    t._arcAngle=arcAngle56;
    const normArc=((arcAngle56-Math.PI*1.5)%(Math.PI*2)+Math.PI*2)%(Math.PI*2);
    const inZone56=normArc<Math.PI*0.5;
    t._inZone=inZone56;
    drawArcPulse(ctx,t.radius,ts,arcAngle56,inZone56);
  }
  else if(t.type==="void_bloom"||t.type==="void_petal"){
    const isPetal56=t.type==="void_petal";
    const bloomTaps56=t._bloomTaps||0;
    const petalPct56=bloomTaps56/4;
    drawVoidBloom(ctx,t.radius,ts,isPetal56,petalPct56);
  }
  else if(t.type==="crystal_web"||t.type==="crystal_web_node"){
    const isNode55=t.type==="crystal_web_node";
    const webTaps55=t._webTaps||0;
    const chainPct55=webTaps55/3;
    drawCrystalWeb(ctx,t.radius,ts,isNode55,chainPct55);
    // Draw lines connecting main web to its nodes
    if(!isNode55&&t._nodeIds){
      ctx.save();
      t._nodeIds.forEach(nid=>{
        const n=targetsRef.current.find(o=>o.id===nid&&!o.dying);
        if(!n)return;
        ctx.strokeStyle=n._tapped55?"#fbbf24":"rgba(34,211,238,0.5)";
        ctx.lineWidth=1.5;ctx.setLineDash([4,4]);
        ctx.beginPath();ctx.moveTo(0,0);ctx.lineTo(n.x-t.x,n.y-t.y);ctx.stroke();ctx.setLineDash([]);
      });
      ctx.restore();
    }
  }
  else if(t.type==="ice_phoenix"){
    const hatchPct46=Math.min(1,(Date.now()-t.spawnedAt)/2500);
    t._hatchPct=hatchPct46;
    drawIcePhoenix(ctx,t.radius,ts,hatchPct46);
  }
  else if(t.type==="glow_shard"){
    drawGlowShard(ctx,t.radius,ts,false,t.color||"#f0abfc");
  }
  else if(t.type==="glow_fragment"){
    drawGlowShard(ctx,t.radius,ts,true,t._fragColor||"#f0abfc");
  }
  else if(t.type==="quantum_leech"){
    const elapsed45=Date.now()-t.spawnedAt;
    const drainPct45=Math.min(1,elapsed45/(t.lifetime||5000));
    t._drainPct=drainPct45;
    t._drainedTotal=Math.floor(elapsed45/1000)*10; // 10pts per second
    drawQuantumLeech(ctx,t.radius,ts,drainPct45);
  }
  else if(t.type==="star_bomb"){
    // fuse: 1.0 (just spawned) → 0.0 (detonating)
    const fuse44=Math.max(0,1-(Date.now()-t.spawnedAt)/(t.lifetime||3500));
    t._fuseLeft=fuse44;
    drawStarBomb(ctx,t.radius,ts,fuse44);
  }
  else if(t.type==="energy_orb2"){
    const chargePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||6000));
    t._chargePct=chargePct;
    drawEnergyOrb(ctx,t.radius,ts,chargePct);
  }
  else if(t.type==="comet_trail"){
    // Record trail positions
    if(!t._trailPos)t._trailPos=[];
    if(t._trailPos.length===0||Math.hypot(t.x-(t._trailPos[t._trailPos.length-1]?.x||0),t.y-(t._trailPos[t._trailPos.length-1]?.y||0))>12){
      t._trailPos.push({x:t.x,y:t.y});
      if(t._trailPos.length>8)t._trailPos.shift();
    }
    // Draw trail
    t._trailPos.forEach((pt2,i)=>{
      const alpha=(i/t._trailPos.length)*0.5;
      ctx.save();ctx.globalAlpha=alpha;ctx.fillStyle="#fb923c";ctx.shadowBlur=6;ctx.shadowColor="#f97316";
      ctx.beginPath();ctx.arc(pt2.x-t.x,pt2.y-t.y,t.radius*(0.2+i/t._trailPos.length*0.3),0,Math.PI*2);ctx.fill();
      ctx.restore();
    });
    drawCometTrail(ctx,t.radius,ts);
  }
  else if(t.type==="star_cluster"){
    drawStarCluster(ctx,t.radius,ts);
  }
  else if(t.type==="star_sparkle"){
    drawStarSparkle(ctx,t.radius,ts,t._sparkleIdx||0);
  }
  else if(t.type==="void_crystal"){
    // Shrinks from 100% to 10% over lifetime
    const ageVC=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||7000));
    const sizePct=Math.max(0.1,1-ageVC*0.9);
    t.radius=BASE_R*1.25*sizePct;
    t._voidSizePct=sizePct; // store for tap handler
    if(sizePct<=0.1&&!t._vanishing){t._vanishing=true;spawnParticles(t.x,t.y,"#7c3aed",8,"spark");}
    drawVoidCrystal(ctx,t.radius,ts,sizePct);
  }
  else if(t.type==="neon_pulse"){
    // Pulse cycle: 1s bright, 1s dim (repeating)
    const cycleMs=(Date.now()-t.spawnedAt)%2000;
    const brightPct=cycleMs<1000?cycleMs/1000:1-(cycleMs-1000)/1000;
    t._brightPct=brightPct; // store for tap handler
    drawNeonPulse(ctx,t.radius,ts,brightPct);
  }
  else if(t.type==="mirror_twin"){
    // Spawn mirrored copy on first tick
    if(!t._mirrorSpawned){
      t._mirrorSpawned=true;
      const cw88=canvasRef.current?.width||360;const ch88=canvasRef.current?.height||640;
      const groupId88=t.id;
      const mx88=Math.max(30,Math.min(cw88-30,cw88-t.x));
      const my88=Math.max(30,Math.min(ch88-30,t.y));
      const mirror88={id:Math.random().toString(36).slice(2),type:"mirror_twin",
        rarity:t.rarity,x:mx88,y:my88,radius:t.radius,color:"#f9a8d4",glow:"#fce7f3",
        lifetime:t.lifetime,spawnedAt:t.spawnedAt,born:performance.now(),anticipateMs:0,
        moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
        worldId:t.worldId,worldColor:t.worldColor,bossPattern:null,bossPhase:1,cx:mx88,cy:my88,
        _mirrorGroup:groupId88,_mirrorIsClone:true,_mirrorSpawned:true};
      t._mirrorGroup=groupId88;t._mirrorCloneId=mirror88.id;
      targetsRef.current.push(mirror88);
    }
    drawMirrorTwin(ctx,t.radius,ts,!!t._mirrorIsClone);
  }
  else if(t.type==="gemini_pair"){
    // Spawn partner on first tick
    if(!t._geminiSpawned){
      t._geminiSpawned=true;
      const cw66=canvasRef.current?.width||360;const ch66=canvasRef.current?.height||640;
      const groupId66=t.id;
      let px66=Math.random()*cw66*0.6+cw66*0.2;
      let py66=Math.random()*ch66*0.5+ch66*0.2;
      // Keep partner at least 80px from main
      if(Math.hypot(px66-t.x,py66-t.y)<80){px66=Math.min(cw66-30,t.x+90);py66=Math.min(ch66-30,t.y+60);}
      const partner={id:Math.random().toString(36).slice(2),type:"gemini_pair",
        rarity:t.rarity,x:px66,y:py66,radius:t.radius,color:"#a78bfa",glow:"#f5f3ff",
        lifetime:t.lifetime,spawnedAt:t.spawnedAt,born:performance.now(),anticipateMs:0,
        moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
        worldId:t.worldId,worldColor:t.worldColor,bossPattern:null,bossPhase:1,cx:px66,cy:py66,
        _geminiGroup:groupId66,_geminiPartnerId:t.id,_geminiIsPartner:true,_geminiSpawned:true};
      t._geminiGroup=groupId66;t._geminiPartnerId=partner.id;
      targetsRef.current.push(partner);
    }
    drawGeminiPair(ctx,t.radius,ts,!!t._geminiTapped);
  }
  else if(t.type==="aurora_ring"){
    const expandPct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
    drawAuroraRing(ctx,t.radius,ts,expandPct);
  }
  else if(t.type==="lightning_chain"){
    drawLightningChain(ctx,t.radius,ts);
  }
  else if(t.type==="magma_seed"){
    // Lazy-spawn 5 lava drops in orbit
    if(!t._lavaSpawned){
      t._lavaSpawned=true;
      const cw55=canvasRef.current?.width||360;const ch55=canvasRef.current?.height||640;
      for(let i=0;i<5;i++){
        const pa55=(i/5)*Math.PI*2;const pd55=60;
        const px55=Math.max(15,Math.min(cw55-15,t.x+Math.cos(pa55)*pd55));
        const py55=Math.max(15,Math.min(ch55-15,t.y+Math.sin(pa55)*pd55));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"lava_drop",
          rarity:null,x:px55,y:py55,radius:10,color:"#f97316",glow:"#fee2e2",
          lifetime:5500,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
          worldId:t.worldId,worldColor:t.worldColor,bossPattern:null,bossPhase:1,cx:px55,cy:py55,
          _magmaGroup:t.id,_magmaIdx:i,_orbitAngle:pa55,_orbitCx:t.x,_orbitCy:t.y,_orbitR:pd55});
      }
    }
    // Orbit drops around the seed
    const ageMSeed=Date.now()-t.spawnedAt;
    targetsRef.current.filter(o=>o._magmaGroup===t.id&&!o.dying).forEach(o=>{
      o._orbitAngle=(o._orbitAngle||0)+0.025;
      o.x=t.x+Math.cos(o._orbitAngle)*o._orbitR;
      o.y=t.y+Math.sin(o._orbitAngle)*o._orbitR*0.7;
    });
    drawMagmaSeed(ctx,t.radius,ts);
  }
  else if(t.type==="lava_drop"){
    drawLavaDrop(ctx,t.radius,ts);
  }
  else if(t.type==="prism_shard"){
    drawPrismShard(ctx,t.radius,ts);
  }
  else if(t.type==="prism_mini"){
    drawPrismMiniShard(ctx,t.radius,ts,t._prismIdx||0);
  }
  else if(t.type==="temporal_orb"){
    // Rewind logic: every 2s teleport to a new random position
    const nowTO=Date.now();
    if(!t._lastRewind)t._lastRewind=t.spawnedAt;
    const rewindPct=((nowTO-t._lastRewind)%2000)/2000;
    if(rewindPct<0.05&&nowTO-t._lastRewind>100){
      // Trigger rewind
      t._lastRewind=nowTO;t._justRewound=nowTO;
      const cw22=canvasRef.current?.width||360;const ch22=canvasRef.current?.height||640;
      t.x=Math.max(t.radius+10,Math.min(cw22-t.radius-10,Math.random()*cw22));
      t.y=Math.max(t.radius+10,Math.min(ch22-t.radius-10,Math.random()*ch22));
      spawnParticles(t.x,t.y,"#38bdf8",8,"spark");
    }
    drawTemporalOrb(ctx,t.radius,ts,rewindPct);
  }
  else if(t.type==="void_seed"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||8000));
    // Update radius dynamically to reflect growth
    t.radius=BASE_R*(0.6+0.8*agePct);
    const maxed=agePct>=0.98;
    if(maxed&&!t._detonating){
      t._detonating=true;
      spawnParticles(t.x,t.y,"#7c3aed",16,"spark");
      spawnPopup(t.x,t.y-28,"💥 BLOOMED!","#c084fc",18);
    }
    drawVoidSeed(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="nano_unit"){
    ctx.save();ctx.shadowBlur=8;ctx.shadowColor="#34d399";ctx.fillStyle="#34d399";ctx.globalAlpha=0.9;
    ctx.beginPath();ctx.arc(0,0,t.radius,0,Math.PI*2);ctx.fill();ctx.restore();
  }
  else if(t.type==="spiral_bomb"){
    const fillPct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4000));
    drawSpiralBomb(ctx,t.radius,ts,fillPct);
  }
  else if(t.type==="chain_orb")   drawChainOrb(ctx,t.radius,ts,t.hitsLeft||4);
  else if(t.type==="bolt_catcher"){
    const urgencyPct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||1500));
    drawBoltCatcher(ctx,t.radius,ts,urgencyPct);
  }
  else if(t.type==="gem_harvest"){
    // Count absorbed gems in draw tick
    if(!t._gemCount)t._gemCount=0;
    const HARVEST_R=160;
    const now2=Date.now();
    if(!t._lastHarvestCheck||now2-t._lastHarvestCheck>800){
      t._lastHarvestCheck=now2;
      const victims=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<HARVEST_R);
      victims.forEach(o=>{o.dying=performance.now();t._gemCount=(t._gemCount||0)+1;});
    }
    drawGemHarvest(ctx,t.radius,ts,t._gemCount||0);
  }
  else if(t.type==="mirror_echo"){
    // Translucent mirror copy of a normal target
    ctx.save();ctx.globalAlpha=0.6;ctx.shadowBlur=12;ctx.shadowColor="#818cf8";
    const gMe=ctx.createRadialGradient(0,0,0,0,0,t.radius);
    gMe.addColorStop(0,"#fff");gMe.addColorStop(0.5,"#c7d2fe");gMe.addColorStop(1,"#1e1b4b");
    ctx.fillStyle=gMe;ctx.beginPath();ctx.arc(0,0,t.radius,0,Math.PI*2);ctx.fill();
    ctx.globalAlpha=0.8;ctx.font=`${Math.floor(t.radius*0.55)}px sans-serif`;
    ctx.textAlign="center";ctx.textBaseline="middle";ctx.fillText("🌀",0,1);
    ctx.restore();
  }
  else if(t.type==="omega_orb")     drawOmegaOrb(ctx,t.radius,ts);
  else if(t.type==="thunder_ball")  drawThunderBall(ctx,t.radius,ts);
  else if(t.type==="cosmic_aura")   drawCosmicAura(ctx,t.radius,ts);
  else if(t.type==="combo_rush")    drawComboRush(ctx,t.radius,ts,gsRef.current?.streak||0);
  else if(t.type==="neon_crystal"){
    const phase=Math.floor((Date.now()-t.spawnedAt)/800)%3;
    drawNeonCrystal(ctx,t.radius,ts,phase);
  }
  else if(t.type==="sonar_ping"){
    if(!t._pings)t._pings=[];
    const nowP=Date.now();
    if(!t._lastPing||nowP-t._lastPing>900){t._lastPing=nowP;t._pings.push(0);}
    t._pings=t._pings.map(a=>a+(16.67/900)).filter(a=>a<1.5);
    drawSonarPing(ctx,t.radius,ts,t._pings);
  }
  else if(t.type==="gravity_bomb"){
    const fallPct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4500));
    drawGravityBomb(ctx,t.radius,ts,fallPct);
  }
  else if(t.type==="speed_nova"){
    const sizePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4000));
    drawSpeedNova(ctx,t.radius,ts,sizePct);
  }
  else if(t.type==="quantum_split") drawQuantumSplit(ctx,t.radius,ts,0);
  else if(t.type==="quantum_half")  drawQuantumHalf(ctx,t.radius,ts);
  else if(t.type==="frost_bolt")    drawFrostBolt(ctx,t.radius,ts,1);
  else if(t.type==="vortex_star"){
    const starCount=Math.min(8,Math.floor((Date.now()-t.spawnedAt)/500));
    t._starCount=starCount;
    drawVortexStar(ctx,t.radius,ts,starCount);
  }
  else if(t.type==="pixel_burst")   drawPixelBurst(ctx,t.radius,ts,t.hitsLeft||3);
  else if(t.type==="star_forge"){
    const chargePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4500));
    drawStarForge(ctx,t.radius,ts,chargePct);
  }
  else if(t.type==="magic_dice"){
    const face=1+Math.floor((Date.now()-t.spawnedAt)/600)%6;
    drawMagicDice(ctx,t.radius,ts,face);
  }
  else if(t.type==="wormhole_tgt") drawWormholeTgt(ctx,t.radius,ts);
  else if(t.type==="shadow_pulse"){
    const isDark=Math.floor((Date.now()-t.spawnedAt)/800)%2===0;
    drawShadowPulse(ctx,t.radius,ts,isDark);
  }
  else if(t.type==="eclipse_orb"){
    const isMoon=Math.floor((Date.now()-t.spawnedAt)/800)%2===1;
    drawEclipseOrb(ctx,t.radius,ts,isMoon);
  }
  else if(t.type==="spin_bloom"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4000));
    const petalsLeft=Math.max(1,Math.ceil(8*(1-agePct*0.9)));
    drawSpinBloom(ctx,t.radius,ts,petalsLeft);
  }
  else if(t.type==="tesla_node") drawTeslaNode(ctx,t.radius,ts,t._bank||0);
  else if(t.type==="rune_circle"){
    const runeIdx=Math.floor((Date.now()-t.spawnedAt)/550)%6;
    drawRuneCircle(ctx,t.radius,ts,runeIdx);
  }
  else if(t.type==="solar_orbit") drawSolarOrbit(ctx,t.radius,ts);
  else if(t.type==="chroma_shift"){
    const colorIdx=Math.floor((Date.now()-t.spawnedAt)/400)%6;
    drawChromaShift(ctx,t.radius,ts,colorIdx);
  }
  else if(t.type==="neon_pulsar"){
    const cycleMs=1200;
    const phase=(Date.now()-t.spawnedAt)%cycleMs/cycleMs;
    const phasePct=Math.sin(phase*Math.PI); // 0→1→0
    drawNeonPulsar(ctx,t.radius,ts,phasePct);
  }
  else if(t.type==="drift_mine"){
    const timeLeftPct=Math.max(0,1-(Date.now()-t.spawnedAt)/(t.lifetime||4500));
    drawDriftMine(ctx,t.radius,ts,timeLeftPct);
  }
  else if(t.type==="arc_light"){
    const arcDist=190;
    const arcTgts=targetsRef.current.filter(x=>x.id!==t.id&&!x.dying&&Math.hypot(x.x-t.x,x.y-t.y)<arcDist).slice(0,4).map(x=>({dx:x.x-t.x,dy:x.y-t.y}));
    drawArcLight(ctx,t.radius,ts,arcTgts);
  }
  else if(t.type==="pulse_chain")   drawPulseChain(ctx,t.radius,ts);
  else if(t.type==="ghost_orb"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4200));
    drawGhostOrb(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="time_fracture") drawTimeFracture(ctx,t.radius,ts);
  else if(t.type==="venom_orb"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||3500));
    drawVenomOrb(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="thunder_egg"){
    const phase=Math.min(3,Math.floor((Date.now()-t.spawnedAt)/1200));
    drawThunderEgg(ctx,t.radius,ts,phase);
  }
  else if(t.type==="moon_glyph"){
    const symIdx=Math.floor((Date.now()-t.spawnedAt)/600)%5;
    drawMoonGlyph(ctx,t.radius,ts,symIdx);
  }
  else if(t.type==="comet_tail")    drawCometTail(ctx,t.radius,ts);
  else if(t.type==="ember_ring"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||3200));
    drawEmberRing(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="accumulator")   drawAccumulator(ctx,t.radius,ts,t._stacks||0);
  else if(t.type==="jackpot_target")drawJackpotTarget(ctx,t.radius,ts);
  else if(t.type==="phase_orb"){
    const phase=Math.floor((Date.now()-t.spawnedAt)/1200)%3;
    drawPhaseOrb(ctx,t.radius,ts,phase);
  }
  else if(t.type==="ripple_wave"){
    if(!t._ripRings)t._ripRings=[];
    const now2=Date.now();
    if(!t._lastRip||now2-t._lastRip>800){t._lastRip=now2;t._ripRings.push(0);}
    t._ripRings=t._ripRings.map(a=>a+(16.67/800)).filter(a=>a<1);
    drawRippleWave(ctx,t.radius,ts,t._ripRings);
  }
  else if(t.type==="laser_grid")    drawLaserGrid(ctx,t.radius,ts);
  else if(t.type==="bouncing_orb")  drawBouncingOrb(ctx,t.radius,ts,t._bounces||0);
  else if(t.type==="time_capsule"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
    drawTimeCapsule(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="nether_shard"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
    drawNetherShard(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="solar_burst")   drawSolarBurst(ctx,t.radius,ts);
  else if(t.type==="frost_shard"){
    ctx.save();ctx.shadowBlur=10;ctx.shadowColor="#7dd3fc";ctx.fillStyle="#bae6fd";ctx.globalAlpha=0.85;
    ctx.beginPath();const fsr=t.radius;
    for(let i=0;i<6;i++){const a=(i/6)*Math.PI*2;ctx.lineTo(Math.cos(a)*fsr,Math.sin(a)*fsr);}
    ctx.closePath();ctx.fill();ctx.restore();
  }
  else if(t.type==="star_rain_drop"){
    ctx.save();ctx.shadowBlur=16;ctx.shadowColor="#ffd700";
    const pulse=0.9+0.1*Math.sin(ts*0.015);
    // 5-point star shape
    ctx.fillStyle="#ffd700";ctx.globalAlpha=0.95;ctx.beginPath();
    for(let i=0;i<10;i++){
      const a=(i/10)*Math.PI*2-Math.PI/2;const sr=t.radius*(i%2===0?1:0.45)*pulse;
      i===0?ctx.moveTo(Math.cos(a)*sr,Math.sin(a)*sr):ctx.lineTo(Math.cos(a)*sr,Math.sin(a)*sr);
    }
    ctx.closePath();ctx.fill();ctx.restore();
  }
  else if(t.type==="frost_comet")   drawFrostComet(ctx,t.radius,ts);
  else if(t.type==="energy_web"){
    const webCount=(t._webNodes||0);
    drawEnergyWeb(ctx,t.radius,ts,webCount);
  }
  else if(t.type==="void_rift"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
    drawVoidRift(ctx,t.radius,ts,agePct,t._devouredCount||0);
  }
  else if(t.type==="quantum_leap")  drawQuantumLeap(ctx,t.radius,ts,t._qlTrailAlpha||0);
  else if(t.type==="magma_burst"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4000));
    drawMagmaBurst(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="lightning_rod") drawLightningRod(ctx,t.radius,ts,t._rodAttract||0);
  else if(t.type==="prism_gate")    drawPrismGate(ctx,t.radius,ts);
  else if(t.type==="bubble_cluster")drawBubbleCluster(ctx,t.radius,ts,t._popped);
  else if(t.type==="void_shard")    drawVoidShard(ctx,t.radius,ts,t._absorbedPts||0);
  else if(t.type==="stasis_field"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||2500));
    drawStasisField(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="crystalline")   drawCrystalline(ctx,t.radius,ts);
  else if(t.type==="gravity_well"){
    const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4200));
    drawGravityWell(ctx,t.radius,ts,agePct);
  }
  else if(t.type==="spectrum_orb")  drawSpectrumOrb(ctx,t.radius,ts);
  else if(t.type==="shard_storm")   drawShardStorm(ctx,t.radius,ts);
  else if(t.type==="pulse_nova"){
    const chargePct=Math.min(1,(Date.now()-t.spawnedAt)/(1500));
    drawPulseNova(ctx,t.radius,ts,chargePct);
  }
  else if(t.type==="surge_ball")    drawSurgeBall(ctx,t.radius,ts);
  else if(t.type==="ticking_clock"){
    const elapsed=(Date.now()-t.spawnedAt)/1000;
    const totalSecs=(t.lifetime||4000)/1000;
    const secsLeft=Math.max(0,totalSecs-elapsed);
    drawTickingClock(ctx,t.radius,ts,secsLeft,totalSecs);
  }
  else if(t.type==="echo_pulse"){
    if(!t._rings)t._rings=[];
    const now2=Date.now();const interval=700;
    if(!t._lastRingAt||now2-t._lastRingAt>interval){
      t._lastRingAt=now2;
      t._rings.push({r:t.radius*0.55,maxR:t.radius*2.2,born:now2,life:900});
    }
    t._rings=t._rings.filter(rg=>now2-rg.born<rg.life);
    t._rings.forEach(rg=>{const age=(now2-rg.born)/rg.life;rg.r=t.radius*0.55+age*(rg.maxR-t.radius*0.55);});
    drawEchoPulse(ctx,t.radius,ts,t._rings);
  }
  else if(t.type==="solar_drone")   drawSolarDrone(ctx,t.radius,ts);
  else if(t.type==="crystal_cluster") drawCrystalCluster(ctx,t.radius,ts);
  else if(t.type==="swarm")         drawSwarm(ctx,t.radius,ts);
  else if(t.type==="celestial")     drawCelestialOrb(ctx,t.radius,ts);
  else if(t.type==="fractal"||t.type==="fractal_mini") drawFractal(ctx,t.radius,ts,t._fractalDepth||0);
  else if(t.type==="plasma"){const chg=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||3000));drawPlasma(ctx,t.radius,ts,chg);}
  else if(t.type==="timewarp_tgt") drawTimewarpTarget(ctx,t.radius,ts);
  else if(t.type==="thunderclap") drawThunderclap(ctx,t.radius,ts);
  else if(t.type==="mirror_shard") drawMirrorShard(ctx,t.radius,ts,t.hitsLeft,t.maxHits);
  else if(t.type==="rune_stone"){const phase=Math.floor((Date.now()-t.spawnedAt)/1500)%8;drawRuneStone(ctx,t.radius,ts,phase);}
  else{
    const nm=t.rarity?.name;
    if     (nm==="common")    drawCommon(ctx,t.radius,t.color,t.glow,ts,timeLeft);
    else if(nm==="uncommon")  drawUncommon(ctx,t.radius,t.color,t.glow,ts,timeLeft);
    else if(nm==="rare")      drawRare(ctx,t.radius,t.color,t.glow,ts,timeLeft);
    else if(nm==="epic")      drawEpic(ctx,t.radius,t.color,t.glow,ts,timeLeft);
    else if(nm==="legendary") drawLegendary(ctx,t.radius,t.color,t.glow,ts,timeLeft);
  }
  // Perfect zone sweet-spot dot — subtle center glow on normal targets
  if(t.type==="normal"&&t.rarity&&t.rarity.name!=="common"){
    const tlNow=Math.max(0,1-(Date.now()-t.spawnedAt)/t.lifetime);
    const inPZ=tlNow>0.36&&tlNow<0.67;
    if(inPZ){
      ctx.save();ctx.globalAlpha=0.45+0.3*Math.abs(Math.sin(ts/90));
      ctx.fillStyle="#ffffff";ctx.shadowColor="#ffd700";ctx.shadowBlur=8;
      ctx.beginPath();ctx.arc(0,0,t.radius*0.22,0,Math.PI*2);ctx.fill();
      ctx.restore();
    }
  }
  // Colorblind rarity symbols
  if(_colorblindOn&&t.type==="normal"&&t.rarity){
    const symbols={common:"●",uncommon:"■",rare:"◆",epic:"★",legendary:"♛"};
    const sym=symbols[t.rarity.name]||"●";
    ctx.save();
    ctx.font=`bold ${Math.max(10,t.radius*0.65)}px sans-serif`;
    ctx.textAlign="center";ctx.textBaseline="middle";
    ctx.fillStyle="#ffffff";ctx.shadowColor="#000";ctx.shadowBlur=4;
    ctx.fillText(sym,0,0);
    ctx.restore();
  }
  // Timer ring
  if(t.type!=="bomb"&&t.type!=="boss"){
    const rr=t.radius+7;
    ctx.shadowBlur=0; ctx.strokeStyle=timeLeft<0.25?"#ef4444":t.color;
    ctx.lineWidth=3; ctx.globalAlpha=0.5*(ghostAlpha);
    ctx.beginPath(); ctx.arc(0,0,rr,-Math.PI/2,-Math.PI/2+2*Math.PI*timeLeft); ctx.stroke();
  }
  // Direction arrow for moving targets
  if(t.moving) drawArrow(ctx,t.vx,t.vy,t.radius,t.color);
  ctx.restore();
}

// ── Particle draw ──
function drawParticle(ctx,p,now){
  const age=now-p.born, life=Math.max(0,1-age/p.duration);
  ctx.save(); ctx.globalAlpha=life*(p.alpha||1);
  if(p.type==="shockwave"){
    ctx.strokeStyle=p.color; ctx.lineWidth=2*(1-life); ctx.shadowColor=p.color; ctx.shadowBlur=12;
    ctx.beginPath(); ctx.arc(p.x,p.y,(1-life)*65,0,Math.PI*2); ctx.stroke();
  } else if(p.type==="popup"){
    const yOff=age*0.065;
    ctx.fillStyle=p.color; ctx.shadowColor=p.color; ctx.shadowBlur=12;
    ctx.font=`bold ${p.size||14}px 'Segoe UI',sans-serif`;
    ctx.textAlign="center"; ctx.textBaseline="middle";
    ctx.fillText(p.text,p.x,p.y-yOff);
  } else if(p.type==="spark"){
    ctx.strokeStyle=p.color; ctx.lineWidth=Math.max(0.5,p.size*life); ctx.lineCap="round";
    ctx.shadowColor=p.color; ctx.shadowBlur=p.size*3;
    ctx.beginPath(); ctx.moveTo(p.x-p.vx*2,p.y-p.vy*2); ctx.lineTo(p.x,p.y); ctx.stroke();
  } else if(p.type==="arc"){
    // Lightning arc from (x,y) to (x2,y2) — chain reaction visual
    ctx.strokeStyle=p.color; ctx.lineWidth=Math.max(1,4*life); ctx.lineCap="round";
    ctx.shadowColor=p.color; ctx.shadowBlur=20;
    ctx.beginPath(); ctx.moveTo(p.x,p.y);
    const segments=6;
    for(let i=1;i<=segments;i++){
      const t=i/segments;
      const lx=p.x+(p.x2-p.x)*t+(Math.random()-0.5)*16*(1-Math.abs(t-0.5)*2);
      const ly=p.y+(p.y2-p.y)*t+(Math.random()-0.5)*16*(1-Math.abs(t-0.5)*2);
      ctx.lineTo(lx,ly);
    }
    ctx.stroke();
  } else if(p.type==="trail"){
    // Cross/star sparkle — perfect-tap trail effect
    const s=Math.max(0,p.size*life);
    ctx.strokeStyle=p.color; ctx.lineWidth=Math.max(0.5,s*0.6); ctx.lineCap="round";
    ctx.shadowColor=p.color; ctx.shadowBlur=s*3;
    ctx.beginPath();ctx.moveTo(p.x-s,p.y);ctx.lineTo(p.x+s,p.y);ctx.stroke();
    ctx.beginPath();ctx.moveTo(p.x,p.y-s);ctx.lineTo(p.x,p.y+s);ctx.stroke();
    ctx.beginPath();ctx.moveTo(p.x-s*0.7,p.y-s*0.7);ctx.lineTo(p.x+s*0.7,p.y+s*0.7);ctx.stroke();
    ctx.beginPath();ctx.moveTo(p.x+s*0.7,p.y-s*0.7);ctx.lineTo(p.x-s*0.7,p.y+s*0.7);ctx.stroke();
  } else {
    ctx.fillStyle=p.color; ctx.shadowColor=p.color; ctx.shadowBlur=p.size*2.5;
    const s=Math.max(0,p.size*life);
    ctx.beginPath(); ctx.arc(p.x,p.y,s,0,Math.PI*2); ctx.fill();
  }
  ctx.restore();
}

// ── World Foreground Silhouettes (drawn AFTER targets for depth) ──
function drawWorldForeground(ctx,w,h,worldId,ts){
  if(!worldId)return;
  const pulse=0.5+0.5*Math.sin(ts*0.0009);
  ctx.save();
  if(worldId===1){
    // Dragon's Lair: stalactites + lava seam
    ctx.fillStyle="rgba(35,4,0,0.88)";
    for(let i=0;i<9;i++){
      const sx=w*0.06+w*0.88*(i/8);
      const sh=18+Math.sin(i*1.9)*15+Math.sin(i*3.1)*8;
      const sw=10+Math.sin(i*2.4)*5;
      ctx.beginPath();ctx.moveTo(sx-sw,0);ctx.lineTo(sx+sw,0);ctx.lineTo(sx,sh);ctx.closePath();ctx.fill();
    }
    // Lava seam glow at bottom
    const lv=ctx.createLinearGradient(0,h-20,0,h);
    lv.addColorStop(0,"transparent");lv.addColorStop(1,`rgba(255,60,0,${0.35+pulse*0.18})`);
    ctx.fillStyle=lv;ctx.fillRect(0,h-20,w,20);
    ctx.strokeStyle=`rgba(255,100,0,${0.5+pulse*0.2})`;ctx.lineWidth=2.5;
    ctx.shadowColor="#ff4500";ctx.shadowBlur=22;
    ctx.beginPath();ctx.moveTo(0,h-3);ctx.lineTo(w,h-3);ctx.stroke();
    // Drip dots
    for(let i=0;i<5;i++){
      const dx=w*0.12+w*0.76*(i/4),dy=h-6-Math.abs(Math.sin(ts*0.0012+i*1.4))*9;
      ctx.fillStyle=`rgba(255,80,0,${0.7+pulse*0.2})`;
      ctx.beginPath();ctx.arc(dx,dy,2.5+Math.abs(Math.sin(ts*0.0018+i))*1.5,0,Math.PI*2);ctx.fill();
    }
  } else if(worldId===2){
    // Troll Forest: pine tree silhouettes on sides + ground fog
    const drawPine=(bx,base,h2,w2)=>{
      ctx.fillStyle="rgba(4,18,4,0.9)";
      ctx.beginPath();ctx.moveTo(bx,base);
      ctx.lineTo(bx-w2*0.5,base-h2*0.3);ctx.lineTo(bx-w2*0.3,base-h2*0.3);
      ctx.lineTo(bx-w2*0.42,base-h2*0.58);ctx.lineTo(bx-w2*0.22,base-h2*0.58);
      ctx.lineTo(bx-w2*0.3,base-h2*0.82);ctx.lineTo(bx,base-h2);
      ctx.lineTo(bx+w2*0.3,base-h2*0.82);ctx.lineTo(bx+w2*0.22,base-h2*0.58);
      ctx.lineTo(bx+w2*0.42,base-h2*0.58);ctx.lineTo(bx+w2*0.3,base-h2*0.3);
      ctx.lineTo(bx+w2*0.5,base-h2*0.3);ctx.closePath();ctx.fill();
    };
    drawPine(-10,h*0.95+5,h*0.58,72);drawPine(50,h*0.98+5,h*0.44,55);
    drawPine(w+10,h*0.95+5,h*0.6,72);drawPine(w-50,h*0.98+5,h*0.46,55);
    // Canopy top hint
    const cg=ctx.createLinearGradient(0,0,0,h*0.12);
    cg.addColorStop(0,"rgba(5,22,5,0.6)");cg.addColorStop(1,"transparent");
    ctx.fillStyle=cg;ctx.fillRect(0,0,w,h*0.12);
    // Ground fog
    const fg=ctx.createLinearGradient(0,h*0.78,0,h);
    fg.addColorStop(0,"transparent");fg.addColorStop(1,"rgba(8,28,8,0.65)");
    ctx.fillStyle=fg;ctx.fillRect(0,h*0.78,w,h*0.22);
  } else if(worldId===3){
    // Elven Kingdom: crystal pillar silhouettes at bottom corners + starlight arch
    const drawCrystal=(cx,base,cw2,ch2)=>{
      ctx.fillStyle=`rgba(200,168,75,${0.12+pulse*0.06})`;
      ctx.shadowColor="#c8a84b";ctx.shadowBlur=25;
      ctx.beginPath();ctx.moveTo(cx-cw2*0.28,base);ctx.lineTo(cx-cw2*0.5,base-ch2*0.8);
      ctx.lineTo(cx,base-ch2);ctx.lineTo(cx+cw2*0.5,base-ch2*0.8);
      ctx.lineTo(cx+cw2*0.28,base);ctx.closePath();ctx.fill();
      ctx.shadowBlur=0;
      // Crystal shine
      ctx.fillStyle=`rgba(255,240,180,${0.35+pulse*0.3})`;
      ctx.beginPath();ctx.arc(cx,base-ch2,3.5,0,Math.PI*2);ctx.fill();
    };
    drawCrystal(16,h,14,h*0.22);drawCrystal(44,h-8,10,h*0.16);
    drawCrystal(w-16,h,14,h*0.22);drawCrystal(w-44,h-8,10,h*0.16);
    // Golden arch at top
    ctx.strokeStyle=`rgba(200,168,75,${0.08+pulse*0.04})`;ctx.lineWidth=2;
    ctx.shadowColor="#c8a84b";ctx.shadowBlur=15;
    ctx.beginPath();ctx.arc(w/2,-h*0.3,w*0.65,0,Math.PI);ctx.stroke();
    ctx.shadowBlur=0;
  } else if(worldId===4){
    // Dark Wizard Tower: floating rune sigils + lightning at corners
    const runes=[[w*0.08,h*0.14],[w*0.9,h*0.22],[w*0.05,h*0.74],[w*0.94,h*0.68]];
    runes.forEach(([rx,ry],i)=>{
      const rr=16+i*3;
      ctx.save();ctx.translate(rx,ry);ctx.rotate(ts*0.00025*(i%2?1:-1));
      ctx.strokeStyle=`rgba(155,89,182,${0.22+pulse*0.1})`;
      ctx.lineWidth=1.5;ctx.setLineDash([3,4]);
      ctx.beginPath();ctx.arc(0,0,rr,0,Math.PI*2);ctx.stroke();
      ctx.setLineDash([]);
      for(let p=0;p<6;p++){
        const pa=(p/6)*Math.PI*2;
        ctx.fillStyle=`rgba(155,89,182,${0.45+pulse*0.3})`;
        ctx.beginPath();ctx.arc(Math.cos(pa)*rr,Math.sin(pa)*rr,2,0,Math.PI*2);ctx.fill();
      }
      ctx.restore();
    });
    if(Math.sin(ts*0.023)>0.88){
      ctx.strokeStyle="rgba(180,80,255,0.65)";ctx.lineWidth=1.5;ctx.shadowColor="#9b59b6";ctx.shadowBlur=18;
      ctx.beginPath();ctx.moveTo(8,0);ctx.lineTo(28,22);ctx.lineTo(14,38);ctx.lineTo(40,58);ctx.stroke();
      ctx.beginPath();ctx.moveTo(w-8,0);ctx.lineTo(w-28,22);ctx.lineTo(w-14,38);ctx.lineTo(w-40,58);ctx.stroke();
      ctx.shadowBlur=0;
    }
  } else if(worldId===5){
    // Viking Fjords: mountain silhouettes + aurora edge
    ctx.fillStyle="rgba(4,12,28,0.85)";
    ctx.beginPath();ctx.moveTo(0,h);ctx.lineTo(0,h*0.7);ctx.lineTo(w*0.07,h*0.55);
    ctx.lineTo(w*0.13,h*0.64);ctx.lineTo(w*0.2,h*0.49);ctx.lineTo(w*0.27,h*0.61);ctx.lineTo(w*0.32,h);ctx.closePath();ctx.fill();
    ctx.beginPath();ctx.moveTo(w,h);ctx.lineTo(w,h*0.7);ctx.lineTo(w*0.93,h*0.55);
    ctx.lineTo(w*0.87,h*0.64);ctx.lineTo(w*0.8,h*0.49);ctx.lineTo(w*0.73,h*0.61);ctx.lineTo(w*0.68,h);ctx.closePath();ctx.fill();
    // Snow caps
    ctx.fillStyle=`rgba(200,225,255,${0.32+pulse*0.12})`;
    [[w*0.07,h*0.55,13],[w*0.2,h*0.49,16],[w*0.93,h*0.55,13],[w*0.8,h*0.49,16]].forEach(([px,py,pr])=>{
      ctx.beginPath();ctx.arc(px,py,pr,Math.PI,0);ctx.fill();
    });
    // Aurora edge at top
    const au=ctx.createLinearGradient(0,0,w,h*0.18);
    au.addColorStop(0,"rgba(0,200,120,0)");au.addColorStop(0.4,`rgba(0,180,140,${0.06+pulse*0.04})`);au.addColorStop(0.7,"rgba(0,150,255,0.04)");au.addColorStop(1,"transparent");
    ctx.fillStyle=au;ctx.fillRect(0,0,w,h*0.18);
  } else if(worldId===6){
    // Goblin Mines: mine frame timbers
    const bc="rgba(55,35,8,0.9)";ctx.fillStyle=bc;
    ctx.fillRect(0,h*0.08,18,h*0.84);ctx.fillRect(w-18,h*0.08,18,h*0.84);
    ctx.fillRect(0,h*0.08,w,18);ctx.fillRect(0,h*0.9,w,18);
    [0.28,0.54,0.77].forEach(f=>{
      ctx.fillRect(0,h*f-8,38,16);ctx.fillRect(w-38,h*f-8,38,16);
    });
    // Gold ore glints in walls
    ctx.shadowColor="#ffd700";ctx.shadowBlur=14;
    [[10,h*0.25],[10,h*0.52],[10,h*0.76],[w-10,h*0.3],[w-10,h*0.6],[w-10,h*0.78]].forEach(([gx,gy])=>{
      ctx.fillStyle=`rgba(255,210,0,${0.4+Math.abs(Math.sin(ts*0.002+gx))*0.4})`;
      ctx.beginPath();ctx.arc(gx,gy,3,0,Math.PI*2);ctx.fill();
    });
    ctx.shadowBlur=0;
  } else if(worldId===7){
    // Undead Catacombs: tombstones + bone arch at top
    const drawTomb=(tx,ty,tw2,th2)=>{
      ctx.fillStyle="rgba(12,22,12,0.92)";
      ctx.beginPath();ctx.moveTo(tx-tw2/2,ty+th2);ctx.lineTo(tx-tw2/2,ty+th2*0.3);
      ctx.arc(tx,ty,tw2/2,Math.PI,0);ctx.lineTo(tx+tw2/2,ty+th2);ctx.closePath();ctx.fill();
      // RIP text
      ctx.fillStyle=`rgba(100,150,100,${0.3+pulse*0.15})`;
      ctx.font=`bold ${tw2*0.28}px monospace`;ctx.textAlign="center";ctx.textBaseline="middle";
      ctx.fillText("R.I.P",tx,ty+th2*0.6);
    };
    drawTomb(w*0.07,h-55,32,58);drawTomb(w*0.18,h-50,26,50);
    drawTomb(w*0.82,h-55,28,52);drawTomb(w*0.93,h-50,30,56);
    // Bone arch at top
    ctx.strokeStyle=`rgba(90,130,90,${0.15+pulse*0.07})`;ctx.lineWidth=14;ctx.lineCap="round";
    ctx.beginPath();ctx.arc(w/2,-h*0.25,w*0.6,0,Math.PI);ctx.stroke();
    // Miasma on ground
    const mg=ctx.createLinearGradient(0,h*0.8,0,h);
    mg.addColorStop(0,"transparent");mg.addColorStop(1,"rgba(15,45,15,0.6)");
    ctx.fillStyle=mg;ctx.fillRect(0,h*0.8,w,h*0.2);
  } else if(worldId===8){
    // Sea Serpent's Deep: seaweed + water surface shimmer
    for(let i=0;i<7;i++){
      const sx=w*0.05+w*0.9*(i/6);
      const sh=50+Math.sin(i*1.8)*22;
      ctx.strokeStyle=`rgba(0,110,85,0.72)`;ctx.lineWidth=7+Math.sin(i*1.3)*3;ctx.lineCap="round";
      ctx.shadowColor="#1abc9c";ctx.shadowBlur=6;
      ctx.beginPath();ctx.moveTo(sx,h);
      for(let j=5;j>=0;j--){
        const t=j/5,sway=Math.sin(ts*0.0009+i*0.9+t*Math.PI)*9;
        ctx.lineTo(sx+sway,h-sh*t);
      }
      ctx.stroke();
      ctx.shadowBlur=0;
    }
    // Water caustic flicker at top
    ctx.strokeStyle=`rgba(26,188,156,${0.18+pulse*0.1})`;ctx.lineWidth=2;
    ctx.beginPath();
    for(let x=0;x<=w;x+=18){
      const y=3+Math.sin(x*0.045+ts*0.0007)*4;
      x===0?ctx.moveTo(x,y):ctx.lineTo(x,y);
    }
    ctx.stroke();
    // Bubble highlights top
    const bs=ctx.createLinearGradient(0,0,0,h*0.08);
    bs.addColorStop(0,`rgba(26,188,156,${0.1+pulse*0.05})`);bs.addColorStop(1,"transparent");
    ctx.fillStyle=bs;ctx.fillRect(0,0,w,h*0.08);
  } else if(worldId===9){
    // Giant's Peak: rocky peaks + lightning flash
    ctx.fillStyle="rgba(18,22,32,0.88)";
    ctx.beginPath();ctx.moveTo(0,h);ctx.lineTo(0,h*0.76);ctx.lineTo(w*0.06,h*0.6);
    ctx.lineTo(w*0.12,h*0.7);ctx.lineTo(w*0.19,h*0.53);ctx.lineTo(w*0.25,h*0.66);ctx.lineTo(w*0.3,h);ctx.closePath();ctx.fill();
    ctx.beginPath();ctx.moveTo(w,h);ctx.lineTo(w,h*0.76);ctx.lineTo(w*0.94,h*0.6);
    ctx.lineTo(w*0.88,h*0.7);ctx.lineTo(w*0.81,h*0.53);ctx.lineTo(w*0.75,h*0.66);ctx.lineTo(w*0.7,h);ctx.closePath();ctx.fill();
    ctx.fillStyle=`rgba(215,228,255,${0.28+pulse*0.1})`;
    [[w*0.06,h*0.6,14],[w*0.19,h*0.53,17],[w*0.94,h*0.6,14],[w*0.81,h*0.53,17]].forEach(([px,py,pr])=>{
      ctx.beginPath();ctx.arc(px,py,pr,Math.PI,0);ctx.fill();
    });
    if(Math.sin(ts*0.019)>0.9){
      ctx.strokeStyle="rgba(210,225,255,0.85)";ctx.lineWidth=2.5;ctx.shadowColor="#dce4ff";ctx.shadowBlur=25;
      const lx=w*0.5+Math.sin(ts*0.003)*w*0.18;
      ctx.beginPath();ctx.moveTo(lx,0);ctx.lineTo(lx-12,h*0.28);ctx.lineTo(lx+6,h*0.28);ctx.lineTo(lx-16,h*0.56);ctx.stroke();
      ctx.shadowBlur=0;
    }
  } else if(worldId===10){
    // Ancient Dragon God: temple pillars + sacred altar
    const drawPillar=(cx,y0,cw2,ch2)=>{
      ctx.fillStyle=`rgba(170,120,8,${0.28+pulse*0.08})`;
      ctx.shadowColor="#ffd700";ctx.shadowBlur=22;
      ctx.fillRect(cx-cw2*0.75,y0,cw2*1.5,ch2*0.06);
      ctx.fillRect(cx-cw2*0.5,y0+ch2*0.06,cw2,ch2*0.88);
      ctx.fillRect(cx-cw2*0.75,y0+ch2*0.94,cw2*1.5,ch2*0.06);
      ctx.shadowBlur=0;
      ctx.strokeStyle="rgba(255,200,50,0.12)";ctx.lineWidth=1;
      for(let li=0;li<3;li++){
        ctx.beginPath();ctx.moveTo(cx-cw2*0.3+li*cw2*0.3,y0+ch2*0.07);
        ctx.lineTo(cx-cw2*0.3+li*cw2*0.3,y0+ch2*0.93);ctx.stroke();
      }
    };
    drawPillar(12,h*0.04,22,h*0.96);drawPillar(w-12,h*0.04,22,h*0.96);
    // Sacred altar glow at bottom
    const ag=ctx.createLinearGradient(w*0.3,h-10,w*0.7,h-10);
    ag.addColorStop(0,"transparent");ag.addColorStop(0.5,`rgba(255,200,0,${0.3+pulse*0.14})`);ag.addColorStop(1,"transparent");
    ctx.fillStyle=ag;ctx.fillRect(w*0.3,h-10,w*0.4,10);
    // Sacred glow pillar
    const sp=ctx.createLinearGradient(w*0.42,0,w*0.58,0);
    sp.addColorStop(0,"transparent");sp.addColorStop(0.5,`rgba(255,200,0,${0.04+pulse*0.03})`);sp.addColorStop(1,"transparent");
    ctx.fillStyle=sp;ctx.fillRect(w*0.42,0,w*0.16,h);
  }
  ctx.restore();
}

// ── World Weather Particles (deterministic, no state needed) ──
function drawWeatherParticles(ctx,w,h,worldId,ts){
  if(!worldId)return;
  const N=26;const gr=2.39996;
  ctx.save();
  if(worldId===1){
    // Dragon's Lair: fire embers rising
    for(let i=0;i<N;i++){
      const spd=0.000038+(i%5)*0.000014;
      const t=((ts*spd+i*0.14)%1+1)%1;
      const x=((Math.sin(i*gr)*0.5+0.5)*w*0.88+w*0.06+Math.sin(ts*0.0008+i*0.7)*20)%w;
      const y=h-(t*(h*1.08))-20;
      const fade=t<0.18?t/0.18:t>0.82?(1-t)/0.18:1;
      const flicker=0.55+0.45*Math.sin(ts*0.013+i*1.3);
      ctx.globalAlpha=fade*0.52*flicker;
      ctx.fillStyle=i%3===0?"#ff6600":i%3===1?"#ffaa00":"#ff3300";
      ctx.shadowColor=ctx.fillStyle;ctx.shadowBlur=7;
      ctx.beginPath();ctx.arc(x,y,1.4+(i%4)*0.6,0,Math.PI*2);ctx.fill();
    }
  } else if(worldId===2){
    // Troll Forest: leaves + fireflies
    for(let i=0;i<N;i++){
      const isFirefly=i%7===0;
      if(isFirefly){
        const fx=(Math.sin(i*gr+ts*0.00028)*0.45+0.5)*w;
        const fy=(Math.cos(i*gr*1.3+ts*0.00022)*0.38+0.5)*h;
        const glow=0.4+0.6*Math.sin(ts*0.005+i*2.1);
        ctx.globalAlpha=glow*0.55;ctx.fillStyle="#ccff44";
        ctx.shadowColor="#88ff00";ctx.shadowBlur=10;
        ctx.beginPath();ctx.arc(fx,fy,1.4,0,Math.PI*2);ctx.fill();
      } else {
        const spd=0.000017+(i%6)*0.0000055;
        const t=((ts*spd+i*0.13)%1+1)%1;
        const x=((Math.sin(i*gr*2)*0.5+0.5)*w+Math.sin(ts*0.0006+i*0.9)*28)%w;
        const y=t*(h+18)-9;
        const fade=t<0.1?t*10:t>0.9?(1-t)*10:0.7;
        ctx.globalAlpha=fade*0.48;ctx.shadowBlur=0;
        ctx.save();ctx.translate(x,y);ctx.rotate(ts*0.002+i*0.8);
        ctx.fillStyle=i%3===0?"#2d5a1b":i%3===1?"#1a3d0e":"#3a6e22";
        ctx.beginPath();ctx.ellipse(0,0,3.5,1.5,0,0,Math.PI*2);ctx.fill();
        ctx.restore();
      }
    }
  } else if(worldId===3){
    // Elven Kingdom: golden sparkle dust drifting up
    for(let i=0;i<N;i++){
      const spd=0.000028+(i%4)*0.00001;
      const t=((ts*spd+i*0.11)%1+1)%1;
      const x=(Math.sin(i*gr+ts*0.00018)*0.45+0.5)*w+Math.sin(ts*0.0009+i*0.6)*14;
      const y=h-(t*h*1.04)+Math.sin(ts*0.0009+i*0.55)*13;
      const sparkle=0.5+0.5*Math.sin(ts*0.009+i*1.7);
      const fade=t<0.15?t/0.15:t>0.85?(1-t)/0.15:1;
      ctx.globalAlpha=fade*sparkle*0.55;
      ctx.fillStyle=i%2===0?"#ffd700":"#ffe44d";
      ctx.shadowColor="#ffd700";ctx.shadowBlur=8;
      ctx.beginPath();ctx.arc(x,y,0.9+sparkle*1.4,0,Math.PI*2);ctx.fill();
    }
  } else if(worldId===4){
    // Dark Wizard Tower: purple shadow wisps drifting
    for(let i=0;i<20;i++){
      const ox=(Math.sin(i*gr)*0.5+0.5)*w;
      const oy=(Math.cos(i*gr*1.4)*0.5+0.5)*h;
      const wx=ox+Math.sin(ts*0.00035+i*0.8)*44;
      const wy=oy+Math.cos(ts*0.00028+i*1.1)*32;
      const pulse=0.5+0.5*Math.sin(ts*0.003+i*2.2);
      ctx.globalAlpha=pulse*0.17;
      ctx.fillStyle=i%3===0?"#9b59b6":i%3===1?"#6c3483":"#7d3c98";
      ctx.shadowColor="#9b59b6";ctx.shadowBlur=14;
      ctx.beginPath();ctx.arc(wx,wy,5+pulse*8,0,Math.PI*2);ctx.fill();
    }
  } else if(worldId===5){
    // Viking Fjords: snowflakes drifting down
    for(let i=0;i<N;i++){
      const spd=0.000023+(i%5)*0.00001;
      const t=((ts*spd+i*0.12)%1+1)%1;
      const drift=Math.sin(ts*0.0005+i*0.7)*22;
      const x=((Math.sin(i*gr*2.1)*0.5+0.5)*w+drift)%w;
      const y=t*(h+14)-7;
      const fade=t<0.07?t/0.07:t>0.93?(1-t)/0.07:1;
      ctx.globalAlpha=fade*0.42;
      ctx.fillStyle="#c8e6ff";ctx.shadowColor="#a8c8ff";ctx.shadowBlur=4;
      ctx.beginPath();ctx.arc(x,y,0.9+(i%3)*0.7,0,Math.PI*2);ctx.fill();
    }
  } else if(worldId===6){
    // Goblin Mines: coal dust + gold sparks
    for(let i=0;i<N;i++){
      const spd=0.000028+(i%6)*0.000011;
      const t=((ts*spd+i*0.1)%1+1)%1;
      const x=((Math.sin(i*gr)*0.5+0.5)*w+Math.sin(ts*0.001+i*0.5)*11)%w;
      const y=t*(h+10)-5;
      const isSpark=i%6===0;
      const fade=t<0.1?t*10:t>0.9?(1-t)*10:0.6;
      if(isSpark){
        ctx.globalAlpha=fade*Math.abs(Math.sin(ts*0.018+i*3));
        ctx.fillStyle="#ffd700";ctx.shadowColor="#ff8800";ctx.shadowBlur=8;
        ctx.beginPath();ctx.arc(x,y,1.2,0,Math.PI*2);ctx.fill();
      } else {
        ctx.globalAlpha=fade*0.32;ctx.shadowBlur=0;
        ctx.fillStyle="#443322";
        ctx.beginPath();ctx.arc(x,y,1.4+(i%3)*0.5,0,Math.PI*2);ctx.fill();
      }
    }
  } else if(worldId===7){
    // Undead Catacombs: ghostly orbs drifting sideways
    for(let i=0;i<18;i++){
      const ox=(Math.sin(i*gr*1.5)*0.5+0.5)*w;
      const oy=60+(Math.cos(i*gr)*0.5+0.5)*(h-130);
      const gx=(ox+ts*0.024*(i%2?1:-1)+i*44)%(w+44)-22;
      const gy=oy+Math.sin(ts*0.0008+i*0.9)*20;
      const glow=0.4+0.6*Math.sin(ts*0.0045+i*1.5);
      ctx.globalAlpha=glow*0.2;
      ctx.fillStyle=i%3===0?"#88bb88":"#aaddaa";
      ctx.shadowColor="#66aa66";ctx.shadowBlur=16;
      ctx.beginPath();ctx.arc(gx,gy,5+glow*7,0,Math.PI*2);ctx.fill();
    }
  } else if(worldId===8){
    // Sea Serpent's Deep: bubbles rising
    for(let i=0;i<N;i++){
      const spd=0.00002+(i%5)*0.000009;
      const t=((ts*spd+i*0.115)%1+1)%1;
      const x=((Math.sin(i*gr*1.8)*0.5+0.5)*w*0.88+w*0.06+Math.sin(ts*0.0007+i*0.6)*14);
      const y=h-(t*(h+18))-9;
      const fade=t<0.09?t/0.09:t>0.91?(1-t)/0.09:0.5;
      const r=1.4+(i%4)*0.75;
      ctx.globalAlpha=fade*0.38;
      ctx.strokeStyle="#1abc9c";ctx.lineWidth=0.75;ctx.shadowColor="#1abc9c";ctx.shadowBlur=4;
      ctx.fillStyle="rgba(26,188,156,0.08)";
      ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill();ctx.stroke();
    }
  } else if(worldId===9){
    // Giant's Peak: sleet streaks at sharp angle
    for(let i=0;i<N;i++){
      const spd=0.000038+(i%4)*0.000014;
      const t=((ts*spd+i*0.12)%1+1)%1;
      const x=((Math.sin(i*gr*1.9)*0.5+0.5)*w+t*w*0.32)%w;
      const y=t*(h+20)-10;
      const fade=t<0.06?t/0.06:t>0.94?(1-t)/0.06:0.6;
      ctx.globalAlpha=fade*0.48;ctx.shadowBlur=0;
      ctx.strokeStyle="#dce8ff";ctx.lineWidth=0.9;
      ctx.beginPath();ctx.moveTo(x,y);ctx.lineTo(x+4,y+8);ctx.stroke();
    }
  } else if(worldId===10){
    // Ancient Dragon God: divine golden motes rising with cross-sparkle
    for(let i=0;i<N;i++){
      const spd=0.000024+(i%5)*0.0000095;
      const t=((ts*spd+i*0.11)%1+1)%1;
      const x=(Math.sin(i*gr+ts*0.00014)*0.45+0.5)*w+Math.sin(ts*0.0006+i*0.8)*18;
      const y=h-(t*h*1.04);
      const glow=0.5+0.5*Math.sin(ts*0.007+i*2.1);
      const fade=(t<0.09?t/0.09:t>0.91?(1-t)/0.09:1)*glow*0.52;
      ctx.globalAlpha=fade;
      ctx.fillStyle=i%2===0?"#ffd700":"#ffcc00";
      ctx.shadowColor="#ffaa00";ctx.shadowBlur=10;
      ctx.beginPath();ctx.arc(x,y,0.9+glow*1.7,0,Math.PI*2);ctx.fill();
      if(glow>0.85){
        // Tiny cross sparkle at peak brightness
        ctx.globalAlpha=fade*0.6;ctx.strokeStyle="#fff8cc";ctx.lineWidth=0.7;
        ctx.beginPath();ctx.moveTo(x-3,y);ctx.lineTo(x+3,y);ctx.stroke();
        ctx.beginPath();ctx.moveTo(x,y-3);ctx.lineTo(x,y+3);ctx.stroke();
      }
    }
  }
  ctx.restore();
}

// ── Depth vignette ──
function drawVignette(ctx,w,h,worldColor){
  const vg=ctx.createRadialGradient(w/2,h/2,w*0.28,w/2,h/2,w*0.9);
  vg.addColorStop(0,"transparent");
  vg.addColorStop(1,"rgba(0,0,0,0.72)");
  ctx.fillStyle=vg;ctx.fillRect(0,0,w,h);
  if(worldColor){
    const eg=ctx.createRadialGradient(w/2,h/2,w*0.55,w/2,h/2,w*1.05);
    eg.addColorStop(0,"transparent");
    eg.addColorStop(1,worldColor+"18");
    ctx.fillStyle=eg;ctx.fillRect(0,0,w,h);
  }
}

// ── Background ──
function drawBg(ctx,w,h,accent,gridColor,ts,fever,worldId=0){
  ctx.clearRect(0,0,w,h);
  // Fill with world background color
  if(worldId>0){ctx.fillStyle=WORLDS[worldId-1].bg;ctx.fillRect(0,0,w,h);}
  const pulse=0.5+0.5*Math.sin(ts/1500);

  // ── World-specific atmospheric overlays ──
  if(worldId===1){
    // Dragon's Lair: lava glow rising from bottom, flickering heat
    const g=ctx.createLinearGradient(0,h*0.55,0,h);
    g.addColorStop(0,"transparent");
    g.addColorStop(1,`rgba(200,40,0,${0.15+pulse*0.08})`);
    ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
    // Side lava veins glow
    const gl=ctx.createLinearGradient(0,0,w*0.25,0);
    gl.addColorStop(0,`rgba(255,60,0,${0.07+pulse*0.04})`); gl.addColorStop(1,"transparent");
    ctx.fillStyle=gl; ctx.fillRect(0,0,w,h);
  } else if(worldId===2){
    // Troll Forest: mist settling from top, deep green murk
    const g=ctx.createLinearGradient(0,0,0,h*0.45);
    g.addColorStop(0,`rgba(10,40,10,${0.18+pulse*0.06})`); g.addColorStop(1,"transparent");
    ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
    const g2=ctx.createLinearGradient(0,h*0.7,0,h);
    g2.addColorStop(0,"transparent"); g2.addColorStop(1,`rgba(5,30,5,${0.14+pulse*0.04})`);
    ctx.fillStyle=g2; ctx.fillRect(0,0,w,h);
  } else if(worldId===3){
    // Elven Kingdom: magical radial shimmer
    const g=ctx.createRadialGradient(w/2,h/2,0,w/2,h/2,w*0.65);
    g.addColorStop(0,`rgba(200,168,75,${0.05+pulse*0.04})`); g.addColorStop(1,"transparent");
    ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
    // Silver edge glow
    const g2=ctx.createRadialGradient(w/2,h/2,w*0.3,w/2,h/2,w*0.8);
    g2.addColorStop(0,"transparent"); g2.addColorStop(1,`rgba(180,220,255,${0.04+pulse*0.02})`);
    ctx.fillStyle=g2; ctx.fillRect(0,0,w,h);
  } else if(worldId===4){
    // Dark Wizard Tower: arcane vortex glow from center
    const g=ctx.createRadialGradient(w/2,h/2,0,w/2,h/2,w*0.55);
    g.addColorStop(0,`rgba(100,20,160,${0.10+pulse*0.06})`); g.addColorStop(1,"transparent");
    ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
    // Lightning flicker at top
    if(Math.sin(ts*0.03)>0.85){
      ctx.fillStyle=`rgba(150,50,255,${0.06})`; ctx.fillRect(0,0,w,h*0.3);
    }
  } else if(worldId===5){
    // Viking Fjords: aurora borealis shimmer + ice haze
    const g=ctx.createLinearGradient(0,0,0,h*0.5);
    g.addColorStop(0,`rgba(0,60,100,${0.16+pulse*0.06})`);
    g.addColorStop(0.5,`rgba(0,80,60,${0.08+pulse*0.03})`);
    g.addColorStop(1,"transparent");
    ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
    // Aurora band
    ctx.save(); ctx.globalAlpha=0.05+Math.abs(Math.sin(ts*0.0005))*0.05;
    const ga=ctx.createLinearGradient(0,h*0.05,w,h*0.2);
    ga.addColorStop(0,"rgba(0,200,100,0)"); ga.addColorStop(0.5,"rgba(0,200,150,1)"); ga.addColorStop(1,"rgba(0,150,255,0)");
    ctx.fillStyle=ga; ctx.fillRect(0,h*0.05,w,h*0.15); ctx.restore();
  } else if(worldId===6){
    // Goblin Mines: golden ore glow from below, dusty cave
    const g=ctx.createRadialGradient(w/2,h,0,w/2,h,w*0.8);
    g.addColorStop(0,`rgba(180,130,0,${0.12+pulse*0.06})`); g.addColorStop(1,"transparent");
    ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
    // Rocky walls darkening sides
    const gl=ctx.createLinearGradient(0,0,w*0.2,0);
    gl.addColorStop(0,`rgba(30,20,0,0.22)`); gl.addColorStop(1,"transparent");
    ctx.fillStyle=gl; ctx.fillRect(0,0,w,h);
    const gr=ctx.createLinearGradient(w,0,w*0.8,0);
    gr.addColorStop(0,`rgba(30,20,0,0.22)`); gr.addColorStop(1,"transparent");
    ctx.fillStyle=gr; ctx.fillRect(0,0,w,h);
  } else if(worldId===7){
    // Undead Catacombs: sickly green miasma on the ground, bone-cold
    const g=ctx.createLinearGradient(0,h*0.65,0,h);
    g.addColorStop(0,"transparent"); g.addColorStop(1,`rgba(20,60,15,${0.18+pulse*0.06})`);
    ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
    const g2=ctx.createLinearGradient(0,0,0,h*0.3);
    g2.addColorStop(0,`rgba(5,10,5,0.25)`); g2.addColorStop(1,"transparent");
    ctx.fillStyle=g2; ctx.fillRect(0,0,w,h);
  } else if(worldId===8){
    // Sea Serpent's Deep: bioluminescent depth gradient, caustic shimmer
    const g=ctx.createLinearGradient(0,0,0,h);
    g.addColorStop(0,`rgba(0,40,60,${0.12+pulse*0.04})`);
    g.addColorStop(1,`rgba(0,80,70,${0.18+pulse*0.06})`);
    ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
    // Caustic light patches
    ctx.save(); ctx.globalAlpha=0.03+Math.abs(Math.sin(ts*0.0008))*0.03;
    for(let i=0;i<3;i++){
      const gci=ctx.createRadialGradient((w*0.2+i*w*0.3),h*0.4,0,(w*0.2+i*w*0.3),h*0.4,w*0.25);
      gci.addColorStop(0,"rgba(26,188,156,0.6)"); gci.addColorStop(1,"transparent");
      ctx.fillStyle=gci; ctx.fillRect(0,0,w,h);
    }
    ctx.restore();
  } else if(worldId===9){
    // Giant's Peak: stormy sky from top, icy mountain base
    const g=ctx.createLinearGradient(0,0,0,h*0.6);
    g.addColorStop(0,`rgba(30,40,60,${0.20+pulse*0.06})`);
    g.addColorStop(1,"transparent");
    ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
    // Lightning flash
    if(Math.sin(ts*0.017)>0.92){
      ctx.fillStyle=`rgba(176,184,193,0.08)`; ctx.fillRect(0,0,w,h);
    }
  } else if(worldId===10){
    // Ancient Dragon God: divine golden radiance, sacred fire
    const g=ctx.createRadialGradient(w/2,h/2,0,w/2,h/2,w*0.7);
    g.addColorStop(0,`rgba(200,150,0,${0.08+pulse*0.05})`); g.addColorStop(1,"transparent");
    ctx.fillStyle=g; ctx.fillRect(0,0,w,h);
    const g2=ctx.createLinearGradient(0,h*0.6,0,h);
    g2.addColorStop(0,"transparent"); g2.addColorStop(1,`rgba(180,80,0,${0.12+pulse*0.06})`);
    ctx.fillStyle=g2; ctx.fillRect(0,0,w,h);
    // Sacred pillar glow
    ctx.save(); ctx.globalAlpha=0.04+pulse*0.03;
    const gp=ctx.createLinearGradient(w*0.35,0,w*0.65,0);
    gp.addColorStop(0,"transparent"); gp.addColorStop(0.5,"rgba(255,215,0,0.8)"); gp.addColorStop(1,"transparent");
    ctx.fillStyle=gp; ctx.fillRect(w*0.35,0,w*0.3,h); ctx.restore();
  }

  // ── Grid (world-specific style) ──
  const gsize = worldId===6 ? 36 : worldId===8 ? 52 : 44;
  ctx.strokeStyle=gridColor; ctx.lineWidth=0.7;
  if(worldId===8){
    // Ocean: undulating wave lines
    for(let y=0;y<h;y+=gsize){
      ctx.beginPath();
      for(let x=0;x<=w;x+=10){
        const wave=Math.sin((x*0.03)+(ts*0.0006))*7;
        x===0?ctx.moveTo(x,y+wave):ctx.lineTo(x,y+wave);
      }
      ctx.stroke();
    }
    for(let x=0;x<w;x+=gsize){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,h);ctx.stroke();}
  } else if(worldId===2){
    // Forest: slightly diagonal grid for organic feel
    for(let x=-h;x<w+h;x+=gsize){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x+h*0.08,h);ctx.stroke();}
    for(let y=0;y<h;y+=gsize){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke();}
  } else {
    for(let x=0;x<w;x+=gsize){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,h);ctx.stroke();}
    for(let y=0;y<h;y+=gsize){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke();}
  }

  // ── Accent lines ──
  ctx.strokeStyle=accent; ctx.globalAlpha=(0.07+pulse*0.05)*(fever?3:1); ctx.lineWidth=1.2;
  for(let x=0;x<w;x+=gsize*5){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,h);ctx.stroke();}
  for(let y=0;y<h;y+=gsize*5){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke();}
  ctx.globalAlpha=1;
}

function drawBgParticles(ctx,parts,accent,fever,worldId=0,speedMult=1){
  const cw=ctx.canvas.width,ch=ctx.canvas.height;
  const sm=speedMult*(fever?1.8:1); // fever already boosted alpha below
  // Boss kill blast: repel bg particles from explosion point for ~700ms
  const blastAge=_bgBlast?(performance.now()-_bgBlast.at):99999;
  const blastActive=blastAge<700;
  if(blastActive&&blastAge>700)_bgBlast=null;
  parts.forEach(p=>{
    if(blastActive){
      const force=Math.max(0,1-blastAge/700)*14;
      const dx=p.x-_bgBlast.x,dy=p.y-_bgBlast.y;
      const dist=Math.max(Math.hypot(dx,dy),1);
      p.x+=(dx/dist)*force;p.y+=(dy/dist)*force;
    }
    const wid=p.worldId||worldId;
    // World-specific movement
    if(wid===1){
      p.x+=(p.vx+Math.sin(p.phase+(performance.now()*0.001))*0.3)*sm;
      p.y+=p.vy*sm;
      if(p.y<-10){p.y=ch+10;p.x=Math.random()*cw;}
      if(p.x<0)p.x=cw;if(p.x>cw)p.x=0;
    } else if(wid===2){
      p.x+=(p.vx+Math.sin(p.phase+(performance.now()*0.0008))*0.4)*sm;
      p.y+=Math.abs(p.vy)*0.8*sm;
      p.angle=(p.angle||0)+0.02*sm;
      if(p.y>ch+10){p.y=-10;p.x=Math.random()*cw;}
      if(p.x<0)p.x=cw;if(p.x>cw)p.x=0;
    } else if(wid===5||wid===9){
      p.x+=Math.sin(p.phase+(performance.now()*0.0006))*0.5*sm;
      p.y+=(Math.abs(p.vy)*0.6+0.3)*sm;
      if(p.y>ch+10){p.y=-10;p.x=Math.random()*cw;}
      if(p.x<0)p.x=cw;if(p.x>cw)p.x=0;
    } else if(wid===8){
      p.x+=Math.sin(p.phase+(performance.now()*0.001))*0.4*sm;
      p.y-=(Math.abs(p.vy)*0.5+0.2)*sm;
      if(p.y<-10){p.y=ch+10;p.x=Math.random()*cw;}
    } else if(wid===7){
      p.x+=Math.sin(p.phase+(performance.now()*0.0007))*0.8*sm;
      p.y+=Math.cos(p.phase+(performance.now()*0.0005))*0.5*sm;
      if(p.x<0)p.x=cw;if(p.x>cw)p.x=0;if(p.y<0)p.y=ch;if(p.y>ch)p.y=0;
    } else {
      p.x+=p.vx*sm; p.y+=p.vy*sm;
      if(p.x<0)p.x=cw;if(p.x>cw)p.x=0;if(p.y<0)p.y=ch;if(p.y>ch)p.y=0;
    }

    ctx.save();
    ctx.globalAlpha=p.alpha*(fever?1.8:1);
    const pcolor=fever?"#ffd700":(p.color||accent);
    ctx.fillStyle=pcolor; ctx.shadowColor=pcolor; ctx.shadowBlur=p.r*4;

    if(wid===8){
      // Bubbles: hollow circle
      ctx.globalAlpha*=0.6;
      ctx.strokeStyle=pcolor; ctx.lineWidth=0.8; ctx.shadowBlur=p.r*2;
      ctx.beginPath(); ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.stroke();
    } else if(wid===2){
      // Leaves: small elongated shape
      ctx.save(); ctx.translate(p.x,p.y); ctx.rotate(p.angle||0);
      ctx.fillStyle=pcolor; ctx.shadowBlur=p.r*2;
      ctx.beginPath();
      ctx.ellipse(0,0,p.r*1.6,p.r*0.7,0,0,Math.PI*2);
      ctx.fill(); ctx.restore();
    } else if(wid===5||wid===9){
      // Snowflakes: 6-pointed star
      ctx.save(); ctx.translate(p.x,p.y); ctx.rotate((performance.now()*0.0005*(p.phase||1)));
      ctx.strokeStyle=pcolor; ctx.lineWidth=0.9; ctx.shadowBlur=p.r*3;
      for(let i=0;i<6;i++){
        ctx.beginPath(); ctx.moveTo(0,0);
        ctx.lineTo(Math.cos(i*Math.PI/3)*p.r*1.5,Math.sin(i*Math.PI/3)*p.r*1.5);
        ctx.stroke();
      }
      ctx.restore();
    } else if(wid===3){
      // Sparkles: cross/star flash
      ctx.save(); ctx.translate(p.x,p.y);
      ctx.strokeStyle=pcolor; ctx.lineWidth=0.8; ctx.shadowBlur=p.r*5;
      const sz=p.r*(1+0.3*Math.sin(performance.now()*0.004+p.phase));
      ctx.beginPath(); ctx.moveTo(-sz,0); ctx.lineTo(sz,0); ctx.stroke();
      ctx.beginPath(); ctx.moveTo(0,-sz); ctx.lineTo(0,sz); ctx.stroke();
      ctx.beginPath(); ctx.moveTo(-sz*0.7,-sz*0.7); ctx.lineTo(sz*0.7,sz*0.7); ctx.stroke();
      ctx.beginPath(); ctx.moveTo(sz*0.7,-sz*0.7); ctx.lineTo(-sz*0.7,sz*0.7); ctx.stroke();
      ctx.restore();
    } else if(wid===1){
      // Embers: tiny bright dot with flicker
      const flickAlpha=0.4+0.6*Math.abs(Math.sin(performance.now()*0.006+p.phase));
      ctx.globalAlpha*=flickAlpha;
      ctx.beginPath(); ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.fill();
    } else {
      ctx.beginPath(); ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.fill();
    }
    ctx.restore();
  });
}

function drawRipples(ctx,ripples){
  for(let i=ripples.length-1;i>=0;i--){
    const rp=ripples[i]; rp.r+=4; rp.alpha-=0.048;
    if(rp.alpha<=0){ripples.splice(i,1);continue;}
    ctx.save(); ctx.globalAlpha=rp.alpha; ctx.strokeStyle=rp.color;
    ctx.lineWidth=2.2; ctx.shadowColor=rp.color; ctx.shadowBlur=10;
    ctx.beginPath(); ctx.arc(rp.x,rp.y,rp.r,0,Math.PI*2); ctx.stroke(); ctx.restore();
  }
}

function drawTapTrail(ctx,trail,themeAccent,streak){
  const now=performance.now();
  // Combo intensity: higher streak = thicker, brighter, longer trail
  const comboLevel=Math.min(10,Math.floor((streak||0)/5));
  const trailDur=350+comboLevel*40; // up to 750ms at max combo
  const baseAlpha=0.45+comboLevel*0.04;
  const baseSz=2.5+comboLevel*0.5;
  for(let i=trail.length-1;i>=0;i--){
    const p=trail[i];
    const age=now-p.ts;
    if(age>trailDur){trail.splice(i,1);continue;}
    const ratio=1-age/trailDur;
    const alpha=ratio*baseAlpha;
    const sz=baseSz*ratio;
    ctx.save();ctx.globalAlpha=alpha;
    // Fever/high combo: rainbow trail
    const trailColor=streak>=20?`hsl(${(now*0.18+i*15)%360},100%,70%)`:themeAccent||"#a78bfa";
    ctx.fillStyle=trailColor;
    ctx.shadowColor=trailColor;ctx.shadowBlur=6+comboLevel*2;
    ctx.beginPath();ctx.arc(p.x,p.y,sz,0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
}


// ═══════════════════════════════════════════════════════════════
// NEON BUTTON
// ═══════════════════════════════════════════════════════════════
function NeonButton({children,onClick,style,className="",disabled=false}){
  const [pressed,setPressed]=useState(false);
  return(
    <button disabled={disabled}
      onMouseDown={()=>setPressed(true)} onMouseUp={()=>setPressed(false)} onMouseLeave={()=>setPressed(false)}
      onTouchStart={e=>{e.preventDefault();setPressed(true);}}
      onTouchEnd={e=>{e.preventDefault();setPressed(false);if(!disabled&&onClick)onClick(e);}}
      onClick={e=>{if(!disabled&&onClick)onClick(e);}}
      className={`shine-btn select-none rounded-2xl font-bold text-white ${className}`}
      style={{
        transform:pressed?"scale(0.92)":"scale(1)",
        opacity:disabled?0.35:1,
        cursor:disabled?"not-allowed":"pointer",
        userSelect:"none",WebkitTapHighlightColor:"transparent",
        transition:pressed?"transform 0.06s ease":"transform 0.12s cubic-bezier(0.34,1.56,0.64,1)",
        letterSpacing:"0.04em",fontFamily:"inherit",
        ...style
      }}>
      {children}
    </button>
  );
}

// ═══════════════════════════════════════════════════════════════
// MAIN COMPONENT
// ═══════════════════════════════════════════════════════════════
export default function NexusTap(){
  // Persistent
  const saveRef   = useRef(loadSave());
  const audioRef  = useRef(null);
  const saveTimer = useRef(null);

  // UI state
  const [screen,      setScreen]    = useState("menu");
  const [theme,       setTheme]     = useState(()=>THEMES.find(t=>t.id===saveRef.current.themeId)||THEMES[0]);
  const [soundOn,     setSoundOn]   = useState(()=>saveRef.current.soundEnabled);
  const [notif,       setNotif]     = useState(null);
  const [cartItems,   setCartItems] = useState([]);

  // HUD (updated 20fps)
  const [hud,setHud] = useState({score:0,lives:3,streak:0,fever:false,coins:0,timeLeft:null,modGoal:null});
  const hudRef = useRef(0);

  // Overlay
  const [activePwrDisp, setActivePwrDisp] = useState([]);
  const [comboLabel,    setComboLabel]    = useState("");
  const [countdownVal,  setCountdownVal]  = useState(null);
  const [paused,        setPaused]        = useState(false);
  const [screenShake,   setScreenShake]   = useState(false);
  const [damageFlash,   setDamageFlash]   = useState(false); // red vignette on life loss
  const [epicFlash,     setEpicFlash]     = useState(false);
  const [legendaryFlash,setLegendaryFlash]= useState(false);
  const [feverBorder,   setFeverBorder]   = useState(false);
  const [perfectFlash,  setPerfectFlash]  = useState(false);
  const [levelCompleteData, setLevelCompleteData] = useState(null);
  const [gameOverData,      setGameOverData]      = useState(null);
  const [selectedLevel,     setSelectedLevel]     = useState(1);
  const [scrollToLevel,     setScrollToLevel]     = useState(null);
  const [storyData,         setStoryData]         = useState(null); // {worldId, type:'world'|'main', onDone:fn}
  const [streakShieldActive,setStreakShieldActive] = useState(false);
  const [luckyMode,         setLuckyMode]         = useState(false);
  const [newRecord,         setNewRecord]         = useState(false);
  const [rushMode,          setRushMode]          = useState(false);
  const [goldRushMode,      setGoldRushMode]      = useState(false); // Gold Rush — all coin rewards × 3
  const [bossTaunt,         setBossTaunt]         = useState(null); // {text, phase}
  const [closeBanner,       setCloseBanner]       = useState(false); // "SO CLOSE!" banner
  const luckyRef    = useRef(null);   // null | "active" | "countdown"
  const luckyTimer  = useRef(null);
  const triggerLuckyRef = useRef(null); // ref to triggerLucky for forward-reference calls
  const streakShRef = useRef(false);
  const [tutStep,       setTutStep]       = useState(null);
  const [mascotMood,    setMascotMood]    = useState("idle");
  const [mascotDancing, setMascotDancing] = useState(false);
  const [mascotUnlockedData, setMascotUnlockedData] = useState(null); // newly unlocked mascot
  const [dailyBonusData, setDailyBonusData] = useState(null); // {day, coins, xp, isWeekly}
  const [offlineCoinsData, setOfflineCoinsData] = useState(null); // {coins, hours} shown once on startup
  const [weeklyRecapData,  setWeeklyRecapData]  = useState(null); // {levelsPlayed, bestScore, coinsEarned, streak} on Sunday
  const [shopConfirm, setShopConfirm] = useState(null); // {total, items, onConfirm}
  const [spinState,     setSpinState]     = useState(null);
  const [spinResult,    setSpinResult]    = useState(null);
  const [spinDeg,       setSpinDeg]       = useState(0);
  const [streakBurst,   setStreakBurst]   = useState(null); // null | {n, label, color}
  // Las Vegas mechanics
  const [mysteryReveal,  setMysteryReveal]  = useState(null); // null | {phase,reels,prize}
  const tensionRef                          = useRef(0);
  const [tensionLevel,   setTensionLevel]   = useState(0);   // 0-4
  const [rescueSecondsLeft,setRescueSecondsLeft]=useState(0);// 0 = no rescue offered
  const rescuedRef                          = useRef(false);
  const [bonusRound,     setBonusRound]     = useState(false);
  const [displayedCoins, setDisplayedCoins] = useState(0);
  const [displayedXP,    setDisplayedXP]    = useState(0);
  const [payoutDone,     setPayoutDone]     = useState(false);
  // Mascot speech bubble
  const [mascotSpeech,   setMascotSpeech]  = useState(null);
  const speechTimerRef                      = useRef(null);
  const [mascotBounce,   setMascotBounce]  = useState(false);
  const bounceTimerRef                      = useRef(null);
  // New features
  const [streakDecaying, setStreakDecaying] = useState(false);
  const decayTimerRef                       = useRef(null);
  const [achPopup, setAchPopup]             = useState(null); // {icon, label, xp}
  const [replayModal,    setReplayModal]    = useState(null); // null | levelId
  const [infinityRound,  setInfinityRound]  = useState(1);
  const [gauntletState,  setGauntletState]  = useState(null); // null | {bossIndex, lives, score}
  const [prestigeAnim,   setPrestigeAnim]   = useState(false);
  const [chainFlash,     setChainFlash]     = useState(null); // null | {label, at}
  const [lbTab,          setLbTab]          = useState("all"); // "today"|"week"|"all"
  const [zenWorld,       setZenWorld]       = useState(1);    // world selector for zen mode
  const [taWorld,        setTaWorld]        = useState(1);    // world selector for time attack
  // Bounty Board — rotating wanted rarity for bonus pts
  const [bountyData,     setBountyData]     = useState(null); // {rarity,icon,mult,expiresAt}
  const bountyRef        = useRef(null);
  const bountyTimerRef   = useRef(null);

  // Canvas & game refs
  const canvasRef    = useRef(null);
  const gsRef        = useRef(null);
  const targetsRef   = useRef([]);
  const particlesRef = useRef([]);
  const activePwrRef = useRef([]);
  const rafRef       = useRef(null);
  const lastTickRef  = useRef(0);
  const spawnTimer   = useRef(0);
  const bgPartsRef   = useRef([]);
  const ripplesRef   = useRef([]);
  const tapTrailRef  = useRef([]); // finger trail {x,y,ts,alpha}
  const missionProg  = useRef({});
  const pausedRef    = useRef(false);
  const levelCfgRef  = useRef(null); // current level config
  const mascotHappyRef = useRef(0);  // 9B — session tap happiness counter (resets per level)

  // Audio
  useEffect(()=>{audioRef.current=createAudio();},[]);

  // View Transition helper — smooth animated screen changes
  const go = useCallback((s)=>{
    if(typeof document.startViewTransition==="function"){
      document.startViewTransition(()=>{flushSync(()=>setScreen(s));});
    }else{
      setScreen(s);
    }
  },[]);

  // GSAP screen entrance — bounces/fades new screen content when screen changes
  useLayoutEffect(()=>{
    if(screen==="playing")return; // no entrance for game canvas
    const el=document.querySelector(".screen-root");
    if(!el)return;
    gsap.fromTo(el,{opacity:0,y:16},{opacity:1,y:0,duration:0.28,ease:"power2.out",clearProps:"all"});
  },[screen]);
  const sfx=useCallback((n,...args)=>{if(soundOn&&audioRef.current?.[n])audioRef.current[n](...args);},[soundOn]);

  // Save
  const flushSave=useCallback(()=>{try{localStorage.setItem("nexustap_v5",JSON.stringify(saveRef.current));}catch{}},[]);
  const debounceSave=useCallback(()=>{
    if(saveTimer.current)clearTimeout(saveTimer.current);
    saveTimer.current=setTimeout(flushSave,1800);
  },[flushSave]);

  // Notification
  useEffect(()=>{if(!notif)return;const t=setTimeout(()=>setNotif(null),2600);return()=>clearTimeout(t);},[notif]);

  // Achievements
  const unlock=useCallback((id)=>{
    const sv=saveRef.current;
    if(sv.unlockedAchievements.includes(id))return;
    sv.unlockedAchievements=[...sv.unlockedAchievements,id];
    const ach=ACHIEVEMENTS.find(a=>a.id===id);
    if(ach){
      sv.xp+=ach.xp||0;
      // Mid-game: show rich popup; out of game: use notif toast
      if(gsRef.current){
        setAchPopup({icon:ach.icon,label:ach.label,xp:ach.xp||0});
        setTimeout(()=>setAchPopup(null),2200);
      } else {
        setNotif(`${ach.icon} ${ach.label}!`);
      }
    }
    debounceSave();
  },[debounceSave]);

  // Daily login — 7-day rotating reward calendar
  useEffect(()=>{
    const sv=saveRef.current, today=getTodayKey();
    if(sv.lastLoginDate===today)return;
    const yesterday=(d=>{d.setDate(d.getDate()-1);return`${d.getFullYear()}-${d.getMonth()}-${d.getDate()}`;})(new Date());
    sv.loginStreak=sv.lastLoginDate===yesterday?(sv.loginStreak||0)+1:1;
    sv.lastLoginDate=today;
    // 7-day cycle — day 7 = jackpot, then resets
    const dayInCycle=((sv.loginStreak-1)%7)+1; // 1..7
    const COIN_REWARD=[40,60,80,120,160,220,400]; // day 1..7
    const XP_REWARD=[25,35,50,75,100,140,250];
    const coinsGiven=COIN_REWARD[dayInCycle-1];
    const xpGiven=XP_REWARD[dayInCycle-1];
    sv.coins=(sv.coins||0)+coinsGiven;
    sv.totalCoins=(sv.totalCoins||0)+coinsGiven;
    sv.xp+=xpGiven;
    if(sv.loginStreak>=7)unlock("daily_7");
    flushSave();
    // Show popup
    setDailyBonusData({day:dayInCycle,streak:sv.loginStreak,coins:coinsGiven,xp:xpGiven,isJackpot:dayInCycle===7});
  },[]);// eslint-disable-line

  // Offline coins — award coins earned while away (max 4 hours, 1 coin/10s)
  useEffect(()=>{
    const sv=saveRef.current;
    const earned=calcOfflineCoins(sv.lastActiveTime);
    if(earned>=5){ // only show if at least 5 coins earned
      sv.coins=(sv.coins||0)+earned;
      sv.totalCoins=(sv.totalCoins||0)+earned;
      const hoursAway=Math.max(0,(Date.now()-(sv.lastActiveTime||Date.now())))/3600000;
      setOfflineCoinsData({coins:earned,hours:Math.min(4,hoursAway)});
      // Achievement: earned coins offline
      if(!sv.unlockedAchievements?.includes("offline_earn")){
        sv.unlockedAchievements=[...(sv.unlockedAchievements||[]),"offline_earn"];
        const ach=ACHIEVEMENTS.find(a=>a.id==="offline_earn");
        if(ach)sv.xp=(sv.xp||0)+(ach.xp||0);
      }
      flushSave();
    }
    // Track active time — update on mount and on page unload
    sv.lastActiveTime=Date.now();
    const onHide=()=>{saveRef.current.lastActiveTime=Date.now();flushSave();};
    document.addEventListener("visibilitychange",onHide);
    window.addEventListener("pagehide",onHide);
    return()=>{document.removeEventListener("visibilitychange",onHide);window.removeEventListener("pagehide",onHide);};
  },[]);// eslint-disable-line

  // Weekly recap — shown on Sunday if player hasn't seen it this week (12C)
  useEffect(()=>{
    const today=new Date();
    if(today.getDay()!==0)return; // only Sunday (0)
    const sv=saveRef.current;
    const wk=getWeekKey();
    if((sv.lastWeeklyRecap||"")===wk)return; // already shown this week
    // Gather this week's data from timestamped scores
    const weekScores=(sv.scores||[]).map(e=>typeof e==="number"?{score:e,week:""}:e).filter(e=>e.week===wk);
    if(weekScores.length===0)return; // nothing to recap
    const bestScore=Math.max(...weekScores.map(e=>e.score));
    const levelsPlayed=weekScores.length;
    // Approximate weekly XP and coins from differences — use rough estimate
    const weeklyCoins=Math.round(weekScores.reduce((s,e)=>s+e.score*0.14,0));
    sv.lastWeeklyRecap=wk;
    flushSave();
    setWeeklyRecapData({levelsPlayed,bestScore,coinsEarned:weeklyCoins,weekKey:wk});
  },[]);// eslint-disable-line

  // Missions
  const initMissions=useCallback(()=>{
    const sv=saveRef.current,today=getTodayKey();
    if(sv.missionDate!==today){sv.missionDate=today;sv.missionProgress={};sv.missionCompleted=false;flushSave();}
    missionProg.current={...sv.missionProgress};
  },[flushSave]);

  const updateMissions=useCallback((stats)=>{
    const missions=getDailyMissions(),prog=missionProg.current;
    let changed=false;
    missions.forEach(m=>{
      const cur=prog[m.id]||0,val=stats[m.key]||0;
      if(val>cur){prog[m.id]=Math.min(val,m.goal);changed=true;}
    });
    if(changed){
      saveRef.current.missionProgress={...prog};
      if(missions.every(m=>(prog[m.id]||0)>=m.goal)&&!saveRef.current.missionCompleted){
        saveRef.current.missionCompleted=true;saveRef.current.xp+=200;
        setNotif("All missions done! +200 XP 🎉");unlock("missions_all");
      }
      debounceSave();
    }
  },[debounceSave,unlock]);

  // Canvas setup
  const initBgParts=useCallback((worldId=0)=>{
    const w=window.innerWidth,h=window.innerHeight;
    // World-specific particle configs
    const cfgs={
      1: ()=>({ color:"#ff5500", r:Math.random()*1.8+0.6, vx:(Math.random()-0.5)*0.4, vy:-(Math.random()*0.6+0.2), alpha:Math.random()*0.5+0.2, phase:Math.random()*Math.PI*2 }),
      2: ()=>({ color:Math.random()>0.5?"#3cb371":"#2d8a55", r:Math.random()*2.5+1, vx:(Math.random()-0.5)*0.5, vy:Math.random()*0.3+0.1, alpha:Math.random()*0.4+0.15, phase:Math.random()*Math.PI*2, angle:Math.random()*Math.PI*2 }),
      3: ()=>({ color:Math.random()>0.5?"#ffd700":"#c8a84b", r:Math.random()*1.5+0.5, vx:(Math.random()-0.5)*0.4, vy:(Math.random()-0.5)*0.4, alpha:Math.random()*0.45+0.15, phase:Math.random()*Math.PI*2 }),
      4: ()=>({ color:Math.random()>0.5?"#9b59b6":"#6c3483", r:Math.random()*2+0.8, vx:(Math.random()-0.5)*0.35, vy:(Math.random()-0.5)*0.35, alpha:Math.random()*0.35+0.1, phase:Math.random()*Math.PI*2 }),
      5: ()=>({ color:"#a8d8f0", r:Math.random()*2+0.6, vx:(Math.random()-0.5)*0.3, vy:Math.random()*0.4+0.15, alpha:Math.random()*0.5+0.2, phase:Math.random()*Math.PI*2 }),
      6: ()=>({ color:Math.random()>0.4?"#e8c020":"#ffd700", r:Math.random()*1.4+0.5, vx:(Math.random()-0.5)*0.5, vy:(Math.random()-0.5)*0.3, alpha:Math.random()*0.5+0.2, phase:Math.random()*Math.PI*2 }),
      7: ()=>({ color:Math.random()>0.5?"#7fad7a":"#b0c9a0", r:Math.random()*2.5+1, vx:(Math.random()-0.5)*0.25, vy:(Math.random()-0.5)*0.2, alpha:Math.random()*0.3+0.08, phase:Math.random()*Math.PI*2 }),
      8: ()=>({ color:Math.random()>0.5?"#1abc9c":"#a8f0e0", r:Math.random()*3+1, vx:(Math.random()-0.5)*0.2, vy:-(Math.random()*0.4+0.1), alpha:Math.random()*0.35+0.1, phase:Math.random()*Math.PI*2 }),
      9: ()=>({ color:Math.random()>0.6?"#ffffff":"#b0b8c1", r:Math.random()*2+0.7, vx:(Math.random()-0.5)*0.3, vy:Math.random()*0.35+0.1, alpha:Math.random()*0.5+0.15, phase:Math.random()*Math.PI*2 }),
      10:()=>({ color:Math.random()>0.5?"#ffd700":"#ff8c00", r:Math.random()*2+0.6, vx:(Math.random()-0.5)*0.35, vy:(Math.random()-0.5)*0.25, alpha:Math.random()*0.45+0.15, phase:Math.random()*Math.PI*2 }),
    };
    const getCfg=cfgs[worldId]||(()=>({ r:Math.random()*2+0.5, vx:(Math.random()-0.5)*0.3, vy:(Math.random()-0.5)*0.3, alpha:Math.random()*0.32+0.06, phase:Math.random()*Math.PI*2 }));
    bgPartsRef.current=Array.from({length:55},()=>({
      x:Math.random()*w, y:Math.random()*h, worldId, ...getCfg(),
    }));
  },[]);

  useEffect(()=>{
    const resize=()=>{if(canvasRef.current){canvasRef.current.width=window.innerWidth;canvasRef.current.height=window.innerHeight;}};
    resize();window.addEventListener("resize",resize);initBgParts();
    return()=>window.removeEventListener("resize",resize);
  },[initBgParts]);

  // Spawn helpers
  const spawnParticles=useCallback((x,y,color,count=10,type="dot")=>{
    const now=performance.now();
    for(let i=0;i<count;i++){
      const angle=Math.random()*Math.PI*2,spd=2+Math.random()*8;
      particlesRef.current.push({type,x,y,vx:Math.cos(angle)*spd,vy:Math.sin(angle)*spd-(type==="spark"?2:0),
        color,size:Math.random()*5+1.5,born:now,duration:380+Math.random()*340,alpha:1});
    }
    if(count>=10) particlesRef.current.push({type:"shockwave",x,y,r:1,color,born:now,duration:480,alpha:0.8});
  },[]);

  const spawnPopup=useCallback((x,y,text,color,size=14)=>{
    particlesRef.current.push({type:"popup",x,y,vx:0,vy:0,text,color,size,born:performance.now(),duration:900,alpha:1});
  },[]);

  const pickPos=useCallback((radius)=>{
    const margin=radius*2+25,w=window.innerWidth,h=window.innerHeight;
    let best=null,bestD=-1;
    for(let i=0;i<6;i++){
      const x=margin+Math.random()*(w-margin*2),y=margin+100+Math.random()*(h-margin*2-100);
      let minD=Infinity;
      targetsRef.current.forEach(t=>{const d=Math.hypot(t.x-x,t.y-y);if(d<minD)minD=d;});
      if(minD>bestD){bestD=minD;best={x,y};}
    }
    return best||{x:margin+Math.random()*(w-margin*2),y:130+Math.random()*(h-280)};
  },[]);

  // Power-ups
  const activatePowerUp=useCallback((x,y,ptype)=>{
    const gs=gsRef.current;if(!gs)return;
    sfx("powerUp");vibrate([10,30,10]);unlock("powerup_use");
    gs.sessionStats.powerupCollected=(gs.sessionStats.powerupCollected||0)+1;
    if(ptype==="LIFE"){
      if(gs.lives<MAX_LIVES)gs.lives++;
      spawnPopup(x,y,"+LIFE","#34d399",16);
    } else if(ptype==="LUCKY"){
      // LUCKY power-up — triggers 7s all-legendary mode
      triggerLuckyRef.current?.();
      spawnPopup(x,y,"⭐ LUCKY STARS!","#ffd700",18);
    } else if(ptype==="COMBO_FREEZE"){
      // COMBO_FREEZE — combo streak is protected for 10s (misses don't reset streak)
      const dur=10000;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COMBO_FREEZE");
      activePwrRef.current.push({type:"COMBO_FREEZE",endsAt:Date.now()+dur});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🧊 COMBO LOCK!","#67e8f9",18);
    } else if(ptype==="MIRROR"){
      // MIRROR — flip all current target positions horizontally for 5s chaos
      const canvas=canvasRef.current;
      const cw=canvas?canvas.width:390;
      targetsRef.current.forEach(t=>{if(t.type!=="boss"&&!t.dying)t.x=cw-t.x;});
      setScreenShake(true);setTimeout(()=>setScreenShake(false),300);
      spawnPopup(x,y,"🪞 MIRROR!","#c084fc",18);
      setNotif("🪞 MIRROR! All targets flipped!");
    } else if(ptype==="MULTIPLIER"){
      // MULTIPLIER — 3× score for 8 seconds
      const dur=8000;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!==ptype);
      activePwrRef.current.push({type:ptype,endsAt:Date.now()+dur});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"×3 MULTIPLIER!","#f43f5e",18);
    } else if(ptype==="GRAVITY"){
      // GRAVITY — pulls all targets toward screen center instantly
      const canvas=canvasRef.current;
      const gcx=canvas?canvas.width/2:195,gcy=canvas?canvas.height/2:350;
      targetsRef.current.forEach(t=>{
        if(t.dying||t.type==="boss")return;
        const dx=gcx-t.x,dy=gcy-t.y,dist=Math.hypot(dx,dy);
        if(dist>0){t.x+=dx*0.55;t.y+=dy*0.55;} // pull 55% of the way to center
      });
      spawnPopup(gcx,gcy,"🌐 GRAVITY PULL!","#a78bfa",20);
      spawnParticles(gcx,gcy,"#a78bfa",20,"spark");
      sfx("chainBonus");vibrate([15,8,25]);
      setScreenShake(true);setTimeout(()=>setScreenShake(false),350);
      setNotif("🌐 GRAVITY! All targets pulled to center!");
    } else if(ptype==="CHAIN_LIGHTNING"){
      // CHAIN LIGHTNING — auto-scores up to 4 random non-boss targets instantly
      const eligible=targetsRef.current.filter(t=>!t.dying&&t.type!=="boss"&&t.type!=="bomb"&&t.type!=="volatile");
      const toZap=eligible.sort(()=>Math.random()-0.5).slice(0,4);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      let totalPts=0;
      toZap.forEach((t,i)=>{
        setTimeout(()=>{
          if(!t.dying){
            t.dying=performance.now();
            const pts=Math.round(60*combo*feverMult);
            gs.score+=pts;gs.sessionStats.score=gs.score;totalPts+=pts;
            spawnParticles(t.x,t.y,"#fbbf24",8,"spark");
            spawnPopup(t.x,t.y-22,`⚡ +${pts}`,"#fbbf24",13);
            sfx("comboNote",Math.min(12,gs.streak+i));
          }
        },i*150);
      });
      spawnPopup(x,y,"⚡ CHAIN LIGHTNING!","#fbbf24",20);
      vibrate([10,8,10,8,25]);sfx("chainBonus");
      unlock("chain_lightning_hit");
      setNotif(`⚡ CHAIN LIGHTNING! Zapping ${toZap.length} targets!`);
    } else if(ptype==="LIFE_SURGE"){
      // LIFE SURGE — instant +2 lives (max cap: starting lives + 2)
      const maxLives=(levelCfgRef.current?.lives||3)+2;
      const gained=Math.min(2,maxLives-gs.lives);
      gs.lives+=gained;
      spawnPopup(x,y,`❤️ +${gained} LIVES!`,"#4ade80",20);
      spawnParticles(x,y,"#4ade80",16,"spark");
      sfx("starEarn");vibrate([15,8,15,8,25]);
      setNotif(`❤️ Life Surge! +${gained} lives!`);
    } else if(ptype==="SCORE_BOOST"){
      // SCORE BOOST — next 3 taps score 5× normal
      gs._scoreBoostTaps=3;
      spawnPopup(x,y,"×5 SCORE BOOST!","#f43f5e",22);
      spawnParticles(x,y,"#f43f5e",16,"spark");
      sfx("powerUp");vibrate([10,6,10,6,25]);
      setNotif("×5 SCORE BOOST! Next 3 taps × 5!");
      unlock("score_boost_use");
    } else if(ptype==="TIME_WARP"){
      // TIME WARP — drastically slows all targets for 6 seconds
      gs._timeWarpEndsAt=Date.now()+6000;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TIME_WARP");
      activePwrRef.current.push({type:"TIME_WARP",endsAt:Date.now()+6000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⏱️ TIME WARP!","#818cf8",20);
      spawnParticles(x,y,"#818cf8",18,"spark");
      sfx("powerUp");vibrate([12,8,12,8,30]);
      setScreenShake(true);setTimeout(()=>setScreenShake(false),300);
      setNotif("⏱️ TIME Warp! Everything slowed to 20%!");
      unlock("time_warp_use");
    } else if(ptype==="MAGNET_FIELD"){
      // MAGNET FIELD — all targets drift toward your last tap for 5s
      gs._magnetFieldEndsAt=Date.now()+5000;
      gs._magnetFieldX=x;gs._magnetFieldY=y;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="MAGNET_FIELD");
      activePwrRef.current.push({type:"MAGNET_FIELD",endsAt:Date.now()+5000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🧲 MAGNET FIELD!","#ec4899",20);
      spawnParticles(x,y,"#ec4899",18,"spark");
      sfx("powerUp");vibrate([12,8,20,8,12]);
      setNotif("🧲 Magnet Field! Targets pulled toward you!");
    } else if(ptype==="OVERCLOCK"){
      // OVERCLOCK — all points ×2 for 10s (stacks with fever and other multipliers)
      gs._overclockEndsAt=Date.now()+10000;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="OVERCLOCK");
      activePwrRef.current.push({type:"OVERCLOCK",endsAt:Date.now()+10000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⚡ OVERCLOCK! ×2 PTS!","#fbbf24",22);
      spawnParticles(x,y,"#fbbf24",20,"spark");spawnParticles(x,y,"#ffffff",8,"dot");
      sfx("feverStart");vibrate([15,8,15,8,20]);
      setNotif("⚡ Overclock! All points ×2 for 10 seconds!");
    } else if(ptype==="SHIELD_WALL"){
      // SHIELD_WALL — absorbs up to 3 hits (bombs, misses, siphons) before breaking
      gs._shieldWallHits=3;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_WALL");
      activePwrRef.current.push({type:"SHIELD_WALL",endsAt:Date.now()+30000,hitsLeft:3});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🏰 SHIELD WALL! ×3","#a78bfa",20);
      spawnParticles(x,y,"#a78bfa",18,"spark");
      sfx("lucky");vibrate([12,8,12,8,12]);
      setNotif("🏰 Shield Wall! Absorbs 3 hits!");
    } else if(ptype==="COIN_RAIN"){
      // COIN_RAIN — instantly awards 8-15 coins directly to balance
      const rainCoins=8+Math.floor(Math.random()*8);
      saveRef.current.coins=(saveRef.current.coins||0)+rainCoins;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+rainCoins;
      flushSave();
      // Spawn visual coin particles
      const canvas8=canvasRef.current;const cw8=canvas8?.width||390,ch8=canvas8?.height||700;
      for(let i=0;i<rainCoins;i++){
        particlesRef.current.push({type:"dot",x:Math.random()*cw8,y:-10,
          color:"#ffd700",alpha:1,size:5,vx:(Math.random()-0.5)*0.6,vy:1.5+Math.random()*1.5,
          born:performance.now()+i*60,duration:2500});
      }
      spawnPopup(x,y,`🪙 COIN RAIN! +${rainCoins} coins!`,"#ffd700",22);
      spawnParticles(x,y,"#ffd700",18,"dot");
      sfx("coin");vibrate([10,5,10,5,14]);
      setNotif(`🪙 Coin Rain! +${rainCoins} coins!`);
    } else if(ptype==="PHANTOM_TOUCH"){
      // PHANTOM_TOUCH — bombs pass through you harmlessly for 5s
      gs._phantomTouchEndsAt=Date.now()+5000;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PHANTOM_TOUCH");
      activePwrRef.current.push({type:"PHANTOM_TOUCH",endsAt:Date.now()+5000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"👻 PHANTOM TOUCH!","#818cf8",20);
      spawnParticles(x,y,"#818cf8",16,"spark");
      sfx("lucky");vibrate([10,6,10,6,14]);
      setNotif("👻 Phantom Touch! Bombs are harmless for 5s!");
    } else if(ptype==="BERSERKER"){
      // BERSERKER — all targets 5× pts for 8s; bombs cost 2 lives!
      gs._berserkerEndsAt=Date.now()+8000;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BERSERKER");
      activePwrRef.current.push({type:"BERSERKER",endsAt:Date.now()+8000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⚔️ BERSERKER! ×5 pts!","#ef4444",22);
      spawnParticles(x,y,"#ef4444",20,"spark");
      sfx("hotStreak");vibrate([20,6,20,6,25]);
      setNotif("⚔️ Berserker! ×5 score — beware bombs cost 2 lives!");
      unlock("berserker_use");
    } else if(ptype==="GHOST_MODE"){
      // GHOST_MODE — bombs don't cost lives for 7s
      gs._ghostModeEndsAt=Date.now()+7000;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="GHOST_MODE");
      activePwrRef.current.push({type:"GHOST_MODE",endsAt:Date.now()+7000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"👻 GHOST MODE!","#818cf8",20);
      spawnParticles(x,y,"#818cf8",14,"spark");
      sfx("lucky");vibrate([10,5,10,5,10]);
      setNotif("👻 Ghost Mode! Bombs harmless for 7s!");
      unlock("ghost_mode_use");
    } else if(ptype==="SCORE_SHIELD"){
      // SCORE_SHIELD — next 3 misses don't reset streak (score protected)
      gs._scoreShieldCharges=3;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_SHIELD");
      activePwrRef.current.push({type:"SCORE_SHIELD",endsAt:Date.now()+30000,charges:3});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔰 SCORE SHIELD!","#a3e635",20);
      spawnParticles(x,y,"#a3e635",12,"spark");
      sfx("lucky");vibrate([8,4,8]);
      setNotif("🔰 Score Shield! Next 3 misses won't break streak!");
    } else if(ptype==="DOUBLE_SPAWN"){
      // DOUBLE_SPAWN — next 10 spawns create 2 targets at once
      gs._doubleSpawnLeft=10;
      spawnPopup(x,y,"🎯×2 DOUBLE SPAWN!","#f0abfc",20);
      spawnParticles(x,y,"#f0abfc",12,"spark");
      sfx("lucky");vibrate([8,4,8]);
      setNotif("🎯 Double Spawn! 10× dual-target spawns!");
    } else if(ptype==="TIME_STOP"){
      // TIME_STOP — freeze target expiry timers for 4s (nothing expires while active)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TIME_STOP");
      activePwrRef.current.push({type:"TIME_STOP",endsAt:Date.now()+4000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⏸️ TIME STOP!","#67e8f9",22);
      spawnParticles(x,y,"#67e8f9",14,"spark");
      sfx("feverStart");vibrate([8,4,12,4,8]);
      setNotif("⏸️ Time Stop! Nothing expires for 4s!");
    } else if(ptype==="MEGA_TAP"){
      // MEGA_TAP — next 10 taps each give 3× pts
      gsRef.current._megaTapCharges=10;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="MEGA_TAP");
      activePwrRef.current.push({type:"MEGA_TAP",charges:10,endsAt:Date.now()+20000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💥 MEGA TAP!","#fb923c",22);
      spawnParticles(x,y,"#fb923c",14,"spark");
      sfx("chainBonus");vibrate([8,4,14,4,8]);
      setNotif("💥 Mega Tap! Next 10 taps = 3× pts!");
    } else if(ptype==="RAINBOW_SURGE"){
      // RAINBOW_SURGE — all targets spawn as Legendary quality for 6s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="RAINBOW_SURGE");
      activePwrRef.current.push({type:"RAINBOW_SURGE",endsAt:Date.now()+6000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌈 RAINBOW SURGE!","#f472b6",22);
      spawnParticles(x,y,"#f472b6",16,"spark");
      sfx("legendary");vibrate([8,4,12,4,8]);
      setNotif("🌈 Rainbow Surge! All targets are LEGENDARY for 6s!");
    } else if(ptype==="TWIN_STRIKE"){
      // TWIN_STRIKE — every tap creates a mirrored second tap for 8s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TWIN_STRIKE");
      activePwrRef.current.push({type:"TWIN_STRIKE",endsAt:Date.now()+8000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"👥 TWIN STRIKE!","#f472b6",22);
      spawnParticles(x,y,"#f472b6",14,"spark");
      sfx("powerUp");vibrate([6,3,10,3,6]);
      setNotif("👥 Twin Strike! Every tap fires twice for 8s!");
    } else if(ptype==="GOLDEN_TOUCH"){
      // GOLDEN_TOUCH — next 5 taps score Legendary rarity mult regardless
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="GOLDEN_TOUCH");
      activePwrRef.current.push({type:"GOLDEN_TOUCH",endsAt:Date.now()+30000,charges:5});
      gsRef.current._goldenCharges=5;
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"✨ GOLDEN TOUCH!","#ffd700",22);
      spawnParticles(x,y,"#ffd700",18,"spark");
      sfx("legendary");vibrate([8,4,12,4,8,4,12]);
      setNotif("✨ Golden Touch! Next 5 taps = LEGENDARY rarity!");
    } else if(ptype==="LUCK_SURGE"){
      // LUCK_SURGE — mystery boxes guarantee RARE+ for 12s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LUCK_SURGE");
      activePwrRef.current.push({type:"LUCK_SURGE",endsAt:Date.now()+12000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🍀 LUCK SURGE!","#4ade80",22);
      spawnParticles(x,y,"#4ade80",16,"spark");
      sfx("lucky");vibrate([5,3,10,3,15]);
      setNotif("🍀 Luck Surge! All mystery boxes are RARE+ for 12s!");
    } else if(ptype==="FREEZE_ZONE"){
      // FREEZE_ZONE — nearby targets to tap location slowed by 70% for 8s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="FREEZE_ZONE");
      activePwrRef.current.push({type:"FREEZE_ZONE",endsAt:Date.now()+8000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❄️ FREEZE ZONE!","#67e8f9",22);
      spawnParticles(x,y,"#67e8f9",16,"spark");
      sfx("powerUp");vibrate([6,3,10]);
      setNotif("❄️ Freeze Zone! Tap area slows nearby targets!");
    } else if(ptype==="SCORE_X2"){
      // SCORE_X2 — doubles all pts for 30s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_X2");
      activePwrRef.current.push({type:"SCORE_X2",endsAt:Date.now()+30000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"×2 SCORE!","#ffd700",24);
      spawnParticles(x,y,"#ffd700",20,"spark");
      sfx("legendary");vibrate([8,4,12,4,16]);
      setNotif("×2 Score! ALL pts doubled for 30 seconds!");
    } else if(ptype==="MAGNET_PULL"){
      // MAGNET_PULL — all targets pulled toward player taps for 8s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="MAGNET_PULL");
      activePwrRef.current.push({type:"MAGNET_PULL",endsAt:Date.now()+8000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🧲 MAGNET PULL!","#ec4899",22);
      spawnParticles(x,y,"#ec4899",14,"spark");
      sfx("powerUp");vibrate([6,3,10,3,6]);
      setNotif("🧲 Magnet Pull! Targets attracted to your taps!");
    } else if(ptype==="STAR_RAIN"){
      // STAR_RAIN — spawns 5 star targets (each worth 200pts extra)
      for(let i=0;i<5;i++){
        setTimeout(()=>{
          const cw=canvasRef.current?.width||390;const ch=canvasRef.current?.height||700;
          const sx=20+Math.random()*(cw-40),sy=100+Math.random()*(ch-180);
          targetsRef.current.push({
            id:Math.random().toString(36).slice(2),type:"star_rain_drop",
            x:sx,y:sy,radius:22,color:"#ffd700",glow:"#fbbf24",
            lifetime:2200,spawnedAt:Date.now(),born:performance.now(),
            moving:false,ghost:false,vx:0,vy:0,trail:[],hitsLeft:1,maxHits:1,dying:null,
          });
        },i*250);
      }
      spawnPopup(x,y,"⭐ STAR RAIN!","#ffd700",20);
      spawnParticles(x,y,"#ffd700",14,"spark");
      sfx("starEarn");vibrate([6,3,10,3,6]);
      setNotif("⭐ Star Rain! 5 golden stars incoming!");
    } else if(ptype==="COMBO_SHIELD"){
      // COMBO_SHIELD — protects combo streak from miss reset for 10s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COMBO_SHIELD");
      activePwrRef.current.push({type:"COMBO_SHIELD",endsAt:Date.now()+10000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔒 COMBO SHIELD!","#f59e0b",22);
      spawnParticles(x,y,"#fcd34d",12,"spark");
      sfx("powerUp");vibrate([6,3,8,3,6]);
      setNotif("🔒 Combo Shield! Your streak is protected for 10s!");
    } else if(ptype==="PERFECT_AIM"){
      // PERFECT_AIM — auto-hits nearest target every 2s for 5 hits
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PERFECT_AIM");
      activePwrRef.current.push({type:"PERFECT_AIM",endsAt:Date.now()+10000,charges:5,lastAutoHit:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🎯 PERFECT AIM!","#22d3ee",22);
      spawnParticles(x,y,"#67e8f9",12,"spark");
      sfx("powerUp");vibrate([6,3,8,3,10]);
      setNotif("🎯 Perfect Aim! Auto-hitting 5 targets!");
    } else if(ptype==="CHAIN_BURST"){
      // CHAIN_BURST — for 12s, each tap when streak >=3 gives +75 bonus pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CHAIN_BURST");
      activePwrRef.current.push({type:"CHAIN_BURST",endsAt:Date.now()+12000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔥 CHAIN BURST!","#f97316",22);
      spawnParticles(x,y,"#fb923c",14,"spark");
      sfx("powerUp");vibrate([5,3,5,3,10,3,10]);
      unlock("chain_burst_use");
      setNotif("🔥 Chain Burst! +75 per streak tap!");
    } else if(ptype==="MAGNET_BURST"){
      // MAGNET_BURST — one-shot: collect all normal targets immediately for 40 pts each
      const victims=targetsRef.current.filter(o=>!o.dying&&o.type==="normal");
      let bonusPts=0;
      victims.forEach(o=>{
        o.dying=performance.now();
        bonusPts+=40;
        spawnParticles(o.x,o.y,"#a78bfa",8,"spark");
      });
      const gs0=gsRef.current;
      if(gs0&&bonusPts>0){
        gs0.score+=bonusPts;
        setHud({score:gs0.score,lives:gs0.lives,streak:gs0.streak});
        spawnPopup(x,y,`🧲 +${bonusPts}!`,"#a78bfa",24);
      }
      sfx("jackpot");vibrate([8,3,12,3,8]);
      unlock("magnet_burst_use");
      setNotif(`🧲 Magnet Burst! Swept ${victims.length} targets!`);
    } else if(ptype==="SCORE_PULSE"){
      // SCORE_PULSE — auto-grants +50 pts every second for 10s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_PULSE");
      activePwrRef.current.push({type:"SCORE_PULSE",endsAt:Date.now()+10000,lastTick:Date.now()});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💫 SCORE PULSE!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",12,"spark");
      sfx("powerUp");vibrate([6,3,10]);
      unlock("score_pulse_use");
      setNotif("💫 Score Pulse! +50 pts/sec for 10s!");
    } else if(ptype==="LIFE_SHIELD"){
      // LIFE_SHIELD — blocks the next single life loss
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_SHIELD");
      activePwrRef.current.push({type:"LIFE_SHIELD",endsAt:Date.now()+30000,charges:1});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🛡️ LIFE SHIELD!","#86efac",22);
      spawnParticles(x,y,"#4ade80",12,"spark");
      sfx("powerUp");vibrate([4,3,6,3,10]);
      unlock("life_shield_use");
      setNotif("🛡️ Life Shield! Next life loss blocked!");
    } else if(ptype==="MULTIPLIER_SURGE"){
      // MULTIPLIER_SURGE — for 8s, combo multiplier is doubled
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="MULTIPLIER_SURGE");
      activePwrRef.current.push({type:"MULTIPLIER_SURGE",endsAt:Date.now()+8000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⚡× SURGE!","#f59e0b",22);
      spawnParticles(x,y,"#fde68a",14,"spark");
      sfx("powerUp");vibrate([6,3,6,3,12]);
      unlock("mult_surge_use");
      setNotif("⚡× Multiplier Surge! Combos doubled for 8s!");
    } else if(ptype==="BOUNCE_BACK"){
      // BOUNCE_BACK — 3 charges: missed normal targets respawn once with 2s lifetime
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BOUNCE_BACK");
      activePwrRef.current.push({type:"BOUNCE_BACK",endsAt:Date.now()+30000,charges:3});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔄 BOUNCE BACK!","#34d399",22);
      spawnParticles(x,y,"#6ee7b7",12,"spark");
      sfx("powerUp");vibrate([5,3,8,3,5]);
      unlock("bounce_back_use");
      setNotif("🔄 Bounce Back! Missed targets respawn 3 times!");
    } else if(ptype==="FEVER_EXTEND"){
      // FEVER_EXTEND — extend active fever by 5s; if none, trigger 5s fever
      const gs6=gsRef.current;
      if(gs6){
        if(gs6.feverActive){
          gs6.feverTimeLeft=(gs6.feverTimeLeft||0)+5000;
        } else {
          gs6.feverActive=true;gs6.feverTimeLeft=5000;
          setFeverBorder(true);sfx("feverStart");vibrate([20,10,20,10,20]);
        }
      }
      spawnPopup(x,y,"🌡️ FEVER EXTENDED!","#f43f5e",24);
      spawnParticles(x,y,"#fda4af",16,"spark");
      sfx("powerUp");vibrate([8,4,12]);
      unlock("fever_extend_use");
      setNotif("🌡️ Fever Extended by 5 seconds!");
    } else if(ptype==="COIN_RUSH"){
      // COIN_RUSH — 15s: 30% chance of +1 coin per tap
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COIN_RUSH");
      activePwrRef.current.push({type:"COIN_RUSH",endsAt:Date.now()+15000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🪙 COIN RUSH!","#fcd34d",22);
      spawnParticles(x,y,"#fbbf24",14,"spark");
      sfx("coin");vibrate([6,3,6,3,6]);
      unlock("coin_rush_use");
      setNotif("🪙 Coin Rush! 30% coin chance per tap for 15s!");
    } else if(ptype==="GRAVITY_WELL"){
      // GRAVITY_WELL — for 8s, all targets pulled toward center
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="GRAVITY_WELL");
      activePwrRef.current.push({type:"GRAVITY_WELL",endsAt:Date.now()+8000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌀 GRAVITY WELL!","#818cf8",22);
      spawnParticles(x,y,"#c4b5fd",14,"spark");
      sfx("powerUp");vibrate([6,3,10,3,6]);
      unlock("gravity_well_use");
      setNotif("🌀 Gravity Well! All targets pulled to center!");
    } else if(ptype==="CLONE_ARMY"){
      // CLONE_ARMY — one-shot: spawn up to 5 mini-clones of current normals
      const normalTargets=targetsRef.current.filter(o=>!o.dying&&o.type==="normal").slice(0,5);
      const cw77=canvasRef.current?.width||360;const ch77=canvasRef.current?.height||640;
      normalTargets.forEach(o=>{
        const bx=Math.max(20,Math.min(cw77-20,o.x+(Math.random()-0.5)*120));
        const by=Math.max(20,Math.min(ch77-20,o.y+(Math.random()-0.5)*120));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"normal",
          rarity:RARITY.UNCOMMON,color:RARITY.UNCOMMON.color,glow:RARITY.UNCOMMON.glow,
          x:bx,y:by,radius:o.radius*0.75,
          lifetime:2500,spawnedAt:Date.now(),born:performance.now(),
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:[],
          worldId:o.worldId,worldColor:o.worldColor,_isShard:true,anticipateMs:0,
          bossPattern:null,bossPhase:1,cx:bx,cy:by});
      });
      spawnPopup(x,y,`👥 CLONED! +${normalTargets.length}`,"#a78bfa",22);
      spawnParticles(x,y,"#c4b5fd",12,"spark");
      sfx("powerUp");vibrate([5,3,5,3,5,3,5]);
      unlock("clone_army_use");
      setNotif(`👥 Clone Army! Spawned ${normalTargets.length} clones!`);
    } else if(ptype==="RUSH_MODE"){
      // RUSH_MODE — every 3s a random normal target flashes gold and gives 2× for 1s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="RUSH_MODE");
      activePwrRef.current.push({type:"RUSH_MODE",endsAt:Date.now()+10000,lastRush:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⚡ RUSH MODE!","#fcd34d",22);
      spawnParticles(x,y,"#fbbf24",14,"spark");
      sfx("powerUp");vibrate([4,3,4,3,12]);
      unlock("rush_mode_use");
      setNotif("⚡ Rush Mode! Gold targets appear every 3s!");
    } else if(ptype==="INVINCIBLE"){
      // INVINCIBLE — 5s god mode: no lives lost, no miss penalty
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="INVINCIBLE");
      activePwrRef.current.push({type:"INVINCIBLE",endsAt:Date.now()+5000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💫 INVINCIBLE!","#fbbf24",26);
      spawnParticles(x,y,"#fef3c7",16,"spark");
      sfx("powerUp");vibrate([10,5,15,5,20]);
      unlock("invincible_use");
      setNotif("💫 INVINCIBLE! 5 seconds of god mode!");
    } else if(ptype==="HEALER"){
      // HEALER — immediately restore 1 life (up to starting lives)
      const gs7=gsRef.current;
      if(gs7){
        const cfg7=levelCfgRef.current;
        const maxLives=cfg7?.lives||3;
        gs7.lives=Math.min(maxLives+1,gs7.lives+1); // +1 even above max as a bonus
        setHud({score:gs7.score,lives:gs7.lives,streak:gs7.streak});
      }
      spawnPopup(x,y,"❤️ +1 LIFE!","#f87171",24);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("powerUp");vibrate([10,5,18,5,10]);
      unlock("healer_use");
      setNotif("❤️ Healer! +1 Life restored!");
    } else if(ptype==="SCORE_GUARD"){
      // SCORE_GUARD — for 12s, score cannot decrease from any source
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_GUARD");
      activePwrRef.current.push({type:"SCORE_GUARD",endsAt:Date.now()+12000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔰 SCORE GUARD!","#a3e635",22);
      spawnParticles(x,y,"#bef264",12,"spark");
      sfx("powerUp");vibrate([5,3,5,3,8]);
      unlock("score_guard_use");
      setNotif("🔰 Score Guard! No score loss for 12s!");
    } else if(ptype==="SWIPE_BOOST"){
      // SWIPE_BOOST — 8s: rapid-tap bonus, stacks +50 per tap within 0.5s of last tap
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SWIPE_BOOST");
      activePwrRef.current.push({type:"SWIPE_BOOST",endsAt:Date.now()+8000,stack:0,lastTap:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💨 SWIPE BOOST!","#67e8f9",22);
      spawnParticles(x,y,"#a5f3fc",12,"spark");
      sfx("powerUp");vibrate([4,2,4,2,8]);
      unlock("swipe_boost_use");
      setNotif("💨 Swipe Boost! Rapid taps stack +50 bonus!");
    } else if(ptype==="LUCKY_COIN"){
      // LUCKY_COIN — one-shot: awards 3-10 coins immediately
      const coins=3+Math.floor(Math.random()*8);
      saveRef.current.coins=(saveRef.current.coins||0)+coins;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+coins;
      flushSave();
      spawnPopup(x,y,`🍀 +${coins} COINS!`,"#fcd34d",24);
      spawnParticles(x,y,"#fbbf24",16,"spark");
      sfx("jackpot");vibrate([6,3,6,3,6,3,10]);
      unlock("lucky_coin_use");
      setNotif(`🍀 Lucky Coin! +${coins} coins!`);
    } else if(ptype==="ECHO_SHOT"){
      // ECHO_SHOT — 10s: each normal tap auto-hits nearest other normal for 70 pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="ECHO_SHOT");
      activePwrRef.current.push({type:"ECHO_SHOT",endsAt:Date.now()+10000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"📡 ECHO SHOT!","#7dd3fc",22);
      spawnParticles(x,y,"#38bdf8",12,"spark");
      sfx("chainBonus");vibrate([8,4,8,4,8]);
      unlock("echo_shot_use");
      setNotif("📡 Echo Shot! Auto-hit on every tap!");
    } else if(ptype==="BURST_SHIELD"){
      // BURST_SHIELD — absorbs next bomb/volatile expiry + 200 pts bonus
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BURST_SHIELD");
      activePwrRef.current.push({type:"BURST_SHIELD",endsAt:Date.now()+30000,charges:1});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💣🛡 BURST SHIELD!","#fbbf24",22);
      spawnParticles(x,y,"#fcd34d",12,"spark");
      sfx("shieldBreak");vibrate([6,3,10,3,6]);
      unlock("burst_shield_use");
      setNotif("💣🛡 Burst Shield! Next explosion absorbed!");
    } else if(ptype==="MAGNET_ORBIT"){
      // MAGNET_ORBIT — 8s: all current non-boss targets pulled toward center in circular orbit
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="MAGNET_ORBIT");
      activePwrRef.current.push({type:"MAGNET_ORBIT",endsAt:Date.now()+8000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌀🧲 MAGNET ORBIT!","#c084fc",22);
      spawnParticles(x,y,"#a78bfa",12,"spark");
      sfx("powerUp");vibrate([8,3,12,3,8]);
      unlock("magnet_orbit_use");
      setNotif("🌀🧲 Magnet Orbit! Targets pulled to center!");
    } else if(ptype==="SCORE_ECHO"){
      // SCORE_ECHO — 12s: every 5th tap echoes for 2× the last pts scored
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_ECHO");
      activePwrRef.current.push({type:"SCORE_ECHO",endsAt:Date.now()+12000,tapCount:0,lastPts:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"📢 SCORE ECHO!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",12,"spark");
      sfx("chainBonus");vibrate([6,3,6,3,10]);
      unlock("score_echo_use");
      setNotif("📢 Score Echo! Every 5th tap echoes 2×!");
    } else if(ptype==="PHANTOM_BURST"){
      // PHANTOM_BURST — 8s: each normal tap spawns a phantom copy giving +30 bonus pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PHANTOM_BURST");
      activePwrRef.current.push({type:"PHANTOM_BURST",endsAt:Date.now()+8000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"👻💥 PHANTOM BURST!","#c084fc",22);
      spawnParticles(x,y,"#a78bfa",12,"spark");
      sfx("chainBonus");vibrate([5,3,8,3,5]);
      unlock("phantom_burst_use");
      setNotif("👻💥 Phantom Burst! Copies spawn on every tap!");
    } else if(ptype==="STREAK_STORM"){
      // STREAK_STORM — 10s: at streak ≥10, bonus pts per tap equal to streak count
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="STREAK_STORM");
      activePwrRef.current.push({type:"STREAK_STORM",endsAt:Date.now()+10000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⛈️ STREAK STORM!","#67e8f9",22);
      spawnParticles(x,y,"#38bdf8",12,"spark");
      sfx("feverStart");vibrate([8,4,12,4,8]);
      unlock("streak_storm_use");
      setNotif("⛈️ Streak Storm! Streak ≥10 = huge bonus!");
    } else if(ptype==="TIME_BUBBLE"){
      // TIME_BUBBLE — 6s: targets inside bubble radius get +5s extra lifetime
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TIME_BUBBLE");
      activePwrRef.current.push({type:"TIME_BUBBLE",endsAt:Date.now()+6000,applied:false});
      setActivePwrDisp([...activePwrRef.current]);
      // Extend lifetime of all current normal targets
      targetsRef.current.filter(o=>!o.dying&&o.type==="normal").forEach(o=>{o.lifetime+=5000;});
      spawnPopup(x,y,"⏱️💫 TIME BUBBLE!","#818cf8",22);
      spawnParticles(x,y,"#c7d2fe",12,"spark");
      particlesRef.current.push({type:"shockwave",x,y,r:20,maxR:200,born:performance.now(),color:"#818cf8",alpha:0.5,life:800});
      sfx("timeWarp");vibrate([6,3,6,3,6]);
      unlock("time_bubble_use");
      setNotif("⏱️💫 Time Bubble! All targets get +5s!");
    } else if(ptype==="CRIT_BOOST"){
      // CRIT_BOOST — 10s: every 3rd tap is a critical hit for 3× pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CRIT_BOOST");
      activePwrRef.current.push({type:"CRIT_BOOST",endsAt:Date.now()+10000,tapCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💥×3 CRIT BOOST!","#ef4444",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("epic");vibrate([8,4,12,4,8]);
      unlock("crit_boost_use");
      setNotif("💥×3 Crit Boost! Every 3rd tap crits!");
    } else if(ptype==="GOLD_RUSH"){
      // GOLD_RUSH — 8s: all normal taps worth 2× pts, treat rarity as LEGENDARY
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="GOLD_RUSH");
      activePwrRef.current.push({type:"GOLD_RUSH",endsAt:Date.now()+8000});
      setActivePwrDisp([...activePwrRef.current]);
      // Tint all normals gold
      targetsRef.current.filter(o=>!o.dying&&o.type==="normal").forEach(o=>{o._goldRush=true;});
      spawnPopup(x,y,"🥇 GOLD RUSH!","#ffd700",24);
      spawnParticles(x,y,"#fde68a",16,"spark");
      sfx("jackpot");vibrate([10,5,10,5,14]);
      unlock("gold_rush_use");
      setNotif("🥇 Gold Rush! All taps 2× pts for 8s!");
    } else if(ptype==="MIRROR_MAZE"){
      // MIRROR_MAZE — 10s: each normal tap also ghost-taps mirrored position
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="MIRROR_MAZE");
      activePwrRef.current.push({type:"MIRROR_MAZE",endsAt:Date.now()+10000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🪞🌀 MIRROR MAZE!","#a78bfa",22);
      spawnParticles(x,y,"#c4b5fd",12,"spark");
      sfx("chainBonus");vibrate([6,3,8,3,6]);
      unlock("mirror_maze_use");
      setNotif("🪞🌀 Mirror Maze! Every tap echoes mirrored!");
    } else if(ptype==="BLAST_WAVE"){
      // BLAST_WAVE — one-shot: instantly kills all current normal targets for 50 pts each
      const gs48=gsRef.current;
      const feverMult48=gs48?.feverActive?2:1;
      const normals48=targetsRef.current.filter(o=>!o.dying&&o.type==="normal");
      const blasted48=normals48.length;
      normals48.forEach(o=>{
        o.dying=performance.now();
        if(gs48)gs48.score+=(50*feverMult48);
        spawnParticles(o.x,o.y,"#f97316",6,"spark");
      });
      const cw48=canvasRef.current?.width||360;const ch48=canvasRef.current?.height||640;
      particlesRef.current.push({type:"shockwave",x:cw48/2,y:ch48/2,r:20,maxR:360,born:performance.now(),color:"#f97316",alpha:0.6,life:700});
      spawnPopup(x,y,`💥 BLAST WAVE! ×${blasted48} +${blasted48*50*feverMult48}`,"#f97316",24);
      spawnParticles(x,y,"#fb923c",20,"spark");
      sfx("bossKill");vibrate([20,10,30,10,20]);setScreenShake(true);setTimeout(()=>setScreenShake(false),400);
      unlock("blast_wave_use");
      if(blasted48>=5)unlock("blast_wave_sweep");
      setNotif(`💥 Blast Wave! ${blasted48} targets cleared!`);
      return;
    } else if(ptype==="TEMPO_SYNC"){
      // TEMPO_SYNC — 10s: targets blink bright/dim; bright phase = 2× pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TEMPO_SYNC");
      activePwrRef.current.push({type:"TEMPO_SYNC",endsAt:Date.now()+10000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🎵⚡ TEMPO SYNC!","#f0abfc",22);
      spawnParticles(x,y,"#e879f9",12,"spark");
      sfx("feverStart");vibrate([6,3,6,3,6]);
      unlock("tempo_sync_use");
      setNotif("🎵⚡ Tempo Sync! Tap in the bright phase for 2×!");
    } else if(ptype==="LIFE_LEECH"){
      // LIFE_LEECH — 15s: every 10 taps restores 1 life (max 5)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_LEECH");
      activePwrRef.current.push({type:"LIFE_LEECH",endsAt:Date.now()+15000,tapCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❤️🩸 LIFE LEECH!","#f87171",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("powerUp");vibrate([6,3,8,3,6]);
      unlock("life_leech_use");
      setNotif("❤️🩸 Life Leech! Restore 1 life every 10 taps!");
    } else if(ptype==="SCORE_RAIN"){
      // SCORE_RAIN — 8s: random score orbs rain from top (20-80 pts each, auto-scored in RAF)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_RAIN");
      activePwrRef.current.push({type:"SCORE_RAIN",endsAt:Date.now()+8000,lastDrop:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌧️⭐ SCORE RAIN!","#fde68a",22);
      spawnParticles(x,y,"#fbbf24",14,"spark");
      sfx("jackpot");vibrate([6,3,6,3,8]);
      unlock("score_rain_use");
      setNotif("🌧️⭐ Score Rain! Stars are falling!");
    } else if(ptype==="CHAIN_MAGNET"){
      // CHAIN_MAGNET — 8s: after each tap, the nearest normal target auto-taps within 0.6s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CHAIN_MAGNET");
      activePwrRef.current.push({type:"CHAIN_MAGNET",endsAt:Date.now()+8000,pullCount:0,lastPullAt:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🧲⛓️ CHAIN MAGNET!","#34d399",22);
      spawnParticles(x,y,"#6ee7b7",12,"spark");
      sfx("powerUp");vibrate([5,3,8,3,5]);
      unlock("chain_magnet_use");
      setNotif("🧲⛓️ Chain Magnet! Nearest target auto-pulled!");
    } else if(ptype==="SCORE_SURGE"){
      // SCORE_SURGE — 10s: multiplier doubles every 5 consecutive taps (×1→×2→×4→×8); resets on miss
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_SURGE");
      activePwrRef.current.push({type:"SCORE_SURGE",endsAt:Date.now()+10000,tapCount:0,mult:1});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💹 SCORE SURGE!","#4ade80",22);
      spawnParticles(x,y,"#86efac",12,"spark");
      sfx("powerUp");vibrate([6,3,6,3,8]);
      unlock("score_surge_use");
      setNotif("💹 Score Surge! Mult doubles every 5 taps!");
    } else if(ptype==="ORBIT_SHIELD"){
      // ORBIT_SHIELD — 10s: absorbs next bomb explosion (like a bomb-specific shield)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="ORBIT_SHIELD");
      activePwrRef.current.push({type:"ORBIT_SHIELD",endsAt:Date.now()+10000,charges:1});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🛸🛡 ORBIT SHIELD!","#93c5fd",22);
      spawnParticles(x,y,"#bfdbfe",12,"spark");
      sfx("powerUp");vibrate([5,3,8,3,5]);
      unlock("orbit_shield_use");
      setNotif("🛸🛡 Orbit Shield! Next bomb absorbed!");
    } else if(ptype==="ECHO_WAVE"){
      // ECHO_WAVE — 8s: each normal tap sends shockwave scoring 25pts per nearby target
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="ECHO_WAVE");
      activePwrRef.current.push({type:"ECHO_WAVE",endsAt:Date.now()+8000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"〰️💥 ECHO WAVE!","#67e8f9",22);
      spawnParticles(x,y,"#a5f3fc",12,"spark");
      sfx("powerUp");vibrate([6,3,6,3,6]);
      unlock("echo_wave_use");
      setNotif("〰️💥 Echo Wave! Tap sends scoring shockwave!");
    } else if(ptype==="COMBO_BURST"){
      // COMBO_BURST — instant: awards 20pts × current streak as burst bonus
      const gs52=gsRef.current;
      const streak52=gs52?.streak||0;
      const burstBns=Math.max(20,streak52*20);
      if(gs52)gs52.score+=burstBns;
      spawnPopup(x,y,`💥⛓️ BURST! +${burstBns}`,"#fb923c",24);
      spawnParticles(x,y,"#fed7aa",16,"spark");
      sfx("chainBonus");vibrate([8,4,12,4,8]);
      unlock("combo_burst_use");
      if(streak52>=15)unlock("combo_burst_big");
      setNotif(`💥⛓️ Combo Burst! +${burstBns} pts!`);
    } else if(ptype==="PHASE_LOCK"){
      // PHASE_LOCK — 10s: all targets freeze in place; tapping them scores 2×
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PHASE_LOCK");
      activePwrRef.current.push({type:"PHASE_LOCK",endsAt:Date.now()+10000,scoreGained:0});
      setActivePwrDisp([...activePwrRef.current]);
      targetsRef.current.forEach(t=>{if(!t.dying&&t.moving){t._prePhaseLockVx=t.vx;t._prePhaseLockVy=t.vy;t.vx=0;t.vy=0;}});
      spawnPopup(x,y,"🔒⏸ PHASE LOCK!","#a78bfa",22);
      spawnParticles(x,y,"#c4b5fd",12,"spark");
      sfx("freeze");vibrate([10,5,15,5,10]);
      unlock("phase_lock_use");
      setNotif("🔒⏸ Phase Lock! All targets frozen for 2× score!");
    } else if(ptype==="SCORE_LEECH"){
      // SCORE_LEECH — 12s: normal target expiry grants +30pts instead of nothing
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_LEECH");
      activePwrRef.current.push({type:"SCORE_LEECH",endsAt:Date.now()+12000,totalGained:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔋💰 SCORE LEECH!","#a3e635",22);
      spawnParticles(x,y,"#d9f99d",12,"spark");
      sfx("powerUp");vibrate([5,3,8,3,5]);
      unlock("score_leech_use");
      setNotif("🔋💰 Score Leech! Target expiries give bonus pts!");
    } else if(ptype==="SPLIT_SHOT"){
      // SPLIT_SHOT — 8s: each tap auto-taps 1 additional nearest target for 50pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SPLIT_SHOT");
      activePwrRef.current.push({type:"SPLIT_SHOT",endsAt:Date.now()+8000,autoCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🎯🎯 SPLIT SHOT!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",12,"spark");
      sfx("powerUp");vibrate([6,3,6,3,6]);
      unlock("split_shot_use");
      setNotif("🎯🎯 Split Shot! Each tap also hits nearest target!");
    } else if(ptype==="FEVER_STORM"){
      // FEVER_STORM — instant: activates fever mode for 6s regardless of fever bar
      const gs54=gsRef.current;
      if(gs54){
        gs54.feverActive=true;gs54.feverTimeLeft=6000;gs54.feverCount=(gs54.feverCount||0)+1;
        gs54.sessionStats.feverCount=(gs54.sessionStats?.feverCount||0)+1;
      }
      setFeverBorder(true);setTimeout(()=>setFeverBorder(false),6000);
      sfx("feverStart");vibrate([10,5,15,5,10]);
      spawnPopup(x,y,"🌪️🌡️ FEVER STORM!","#fb923c",24);
      spawnParticles(x,y,"#fbbf24",20,"spark");
      unlock("fever_storm_use");
      setNotif("🌪️🌡️ FEVER STORM! 6s fever activated!");
    } else if(ptype==="RARITY_BOOST"){
      // RARITY_BOOST — 15s: all new targets spawn as RARE or higher
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="RARITY_BOOST");
      activePwrRef.current.push({type:"RARITY_BOOST",endsAt:Date.now()+15000,legCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"✨⬆️ RARITY BOOST!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",14,"spark");
      sfx("lucky");vibrate([6,3,8,3,8]);
      unlock("rarity_boost_use");
      setNotif("✨⬆️ Rarity Boost! All targets are RARE+!");
    } else if(ptype==="TWIN_FEVER"){
      // TWIN_FEVER — instant: activates fever for 8s + spawns 5 legendary targets
      const gs55=gsRef.current;
      if(gs55){gs55.feverActive=true;gs55.feverTimeLeft=8000;gs55.sessionStats.feverCount=(gs55.sessionStats?.feverCount||0)+1;}
      setFeverBorder(true);setTimeout(()=>setFeverBorder(false),8000);
      // Spawn 5 legendary normals
      const cw55=canvasRef.current?.width||360;const ch55=canvasRef.current?.height||640;
      for(let i=0;i<5;i++){
        const tx55=Math.max(40,Math.min(cw55-40,40+Math.random()*(cw55-80)));
        const ty55=Math.max(80,Math.min(ch55-80,80+Math.random()*(ch55-160)));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"normal",
          rarity:RARITY.LEGENDARY,x:tx55,y:ty55,radius:BASE_R*RARITY.LEGENDARY.size,
          color:RARITY.LEGENDARY.color,glow:RARITY.LEGENDARY.glow,
          lifetime:4000,spawnedAt:Date.now(),born:performance.now(),moving:false,ghost:false,
          vx:0,vy:0,trail:null,hitsLeft:1,maxHits:1,dying:null,
          worldId:null,worldColor:null,bossPattern:null,bossPhase:1,cx:tx55,cy:ty55,
          anticipateMs:0,pwrType:null});
      }
      sfx("legendary");vibrate([10,5,15,5,10,5,15]);
      spawnPopup(x,y,"🌡️×2 TWIN FEVER!","#f97316",24);
      spawnParticles(x,y,"#fde68a",20,"spark");
      unlock("twin_fever_use");
      setNotif("🌡️×2 Twin Fever! 5 Legendary targets + 8s fever!");
    } else if(ptype==="REFLECT_WAVE"){
      // REFLECT_WAVE — 10s: bomb expiry sends shockwave scoring 100pts per nearby normal
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="REFLECT_WAVE");
      activePwrRef.current.push({type:"REFLECT_WAVE",endsAt:Date.now()+10000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💥🔄 REFLECT WAVE!","#f87171",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("powerUp");vibrate([6,3,6,3,6]);
      unlock("reflect_wave_use");
      setNotif("💥🔄 Reflect Wave! Bombs now score on expiry!");
    } else if(ptype==="NOVA_BOOST"){
      // NOVA_BOOST — 12s: every 3rd tap on normal targets fires a mini nova shockwave (+150pts)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="NOVA_BOOST");
      activePwrRef.current.push({type:"NOVA_BOOST",endsAt:Date.now()+12000,novaCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💥🌟 NOVA BOOST!","#38bdf8",22);
      spawnParticles(x,y,"#7dd3fc",12,"spark");
      sfx("powerUp");vibrate([8,4,8]);
      unlock("nova_boost_use");
      setNotif("💥🌟 Nova Boost! Every 3rd tap fires a nova!");
    } else if(ptype==="STREAK_LOCK"){
      // STREAK_LOCK — 10s: misses don't reset streak
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="STREAK_LOCK");
      activePwrRef.current.push({type:"STREAK_LOCK",endsAt:Date.now()+10000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔐⛓️ STREAK LOCK!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",12,"spark");
      sfx("powerUp");vibrate([6,3,6]);
      unlock("streak_lock_use");
      setNotif("🔐⛓️ Streak Lock! Misses won't reset your streak!");
    } else if(ptype==="MAGNET_STORM"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="MAGNET_STORM");
      activePwrRef.current.push({type:"MAGNET_STORM",endsAt:Date.now()+5000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌪️🧲 MAGNET STORM!","#38bdf8",22);
      spawnParticles(x,y,"#7dd3fc",14,"spark");
      sfx("powerUp");vibrate([8,4,12,4,8]);
      unlock("magnet_storm_use");
      setNotif("🌪️🧲 Magnet Storm! Targets pulled toward your taps!");
    } else if(ptype==="SCORE_FREEZE"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_FREEZE");
      const scoreFloor57=gsRef.current?.score||0;
      activePwrRef.current.push({type:"SCORE_FREEZE",endsAt:Date.now()+8000,scoreFloor:scoreFloor57});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❄️🔒 SCORE FREEZE!","#67e8f9",22);
      spawnParticles(x,y,"#cffafe",12,"dot");
      sfx("powerUp");vibrate([6,3,6]);
      unlock("score_freeze_use");
      setNotif("❄️🔒 Score Freeze! Your score is locked in place!");
    } else if(ptype==="CHAIN_FEVER"){
      // CHAIN_FEVER — 15s: hitting 5+ targets in 2s triggers instant fever
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CHAIN_FEVER");
      activePwrRef.current.push({type:"CHAIN_FEVER",endsAt:Date.now()+15000,chainTaps:[],triggered:false});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⛓️🌡️ CHAIN FEVER!","#f97316",22);
      spawnParticles(x,y,"#fdba74",12,"spark");
      sfx("powerUp");vibrate([6,4,8,4,6]);
      unlock("chain_fever_use");
      setNotif("⛓️🌡️ Chain Fever! Hit 5 targets in 2s to ignite!");
    } else if(ptype==="LIFE_BOMB"){
      // LIFE_BOMB — next bomb tap gives +1 life instead of penalty (one charge)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_BOMB");
      activePwrRef.current.push({type:"LIFE_BOMB",endsAt:Date.now()+30000,charges:1});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💣❤️ LIFE BOMB!","#f87171",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("powerUp");vibrate([8,4,8]);
      unlock("life_bomb_use");
      setNotif("💣❤️ Life Bomb! Next bomb tap restores a life!");
    } else if(ptype==="TURBO_TAP"){
      // TURBO_TAP — 3 charges: next 3 normal taps score ×3
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TURBO_TAP");
      activePwrRef.current.push({type:"TURBO_TAP",endsAt:Date.now()+30000,charges:3,used:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⚡⚡⚡ TURBO TAP!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",14,"spark");
      sfx("powerUp");vibrate([4,2,4,2,4]);
      unlock("turbo_tap_use");
      setNotif("⚡⚡⚡ Turbo Tap! Next 3 taps score ×3!");
    } else if(ptype==="COIN_MAGNET"){
      // COIN_MAGNET — 10s: auto-collect all coin drops at 2× value
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COIN_MAGNET");
      activePwrRef.current.push({type:"COIN_MAGNET",endsAt:Date.now()+10000,coinsCollected:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🧲🪙 COIN MAGNET!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",12,"spark");
      sfx("coin");vibrate([6,3,6]);
      unlock("coin_magnet_use");
      setNotif("🧲🪙 Coin Magnet! Coins auto-collected at 2× value!");
    } else if(ptype==="BLAST_RADIUS"){
      // BLAST_RADIUS — next 5 taps also score all targets within 80px radius
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BLAST_RADIUS");
      activePwrRef.current.push({type:"BLAST_RADIUS",endsAt:Date.now()+12000,charges:5,aoeHits:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💥🌐 BLAST RADIUS!","#f97316",22);
      spawnParticles(x,y,"#fdba74",14,"spark");
      sfx("tap");vibrate([10,5,10,5,10]);
      unlock("blast_radius_use");
      setNotif("💥 Blast Radius! Next 5 taps hit area of 80px!");
    } else if(ptype==="SCORE_ACCELERATOR"){
      // SCORE_ACCELERATOR — 8 taps with building +10% each; resets after 8
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_ACCELERATOR");
      activePwrRef.current.push({type:"SCORE_ACCELERATOR",endsAt:Date.now()+15000,tapCount:0,peakReached:false});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"📈⚡ ACCELERATOR!","#34d399",22);
      spawnParticles(x,y,"#6ee7b7",14,"spark");
      sfx("comboNote",4);vibrate([8,4,8]);
      unlock("score_accel_use");
      setNotif("📈 Score Accelerator! Each tap = +10% more score (×8 max)!");
    } else if(ptype==="LIFE_CHAIN"){
      // LIFE_CHAIN — every 5 taps restore 1 life, up to 3 times (15 taps)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_CHAIN");
      activePwrRef.current.push({type:"LIFE_CHAIN",endsAt:Date.now()+20000,tapCount:0,healsGiven:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❤️⛓️ LIFE CHAIN!","#f87171",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("tap");vibrate([6,3,6,3,6]);
      unlock("life_chain_use");
      setNotif("❤️ Life Chain! Every 5 taps restores 1 life (3 times)!");
    } else if(ptype==="SCORE_MIRROR"){
      // SCORE_MIRROR — 10s: mirrors all score gains into a ghost bank; pays out at end
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_MIRROR");
      activePwrRef.current.push({type:"SCORE_MIRROR",endsAt:Date.now()+10000,mirrorBank:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🪞💯 SCORE MIRROR!","#c084fc",22);
      spawnParticles(x,y,"#e9d5ff",12,"spark");
      sfx("lucky");vibrate([8,4,12]);
      unlock("score_mirror_use");
      setNotif("🪞 Score Mirror! All score gains banked — pays out at end!");
    } else if(ptype==="FEVER_LOCK"){
      // FEVER_LOCK — maintains fever for 8s regardless of natural expiry
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="FEVER_LOCK");
      activePwrRef.current.push({type:"FEVER_LOCK",endsAt:Date.now()+8000,triggered:false});
      setActivePwrDisp([...activePwrRef.current]);
      // Immediately restore fever
      const gs62f=gsRef.current;if(gs62f){gs62f.feverActive=true;gs62f.feverTimeLeft=8000;setFeverBorder(true);}
      spawnPopup(x,y,"🔒🌡️ FEVER LOCK!","#f43f5e",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("feverStart");vibrate([8,4,12,4,8]);
      unlock("fever_lock_use");
      setNotif("🔒 Fever Lock! Fever locked on for 8 seconds!");
    } else if(ptype==="STREAK_SURGE"){
      // STREAK_SURGE — 10 taps: bonus = pts×(streak/5) on each tap
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="STREAK_SURGE");
      activePwrRef.current.push({type:"STREAK_SURGE",endsAt:Date.now()+15000,charges:10,peaked:false});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔥⬆️ STREAK SURGE!","#fb923c",22);
      spawnParticles(x,y,"#fed7aa",12,"spark");
      sfx("comboNote",6);vibrate([8,4,8]);
      unlock("streak_surge_use");
      setNotif("🔥 Streak Surge! Score bonus = streak × your combo for 10 taps!");
    } else if(ptype==="COMBO_ECHO"){
      // COMBO_ECHO — 12s: each combo tap auto-hits the nearest normal target
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COMBO_ECHO");
      activePwrRef.current.push({type:"COMBO_ECHO",endsAt:Date.now()+12000,echoCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔊⛓️ COMBO ECHO!","#a78bfa",22);
      spawnParticles(x,y,"#c4b5fd",12,"spark");
      sfx("tap");vibrate([8,4,8]);
      unlock("combo_echo_use");
      setNotif("🔊 Combo Echo! Each combo tap auto-hits the nearest target!");
    } else if(ptype==="GOLD_SHIELD"){
      // GOLD_SHIELD — 15s: life losses convert to +100 coins instead
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="GOLD_SHIELD");
      activePwrRef.current.push({type:"GOLD_SHIELD",endsAt:Date.now()+15000,procCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🛡️🥇 GOLD SHIELD!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",12,"spark");
      sfx("coin");vibrate([6,3,6,3,12]);
      unlock("gold_shield_use");
      setNotif("🛡️ Gold Shield! Missed lives become +100 coins for 15s!");
    } else if(ptype==="PHANTOM_LIFE"){
      // PHANTOM_LIFE — one-time: if lives reach 0, restore to 1 (once per game)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PHANTOM_LIFE");
      activePwrRef.current.push({type:"PHANTOM_LIFE",endsAt:Date.now()+99999000,used:false});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"👻❤️ PHANTOM LIFE!","#c084fc",22);
      spawnParticles(x,y,"#e9d5ff",12,"spark");
      sfx("tap");vibrate([8,4,12]);
      unlock("phantom_life_use");
      setNotif("👻 Phantom Life! One automatic rescue if you run out of lives!");
    } else if(ptype==="SCORE_DOUBLER"){
      // SCORE_DOUBLER — next 3 normal targets score ×2
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_DOUBLER");
      activePwrRef.current.push({type:"SCORE_DOUBLER",endsAt:Date.now()+30000,charges:3,used:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"×2🎯 SCORE DOUBLER!","#34d399",22);
      spawnParticles(x,y,"#6ee7b7",12,"spark");
      sfx("comboNote",3);vibrate([6,3,6]);
      unlock("score_doubler_use");
      setNotif("×2 Score Doubler! Next 3 targets score double!");
    } else if(ptype==="MAGNET_BLITZ"){
      // MAGNET_BLITZ — 6s: each tap auto-collects closest non-bomb within 120px
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="MAGNET_BLITZ");
      activePwrRef.current.push({type:"MAGNET_BLITZ",endsAt:Date.now()+6000,pullCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🧲⚡ MAGNET BLITZ!","#ec4899",22);
      spawnParticles(x,y,"#fbcfe8",12,"spark");
      sfx("tap");vibrate([8,4,10,4,8]);
      unlock("magnet_blitz_use");
      setNotif("🧲 Magnet Blitz! Auto-collect nearest targets within 120px!");
    } else if(ptype==="CRIT_STORM"){
      // CRIT_STORM — 15s: 20% chance on each tap for 3× crit bonus
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CRIT_STORM");
      activePwrRef.current.push({type:"CRIT_STORM",endsAt:Date.now()+15000,critCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💥🌩️ CRIT STORM!","#f43f5e",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("comboNote",5);vibrate([10,5,15]);
      unlock("crit_storm_use");
      setNotif("💥 Crit Storm! 20% chance of 3× crit on each tap!");
    } else if(ptype==="BONUS_WAVE"){
      // BONUS_WAVE — spawn 5 bonus orbs worth 50pts each; auto-score in 5s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BONUS_WAVE");
      const bwPwr={type:"BONUS_WAVE",endsAt:Date.now()+5000,orbsLeft:5,orbs:[],collected:0};
      activePwrRef.current.push(bwPwr);
      setActivePwrDisp([...activePwrRef.current]);
      // Spawn 5 bonus orbs
      const cw66=canvasRef.current?.width||360;const ch66=canvasRef.current?.height||640;
      const gs66bw=gsRef.current;
      for(let bi=0;bi<5;bi++){
        const bx66=Math.max(30,Math.min(cw66-30,40+Math.random()*(cw66-80)));
        const by66=Math.max(30,Math.min(ch66-30,40+Math.random()*(ch66-80)));
        const bid66=Math.random().toString(36).slice(2);
        bwPwr.orbs.push(bid66);
        targetsRef.current.push({id:bid66,type:"bonus_orb",rarity:null,
          x:bx66,y:by66,radius:BASE_R*0.65,color:"#10b981",glow:"#d1fae5",
          lifetime:5000,spawnedAt:Date.now(),born:performance.now(),
          moving:false,ghost:false,vx:0,vy:0,trail:null,
          hitsLeft:1,maxHits:1,dying:null,worldId:null,worldColor:null,
          bossPattern:null,bossPhase:1,cx:bx66,cy:by66,anticipateMs:0,pwrType:null,
          _bonusOrb:true,_bwRef:bwPwr});
      }
      spawnPopup(x,y,"🌊🎁 BONUS WAVE!","#10b981",22);
      spawnParticles(x,y,"#6ee7b7",14,"spark");
      sfx("coin");vibrate([8,4,8]);
      unlock("bonus_wave_use");
      setNotif("🌊 Bonus Wave! 5 bonus orbs spawned — collect them!");
    } else if(ptype==="TAP_FURY"){
      // TAP_FURY — 8s: all taps score as if fever active (2× multiplier forced)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TAP_FURY");
      activePwrRef.current.push({type:"TAP_FURY",endsAt:Date.now()+8000,bonusScore:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔥👊 TAP FURY!","#ef4444",22);
      spawnParticles(x,y,"#fca5a5",14,"spark");
      sfx("feverStart");vibrate([10,5,15,5,10]);
      unlock("tap_fury_use");
      setNotif("🔥 Tap Fury! All taps score at 2× for 8 seconds!");
    } else if(ptype==="SCORE_SPIKE"){
      // SCORE_SPIKE — 10s: first tap in each 2s window = 5×
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_SPIKE");
      activePwrRef.current.push({type:"SCORE_SPIKE",endsAt:Date.now()+10000,lastWindow:0,spikeCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"📊⚡ SCORE SPIKE!","#f43f5e",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("comboNote",6);vibrate([8,4,12]);
      unlock("score_spike_use");
      setNotif("📊 Score Spike! First tap every 2s deals 5× score!");
    } else if(ptype==="LIFE_BURST"){
      // LIFE_BURST — instant: restore 2 lives + spawn 3 mini healing orbs
      const gs67lb=gsRef.current;
      if(gs67lb)gs67lb.lives=Math.min(5,gs67lb.lives+2);
      spawnPopup(x,y,"❤️💥 LIFE BURST!","#f87171",22);
      spawnParticles(x,y,"#fca5a5",14,"spark");
      sfx("tap");vibrate([8,4,12,4,8]);
      unlock("life_burst_use");unlock("life_burst_heal");
      // Spawn 3 mini heal orbs
      const cw67=canvasRef.current?.width||360;const ch67=canvasRef.current?.height||640;
      for(let hi=0;hi<3;hi++){
        const hx67=Math.max(30,Math.min(cw67-30,x+(hi-1)*80));
        const hy67=Math.max(30,Math.min(ch67-30,y-60));
        const hid67=Math.random().toString(36).slice(2);
        targetsRef.current.push({id:hid67,type:"heal_orb",rarity:null,
          x:hx67,y:hy67,radius:BASE_R*0.55,color:"#4ade80",glow:"#bbf7d0",
          lifetime:3000,spawnedAt:Date.now(),born:performance.now(),
          moving:false,ghost:false,vx:0,vy:0,trail:null,
          hitsLeft:1,maxHits:1,dying:null,worldId:null,worldColor:null,
          bossPattern:null,bossPhase:1,cx:hx67,cy:hy67,anticipateMs:0,pwrType:null,
          _healOrb:true});
      }
      setNotif("❤️❤️ Life Burst! +2 lives restored!");
    } else if(ptype==="PHASE_BURST"){
      // PHASE_BURST — next 3 taps score bombs safely (instead of losing life)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PHASE_BURST");
      activePwrRef.current.push({type:"PHASE_BURST",endsAt:Date.now()+20000,charges:3});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💠⚡ PHASE BURST!","#38bdf8",22);
      spawnParticles(x,y,"#7dd3fc",12,"spark");
      sfx("comboNote",5);vibrate([6,4,10]);
      unlock("phase_burst_use");
      setNotif("💠 Phase Burst! Next 3 bomb taps = safe + score!");
    } else if(ptype==="VORTEX_TAP"){
      // VORTEX_TAP — each tap for 8s creates a small pull zone attracting targets
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="VORTEX_TAP");
      activePwrRef.current.push({type:"VORTEX_TAP",endsAt:Date.now()+8000,totalPulled:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌀👊 VORTEX TAP!","#818cf8",22);
      spawnParticles(x,y,"#a5b4fc",12,"spark");
      sfx("comboNote",7);vibrate([8,4,6,4,8]);
      unlock("vortex_tap_use");
      setNotif("🌀 Vortex Tap! Taps pull nearby targets!");
    } else if(ptype==="COMBO_SURGE"){
      // COMBO_SURGE — combo decay is frozen for 6s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COMBO_SURGE");
      activePwrRef.current.push({type:"COMBO_SURGE",endsAt:Date.now()+6000,maxStreakDuring:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔥⛓️ COMBO SURGE!","#f97316",22);
      spawnParticles(x,y,"#fed7aa",12,"spark");
      sfx("comboNote",7);vibrate([8,4,8,4,8]);
      unlock("combo_surge_use");
      setNotif("🔥 Combo Surge! Streak won't decay for 6s!");
    } else if(ptype==="SCORE_MAGNET"){
      // SCORE_MAGNET — expired targets grant 30% score for 8s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_MAGNET");
      activePwrRef.current.push({type:"SCORE_MAGNET",endsAt:Date.now()+8000,savedCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🧲💰 SCORE MAGNET!","#6ee7b7",22);
      spawnParticles(x,y,"#a7f3d0",12,"spark");
      sfx("comboNote",5);vibrate([6,4,8]);
      unlock("score_magnet_use");
      setNotif("🧲 Score Magnet! Expired targets still score 30%!");
    } else if(ptype==="LIFE_OVERFLOW"){
      // LIFE_OVERFLOW — fill lives to max; overflow = 200pts each
      const loGs76=gsRef.current;const cfgLO=levelCfgRef.current;
      if(loGs76&&cfgLO){
        const maxLives76=cfgLO.lives||3;
        const deficit76=Math.max(0,maxLives76-loGs76.lives);
        const overflow76=Math.max(0,-deficit76); // 0 or more lives over max
        if(deficit76>0){
          loGs76.lives=maxLives76;
          spawnPopup(x,y,`❤️ FULL +${deficit76}❤️`,"#f87171",22);
        } else {
          // Already at max — give bonus
          const ovPts76=200;
          loGs76.score+=ovPts76;
          spawnPopup(x,y,`❤️ OVERFLOW +${ovPts76}`,"#f87171",22);
          unlock("life_overflow_bonus");
        }
        spawnParticles(x,y,"#fca5a5",12,"spark");
        sfx("comboNote",4);vibrate([6,4,10]);
        unlock("life_overflow_use");
        setNotif("❤️ Life Overflow! Lives filled to max!");
      }
    } else if(ptype==="STREAK_HEAL"){
      // STREAK_HEAL — restores 1 life for every 5 streak; minimum 1
      const shGS77=gsRef.current;const cfgSH77=levelCfgRef.current;
      if(shGS77&&cfgSH77){
        const maxSH77=cfgSH77.lives||3;
        const streakLives77=Math.max(1,Math.floor((shGS77.streak||0)/5));
        const prevLives77=shGS77.lives;
        shGS77.lives=Math.min(maxSH77,shGS77.lives+streakLives77);
        const gained77=shGS77.lives-prevLives77;
        spawnPopup(x,y,`❤️🔥 +${gained77} LIFE${gained77!==1?"S":""}!`,"#f87171",22);
        spawnParticles(x,y,"#fca5a5",10,"spark");
        sfx("comboNote",4);vibrate([6,4,8]);
        unlock("streak_heal_use");
        if((shGS77._streakHealTotal77=(shGS77._streakHealTotal77||0)+gained77)>=3)unlock("streak_heal_full");
        setNotif(`❤️ Streak Heal! +${gained77} life restored!`);
      }
    } else if(ptype==="TEMPO_BURST"){
      // TEMPO_BURST — for 7s, every tap adds streak×2 bonus pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TEMPO_BURST");
      activePwrRef.current.push({type:"TEMPO_BURST",endsAt:Date.now()+7000,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🎵💥 TEMPO BURST!","#f0abfc",22);
      spawnParticles(x,y,"#e879f9",12,"spark");
      sfx("comboNote",5);vibrate([6,4,8,4,6]);
      unlock("tempo_burst_use");
      setNotif("🎵 Tempo Burst! Each tap earns streak×2 bonus pts!");
    } else if(ptype==="CRIT_SURGE"){
      // CRIT_SURGE — for 10s, every tap has 30% chance to deal 3× damage
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CRIT_SURGE");
      activePwrRef.current.push({type:"CRIT_SURGE",endsAt:Date.now()+10000,critCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💥🔥 CRIT SURGE!","#fb923c",22);
      spawnParticles(x,y,"#fed7aa",12,"spark");
      sfx("comboNote",5);vibrate([8,4,10,4,8]);
      unlock("crit_surge_use");
      setNotif("💥 Crit Surge! 30% chance for 3× pts on each tap!");
    } else if(ptype==="LIFE_PULSE"){
      // LIFE_PULSE — for 12s, every 5th tap restores 1 life
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_PULSE");
      activePwrRef.current.push({type:"LIFE_PULSE",endsAt:Date.now()+12000,tapCount:0,livesGiven:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💓⚡ LIFE PULSE!","#fb7185",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("comboNote",4);vibrate([6,4,10]);
      unlock("life_pulse_use");
      setNotif("💓 Life Pulse! Every 5th tap restores 1 life for 12s!");
    } else if(ptype==="CHAIN_WAVE"){
      // CHAIN_WAVE — for 8s, each tap auto-scores the nearest valid target too (chain effect)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CHAIN_WAVE");
      activePwrRef.current.push({type:"CHAIN_WAVE",endsAt:Date.now()+8000,chainCount:0,lastChainTs:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌊⛓️ CHAIN WAVE!","#38bdf8",22);
      spawnParticles(x,y,"#7dd3fc",12,"spark");
      sfx("chainBonus");vibrate([6,4,10,4,6]);
      unlock("chain_wave_use");
      setNotif("🌊 Chain Wave! Each tap also scores the nearest target!");
    } else if(ptype==="SCORE_OVERDRIVE"){
      // SCORE_OVERDRIVE — for 6s, multiply all tap scores by current streak (capped at ×5)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_OVERDRIVE");
      activePwrRef.current.push({type:"SCORE_OVERDRIVE",endsAt:Date.now()+6000,totalAdded:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"📈🔥 OVERDRIVE!","#f87171",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("comboNote",6);vibrate([8,4,12,4,8]);
      unlock("score_od_use");
      setNotif("📈 Score Overdrive! All taps scaled by streak (up to ×5) for 6s!");
    } else if(ptype==="PHANTOM_STRIKE"){
      // PHANTOM_STRIKE — for 8s, tapping a ghost/phantom target scores double + destroys nearby
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PHANTOM_STRIKE");
      activePwrRef.current.push({type:"PHANTOM_STRIKE",endsAt:Date.now()+8000,ghostHits:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"👻⚡ PHANTOM STRIKE!","#a78bfa",22);
      spawnParticles(x,y,"#ddd6fe",12,"spark");
      sfx("comboNote",5);vibrate([6,4,8,4,6]);
      unlock("phantom_strike_use");
      setNotif("👻 Phantom Strike! Ghost/phantom taps score double + chain nearby!");
    } else if(ptype==="COIN_SURGE"){
      // COIN_SURGE — for 10s, each tap also earns 1–3 coins
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COIN_SURGE");
      activePwrRef.current.push({type:"COIN_SURGE",endsAt:Date.now()+10000,totalCoins:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🪙🚀 COIN SURGE!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",12,"spark");
      sfx("coin");vibrate([6,4,10,4,6]);
      unlock("coin_surge_use");
      setNotif("🪙 Coin Surge! Every tap earns 1–3 bonus coins for 10s!");
    } else if(ptype==="FREEZE_SURGE"){
      // FREEZE_SURGE — instant: freeze all targets 3s; then score each for 40pts
      const fsGs81=gsRef.current;
      if(fsGs81){
        const frozenFS81=targetsRef.current.filter(o=>!o.dying&&o.type!=="boss"&&o.type!=="bomb");
        frozenFS81.forEach(o=>{fsGs81.score+=40;});
        activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="FREEZE");
        activePwrRef.current.push({type:"FREEZE",endsAt:Date.now()+3000});
        setActivePwrDisp([...activePwrRef.current]);
        const fsTotal81=frozenFS81.length;
        spawnPopup(x,y,`❄️⚡ FREEZE! ${fsTotal81} targets +${fsTotal81*40}pts`,"#93c5fd",22);
        spawnParticles(x,y,"#bfdbfe",12,"spark");
        sfx("feverStart");vibrate([8,4,10,4,8]);
        unlock("freeze_surge_use");
        if(fsTotal81>=5)unlock("freeze_surge_big");
        setNotif(`❄️ Freeze Surge! ${fsTotal81} targets frozen + scored!`);
      }
    } else if(ptype==="LUCKY_STRIKE"){
      // LUCKY_STRIKE — instant: 25% jackpot (800pts), 50% double next 3 taps, 25% life+1
      const rand81=Math.random();
      const lsGs81=gsRef.current;const cfgLS81=levelCfgRef.current;
      if(rand81<0.25){
        // Jackpot!
        if(lsGs81)lsGs81.score+=800;
        spawnPopup(x,y,"🍀 JACKPOT! +800","#fbbf24",28);
        spawnParticles(x,y,"#fde68a",18,"spark");
        sfx("legendary");vibrate([10,4,16,4,10]);
        setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),1200);
        unlock("lucky_strike_jack");
      } else if(rand81<0.75){
        // Double next 3 taps
        activePwrRef.current.push({type:"SCORE_DOUBLER",endsAt:Date.now()+30000,tapCharges:3});
        setActivePwrDisp([...activePwrRef.current]);
        spawnPopup(x,y,"🍀 ×2 NEXT 3 TAPS!","#4ade80",24);
        spawnParticles(x,y,"#86efac",12,"spark");
        sfx("comboNote",5);vibrate([6,4,10]);
      } else {
        // Life +1
        if(lsGs81&&cfgLS81){const maxL81=cfgLS81.lives||3;lsGs81.lives=Math.min(maxL81,lsGs81.lives+1);}
        spawnPopup(x,y,"🍀 LUCKY LIFE! +1❤️","#f87171",24);
        spawnParticles(x,y,"#fca5a5",12,"spark");
        sfx("comboNote",4);vibrate([6,4,8]);
      }
      unlock("lucky_strike_use");
      setNotif("🍀 Lucky Strike! Jackpot, double taps, or extra life!");
    } else if(ptype==="ECHO_BURST"){
      // ECHO_BURST — for 8s, each tap echoes at full value after 0.4s (no extra target needed)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="ECHO_BURST");
      activePwrRef.current.push({type:"ECHO_BURST",endsAt:Date.now()+8000,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔊💥 ECHO BURST!","#f0abfc",22);
      spawnParticles(x,y,"#e879f9",12,"spark");
      sfx("comboNote",5);vibrate([6,4,8,4,6]);
      unlock("echo_burst_use");
      setNotif("🔊 Echo Burst! Every tap echoes again after 0.4s for 8s!");
    } else if(ptype==="SHIELD_SURGE"){
      // SHIELD_SURGE — grants 4-hit shield wall for 25s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_WALL"&&p.type!=="SHIELD_SURGE");
      activePwrRef.current.push({type:"SHIELD_WALL",endsAt:Date.now()+25000,hitsLeft:4,_surgeSrc:true});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🛡️🚀 SHIELD SURGE! ×4","#a78bfa",22);
      spawnParticles(x,y,"#ddd6fe",12,"spark");
      sfx("comboNote",4);vibrate([8,4,12,4,8]);
      unlock("shield_surge_use");
      setNotif("🛡️ Shield Surge! 4-hit shield wall for 25s!");
    } else if(ptype==="SCORE_SNAP"){
      // SCORE_SNAP — for 12s, save 50% of score that would be lost on misses
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_SNAP");
      activePwrRef.current.push({type:"SCORE_SNAP",endsAt:Date.now()+12000,savedPts:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"📸💰 SCORE SNAP!","#a3e635",22);
      spawnParticles(x,y,"#d9f99d",12,"spark");
      sfx("comboNote",4);vibrate([6,4,10]);
      unlock("score_snap_use");
      setNotif("📸 Score Snap! Saves 50% of score on each miss for 12s!");
    } else if(ptype==="LIFE_GUARD"){
      // LIFE_GUARD — for 30s, prevent game over once (revive with 1 life)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_GUARD");
      activePwrRef.current.push({type:"LIFE_GUARD",endsAt:Date.now()+30000,used:false});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❤️🛡️ LIFE GUARD!","#fb7185",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("comboNote",4);vibrate([6,4,10,4,6]);
      unlock("life_guard_use");
      setNotif("❤️ Life Guard! Prevents game over once within 30s!");
    } else if(ptype==="BURST_FEVER"){
      // BURST_FEVER — instant fever burst for 6s; score multiplied 3× for duration
      const bfGs84=gsRef.current;
      if(bfGs84){
        bfGs84.feverActive=true;bfGs84.feverTimeLeft=6000;
        bfGs84.streak=Math.max(bfGs84.streak,FEVER_STREAK);
        // Mark fever as triple-score burst
        activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BURST_FEVER");
        activePwrRef.current.push({type:"BURST_FEVER",endsAt:Date.now()+6000,totalScore:0,mult:3});
        setActivePwrDisp([...activePwrRef.current]);
        setFeverBorder(true);sfx("feverStart");
        bfGs84.sessionStats.feverCount=(bfGs84.sessionStats.feverCount||0)+1;
      }
      spawnPopup(x,y,"🌡️💥 BURST FEVER! ×3","#f43f5e",26);
      spawnParticles(x,y,"#fca5a5",16,"spark");
      vibrate([10,4,16,4,10]);
      unlock("burst_fever_use");
      setNotif("🌡️ Burst Fever! Triple score fever for 6s!");
    } else if(ptype==="SCORE_TRAIL"){
      // SCORE_TRAIL — for 9s, each tap also adds 10% of score diff from last tap
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_TRAIL");
      activePwrRef.current.push({type:"SCORE_TRAIL",endsAt:Date.now()+9000,lastScore:gsRef.current?.score||0,totalAdded:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"✨📈 SCORE TRAIL!","#a3e635",22);
      spawnParticles(x,y,"#d9f99d",12,"spark");
      sfx("comboNote",5);vibrate([6,4,10,4,6]);
      unlock("score_trail_use");
      setNotif("✨ Score Trail! 10% bonus on each score increase for 9s!");
    } else if(ptype==="CHAIN_SNAP"){
      // CHAIN_SNAP — for 7s, after each tap auto-score nearest nearly-expired normal target
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CHAIN_SNAP");
      activePwrRef.current.push({type:"CHAIN_SNAP",endsAt:Date.now()+7000,snapCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔗📸 CHAIN SNAP!","#38bdf8",22);
      spawnParticles(x,y,"#bae6fd",12,"spark");
      sfx("comboNote",5);vibrate([6,4,10,4,6]);
      unlock("chain_snap_use");
      setNotif("🔗 Chain Snap! Auto-scores expiring targets for 7s!");
    } else if(ptype==="LIFE_ECHO"){
      // LIFE_ECHO — +1 life now; then every 5 taps grant +1 life for 15s (up to max)
      const leGs85=gsRef.current;const leCfg85=levelCfgRef.current;
      if(leGs85&&leCfg85){
        const maxLives85=leCfg85.lives||3;
        if(leGs85.lives<maxLives85){leGs85.lives++;spawnPopup(x,y,"❤️ +1","#f87171",20);}
      }
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_ECHO");
      activePwrRef.current.push({type:"LIFE_ECHO",endsAt:Date.now()+15000,tapCounter:0,triggered:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❤️🔊 LIFE ECHO!","#fb7185",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("comboNote",4);vibrate([6,4,10,4,6]);
      unlock("life_echo_use");
      setNotif("❤️ Life Echo! Instant life + restore every 5 taps for 15s!");
    } else if(ptype==="SCORE_VAULT"){
      // SCORE_VAULT — for 10s, bank 15% of each tap's score; releases all at end
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_VAULT");
      const svEntry85={type:"SCORE_VAULT",endsAt:Date.now()+10000,banked:0};
      activePwrRef.current.push(svEntry85);
      setActivePwrDisp([...activePwrRef.current]);
      // Schedule vault release
      setTimeout(()=>{
        const gsV85=gsRef.current;const pvFound=activePwrRef.current.find(p=>p.type==="SCORE_VAULT");
        if(gsV85&&pvFound&&pvFound.banked>0){
          gsV85.score+=pvFound.banked;
          spawnPopup(canvasRef.current?.width/2||180,canvasRef.current?.height/2||320,`🏦 VAULT! +${pvFound.banked}`,"#fbbf24",28);
          spawnParticles(canvasRef.current?.width/2||180,canvasRef.current?.height/2||320,"#fef3c7",18,"spark");
          sfx("coin");vibrate([8,4,12,4,8]);
          if(pvFound.banked>=800)unlock("score_vault_big");
        }
        activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_VAULT");
        setActivePwrDisp([...activePwrRef.current]);
      },10000);
      spawnPopup(x,y,"🏦💰 SCORE VAULT!","#fbbf24",22);
      spawnParticles(x,y,"#fef3c7",12,"spark");
      sfx("coin");vibrate([6,4,10]);
      unlock("score_vault_use");
      setNotif("🏦 Score Vault! Banks 15% of each tap score, releases all after 10s!");
    } else if(ptype==="STREAK_ECHO"){
      // STREAK_ECHO — for 8s, each tap grants bonus pts = streak×3 (echoes your streak)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="STREAK_ECHO");
      activePwrRef.current.push({type:"STREAK_ECHO",endsAt:Date.now()+8000,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔥🔊 STREAK ECHO!","#f97316",22);
      spawnParticles(x,y,"#fed7aa",12,"spark");
      sfx("comboNote",5);vibrate([6,4,10,4,6]);
      unlock("streak_echo_use");
      setNotif("🔥 Streak Echo! Every tap gives streak×3 bonus for 8s!");
    } else if(ptype==="NOVA_PULSE"){
      // NOVA_PULSE — every 4 taps emit a score nova: bonus = current streak × 10
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="NOVA_PULSE");
      activePwrRef.current.push({type:"NOVA_PULSE",endsAt:Date.now()+10000,tapCounter:0,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💥🫀 NOVA PULSE!","#f43f5e",22);
      spawnParticles(x,y,"#fecdd3",14,"spark");
      sfx("comboNote",5);vibrate([8,4,12,4,8]);
      unlock("nova_pulse_use");
      setNotif("💥 Nova Pulse! Every 4 taps fires a score nova for 10s!");
    } else if(ptype==="COIN_ECHO"){
      // COIN_ECHO — for 12s, whenever coins are earned double them (echo = +equal)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COIN_ECHO");
      activePwrRef.current.push({type:"COIN_ECHO",endsAt:Date.now()+12000,totalCoins:0});
      setActivePwrDisp([...activePwrRef.current]);
      // Also give 3 coins upfront
      saveRef.current.coins=(saveRef.current.coins||0)+3;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+3;
      debounceSave();
      spawnPopup(x,y,"🪙🔊 COIN ECHO! +3","#fbbf24",22);
      spawnParticles(x,y,"#fef9c3",12,"spark");
      sfx("coin");vibrate([6,4,10]);
      unlock("coin_echo_use");
      setNotif("🪙 Coin Echo! All coin earnings doubled for 12s!");
    } else if(ptype==="TIME_SHIELD"){
      // TIME_SHIELD — for 12s, the nearest-expiring target gets +1s lifetime extension each tap
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TIME_SHIELD");
      activePwrRef.current.push({type:"TIME_SHIELD",endsAt:Date.now()+12000,saves:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⏱️🛡️ TIME SHIELD!","#60a5fa",22);
      spawnParticles(x,y,"#bfdbfe",12,"spark");
      sfx("comboNote",4);vibrate([6,4,10,4,6]);
      unlock("time_shield_use");
      setNotif("⏱️ Time Shield! Each tap extends the nearest expiring target!");
    } else if(ptype==="SCORE_BLITZ"){
      // SCORE_BLITZ — for 5s, every tap scores double AND combo increases by 2 instead of 1
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_BLITZ");
      activePwrRef.current.push({type:"SCORE_BLITZ",endsAt:Date.now()+5000,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⚡💯 SCORE BLITZ!","#f59e0b",22);
      spawnParticles(x,y,"#fef3c7",14,"spark");
      sfx("comboNote",6);vibrate([8,4,12,4,8]);
      unlock("score_blitz_use");
      setNotif("⚡ Score Blitz! Double score + combo ×2 speed for 5s!");
    } else if(ptype==="PHANTOM_RUSH"){
      // PHANTOM_RUSH — for 6s, each tap also scores a phantom hit on a random target
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PHANTOM_RUSH");
      activePwrRef.current.push({type:"PHANTOM_RUSH",endsAt:Date.now()+6000,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"👻⚡ PHANTOM RUSH!","#818cf8",22);
      spawnParticles(x,y,"#c7d2fe",14,"spark");
      sfx("comboNote",5);vibrate([6,4,10,4,6]);
      unlock("phantom_rush_use");
      setNotif("👻 Phantom Rush! Each tap ghosts a second hit for 6s!");
    } else if(ptype==="COIN_STORM"){
      // COIN_STORM — burst of 8 coins now + 2 coins every 2s for 12s
      const csNow89=gsRef.current;
      saveRef.current.coins=(saveRef.current.coins||0)+8;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+8;
      debounceSave();
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COIN_STORM");
      activePwrRef.current.push({type:"COIN_STORM",endsAt:Date.now()+12000,totalCoins:8});
      setActivePwrDisp([...activePwrRef.current]);
      // Periodic coin drip
      const coinDrip89=setInterval(()=>{
        if(!activePwrRef.current.find(p=>p.type==="COIN_STORM"&&p.endsAt>Date.now())){clearInterval(coinDrip89);return;}
        const csvP89=activePwrRef.current.find(p=>p.type==="COIN_STORM");
        saveRef.current.coins=(saveRef.current.coins||0)+2;saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+2;
        if(csvP89){csvP89.totalCoins=(csvP89.totalCoins||0)+2;if(csvP89.totalCoins>=80)unlock("coin_storm_rich");}
        debounceSave();
      },2000);
      setTimeout(()=>clearInterval(coinDrip89),13000);
      spawnPopup(x,y,"🪙🌪️ COIN STORM! +8","#fbbf24",24);
      spawnParticles(x,y,"#fef3c7",16,"spark");
      sfx("coin");vibrate([8,4,12]);
      unlock("coin_storm_use");
      setNotif("🪙 Coin Storm! +8 coins + 2 every 2s for 12s!");
    } else if(ptype==="STREAK_BLAZE"){
      // STREAK_BLAZE — for 8s, bonus pts = streak²/10 on every tap (blazing acceleration)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="STREAK_BLAZE");
      activePwrRef.current.push({type:"STREAK_BLAZE",endsAt:Date.now()+8000,peakStreak:0,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔥🌪️ STREAK BLAZE!","#f97316",22);
      spawnParticles(x,y,"#fed7aa",14,"spark");
      sfx("comboNote",6);vibrate([8,4,12,4,8]);
      unlock("streak_blaze_use");
      setNotif("🔥 Streak Blaze! Bonus pts = streak² for 8s!");
    } else if(ptype==="LIFE_BLOOM"){
      // LIFE_BLOOM — restore all lives to max now; then grant +1 life after 10s if level still ongoing
      const lbGs90=gsRef.current;const lbCfg90=levelCfgRef.current;
      if(lbGs90&&lbCfg90){
        const maxLivesLB90=lbCfg90.lives||3;
        lbGs90.lives=maxLivesLB90;
      }
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_BLOOM");
      activePwrRef.current.push({type:"LIFE_BLOOM",endsAt:Date.now()+15000,bloomed:false});
      setActivePwrDisp([...activePwrRef.current]);
      setTimeout(()=>{
        const gsLB90b=gsRef.current;const cfgLB90b=levelCfgRef.current;const lbPwr=activePwrRef.current.find(p=>p.type==="LIFE_BLOOM");
        if(gsLB90b&&cfgLB90b&&lbPwr&&!lbPwr.bloomed&&gsLB90b.lives>0){
          const maxB90=(cfgLB90b.lives||3);
          if(gsLB90b.lives<maxB90){gsLB90b.lives++;}
          lbPwr.bloomed=true;
          spawnPopup(canvasRef.current?.width/2||180,60,"🌸 BLOOM +1❤️","#fb7185",22);
        }
      },10000);
      spawnPopup(x,y,"❤️🌸 LIFE BLOOM! MAX","#fb7185",22);
      spawnParticles(x,y,"#fca5a5",14,"spark");
      sfx("comboNote",4);vibrate([6,4,10,4,6]);
      unlock("life_bloom_use");
      setNotif("❤️ Life Bloom! Full heal now + bloom life in 10s!");
    } else if(ptype==="FOCUS_BLAST"){
      // FOCUS_BLAST — for 7s, each tap also detonates on nearest target within 100px
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="FOCUS_BLAST");
      activePwrRef.current.push({type:"FOCUS_BLAST",endsAt:Date.now()+7000,hits:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🎯💥 FOCUS BLAST!","#f43f5e",22);
      spawnParticles(x,y,"#fecaca",14,"spark");
      sfx("comboNote",5);vibrate([6,4,10,4,6]);
      unlock("focus_blast_use");
      setNotif("🎯 Focus Blast! Auto-detonates nearest target for 7s!");
    } else if(ptype==="COIN_CHAIN"){
      // COIN_CHAIN — for 12s, coins accumulate with each tap (1 coin×tapCount, up to 5/tap)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COIN_CHAIN");
      activePwrRef.current.push({type:"COIN_CHAIN",endsAt:Date.now()+12000,tapCounter:0,totalCoins:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🪙⛓️ COIN CHAIN!","#fbbf24",22);
      spawnParticles(x,y,"#fef3c7",12,"spark");
      sfx("coin");vibrate([6,4,10]);
      unlock("coin_chain_use");
      setNotif("🪙 Coin Chain! Earns +1 to +5 coins per tap for 12s!");
    } else if(ptype==="SHIELD_BURST"){
      // SHIELD_BURST — absorbs next 3 miss-penalties; each absorbed miss grants +50 bonus pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_BURST");
      activePwrRef.current.push({type:"SHIELD_BURST",endsAt:Date.now()+20000,charges:3,tanked:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🛡️💥 SHIELD BURST!","#38bdf8",22);
      spawnParticles(x,y,"#bae6fd",14,"spark");
      sfx("comboNote",4);vibrate([8,4,8,4,12]);
      unlock("shield_burst_use");
      setNotif("🛡️ Shield Burst! Absorbs 3 misses and grants +50 pts each!");
    } else if(ptype==="SCORE_ECHO2"){
      // SCORE_ECHO2 — for 10s, each normal tap echoes 30% of its points as bonus after 0.5s delay
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_ECHO2");
      activePwrRef.current.push({type:"SCORE_ECHO2",endsAt:Date.now()+10000,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"📊🔁 SCORE ECHO!","#a78bfa",22);
      spawnParticles(x,y,"#ede9fe",12,"spark");
      sfx("comboNote",6);vibrate([5,3,8,3,5]);
      unlock("score_echo_use");
      setNotif("📊 Score Echo! Each tap echoes 30% bonus pts for 10s!");
    } else if(ptype==="COMBO_SURGE2"){
      // COMBO_SURGE2 — for 15s, every 3 taps the streak multiplier is applied again (+1 per 3-tap cycle)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COMBO_SURGE2");
      activePwrRef.current.push({type:"COMBO_SURGE2",endsAt:Date.now()+15000,tapCycle:0,extraMult:1});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔥⛓️ COMBO SURGE!","#f97316",22);
      spawnParticles(x,y,"#fed7aa",14,"spark");
      sfx("comboNote",7);vibrate([6,3,10,3,6]);
      unlock("combo_surge2_use");
      setNotif("🔥 Combo Surge II! Every 3 taps adds +1 multiplier for 15s!");
    } else if(ptype==="LIFE_SPIRAL"){
      // LIFE_SPIRAL — for 20s, tapping targets in spiral pattern (right→left→right) restores 1 life every 5 taps
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_SPIRAL");
      activePwrRef.current.push({type:"LIFE_SPIRAL",endsAt:Date.now()+20000,tapCount:0,livesGained:0,lastX:null});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❤️🌀 LIFE SPIRAL!","#f43f5e",22);
      spawnParticles(x,y,"#fecaca",14,"spark");
      sfx("comboNote",3);vibrate([8,4,10,4,8]);
      unlock("life_spiral_use");
      setNotif("❤️ Life Spiral! Earn +1 life every 5 taps for 20s!");
    } else if(ptype==="TEMPO_ECHO"){
      // TEMPO_ECHO — for 12s, every 2nd tap fires an echoed tap on the previous target's position
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TEMPO_ECHO");
      activePwrRef.current.push({type:"TEMPO_ECHO",endsAt:Date.now()+12000,tapCount:0,beatCount:0,lastPos:null});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🎵🔊 TEMPO ECHO!","#a78bfa",22);
      spawnParticles(x,y,"#ede9fe",12,"spark");
      sfx("comboNote",5);vibrate([5,3,8,3,5]);
      unlock("tempo_echo_use");
      setNotif("🎵 Tempo Echo! Every 2nd tap echoes last position for 12s!");
    } else if(ptype==="COIN_BLOOM"){
      // COIN_BLOOM — for 15s, every 4th tap spawns a coin burst (4-8 coins)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COIN_BLOOM");
      activePwrRef.current.push({type:"COIN_BLOOM",endsAt:Date.now()+15000,tapCount:0,totalCoins:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🪙🌸 COIN BLOOM!","#fbbf24",22);
      spawnParticles(x,y,"#fef3c7",14,"spark");
      sfx("coin");vibrate([8,4,10,4,8]);
      unlock("coin_bloom_use");
      setNotif("🪙 Coin Bloom! Every 4th tap blooms 4-8 coins for 15s!");
    } else if(ptype==="STREAK_NOVA"){
      // STREAK_NOVA — for 12s, when streak reaches 20+ fires a nova explosion hitting all visible targets for 30pts each
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="STREAK_NOVA");
      activePwrRef.current.push({type:"STREAK_NOVA",endsAt:Date.now()+12000,novaFired:false,bestStreak:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⛓️💥 STREAK NOVA!","#f59e0b",22);
      spawnParticles(x,y,"#fef3c7",14,"spark");
      sfx("comboNote",8);vibrate([8,4,12,4,8]);
      unlock("streak_nova_use");
      setNotif("⛓️ Streak Nova! Reaches 20+ streak = nova explosion for 12s!");
    } else if(ptype==="LIFE_STORM"){
      // LIFE_STORM — immediately restores lives based on current streak (1 per 5 streak, max +3)
      const gsLS95=gsRef.current;
      if(gsLS95){
        const livesBack95=Math.min(3,1+Math.floor((gsLS95.streak||0)/5));
        gsLS95.lives=Math.min(5,gsLS95.lives+livesBack95);
        spawnPopup(x,y,`❤️⛈️ +${livesBack95} LIVES!`,"#f43f5e",26);
        spawnParticles(x,y,"#fecaca",livesBack95*4,"spark");
        sfx("comboNote",4);vibrate([8,4,8,4,8]);
        unlock("life_storm_use");
        if(gsLS95.lives>=5)unlock("life_storm_max");
        setNotif(`❤️ Life Storm! +${livesBack95} lives restored!`);
      }
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_STORM");
      setActivePwrDisp([...activePwrRef.current]);
    } else if(ptype==="GOLD_FRENZY"){
      // GOLD_FRENZY — for 10s, each tap earns gold coins = 2× target rarity mult (min 2/tap)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="GOLD_FRENZY");
      activePwrRef.current.push({type:"GOLD_FRENZY",endsAt:Date.now()+10000,totalCoins:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🥇🔥 GOLD FRENZY!","#fbbf24",22);
      spawnParticles(x,y,"#fef3c7",16,"spark");
      sfx("coin");vibrate([8,4,12,4,8]);
      unlock("gold_frenzy_use");
      setNotif("🥇 Gold Frenzy! Each tap earns gold coins for 10s!");
    } else if(ptype==="PHANTOM_COIN"){
      // PHANTOM_COIN — spawns 5 invisible phantom coins on screen; tapping their positions collects them (20 coins each)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PHANTOM_COIN");
      const cw96=canvasRef.current?.width||360;const ch96=canvasRef.current?.height||640;
      const phantoms96=Array.from({length:5},()=>({x:50+Math.random()*(cw96-100),y:80+Math.random()*(ch96-160),collected:false}));
      activePwrRef.current.push({type:"PHANTOM_COIN",endsAt:Date.now()+15000,coins:phantoms96,collected:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"👻🪙 PHANTOM COIN!","#a78bfa",22);
      spawnParticles(x,y,"#ede9fe",12,"spark");
      sfx("coin");vibrate([5,3,10,3,5]);
      unlock("phantom_coin_use");
      setNotif("👻 Phantom Coin! 5 invisible coins hidden on screen!");
    } else if(ptype==="SCORE_FRENZY"){
      // SCORE_FRENZY — for 8s, all score is doubled; after it ends, awards half the difference
      const sfGs97=gsRef.current;
      const sfStart97=sfGs97?sfGs97.score:0;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_FRENZY");
      activePwrRef.current.push({type:"SCORE_FRENZY",endsAt:Date.now()+8000,scoreAtStart:sfStart97,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"📊🔥 SCORE FRENZY!","#ef4444",22);
      spawnParticles(x,y,"#fecaca",14,"spark");
      sfx("comboNote",9);vibrate([8,4,14,4,8]);
      unlock("score_frenzy_use");
      setNotif("📊 Score Frenzy! All score doubled for 8s!");
    } else if(ptype==="LIFE_BEACON"){
      // LIFE_BEACON — passive; if lives drop to 1, auto-fires a 3-life restore (once)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_BEACON");
      activePwrRef.current.push({type:"LIFE_BEACON",endsAt:Date.now()+99999000,triggered:false});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❤️🔦 LIFE BEACON!","#f43f5e",22);
      spawnParticles(x,y,"#fecaca",12,"spark");
      sfx("comboNote",3);vibrate([6,4,8,4,6]);
      unlock("life_beacon_use");
      setNotif("❤️ Life Beacon! Auto-restores 3 lives when you reach 1 life!");
    } else if(ptype==="BLAZE_TRAIL"){
      // BLAZE_TRAIL — for 10s, each tap leaves a 1s flame trail; any target passing through = +15pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BLAZE_TRAIL");
      activePwrRef.current.push({type:"BLAZE_TRAIL",endsAt:Date.now()+10000,totalBonus:0,flames:[]});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔥✨ BLAZE TRAIL!","#f97316",22);
      spawnParticles(x,y,"#fed7aa",14,"spark");
      sfx("comboNote",6);vibrate([6,3,10,3,6]);
      unlock("blaze_trail_use");
      setNotif("🔥 Blaze Trail! Flames follow your taps for 10s!");
    } else if(ptype==="COIN_PULSE"){
      // COIN_PULSE — for 12s, pulses every 3s granting 3-6 coins
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COIN_PULSE");
      activePwrRef.current.push({type:"COIN_PULSE",endsAt:Date.now()+12000,lastPulse:Date.now(),totalCoins:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🪙💓 COIN PULSE!","#fbbf24",22);
      spawnParticles(x,y,"#fef3c7",12,"spark");
      sfx("coin");vibrate([5,3,8,3,5]);
      unlock("coin_pulse_use");
      setNotif("🪙 Coin Pulse! Grants 3-6 coins every 3s for 12s!");
    } else if(ptype==="FEVER_BLOOM"){
      // FEVER_BLOOM — immediately activates fever; when fever ends, re-activates it once more
      const fbGs99=gsRef.current;
      if(fbGs99){
        fbGs99.feverActive=true;fbGs99.feverTimeLeft=5000;
        fbGs99.streak=Math.max(fbGs99.streak,FEVER_STREAK);
        setFeverBorder(true);sfx("feverStart");
        fbGs99.sessionStats.feverCount=(fbGs99.sessionStats.feverCount||0)+1;
      }
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="FEVER_BLOOM");
      activePwrRef.current.push({type:"FEVER_BLOOM",endsAt:Date.now()+30000,rebloomedOnce:false});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌡️🌸 FEVER BLOOM!","#f43f5e",24);
      spawnParticles(x,y,"#fecaca",16,"spark");
      sfx("feverStart");vibrate([10,5,14,5,10]);
      unlock("fever_bloom_use");
      setNotif("🌡️ Fever Bloom! Activates fever now + once more!");
    } else if(ptype==="COIN_CASCADE"){
      // COIN_CASCADE — for 15s, coins earned cascade: each coin earned adds +1 to next coin yield (max 10)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COIN_CASCADE");
      activePwrRef.current.push({type:"COIN_CASCADE",endsAt:Date.now()+15000,multiplier:1,totalCoins:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🪙💦 COIN CASCADE!","#fbbf24",22);
      spawnParticles(x,y,"#fef3c7",14,"spark");
      sfx("coin");vibrate([6,3,10,3,6]);
      unlock("coin_cascade_use");
      setNotif("🪙 Coin Cascade! Cascading coin multiplier for 15s!");
    } else if(ptype==="SCORE_OMEGA"){
      // SCORE_OMEGA — for 15s, each tap adds +1 to a permanent multiplier that stacks until deactivated
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_OMEGA");
      activePwrRef.current.push({type:"SCORE_OMEGA",endsAt:Date.now()+15000,mult:1,tapCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"Ω💥 SCORE OMEGA!","#a855f7",24);
      spawnParticles(x,y,"#f5d0fe",16,"spark");
      particlesRef.current.push({type:"shockwave",x,y,color:"#a855f7",size:150,born:performance.now(),duration:800,alpha:0.6});
      sfx("legendary");vibrate([10,5,16,5,10,5,16]);
      unlock("score_omega_use");
      setNotif("Ω Score Omega! Multiplier builds with each tap for 15s!");
    } else if(ptype==="ETERNAL_LIFE"){
      // ETERNAL_LIFE — for 30s, if lives would hit 0 they reset to 3 instead (once)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="ETERNAL_LIFE");
      activePwrRef.current.push({type:"ETERNAL_LIFE",endsAt:Date.now()+30000,triggered:false});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"♾️❤️ ETERNAL LIFE!","#f43f5e",24);
      spawnParticles(x,y,"#fecaca",16,"spark");
      sfx("comboNote",2);vibrate([8,4,8,4,12]);
      unlock("eternal_life_use");
      setNotif("♾️ Eternal Life! Prevents death once for 30s!");
    } else if(ptype==="ARC_SURGE"){
      // ARC_SURGE — for 10s, each tap chains to the nearest target within 120px for +40pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="ARC_SURGE");
      activePwrRef.current.push({type:"ARC_SURGE",endsAt:Date.now()+10000,chainCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⚡🌊 ARC SURGE!","#38bdf8",22);
      spawnParticles(x,y,"#e0f2fe",14,"spark");
      sfx("comboNote",7);vibrate([6,3,10,3,6]);
      unlock("arc_surge_use");
      setNotif("⚡ Arc Surge! Each tap chains to nearest target for 10s!");
    } else if(ptype==="RELIC_PULSE"){
      // RELIC_PULSE — for 15s, every 5th tap triggers a pulse granting +streak×10 pts
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="RELIC_PULSE");
      activePwrRef.current.push({type:"RELIC_PULSE",endsAt:Date.now()+15000,tapCount:0,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🏺💓 RELIC PULSE!","#a78bfa",22);
      spawnParticles(x,y,"#ede9fe",12,"spark");
      sfx("comboNote",4);vibrate([6,3,8,3,6]);
      unlock("relic_pulse_use");
      setNotif("🏺 Relic Pulse! Every 5th tap triggers streak bonus for 15s!");
    } else if(ptype==="VENOM_SURGE"){
      // VENOM_SURGE — for 12s, each miss converts to +50pts bonus instead of losing a life
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="VENOM_SURGE");
      activePwrRef.current.push({type:"VENOM_SURGE",endsAt:Date.now()+12000,missesConverted:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"☠️⚡ VENOM SURGE!","#84cc16",22);
      spawnParticles(x,y,"#d9f99d",14,"spark");
      sfx("comboNote",5);vibrate([8,3,12,3,8]);
      unlock("venom_surge_use");
      setNotif("☠️ Venom Surge! Misses convert to +50pts for 12s!");
    } else if(ptype==="LUNAR_BLOOM"){
      // LUNAR_BLOOM — for 15s, every 3rd tap spawns 2 ghost bonus targets
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LUNAR_BLOOM");
      activePwrRef.current.push({type:"LUNAR_BLOOM",endsAt:Date.now()+15000,tapCount:0,ghostsSpawned:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌙🌸 LUNAR BLOOM!","#c084fc",22);
      spawnParticles(x,y,"#f3e8ff",12,"spark");
      sfx("comboNote",3);vibrate([5,3,8,3,5]);
      unlock("lunar_bloom_use");
      setNotif("🌙 Lunar Bloom! Every 3rd tap blooms ghost targets for 15s!");
    } else if(ptype==="STAR_CHAIN"){
      // STAR_CHAIN — for 12s, consecutive taps within 1.5s each add +20pts stacking chain bonus
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="STAR_CHAIN");
      activePwrRef.current.push({type:"STAR_CHAIN",endsAt:Date.now()+12000,chain:0,lastTap:0,totalLinks:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⭐⛓️ STAR CHAIN!","#fbbf24",22);
      spawnParticles(x,y,"#fef9c3",14,"spark");
      sfx("comboNote",6);vibrate([5,3,9,3,5]);
      unlock("star_chain_use");
      setNotif("⭐ Star Chain! Consecutive taps stack +20pts each for 12s!");
    } else if(ptype==="VOID_BLOOM"){
      // VOID_BLOOM — for 14s, each tap in a cluster of 3+ targets within 80px grants +60pts aoe
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="VOID_BLOOM");
      activePwrRef.current.push({type:"VOID_BLOOM",endsAt:Date.now()+14000,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌑🌸 VOID BLOOM!","#6d28d9",22);
      spawnParticles(x,y,"#ede9fe",14,"spark");
      sfx("comboNote",2);vibrate([6,3,10,3,6]);
      unlock("void_bloom_use");
      setNotif("🌑 Void Bloom! Clusters of 3+ targets grant +60pts AoE for 14s!");
    } else if(ptype==="PULSE_WAVE"){
      // PULSE_WAVE — instantly detonates a shockwave clearing all targets on screen for pts
      const pwGs104=gsRef.current;
      if(pwGs104){
        const pwTargets=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb"&&t.type!=="boss");
        const pwPts=Math.round(pwTargets.length*35*(pwGs104.feverActive?2:1));
        pwTargets.forEach(t=>{t.dying=performance.now();spawnParticles(t.x,t.y,"#bfdbfe",5,"spark");});
        pwGs104.score+=pwPts;
        particlesRef.current.push({type:"shockwave",x:canvasRef.current?.width/2||200,y:canvasRef.current?.height/2||350,color:"#3b82f6",size:280,born:performance.now(),duration:900,alpha:0.6});
        spawnPopup(x,y,`〰️ PULSE WAVE! +${pwPts}`,"#3b82f6",26);
        activePwrRef.current.push({type:"PULSE_WAVE",endsAt:Date.now()+1,cleared:pwTargets.length});
        unlock("pulse_wave_use");
        if(pwTargets.length>=8)unlock("pulse_wave_max");
        sfx("legendary");vibrate([10,5,20,5,10]);
        setNotif(`〰️ Pulse Wave cleared ${pwTargets.length} targets for +${pwPts}pts!`);
      }
    } else if(ptype==="COIN_VORTEX"){
      // COIN_VORTEX — for 12s, each tap earns 2–5 coins based on proximity to screen center
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COIN_VORTEX");
      activePwrRef.current.push({type:"COIN_VORTEX",endsAt:Date.now()+12000,totalCoins:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🪙🌀 COIN VORTEX!","#fbbf24",22);
      spawnParticles(x,y,"#fef3c7",14,"spark");
      sfx("coin");vibrate([5,3,8,3,5]);
      unlock("coin_vortex_use");
      setNotif("🪙 Coin Vortex! Each tap earns coins based on center proximity for 12s!");
    } else if(ptype==="GRAVITY_BURST2"){
      // GRAVITY_BURST2 — pulls all targets toward tapped position for 1s then detonates for pts
      const gb2Gs105=gsRef.current;
      if(gb2Gs105){
        const gb2Targets=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb"&&t.type!=="boss");
        gb2Targets.forEach(t=>{t.vx=(x-t.x)*0.08;t.vy=(y-t.y)*0.08;t.moving=true;});
        setTimeout(()=>{
          const gb2Gs2=gsRef.current;if(!gb2Gs2)return;
          const near=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb"&&Math.hypot(t.x-x,t.y-y)<80);
          const gb2Pts=Math.round(near.length*45*(gb2Gs2.feverActive?2:1));
          near.forEach(t=>{t.dying=performance.now();spawnParticles(t.x,t.y,"#c7d2fe",6,"spark");});
          gb2Gs2.score+=gb2Pts;
          particlesRef.current.push({type:"shockwave",x:x,y:y,color:"#6366f1",size:160,born:performance.now(),duration:700,alpha:0.6});
          spawnPopup(x,y,`🌀💥 CRUSH! +${gb2Pts}`,"#6366f1",24);
          unlock("gravity_burst2_use");
          if(near.length>=5)unlock("gravity_burst2_max");
          sfx("epic");vibrate([8,4,14,4,8]);
        },1000);
        spawnPopup(x,y,"🌀 GRAVITY PULL!","#818cf8",20);
        setNotif("🌀 Gravity Burst II! All targets pulled to tap position!");
      }
    } else if(ptype==="SCORE_AVALANCHE"){
      // SCORE_AVALANCHE — for 12s, each tap adds to an avalanche multiplier (resets on miss)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_AVALANCHE");
      activePwrRef.current.push({type:"SCORE_AVALANCHE",endsAt:Date.now()+12000,mult:1,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🏔️💰 SCORE AVALANCHE!","#94a3b8",22);
      spawnParticles(x,y,"#f1f5f9",14,"spark");
      sfx("comboNote",5);vibrate([6,3,10,3,6]);
      unlock("score_avalanche_use");
      setNotif("🏔️ Score Avalanche! Each tap grows your multiplier for 12s!");
    } else if(ptype==="INFERNO_CHAIN"){
      // INFERNO_CHAIN — for 10s, each consecutive tap within 0.8s doubles the inferno multiplier (max ×8)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="INFERNO_CHAIN");
      activePwrRef.current.push({type:"INFERNO_CHAIN",endsAt:Date.now()+10000,mult:1,lastTap:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔥⛓️ INFERNO CHAIN!","#ef4444",22);
      spawnParticles(x,y,"#fee2e2",14,"spark");
      sfx("comboNote",7);vibrate([8,3,14,3,8]);
      unlock("inferno_chain_use");
      setNotif("🔥 Inferno Chain! Quick taps double your bonus multiplier for 10s!");
    } else if(ptype==="SPECTRAL_COIN"){
      // SPECTRAL_COIN — for 15s, every miss (not dying) spawns 3 collectible spectral coins worth 5 each
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SPECTRAL_COIN");
      activePwrRef.current.push({type:"SPECTRAL_COIN",endsAt:Date.now()+15000,totalCoins:0,pool:[]});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"👻💰 SPECTRAL COIN!","#c084fc",22);
      spawnParticles(x,y,"#f3e8ff",12,"spark");
      sfx("coin");vibrate([5,3,8,3,5]);
      unlock("spectral_coin_use");
      setNotif("👻 Spectral Coin! Misses spawn ghost coins worth 5 each for 15s!");
    } else if(ptype==="MAGMA_WAVE"){
      // MAGMA_WAVE — every 4s, a magma wave sweeps the screen dealing AoE +30pts to all targets nearby
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="MAGMA_WAVE");
      activePwrRef.current.push({type:"MAGMA_WAVE",endsAt:Date.now()+16000,lastWave:Date.now(),totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌋〰️ MAGMA WAVE!","#dc2626",22);
      spawnParticles(x,y,"#fee2e2",14,"spark");
      sfx("comboNote",8);vibrate([8,4,14,4,8]);
      unlock("magma_wave_use");
      setNotif("🌋 Magma Wave! AoE wave sweeps the field every 4s for 16s!");
    } else if(ptype==="COSMIC_ECHO"){
      // COSMIC_ECHO — for 12s, every tap echoes the score from 0.6s ago (double your best recent tap)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="COSMIC_ECHO");
      activePwrRef.current.push({type:"COSMIC_ECHO",endsAt:Date.now()+12000,recentPts:[],totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌌🔊 COSMIC ECHO!","#818cf8",22);
      spawnParticles(x,y,"#e0e7ff",12,"spark");
      sfx("comboNote",3);vibrate([5,3,8,3,5]);
      unlock("cosmic_echo_use");
      setNotif("🌌 Cosmic Echo! Each tap echoes its own pts from 0.6s ago for 12s!");
    } else if(ptype==="FROST_LANCE"){
      // FROST_LANCE — for 8s, each tap shoots a "lance" that pierces up to 2 nearby targets for +50pts each
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="FROST_LANCE");
      activePwrRef.current.push({type:"FROST_LANCE",endsAt:Date.now()+8000,pierced:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❄️🗡️ FROST LANCE!","#93c5fd",22);
      spawnParticles(x,y,"#dbeafe",14,"spark");
      sfx("comboNote",5);vibrate([6,3,10,3,6]);
      unlock("frost_lance_use");
      setNotif("❄️ Frost Lance! Each tap pierces nearby targets for +50pts each for 8s!");
    } else if(ptype==="LIFE_MATRIX"){
      // LIFE_MATRIX — for 20s, check every 5s and restore 1 life if lives < 3
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_MATRIX");
      activePwrRef.current.push({type:"LIFE_MATRIX",endsAt:Date.now()+20000,lastCheck:Date.now(),totalRestored:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❤️🔲 LIFE MATRIX!","#f43f5e",22);
      spawnParticles(x,y,"#fecaca",12,"spark");
      sfx("comboNote",3);vibrate([5,3,8,3,5]);
      unlock("life_matrix_use");
      setNotif("❤️ Life Matrix! Restores a life every 5s when you're low for 20s!");
    } else if(ptype==="THUNDER_BURST"){
      // THUNDER_BURST — immediately sends a thunderbolt clearing nearest 5 targets for big pts
      const tbGs109=gsRef.current;
      if(tbGs109){
        const tbTargets=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb"&&t.type!=="boss").sort((a,b)=>Math.hypot(a.x-x,a.y-y)-Math.hypot(b.x-x,b.y-y)).slice(0,5);
        const tbPts=Math.round(tbTargets.length*55*(tbGs109.feverActive?2:1));
        tbTargets.forEach((t,i)=>{
          setTimeout(()=>{t.dying=performance.now();spawnParticles(t.x,t.y,"#fef9c3",8,"spark");},i*80);
        });
        tbGs109.score+=tbPts;
        particlesRef.current.push({type:"shockwave",x:x,y:y,color:"#f59e0b",size:220,born:performance.now(),duration:800,alpha:0.55});
        spawnPopup(x,y,`⚡ THUNDER BURST! +${tbPts}`,"#f59e0b",26);
        unlock("thunder_burst_use");
        if(tbTargets.length>=5)unlock("thunder_burst_max");
        sfx("legendary");vibrate([10,5,18,5,10]);
        setNotif(`⚡ Thunder Burst zapped ${tbTargets.length} targets for +${tbPts}pts!`);
      }
    } else if(ptype==="SHIMMER_COIN"){
      // SHIMMER_COIN — for 12s, rare/epic/legendary targets drop bonus coins on tap (5/10/20)
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIMMER_COIN");
      activePwrRef.current.push({type:"SHIMMER_COIN",endsAt:Date.now()+12000,totalCoins:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"✨🪙 SHIMMER COIN!","#fbbf24",22);
      spawnParticles(x,y,"#fef9c3",14,"spark");
      sfx("coin");vibrate([5,3,8,3,5]);
      unlock("shimmer_coin_use");
      setNotif("✨ Shimmer Coin! Rare+ targets drop bonus coins for 12s!");
    } else if(ptype==="SCORE_QUAKE"){
      // SCORE_QUAKE — for 12s, each 5th tap triggers a "quake" that scores all targets on screen
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_QUAKE");
      activePwrRef.current.push({type:"SCORE_QUAKE",endsAt:Date.now()+12000,tapCount:0,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌍💥 SCORE QUAKE!","#78716c",22);
      spawnParticles(x,y,"#f5f5f4",14,"spark");
      sfx("comboNote",6);vibrate([8,4,14,4,8]);
      unlock("score_quake_use");
      setNotif("🌍 Score Quake! Every 5th tap shakes pts from all targets for 12s!");
    } else if(ptype==="ECHO_LIFE"){
      // ECHO_LIFE — for 20s, whenever you lose a life, gain +1 back 3s later once per loss
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="ECHO_LIFE");
      activePwrRef.current.push({type:"ECHO_LIFE",endsAt:Date.now()+20000,totalRestored:0,lastEcho:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❤️〰️ ECHO LIFE!","#f43f5e",22);
      spawnParticles(x,y,"#fecaca",12,"spark");
      sfx("comboNote",3);vibrate([6,3,10,3,6]);
      unlock("echo_life_use");
      setNotif("❤️ Echo Life! Lost lives echo back after 3s for 20s!");
    } else if(ptype==="NOVA_CASCADE"){
      // NOVA_CASCADE — for 10s, each tap chains to a nova that adds +tap count² pts bonus
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="NOVA_CASCADE");
      activePwrRef.current.push({type:"NOVA_CASCADE",endsAt:Date.now()+10000,tapCount:0,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💥🌊 NOVA CASCADE!","#f43f5e",22);
      spawnParticles(x,y,"#fecdd3",14,"spark");
      sfx("comboNote",7);vibrate([8,4,12,4,8]);
      unlock("nova_cascade_use");
      setNotif("💥 Nova Cascade! Tap count² bonus every tap for 10s!");
    } else if(ptype==="TIME_WARP2"){
      // TIME_WARP2 — for 8s, extends lifetime of every existing target on screen by +2s
      const tw2Gs111=gsRef.current;
      if(tw2Gs111){
        let extended=0;
        targetsRef.current.filter(t=>!t.dying).forEach(t=>{
          t.lifetime=(t.lifetime||3000)+2000;extended++;
        });
        activePwrRef.current.push({type:"TIME_WARP2",endsAt:Date.now()+8000,totalExtended:extended});
        setActivePwrDisp([...activePwrRef.current]);
        spawnPopup(x,y,`⏱️×2 TIME WARP II +${extended}t`,"#a78bfa",22);
        spawnParticles(x,y,"#ede9fe",12,"spark");
        sfx("comboNote",4);vibrate([6,3,10,3,6]);
        unlock("time_warp2_use");
        if(extended>=8)unlock("time_warp2_max");
        setNotif(`⏱️ Time Warp II! Extended ${extended} targets' lifetime!`);
      }
    } else if(ptype==="SOLAR_PULSE"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SOLAR_PULSE");
      activePwrRef.current.push({type:"SOLAR_PULSE",endsAt:Date.now()+10000,tapCount:0,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"☀️💓 SOLAR PULSE!","#fbbf24",22);
      spawnParticles(x,y,"#fef3c7",14,"spark");
      sfx("comboNote",6);vibrate([8,4,8,4,12]);
      unlock("solar_pulse_use");
      setNotif("☀️ Solar Pulse! +15pts per active target on each tap for 10s!");
    } else if(ptype==="VOID_CHAIN"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="VOID_CHAIN");
      activePwrRef.current.push({type:"VOID_CHAIN",endsAt:Date.now()+12000,chainCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌑⛓️ VOID CHAIN!","#7c3aed",22);
      spawnParticles(x,y,"#ddd6fe",12,"spark");
      sfx("comboNote",8);vibrate([6,3,12,3,6]);
      unlock("void_chain_use");
      setNotif("🌑 Void Chain! Each tap chains to nearest target for 50% bonus for 12s!");
    } else if(ptype==="EMBER_WAVE"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="EMBER_WAVE");
      activePwrRef.current.push({type:"EMBER_WAVE",endsAt:Date.now()+10000,tapCount:0,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔥〰️ EMBER WAVE!","#f97316",22);
      spawnParticles(x,y,"#fed7aa",14,"spark");
      sfx("comboNote",5);vibrate([8,3,8,3,10]);
      unlock("ember_wave_use");
      setNotif("🔥 Ember Wave! Every 4th tap triggers a wave worth ×streak bonus for 10s!");
    } else if(ptype==="CRYSTAL_NET"){
      const cnNow113=Date.now();
      const cnTargets113=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb"&&t.type!=="boss");
      cnTargets113.forEach(t=>{t._cnSlowed113=cnNow113+5000;t.lifetime=(t.lifetime||3000)+1500;});
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CRYSTAL_NET");
      activePwrRef.current.push({type:"CRYSTAL_NET",endsAt:cnNow113+8000,netted:cnTargets113.length});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,`💎🕸️ CRYSTAL NET! ×${cnTargets113.length}`,"#22d3ee",22);
      spawnParticles(x,y,"#cffafe",12,"dot");
      sfx("comboNote",3);vibrate([6,4,6,4,8]);
      unlock("crystal_net_use");
      if(cnTargets113.length>=8)unlock("crystal_net_max");
      setNotif(`💎 Crystal Net! Slowed ${cnTargets113.length} targets, +1.5s lifetime each!`);
    } else if(ptype==="PRISM_ECHO"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PRISM_ECHO");
      activePwrRef.current.push({type:"PRISM_ECHO",endsAt:Date.now()+10000,echoCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌈🔊 PRISM ECHO!","#ec4899",22);
      spawnParticles(x,y,"#fce7f3",14,"spark");
      sfx("comboNote",6);vibrate([7,3,11,3,7]);
      unlock("prism_echo_use");
      setNotif("🌈 Prism Echo! Every 3rd tap echoes score bonus ×1.5 for 10s!");
    } else if(ptype==="VOLT_SURGE"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="VOLT_SURGE");
      activePwrRef.current.push({type:"VOLT_SURGE",endsAt:Date.now()+12000,tapCount:0,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"⚡🔥 VOLT SURGE!","#f59e0b",22);
      spawnParticles(x,y,"#fef3c7",16,"spark");
      sfx("comboNote",9);vibrate([10,4,10,4,14]);
      unlock("volt_surge_use");
      setNotif("⚡ Volt Surge! Streak×10 bonus per tap for 12s!");
    } else if(ptype==="SHADOW_BLOOM"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHADOW_BLOOM");
      activePwrRef.current.push({type:"SHADOW_BLOOM",endsAt:Date.now()+15000,missesAbsorbed:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌑🌸 SHADOW BLOOM!","#6d28d9",22);
      spawnParticles(x,y,"#ddd6fe",14,"spark");
      sfx("comboNote",4);vibrate([8,4,8,4,8]);
      unlock("shadow_bloom_use");
      setNotif("🌑 Shadow Bloom! Absorbs next 4 misses for 15s!");
    } else if(ptype==="NEXUS_WAVE"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="NEXUS_WAVE");
      activePwrRef.current.push({type:"NEXUS_WAVE",endsAt:Date.now()+10000,tapCount:0,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌐〰️ NEXUS WAVE!","#0ea5e9",22);
      spawnParticles(x,y,"#e0f2fe",14,"dot");
      sfx("comboNote",7);vibrate([9,4,13,4,9]);
      unlock("nexus_wave_use");
      setNotif("🌐 Nexus Wave! Score bonus multiplies by number of active targets per tap!");
    } else if(ptype==="QUANTUM_ECHO"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="QUANTUM_ECHO");
      activePwrRef.current.push({type:"QUANTUM_ECHO",endsAt:Date.now()+12000,echoCount:0,lastPts:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔮🔊 QUANTUM ECHO!","#818cf8",22);
      spawnParticles(x,y,"#e0e7ff",14,"spark");
      sfx("comboNote",8);vibrate([7,3,11,3,7]);
      unlock("quantum_echo_use");
      setNotif("🔮 Quantum Echo! Every tap re-echoes 40% of previous tap bonus for 12s!");
    } else if(ptype==="FROST_CROWN"){
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="FROST_CROWN");
      activePwrRef.current.push({type:"FROST_CROWN",endsAt:Date.now()+10000,tapCount:0,totalBonus:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❄️👑 FROST CROWN!","#7dd3fc",22);
      spawnParticles(x,y,"#e0f2fe",16,"spark");
      sfx("comboNote",6);vibrate([10,5,10,5,14]);
      unlock("frost_crown_use");
      setNotif("❄️ Frost Crown! Score += streak² every tap for 10s!");
    } else if(ptype==="DOUBLE_FEVER"){
      // DOUBLE_FEVER — activate fever now; start second wave after 3s
      const dfGs75=gsRef.current;
      if(dfGs75){
        dfGs75.feverActive=true;dfGs75.feverTimeLeft=4000;
        dfGs75.streak=Math.max(dfGs75.streak,FEVER_STREAK);
        setFeverBorder(true);sfx("feverStart");
        dfGs75.sessionStats.feverCount=(dfGs75.sessionStats.feverCount||0)+1;
      }
      spawnPopup(x,y,"🌡️×2 DOUBLE FEVER!","#f43f5e",24);
      spawnParticles(x,y,"#fca5a5",14,"spark");
      vibrate([8,4,12,4,8]);
      unlock("double_fever_use");
      // Schedule second wave
      setTimeout(()=>{
        const df2Gs=gsRef.current;
        if(!df2Gs||df2Gs.lives<=0)return;
        df2Gs.feverActive=true;df2Gs.feverTimeLeft=4000;
        df2Gs.streak=Math.max(df2Gs.streak,FEVER_STREAK);
        setFeverBorder(true);sfx("feverStart");
        spawnPopup(180,200,"🌡️ WAVE 2!","#f43f5e",22);
        df2Gs.sessionStats.feverCount=(df2Gs.sessionStats.feverCount||0)+1;
        unlock("double_fever_2nd");
      },3000);
      setNotif("🌡️×2 Double Fever! 2 fever waves incoming!");
    } else if(ptype==="BOMB_CONVERT"){
      // BOMB_CONVERT — all new bombs spawn as light_bombs for 8s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BOMB_CONVERT");
      activePwrRef.current.push({type:"BOMB_CONVERT",endsAt:Date.now()+8000,converted:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💣💡 BOMB CONVERT!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",12,"spark");
      sfx("comboNote",5);vibrate([6,4,6]);
      unlock("bomb_convert_use");
      setNotif("💡 Bomb Convert! Bombs become safe Light Bombs!");
    } else if(ptype==="RICOCHET_BLAST"){
      // RICOCHET_BLAST — next 4 taps: auto-score nearest neighbor
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="RICOCHET_BLAST");
      activePwrRef.current.push({type:"RICOCHET_BLAST",endsAt:Date.now()+20000,charges:4});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🎯⛓️ RICOCHET BLAST!","#fb923c",22);
      spawnParticles(x,y,"#fed7aa",12,"spark");
      sfx("comboNote",6);vibrate([8,4,8]);
      unlock("ricochet_blast_use");
      setNotif("🎯 Ricochet Blast! Next 4 taps score a neighbor!");
    } else if(ptype==="FEVER_FOUNTAIN"){
      // FEVER_FOUNTAIN — start fever 8s; extend by 6s if already active
      const ffGs74=gsRef.current;
      if(ffGs74){
        if(ffGs74.feverActive){
          ffGs74.feverTimeLeft+=6000;
          spawnPopup(x,y,"🌡️ EXTENDED +6s!","#f43f5e",22);
          unlock("fever_fountain_ext");
        } else {
          ffGs74.feverActive=true;ffGs74.feverTimeLeft=8000;
          ffGs74.streak=Math.max(ffGs74.streak,FEVER_STREAK);
          setFeverBorder(true);sfx("feverStart");
          spawnPopup(x,y,"🌡️⛲ FEVER START!","#f43f5e",22);
          ffGs74.sessionStats.feverCount=(ffGs74.sessionStats.feverCount||0)+1;
        }
      }
      spawnParticles(x,y,"#fca5a5",14,"spark");
      vibrate([8,4,12,4,8]);
      unlock("fever_fountain_use");
      setNotif("🌡️ Fever Fountain! Fever activated!");
    } else if(ptype==="PULSE_SHIELD"){
      // PULSE_SHIELD — blocks next 2 missed-target life losses
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PULSE_SHIELD");
      activePwrRef.current.push({type:"PULSE_SHIELD",endsAt:Date.now()+20000,charges:2});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"💠🛡️ PULSE SHIELD!","#38bdf8",22);
      spawnParticles(x,y,"#7dd3fc",12,"spark");
      sfx("comboNote",5);vibrate([6,4,6,4,6]);
      unlock("pulse_shield_use");
      setNotif("💠 Pulse Shield! Next 2 misses blocked!");
    } else if(ptype==="SCORE_AMPLIFY"){
      // SCORE_AMPLIFY — each tap adds 10% of current score as bonus (cap 500) for 8s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_AMPLIFY");
      activePwrRef.current.push({type:"SCORE_AMPLIFY",endsAt:Date.now()+8000,amplifyTotal:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"📈🔊 SCORE AMPLIFY!","#a3e635",22);
      spawnParticles(x,y,"#d9f99d",12,"spark");
      sfx("comboNote",7);vibrate([8,4,10]);
      unlock("score_amplify_use");
      setNotif("📈 Score Amplify! +10% of score on every tap!");
    } else if(ptype==="STAR_FORGE"){
      // STAR_FORGE — instant: upgrade all active targets to rare+ rarity
      const sfGs72=gsRef.current;
      let legCount72=0;
      targetsRef.current.forEach(t=>{
        if(t.dying||t.type==="bomb"||t.type==="boss")return;
        const oldRarity=t.rarity||RARITY.COMMON;
        if(oldRarity===RARITY.LEGENDARY)return;
        const rarities=[RARITY.COMMON,RARITY.UNCOMMON,RARITY.RARE,RARITY.EPIC,RARITY.LEGENDARY];
        const idx=rarities.indexOf(oldRarity);
        t.rarity=rarities[Math.min(4,idx+2)]; // upgrade by 2 tiers
        if(t.rarity===RARITY.LEGENDARY)legCount72++;
        t.color=t.rarity.color||t.color;
        t.glow=t.rarity.glow||t.glow;
      });
      spawnPopup(x,y,"⭐🔥 STAR FORGE!","#fbbf24",24);
      spawnParticles(x,y,"#fde68a",16,"spark");
      particlesRef.current.push({type:"shockwave",x:canvasRef.current?.width/2||180,y:canvasRef.current?.height/2||320,r:20,maxR:400,born:performance.now(),color:"#fbbf24",alpha:0.5,life:900});
      sfx("legendary");vibrate([8,4,16,4,8]);
      unlock("star_forge_use");
      if(legCount72>0)unlock("star_forge_leg");
      setNotif("⭐ Star Forge! All targets upgraded to rare+!");
    } else if(ptype==="SCORE_FLOOD"){
      // SCORE_FLOOD — each tap spawns an extra +50 popup; 10s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_FLOOD");
      activePwrRef.current.push({type:"SCORE_FLOOD",endsAt:Date.now()+10000,floodTotal:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌊💰 SCORE FLOOD!","#0ea5e9",22);
      spawnParticles(x,y,"#7dd3fc",12,"spark");
      sfx("comboNote",6);vibrate([8,4,8]);
      unlock("score_flood_use");
      setNotif("🌊 Score Flood! Every tap awards +50 bonus!");
    } else if(ptype==="LIFE_LEECH_PLUS"){
      // LIFE_LEECH_PLUS — every 3 taps restore 1 life for 8s
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_LEECH_PLUS");
      activePwrRef.current.push({type:"LIFE_LEECH_PLUS",endsAt:Date.now()+8000,tapCounter:0,livesRestored:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"❤️🩸 LIFE LEECH+!","#f87171",22);
      spawnParticles(x,y,"#fca5a5",12,"spark");
      sfx("comboNote",4);vibrate([6,4,10]);
      unlock("life_leech_plus");
      setNotif("❤️ Life Leech+! Every 3 taps restore a life!");
    } else if(ptype==="ECHO_CHAIN"){
      // ECHO_CHAIN — each tap echoes 0.5s later re-scoring 60% at same position
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="ECHO_CHAIN");
      activePwrRef.current.push({type:"ECHO_CHAIN",endsAt:Date.now()+10000,echoCount:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔊⛓️ ECHO CHAIN!","#22d3ee",22);
      spawnParticles(x,y,"#a5f3fc",12,"spark");
      sfx("comboNote",5);vibrate([6,4,8]);
      unlock("echo_chain_use");
      setNotif("🔊 Echo Chain! Every tap echoes 0.5s later for +60%!");
    } else if(ptype==="GRAVITY_BURST"){
      // GRAVITY_BURST — instant: pull all targets to center and score them all
      const gbGs70=gsRef.current;
      const cw70=canvasRef.current?.width||360;const ch70=canvasRef.current?.height||640;
      const cx70=cw70/2;const cy70=ch70/2;
      const validGB70=targetsRef.current.filter(o=>!o.dying&&o.type!=="bomb"&&o.type!=="boss");
      validGB70.forEach(o=>{
        o.x+=(cx70-o.x)*0.7;o.y+=(cy70-o.y)*0.7;
        o.dying=performance.now();
      });
      const gbPts70=Math.round(validGB70.length*55*((gbGs70?.feverActive)?2:1));
      if(gbGs70)gbGs70.score+=gbPts70;
      particlesRef.current.push({type:"shockwave",x:cx70,y:cy70,r:10,maxR:300,born:performance.now(),color:"#818cf8",alpha:0.6,life:800});
      spawnParticles(cx70,cy70,"#c4b5fd",validGB70.length*2,"spark");
      spawnPopup(cx70,cy70-40,`🌀 BURST! ×${validGB70.length} +${gbPts70}`,"#818cf8",26);
      sfx("chainBonus");vibrate([10,4,14,4,10]);
      unlock("gravity_burst_use");
      if(validGB70.length>=4)unlock("gravity_burst_big");
      setNotif(`🌀 Gravity Burst! ${validGB70.length} targets pulled!`);
    } else if(ptype==="CHAIN_LOCK"){
      // CHAIN_LOCK — next 5 taps chain: each adds previous tap's pts to next
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CHAIN_LOCK");
      activePwrRef.current.push({type:"CHAIN_LOCK",endsAt:Date.now()+15000,charges:5,lastPts:0,chainTotal:0});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🔐⛓️ CHAIN LOCK!","#f59e0b",22);
      spawnParticles(x,y,"#fde68a",12,"spark");
      sfx("comboNote",6);vibrate([6,4,10,4,6]);
      unlock("chain_lock_use");
      setNotif("🔐 Chain Lock! Each tap adds last tap's score!");
    } else if(ptype==="NOVA_STORM"){
      // NOVA_STORM — every 2s for 6s auto-detonates nearest target for half score
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="NOVA_STORM");
      const novaStormPwr69={type:"NOVA_STORM",endsAt:Date.now()+6000,nextStrike:Date.now()+2000};
      activePwrRef.current.push(novaStormPwr69);
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(x,y,"🌩️💥 NOVA STORM!","#fbbf24",22);
      spawnParticles(x,y,"#fde68a",14,"spark");
      sfx("comboNote",8);vibrate([8,4,12,4,8]);
      unlock("nova_storm_use");
      setNotif("🌩️ Nova Storm! Targets auto-detonate every 2s!");
    } else {
      const dur=ptype==="SLOW"?7000:ptype==="FREEZE"?5000:9000;
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!==ptype);
      activePwrRef.current.push({type:ptype,endsAt:Date.now()+dur});
      setActivePwrDisp([...activePwrRef.current]);
      const pLabels={SHIELD:"🛡 SHIELD",SLOW:"🐢 SLOW",DOUBLE:"×2 DOUBLE",FREEZE:"❄️ FREEZE"};
      spawnPopup(x,y,pLabels[ptype]||`+${ptype}`,"#60a5fa",15);
    }
    missionProg.current={...missionProg.current,powerupCollected:(missionProg.current.powerupCollected||0)+1};
  },[sfx,unlock,spawnPopup]);

  // Spawn target
  const spawnTarget=useCallback(()=>{
    const gs=gsRef.current,cfg=levelCfgRef.current;
    if(!gs||gs.lives<=0||!cfg)return;
    const {bombRate,movingRate,ghostRate,bossEnabled,bossRate,rarityBonus,modifier}=cfg;
    // Modifier overrides
    const effMoving=modifier?.type==="moving_only"?0.95:movingRate;
    const effGhost=modifier?.type==="ghost_rush"?0.7:ghostRate;
    const effBomb=modifier?.type==="no_miss"?0:bombRate;
    const r=Math.random();
    // Lucky mode: force legendary rarity!
    const isLucky=luckyRef.current==="active";
    const rarityBoostActive=activePwrRef.current.some(p=>p.type==="RARITY_BOOST"&&p.endsAt>Date.now());
    let type="normal",rarity=isLucky?RARITY.LEGENDARY:getRarity(rarityBonus),color,glow,moving=false,ghost=false;
    if(rarityBoostActive&&[RARITY.COMMON,RARITY.UNCOMMON].includes(rarity))rarity=RARITY.RARE;
    let pwrType=null,hitsLeft=1,maxHits=1,vx=0,vy=0;
    if(modifier?.type==="final_boss"){
      type="boss";color="#fbbf24";glow="#d97706";maxHits=10;hitsLeft=10;
      moving=true;const a0=Math.random()*Math.PI*2;vx=Math.cos(a0)*0.8;vy=Math.sin(a0)*0.8;
    } else if(r<bossRate&&bossEnabled){
      type="boss";color="#ff6030";glow="#ff2000";maxHits=3;hitsLeft=3;
      // Boss patterns by world — every boss now moves from spawn
      moving=true;const ab=Math.random()*Math.PI*2;const spB=0.7+Math.random()*0.6;
      vx=Math.cos(ab)*spB;vy=Math.sin(ab)*spB;
    } else if(r<(bossRate||0)+effBomb){
      // BOMB_CONVERT — convert new bomb to safe light_bomb
      const bcPwr75=activePwrRef.current.find(p=>p.type==="BOMB_CONVERT"&&p.endsAt>Date.now());
      if(bcPwr75){
        type="light_bomb";color="#fbbf24";glow="#fef3c7";
        bcPwr75.converted=(bcPwr75.converted||0)+1;
        unlock("bomb_convert_proc");
      } else {
        type="bomb";color="#ef4444";glow="#dc2626";sfx("bombSpawn");
      }
    } else if(r<(bossRate||0)+effBomb+0.07){
      type="powerup";color="#60a5fa";glow="#3b82f6";
      const pt=["SHIELD","SLOW","DOUBLE","LIFE","FREEZE","LUCKY","MIRROR","MULTIPLIER","COMBO_FREEZE","GRAVITY","CHAIN_LIGHTNING","TIME_WARP","SCORE_BOOST","LIFE_SURGE","MAGNET_FIELD","OVERCLOCK","SHIELD_WALL","PHANTOM_TOUCH","COIN_RAIN","BERSERKER","GHOST_MODE","SCORE_SHIELD","DOUBLE_SPAWN","STAR_RAIN","TIME_STOP","MEGA_TAP","RAINBOW_SURGE","TWIN_STRIKE","GOLDEN_TOUCH","LUCK_SURGE","FREEZE_ZONE","SCORE_X2","MAGNET_PULL","COMBO_SHIELD","PERFECT_AIM","CHAIN_BURST","MAGNET_BURST","SCORE_PULSE","LIFE_SHIELD","MULTIPLIER_SURGE","BOUNCE_BACK","FEVER_EXTEND","COIN_RUSH","GRAVITY_WELL","CLONE_ARMY","RUSH_MODE","INVINCIBLE","HEALER","SCORE_GUARD","SWIPE_BOOST","LUCKY_COIN","ECHO_SHOT","BURST_SHIELD","MAGNET_ORBIT","SCORE_ECHO","PHANTOM_BURST","STREAK_STORM","TIME_BUBBLE","CRIT_BOOST","GOLD_RUSH","MIRROR_MAZE","BLAST_WAVE","TEMPO_SYNC","LIFE_LEECH","SCORE_RAIN","CHAIN_MAGNET","SCORE_SURGE","ORBIT_SHIELD","ECHO_WAVE","COMBO_BURST","PHASE_LOCK","SCORE_LEECH","SPLIT_SHOT","FEVER_STORM","RARITY_BOOST","TWIN_FEVER","REFLECT_WAVE","NOVA_BOOST","STREAK_LOCK","MAGNET_STORM","SCORE_FREEZE","CHAIN_FEVER","LIFE_BOMB","TURBO_TAP","COIN_MAGNET","BLAST_RADIUS","SCORE_ACCELERATOR","LIFE_CHAIN","SCORE_MIRROR","FEVER_LOCK","STREAK_SURGE","COMBO_ECHO","GOLD_SHIELD","PHANTOM_LIFE","SCORE_DOUBLER","MAGNET_BLITZ","CRIT_STORM","BONUS_WAVE","TAP_FURY","SCORE_SPIKE","LIFE_BURST","PHASE_BURST","VORTEX_TAP","CHAIN_LOCK","NOVA_STORM","ECHO_CHAIN","GRAVITY_BURST","SCORE_FLOOD","LIFE_LEECH_PLUS","COMBO_SURGE","STAR_FORGE","PULSE_SHIELD","SCORE_AMPLIFY","RICOCHET_BLAST","FEVER_FOUNTAIN","DOUBLE_FEVER","BOMB_CONVERT","SCORE_MAGNET","LIFE_OVERFLOW","STREAK_HEAL","TEMPO_BURST","CRIT_SURGE","LIFE_PULSE","CHAIN_WAVE","SCORE_OVERDRIVE","PHANTOM_STRIKE","COIN_SURGE","FREEZE_SURGE","LUCKY_STRIKE","ECHO_BURST","SHIELD_SURGE","SCORE_SNAP","LIFE_GUARD","BURST_FEVER","SCORE_TRAIL","CHAIN_SNAP","LIFE_ECHO","SCORE_VAULT","STREAK_ECHO","NOVA_PULSE","COIN_ECHO","TIME_SHIELD","SCORE_BLITZ","PHANTOM_RUSH","COIN_STORM","STREAK_BLAZE","LIFE_BLOOM","FOCUS_BLAST","COIN_CHAIN","SHIELD_BURST","SCORE_ECHO2","COMBO_SURGE2","LIFE_SPIRAL","TEMPO_ECHO","COIN_BLOOM","STREAK_NOVA","LIFE_STORM","GOLD_FRENZY","PHANTOM_COIN","SCORE_FRENZY","LIFE_BEACON","BLAZE_TRAIL","COIN_PULSE","FEVER_BLOOM","COIN_CASCADE","SCORE_OMEGA","ETERNAL_LIFE","ARC_SURGE","RELIC_PULSE","VENOM_SURGE","LUNAR_BLOOM","STAR_CHAIN","VOID_BLOOM","PULSE_WAVE","COIN_VORTEX","GRAVITY_BURST2","SCORE_AVALANCHE","INFERNO_CHAIN","SPECTRAL_COIN","MAGMA_WAVE","COSMIC_ECHO","FROST_LANCE","LIFE_MATRIX","THUNDER_BURST","SHIMMER_COIN","SCORE_QUAKE","ECHO_LIFE","NOVA_CASCADE","TIME_WARP2","SOLAR_PULSE","VOID_CHAIN","EMBER_WAVE","CRYSTAL_NET","PRISM_ECHO","VOLT_SURGE","SHADOW_BLOOM","NEXUS_WAVE","QUANTUM_ECHO","FROST_CROWN"];
      pwrType=pt[Math.floor(Math.random()*pt.length)];
    } else if(r<(bossRate||0)+effBomb+0.07+0.045&&(gs.score>0||Math.random()<0.3)&&luckyRef.current!=="active"){
      // 4.5% treasure chest — the variable reward slot machine
      type="treasure";color="#ffd700";glow="#c8a000";
    } else if((cfg.id||0)>=15&&Math.random()<0.04&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 4% Mimic — copies the last tapped rarity for its score
      type="mimic";color="#c8c8c8";glow="#ffffff";
    } else if((cfg.id||0)>=8&&Math.random()<0.03&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 3% Anchor — freezes all targets for 3s when tapped
      type="anchor";color="#06b6d4";glow="#0e7490";
    } else if((cfg.id||0)>=12&&Math.random()<0.025&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2.5% Splitter — splits into 3 small targets when tapped
      type="splitter";color="#f97316";glow="#c2410c";
    } else if((cfg.id||0)>=10&&Math.random()<0.03&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 3% Shielded — requires 2 taps (shield absorbs first hit)
      type="shielded";color="#6366f1";glow="#4f46e5";hitsLeft=2;maxHits=2;
    } else if((cfg.id||0)>=14&&Math.random()<0.025&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2.5% Magnet — pulls nearby targets toward it
      type="magnet";color="#ec4899";glow="#9d174d";
    } else if((cfg.id||0)>=20&&Math.random()<0.022&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2.2% Phantom — ultra-short lifetime (1.1s), huge points, ghostly appearance
      type="phantom";color="#e879f9";glow="#a21caf";
    } else if((cfg.id||0)>=35&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Ninja — invisible 80% of lifetime, visible only last 20%; huge score reward
      type="ninja";color="#6b7280";glow="#374151";
    } else if((cfg.id||0)>=16&&Math.random()<0.025&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2.5% Bouncy — fast bouncing target, higher points for catching something hard to hit
      type="bouncy";color="#fde047";glow="#eab308";
      moving=true;const boa=Math.random()*Math.PI*2,bsp=2.8+Math.random()*1.8;
      vx=Math.cos(boa)*bsp;vy=Math.sin(boa)*bsp;
    } else if((cfg.id||0)>=28&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Frozen — smaller hit radius (70%), but 3× points
      type="frozen";color="#bfdbfe";glow="#93c5fd";
    } else if((cfg.id||0)>=30&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Rainbow — cycles through rarity tiers every 2s; score based on current tier when tapped
      type="rainbow";color="#ff6030";glow="#ff4400";
    } else if((cfg.id||0)>=22&&Math.random()<0.02&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2% Twin — spawns as a pair; tapping one scores both (bonus points for efficiency)
      type="twin";color="#f59e0b";glow="#d97706";
      // Spawn a matching partner at a nearby position
      const partnerPos=pickPos(BASE_R*1.1);
      const twinId=Math.random().toString(36).slice(2);
      const twinId2=Math.random().toString(36).slice(2);
      // We'll link them by sharing a twinGroupId; handled in tap handler
      gs._pendingTwin={id:twinId2,pos:partnerPos,groupId:twinId,lt:lifetime};
    } else if((cfg.id||0)>=18&&Math.random()<0.015&&gs.lives<cfg.lives&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Healer — only spawns if player has lost lives; restores 1 life when tapped
      type="healer";color="#34d399";glow="#059669";
    } else if((cfg.id||0)>=25&&Math.random()<0.018&&!gs.bonusRoundActive&&effBomb>0&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Volatile — tap to defuse (scores 250pts), or it explodes on expiry (costs a life)
      type="volatile";color="#ff4500";glow="#dc2626";
    } else if((cfg.id||0)>=5&&Math.random()<0.028&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2.8% Bubble — large hit zone (1.8× radius), pops for points (easy/fun target for beginners)
      type="bubble";color="#e0f7ff";glow="#67e8f9";
    } else if((cfg.id||0)>=20&&Math.random()<0.022&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2.2% Echo — tap it to score, then a ghost echo appears for a bonus second tap
      type="echo";color="#a78bfa";glow="#7c3aed";
    } else if((cfg.id||0)>=30&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Crystal — hexagonal gem; shatters into 3 scoreable shards on tap
      type="crystal";color="#bfdbfe";glow="#3b82f6";
    } else if((cfg.id||0)>=24&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Tornado — scrambles nearby target positions in a vortex effect
      type="tornado";color="#67e8f9";glow="#06b6d4";
    } else if((cfg.id||0)>=8&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Loot Chest — big golden chest, drops 3-5 coins on tap
      type="lootchest";color="#ffd700";glow="#b45309";
    } else if((cfg.id||0)>=25&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Rage — starts calm, grows angry/faster over lifetime; worth more points when tapped at high rage
      type="rage";color="#ef4444";glow="#dc2626";moving=true;
    } else if((cfg.id||0)>=15&&Math.random()<0.02&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2% Divider — large orange orb; splits into 2 medium targets when tapped
      type="divider";color="#fb923c";glow="#ea580c";
    } else if((cfg.id||0)>=22&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Vanishing — blinks in/out of visibility; big reward for blind timing
      type="vanishing";color="#67e8f9";glow="#0ea5e9";
    } else if((cfg.id||0)>=12&&Math.random()<0.02&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2% Homing — slowly drifts toward screen center; strategic since it comes to you
      type="homing";color="#34d399";glow="#059669";moving=true;
    } else if((cfg.id||0)>=15&&Math.random()<0.022&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2.2% Morph — cycles through rarity tiers every 1.4s; catch during legendary = jackpot
      type="morph";color="#ffffff";glow="#ffffff";moving=Math.random()<0.3;
      if(moving){const a=Math.random()*Math.PI*2,sp=0.5+Math.random()*0.8;vx=Math.cos(a)*sp;vy=Math.sin(a)*sp;}
    } else if((cfg.id||0)>=50&&Math.random()<0.004&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.4% Nexus — ultra-rare cosmic core; 2000+ pts; once per session check
      type="nexus";color="#ffd700";glow="#b8860b";
    } else if((cfg.id||0)>=18&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Ice Comet — streaks across screen freezing nearby targets on impact
      type="icecomet";color="#38bdf8";glow="#0ea5e9";moving=true;
      const angle=(Math.PI*0.2+Math.random()*Math.PI*0.6);
      const speed=2.0+Math.random()*1.0;
      vx=Math.cos(angle)*speed;vy=Math.sin(angle)*speed;
    } else if((cfg.id||0)>=12&&Math.random()<0.02&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2% Phoenix — fiery bird; respawns once as epic when expired
      type="phoenix";color="#f97316";glow="#dc2626";moving=Math.random()<0.35;
      if(moving){const a=Math.random()*Math.PI*2,sp=0.5+Math.random()*0.7;vx=Math.cos(a)*sp;vy=Math.sin(a)*sp;}
    } else if((cfg.id||0)>=30&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Mirror Ball — disco sphere; tap to spawn 3 mini copies
      type="mirrorball";color="#e2e8f0";glow="#94a3b8";
    } else if((cfg.id||0)>=20&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Comet — streaks across the screen, 3.5s lifetime; trail particles
      type="comet";color="#fde68a";glow="#f59e0b";moving=true;
      // Start from a random edge and fly to the opposite side
      const fromSide=Math.floor(Math.random()*2); // 0=top, 1=left
      // We'll set position via pickPos override below, vx/vy set here
      const speed=2.2+Math.random()*1.2;
      const angle=fromSide===0
        ?(Math.PI*0.3+Math.random()*Math.PI*0.4) // top → down-ish
        :(Math.random()*Math.PI*0.6-Math.PI*0.3); // left → right-ish
      vx=Math.cos(angle)*speed;vy=Math.sin(angle)*speed;
    } else if((cfg.id||0)>=25&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Prism — beautiful rainbow diamond; splits score into 3 rainbow beams
      type="prism";color="#f0abfc";glow="#a21caf";
    } else if((cfg.id||0)>=22&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Glitch — teleports randomly every 0.9s; premium points
      type="glitch";color="#00ff88";glow="#00cc66";moving=true;
      const a=Math.random()*Math.PI*2;vx=Math.cos(a)*0.6;vy=Math.sin(a)*0.6;
    } else if((cfg.id||0)>=18&&!cfg.isZen&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Siphon — drains 1 life on expiry; tapping it rewards 1000+ pts
      type="siphon";color="#dc2626";glow="#7f1d1d";moving=Math.random()<0.4;
      if(moving){const a=Math.random()*Math.PI*2,sp=0.5+Math.random()*0.7;vx=Math.cos(a)*sp;vy=Math.sin(a)*sp;}
    } else if((cfg.id||0)>=8&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!gs._conductorActive&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Conductor — when tapped boosts spawn rate for 5s and gives staccato combo bonus
      type="conductor";color="#f97316";glow="#c2410c";
    } else if((cfg.id||0)>=40&&Math.random()<0.01&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1% Gemstone — rare prismatic gem; scores 500+ base pts (like legendary rarity)
      type="gemstone";color="#e879f9";glow="#9333ea";
    } else if((cfg.id||0)>=20&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&effBomb>0&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Poison — DON'T tap it (or tap to neutralize); expires = next target 50% score
      type="poison";color="#4ade80";glow="#16a34a";
      const homingAngle=Math.random()*Math.PI*2;
      vx=Math.cos(homingAngle)*0.4;vy=Math.sin(homingAngle)*0.4;
      const initSpd=0.6+Math.random()*0.4;const angle=Math.random()*Math.PI*2;
      vx=Math.cos(angle)*initSpd;vy=Math.sin(angle)*initSpd;
    } else if((cfg.id||0)>=25&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Spectral — phases in/out; 800pts if caught visible, normal pts if invisible
      type="spectral";color="#a5b4fc";glow="#6366f1";moving=Math.random()<0.25;
      if(moving){const a=Math.random()*Math.PI*2,sp=0.3+Math.random()*0.5;vx=Math.cos(a)*sp;vy=Math.sin(a)*sp;}
    } else if((cfg.id||0)>=6&&Math.random()<0.016&&gs.lives<(levelCfgRef.current?.lives||3)+1&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Heart — gives +1 life; only spawns when player has lost lives
      type="heart";color="#f43f5e";glow="#be123c";
    } else if((cfg.id||0)>=30&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Nova — freezes all targets for 2s + base score
      type="nova";color="#fbbf24";glow="#b45309";
    } else if((cfg.id||0)>=14&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Beacon — stationary; doubles nearby tap scores while alive
      type="beacon";color="#fbbf24";glow="#b45309";moving=false;
    } else if((cfg.id||0)>=18&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Shadow — leaves clone on tap; tap clone for bonus
      type="shadow";color="#4338ca";glow="#6366f1";moving=Math.random()<0.35;
      if(moving){const a=Math.random()*Math.PI*2,sp=0.4+Math.random()*0.6;vx=Math.cos(a)*sp;vy=Math.sin(a)*sp;}
    } else if((cfg.id||0)>=20&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Portal — teleports all targets to new positions on tap
      type="portal";color="#818cf8";glow="#4f46e5";
    } else if((cfg.id||0)>=15&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&effBomb>0&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Particle Bomb — explodes into scoreable sparks when tapped
      type="particlebomb";color="#ff8c00";glow="#ff4500";
    } else if((cfg.id||0)>=12&&Math.random()<0.019&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.9% Ricochet — tap to ricochet-kill 2 nearby targets
      type="ricochet";color="#94a3b8";glow="#cbd5e1";moving=Math.random()<0.4;
      if(moving){const a=Math.random()*Math.PI*2,sp=1.0+Math.random()*1.0;vx=Math.cos(a)*sp;vy=Math.sin(a)*sp;}
    } else if((cfg.id||0)>=28&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Aurora — rhythm-based tap multiplier (1-5×)
      type="aurora";color="#38bdf8";glow="#0284c7";
    } else if((cfg.id||0)>=10&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Lucky Clover — random 1-10× multiplier on tap
      type="clover";color="#22c55e";glow="#16a34a";
    } else if((cfg.id||0)>=15&&Math.random()<0.02&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2% Voltage — zaps 3 nearby targets in a chain when tapped
      type="voltage";color="#fbbf24";glow="#f59e0b";moving=Math.random()<0.3;
      if(moving){const a=Math.random()*Math.PI*2,sp=0.4+Math.random()*0.6;vx=Math.cos(a)*sp;vy=Math.sin(a)*sp;}
    } else if((cfg.id||0)>=35&&Math.random()<0.01&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1% Void — absorbs up to 5 nearby targets for massive bonus score
      type="void";color="#7c3aed";glow="#4c1d95";
    } else if((cfg.id||0)>=15&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Pinwheel — spinning target; bonus pts if caught at "top" spin position
      type="pinwheel";color="#fbbf24";glow="#f97316";moving=true;
      const pw=canvasRef.current?.width||390,ph2=canvasRef.current?.height||700;
      // Pinwheel orbits around center of screen
      const orbitA=Math.random()*Math.PI*2;const orbitR=60+Math.random()*80;
      const ocx=pw/2+(-1+Math.random()*2)*60,ocy=ph2/2+(-1+Math.random()*2)*60;
      vx=0;vy=0; // velocity handled by orbit logic
      // Store orbit params on pending push — we'll use a trick with the target object
    } else if((cfg.id||0)>=18&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Supercell — electrifies nearby targets (makes them score 2× for 6s)
      type="supercell";color="#38bdf8";glow="#7dd3fc";
    } else if((cfg.id||0)>=22&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Prism Bomb — sends 3 rainbow beams on tap, each killing first target hit
      type="prismbomb";color="#f0abfc";glow="#c084fc";
    } else if((cfg.id||0)>=30&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Ice Cage — freezes nearby targets; tap to collect all frozen at once
      type="icecage";color="#7dd3fc";glow="#bae6fd";
    } else if((cfg.id||0)>=20&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Solar Flare — expands over lifetime; less pts the bigger it grows
      type="solarflare";color="#f97316";glow="#fde68a";
    } else if((cfg.id||0)>=28&&!cfg.isZen&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Magma — hunts nearest normal target; defuse before it destroys it
      type="magma";color="#dc2626";glow="#f97316";moving=true;
      const ma=Math.random()*Math.PI*2,ms=0.6+Math.random()*0.4;vx=Math.cos(ma)*ms;vy=Math.sin(ma)*ms;
    } else if((cfg.id||0)>=12&&Math.random()<0.02&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2% Chain Collector — auto-grabs 3 nearest targets on tap
      type="chaincollector";color="#34d399";glow="#6ee7b7";
    } else if((cfg.id||0)>=18&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Stardust — releases 8 collectible star particles
      type="stardust";color="#fbbf24";glow="#fde68a";moving=Math.random()<0.25;
      if(moving){const a=Math.random()*Math.PI*2,sp=0.4+Math.random()*0.5;vx=Math.cos(a)*sp;vy=Math.sin(a)*sp;}
    } else if((cfg.id||0)>=15&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Bloom — flower that explodes into 5 petal targets on tap
      type="bloom";color="#ff6b9d";glow="#ffd1dc";
    } else if((cfg.id||0)>=20&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Reflector — hex mirror; reverses all moving targets + 600pts
      type="reflector";color="#22d3ee";glow="#67e8f9";
    } else if((cfg.id||0)>=32&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Quantum — requires 3 taps; teleports on each hit until stabilized
      type="quantum";color="#a78bfa";glow="#7c3aed";hitsLeft=3;maxHits=3;
      moving=true;const qa=Math.random()*Math.PI*2,qs=0.5+Math.random()*0.8;vx=Math.cos(qa)*qs;vy=Math.sin(qa)*qs;
    } else if((cfg.id||0)>=20&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Clockwork — slows all targets to 30% speed for 3s when tapped
      type="clockwork";color="#94a3b8";glow="#cbd5e1";
    } else if((cfg.id||0)>=22&&!cfg.isZen&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Thunderbolt — fast falling target; defuse for 700pts or it exits and costs a life
      type="thunderbolt";color="#facc15";glow="#fef08a";moving=true;
      vx=0;vy=2.8+Math.random()*1.4; // falls straight down
    } else if((cfg.id||0)>=25&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Gravity Orb — pulls normal targets toward it; tap to score and release them
      type="gravityorb";color="#7c3aed";glow="#a78bfa";
    } else if((cfg.id||0)>=30&&Math.random()<0.01&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1% Crystal Ball — guarantees lucky spawn after tapped
      type="crystalball";color="#c084fc";glow="#e879f9";
    } else if((cfg.id||0)>=18&&Math.random()<0.022&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2.2% Firefly — tiny fast flickering target; hard to hit, 500+ pts
      type="firefly";color="#22c55e";glow="#84ef63";moving=true;
      const fa=Math.random()*Math.PI*2;const fsp=2.6+Math.random()*1.8;
      vx=Math.cos(fa)*fsp;vy=Math.sin(fa)*fsp;
    } else if((cfg.id||0)>=20&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Geode — 2-tap rock; first tap cracks it, second awards gem + coins
      type="geode";color="#64748b";glow="#94a3b8";hitsLeft=2;maxHits=2;
    } else if((cfg.id||0)>=28&&!cfg.isZen&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Time Bomb — tap to defuse (+800 pts) or it detonates (−2 lives) after 3s
      type="timebomb";color="#f97316";glow="#dc2626";moving=Math.random()<0.3;
      if(moving){const a=Math.random()*Math.PI*2,sp=0.4+Math.random()*0.5;vx=Math.cos(a)*sp;vy=Math.sin(a)*sp;}
    } else if((cfg.id||0)>=20&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Vortex — dark swirling portal that pulls nearby targets in
      type="vortex";color="#7c3aed";glow="#a78bfa";
    } else if((cfg.id||0)>=18&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Pixel — tiny retro 8-bit target; tiny hitbox, 800+ pts
      type="pixel";color="#00ff88";glow="#00cc66";
    } else if((cfg.id||0)>=12&&Math.random()<0.02&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2% Neon Sign — flashes between 3 color phases; bonus based on phase when tapped
      type="neon";color="#ff2244";glow="#ff6688";
    } else if((cfg.id||0)>=8&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Lantern — tap to trigger chain reaction with 2 nearest targets
      type="lantern";color="#fbbf24";glow="#f97316";
    } else if((cfg.id||0)>=16&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Drift — wavy moving target; more pts the further from center when tapped
      type="drift";color="#38bdf8";glow="#7dd3fc";moving=true;
      const da=Math.random()*Math.PI*2,ds=0.7+Math.random()*0.9;
      vx=Math.cos(da)*ds;vy=Math.sin(da)*ds;
    } else if((cfg.id||0)>=22&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Nebula — cosmic cloud; releases 3-4 star_piece collectibles on tap
      type="nebula";color="#818cf8";glow="#6d28d9";
    } else if((cfg.id||0)>=25&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Wisp — ethereal; nearly invisible when moving, huge pts when still
      type="wisp";color="#e0f2fe";glow="#7dd3fc";moving=Math.random()<0.5;
      if(moving){const wa=Math.random()*Math.PI*2,ws=0.6+Math.random()*0.8;vx=Math.cos(wa)*ws;vy=Math.sin(wa)*ws;}
    } else if((cfg.id||0)>=18&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Meteor — fast diagonal rock; tap early for bonus
      type="meteor";color="#f97316";glow="#fde68a";moving=true;
      const ma=(Math.PI*0.2+Math.random()*Math.PI*0.6);vx=Math.cos(ma)*(2.2+Math.random()*1.0);vy=Math.sin(ma)*(2.2+Math.random()*1.0);
    } else if((cfg.id||0)>=14&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Aura Ring — pulsing ring; edge tap = jackpot vs center = base pts
      type="aura";color="#818cf8";glow="#6366f1";
    } else if((cfg.id||0)>=6&&Math.random()<0.02&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 2% Smoke Ring — expands outward; tap early for more pts
      type="smoke_ring";color="#94a3b8";glow="#cbd5e1";
    } else if((cfg.id||0)>=20&&!cfg.isZen&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&effBomb>0&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Chain Bomb — connected to a bomb; defuse both for 1500pts jackpot
      type="chain_bomb";color="#f97316";glow="#fde68a";
    } else if((cfg.id||0)>=25&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Warp Gate — teleports all targets to new positions on tap
      type="warp_gate";color="#06b6d4";glow="#67e8f9";
    } else if((cfg.id||0)>=30&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Mirage — looks like legendary but is decoy (50pts); learn to spot it
      type="mirage";color="#fbbf24";glow="#fde68a";
    } else if((cfg.id||0)>=14&&Math.random()<0.017&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.7% Solar Drone — orbiting solar target; 2× pts if caught mid-orbit
      type="solar_drone";color="#fbbf24";glow="#f97316";moving=true;
      const sda=Math.random()*Math.PI*2,sds=0.8+Math.random()*0.6;
      vx=Math.cos(sda)*sds;vy=Math.sin(sda)*sds;
    } else if((cfg.id||0)>=18&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Crystal Cluster — 4 mini crystals; shatter all at once for rainbow bonus
      type="crystal_cluster";color="#38bdf8";glow="#818cf8";
    } else if((cfg.id||0)>=10&&Math.random()<0.019&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.9% Swarm — orbit bee swarm; big pts plus session counter for master
      type="swarm";color="#f97316";glow="#fed7aa";moving=true;
      const swa=Math.random()*Math.PI*2,sws=0.7+Math.random()*0.7;
      vx=Math.cos(swa)*sws;vy=Math.sin(swa)*sws;
    } else if((cfg.id||0)>=24&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Celestial Orb — large slow target; each tap tracked for celestial_legend
      type="celestial";color="#1e40af";glow="#93c5fd";moving=Math.random()<0.2;
      if(moving){const ca=Math.random()*Math.PI*2;vx=Math.cos(ca)*0.4;vy=Math.sin(ca)*0.4;}
    } else if((cfg.id||0)>=18&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Fractal — splits into 2 smaller copies on tap (up to depth 2)
      type="fractal";color="#818cf8";glow="#6366f1";
    } else if((cfg.id||0)>=22&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Plasma — charges up; top pts at 70-90% charge window
      type="plasma";color="#22d3ee";glow="#00ffcc";
    } else if((cfg.id||0)>=20&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Timewarp Target — extends all targets' lifetimes +2s when tapped
      type="timewarp_tgt";color="#818cf8";glow="#c7d2fe";
    } else if((cfg.id||0)>=16&&!cfg.isZen&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Thunderclap — shockwave on tap knocks back nearby targets
      type="thunderclap";color="#4338ca";glow="#fde047";
    } else if((cfg.id||0)>=22&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Mirror Shard — 3-tap reflective diamond; each tap worth more
      type="mirror_shard";color="#e0f2fe";glow="#bae6fd";hitsLeft=3;maxHits=3;
    } else if((cfg.id||0)>=20&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Rune Stone — cycles power runes; score based on which rune phase when tapped
      type="rune_stone";color="#7c3aed";glow="#a855f7";
    } else if((cfg.id||0)>=16&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Surge Ball — charged electric sphere; chains lightning to 3 nearest targets on tap
      type="surge_ball";color="#facc15";glow="#fef08a";
    } else if((cfg.id||0)>=22&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Ticking Clock — tap as the hand sweeps near 12 o'clock for jackpot
      type="ticking_clock";color="#94a3b8";glow="#e2e8f0";
    } else if((cfg.id||0)>=18&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Echo Pulse — emits expanding rings; tap to collect all live rings for bonus
      type="echo_pulse";color="#c084fc";glow="#e879f9";
    } else if((cfg.id||0)>=24&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Gravity Well — dark sphere; pulls nearby targets as it ages; tap for pts based on pulled count
      type="gravity_well";color="#7c3aed";glow="#a78bfa";
    } else if((cfg.id||0)>=10&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Spectrum Orb — cycling rainbow; pts multiplier peaks at yellow phase
      type="spectrum_orb";color="#e879f9";glow="#f0abfc";
    } else if((cfg.id||0)>=20&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Shard Storm — rotating crystal shards; jackpot when shards align north
      type="shard_storm";color="#67e8f9";glow="#a5f3fc";
    } else if((cfg.id||0)>=26&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Pulse Nova — charges then releases; big pts during release window
      type="pulse_nova";color="#fb7185";glow="#fda4af";
    } else if((cfg.id||0)>=8&&Math.random()<0.019&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.9% Bubble Cluster — 5 linked soap bubbles; pop all for burst bonus
      type="bubble_cluster";color="#7dd3fc";glow="#e0f2fe";
    } else if((cfg.id||0)>=28&&!cfg.isZen&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Void Shard — absorbs pts from nearby targets while alive; tap to reclaim all
      type="void_shard";color="#0ea5e9";glow="#38bdf8";
    } else if((cfg.id||0)>=18&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Stasis Field — expanding freeze ring; jackpot inside the ring before it passes
      type="stasis_field";color="#38bdf8";glow="#bae6fd";
    } else if((cfg.id||0)>=12&&Math.random()<0.017&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.7% Crystalline — rotating faceted gem; score based on visible facets at tap time
      type="crystalline";color="#c084fc";glow="#e9d5ff";
    } else if((cfg.id||0)>=20&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Quantum Leap — teleports every 1.2s; catch in-between phases for jackpot
      type="quantum_leap";color="#818cf8";glow="#c7d2fe";
    } else if((cfg.id||0)>=14&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Magma Burst — heats up over lifetime; tap at white-hot for 900pts
      type="magma_burst";color="#ef4444";glow="#fca5a5";
    } else if((cfg.id||0)>=18&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Lightning Rod — static; score 60pts per attracted nearby target
      type="lightning_rod";color="#fbbf24";glow="#fef08a";
    } else if((cfg.id||0)>=22&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Prism Gate — splits into 3 shards on tap; catch all for tri-color bonus
      type="prism_gate";color="#67e8f9";glow="#a5f3fc";
    } else if((cfg.id||0)>=12&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Solar Burst — radiant sun; AoE scores all targets within range on tap
      type="solar_burst";color="#fbbf24";glow="#fef9c3";
    } else if((cfg.id||0)>=16&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Frost Comet — splits into 3 ice shards; shards can freeze nearby targets
      type="frost_comet";color="#7dd3fc";glow="#f0f9ff";moving=true;
      const fca=Math.random()*Math.PI*2;vx=Math.cos(fca)*(1.0+Math.random()*0.8);vy=Math.sin(fca)*(1.0+Math.random()*0.8);
    } else if((cfg.id||0)>=20&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Energy Web — connects to nearby targets; score × node count on tap
      type="energy_web";color="#34d399";glow="#a7f3d0";
    } else if((cfg.id||0)>=30&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Void Rift — grows over time devouring nearby targets; massive pts per devoured
      type="void_rift";color="#7c3aed";glow="#a78bfa";
    } else if((cfg.id||0)>=16&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Laser Grid — rotating laser reticle; beam alignment = jackpot
      type="laser_grid";color="#f43f5e";glow="#fda4af";
    } else if((cfg.id||0)>=10&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Bouncing Orb — bounces off walls; multiplier grows per bounce (max 8×)
      type="bouncing_orb";color="#4ade80";glow="#86efac";moving=true;
      const boa=Math.random()*Math.PI*2;const bos=1.4+Math.random()*0.8;
      vx=Math.cos(boa)*bos;vy=Math.sin(boa)*bos;
    } else if((cfg.id||0)>=24&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Time Capsule — fixed 5s; golden window at 2.5-3.5s = 1000pts jackpot
      type="time_capsule";color="#f59e0b";glow="#fcd34d";
    } else if((cfg.id||0)>=28&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Nether Shard — grows stronger the longer it lives (max ×10 at end of lifetime)
      type="nether_shard";color="#7c3aed";glow="#c4b5fd";
    } else if((cfg.id||0)>=12&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Accumulator — grows with each expired target nearby; score scales with stacks
      type="accumulator";color="#3b82f6";glow="#93c5fd";
    } else if(!gs._jackpotSpawned&&(cfg.id||0)>=20&&Math.random()<0.008&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.8% Jackpot — once per level giant moving target with guaranteed high pts
      type="jackpot_target";color="#ffd700";glow="#fef08a";moving=true;gs._jackpotSpawned=true;
      const ja=Math.random()*Math.PI*2;vx=Math.cos(ja)*(0.8+Math.random()*0.6);vy=Math.sin(ja)*(0.8+Math.random()*0.6);
    } else if((cfg.id||0)>=14&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Phase Orb — cycles through 3 phases; tap during bonus phase for big pts
      type="phase_orb";color="#3b82f6";glow="#93c5fd";
    } else if((cfg.id||0)>=18&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Ripple Wave — emits expanding rings that stun nearby targets; tap to collect
      type="ripple_wave";color="#06b6d4";glow="#67e8f9";
    } else if((cfg.id||0)>=10&&Math.random()<0.017&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.7% Venom Orb — tap within 1s for antidote (500pts), late tap = 50pts
      type="venom_orb";color="#4ade80";glow="#86efac";
    } else if((cfg.id||0)>=14&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Thunder Egg — cracks in 3 phases; jackpot at full crack
      type="thunder_egg";color="#fbbf24";glow="#fef08a";
    } else if((cfg.id||0)>=18&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Moon Glyph — cycles 5 symbols; tap during ☽ phase = jackpot
      type="moon_glyph";color="#818cf8";glow="#c7d2fe";
    } else if((cfg.id||0)>=12&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Comet Tail — fast moving; early tap = big bonus
      type="comet_tail";color="#fbbf24";glow="#fef9c3";moving=true;
      const cta=Math.random()*Math.PI*2;vx=Math.cos(cta)*(2.0+Math.random()*1.2);vy=Math.sin(cta)*(2.0+Math.random()*1.2);
    } else if((cfg.id||0)>=16&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Ember Ring — expands outward; tap at max expansion for jackpot
      type="ember_ring";color="#f97316";glow="#fed7aa";
    } else if((cfg.id||0)>=20&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Supernova — tap to explode into 5 mini-orbs; collect for bonus
      type="supernova";color="#fbbf24";glow="#fef9c3";
    } else if((cfg.id||0)>=16&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Crystal Matrix — 3×3 grid; 3-tap to shatter all layers
      type="crystal_matrix";color="#38bdf8";glow="#7dd3fc";hitsLeft=3;maxHits=3;
    } else if((cfg.id||0)>=24&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Plasma Storm — electrifies all nearby targets; score × count on tap
      type="plasma_storm";color="#22d3ee";glow="#a5f3fc";
    } else if((cfg.id||0)>=26&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")&&!gs._nexusActive){
      // 1.1% Nexus Gate — spawns pair; tap = portal chain
      type="nexus_gate";color="#6366f1";glow="#a5b4fc";gs._nexusActive=true;
    } else if((cfg.id||0)>=8&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Twilight Orb — fades to invisible; jackpot at <15% visible
      type="twilight_orb";color="#6366f1";glow="#e0e7ff";
    } else if((cfg.id||0)>=30&&Math.random()<0.006&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")&&gs.lives<=(levelCfgRef.current?.lives||3)){
      // 0.6% Life Orb — only spawns when player is low on lives; +1 life
      type="life_orb";color="#22c55e";glow="#dcfce7";
    } else if((cfg.id||0)>=20&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Score Amp Orb — nearby taps get 2× while it lives
      type="score_amp";color="#f43f5e";glow="#ffe4e6";
    } else if((cfg.id||0)>=15&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Night Orb — brightens over time; peak = jackpot
      type="night_orb";color="#4c1d95";glow="#a78bfa";
    } else if((cfg.id||0)>=40&&Math.random()<0.006&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.6% Omega Orb — rare legendary; 3000pts + spectacle
      type="omega_orb";color="#ffd700";glow="#fef9c3";
    } else if((cfg.id||0)>=20&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Thunder Ball — charges nearby targets for 2× pts
      type="thunder_ball";color="#f59e0b";glow="#fef3c7";
    } else if((cfg.id||0)>=28&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Cosmic Aura — 300pts + LUCKY power-up for 5s
      type="cosmic_aura";color="#6366f1";glow="#e0e7ff";
    } else if((cfg.id||0)>=10&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Combo Rush — pts = 80 × current streak (capped at 50 streak)
      type="combo_rush";color="#f97316";glow="#fed7aa";
    } else if((cfg.id||0)>=8&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Neon Crystal — 3 phases (cold/warm/hot); hot = jackpot
      type="neon_crystal";color="#67e8f9";glow="#ecfeff";
    } else if((cfg.id||0)>=20&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Sonar Ping — expanding rings; bonus per target within ring
      type="sonar_ping";color="#4ade80";glow="#dcfce7";
    } else if((cfg.id||0)>=14&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Gravity Bomb — falls toward bottom; defuse or -1 life on floor hit
      type="gravity_bomb";color="#f97316";glow="#fed7aa";vy=0.5+Math.random()*0.3;moving=true;
    } else if((cfg.id||0)>=16&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Speed Nova — starts tiny, grows to max; pts scale with size
      type="speed_nova";color="#c084fc";glow="#fdf4ff";
    } else if((cfg.id||0)>=18&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Quantum Split — splits into 2 halves on tap; collect both for bonus
      type="quantum_split";color="#3b82f6";glow="#dbeafe";hitsLeft=1;maxHits=1;
    } else if((cfg.id||0)>=10&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Frost Bolt — ultrafast; tap early for 3× pts
      type="frost_bolt";color="#22d3ee";glow="#ecfeff";
      moving=true;const fba=Math.random()*Math.PI*2;const fbsp=2.8+Math.random()*1.5;
      vx=Math.cos(fba)*fbsp;vy=Math.sin(fba)*fbsp;
    } else if((cfg.id||0)>=14&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Vortex Star — collects trailing stars; pts = 50 × star count
      type="vortex_star";color="#f59e0b";glow="#fef3c7";
    } else if((cfg.id||0)>=12&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Pixel Burst — 3-tap target; each tap un-pixelates it
      type="pixel_burst";color="#6366f1";glow="#a5b4fc";hitsLeft=3;maxHits=3;
    } else if((cfg.id||0)>=16&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Star Forge — charges over 4s; 3 pts tiers
      type="star_forge";color="#6366f1";glow="#a5b4fc";
    } else if((cfg.id||0)>=8&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Magic Dice — rolling die; pts = 100 × face
      type="magic_dice";color="#a855f7";glow="#e9d5ff";
    } else if((cfg.id||0)>=24&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Wormhole — teleports all targets on tap
      type="wormhole_tgt";color="#06b6d4";glow="#cffafe";
    } else if((cfg.id||0)>=12&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Shadow Pulse — dark/bright cycling; dark phase = 3× pts
      type="shadow_pulse";color="#4c1d95";glow="#8b5cf6";
    } else if((cfg.id||0)>=10&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Eclipse Orb — cycles sun/moon; moon phase = 3× pts
      type="eclipse_orb";color="#6366f1";glow="#c7d2fe";
    } else if((cfg.id||0)>=14&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Spin Bloom — 8 petals fade over time; pts per petal remaining
      type="spin_bloom";color="#f472b6";glow="#fce7f3";
    } else if((cfg.id||0)>=22&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Tesla Node — auto-banks pts by zapping nearby targets every 1.2s
      type="tesla_node";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=18&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Rune Circle — 6 rotating runes; legendary rune = jackpot
      type="rune_circle";color="#a78bfa";glow="#ede9fe";
    } else if((cfg.id||0)>=20&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Solar Orbit — 3 orbiting planets; bonus per planet in alignment
      type="solar_orbit";color="#f59e0b";glow="#fef3c7";
    } else if((cfg.id||0)>=15&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Chroma Shift — cycles 6 colors; pts depend on active color
      type="chroma_shift";color="#e879f9";glow="#fdf4ff";
    } else if((cfg.id||0)>=18&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Neon Pulsar — pulses in size; 3× pts at peak
      type="neon_pulsar";color="#e879f9";glow="#fdf4ff";
    } else if((cfg.id||0)>=12&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Drift Mine — slow hazard; defuse it or lose a life on expiry
      type="drift_mine";color="#ef4444";glow="#fca5a5";
      const dma=Math.random()*Math.PI*2;vx=Math.cos(dma)*0.35;vy=Math.sin(dma)*0.35;moving=true;
    } else if((cfg.id||0)>=22&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Arc Light — electric node; arcs to nearby targets, bonus per arc
      type="arc_light";color="#facc15";glow="#fef9c3";
    } else if((cfg.id||0)>=18&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Pulse Chain — chain reaction destroys nearby normals simultaneously
      type="pulse_chain";color="#f97316";glow="#fed7aa";
    } else if((cfg.id||0)>=14&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Ghost Orb — fades invisible; phantom tap at 90%+ for 3× pts
      type="ghost_orb";color="#8b5cf6";glow="#ddd6fe";
    } else if((cfg.id||0)>=25&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Time Fracture — freezes all other targets 2.5s when tapped
      type="time_fracture";color="#06b6d4";glow="#cffafe";
    } else if((cfg.id||0)>=10&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Shifting Target — cycles 5 phases every 600ms; pts scale with phase
      type="shift_target";color="#a3e635";glow="#84cc16";
    } else if((cfg.id||0)>=8&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Lucky Wheel — spinning number (1-20); pts = 100 × number
      type="lucky_wheel";color="#f59e0b";glow="#fcd34d";
    } else if((cfg.id||0)>=15&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Abyss Orb — penalty target; drains 200pts on expiry if untapped
      type="abyss_orb";color="#7c3aed";glow="#a78bfa";
    } else if((cfg.id||0)>=20&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Cosmic Portal — next tap after this gives 5× pts
      type="cosmic_portal";color="#6d28d9";glow="#c4b5fd";
    } else if((cfg.id||0)>=12&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Flame Ring — shrinking ring of fire; sweet spot for jackpot
      type="flame_ring";color="#f97316";glow="#fed7aa";
    } else if((cfg.id||0)>=18&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Ice Clock — 5s countdown; pts = 200 × secs left
      type="ice_clock";color="#7dd3fc";glow="#e0f2fe";
    } else if((cfg.id||0)>=10&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Holo Decoy — real (60%) or decoy (40%); unknown to player
      type="holo_decoy";color="#22d3ee";glow="#cffafe";
    } else if((cfg.id||0)>=22&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Mirror Gate — next tap creates a mirrored bonus clone
      type="mirror_gate";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=16&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Spiral Bomb — fills spiral; jackpot at full
      type="spiral_bomb";color="#f97316";glow="#fed7aa";
    } else if((cfg.id||0)>=12&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Chain Orb — 4-tap chain with escalating pts
      type="chain_orb";color="#38bdf8";glow="#e0f2fe";hitsLeft=4;maxHits=4;
    } else if((cfg.id||0)>=10&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Bolt Catcher — fast urgency; miss = lightning penalty
      type="bolt_catcher";color="#f59e0b";glow="#fef3c7";
    } else if((cfg.id||0)>=20&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Gem Harvest — collects nearby targets into gems
      type="gem_harvest";color="#10b981";glow="#d1fae5";
    } else if((cfg.id||0)>=14&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Prism Pulse — cycling rainbow; must match target color
      type="prism_pulse";color="#e879f9";glow="#fdf4ff";
    } else if((cfg.id||0)>=22&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Dark Matter — nearly invisible; tap blind for jackpot
      type="dark_matter";color="#6d28d9";glow="#a78bfa";
    } else if((cfg.id||0)>=10&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Comet Blast — fast mover; spawns 4 shards on tap
      type="comet_blast";color="#f97316";glow="#fef3c7";
      moving=true;const cba=Math.random()*Math.PI*2,cbsp=3.5+Math.random()*2;
      vx=Math.cos(cba)*cbsp;vy=Math.sin(cba)*cbsp;
    } else if((cfg.id||0)>=18&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Time Prison — cages 3 nearby targets; release on tap
      type="time_prison";color="#0ea5e9";glow="#e0f2fe";
    } else if((cfg.id||0)>=16&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Shadow Twin — real + shadow clone; tap real for 400pts
      type="shadow_twin";color="#f472b6";glow="#fce7f3";
    } else if((cfg.id||0)>=12&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Photon Burst — flashes 150ms on/off; 600pts only when visible
      type="photon_burst";color="#facc15";glow="#fef9c3";
    } else if((cfg.id||0)>=18&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Vault Orb — banks 100pts/s; tap to claim banked amount
      type="vault_orb";color="#4ade80";glow="#d1fae5";
    } else if((cfg.id||0)>=14&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Nano Swarm — splits into 6 nano targets on tap
      type="nano_swarm";color="#34d399";glow="#d1fae5";
    } else if((cfg.id||0)>=16&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Rune Chain — 3 sequential runes; tap in order for max pts
      type="rune_chain";color="#a78bfa";glow="#ede9fe";hitsLeft=3;maxHits=3;
    } else if((cfg.id||0)>=18&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Plasma Web — scores 80pts per web connection to nearby targets
      type="plasma_web";color="#7c3aed";glow="#ddd6fe";
    } else if((cfg.id||0)>=12&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Astral Orb — teleports every 1s; jackpot within 0.3s of teleport
      type="astral_orb";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=14&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Crystal Clock — fills 8 segments; pts = 100 × segments
      type="crystal_clock";color="#0ea5e9";glow="#e0f2fe";
    } else if((cfg.id||0)>=10&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Sunflower Orb — petals drop over time; pts = 50 × remaining petals
      type="sunflower_orb";color="#fbbf24";glow="#fef9c3";
    } else if((cfg.id||0)>=14&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Toxic Cloud — penalty zone; tap to clear for 350pts
      type="toxic_cloud";color="#4ade80";glow="#dcfce7";
    } else if((cfg.id||0)>=20&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Laser Prism — charges nearby targets; tap for 300pts
      type="laser_prism";color="#f43f5e";glow="#fce7f3";
    } else if((cfg.id||0)>=12&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Echo Star — spawns 4 fading echoes; tap for 500pts + collect echoes
      type="echo_star";color="#ffd700";glow="#fef9c3";
    } else if((cfg.id||0)>=14&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Meteor Shower — tap core for 250pts + 6 shower shards
      type="meteor_shower";color="#fb923c";glow="#fed7aa";
    } else if((cfg.id||0)>=30&&Math.random()<0.004&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.4% Golden Gate — ultra rare, 0.8s window, 2000pts
      type="golden_gate";color="#ffd700";glow="#fef9c3";
    } else if((cfg.id||0)>=10&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Pulse Ring — expanding ring; sweet spot for 400pts
      type="pulse_ring";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=16&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Clockwork Bomb — 5 gears; tap to advance; gear 4 = jackpot
      type="clockwork_bomb";color="#60a5fa";glow="#dbeafe";hitsLeft=5;maxHits=5;
    } else if((cfg.id||0)>=8&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Nebula Vortex — grows over time; pts scale with size
      type="nebula_vortex";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=12&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Frost Hex — freezes nearby moving targets; pts = 200 + 100/frozen
      type="frost_hex";color="#bae6fd";glow="#e0f2fe";
    } else if((cfg.id||0)>=18&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Lightning Orb — chains sparks to nearby targets on tap
      type="lightning_orb";color="#fcd34d";glow="#fef9c3";
    } else if((cfg.id||0)>=5&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Star Burst — expands; max size = 600pts; early = 200pts
      type="star_burst";color="#fbbf24";glow="#fef9c3";
    } else if((cfg.id||0)>=20&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Crystal Spire — charges then shatters
      type="crystal_spire";color="#a5f3fc";glow="#e0f2fe";
    } else if((cfg.id||0)>=15&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Phase Gate — solid/ghost phases cycle; only tap when solid
      type="phase_gate";color="#22d3ee";glow="#cffafe";
    } else if((cfg.id||0)>=25&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Void Pulsar — shrinks; miss = -150pts penalty
      type="void_pulsar";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=30&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Nova Cannon — charges up; shockwave clears nearby targets
      type="nova_cannon";color="#f97316";glow="#ffedd5";
    } else if((cfg.id||0)>=22&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Hex Grid — tap 6 hexes for jackpot
      type="hex_grid";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=18&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Quantum Tunnel — A portal (B spawned separately)
      type="quantum_tunnel_a";color="#22d3ee";glow="#cffafe";
    } else if((cfg.id||0)>=12&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Inferno Orb — heat stacks each second; max = 1000pts
      type="inferno_orb";color="#f97316";glow="#fed7aa";
    } else if((cfg.id||0)>=20&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Chrono Sphere — slows nearby moving targets; 400pts + 80/slowed
      type="chrono_sphere";color="#6366f1";glow="#e0e7ff";
    } else if((cfg.id||0)>=10&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Prismatic Lens — cycling colors; tap focus for 800pts
      type="prismatic_lens";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=25&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Shadow Mirror — reflect tap position; high pts
      type="shadow_mirror";color="#c084fc";glow="#fae8ff";
    } else if((cfg.id||0)>=15&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Plasma Comet — parabolic arc; apex = 1000pts
      type="plasma_comet";color="#c084fc";glow="#fae8ff";moving=true;vx=1.8;vy=-2.0;
    } else if((cfg.id||0)>=35&&Math.random()<0.009&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.9% Abyssal Gate — drains normals; huge pts for tapping
      type="abyssal_gate";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Warp Ball — teleports every 0.8s; big pts if caught
      type="warp_ball";color="#0ea5e9";glow="#bae6fd";
    } else if((cfg.id||0)>=20&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Crystal Spike — danger zone spikes; only safe from safe arc
      type="crystal_spike";color="#a5f3fc";glow="#e0f2fe";
    } else if((cfg.id||0)>=15&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Energy Core — 3 phases; green = safe, red = dangerous
      type="energy_core";color="#4ade80";glow="#dcfce7";
    } else if((cfg.id||0)>=25&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Arcane Circle — 5-symbol cycle; tap correct symbol for bonus
      type="arcane_circle";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=6&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Stellar Drift — slow mover; pts grow with distance from spawn
      type="stellar_drift";color="#a5f3fc";glow="#e0f2fe";moving=true;vx=(Math.random()-0.5)*0.5;vy=(Math.random()-0.5)*0.5;
    } else if((cfg.id||0)>=10&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Bubble Wave — grows; tap before burst; burst = -1 life
      type="bubble_wave";color="#38bdf8";glow="#bae6fd";
    } else if((cfg.id||0)>=18&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Iron Shield — 3-hit target; rapid taps required
      type="iron_shield";color="#94a3b8";glow="#e2e8f0";hitsLeft=3;maxHits=3;
    } else if((cfg.id||0)>=28&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Time Echo — ghost phantom of recent movement
      type="time_echo";color="#a78bfa";glow="#ede9fe";
    } else if((cfg.id||0)>=40&&Math.random()<0.008&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.8% Gravity Orb Mk2 — black hole; pulls all targets; huge pts
      type="gravity_orb2";color="#4c1d95";glow="#ede9fe";
    } else if((cfg.id||0)>=30&&Math.random()<0.009&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.9% Rune Burst — fires 5 rune blasts at normals on tap
      type="rune_burst";color="#c084fc";glow="#f5f3ff";
    } else if((cfg.id||0)>=35&&Math.random()<0.007&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.7% Crystal Golem — 3 HP; tap 3x for full bonus
      type="crystal_golem";color="#86efac";glow="#dcfce7";hitsLeft=3;maxHits=3;
    } else if((cfg.id||0)>=20&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Freeze Orb — tapping freezes all normal targets for 3s
      type="freeze_orb";color="#a5f3fc";glow="#ecfeff";
    } else if((cfg.id||0)>=25&&Math.random()<0.009&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.9% Plasma Vortex — spinning ring; tap at peak alignment for bonus
      type="plasma_vortex";color="#c084fc";glow="#f5f3ff";
    } else if((cfg.id||0)>=15&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Glow Shard — splits into 3 colorful shards on tap
      type="glow_shard";color="#f0abfc";glow="#fdf4ff";
    } else if((cfg.id||0)>=22&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Nova Star — sweeps nearby normals on tap
      type="nova_star";color="#fbbf24";glow="#fffbeb";
    } else if((cfg.id||0)>=18&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Spiral Orb — spirals outward; tap at 75%+ travel for bonus
      type="spiral_orb";color="#34d399";glow="#d1fae5";
      moving=true;const asp=Math.random()*Math.PI*2;vx=Math.cos(asp)*0.8;vy=Math.sin(asp)*0.8;
    } else if((cfg.id||0)>=20&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Lunar Eclipse — grows then shrinks; max size tap = legendary pts
      type="lunar_eclipse";color="#94a3b8";glow="#f8fafc";
    } else if((cfg.id||0)>=12&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Chain Wall — 100 pts; preserves streak if tapped within 1.5s of last tap
      type="chain_wall";color="#92400e";glow="#fef3c7";
    } else if((cfg.id||0)>=25&&Math.random()<0.009&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.9% Dark Pulse — anti-target; pay -20pts, receive 2 bonus normals worth 200 each
      type="dark_pulse";color="#7c3aed";glow="#4c1d95";
      // 1.1% Ice Phoenix — tap before hatch (2.5s) for freeze bonus; after = reduced pts
      type="ice_phoenix";color="#a5f3fc";glow="#ecfeff";
    } else if((cfg.id||0)>=20&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Quantum Leech — drains -10pts/s; tap to reclaim drain + 150 base pts
      type="quantum_leech";color="#4ade80";glow="#dcfce7";
    } else if((cfg.id||0)>=18&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Star Bomb — tap early for full pts; auto-detonates for area bonus
      type="star_bomb";color="#fbbf24";glow="#fffbeb";
    } else if((cfg.id||0)>=22&&Math.random()<0.009&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.9% Twin Comet — spawns mirrored partner; tap either to kill both
      type="twin_comet";color="#fb923c";glow="#ffedd5";moving=true;
      const atc=Math.random()*Math.PI*2;vx=Math.cos(atc)*2.0;vy=Math.sin(atc)*2.0;
    } else if((cfg.id||0)>=20&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Energy Orb — charges; at 80%+ tap = shockwave that boosts all normals
      type="energy_orb2";color="#06b6d4";glow="#cffafe";
    } else if((cfg.id||0)>=25&&Math.random()<0.009&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.9% Comet Trail — fast moving with trail; tap earns bonus per trail dot near touch
      type="comet_trail";color="#f97316";glow="#ffedd5";moving=true;
      const a=Math.random()*Math.PI*2;vx=Math.cos(a)*2.2;vy=Math.sin(a)*2.2;
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Star Cluster — explodes into 5 sparkle mini-targets
      type="star_cluster";color="#fbbf24";glow="#fffbeb";
    } else if((cfg.id||0)>=15&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Void Crystal — shrinks; smaller = more pts
      type="void_crystal";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=10&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Neon Pulse — alternates bright/dim; bright = 3× pts
      type="neon_pulse";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=30&&Math.random()<0.008&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.8% Mirror Twin — spawns mirrored copy; tap both for bonus
      type="mirror_twin";color="#34d399";glow="#d1fae5";
    } else if((cfg.id||0)>=20&&Math.random()<0.009&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.9% Gemini Pair — two linked targets spawned simultaneously
      type="gemini_pair";color="#a78bfa";glow="#f5f3ff";
    } else if((cfg.id||0)>=28&&Math.random()<0.009&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.9% Aurora Ring — expanding ring that bursts nearby targets
      type="aurora_ring";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=15&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Lightning Chain — chains to 3 nearest normals on tap
      type="lightning_chain";color="#fbbf24";glow="#fffbeb";
    } else if((cfg.id||0)>=22&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Magma Seed — orbited by 5 lava drops
      type="magma_seed";color="#ef4444";glow="#fee2e2";
    } else if((cfg.id||0)>=20&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Prism Shard — splits into 3 mini shards on tap
      type="prism_shard";color="#f0abfc";glow="#fae8ff";
    } else if((cfg.id||0)>=30&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Temporal Orb — rewinds position every 2s
      type="temporal_orb";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=25&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Chrono Crystal — tap within 3s to freeze; second tap = 2× pts
      type="chrono_crystal";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=18&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Void Seed — grows over time; score scales with size
      type="void_seed";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=15&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Bounce Web — sticky web; snares up to 3 nearest moving targets for 2s
      type="bounce_web";color="#a855f7";glow="#e9d5ff";
    } else if((cfg.id||0)>=22&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Meteor Shower — tap to summon 4 mini-meteors that auto-score after 2s each
      type="meteor_shower";color="#fb923c";glow="#ffedd5";
    } else if((cfg.id||0)>=12&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Prismatic Orb — shifts hue cycle; tap on target hue for 3× pts
      type="prismatic_orb";color="#e879f9";glow="#fae8ff";
    } else if((cfg.id||0)>=18&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Void Tether — linked pair; tap one pulls other to same spot, combo = jackpot
      type="void_tether";color="#6d28d9";glow="#ede9fe";
    } else if((cfg.id||0)>=10&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Crystal Pulsar — rhythmic pulse; tap at peak (>80%) for legendary pts
      type="crystal_pulsar";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=16&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Shadow Echo — dark orb; 1.5s later spawns bright echo for bonus tap
      type="shadow_echo";color="#312e81";glow="#6366f1";
    } else if((cfg.id||0)>=8&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Flux Ring — expanding ring; thick=high pts, thin=low
      type="flux_ring";color="#06b6d4";glow="#cffafe";
    } else if((cfg.id||0)>=20&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Quantum Flare — tap spawns 3 decoys; only real one gives full pts
      type="quantum_flare";color="#e879f9";glow="#fae8ff";
    } else if((cfg.id||0)>=18&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Nebula Crystal — 3-phase crack target; 3rd hit = jackpot
      type="nebula_crystal";color="#7e22ce";glow="#ede9fe";hitsLeft=3;maxHits=3;
    } else if((cfg.id||0)>=12&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Time Shard — blinks visible/invisible; tap during visible window = 2× pts
      type="time_shard";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=20&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Gravity Node — pulls nearby targets; tap to release all for bonus
      type="gravity_node";color="#4c1d95";glow="#ede9fe";
    } else if((cfg.id||0)>=15&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Lightning Cage — electric cage; score per target inside on tap
      type="lightning_cage";color="#fbbf24";glow="#fef3c7";
    } else if((cfg.id||0)>=10&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Solar Ring — expanding ring; tap near ring = more pts
      type="solar_ring";color="#fbbf24";glow="#fef3c7";
    } else if((cfg.id||0)>=20&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Chrono Vortex — slows nearby targets; tap for time-freeze bonus
      type="chrono_vortex";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=16&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Nova Crystal — 3 charge stages; tap at full charge for explosive bonus
      type="nova_crystal";color="#60a5fa";glow="#dbeafe";hitsLeft=3;maxHits=3;
    } else if((cfg.id||0)>=12&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Phase Shift — shifts solid/ghost every 1.2s; tap only when solid
      type="phase_shift";color="#e879f9";glow="#fae8ff";
    } else if((cfg.id||0)>=18&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Stellar Orb — expands slowly; tap at max size for legendary pts
      type="stellar_orb";color="#fbbf24";glow="#fef3c7";
    } else if((cfg.id||0)>=22&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Rune Gate A — linked pair; tap A then B within 2s for jackpot
      type="rune_gate_a";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=20&&Math.random()<0.008&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.8% Omega Heart — legendary, sync = 5×
      type="omega_heart";color="#ec4899";glow="#fce7f3";
    } else if((cfg.id||0)>=15&&Math.random()<0.009&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.9% Nexus Gem — multi-facet, center = 4×
      type="nexus_gem";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Plasma Halo — ring sweetspot = 3×
      type="plasma_halo";color="#a855f7";glow="#f5d0fe";
    } else if((cfg.id||0)>=5&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Glitch Node — burst phase = 2×
      type="glitch_node";color="#4ade80";glow="#dcfce7";
    } else if((cfg.id||0)>=9&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Astral Core — rings aligned = 3×
      type="astral_core";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Light Bolt — full charge = 3×
      type="light_bolt";color="#eab308";glow="#fef9c3";
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Solar Eye — iris fully open = 3×
      type="solar_eye";color="#fbbf24";glow="#fef3c7";
    } else if((cfg.id||0)>=7&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Void Serpent — vertical = 2×
      type="void_serpent";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Moon Petal — fully bloomed = 3×
      type="moon_petal";color="#a78bfa";glow="#ede9fe";
    } else if((cfg.id||0)>=7&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Star Dust 2 — long trail = special
      type="star_dust2";color="#fbbf24";glow="#fef9c3";
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Ice Nova 2 — freeze peak = 3×
      type="ice_nova2";color="#93c5fd";glow="#dbeafe";
    } else if((cfg.id||0)>=7&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Arc Gem — full charge = 3×
      type="arc_gem";color="#34d399";glow="#d1fae5";
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Flame Orb 2 — peak heat = 3×
      type="flame_orb2";color="#f97316";glow="#ffedd5";
    } else if((cfg.id||0)>=7&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Thor Gem — storm phase = 2×
      type="thor_gem";color="#fbbf24";glow="#fef9c3";
    } else if((cfg.id||0)>=9&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Cosmic Lens — focused = 3×
      type="cosmic_lens";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Nebula Burst 2 — nova moment = 3×
      type="nebula_burst2";color="#c084fc";glow="#f3e8ff";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Dark Pulsar — burst phase = 3×
      type="dark_pulsar";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Crystal Bomb — defuse before explosion = 2×
      type="crystal_bomb";color="#34d399";glow="#d1fae5";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Glacier Spike — very tip = 3×
      type="glacier_spike";color="#7dd3fc";glow="#e0f2fe";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Plasma Pulse 2 — pulse peak = 3×
      type="plasma_pulse2";color="#c084fc";glow="#f3e8ff";
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Quantum Burst 2 — smallest phase = 3×
      type="quantum_burst2";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Nebula Ring — outer edge = 2×
      type="nebula_ring";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Flux Ring 2 — inner core = 2×
      type="flux_ring2";color="#3b82f6";glow="#dbeafe";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Solar Vortex — calm center = 3×
      type="solar_vortex";color="#f59e0b";glow="#fef3c7";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Drift Orb — still moment = 2×
      type="drift_orb";color="#818cf8";glow="#e0e7ff";moving=true;
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Chrono Star — apex phase = 3×
      type="chrono_star";color="#c084fc";glow="#f3e8ff";
    } else if((cfg.id||0)>=9&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Prism Coil — color sync = 3×
      type="prism_coil";color="#a855f7";glow="#f3e8ff";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Star Veil — reveal moment = 2×
      type="star_veil";color="#fde68a";glow="#fef9c3";
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Frost Halo — freeze peak = 3×
      type="frost_halo";color="#93c5fd";glow="#dbeafe";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Ember Clock — final 20% = 2×
      type="ember_clock";color="#ef4444";glow="#fee2e2";
    } else if((cfg.id||0)>=9&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Nova Spire — peak energy = 3×
      type="nova_spire";color="#f59e0b";glow="#fef3c7";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Echo Lens — focus phase = 2×
      type="echo_lens";color="#22d3ee";glow="#cffafe";
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Crystal Petal — petals aligned = 3×
      type="crystal_petal";color="#f43f5e";glow="#ffe4e6";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Twilight Shard — twilight phase = 2×
      type="twilight_shard";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Shadow Crystal — dark phase = 3×
      type="shadow_crystal";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Tide Orb — wave crest = 2×
      type="tide_orb";color="#22d3ee";glow="#cffafe";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Lunar Rune — full moon phase = 3×
      type="lunar_rune";color="#fde68a";glow="#fef9c3";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Storm Seed — fully grown = 2×
      type="storm_seed";color="#60a5fa";glow="#dbeafe";
    } else if((cfg.id||0)>=7&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Warp Glyph — glow phase = 3×
      type="warp_glyph";color="#c084fc";glow="#f5d0fe";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Ether Bolt — 3 chain = 2×
      type="ether_bolt";color="#38bdf8";glow="#e0f2fe";moving=true;
    } else if((cfg.id||0)>=5&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Cosmic Dust — peak density = 2×
      type="cosmic_dust";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Nova Bell — resonance peak = 3×
      type="nova_bell";color="#fbbf24";glow="#fef9c3";
    } else if((cfg.id||0)>=9&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Prismatic Eye — gaze phase = 3×
      type="prismatic_eye";color="#e879f9";glow="#fae8ff";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Thunder Crown — full charge = 3×
      type="thunder_crown";color="#fbbf24";glow="#fef9c3";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Solar Beam — center tap = 3×
      type="solar_beam";color="#fef08a";glow="#fef9c3";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Ice Prism — split phase = 2×
      type="ice_prism";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=4&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Star Pulse — sync tap at peak = 4×
      type="star_pulse";color="#fbbf24";glow="#fef9c3";
    } else if((cfg.id||0)>=9&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Void Nova — collapsing; burst zone = 3×
      type="void_nova";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=5&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Flame Orb — peak intensity = 3×
      type="flame_orb";color="#f97316";glow="#ffedd5";
    } else if((cfg.id||0)>=7&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Crystal Shell — crack to core = 2×
      type="crystal_shell";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Nebula Core — center = 3×
      type="nebula_core";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Dusk Sphere — dusk phase = 2×
      type="dusk_sphere";color="#a78bfa";glow="#ede9fe";
    } else if((cfg.id||0)>=5&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Spiral Bead — center tap = 3×
      type="spiral_bead";color="#e879f9";glow="#fae8ff";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Quantum Flux — stable phase = 2×
      type="quantum_flux";color="#34d399";glow="#d1fae5";
    } else if((cfg.id||0)>=3&&Math.random()<0.016&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.6% Nebula Drop — collect 5 for bonus
      type="nebula_drop";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=7&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Pulse Gate — open phase = 2×
      type="pulse_gate";color="#22d3ee";glow="#cffafe";
    } else if((cfg.id||0)>=5&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Frost Gem — freeze bombs on tap
      type="frost_gem";color="#3b82f6";glow="#dbeafe";
    } else if((cfg.id||0)>=4&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Heat Orb — fever active = 3×
      type="heat_orb";color="#f97316";glow="#ffedd5";
    } else if((cfg.id||0)>=4&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Thunder Bead — 3 in a row chain bonus
      type="thunder_bead";color="#eab308";glow="#fefce8";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Mirror Star — high streak = reflect bonus
      type="mirror_star";color="#94a3b8";glow="#f1f5f9";
    } else if((cfg.id||0)>=5&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Aurora Wave — wave crest tap = 2×
      type="aurora_wave";color="#22c55e";glow="#dcfce7";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Crystal Prism — tap splits into 3 score fragments
      type="crystal_prism";color="#06b6d4";glow="#cffafe";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Plasma Ring — inner zone tap = 2×
      type="plasma_ring";color="#c084fc";glow="#f5d0fe";
    } else if((cfg.id||0)>=9&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Void Star — core tap = 3×
      type="void_star";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Ice Dagger — tap tip zone for 3×
      type="ice_dagger";color="#3b82f6";glow="#dbeafe";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Shadow Flare — bright phase = 2×
      type="shadow_flare";color="#f59e0b";glow="#fef3c7";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Nova Drift — expanding nova; center = 2×; fades fast
      type="nova_drift";color="#f43f5e";glow="#ffe4e6";moving=true;
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Energy Pulse — charges up; max = max pts
      type="energy_pulse";color="#0ea5e9";glow="#e0f2fe";
    } else if((cfg.id||0)>=4&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Solar Petal — spinning sun rays; chain 3 for bonus
      type="solar_petal";color="#fbbf24";glow="#fef3c7";
    } else if((cfg.id||0)>=11&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Cosmic Shard — cycles 3 colors; tap 3 aligned = bonus
      type="cosmic_shard";color="#8b5cf6";glow="#ede9fe";
    } else if((cfg.id||0)>=9&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Crystal Wing pair — tap both within 2.5s = bonus
      type="crystal_wing_l";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=10&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Storm Cloud — flashing lightning bolt = 2×
      type="storm_cloud";color="#64748b";glow="#e2e8f0";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Arc Node — arcs to nearby targets; chain lightning bonus
      type="arc_node";color="#3b82f6";glow="#dbeafe";
    } else if((cfg.id||0)>=12&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Prismatic Burst — 5 phases; score all = jackpot
      type="prismatic_burst";color="#8b5cf6";glow="#ede9fe";
    } else if((cfg.id||0)>=5&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Moon Orb — cycles phases; full moon = 2×
      type="moon_orb";color="#fde68a";glow="#fef9c3";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Lava Core — hot molten target; tap 3 in a row = chain bonus
      type="lava_core";color="#f97316";glow="#ffedd5";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Sun Petal — blooms over 5s; full bloom = max pts
      type="sun_petal";color="#fbbf24";glow="#fef3c7";
    } else if((cfg.id||0)>=9&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Ghost Bell — fades in/out; silent (ghost) phase = 3×
      type="ghost_bell";color="#fde68a";glow="#fefce8";
    } else if((cfg.id||0)>=5&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Plasma Disk — spinning disc; edge ring tap = 2×
      type="plasma_disk";color="#f97316";glow="#ffedd5";moving=false;
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Void Ring — donut; center gap tap = 2×
      type="void_ring";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Ether Orb — teal orb; splits into 2 copies on tap
      type="ether_orb";color="#2dd4bf";glow="#ccfbf1";
    } else if((cfg.id||0)>=10&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Quantum Blast — pulses huge↔tiny; tiny phase = 5× jackpot
      type="quantum_blast";color="#c084fc";glow="#f5d0fe";
    } else if((cfg.id||0)>=3&&Math.random()<0.018&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.8% Nexus Core — large target; wide hit zone = easy score
      type="nexus_core";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Blink Star — teleports every 0.7s; tap after blink = ×3
      type="blink_star";color="#f472b6";glow="#fce7f3";
    } else if((cfg.id||0)>=15&&Math.random()<0.009&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 0.9% Omega Pulse — giant, vanishes after 3s, massive points
      type="omega_pulse";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Spiral Orb74 — spiral motion; center zone = 2×
      type="spiral_orb74";color="#22d3ee";glow="#cffafe";moving=true;
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Frost Nova — freeze AoE when tapped; score = 40 + frozen×30
      type="frost_nova";color="#93c5fd";glow="#dbeafe";
    } else if((cfg.id||0)>=4&&Math.random()<0.015&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.5% Light Bomb — safe bomb; always scores, never dangerous
      type="light_bomb";color="#fbbf24";glow="#fef3c7";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Glitch Orb — flickers visible; tap while visible = 2×
      type="glitch_orb";color="#34d399";glow="#d1fae5";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Solar Flare72 — brightens then fades; peak tap = 3×
      type="solar_flare72";color="#fbbf24";glow="#fef3c7";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Crystal Burst — shatters on tap into 4 child shards
      type="crystal_burst";color="#67e8f9";glow="#cffafe";
    } else if((cfg.id||0)>=10&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Time Rift — slowdown + final-second jackpot
      type="time_rift";color="#a78bfa";glow="#ede9fe";
    } else if((cfg.id||0)>=5&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Arc Bloom — petals unfurl over 3s; fully open tap = 3×
      type="arc_bloom";color="#f9a8d4";glow="#fce7f3";moving=false;
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Neon Bolt — zips across screen; catch before exit = bonus
      type="neon_bolt";color="#34d399";glow="#d1fae5";moving=true;
    } else if((cfg.id||0)>=9&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Prism Clock — 3 color segments rotate; tap matching segment = 3×
      type="prism_clock";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Void Pulse — grows/shrinks; tap at max size = 3×
      type="void_pulse";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=7&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Chain Nova — tap to explode, scoring all nearby targets
      type="chain_nova";color="#f43f5e";glow="#fce7f3";
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Mirror Flash — spawns real + decoy; tap the real one
      type="mirror_flash";color="#f0abfc";glow="#fdf4ff";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Dark Mirror — opacity cycles; tap at peak = bonus
      type="dark_mirror";color="#7c3aed";glow="#ede9fe";
    } else if((cfg.id||0)>=5&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Storm Ball — accelerates; catch early = bonus
      type="storm_ball";color="#fbbf24";glow="#fef3c7";moving=true;
    } else if((cfg.id||0)>=16&&Math.random()<0.010&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.0% Cosmic Web — center + 6 nodes; tap all nodes then center = jackpot
      type="cosmic_web";color="#06b6d4";glow="#cffafe";
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Warp Orb — teleports every 1.5s; mid-warp tap = crit
      type="warp_orb";color="#6366f1";glow="#e0e7ff";moving=false;
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Arcane Rune — symbol match; follow-up tap = jackpot
      const sym65=Math.floor(Math.random()*4);
      type="arcane_rune";color="#a78bfa";glow="#ede9fe";
    } else if((cfg.id||0)>=4&&Math.random()<0.014&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.4% Pulse Gem — pulsing; tap at peak = 4×
      type="pulse_gem";color="#f472b6";glow="#fce7f3";
    } else if((cfg.id||0)>=6&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Hue Orb — color cycles; match world color = bonus
      type="hue_orb";color="#a78bfa";glow="#ede9fe";
    } else if((cfg.id||0)>=14&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Lightning Chain 64 — tap chains to 5 nearby targets
      type="lightning_chain_64";color="#facc15";glow="#fef9c3";
    } else if((cfg.id||0)>=14&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Nebula Clock — 5 orbiting dots spawn; collect all = jackpot
      type="nebula_clock";color="#818cf8";glow="#e0e7ff";
    } else if((cfg.id||0)>=10&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Ice Shatter — needs 3 hits to break; score scales
      type="ice_shatter";color="#67e8f9";glow="#cffafe";hitsLeft=3;maxHits=3;
    } else if((cfg.id||0)>=8&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Plasma Burst — tap releases 4 directional bolts
      type="plasma_burst";color="#f97316";glow="#ffedd5";
    } else if((cfg.id||0)>=12&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Star Clock — 5s countdown; tap at 0 = jackpot
      type="star_clock";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=10&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Vortex Star 61 — 3 score zones; outer zone = 4×
      type="vortex_star_61";color="#fbbf24";glow="#fef3c7";moving=Math.random()<0.3;
    } else if((cfg.id||0)>=12&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Quantum Drift — drifts randomly; freeze tap unlocks bonus window
      type="quantum_drift";color="#a78bfa";glow="#ede9fe";moving=true;
    } else if((cfg.id||0)>=15&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Prism Nova — rainbow burst; splits into 3 shards
      type="prism_nova";color="#f472b6";glow="#fce7f3";
    } else if((cfg.id||0)>=8&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Echo Blast — tap leaves scoring ring; ring riders get bonus
      type="echo_blast";color="#8b5cf6";glow="#ede9fe";
    } else if((cfg.id||0)>=10&&Math.random()<0.013&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.3% Arc Pulse — rotating arc; tap in sweet spot for 3× pts
      type="arc_pulse";color="#38bdf8";glow="#e0f2fe";
    } else if((cfg.id||0)>=14&&Math.random()<0.011&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.1% Void Bloom — blooms into 4 petals; collect all for jackpot
      type="void_bloom";color="#c084fc";glow="#fae8ff";
    } else if((cfg.id||0)>=12&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Crystal Web — tap main + 2 nodes within 1.5s for jackpot
      type="crystal_web";color="#06b6d4";glow="#cffafe";
    } else if((cfg.id||0)>=12&&Math.random()<0.012&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")){
      // 1.2% Pixel Swarm — core + 8 pixel units spawn
      type="pixel_swarm";color="#f472b6";glow="#fce7f3";
    } else if(Math.random()<0.035&&!gs.bonusRoundActive&&luckyRef.current!=="active"&&!modifier?.type?.includes("boss")&&!modifier?.type?.includes("final")&&!gs.mysteryPause){
      // 3.5% Mystery Box — Las Vegas variable-ratio slot machine
      type="mystery";color="#ffd700";glow="#b8860b";
    } else {
      if(gs.bonusRoundActive||activePwrRef.current.some(p=>p.type==="RAINBOW_SURGE"&&p.endsAt>Date.now()))rarity=RARITY.LEGENDARY; // Bonus Round or Rainbow Surge: ALL LEGENDARY!
      color=rarity.color;glow=rarity.glow;
      if(Math.random()<effMoving){
        moving=true;const a=Math.random()*Math.PI*2,sp=0.6+Math.random()*1.4;
        vx=Math.cos(a)*sp;vy=Math.sin(a)*sp;
      }
      if(!moving&&Math.random()<effGhost)ghost=true;
    }
    const baseR=type==="boss"?BASE_R*2.4:type==="treasure"?BASE_R*1.7:type==="mystery"?BASE_R*1.5:type==="mimic"?BASE_R*1.35:type==="anchor"?BASE_R*1.3:type==="splitter"?BASE_R*1.4:type==="shielded"?BASE_R*1.25:type==="magnet"?BASE_R*1.3:type==="phantom"?BASE_R*1.4:type==="volatile"?BASE_R*1.45:type==="healer"?BASE_R*1.2:type==="twin"?BASE_R*1.1:type==="rainbow"?BASE_R*1.35:type==="frozen"?BASE_R*1.3:type==="bouncy"?BASE_R*1.0:type==="ninja"?BASE_R*1.2:type==="lootchest"?BASE_R*1.55:type==="tornado"?BASE_R*1.4:type==="bubble"?BASE_R*1.8:type==="echo"?BASE_R*1.25:type==="echo_ghost"?BASE_R*1.15:type==="crystal"?BASE_R*1.3:type==="crystal_shard"?BASE_R*0.65:type==="rage"?BASE_R*1.15:type==="divider"?BASE_R*1.65:type==="vanishing"?BASE_R*1.1:type==="homing"?BASE_R*1.2:type==="gemstone"?BASE_R*1.1:type==="poison"?BASE_R*1.15:type==="morph"?BASE_R*1.2:type==="conductor"?BASE_R*1.25:type==="siphon"?BASE_R*1.3:type==="glitch"?BASE_R*1.1:type==="prism"?BASE_R*1.2:type==="comet"?BASE_R*1.15:type==="mirrorball"?BASE_R*1.45:type==="nexus"?BASE_R*1.8:type==="phoenix"?BASE_R*1.25:type==="phoenix2"?BASE_R*1.4:type==="icecomet"?BASE_R*1.2:type==="voltage"?BASE_R*1.2:type==="void"?BASE_R*1.5:type==="clover"?BASE_R*1.1:type==="ricochet"?BASE_R*1.15:type==="aurora"?BASE_R*1.35:type==="portal"?BASE_R*1.4:type==="particlebomb"?BASE_R*1.25:type==="beacon"?BASE_R*1.3:type==="shadow"?BASE_R*1.1:type==="shadow_clone"?BASE_R*0.9:type==="spectral"?BASE_R*1.1:type==="heart"?BASE_R*1.2:type==="nova"?BASE_R*1.4:type==="firefly"?BASE_R*0.78:type==="geode"?BASE_R*1.3:type==="timebomb"?BASE_R*1.25:type==="thunderbolt"?BASE_R*1.1:type==="gravityorb"?BASE_R*1.5:type==="crystalball"?BASE_R*1.35:type==="quantum"?BASE_R*1.2:type==="clockwork"?BASE_R*1.3:type==="bloom"?BASE_R*1.4:type==="petal"?BASE_R*0.55:type==="reflector"?BASE_R*1.25:type==="chaincollector"?BASE_R*1.35:type==="stardust"?BASE_R*1.3:type==="star_piece"?BASE_R*0.6:type==="solarflare"?BASE_R*1.0:type==="magma"?BASE_R*1.2:type==="prismbomb"?BASE_R*1.3:type==="icecage"?BASE_R*1.4:type==="pinwheel"?BASE_R*1.15:type==="supercell"?BASE_R*1.35:
type==="vortex"?BASE_R*1.4:type==="pixel"?BASE_R*0.65:type==="neon"?BASE_R*1.45:
type==="lantern"?BASE_R*1.2:type==="drift"?BASE_R*1.1:
type==="nebula"?BASE_R*1.5:type==="wisp"?BASE_R*1.2:type==="meteor"?BASE_R*1.1:type==="aura"?BASE_R*1.55:
type==="thunderclap"?BASE_R*1.4:type==="mirror_shard"?BASE_R*1.0:type==="rune_stone"?BASE_R*1.3:
type==="fractal"?BASE_R*1.2:type==="fractal_mini"?BASE_R*0.7:type==="plasma"?BASE_R*1.25:type==="timewarp_tgt"?BASE_R*1.3:
type==="solar_drone"?BASE_R*1.15:type==="crystal_cluster"?BASE_R*1.5:type==="swarm"?BASE_R*1.35:type==="celestial"?BASE_R*1.7:
type==="smoke_ring"?BASE_R*1.2:type==="chain_bomb"?BASE_R*1.25:type==="warp_gate"?BASE_R*1.3:type==="mirage"?BASE_R*(rarity?.size||1.0):
type==="surge_ball"?BASE_R*1.2:type==="ticking_clock"?BASE_R*1.3:type==="echo_pulse"?BASE_R*1.4:
type==="gravity_well"?BASE_R*1.45:type==="spectrum_orb"?BASE_R*1.25:type==="shard_storm"?BASE_R*1.3:type==="pulse_nova"?BASE_R*1.35:
type==="bubble_cluster"?BASE_R*1.5:type==="void_shard"?BASE_R*1.1:type==="stasis_field"?BASE_R*1.4:type==="crystalline"?BASE_R*1.2:
type==="quantum_leap"?BASE_R*1.15:type==="magma_burst"?BASE_R*1.25:type==="lightning_rod"?BASE_R*1.3:type==="prism_gate"?BASE_R*1.35:type==="prism_shard"?BASE_R*0.38:
type==="solar_burst"?BASE_R*1.4:type==="frost_comet"?BASE_R*1.15:type==="frost_shard"?BASE_R*0.35:type==="energy_web"?BASE_R*1.35:type==="void_rift"?BASE_R*1.6:type==="star_rain_drop"?BASE_R*0.85:
type==="laser_grid"?BASE_R*1.3:type==="bouncing_orb"?BASE_R*1.1:type==="time_capsule"?BASE_R*1.35:type==="nether_shard"?BASE_R*1.25:
type==="accumulator"?BASE_R*(1.0+Math.min(0.8,(0))): // dynamic — grows via _stackRadius
type==="jackpot_target"?BASE_R*2.0:type==="phase_orb"?BASE_R*1.3:type==="ripple_wave"?BASE_R*1.2:
type==="venom_orb"?BASE_R*1.2:type==="thunder_egg"?BASE_R*1.25:type==="moon_glyph"?BASE_R*1.3:type==="comet_tail"?BASE_R*1.0:type==="ember_ring"?BASE_R*1.4:
type==="supernova"?BASE_R*1.5:type==="crystal_matrix"?BASE_R*1.55:type==="plasma_storm"?BASE_R*1.35:type==="nexus_gate"?BASE_R*1.4:
type==="supernova_mini"?BASE_R*0.45:
type==="arc_light"?BASE_R*1.3:type==="pulse_chain"?BASE_R*1.25:type==="ghost_orb"?BASE_R*1.2:type==="time_fracture"?BASE_R*1.35:
type==="twilight_orb"?BASE_R*1.15:type==="life_orb"?BASE_R*1.3:type==="score_amp"?BASE_R*1.4:type==="night_orb"?BASE_R*1.1:
type==="omega_orb"?BASE_R*2.2:type==="thunder_ball"?BASE_R*1.25:type==="cosmic_aura"?BASE_R*1.6:type==="combo_rush"?BASE_R*1.15:
type==="neon_crystal"?BASE_R*1.2:type==="sonar_ping"?BASE_R*1.3:type==="gravity_bomb"?BASE_R*1.25:type==="speed_nova"?BASE_R*1.1:
type==="quantum_split"?BASE_R*1.2:type==="quantum_half"?BASE_R*0.75:type==="frost_bolt"?BASE_R*1.0:type==="vortex_star"?BASE_R*1.3:type==="pixel_burst"?BASE_R*1.25:
type==="star_forge"?BASE_R*1.25:type==="magic_dice"?BASE_R*1.3:type==="wormhole_tgt"?BASE_R*1.35:type==="shadow_pulse"?BASE_R*1.1:
type==="eclipse_orb"?BASE_R*1.2:type==="spin_bloom"?BASE_R*1.35:type==="tesla_node"?BASE_R*1.25:type==="rune_circle"?BASE_R*1.4:
type==="solar_orbit"?BASE_R*1.35:type==="chroma_shift"?BASE_R*1.2:type==="neon_pulsar"?BASE_R*1.15:type==="drift_mine"?BASE_R*1.3:
type==="shift_target"?BASE_R*1.15:type==="lucky_wheel"?BASE_R*1.3:type==="abyss_orb"?BASE_R*1.2:type==="cosmic_portal"?BASE_R*1.35:
type==="flame_ring"?BASE_R*1.3:type==="ice_clock"?BASE_R*1.25:type==="holo_decoy"?BASE_R*1.15:type==="mirror_gate"?BASE_R*1.35:
type==="spiral_bomb"?BASE_R*1.3:type==="chain_orb"?BASE_R*1.2:type==="bolt_catcher"?BASE_R*1.1:type==="gem_harvest"?BASE_R*1.4:
type==="prism_pulse"?BASE_R*1.2:type==="dark_matter"?BASE_R*1.25:type==="comet_blast"?BASE_R*1.1:type==="time_prison"?BASE_R*1.5:type==="comet_shard"?BASE_R*0.65:
type==="shadow_twin"?BASE_R*1.15:type==="shadow_clone2"?BASE_R*1.1:type==="photon_burst"?BASE_R*1.2:type==="vault_orb"?BASE_R*1.3:type==="nano_swarm"?BASE_R*1.4:type==="nano_unit"?BASE_R*0.45:
type==="rune_chain"?BASE_R*1.25:type==="plasma_web"?BASE_R*1.3:type==="astral_orb"?BASE_R*1.15:type==="crystal_clock"?BASE_R*1.3:
type==="sunflower_orb"?BASE_R*1.35:type==="toxic_cloud"?BASE_R*1.5:type==="laser_prism"?BASE_R*1.2:type==="echo_star"?BASE_R*1.15:type==="echo_point"?BASE_R*0.7:
type==="meteor_shower"?BASE_R*1.2:type==="meteor_shard"?BASE_R*0.55:type==="golden_gate"?BASE_R*1.6:type==="pulse_ring"?BASE_R*1.3:type==="clockwork_bomb"?BASE_R*1.25:
type==="nebula_vortex"?BASE_R*1.35:type==="frost_hex"?BASE_R*1.25:type==="lightning_orb"?BASE_R*1.2:type==="star_burst"?BASE_R*1.3:
type==="crystal_spire"?BASE_R*1.2:type==="phase_gate"?BASE_R*1.25:type==="void_pulsar"?BASE_R*1.3:type==="nova_cannon"?BASE_R*1.35:type==="crystal_spire_shard"?BASE_R*0.5:
type==="hex_grid"?BASE_R*1.6:type==="quantum_tunnel_a"?BASE_R*1.25:type==="quantum_tunnel_b"?BASE_R*1.25:type==="inferno_orb"?BASE_R*1.2:type==="chrono_sphere"?BASE_R*1.4:
type==="prismatic_lens"?BASE_R*1.3:type==="shadow_mirror"?BASE_R*1.25:type==="plasma_comet"?BASE_R*1.15:type==="abyssal_gate"?BASE_R*1.55:
type==="warp_ball"?BASE_R*1.15:type==="crystal_spike"?BASE_R*1.2:type==="energy_core"?BASE_R*1.25:type==="arcane_circle"?BASE_R*1.35:
type==="stellar_drift"?BASE_R*1.1:type==="bubble_wave"?BASE_R*1.4:type==="iron_shield"?BASE_R*1.3:type==="time_echo"?BASE_R*1.15:
type==="gravity_orb2"?BASE_R*1.7:type==="pixel_swarm"?BASE_R*1.3:type==="pixel_unit"?BASE_R*0.4:
type==="chrono_crystal"?BASE_R*1.25:type==="void_seed"?BASE_R*(0.6+0.4*(1)):
type==="prism_shard"?BASE_R*1.3:type==="prism_mini"?BASE_R*0.55:type==="temporal_orb"?BASE_R*1.2:
type==="lightning_chain"?BASE_R*1.2:type==="magma_seed"?BASE_R*1.3:type==="lava_drop"?BASE_R*0.5:
type==="gemini_pair"?BASE_R*1.2:type==="aurora_ring"?BASE_R*0.6:
type==="neon_pulse"?BASE_R*1.1:type==="mirror_twin"?BASE_R*1.2:
type==="star_cluster"?BASE_R*1.4:type==="star_sparkle"?BASE_R*0.45:type==="void_crystal"?BASE_R*1.25:
type==="energy_orb2"?BASE_R*1.35:type==="comet_trail"?BASE_R*1.1:
type==="rune_burst"?BASE_R*1.25:type==="crystal_golem"?BASE_R*1.5:
type==="freeze_orb"?BASE_R*1.2:type==="twin_comet"?BASE_R*1.1:
type==="plasma_vortex"?BASE_R*1.3:type==="star_bomb"?BASE_R*1.25:
type==="glow_shard"?BASE_R*1.15:type==="glow_fragment"?BASE_R*0.5:type==="quantum_leech"?BASE_R*1.2:
type==="nova_star"?BASE_R*1.4:type==="ice_phoenix"?BASE_R*1.2:
type==="spiral_orb"?BASE_R*1.1:type==="dark_pulse"?BASE_R*1.25:
type==="lunar_eclipse"?BASE_R*1.3:type==="chain_wall"?BASE_R*1.15:
type==="bounce_web"?BASE_R*1.2:type==="meteor_shower"?BASE_R*1.3:
type==="prismatic_orb"?BASE_R*1.2:type==="void_tether"?BASE_R*1.15:
type==="crystal_pulsar"?BASE_R*1.25:type==="shadow_echo"?BASE_R*1.1:type==="shadow_echo_bright"?BASE_R*1.1:
type==="flux_ring"?BASE_R*1.4:type==="quantum_flare"?BASE_R*1.2:type==="quantum_flare_decoy"?BASE_R*1.1:
type==="nebula_crystal"?BASE_R*1.5:type==="time_shard"?BASE_R*1.15:
type==="gravity_node"?BASE_R*1.4:type==="lightning_cage"?BASE_R*1.6:
type==="crystal_web"?BASE_R*1.3:type==="crystal_web_node"?BASE_R*0.9:
type==="arc_pulse"?BASE_R*1.2:type==="void_bloom"?BASE_R*1.35:type==="void_petal"?BASE_R*0.7:
type==="stellar_orb"?BASE_R*0.8:type==="rune_gate_a"?BASE_R*1.2:type==="rune_gate_b"?BASE_R*1.2:
type==="nova_crystal"?BASE_R*1.3:type==="phase_shift"?BASE_R*1.15:
type==="solar_ring"?BASE_R*0.5:type==="chrono_vortex"?BASE_R*1.35:
type==="prism_nova"?BASE_R*1.25:type==="prism_shard"?BASE_R*0.65:type==="echo_blast"?BASE_R*1.1:
type==="vortex_star_61"?BASE_R*0.85:type==="quantum_drift"?BASE_R*1.0:
type==="plasma_burst"?BASE_R*1.15:type==="plasma_bolt"?BASE_R*0.5:type==="star_clock"?BASE_R*1.1:
type==="nebula_clock"?BASE_R*1.2:type==="nebula_dot"?BASE_R*0.4:type==="ice_shatter"?BASE_R*1.1:
type==="hue_orb"?BASE_R*1.1:type==="lightning_chain_64"?BASE_R*1.15:
type==="arcane_rune"?BASE_R*1.15:type==="pulse_gem"?BASE_R*1.0:
type==="cosmic_web"?BASE_R*1.2:type==="cosmic_node"?BASE_R*0.5:type==="warp_orb"?BASE_R*1.0:type==="bonus_orb"?BASE_R*0.65:
type==="omega_heart"?BASE_R*1.2:type==="nexus_gem"?BASE_R*1.1:type==="plasma_halo"?BASE_R*1.1:type==="glitch_node"?BASE_R*1.0:type==="cosmic_lens"?BASE_R*1.05:type==="nebula_burst2"?BASE_R*1.1:type==="flame_orb2"?BASE_R*1.0:type==="thor_gem"?BASE_R*1.0:type==="ice_nova2"?BASE_R*1.05:type==="arc_gem"?BASE_R*1.0:type==="moon_petal"?BASE_R*1.05:type==="star_dust2"?BASE_R*1.0:type==="solar_eye"?BASE_R*1.05:type==="void_serpent"?BASE_R*1.0:type==="astral_core"?BASE_R*1.05:type==="light_bolt"?BASE_R*1.0:type==="dark_pulsar"?BASE_R*1.0:type==="crystal_bomb"?BASE_R*1.0:type==="glacier_spike"?BASE_R*0.95:type==="plasma_pulse2"?BASE_R*1.05:type==="quantum_burst2"?BASE_R*0.9:type==="nebula_ring"?BASE_R*1.1:type==="flux_ring2"?BASE_R*1.1:type==="solar_vortex"?BASE_R*1.05:type==="drift_orb"?BASE_R*1.05:type==="chrono_star"?BASE_R*1.05:type==="prism_coil"?BASE_R*1.05:type==="star_veil"?BASE_R*1.05:type==="frost_halo"?BASE_R*1.05:type==="ember_clock"?BASE_R*1.0:type==="nova_spire"?BASE_R*1.0:type==="echo_lens"?BASE_R*1.05:type==="crystal_petal"?BASE_R*1.05:type==="twilight_shard"?BASE_R*1.0:type==="shadow_crystal"?BASE_R*1.0:type==="tide_orb"?BASE_R*1.05:type==="lunar_rune"?BASE_R*1.0:type==="storm_seed"?BASE_R*1.1:type==="warp_glyph"?BASE_R*1.0:type==="ether_bolt"?BASE_R*0.9:type==="cosmic_dust"?BASE_R*1.05:type==="nova_bell"?BASE_R*1.0:type==="prismatic_eye"?BASE_R*1.0:type==="thunder_crown"?BASE_R*1.1:type==="solar_beam"?BASE_R*1.0:type==="ice_prism"?BASE_R*1.0:type==="star_pulse"?BASE_R*1.05:type==="void_nova"?BASE_R*1.1:type==="flame_orb"?BASE_R*1.0:type==="crystal_shell"?BASE_R*1.05:type==="nebula_core"?BASE_R*1.1:type==="dusk_sphere"?BASE_R*1.0:type==="spiral_bead"?BASE_R*1.0:type==="quantum_flux"?BASE_R*1.05:type==="nebula_drop"?BASE_R*0.85:type==="pulse_gate"?BASE_R*1.05:type==="frost_gem"?BASE_R*0.95:type==="heat_orb"?BASE_R*1.0:type==="thunder_bead"?BASE_R*0.95:type==="mirror_star"?BASE_R*1.0:type==="aurora_wave"?BASE_R*1.15:type==="crystal_prism"?BASE_R*1.0:type==="plasma_ring"?BASE_R*1.1:type==="void_star"?BASE_R*1.0:type==="ice_dagger"?BASE_R*0.9:type==="shadow_flare"?BASE_R*1.0:type==="nova_drift"?BASE_R*1.2:type==="energy_pulse"?BASE_R*1.0:type==="solar_petal"?BASE_R*1.0:type==="cosmic_shard"?BASE_R*0.9:type==="crystal_wing_l"?BASE_R*0.95:type==="crystal_wing_r"?BASE_R*0.95:type==="storm_cloud"?BASE_R*1.1:type==="arc_node"?BASE_R*1.0:type==="prismatic_burst"?BASE_R*1.1:type==="moon_orb"?BASE_R*1.1:type==="lava_core"?BASE_R*1.0:type==="sun_petal"?BASE_R*1.1:type==="ghost_bell"?BASE_R*1.0:type==="plasma_disk"?BASE_R*1.0:type==="void_ring"?BASE_R*1.15:type==="ether_orb"?BASE_R*1.0:type==="ether_copy"?BASE_R*0.6:type==="quantum_blast"?BASE_R*1.1:type==="nexus_core"?BASE_R*1.5:type==="blink_star"?BASE_R*0.9:type==="omega_pulse"?BASE_R*1.8:type==="spiral_orb74"?BASE_R*0.95:type==="frost_nova"?BASE_R*1.2:type==="light_bomb"?BASE_R*1.0:type==="glitch_orb"?BASE_R*1.0:type==="solar_flare72"?BASE_R*1.1:type==="crystal_burst"?BASE_R*1.05:type==="crystal_shard71"?BASE_R*0.45:type==="time_rift"?BASE_R*1.1:type==="arc_bloom"?BASE_R*1.1:type==="neon_bolt"?BASE_R*0.85:type==="prism_clock"?BASE_R*1.1:type==="void_pulse"?BASE_R*1.0:type==="chain_nova"?BASE_R*1.15:type==="mirror_flash"?BASE_R*1.0:type==="mirror_decoy"?BASE_R*1.0:type==="dark_mirror"?BASE_R*1.1:type==="storm_ball"?BASE_R*0.9:type==="heal_orb"?BASE_R*0.55:
type==="normal"?BASE_R*(rarity?.size||1):BASE_R;
    const pos=pickPos(baseR);
    let lifetime=cfg.targetLifetime;
    // World modifiers: Ocean Deep (8) — slower targets (calmer waters), longer lifetimes
    if(cfg.world===8)lifetime*=1.15;
    // World modifiers: Goblin Mines (6) — slightly faster targets (more chaotic) — short lifetimes
    if(cfg.world===6)lifetime*=0.92;
    if(activePwrRef.current.some(p=>p.type==="SLOW"&&p.endsAt>Date.now()))lifetime*=1.6;
    if(activePwrRef.current.some(p=>p.type==="FREEZE"&&p.endsAt>Date.now())){vx=0;vy=0;}
    // Phantom targets: very short lifetime (1100ms fixed) — catch it or lose it!
    if(type==="phantom")lifetime=1100;
    // Volatile targets: medium-short lifetime (2200ms) — defuse or explode
    if(type==="volatile")lifetime=2200;
    // Ninja targets: longer lifetime (3500ms) so visible window is still ~700ms
    if(type==="ninja")lifetime=3500;
    // Comet targets: very short lifetime (2800ms) — it flies across fast!
    if(type==="comet")lifetime=2800;
    // Ice Comet: similar short lifetime
    if(type==="icecomet")lifetime=3000;
    // Time Bomb: fixed 3s detonation fuse
    if(type==="timebomb")lifetime=3000;
    // Firefly: shorter lifetime (they dart away fast)
    if(type==="firefly")lifetime=Math.min(lifetime,2200);
    // Geode: slightly longer (2-tap mechanic)
    if(type==="geode")lifetime=Math.max(lifetime,3400);
    // Thunderbolt: very short (2s) — it falls fast
    if(type==="thunderbolt")lifetime=Math.min(lifetime,2000);
    // Quantum: medium lifetime — 3 taps needed before it vanishes
    if(type==="quantum")lifetime=Math.max(lifetime,4500);
    // Pinwheel: medium lifetime
    if(type==="pinwheel")lifetime=Math.max(lifetime,3500);
    // Supercell: medium - time to electrify
    if(type==="supercell")lifetime=Math.max(lifetime,3800);
    // Prism Bomb: medium
    if(type==="prismbomb")lifetime=Math.max(lifetime,3200);
    // Ice Cage: medium-long — give targets time to get trapped
    if(type==="icecage")lifetime=Math.max(lifetime,4000);
    // Solar Flare: medium lifetime — it expands over time
    if(type==="solarflare")lifetime=Math.max(lifetime,3500);
    // Magma: slightly longer — gives time to defuse
    if(type==="magma")lifetime=Math.max(lifetime,4000);
    // Chain Collector: medium lifetime
    if(type==="chaincollector")lifetime=Math.max(lifetime,3000);
    // Stardust: medium
    if(type==="stardust")lifetime=Math.max(lifetime,3200);
    // Star piece: short — catch them quick!
    if(type==="star_piece")lifetime=2000;
    // Bloom: medium lifetime
    if(type==="bloom")lifetime=Math.max(lifetime,3000);
    // Petal: short — catch petals quickly!
    if(type==="petal")lifetime=1800;
    // Reflector: medium
    if(type==="reflector")lifetime=Math.max(lifetime,3200);
    // Clockwork: medium-long so player can catch it
    if(type==="clockwork")lifetime=Math.max(lifetime,3600);
    // Gravity Orb: medium lifetime (allows it to pull targets for a while)
    if(type==="gravityorb")lifetime=Math.max(lifetime,3200);
    // Crystal Ball: medium-long (gives time to plan the tap)
    if(type==="crystalball")lifetime=Math.max(lifetime,3800);
    // Vortex: medium-long — give it time to pull targets
    if(type==="vortex")lifetime=Math.max(lifetime,4200);
    // Pixel: short — blink and you'll miss it!
    if(type==="pixel")lifetime=Math.min(lifetime,1800);
    // Neon Sign: medium — needs to cycle through all 3 colors
    if(type==="neon")lifetime=Math.max(lifetime,3600);
    // Lantern: medium
    if(type==="lantern")lifetime=Math.max(lifetime,3000);
    // Drift: medium-long — let it drift far from center
    if(type==="drift")lifetime=Math.max(lifetime,4000);
    // Nebula: medium — enough time to release star pieces
    if(type==="nebula")lifetime=Math.max(lifetime,3800);
    // Wisp: medium — toggle between moving/still
    if(type==="wisp")lifetime=Math.max(lifetime,3500);
    // Meteor: very short — fly across fast
    if(type==="meteor")lifetime=Math.min(lifetime,2600);
    // Aura: medium — let ring pulse a few times
    if(type==="aura")lifetime=Math.max(lifetime,3400);
    // Smoke Ring: medium — expands over 3s
    if(type==="smoke_ring")lifetime=3000;
    // Chain Bomb: short-ish — defuse urgency
    if(type==="chain_bomb")lifetime=Math.min(lifetime,2800);
    // Warp Gate: medium
    if(type==="warp_gate")lifetime=Math.max(lifetime,3000);
    // Mirage: medium-long — time to spot it
    if(type==="mirage")lifetime=Math.max(lifetime,4000);
    // Surge Ball: medium — enough for 1-2 arc cycles
    if(type==="surge_ball")lifetime=Math.max(lifetime,3200);
    // Ticking Clock: fixed 4s sweep — hand makes full rotation
    if(type==="ticking_clock")lifetime=4000;
    // Echo Pulse: 3.5s — rings expand and fade over that window
    if(type==="echo_pulse")lifetime=3500;
    // Gravity Well: medium-long — pull builds up over time
    if(type==="gravity_well")lifetime=Math.max(lifetime,4200);
    // Spectrum Orb: medium — hue cycles every 1.5s; gets 2 full rotations
    if(type==="spectrum_orb")lifetime=Math.max(lifetime,3000);
    // Shard Storm: medium — shards align every ~1.5s
    if(type==="shard_storm")lifetime=Math.max(lifetime,3500);
    // Pulse Nova: medium — 1.5s charge then 2s release window
    if(type==="pulse_nova")lifetime=3800;
    // Bubble Cluster: medium — 5 bubbles, tap soon before they drift apart
    if(type==="bubble_cluster")lifetime=Math.max(lifetime,3200);
    // Void Shard: medium-long — needs time to absorb pts
    if(type==="void_shard")lifetime=Math.max(lifetime,4500);
    // Stasis Field: medium — ring expands over 2.5s
    if(type==="stasis_field")lifetime=2500;
    // Crystalline: medium — spins through facet angles
    if(type==="crystalline")lifetime=Math.max(lifetime,3000);
    // Quantum Leap: medium — gets 3 teleports over lifetime
    if(type==="quantum_leap")lifetime=Math.max(lifetime,3800);
    // Magma Burst: medium-long — needs to heat up fully
    if(type==="magma_burst")lifetime=Math.max(lifetime,4000);
    // Lightning Rod: medium-long — needs time to attract targets
    if(type==="lightning_rod")lifetime=Math.max(lifetime,4200);
    // Prism Gate: medium — tap then catch shards
    if(type==="prism_gate")lifetime=Math.max(lifetime,3000);
    // Solar Burst: medium — radiant for a window
    if(type==="solar_burst")lifetime=Math.max(lifetime,3400);
    // Frost Comet: short — it speeds across fast
    if(type==="frost_comet")lifetime=Math.min(lifetime,2800);
    // Energy Web: medium-long — web builds up connections over time
    if(type==="energy_web")lifetime=Math.max(lifetime,4000);
    // Void Rift: medium-long — needs time to devour targets
    if(type==="void_rift")lifetime=Math.max(lifetime,5000);
    // Laser Grid: medium
    if(type==="laser_grid")lifetime=Math.max(lifetime,3500);
    // Bouncing Orb: medium-long — needs multiple bounces
    if(type==="bouncing_orb")lifetime=Math.max(lifetime,4200);
    // Time Capsule: fixed 5s — golden window at 2.5-3.5s
    if(type==="time_capsule")lifetime=5000;
    // Nether Shard: medium-long — grows stronger over full lifetime
    if(type==="nether_shard")lifetime=Math.max(lifetime,5000);
    // Accumulator: medium-long — builds up stacks over time
    if(type==="accumulator")lifetime=Math.max(lifetime,5000);
    // Jackpot: long — give player time to find and tap it
    if(type==="jackpot_target")lifetime=Math.max(lifetime,5500);
    // Phase Orb: medium — 3 phase cycles over 3s
    if(type==="phase_orb")lifetime=Math.max(lifetime,3600);
    // Ripple Wave: medium — rings pulse every 0.8s
    if(type==="ripple_wave")lifetime=Math.max(lifetime,3200);
    // Venom Orb: medium — 1s antidote window then degrades
    if(type==="venom_orb")lifetime=Math.max(lifetime,3500);
    // Thunder Egg: medium — 3 phase cracks over 3.6s
    if(type==="thunder_egg")lifetime=Math.max(lifetime,3600);
    // Moon Glyph: medium — cycles 5 symbols every 600ms
    if(type==="moon_glyph")lifetime=Math.max(lifetime,3000);
    // Comet Tail: short — blazing fast
    if(type==="comet_tail")lifetime=Math.min(lifetime,2400);
    // Ember Ring: medium — expansion peaks near end
    if(type==="ember_ring")lifetime=Math.max(lifetime,3200);
    // Supernova: medium — builds up heat
    if(type==="supernova")lifetime=Math.max(lifetime,3800);
    // Crystal Matrix: medium-long — 3 taps needed
    if(type==="crystal_matrix")lifetime=Math.max(lifetime,4500);
    // Plasma Storm: medium
    if(type==="plasma_storm")lifetime=Math.max(lifetime,3500);
    // Nexus Gate: medium-long — pair needs time
    if(type==="nexus_gate")lifetime=Math.max(lifetime,4000);
    // Arc Light: medium
    if(type==="arc_light")lifetime=Math.max(lifetime,3500);
    // Pulse Chain: medium
    if(type==="pulse_chain")lifetime=Math.max(lifetime,3000);
    // Ghost Orb: long — needs time to fade fully invisible
    if(type==="ghost_orb")lifetime=Math.max(lifetime,4200);
    // Time Fracture: medium
    if(type==="time_fracture")lifetime=Math.max(lifetime,3200);
    // Twilight Orb: long — fades over time
    if(type==="twilight_orb")lifetime=Math.max(lifetime,4500);
    // Life Orb: medium
    if(type==="life_orb")lifetime=Math.max(lifetime,3500);
    // Score Amp: medium — amp duration
    if(type==="score_amp")lifetime=Math.max(lifetime,4000);
    // Night Orb: long — needs time to brighten
    if(type==="night_orb")lifetime=Math.max(lifetime,4500);
    // Omega Orb: medium-long — rare and special
    if(type==="omega_orb")lifetime=Math.max(lifetime,4000);
    // Thunder Ball: medium
    if(type==="thunder_ball")lifetime=Math.max(lifetime,3200);
    // Cosmic Aura: medium
    if(type==="cosmic_aura")lifetime=Math.max(lifetime,3500);
    // Combo Rush: short — streak-based, punishing if you wait
    if(type==="combo_rush")lifetime=Math.min(lifetime,2800);
    // Neon Crystal: medium
    if(type==="neon_crystal")lifetime=Math.max(lifetime,3600);
    // Sonar Ping: medium
    if(type==="sonar_ping")lifetime=Math.max(lifetime,3500);
    // Gravity Bomb: medium-long — falls slowly
    if(type==="gravity_bomb")lifetime=Math.max(lifetime,4500);
    // Speed Nova: long — needs to grow
    if(type==="speed_nova")lifetime=Math.max(lifetime,4000);
    // Quantum Split: medium
    if(type==="quantum_split")lifetime=Math.max(lifetime,3500);
    if(type==="quantum_half")lifetime=Math.max(lifetime,2000);
    // Frost Bolt: short — it's fast
    if(type==="frost_bolt")lifetime=Math.min(lifetime,2200);
    // Vortex Star: medium-long — needs time to accumulate stars
    if(type==="vortex_star")lifetime=Math.max(lifetime,4000);
    // Pixel Burst: medium-long — 3 taps needed
    if(type==="pixel_burst")lifetime=Math.max(lifetime,4500);
    // Star Forge: long — needs time to charge up
    if(type==="star_forge")lifetime=Math.max(lifetime,4500);
    // Magic Dice: medium
    if(type==="magic_dice")lifetime=Math.max(lifetime,3600);
    // Wormhole: medium
    if(type==="wormhole_tgt")lifetime=Math.max(lifetime,3800);
    // Shadow Pulse: medium
    if(type==="shadow_pulse")lifetime=Math.max(lifetime,3200);
    // Eclipse Orb: medium
    if(type==="eclipse_orb")lifetime=Math.max(lifetime,3200);
    // Spin Bloom: medium — petals fade over time
    if(type==="spin_bloom")lifetime=Math.max(lifetime,4000);
    // Tesla Node: long — needs time to bank pts
    if(type==="tesla_node")lifetime=Math.max(lifetime,5000);
    // Rune Circle: medium
    if(type==="rune_circle")lifetime=Math.max(lifetime,3400);
    // Solar Orbit: medium
    if(type==="solar_orbit")lifetime=Math.max(lifetime,3800);
    // Chroma Shift: medium
    if(type==="chroma_shift")lifetime=Math.max(lifetime,3600);
    // Neon Pulsar: medium
    if(type==="neon_pulsar")lifetime=Math.max(lifetime,3600);
    // Drift Mine: longer — gives player chance to defuse
    if(type==="drift_mine")lifetime=Math.max(lifetime,4500);
    // Shifting Target: medium — cycles through 5 phases
    if(type==="shift_target")lifetime=Math.max(lifetime,4500);
    // Lucky Wheel: medium — spinning number
    if(type==="lucky_wheel")lifetime=Math.max(lifetime,3500);
    // Abyss Orb: medium — penalty timer
    if(type==="abyss_orb")lifetime=Math.max(lifetime,4000);
    // Cosmic Portal: medium
    if(type==="cosmic_portal")lifetime=Math.max(lifetime,3500);
    // Flame Ring: medium — ring must shrink visibly
    if(type==="flame_ring")lifetime=Math.max(lifetime,4000);
    // Ice Clock: exactly 5 seconds
    if(type==="ice_clock")lifetime=5000;
    // Holo Decoy: medium
    if(type==="holo_decoy")lifetime=Math.max(lifetime,3200);
    // Mirror Gate: medium
    if(type==="mirror_gate")lifetime=Math.max(lifetime,3500);
    // Spiral Bomb: exactly 4s for spiral to fill
    if(type==="spiral_bomb")lifetime=4000;
    // Chain Orb: long — 4 taps needed
    if(type==="chain_orb")lifetime=Math.max(lifetime,5000);
    // Bolt Catcher: short urgency window
    if(type==="bolt_catcher")lifetime=1500;
    // Gem Harvest: longer — needs time to collect gems
    if(type==="gem_harvest")lifetime=Math.max(lifetime,5000);
    // Prism Pulse: medium — color cycle must be visible
    if(type==="prism_pulse")lifetime=Math.max(lifetime,3600);
    // Dark Matter: long — nearly invisible so player needs time to find it
    if(type==="dark_matter")lifetime=Math.max(lifetime,5000);
    // Comet Blast: short — fast mover
    if(type==="comet_blast")lifetime=Math.min(lifetime,2500);
    // Time Prison: long — needs time to cage nearby targets
    if(type==="time_prison")lifetime=Math.max(lifetime,5000);
    // Shadow Twin: medium
    if(type==="shadow_twin")lifetime=Math.max(lifetime,3500);
    // Photon Burst: medium — visible/invisible cycling
    if(type==="photon_burst")lifetime=Math.max(lifetime,3600);
    // Vault Orb: long — banks pts over time
    if(type==="vault_orb")lifetime=Math.max(lifetime,6000);
    // Nano Swarm: medium
    if(type==="nano_swarm")lifetime=Math.max(lifetime,3200);
    if(type==="nano_unit")lifetime=Math.max(lifetime,1800);
    // Rune Chain: long — 3 sequential rune taps needed
    if(type==="rune_chain")lifetime=Math.max(lifetime,5000);
    // Plasma Web: medium
    if(type==="plasma_web")lifetime=Math.max(lifetime,3500);
    // Astral Orb: medium — teleports frequently
    if(type==="astral_orb")lifetime=Math.max(lifetime,4000);
    // Crystal Clock: long — fills 8 segments over time
    if(type==="crystal_clock")lifetime=Math.max(lifetime,6000);
    // Sunflower Orb: long — lose 1 petal per 300ms (12 petals = 3.6s to lose all)
    if(type==="sunflower_orb")lifetime=Math.max(lifetime,5000);
    // Toxic Cloud: long — persistent penalty zone
    if(type==="toxic_cloud")lifetime=Math.max(lifetime,5000);
    // Laser Prism: long — charges targets over time
    if(type==="laser_prism")lifetime=Math.max(lifetime,5000);
    // Echo Star: medium — spawn echoes at start
    if(type==="echo_star")lifetime=Math.max(lifetime,4000);
    if(type==="echo_point")lifetime=2000;
    // Meteor Shower core: medium
    if(type==="meteor_shower")lifetime=Math.max(lifetime,3500);
    if(type==="meteor_shard")lifetime=2000;
    // Golden Gate: ultra-short — blink and gone
    if(type==="golden_gate")lifetime=800;
    // Pulse Ring: medium — ring must cycle
    if(type==="pulse_ring")lifetime=Math.max(lifetime,4000);
    // Clockwork Bomb: long — 5 gear taps needed
    if(type==="clockwork_bomb")lifetime=Math.max(lifetime,6000);
    // Nebula Vortex: long — grows slowly
    if(type==="nebula_vortex")lifetime=Math.max(lifetime,5000);
    // Frost Hex: medium
    if(type==="frost_hex")lifetime=Math.max(lifetime,4000);
    // Lightning Orb: medium
    if(type==="lightning_orb")lifetime=Math.max(lifetime,3500);
    // Star Burst: medium-long — needs time to expand fully
    if(type==="star_burst")lifetime=Math.max(lifetime,4500);
    // Crystal Spire: medium — charge then shatter
    if(type==="crystal_spire")lifetime=Math.max(lifetime,4000);
    // Phase Gate: medium — needs full cycle
    if(type==="phase_gate")lifetime=Math.max(lifetime,5000);
    // Void Pulsar: medium — shrinks over its lifetime
    if(type==="void_pulsar")lifetime=Math.max(lifetime,3500);
    // Nova Cannon: medium-long — needs charge time
    if(type==="nova_cannon")lifetime=Math.max(lifetime,4500);
    // Hex Grid: long — 6 taps needed
    if(type==="hex_grid")lifetime=Math.max(lifetime,7000);
    // Quantum Tunnel: medium-long — must teleport a few times
    if(type==="quantum_tunnel_a"||type==="quantum_tunnel_b")lifetime=Math.max(lifetime,5000);
    // Inferno Orb: long — heat stacks build over time
    if(type==="inferno_orb")lifetime=Math.max(lifetime,9000);
    // Chrono Sphere: medium
    if(type==="chrono_sphere")lifetime=Math.max(lifetime,4000);
    // Prismatic Lens: medium — must cycle through colors
    if(type==="prismatic_lens")lifetime=Math.max(lifetime,4200);
    // Shadow Mirror: medium
    if(type==="shadow_mirror")lifetime=Math.max(lifetime,3500);
    // Plasma Comet: short-medium — it's moving fast
    if(type==="plasma_comet")lifetime=Math.max(lifetime,2500);
    // Abyssal Gate: long — drains more over time
    if(type==="abyssal_gate")lifetime=Math.max(lifetime,6000);
    // Warp Ball: medium
    if(type==="warp_ball")lifetime=Math.max(lifetime,4000);
    // Crystal Spike: medium
    if(type==="crystal_spike")lifetime=Math.max(lifetime,3500);
    // Energy Core: medium — cycles through phases
    if(type==="energy_core")lifetime=Math.max(lifetime,4500);
    // Arcane Circle: medium — needs to cycle runes
    if(type==="arcane_circle")lifetime=Math.max(lifetime,4000);
    // Stellar Drift: long — needs to drift far
    if(type==="stellar_drift")lifetime=Math.max(lifetime,6000);
    // Bubble Wave: medium — grows until burst
    if(type==="bubble_wave")lifetime=Math.max(lifetime,4000);
    // Iron Shield: medium — 3 hits needed
    if(type==="iron_shield")lifetime=Math.max(lifetime,4500);
    // Time Echo: short — it's a brief ghost
    if(type==="time_echo")lifetime=Math.min(lifetime,2500);
    // Gravity Orb 2: long — pulls more over time
    if(type==="gravity_orb2")lifetime=Math.max(lifetime,7000);
    // Pixel Swarm: long — 8 units to collect
    if(type==="pixel_swarm")lifetime=Math.max(lifetime,6000);
    if(type==="chrono_crystal")lifetime=Math.max(lifetime,7000); // long window for freeze interaction
    if(type==="void_seed")lifetime=Math.max(lifetime,8000); // needs time to grow
    if(type==="prism_shard")lifetime=Math.max(lifetime,3500); // needs time to split
    if(type==="temporal_orb")lifetime=Math.max(lifetime,8000); // multiple rewind cycles
    if(type==="magma_seed")lifetime=Math.max(lifetime,6000); // orbiting drops need time
    if(type==="gemini_pair")lifetime=Math.max(lifetime,5000); // needs partner window
    if(type==="aurora_ring")lifetime=Math.max(lifetime,5000); // ring expansion time
    if(type==="mirror_twin")lifetime=Math.max(lifetime,5500); // needs time for both taps
    if(type==="void_crystal")lifetime=Math.max(lifetime,7000); // needs to shrink fully
    if(type==="energy_orb2")lifetime=Math.max(lifetime,6000); // needs to charge fully
    if(type==="comet_trail")lifetime=Math.max(lifetime,3500); // fast mover
    if(type==="crystal_golem"){lifetime=Math.max(lifetime,7000);} // 3 hits needed
    if(type==="freeze_orb")lifetime=Math.max(lifetime,4000); // pulse animation
    if(type==="twin_comet")lifetime=Math.max(lifetime,3000); // fast comet
    if(type==="plasma_vortex")lifetime=Math.max(lifetime,5000); // alignment cycles
    if(type==="star_bomb")lifetime=3500; // fixed 3.5s fuse
    if(type==="nova_star")lifetime=Math.max(lifetime,4500); // dwell + sweep window
    if(type==="ice_phoenix")lifetime=Math.max(lifetime,4500); // hatch + post-hatch window
    if(type==="spiral_orb")lifetime=Math.max(lifetime,5000); // time to spiral far enough
    if(type==="dark_pulse")lifetime=Math.max(lifetime,4000); // dark anti-target window
    if(type==="lunar_eclipse")lifetime=Math.max(lifetime,5000); // full grow+shrink cycle
    if(type==="chain_wall")lifetime=Math.max(lifetime,3500); // moderate window
    if(type==="bounce_web")lifetime=Math.max(lifetime,4000); // time to snare nearby movers
    if(type==="meteor_shower")lifetime=Math.max(lifetime,4000); // summons meteors on tap
    if(type==="prismatic_orb")lifetime=Math.max(lifetime,5000); // full hue cycle window
    if(type==="void_tether")lifetime=Math.max(lifetime,4500); // time for tether combo
    if(type==="crystal_pulsar")lifetime=Math.max(lifetime,4000); // 2+ full pulse cycles
    if(type==="shadow_echo")lifetime=Math.max(lifetime,4000); // echo spawns at 1.5s
    if(type==="flux_ring")lifetime=3000; // fixed 3s expanding ring
    if(type==="quantum_flare")lifetime=Math.max(lifetime,4000); // tap window
    if(type==="nebula_crystal")lifetime=Math.max(lifetime,6000); // 3-phase crack time
    if(type==="time_shard")lifetime=Math.max(lifetime,5000); // multiple blink cycles
    if(type==="gravity_node")lifetime=Math.max(lifetime,6000); // pull window
    if(type==="lightning_cage")lifetime=Math.max(lifetime,5000); // cage dwell time
    if(type==="crystal_web")lifetime=5000; // 3-node tap chain window
    if(type==="arc_pulse")lifetime=Math.max(lifetime,4000); // rotating arc needs time
    if(type==="void_bloom")lifetime=5500; // bloom + 4 petals collection window
    if(type==="void_petal")lifetime=2500; // petals scatter and expire fast
    if(type==="stellar_orb")lifetime=6000; // grows slowly over 6s to max size
    if(type==="rune_gate_a"||type==="rune_gate_b")lifetime=Math.max(lifetime,4500); // pair window
    if(type==="nova_crystal")lifetime=Math.max(lifetime,5000); // 3 charge stages over lifetime
    if(type==="phase_shift")lifetime=Math.max(lifetime,4800); // multiple phase cycles needed
    if(type==="solar_ring")lifetime=4000; // ring expands over 4s
    if(type==="chrono_vortex")lifetime=Math.max(lifetime,5000); // slow field builds up
    if(type==="prism_nova")lifetime=5000; // time for player to notice shards
    if(type==="prism_shard")lifetime=3500; // short window to collect shards
    if(type==="echo_blast")lifetime=4500; // ring lingers after tap
    if(type==="vortex_star_61")lifetime=5000; // spinning star needs time
    if(type==="quantum_drift")lifetime=Math.max(lifetime,5500); // drift + freeze window
    if(type==="plasma_burst")lifetime=4000; // burst before firing bolts
    if(type==="plasma_bolt")lifetime=2000; // bolt window is short
    if(type==="star_clock")lifetime=6000; // 5s countdown + 1s buffer
    if(type==="nebula_clock")lifetime=6000; // time to spawn and collect dots
    if(type==="nebula_dot")lifetime=3500; // orbiting dot window
    if(type==="ice_shatter")lifetime=Math.max(lifetime,5500); // 3 hits needed
    if(type==="hue_orb")lifetime=Math.max(lifetime,6000); // multiple color cycles
    if(type==="lightning_chain_64")lifetime=4000; // chain opportunity window
    if(type==="arcane_rune"){lifetime=Math.max(lifetime,5000);} // symbol + follow-up window
    if(type==="pulse_gem")lifetime=Math.max(lifetime,4500); // multiple pulse cycles
    if(type==="cosmic_web")lifetime=6000; // time to tap all 6 nodes
    if(type==="cosmic_node")lifetime=5000; // node window
    if(type==="warp_orb")lifetime=Math.max(lifetime,5000); // several warp cycles
    if(type==="bonus_orb")lifetime=5000; // wave lifetime
    if(type==="omega_heart")lifetime=Math.max(lifetime,5000); // legendary sync window
    if(type==="nexus_gem")lifetime=Math.max(lifetime,4500); // gem window
    if(type==="plasma_halo")lifetime=Math.max(lifetime,4500); // halo window
    if(type==="glitch_node")lifetime=Math.max(lifetime,4000); // several burst cycles
    if(type==="astral_core")lifetime=Math.max(lifetime,4500); // ring align window
    if(type==="light_bolt")lifetime=Math.max(lifetime,4500); // charge build window
    if(type==="solar_eye")lifetime=Math.max(lifetime,4500); // iris open/close cycle
    if(type==="void_serpent")lifetime=Math.max(lifetime,4500); // vertical alignment window
    if(type==="moon_petal")lifetime=Math.max(lifetime,4500); // bloom cycle
    if(type==="star_dust2")lifetime=Math.max(lifetime,4000); // trail build time
    if(type==="ice_nova2")lifetime=Math.max(lifetime,4500); // freeze peak window
    if(type==="arc_gem")lifetime=Math.max(lifetime,4500); // charge build time
    if(type==="flame_orb2")lifetime=Math.max(lifetime,4500); // heat peak cycle
    if(type==="thor_gem")lifetime=Math.max(lifetime,4500); // storm phase window
    if(type==="cosmic_lens")lifetime=Math.max(lifetime,4500); // focus convergence
    if(type==="nebula_burst2")lifetime=Math.max(lifetime,4500); // nova moment window
    if(type==="dark_pulsar")lifetime=Math.max(lifetime,4500); // burst phase window
    if(type==="crystal_bomb")lifetime=Math.max(lifetime,3500); // defuse window
    if(type==="glacier_spike")lifetime=Math.max(lifetime,3800); // ice spike
    if(type==="plasma_pulse2")lifetime=Math.max(lifetime,4000); // pulse peak window
    if(type==="quantum_burst2")lifetime=Math.max(lifetime,4000); // pulse cycles
    if(type==="nebula_ring")lifetime=Math.max(lifetime,4500); // ring window
    if(type==="flux_ring2")lifetime=Math.max(lifetime,4000); // ring window
    if(type==="solar_vortex")lifetime=Math.max(lifetime,4500); // vortex window
    if(type==="drift_orb")lifetime=Math.max(lifetime,4500); // drift still window
    if(type==="chrono_star")lifetime=Math.max(lifetime,4500); // apex phase window
    if(type==="prism_coil")lifetime=Math.max(lifetime,4000); // coil sync window
    if(type==="star_veil")lifetime=Math.max(lifetime,5000); // veil reveal window
    if(type==="frost_halo")lifetime=Math.max(lifetime,4500); // freeze phase window
    if(type==="ember_clock")lifetime=Math.max(lifetime,4000); // ember countdown
    if(type==="nova_spire")lifetime=Math.max(lifetime,4500); // peak energy window
    if(type==="echo_lens")lifetime=Math.max(lifetime,4000); // focus phase window
    if(type==="crystal_petal")lifetime=Math.max(lifetime,4500); // petal align window
    if(type==="twilight_shard")lifetime=Math.max(lifetime,5000); // twilight phase window
    if(type==="shadow_crystal")lifetime=Math.max(lifetime,5000); // dark phase window
    if(type==="tide_orb")lifetime=Math.max(lifetime,4500); // wave crest window
    if(type==="lunar_rune")lifetime=Math.max(lifetime,6000); // full moon window
    if(type==="storm_seed")lifetime=Math.max(lifetime,5500); // growth window
    if(type==="warp_glyph")lifetime=Math.max(lifetime,4500); // several glow cycles
    if(type==="ether_bolt")lifetime=Math.max(lifetime,3500); // fast moving bolt
    if(type==="cosmic_dust")lifetime=Math.max(lifetime,4500); // several density cycles
    if(type==="nova_bell")lifetime=Math.max(lifetime,4000); // resonance window
    if(type==="prismatic_eye")lifetime=Math.max(lifetime,4000); // gaze window
    if(type==="thunder_crown")lifetime=Math.max(lifetime,5000); // full charge window
    if(type==="solar_beam")lifetime=Math.max(lifetime,4000); // beam window
    if(type==="ice_prism")lifetime=Math.max(lifetime,5000); // several split cycles
    if(type==="star_pulse")lifetime=Math.max(lifetime,4000); // several pulse cycles
    if(type==="void_nova")lifetime=Math.max(lifetime,4500); // full collapse window
    if(type==="flame_orb")lifetime=Math.max(lifetime,4500); // several intensity cycles
    if(type==="crystal_shell")lifetime=Math.max(lifetime,5000); // crack-to-core window
    if(type==="nebula_core")lifetime=Math.max(lifetime,4500); // nebula window
    if(type==="dusk_sphere")lifetime=Math.max(lifetime,5000); // full dusk cycle
    if(type==="spiral_bead")lifetime=Math.max(lifetime,4000); // spiral window
    if(type==="quantum_flux")lifetime=Math.max(lifetime,4500); // several flux cycles
    if(type==="nebula_drop")lifetime=Math.max(lifetime,3500); // teardrop window
    if(type==="pulse_gate")lifetime=Math.max(lifetime,4500); // several open cycles
    if(type==="frost_gem")lifetime=Math.max(lifetime,4000); // gem window
    if(type==="heat_orb")lifetime=Math.max(lifetime,4000); // molten window
    if(type==="thunder_bead")lifetime=Math.max(lifetime,3800); // zap window
    if(type==="mirror_star")lifetime=Math.max(lifetime,4200); // star window
    if(type==="aurora_wave")lifetime=Math.max(lifetime,4500); // wave crest cycles
    if(type==="crystal_prism")lifetime=Math.max(lifetime,4000); // prism window
    if(type==="plasma_ring")lifetime=Math.max(lifetime,4200); // several spin cycles
    if(type==="void_star")lifetime=Math.max(lifetime,4000); // star window
    if(type==="ice_dagger")lifetime=4000; // sharp window
    if(type==="shadow_flare")lifetime=4500; // several bright/dark cycles
    if(type==="nova_drift")lifetime=3500; // fades as it expands
    if(type==="energy_pulse")lifetime=5500; // full charge cycle
    if(type==="solar_petal")lifetime=Math.max(lifetime,4000); // spinning rays
    if(type==="cosmic_shard")lifetime=Math.max(lifetime,4500); // 3 color cycles
    if(type==="crystal_wing_l"||type==="crystal_wing_r")lifetime=Math.max(lifetime,5000); // pair window
    if(type==="storm_cloud")lifetime=Math.max(lifetime,4500); // several bolt flashes
    if(type==="arc_node")lifetime=Math.max(lifetime,3800); // electric window
    if(type==="prismatic_burst")lifetime=6000; // all 5 phases visible
    if(type==="moon_orb")lifetime=6000; // full moon phase cycle
    if(type==="lava_core")lifetime=Math.max(lifetime,3800); // hot window
    if(type==="sun_petal")lifetime=5000; // full bloom cycle
    if(type==="ghost_bell")lifetime=Math.max(lifetime,4500); // several ring phases
    if(type==="plasma_disk")lifetime=Math.max(lifetime,4000); // several spin cycles
    if(type==="void_ring")lifetime=Math.max(lifetime,4200); // ring orbit visible
    if(type==="ether_orb")lifetime=4500; // clear window for the split
    if(type==="ether_copy")lifetime=3000; // short — grab the copies fast
    if(type==="quantum_blast")lifetime=5000; // several pulse cycles
    if(type==="nexus_core")lifetime=Math.max(lifetime,4000); // generous window for easy target
    if(type==="blink_star")lifetime=Math.max(lifetime,5600); // 8 blink cycles of 0.7s
    if(type==="omega_pulse")lifetime=3000; // 3s window — vanishes fast!
    if(type==="spiral_orb74")lifetime=Math.max(lifetime,5500); // several spiral revolutions
    if(type==="frost_nova")lifetime=Math.max(lifetime,4500); // generous window
    if(type==="light_bomb")lifetime=Math.max(lifetime,4000); // easy safe collect
    if(type==="glitch_orb")lifetime=Math.max(lifetime,5000); // several flicker cycles
    if(type==="solar_flare72")lifetime=Math.max(lifetime,5000); // full bright-fade cycle
    if(type==="crystal_burst")lifetime=Math.max(lifetime,4000); // time to tap before expiry
    if(type==="crystal_shard71")lifetime=2500; // short window per shard
    if(type==="time_rift")lifetime=Math.max(lifetime,8000); // 8s but ticks at half speed
    if(type==="arc_bloom")lifetime=5000; // 3s to bloom fully, 2s window at peak
    if(type==="neon_bolt")lifetime=3000; // short window before it exits
    if(type==="prism_clock")lifetime=Math.max(lifetime,5000); // several rotation cycles
    if(type==="void_pulse")lifetime=Math.max(lifetime,4500); // several pulse cycles
    if(type==="chain_nova")lifetime=Math.max(lifetime,4000); // window to tap
    if(type==="mirror_flash")lifetime=Math.max(lifetime,5000); // flash cycle
    if(type==="mirror_decoy")lifetime=Math.max(lifetime,5000); // matches real
    if(type==="dark_mirror")lifetime=Math.max(lifetime,5500); // several opacity cycles
    if(type==="storm_ball")lifetime=Math.max(lifetime,5000); // acceleration window
    if(type==="heal_orb")lifetime=3000; // short collect window
    if(type==="glow_shard")lifetime=Math.max(lifetime,4000); // enough time to collect frags
    if(type==="glow_fragment")lifetime=Math.max(lifetime,2500); // short fragment window
    if(type==="quantum_leech")lifetime=Math.max(lifetime,5000); // drain over time
    // Solar Drone: medium
    if(type==="solar_drone")lifetime=Math.max(lifetime,3200);
    // Crystal Cluster: medium-long
    if(type==="crystal_cluster")lifetime=Math.max(lifetime,3500);
    // Swarm: medium
    if(type==="swarm")lifetime=Math.max(lifetime,3000);
    // Celestial: long (it's big and slow)
    if(type==="celestial")lifetime=Math.max(lifetime,5000);
    // Fractal: medium
    if(type==="fractal"||type==="fractal_mini")lifetime=Math.max(lifetime,3000);
    // Plasma: medium-long (needs to charge up fully)
    if(type==="plasma")lifetime=Math.max(lifetime,4000);
    // Timewarp Target: medium
    if(type==="timewarp_tgt")lifetime=Math.max(lifetime,3500);
    // Thunderclap: medium
    if(type==="thunderclap")lifetime=Math.max(lifetime,3200);
    // Mirror Shard: medium-long (3 taps needed)
    if(type==="mirror_shard")lifetime=Math.max(lifetime,4500);
    // Rune Stone: medium-long — needs multiple rune cycles
    if(type==="rune_stone")lifetime=Math.max(lifetime,4500);
    // Skill: target_sense — extra lifetime
    const tsk=(saveRef.current.skills||{}).target_sense||0;
    if(tsk>=1)lifetime+=500;if(tsk>=2)lifetime+=500;if(tsk>=3)lifetime+=1000;
    // Normal targets telegraph their position 260ms before becoming active
    const anticipateMs=type==="normal"?260:0;
    // Size scaling: normal targets shrink by up to 20% at level 100 (0.8 at L100, 1.0 at L1)
    const finalR=type==="normal"?Math.round(baseR*(1-Math.min(0.20,(cfg.id||1)/100*0.20))):baseR;
    // Boss pattern: 4 patterns based on worldId, advances phases as HP drops
    const bossPattern=type==="boss"?(cfg.world%4):null;
    const mainId=Math.random().toString(36).slice(2);
    const twinGroupId=type==="twin"?mainId:undefined;
    const pendingTwin=type==="twin"?gs._pendingTwin:null;
    if(type==="twin"&&gs._pendingTwin)delete gs._pendingTwin;
    const holoReal=type==="holo_decoy"?Math.random()<0.60:undefined;
    const prismTargetIdx=type==="prism_pulse"?Math.floor(Math.random()*6):undefined;
    const timePrisonCaged=[];
    targetsRef.current.push({
      id:mainId,type,rarity:type==="normal"?rarity:null,
      x:pos.x,y:pos.y,radius:finalR,color,glow,lifetime,
      spawnedAt:Date.now()+anticipateMs, // lifetime starts after anticipation
      born:performance.now(),anticipateMs,
      moving,ghost,vx,vy,pwrType,hitsLeft,maxHits,trail:moving?[]:null,
      worldId:cfg.world, worldColor:cfg.worldColor,
      bossPattern, bossPhase:1, // phase 1 = full HP, 2 = mid, 3 = rage
      cx:pos.x, cy:pos.y, // anchor for orbit patterns
      twinGroupId,
      _holoReal:holoReal,
      _prismTargetIdx:prismTargetIdx,
    });
    // Spawn cosmic_web nodes immediately
    if(type==="cosmic_web"){
      const cwCenter=targetsRef.current[targetsRef.current.length-1];
      if(cwCenter){
        const cw66c=canvasRef.current?.width||360;const ch66c=canvasRef.current?.height||640;
        const nodeIds66=[];
        for(let ni=0;ni<6;ni++){
          const ang66=ni*(Math.PI*2/6);
          const nid66=Math.random().toString(36).slice(2);
          nodeIds66.push(nid66);
          const nx66=Math.max(25,Math.min(cw66c-25,cwCenter.x+Math.cos(ang66)*70));
          const ny66=Math.max(25,Math.min(ch66c-25,cwCenter.y+Math.sin(ang66)*70));
          targetsRef.current.push({id:nid66,type:"cosmic_node",rarity:null,
            x:nx66,y:ny66,radius:BASE_R*0.5,color:"#38bdf8",glow:"#cffafe",
            lifetime:5000,spawnedAt:Date.now(),born:performance.now(),
            moving:false,ghost:false,vx:0,vy:0,trail:null,
            hitsLeft:1,maxHits:1,dying:null,worldId:cwCenter.worldId,worldColor:cwCenter.worldColor,
            bossPattern:null,bossPhase:1,cx:nx66,cy:ny66,anticipateMs:0,pwrType:null,
            _parentId:cwCenter.id,_nodeIdx:ni});
        }
        cwCenter._nodeIds=nodeIds66;cwCenter._nodesLeft=6;cwCenter._nodesTapped=0;
      }
    }
    // Set arcane_rune symbol property
    if(type==="arcane_rune"){
      const lastT65=targetsRef.current[targetsRef.current.length-1];
      if(lastT65){lastT65._symbol=Math.floor(Math.random()*4);lastT65._primed=false;}
    }
    // Set prism_clock world segment hint
    if(type==="prism_clock"){
      const lastPC69=targetsRef.current[targetsRef.current.length-1];
      if(lastPC69){lastPC69._worldSegHint=((cfg.id||0)>0?Math.floor(((cfg.id||0)-1)/10)%3:0);}
    }
    // Set hue_orb world hue hint
    if(type==="hue_orb"){
      const lastHO65=targetsRef.current[targetsRef.current.length-1];
      if(lastHO65){
        const worldColors65=["#ef4444","#f97316","#eab308","#22c55e","#06b6d4","#818cf8","#a855f7","#ec4899","#f59e0b","#6366f1"];
        const worldHue65=[0,25,55,120,190,240,270,320,45,235];
        lastHO65._worldHue=worldHue65[(cfg.id||0)>0?Math.floor(((cfg.id||0)-1)/10)%10:0]||0;
      }
    }
    // Spawn rune_gate_b partner for rune_gate_a immediately
    if(type==="rune_gate_a"){
      const cw57=canvasRef.current?.width||360;const ch57=canvasRef.current?.height||640;
      const bx57=Math.max(40,Math.min(cw57-40,cw57-pos.x));const by57=Math.max(40,Math.min(ch57-40,pos.y+(Math.random()<0.5?60:-60)));
      const bId57=Math.random().toString(36).slice(2);
      const mainRG=targetsRef.current[targetsRef.current.length-1];
      mainRG._partnerId=bId57;mainRG._primed=false;
      targetsRef.current.push({id:bId57,type:"rune_gate_b",rarity:null,
        x:bx57,y:by57,radius:finalR,color:"#f472b6",glow:"#fce7f3",lifetime,
        spawnedAt:Date.now()+anticipateMs,born:performance.now(),anticipateMs,
        moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
        worldId:cfg.world,worldColor:cfg.worldColor,bossPattern:null,bossPhase:1,cx:bx57,cy:by57,
        _partnerId:mainId,_primed:false});
    }
    // Spawn crystal_wing_r partner for crystal_wing_l at mirrored x
    if(type==="crystal_wing_l"){
      const cwCvs=canvasRef.current;const cwW=cwCvs?.width||360;const cwH=cwCvs?.height||640;
      const rightId81=Math.random().toString(36).slice(2);
      const rx81=Math.max(finalR+10,Math.min(cwW-finalR-10,cwW-pos.x));
      const ry81=Math.max(finalR+100,Math.min(cwH-finalR-10,pos.y));
      const cwMain81=targetsRef.current[targetsRef.current.length-1];
      cwMain81._cwPartner81=rightId81;cwMain81._cwTapTime81=null;
      targetsRef.current.push({id:rightId81,type:"crystal_wing_r",rarity:null,
        x:rx81,y:ry81,radius:finalR,color:"#818cf8",glow:"#e0e7ff",lifetime,
        spawnedAt:Date.now()+anticipateMs,born:performance.now(),anticipateMs,
        moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
        worldId:cfg.world,worldColor:cfg.worldColor,bossPattern:null,bossPhase:1,cx:rx81,cy:ry81,
        _cwPartner81:mainId,_cwTapTime81:null});
    }
    // Spawn shadow clone for shadow_twin immediately at mirrored position
    if(type==="shadow_twin"){
      const cw3=canvasRef.current?.width||360;const ch3=canvasRef.current?.height||640;
      const sx=Math.max(40,Math.min(cw3-40,cw3-pos.x));const sy=Math.max(40,Math.min(ch3-40,pos.y+30));
      targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"shadow_clone2",
        rarity:null,x:sx,y:sy,radius:finalR*0.92,color:"#6b7280",glow:"#374151",lifetime,
        spawnedAt:Date.now()+anticipateMs,born:performance.now(),anticipateMs,
        moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
        worldId:cfg.world,worldColor:cfg.worldColor,bossPattern:null,bossPhase:1,cx:sx,cy:sy,
        _realId:mainId});
    }
    // Spawn quantum_tunnel_b partner immediately at opposite side
    if(type==="quantum_tunnel_a"){
      const cw5=canvasRef.current?.width||360;const ch5=canvasRef.current?.height||640;
      const bx=Math.max(50,Math.min(cw5-50,cw5-pos.x));const by=Math.max(50,Math.min(ch5-50,ch5-pos.y));
      const partnerId=Math.random().toString(36).slice(2);
      const main=targetsRef.current[targetsRef.current.length-1];
      main._partnerId=partnerId;
      targetsRef.current.push({id:partnerId,type:"quantum_tunnel_b",rarity:null,
        x:bx,y:by,radius:finalR,color:"#f472b6",glow:"#fce7f3",lifetime,
        spawnedAt:Date.now()+anticipateMs,born:performance.now(),anticipateMs,
        moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
        worldId:cfg.world,worldColor:cfg.worldColor,bossPattern:null,bossPhase:1,cx:bx,cy:by,
        _partnerId:mainId,_lastTeleportQT:Date.now()});
    }
    // Spawn twin partner immediately
    if(type==="twin"&&pendingTwin){
      targetsRef.current.push({
        id:pendingTwin.id,type:"twin",rarity:null,
        x:pendingTwin.pos.x,y:pendingTwin.pos.y,radius:baseR,color,glow,lifetime,
        spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
        moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
        worldId:cfg.world,worldColor:cfg.worldColor,bossPattern:null,bossPhase:1,
        cx:pendingTwin.pos.x,cy:pendingTwin.pos.y,twinGroupId:mainId,
      });
    }
    // Spawn mirror_decoy partner for mirror_flash
    if(type==="mirror_flash"){
      const cw68=canvasRef.current?.width||360;const ch68=canvasRef.current?.height||640;
      const flashRef=targetsRef.current[targetsRef.current.length-1];
      if(flashRef){
        const decoyId68=Math.random().toString(36).slice(2);
        const dx68=Math.max(35,Math.min(cw68-35,pos.x+(Math.random()<0.5?90:-90)));
        const dy68=Math.max(35,Math.min(ch68-35,pos.y+(Math.random()<0.5?50:-50)));
        flashRef._decoyId=decoyId68;
        targetsRef.current.push({id:decoyId68,type:"mirror_decoy",rarity:null,
          x:dx68,y:dy68,radius:finalR,color:"#6366f1",glow:"#e0e7ff",lifetime,
          spawnedAt:Date.now()+anticipateMs,born:performance.now(),anticipateMs,
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
          worldId:cfg.world,worldColor:cfg.worldColor,bossPattern:null,bossPhase:1,
          cx:dx68,cy:dy68,_realId:mainId});
      }
    }
  },[sfx,pickPos]);

  // Level complete / game over
  const endLevel=useCallback((won)=>{
    const gs=gsRef.current;if(!gs)return;
    if(rafRef.current){cancelAnimationFrame(rafRef.current);rafRef.current=null;}
    if(luckyTimer.current){clearTimeout(luckyTimer.current);luckyTimer.current=null;}
    if(bountyTimerRef.current){clearTimeout(bountyTimerRef.current);bountyTimerRef.current=null;}
    bountyRef.current=null;setBountyData(null);
    luckyRef.current=null;setLuckyMode(false);
    streakShRef.current=false;setStreakShieldActive(false);
    audioRef.current?.stopBgMusic?.();
    const cfg=levelCfgRef.current;
    // SCORE_MIRROR payout — pay out ghost bank before freeze
    {const smPwr=activePwrRef.current.find(p=>p.type==="SCORE_MIRROR");
    if(smPwr&&(smPwr.mirrorBank||0)>0){
      gs.score+=smPwr.mirrorBank;
      spawnPopup(180,200,`🪞 MIRROR PAYOUT +${smPwr.mirrorBank}`,"#c084fc",22);
      if(smPwr.mirrorBank>=500)unlock("score_mirror_big");
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_MIRROR");
    }}
    const sv=saveRef.current,score=gs.score;
    const timeSurvived=Math.floor((Date.now()-gs.startTime)/1000);
    updateMissions({...gs.sessionStats,timeSurvived,score,levelsCompleted:(missionProg.current.levelsCompleted||0)+(won?1:0)});

    if(won){
      sfx("levelComplete");vibrate([20,30,20,30,60]);
      // Stars
      const stars=score>=cfg.scoreGoal*3?3:score>=cfg.scoreGoal*1.8?2:1;
      const prev=sv.levelStars[cfg.id]||0;
      const newStars=Math.max(prev,stars);
      sv.levelStars={...sv.levelStars,[cfg.id]:newStars};
      // Unlock next
      if(cfg.id>=sv.unlockedLevel) sv.unlockedLevel=Math.min(100,cfg.id+1);
      // 11B — Boss incoming notification on level X9 (next level is a boss)
      if(cfg.id%10===9){const nextLvl=cfg.id+1;const bossWorld=WORLDS[Math.floor((nextLvl-1)/10)];setTimeout(()=>setNotif(`⚠️ BOSS INCOMING on Level ${nextLvl}! ${bossWorld?.emoji||"👹"} Prepare yourself!`),1500);}
      // XP + coins
      const _xpBoostLvl=(sv.skills||{}).xp_boost||0;
      const _xpBoostMult=_xpBoostLvl===3?1.3:_xpBoostLvl===2?1.2:_xpBoostLvl===1?1.1:1.0;
      const _xpBombMult=gs._xpBombActive?2.0:1.0;
      const _challengeMult=cfg._challengeBonus?1.5:1.0;
      let xpEarned=Math.round((Math.floor(score/6)+gs.sessionStats.rareHits*8+gs.sessionStats.bossKills*30+(stars-1)*40)*_xpBoostMult*_xpBombMult*_challengeMult);
      if(gs._xpBombActive)setTimeout(()=>setNotif(`⚡ XP Bomb: ×2 XP! +${xpEarned}xp`),500);
      if(cfg._challengeBonus&&won)setTimeout(()=>setNotif(`⚡ Challenge Bonus: +50% XP!`),400);
      let coinsEarned=Math.floor(score*0.14)+gs.sessionStats.bossKills*20+stars*15;
      // Weekly Challenge bonus
      const wk=getWeekKey();
      if(cfg.id===getWeeklyChallengeLevel()&&!cfg.isInfinity&&!cfg.isGauntlet){
        const wasCompleted=sv.weeklyChallengeDate===wk&&sv.weeklyChallengeCompleted;
        sv.weeklyChallengeDate=wk;
        if(sv.weeklyChallengeDate!==wk||!sv.weeklyChallengeBest)sv.weeklyChallengeBest=0;
        if(score>(sv.weeklyChallengeBest||0))sv.weeklyChallengeBest=score;
        if(!wasCompleted){
          sv.weeklyChallengeCompleted=true;
          coinsEarned+=500;xpEarned+=200;
          unlock("weekly_done");
          setTimeout(()=>setNotif("🗓️ Weekly Challenge Cleared! +500🪙 +200XP"),700);
        }
      }
      // Daily Tournament — 2x coins, track best + rank, log history
      const todayKey=getTodayKey();
      if(cfg.id===getDailyChallengeLevel()&&!cfg.isInfinity&&!cfg.isGauntlet){
        if(sv.tournamentDate!==todayKey){
          // Push previous day to history if it exists
          if(sv.tournamentDate&&sv.tournamentBest>0){
            const prevCfg=getLevelConfig(cfg.id); // close enough; we don't store yesterday's level
            const prevRatio=sv.tournamentBest/(prevCfg.scoreGoal||1);
            const prevRank=prevRatio>=3?"Top 5%":prevRatio>=2.2?"Top 15%":prevRatio>=1.5?"Top 35%":prevRatio>=1?"Top 60%":"Bottom 40%";
            sv.tournamentHistory=[{date:sv.tournamentDate,score:sv.tournamentBest,rank:prevRank},...(sv.tournamentHistory||[])].slice(0,7);
          }
          sv.tournamentDate=todayKey;sv.tournamentBest=0;
        }
        if(score>(sv.tournamentBest||0))sv.tournamentBest=score;
        coinsEarned*=2; // 2× coin reward
        setTimeout(()=>setNotif(`🏆 Tournament! 2× coins: +${coinsEarned}🪙`),700);
        // Tournament Top achievement — Top 15% requires ratio >= 2.2
        if(score/(cfg.scoreGoal||1)>=2.2)unlock("tournament_top");
      }
      // 5× Score Goal OVERKILL bonus — legendary achievement, extra coins
      if(!cfg.isInfinity&&!cfg.isZen&&cfg.scoreGoal&&score>=cfg.scoreGoal*5){
        const overkillBonus=Math.floor(coinsEarned*0.75);
        coinsEarned+=overkillBonus;
        setTimeout(()=>setNotif(`🌟 OVERKILL × 5! +${overkillBonus}🪙 LEGENDARY!`),1000);
        unlock("overkill_5x");
      }
      // Day-of-week bonuses
      const dayOfWeek=new Date().getDay();
      if(dayOfWeek===5&&!cfg.isZen){ // Friday — double XP (Friday Fever)
        xpEarned=Math.ceil(xpEarned*2);
        setTimeout(()=>setNotif(`🔥 FRIDAY FEVER! 2× XP: +${xpEarned}xp`),900);
        unlock("friday_fever");
      }
      if((dayOfWeek===0||dayOfWeek===6)&&!cfg.isZen){ // Weekend — +50% coins (Weekend Warrior)
        const weekendBonus=Math.floor(coinsEarned*0.5);
        coinsEarned+=weekendBonus;
        setTimeout(()=>setNotif(`🎉 WEEKEND WARRIOR! +${weekendBonus}🪙 bonus!`),900);
        unlock("weekend_warrior");
      }
      const prevLvl=getLvl(sv.xp);sv.xp+=xpEarned;
      if(getLvl(sv.xp)>prevLvl){
        const gained=getLvl(sv.xp)-prevLvl;
        sv.skillPoints=(sv.skillPoints||0)+gained;
        sfx("levelUp");setNotif(`Level Up! Lv ${getLvl(sv.xp)} 🎉  +${gained} Skill Point${gained>1?"s":""}!`);
      }
      sv.coins=(sv.coins||0)+coinsEarned;sv.totalCoins=(sv.totalCoins||0)+coinsEarned;
      if(score>sv.highScore)sv.highScore=score;
      // Per-world best score tracking — 12B
      if(!cfg.isInfinity&&!cfg.isZen&&cfg.world){
        const wb=sv.worldBest||{};
        if(score>(wb[cfg.world]||0))wb[cfg.world]=score;
        sv.worldBest=wb;
      }
      // Timestamped score entry — normalise legacy plain-number entries on read
      sv.scores=[{score,date:getTodayKey(),week:getWeekKey()},...(sv.scores||[]).map(e=>typeof e==="number"?{score:e,date:"2000-0-0",week:"2000-W0"}:e)].slice(0,50);
      if(gs.streak>sv.bestStreak)sv.bestStreak=gs.streak;
      if(stars===3&&cfg.isBoss)unlock("five_star");
      if(cfg.id>=10)unlock("level_10");if(cfg.id>=25)unlock("level_25");if(cfg.id>=50)unlock("level_50");if(cfg.id>=100)unlock("level_100");
      if(stars>=1)unlock("three_stars");
      // three_stars_5: count distinct levels with 3 stars
      const tsCount=Object.values(sv.levelStars||{}).filter(n=>n>=3).length;
      if(tsCount>=5)unlock("three_stars_5");
      // Infinity best update
      if(cfg.isInfinity){
        if(score>(sv.infinityBest||0))sv.infinityBest=score;
        sv.infinityScores=[score,...(sv.infinityScores||[])].slice(0,10).sort((a,b)=>b-a);
      }
      // Zen mode best update
      if(cfg.isZen){
        if(score>(sv.zenBest||0)){sv.zenBest=score;sv.zenBestWorld=cfg.world;}
        if(cfg.isTimeAttack&&score>(sv.taBest||0)){sv.taBest=score;sv.taBestWorld=cfg.world;}
        coinsEarned+=Math.floor(score*0.08); // bonus coins for zen
        unlock("zen_master");
      }
      // Phase 10A achievement checks
      if(gs.streak>=50||sv.bestStreak>=50)unlock("streak_50");
      if((sv.totalCoins||0)>=500)unlock("coins_500");
      // Track world visits — all_worlds achievement
      const visitedWorlds=new Set(Object.keys(sv.levelStars||{}).map(id=>getLevelConfig(Number(id)).world));
      if(visitedWorlds.size>=10)unlock("all_worlds");
      // World completion — all 10 levels in a world with at least 1 star
      const worldId=cfg.world;
      const worldLevels=Array.from({length:10},(_,i)=>((worldId-1)*10+i+1));
      const worldDone=worldLevels.every(lid=>(sv.levelStars[lid]||0)>=1);
      if(worldDone){unlock(`world_${worldId}_complete`);unlock("world_complete");}
      // Perfect world — all 10 levels with 3 stars
      const worldPerfect=worldLevels.every(lid=>(sv.levelStars[lid]||0)>=3);
      if(worldPerfect)unlock(`world_${worldId}_perfect`);
      // Perfect run bonus — no misses AND lives unchanged → triple coins
      const missedCount=gs.sessionStats.missedTargets||0;
      const startLives=cfg.lives+(gs._extraLifeUsed?-1:0);
      const isPerfectRun=missedCount===0&&gs.lives>=startLives&&(gs.sessionStats.tapsTotal||0)>=5&&!cfg.isInfinity;
      if(isPerfectRun&&!cfg.isZen){
        const perfectBonus=Math.floor(coinsEarned*2);
        coinsEarned+=perfectBonus;
        sv.coins=(sv.coins||0)+perfectBonus;
        sv.totalCoins=(sv.totalCoins||0)+perfectBonus;
        sfx("flawless");
        setTimeout(()=>setNotif(`🎯 PERFECT LEVEL! +${perfectBonus}🪙 bonus!`),800);
      }
      // Speed bonus — finish a level quickly relative to expected time
      if(!cfg.isInfinity&&!cfg.isZen&&!cfg.isGauntlet&&won){
        const expectedTime=Math.max(15,(cfg.spawnInterval||800)*6/1000); // rough expected seconds
        if(timeSurvived>0&&timeSurvived<expectedTime*0.7){ // finished in top 70% of expected time
          const speedBonus=Math.floor(coinsEarned*0.3);
          if(speedBonus>0){
            coinsEarned+=speedBonus;
            setTimeout(()=>setNotif(`⚡ SPEED BONUS! +${speedBonus}🪙`),1200);
            unlock("speed_demon");
          }
        }
      }
      flushSave();
      const canPrestige=cfg.id===100&&(sv.prestigeLevel||0)<5;
      setLevelCompleteData({score,stars,newStars,xpEarned,coinsEarned,levelId:cfg.id,isLast:cfg.isLast,
        bestStreak:sv.bestStreak,sessionStats:{...gs.sessionStats,timeSurvived},canPrestige,isPerfectRun});
      setScrollToLevel(cfg.id);
      setMascotMood("victory");setMascotDancing(true);
      // Check mascot unlocks AFTER save is flushed
      const newMascots=checkMascotUnlocks();
      if(newMascots.length>0)setMascotUnlockedData(newMascots[0]);
      go("levelcomplete");
    } else {
      const pct=score/cfg.scoreGoal;
      const isNearMiss=pct>=0.82&&pct<1;
      if(isNearMiss)sfx("nearMiss"); else sfx("gameOver");
      flushSave();
      // Rescue gamble — offer on near-miss if player has enough coins
      if(isNearMiss&&sv.coins>=50){
        setRescueSecondsLeft(6);
      }
      setGameOverData({score,levelId:cfg.id,levelName:cfg.name,scoreGoal:cfg.scoreGoal,
        sessionStats:{...gs.sessionStats,timeSurvived},
        nearMiss:isNearMiss,shortfall:Math.max(0,cfg.scoreGoal-score)});
      go("gameover");
    }
    gsRef.current=null;targetsRef.current=[];particlesRef.current=[];activePwrRef.current=[];
    setActivePwrDisp([]);setFeverBorder(false);setComboLabel("");setPaused(false);pausedRef.current=false;
  },[sfx,flushSave,updateMissions,unlock]);

  // Tap handler
  const handleTap=useCallback((e)=>{
    e.preventDefault();
    const gs=gsRef.current;if(!gs||gs.lives<=0||pausedRef.current)return;
    const rect=e.currentTarget.getBoundingClientRect();
    const cx=e.touches?e.touches[0].clientX:e.clientX;
    const cy=e.touches?e.touches[0].clientY:e.clientY;
    const tx=cx-rect.left,ty=cy-rect.top;
    let hit=null,hitDist=Infinity;
    for(const t of targetsRef.current){
      if(t.ghost&&Math.sin(performance.now()/200)>0.25)continue;
      const d=Math.hypot(t.x-tx,t.y-ty);
      // Frozen targets have 70% smaller effective hit zone (precision challenge)
      const effectiveR=t.type==="frozen"?t.radius*0.70*1.35:t.type==="bubble"?t.radius*1.8:t.radius*1.35;
      if(d<effectiveR&&d<hitDist){hit=t;hitDist=d;}
    }
    ripplesRef.current.push({x:tx,y:ty,r:12,alpha:0.7,color:hit?(hit.color||"#a78bfa"):"#ffffff44"});
    if(!hit)return;

    // MAGNET — collect all targets currently in range (area-of-effect)
    if(hit.type==="magnet"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const MAGNET_R=100;
      const pulled=targetsRef.current.filter(t=>!t.dying&&t.type==="normal"&&Math.hypot(t.x-hit.x,t.y-hit.y)<MAGNET_R);
      let totalPts=0;
      pulled.forEach(t=>{
        const combo=Math.min(10,1+Math.floor(gs.streak/5));
        const feverMult=gs.feverActive?2:1;
        const ptsMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
        const rarityMult=t.rarity?.mult||1;
        const tpts=Math.round(rarityMult*combo*feverMult*ptsMult);
        totalPts+=tpts;
        t.dying=performance.now();
        spawnParticles(t.x,t.y,"#ec4899",10,"spark");
      });
      const basePts=Math.round(150*(gs.feverActive?2:1));
      gs.score+=basePts+totalPts;
      gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("powerUp");vibrate([15,8,30]);
      spawnParticles(hit.x,hit.y,"#ec4899",24,"spark");
      spawnParticles(hit.x,hit.y,"#fce7f3",10,"dot");
      spawnPopup(hit.x,hit.y-26,`🧲 PULLED ${pulled.length}! +${basePts+totalPts}`,"#ec4899",19);
      if(pulled.length>=2)setEpicFlash(true),setTimeout(()=>setEpicFlash(false),600);
      sfx("comboNote",gs.streak);
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // SHIELDED — first tap destroys shield, second tap scores
    if(hit.type==="shielded"){
      if(hit.hitsLeft>1){
        // Shield absorbs hit — crack it
        hit.hitsLeft=1;
        sfx("nearMiss");vibrate([8,5,8]);
        spawnParticles(hit.x,hit.y,"#a5b4fc",16,"spark");
        spawnParticles(hit.x,hit.y,"#818cf8",6,"dot");
        spawnPopup(hit.x,hit.y-22,"🛡 SHIELD BREAK!","#a5b4fc",15);
        // Brief flash — shield cracked visual already handled by drawShielded(hitsLeft<2)
        return;
      }
      // Shield is broken — final tap scores
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(200*combo*feverMult*prestigeMult); // 2× bonus for persistence
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      gs.sessionStats.rareHits++;
      hit.dying=performance.now();
      const coinBonus=Math.max(2,Math.floor(pts*0.12));
      saveRef.current.coins=(saveRef.current.coins||0)+coinBonus;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+coinBonus;
      sfx("rare");vibrate([10,8,25]);
      spawnParticles(hit.x,hit.y,"#6366f1",30,"spark");
      spawnParticles(hit.x,hit.y,"#c7d2fe",12,"dot");
      spawnPopup(hit.x,hit.y-24,`🛡 CRACKED! +${pts}`,"#818cf8",20);
      setEpicFlash(true);setTimeout(()=>setEpicFlash(false),600);
      sfx("comboNote",gs.streak);
      unlock("shielded_hit");
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // RAINBOW — score based on current color tier when tapped
    if(hit.type==="rainbow"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const tier=getRainbowTier(performance.now()); // use current time for tier
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const isMultiplierR=activePwrRef.current.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
      const pts=Math.round(tier.mult*80*combo*feverMult*prestigeMult*(isMultiplierR?3:1));
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      if(tier.name!=="common")gs.sessionStats.rareHits++;
      hit.dying=performance.now();
      const tierIcons={common:"⚪",uncommon:"🟢",rare:"🔵",epic:"🟣",legendary:"🌟"};
      sfx(tier.name==="legendary"?"legendary":tier.name==="epic"?"epic":tier.name==="rare"?"rare":"tap");
      vibrate([12,8,12]);
      spawnParticles(hit.x,hit.y,tier.color,20+(tier.mult*3),"spark");
      spawnPopup(hit.x,hit.y-30,`${tierIcons[tier.name]||""} ${tier.name.toUpperCase()}! +${pts}`,"#ffffff",18);
      if(tier.name==="legendary"){setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),600);}
      else if(tier.name==="epic"){setEpicFlash(true);setTimeout(()=>setEpicFlash(false),400);}
      if(tier.name==="legendary")unlock("rainbow_legendary");
      unlock("rainbow_catch");mascotHappyRef.current++;
      const cfg3=levelCfgRef.current;
      if(cfg3){if(gs.score>=cfg3.scoreGoal&&(!cfg3.modifier||checkModGoal(cfg3.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // TWIN — tapping one scores both twins for bonus points
    if(hit.type==="twin"){
      const partner=targetsRef.current.find(t=>t!==hit&&t.type==="twin"&&t.twinGroupId===hit.twinGroupId&&!t.dying);
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const isMultiplierT=activePwrRef.current.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
      const basePts=120*combo*feverMult*prestigeMult*(isMultiplierT?3:1);
      let totalPts=Math.round(basePts);
      if(partner){
        partner.dying=performance.now();
        totalPts=Math.round(basePts*2.2); // bonus for finding both
        spawnParticles(partner.x,partner.y,"#f59e0b",15,"spark");
        spawnParticles(partner.x,partner.y,"#fbbf24",6,"dot");
        spawnPopup(partner.x,partner.y-22,"✦ TWIN!","#f59e0b",15);
      }
      gs.score+=totalPts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("coin");vibrate([10,8,10,8,20]);
      spawnParticles(hit.x,hit.y,"#f59e0b",20,"spark");
      spawnPopup(hit.x,hit.y-30,partner?`✦×2 +${totalPts}`:`✦ TWIN +${totalPts}`,"#fbbf24",18);
      if(partner)setEpicFlash(true),setTimeout(()=>setEpicFlash(false),400);
      unlock("twin_hit");mascotHappyRef.current++;
      const cfg2=levelCfgRef.current;
      if(cfg2){if(gs.score>=cfg2.scoreGoal&&(!cfg2.modifier||checkModGoal(cfg2.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // FROZEN — smaller hit zone but 3× points reward
    if(hit.type==="frozen"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const isMultiplierFz=activePwrRef.current.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
      const pts=Math.round(180*3*combo*feverMult*prestigeMult*(isMultiplierFz?3:1));
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      hit.dying=performance.now();
      sfx("rare");vibrate([15,8,15,8,25]);
      spawnParticles(hit.x,hit.y,"#bfdbfe",25,"spark");
      spawnParticles(hit.x,hit.y,"#60a5fa",10,"dot");
      spawnPopup(hit.x,hit.y-32,`❄️ FROZEN! ×3 +${pts}`,"#60a5fa",20);
      unlock("frozen_catch");mascotHappyRef.current++;
      const cfgFz=levelCfgRef.current;
      if(cfgFz){if(gs.score>=cfgFz.scoreGoal&&(!cfgFz.modifier||checkModGoal(cfgFz.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // NINJA — nearly invisible, materializes last 20%; enormous point reward
    if(hit.type==="ninja"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const timeLeftNinja=1-(Date.now()-hit.spawnedAt)/hit.lifetime;
      if(timeLeftNinja>0.2){
        // Tapped while still invisible — counts as a miss (can't game it)
        sfx("miss");vibrate(20);
        spawnPopup(hit.x,hit.y-22,"👁️ WHERE IS IT?","#9ca3af",14);
        return;
      }
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const isMultiplierNj=activePwrRef.current.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
      const pts=Math.round(600*combo*feverMult*prestigeMult*(isMultiplierNj?3:1));
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      hit.dying=performance.now();
      sfx("legendary");vibrate([20,8,20,8,40]);
      spawnParticles(hit.x,hit.y,"#9ca3af",30,"spark");spawnParticles(hit.x,hit.y,"#ffffff",12,"dot");
      spawnPopup(hit.x,hit.y-32,`🥷 NINJA! +${pts}`,"#e2e8f0",22);
      setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),700);
      unlock("ninja_catch");mascotHappyRef.current++;
      const cfgNj=levelCfgRef.current;
      if(cfgNj){if(gs.score>=cfgNj.scoreGoal&&(!cfgNj.modifier||checkModGoal(cfgNj.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // BOUNCY — fast-moving elastic ball, 1.8× points
    if(hit.type==="bouncy"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const isMultiplierBo=activePwrRef.current.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
      const pts=Math.round(100*1.8*combo*feverMult*prestigeMult*(isMultiplierBo?3:1));
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      hit.dying=performance.now();
      sfx("rare");vibrate([12,6,12]);
      spawnParticles(hit.x,hit.y,"#fde047",18,"spark");
      spawnParticles(hit.x,hit.y,"#fbbf24",6,"dot");
      spawnPopup(hit.x,hit.y-28,`⚡ BOUNCY! +${pts}`,"#fde047",17);
      unlock("bouncy_catch");mascotHappyRef.current++;
      const cfgBo=levelCfgRef.current;
      if(cfgBo){if(gs.score>=cfgBo.scoreGoal&&(!cfgBo.modifier||checkModGoal(cfgBo.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // HEALER — restores 1 life when tapped
    if(hit.type==="healer"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const cfg=levelCfgRef.current;
      const maxLvs=cfg?cfg.lives:5;
      if(gs.lives<maxLvs){
        gs.lives++;
        spawnParticles(hit.x,hit.y,"#34d399",20,"spark");
        spawnParticles(hit.x,hit.y,"#6ee7b7",8,"dot");
        spawnPopup(hit.x,hit.y-30,"+1 ❤️ HEALED!","#34d399",20);
        sfx("coin");vibrate([12,6,12,6,24]);
        setEpicFlash(true);setTimeout(()=>setEpicFlash(false),400);
        unlock("healer_catch");
      } else {
        // Already full HP — convert to bonus coins
        const coinGift=20;
        saveRef.current.coins=(saveRef.current.coins||0)+coinGift;
        saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+coinGift;
        spawnPopup(hit.x,hit.y-28,`✨ Full HP! +${coinGift}🪙`,"#34d399",16);
        sfx("coin");
      }
      gs.streak++;gs.lastTapTime=Date.now();gs.sessionStats.tapsTotal++;mascotHappyRef.current++;
      return;
    }

    // VOLATILE — defuse for 250pts, or it explodes costing a life
    if(hit.type==="volatile"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const isMultiplierV=activePwrRef.current.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
      const pts=Math.round(250*combo*feverMult*prestigeMult*(isMultiplierV?3:1));
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("bossHit");vibrate([15,8,15]);
      spawnParticles(hit.x,hit.y,"#ff4500",20,"spark");
      spawnPopup(hit.x,hit.y-28,`💣 DEFUSED! +${pts}`,"#ff8c00",18);
      unlock("volatile_defuse");
      mascotHappyRef.current++;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // PHANTOM — ultra-short lifetime, huge score reward for catching it
    if(hit.type==="phantom"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const isMultiplier2=activePwrRef.current.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
      const pts=Math.round(500*combo*feverMult*prestigeMult*(isMultiplier2?3:1));
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      hit.dying=performance.now();
      sfx("legendary");vibrate([20,10,20,10,40]);
      spawnParticles(hit.x,hit.y,"#e879f9",30,"spark");
      spawnParticles(hit.x,hit.y,"#f0abfc",12,"dot");
      spawnPopup(hit.x,hit.y-32,`👻 PHANTOM! +${pts}`,"#e879f9",22);
      setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);
      unlock("phantom_catch");
      mascotHappyRef.current++;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // BUBBLE — large hit zone, pops with a satisfying burst; decent points
    if(hit.type==="bubble"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const isMultiplierBub=activePwrRef.current.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
      const pts=Math.round(60*combo*feverMult*prestigeMult*(isMultiplierBub?3:1));
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      hit.dying=performance.now();
      sfx("rare");vibrate(10);
      // Pop burst — rainbow iridescent particles
      const bubColors=["#f472b6","#67e8f9","#c084fc","#4ade80","#fbbf24"];
      bubColors.forEach((c,i)=>setTimeout(()=>spawnParticles(hit.x,hit.y,c,8,"dot"),i*20));
      spawnPopup(hit.x,hit.y-26,`🫧 POP! +${pts}`,"#67e8f9",16);
      unlock("bubble_pop");
      mascotHappyRef.current++;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // ECHO — scores points, then spawns a ghost echo at nearby position for bonus tap
    if(hit.type==="echo"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(90*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("comboNote",gs.streak);vibrate([10,5,15]);
      spawnParticles(hit.x,hit.y,"#a78bfa",10,"spark");
      spawnPopup(hit.x,hit.y-28,`◎ ECHO +${pts}`,"#c4b5fd",15);
      // Spawn ghost echo nearby — tap it for bonus
      const canvas=canvasRef.current;const cw=canvas?.width||390,ch=canvas?.height||700;
      const angle=Math.random()*Math.PI*2;
      const dist=45+Math.random()*60;
      const ghostX=Math.max(hit.radius+22,Math.min(cw-hit.radius-22,hit.x+Math.cos(angle)*dist));
      const ghostY=Math.max(hit.radius+100,Math.min(ch-hit.radius-22,hit.y+Math.sin(angle)*dist));
      const ghostId=Math.random().toString(36).slice(2);
      const ghostBorn=performance.now();
      targetsRef.current.push({
        id:ghostId,type:"echo_ghost",x:ghostX,y:ghostY,
        radius:hit.radius*0.9,color:"#c4b5fd",glow:"#7c3aed",
        rarity:hit.rarity,lifetime:1500,spawnedAt:Date.now(),born:ghostBorn,
        moving:false,ghost:false,vx:0,vy:0,trail:[],
        hitsLeft:1,maxHits:1,dying:null,_ghostBorn:ghostBorn,_echoPts:Math.round(pts*1.5),
      });
      mascotHappyRef.current++;
      unlock("echo_tap");
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ECHO GHOST — bonus tap after an echo
    if(hit.type==="echo_ghost"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round((hit._echoPts||135)*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("lucky");vibrate([10,5,10,5,20]);
      spawnParticles(hit.x,hit.y,"#e9d5ff",14,"spark");
      spawnParticles(hit.x,hit.y,"#7c3aed",6,"dot");
      spawnPopup(hit.x,hit.y-32,`✨ ECHO BONUS! +${pts}`,"#e9d5ff",18);
      unlock("echo_bonus");
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // POISON — tapping neutralizes it (small reward); missing applies poison debuff
    if(hit.type==="poison"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(50*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("comboNote",gs.streak);vibrate([8,4,8]);
      spawnParticles(hit.x,hit.y,"#4ade80",10,"spark");
      spawnPopup(hit.x,hit.y-24,`☣️ NEUTRALIZED +${pts}`,"#86efac",14);
      unlock("poison_tap");
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // GEMSTONE — rare prismatic gem; massive score reward
    if(hit.type==="gemstone"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(500*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      gs.sessionStats.rareHits++;
      sfx("legendary");vibrate([25,12,25,12,50]);
      const canvas=canvasRef.current;const cw=canvas?.width||390,ch=canvas?.height||700;
      const rainbowColors=["#ff6030","#ffd700","#34d399","#60a5fa","#f472b6","#a78bfa","#ffffff"];
      rainbowColors.forEach((c,i)=>setTimeout(()=>spawnParticles(hit.x,hit.y,c,10,"spark"),i*40));
      spawnPopup(hit.x,hit.y-40,`💎 GEMSTONE! +${pts}`,"#e879f9",22);
      setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),800);
      unlock("gemstone_tap");
      mascotHappyRef.current+=5;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // MORPH — score depends on which rarity phase it was caught in
    if(hit.type==="morph"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const cycleMs=1400;
      const elapsed=Date.now()-hit.spawnedAt;
      const phase=Math.floor((elapsed/cycleMs)%MORPH_CYCLE.length); // 0=common .. 4=legendary
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const basePts=[30,60,120,220,500][phase]; // scales with tier
      const pts=Math.round(basePts*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      const tier=MORPH_CYCLE[phase];
      if(phase>=3)gs.sessionStats.rareHits++;
      const flashLabels=["✨ COMMON","🌊 UNCOMMON","💜 RARE!","🌟 EPIC!!","🏆 LEGENDARY!!!"][phase];
      spawnParticles(hit.x,hit.y,tier.color,phase>=4?20:8+phase*3,"spark");
      spawnPopup(hit.x,hit.y-36,`${flashLabels} +${pts}`,tier.color,phase>=4?24:18);
      if(phase===4){sfx("legendary");setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),800);vibrate([20,10,20,10,40]);unlock("morph_legendary");}
      else if(phase>=3){sfx("epic");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),600);vibrate([15,8,15]);}
      else{sfx("tap");vibrate([8]);}
      unlock("morph_tap");
      mascotHappyRef.current+=phase>=4?5:1;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // ICE COMET — streaks across screen; freezes nearby targets when tapped
    if(hit.type==="icecomet"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(220*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      // Freeze nearby targets (80px radius) for 2s
      const FREEZE_R=80;
      targetsRef.current.forEach(t=>{
        if(t!==hit&&!t.dying&&t.type==="normal"&&Math.hypot(t.x-hit.x,t.y-hit.y)<FREEZE_R){
          const origVx=t.vx,origVy=t.vy;
          t.vx=0;t.vy=0;t.moving=false;t._frozen=true;
          spawnParticles(t.x,t.y,"#38bdf8",6,"spark");
          setTimeout(()=>{if(t._frozen){t.vx=origVx;t.vy=origVy;t.moving=origVx!==0||origVy!==0;t._frozen=false;}},2000);
        }
      });
      spawnParticles(hit.x,hit.y,"#38bdf8",20,"spark");
      spawnParticles(hit.x,hit.y,"#bae6fd",10,"dot");
      spawnPopup(hit.x,hit.y-40,`❄️ ICE COMET! +${pts}`,"#38bdf8",22);
      sfx("tap");vibrate([10,6,10]);
      unlock("icecomet_tap");
      mascotHappyRef.current+=2;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // PHOENIX — fiery bird; base pts; expires → phoenix2 (epic risen form)
    if(hit.type==="phoenix"||hit.type==="phoenix2"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const isRisen=hit.type==="phoenix2";
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round((isRisen?350:150)*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      if(isRisen)gs.sessionStats.rareHits++;
      const col=isRisen?"#c084fc":"#f97316";
      spawnParticles(hit.x,hit.y,col,isRisen?18:10,"spark");
      spawnPopup(hit.x,hit.y-38,isRisen?`🦋 RISEN! +${pts}`:`🔥 PHOENIX! +${pts}`,col,isRisen?22:18);
      if(isRisen){sfx("epic");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);vibrate([15,8,15]);}
      else sfx("tap");
      unlock("phoenix_tap");
      if(isRisen)unlock("phoenix_risen");
      mascotHappyRef.current+=isRisen?4:1;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // NEXUS — ultra-rare cosmic target; massive score + legendary flash
    if(hit.type==="nexus"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(2000*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak+=5; // instant +5 combo!
      gs.lastTapTime=Date.now();gs.sessionStats.tapsTotal++;
      gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      // Epic particle explosion
      const colors=["#ffd700","#ffffff","#60a5fa","#f472b6","#34d399","#a78bfa"];
      colors.forEach((c,i)=>{
        setTimeout(()=>{spawnParticles(hit.x,hit.y,c,16,"spark");},i*60);
      });
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#ffd700",size:hit.radius*2.5,born:performance.now(),duration:900,alpha:0.85});
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#ffffff",size:hit.radius*1.5,born:performance.now(),duration:600,alpha:0.6});
      spawnPopup(hit.x,hit.y-56,`🌟 NEXUS! +${pts}!`,"#ffd700",28);
      sfx("jackpot");setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),1200);
      setScreenShake(true);setTimeout(()=>setScreenShake(false),600);
      vibrate([30,15,30,15,60,15,30]);
      unlock("nexus_tap");
      mascotHappyRef.current+=10;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // MIRROR BALL — spawns 3 mini normal targets on tap; rainbow particles
    if(hit.type==="mirrorball"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(100*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      // Spawn 3 mini disco copies
      const canvas=canvasRef.current;const cw=canvas?.width||390,ch=canvas?.height||700;
      const minicols=["#ff6060","#60ff60","#6060ff"];
      for(let i=0;i<3;i++){
        const angle=(i/3)*Math.PI*2;
        const spx=Math.max(35,Math.min(cw-35,hit.x+Math.cos(angle)*55));
        const spy=Math.max(100,Math.min(ch-35,hit.y+Math.sin(angle)*55));
        targetsRef.current.push({
          id:Math.random().toString(36).slice(2),type:"normal",
          x:spx,y:spy,radius:BASE_R*0.65,color:minicols[i],glow:minicols[i],
          rarity:RARITY.RARE,lifetime:2200,spawnedAt:Date.now(),born:performance.now(),
          moving:false,ghost:false,vx:0,vy:0,trail:[],hitsLeft:1,maxHits:1,dying:null,_isMini:true
        });
      }
      // Rainbow particle burst in 6 directions
      for(let i=0;i<6;i++){
        const hue=i*60;spawnParticles(hit.x,hit.y,`hsl(${hue},100%,65%)`,8,"spark");
      }
      spawnPopup(hit.x,hit.y-40,`🪩 MIRROR BALL! +${pts}`,"#e2e8f0",20);
      sfx("chainBonus");vibrate([12,6,12,6,20]);
      unlock("mirrorball_tap");
      mascotHappyRef.current+=2;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // COMET — streaks across screen; tap mid-flight for bonus
    if(hit.type==="comet"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      // Early-flight bonus: more points if caught in first 50% of lifetime
      const lifeLeft=Math.max(0,1-(Date.now()-hit.spawnedAt)/hit.lifetime);
      const earlyBonus=lifeLeft>0.5?Math.round(lifeLeft*300):0;
      const pts=Math.round((300+earlyBonus)*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      // Gold comet burst
      ["#ffd700","#fde68a","#ffffff","#f59e0b"].forEach((c,i)=>
        setTimeout(()=>spawnParticles(hit.x,hit.y,c,8,"spark"),i*35));
      const label=earlyBonus>0?`🌠 COMET! +${pts} EARLY BONUS!`:`🌠 COMET! +${pts}`;
      spawnPopup(hit.x,hit.y-40,label,"#fde68a",earlyBonus>0?22:18);
      sfx(earlyBonus>0?"legendary":"epic");
      if(earlyBonus>0){setEpicFlash(true);setTimeout(()=>setEpicFlash(false),600);}
      vibrate([12,8,12,8,20]);
      unlock("comet_tap");
      if(earlyBonus>150)unlock("comet_early");
      mascotHappyRef.current+=earlyBonus>0?4:2;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // PRISM — splits score into 3 rainbow beam particle streams
    if(hit.type==="prism"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(280*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      // Spawn 3 rainbow beam particle waves
      const beamColors=["#ff6060","#60ff60","#6060ff"];
      beamColors.forEach((c,i)=>{
        const angle=-Math.PI/2+(i-1)*(Math.PI/5);
        for(let j=0;j<10;j++){
          particlesRef.current.push({
            type:"dot",x:hit.x,y:hit.y,
            vx:Math.cos(angle+j*0.15)*3,vy:Math.sin(angle+j*0.15)*3,
            color:c,alpha:1,scale:1,born:performance.now(),duration:700+j*40
          });
        }
      });
      spawnPopup(hit.x,hit.y-40,`🔮 PRISM! +${pts}`,"#f0abfc",22);
      sfx("epic");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),600);
      vibrate([15,8,15,8,25]);
      unlock("prism_tap");
      mascotHappyRef.current+=3;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // GLITCH — rewards based on how recently it teleported (mid-teleport = bonus)
    if(hit.type==="glitch"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      // Bonus if caught within 0.3s of last teleport (mid-glitch)
      const timeSinceGlitch=now-(hit._lastGlitch||0);
      const midGlitch=timeSinceGlitch<300;
      const pts=Math.round((midGlitch?400:150)*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      const col=midGlitch?"#00ff88":"#ffffff";
      spawnParticles(hit.x,hit.y,col,14,"spark");
      spawnPopup(hit.x,hit.y-36,midGlitch?`🟢 GLITCH PERFECT! +${pts}`:`⬜ GLITCH! +${pts}`,col,midGlitch?22:18);
      if(midGlitch){sfx("legendary");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);vibrate([15,8,15,8,30]);}
      else{sfx("tap");vibrate([8]);}
      unlock("glitch_tap");
      if(midGlitch)unlock("glitch_perfect");
      mascotHappyRef.current+=midGlitch?4:1;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // SIPHON — drains 1 life if not tapped; massive reward for catching it
    if(hit.type==="siphon"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      // Bonus scales with how much life was left (catching early = more points)
      const lifeLeft=Math.max(0,1-(Date.now()-hit.spawnedAt)/hit.lifetime);
      const urgencyBonus=Math.round(lifeLeft*400); // up to 400 bonus pts for catching early
      const pts=Math.round((600+urgencyBonus)*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      spawnParticles(hit.x,hit.y,"#ef4444",16,"spark");
      spawnParticles(hit.x,hit.y,"#fbbf24",8,"dot");
      spawnPopup(hit.x,hit.y-42,`⚠️ SIPHON BLOCKED! +${pts}`,"#ef4444",22);
      sfx("bossKill");vibrate([20,10,20,10,40]);
      setNotif("🛡 Siphon neutralized — danger averted!");
      unlock("siphon_tap");
      mascotHappyRef.current+=3;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // CONDUCTOR — boosts spawn rate for 5s, gives staccato combo notes
    if(hit.type==="conductor"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs._conductorActive=true;
      gs._conductorEndsAt=Date.now()+5000;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(180*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      spawnParticles(hit.x,hit.y,"#f97316",16,"spark");
      spawnPopup(hit.x,hit.y-38,`♪ CONDUCTOR! +${pts}`,"#f97316",20);
      sfx("chainBonus");vibrate([10,6,10,6,20]);
      setNotif("♪ Conductor! Spawn rate boosted for 5s!");
      unlock("conductor_tap");
      mascotHappyRef.current+=2;
      setTimeout(()=>{if(gsRef.current){gsRef.current._conductorActive=false;gsRef.current._conductorEndsAt=null;}},5000);
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // LUCKY CLOVER — random 1-10× score multiplier reveal on tap
    if(hit.type==="clover"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      // Weighted random: 1-3× common, 4-6× uncommon, 7-9× rare, 10× legendary
      const roll=Math.random();
      const mult=roll<0.45?1+Math.floor(Math.random()*3):roll<0.75?4+Math.floor(Math.random()*3):roll<0.92?7+Math.floor(Math.random()*3):10;
      const basePts=Math.round(80*mult*combo*feverMult*prestigeMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      const isJackpot=mult>=10;
      const isGreat=mult>=7;
      const cloverColors=["#22c55e","#4ade80","#86efac","#bbf7d0"];
      cloverColors.forEach((c,i)=>setTimeout(()=>spawnParticles(hit.x,hit.y,c,isJackpot?14:8,"spark"),i*40));
      if(isJackpot){
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#22c55e",size:hit.radius*3,born:performance.now(),duration:700,alpha:0.9});
        spawnPopup(hit.x,hit.y-50,`🍀 JACKPOT! ×${mult} = +${basePts}`,"#22c55e",26);
        sfx("jackpot");setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),800);
        setScreenShake(true);setTimeout(()=>setScreenShake(false),400);
        vibrate([25,10,25,10,50]);
      } else if(isGreat){
        spawnPopup(hit.x,hit.y-42,`🍀 LUCKY ×${mult}! +${basePts}`,"#4ade80",22);
        sfx("epic");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);
        vibrate([15,8,20]);
      } else {
        spawnPopup(hit.x,hit.y-36,`🍀 ×${mult}! +${basePts}`,"#22c55e",18);
        sfx("rare");vibrate([10,5,10]);
      }
      unlock("clover_tap");
      if(mult>=10)unlock("clover_jackpot");
      mascotHappyRef.current+=mult>=7?4:1;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // RICOCHET — kills 2 nearest normal targets in addition to base score
    if(hit.type==="ricochet"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const basePts=Math.round(100*combo*feverMult*prestigeMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      spawnParticles(hit.x,hit.y,"#cbd5e1",12,"spark");
      // Find 2 nearest normal targets and ricochet to them
      const ricTargets=targetsRef.current
        .filter(t=>!t.dying&&(t.type==="normal"||t.type==="phantom"))
        .sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y))
        .slice(0,2);
      let ricPts=basePts;
      ricTargets.forEach((rt,i)=>{
        setTimeout(()=>{
          if(!rt.dying){
            rt.dying=performance.now();
            const rpts=Math.round(60*combo*feverMult*prestigeMult);
            gs.score+=rpts;ricPts+=rpts;gs.sessionStats.score=gs.score;
            // Draw arc line from hit to target
            particlesRef.current.push({type:"arc",x:hit.x,y:hit.y,x2:rt.x,y2:rt.y,
              color:"#e2e8f0",alpha:1,born:performance.now(),duration:300});
            spawnParticles(rt.x,rt.y,"#94a3b8",8,"spark");
            spawnPopup(rt.x,rt.y-24,`🎯 RICO! +${rpts}`,"#e2e8f0",13);
            sfx("comboNote",gs.streak+i);
          }
        },i*100);
      });
      spawnPopup(hit.x,hit.y-36,`🎯 RICOCHET! +${basePts}`,"#e2e8f0",20);
      sfx("chainBonus");vibrate([12,6,12,6,18]);
      if(ricTargets.length>0)setNotif(`🎯 Ricochet! Killed ${ricTargets.length} nearby!`);
      unlock("ricochet_tap");
      if(ricTargets.length>=2)unlock("ricochet_double");
      mascotHappyRef.current+=1+ricTargets.length;
      const cfgR=levelCfgRef.current;
      if(cfgR){if(gs.score>=cfgR.scoreGoal&&(!cfgR.modifier||checkModGoal(cfgR.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // AURORA — scores based on tap rhythm consistency (taps at even intervals = bonus)
    if(hit.type==="aurora"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      // Rhythm bonus: consistency of last 3 inter-tap intervals
      const times=gs._recentTaps||[];
      let rhythmMult=1;
      if(times.length>=3){
        const intervals=[];
        for(let i=1;i<Math.min(times.length,4);i++)intervals.push(times[i]-times[i-1]);
        if(intervals.length>=2){
          const avg=intervals.reduce((a,b)=>a+b,0)/intervals.length;
          const variance=intervals.reduce((a,b)=>a+(b-avg)**2,0)/intervals.length;
          const cv=Math.sqrt(variance)/Math.max(avg,1); // coefficient of variation (0=perfect)
          rhythmMult=cv<0.1?5:cv<0.2?4:cv<0.35?3:cv<0.5?2:1;
        }
      }
      const pts=Math.round(160*rhythmMult*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      const rhythmColors=["#94a3b8","#34d399","#60a5fa","#c084fc","#ffd700"];
      const rc=rhythmColors[rhythmMult-1]||"#38bdf8";
      spawnParticles(hit.x,hit.y,rc,12+(rhythmMult*3),"spark");
      const rhythmLabels=["GOOD","GOOD","NICE","GREAT","PERFECT"];
      spawnPopup(hit.x,hit.y-44,`🌌 AURORA ×${rhythmMult} ${rhythmLabels[rhythmMult-1]}! +${pts}`,rc,rhythmMult>=4?24:20);
      if(rhythmMult>=4){sfx("legendary");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);}
      else{sfx("epic");}
      vibrate(rhythmMult>=4?[20,8,20,8,35]:[12,6,16]);
      unlock("aurora_tap");
      if(rhythmMult>=5)unlock("aurora_perfect");
      mascotHappyRef.current+=rhythmMult;
      const cfgA=levelCfgRef.current;
      if(cfgA){if(gs.score>=cfgA.scoreGoal&&(!cfgA.modifier||checkModGoal(cfgA.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // SPECTRAL — phasing ghost; massive pts if caught while visible
    if(hit.type==="spectral"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      // Determine visibility at moment of tap
      const cycle=1600;const phase=(performance.now()%cycle)/cycle;
      const vis=phase<0.5?phase*2:2-phase*2;
      const isVisible=vis>0.4; // must be in visible half of cycle for bonus
      const basePts=Math.round((isVisible?800:120)*combo*feverMult*prestigeMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      if(isVisible)gs.sessionStats.rareHits++;
      const col=isVisible?"#fbbf24":"#a5b4fc";
      spawnParticles(hit.x,hit.y,col,isVisible?20:8,"spark");
      spawnPopup(hit.x,hit.y-44,isVisible?`👻 SPECTRAL PERFECT! +${basePts}`:`👻 SPECTRAL +${basePts}`,col,isVisible?26:18);
      if(isVisible){sfx("legendary");setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),800);vibrate([20,10,20,10,50]);}
      else{sfx("rare");vibrate([10,5,10]);}
      unlock("spectral_tap");
      if(isVisible)unlock("spectral_perfect");
      mascotHappyRef.current+=isVisible?6:1;
      const cfgSp=levelCfgRef.current;
      if(cfgSp){if(gs.score>=cfgSp.scoreGoal&&(!cfgSp.modifier||checkModGoal(cfgSp.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // HEART — gives +1 life + coin bonus when tapped
    if(hit.type==="heart"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const maxLives=(levelCfgRef.current?.lives||3)+2;
      const lifeGained=gs.lives<maxLives?1:0;
      const coinBonus=15+Math.floor(Math.random()*10);
      const pts=Math.round(80*combo*feverMult*prestigeMult);
      gs.score+=pts;if(lifeGained>0)gs.lives++;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      saveRef.current.coins=(saveRef.current.coins||0)+coinBonus;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+coinBonus;
      debounceSave();
      spawnParticles(hit.x,hit.y,"#f43f5e",14,"spark");
      if(lifeGained>0){
        spawnPopup(hit.x,hit.y-44,`❤️ +1 LIFE! +${coinBonus}🪙 +${pts}`,"#f43f5e",22);
        sfx("starEarn");setScreenShake(false);vibrate([15,8,15,8,30]);
      } else {
        spawnPopup(hit.x,hit.y-36,`💕 LOVE! +${coinBonus}🪙 +${pts}`,"#f43f5e",20);
        sfx("rare");vibrate([12,6,12]);
      }
      unlock("heart_tap");
      mascotHappyRef.current+=3;
      const cfgHt=levelCfgRef.current;
      if(cfgHt){if(gs.score>=cfgHt.scoreGoal&&(!cfgHt.modifier||checkModGoal(cfgHt.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // NOVA — freezes all targets for 2s + good base score
    if(hit.type==="nova"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(200*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      const canvas=canvasRef.current;const cw=canvas?.width||390,ch=canvas?.height||700;
      // Nova freeze — store frozen state on targets + use FREEZE power-up
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="FREEZE");
      activePwrRef.current.push({type:"FREEZE",endsAt:Date.now()+2000});
      setActivePwrDisp([...activePwrRef.current]);
      // Freeze all existing targets
      targetsRef.current.forEach(t=>{
        if(!t.dying&&t.type!=="boss"){
          const ov=t.vx,oY=t.vy;t.vx=0;t.vy=0;t.moving=false;t._frozenByNova=true;
          setTimeout(()=>{if(t._frozenByNova){t.vx=ov;t.vy=oY;t.moving=ov!==0||oY!==0;t._frozenByNova=false;}},2000);
        }
      });
      // Supernova particle explosion
      const blastColors=["#fde68a","#fbbf24","#ffffff","#f59e0b","#fed7aa"];
      blastColors.forEach((c,i)=>setTimeout(()=>{
        spawnParticles(cw/2,ch/2,c,10,"spark");
      },i*60));
      particlesRef.current.push({type:"shockwave",x:cw/2,y:ch/2,color:"#fbbf24",size:Math.max(cw,ch)*0.5,born:performance.now(),duration:900,alpha:0.7});
      spawnPopup(hit.x,hit.y-50,`💫 NOVA! ALL FROZEN! +${pts}`,"#fbbf24",24);
      sfx("legendary");setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),800);
      setScreenShake(true);setTimeout(()=>setScreenShake(false),500);
      vibrate([25,10,25,10,60]);
      setNotif("💫 NOVA BLAST! All targets frozen for 2s!");
      unlock("nova_tap");
      mascotHappyRef.current+=5;
      const cfgNv=levelCfgRef.current;
      if(cfgNv){if(gs.score>=cfgNv.scoreGoal&&(!cfgNv.modifier||checkModGoal(cfgNv.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // PINWHEEL — spin position determines score (top = jackpot)
    if(hit.type==="pinwheel"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // Score based on how close to "top" the pinwheel was (angle near -PI/2)
      const orbitAngle=hit._orbitAngle||0;
      const topAngle=-Math.PI/2;
      const angleDiff=Math.abs(((orbitAngle-topAngle)%(Math.PI*2)+Math.PI*3)%(Math.PI*2)-Math.PI);
      const topPct=1-(angleDiff/Math.PI); // 1.0 = perfect top, 0 = bottom
      const baseScore=Math.round(200+topPct*1000); // 200→1200 pts
      const pts=Math.round(baseScore*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const isJackpot=topPct>0.8;
      const col=isJackpot?"#fbbf24":"#f97316";
      spawnPopup(hit.x,hit.y-30,(isJackpot?`🌀 JACKPOT! +${pts}`:`🌀 Pinwheel! +${pts}`),col,isJackpot?22:16);
      spawnParticles(hit.x,hit.y,col,isJackpot?18:10,"spark");
      if(isJackpot){sfx("lucky");vibrate([12,6,18]);}else{sfx("tap");vibrate(8);}
      unlock("pinwheel_tap");
      if(isJackpot)unlock("pinwheel_jackpot");
      mascotHappyRef.current+=2;
      updateMissions(gs.sessionStats);
      const cfgPW=levelCfgRef.current;
      if(cfgPW){if(gs.score>=cfgPW.scoreGoal&&(!cfgPW.modifier||checkModGoal(cfgPW.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SUPERCELL — electrifies nearby targets; collect it for 500pts + bonus per electrified target
    if(hit.type==="supercell"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // Count how many targets are electrified (supercell boosted)
      const electrified=targetsRef.current.filter(t=>!t.dying&&t._supercellBoosted&&Date.now()<t._supercellBoosted).length;
      const pts=Math.round((500+electrified*120)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-34,`⛈️ STORM! +${pts} (${electrified} charged)`,"#38bdf8",20);
      spawnParticles(hit.x,hit.y,"#7dd3fc",18,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#38bdf8",size:220,born:performance.now(),duration:700,alpha:0.6});
      sfx(electrified>=3?"legendary":"bossKill");vibrate([12,8,16]);
      unlock("supercell_tap");
      if(electrified>=3)unlock("supercell_storm");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgSC=levelCfgRef.current;
      if(cfgSC){if(gs.score>=cfgSC.scoreGoal&&(!cfgSC.modifier||checkModGoal(cfgSC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PRISM BOMB — sends 3 beams outward; each scores first target hit (200pts)
    if(hit.type==="prismbomb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const canvas=canvasRef.current;const cw7=canvas?.width||390,ch7=canvas?.height||700;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      let beamHits=0;
      // 3 beams at 120° angles from tap position
      for(let b=0;b<3;b++){
        const ba=(b/3)*Math.PI*2+Math.PI/6;
        // Find first target along beam direction (within 60° cone, closest)
        const beamHit=targetsRef.current
          .filter(t=>!t.dying&&t.type!=="boss"&&t.type!=="bomb"&&t.type!=="prismbomb"&&t.type!=="icecage")
          .filter(t=>{
            const dx=t.x-hit.x,dy=t.y-hit.y;const dist=Math.hypot(dx,dy)||1;
            const angle=Math.atan2(dy,dx);const diff=Math.abs(((angle-ba)+Math.PI*3)%(Math.PI*2)-Math.PI);
            return diff<Math.PI/3&&dist<280;
          })
          .sort((a,b2)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b2.x-hit.x,b2.y-hit.y))[0];
        if(beamHit){
          beamHit.dying=performance.now();beamHits++;
          const bpts=Math.round(200*combo*feverMult);gs.score+=bpts;
          spawnParticles(beamHit.x,beamHit.y,"#f0abfc",10,"spark");
          spawnPopup(beamHit.x,beamHit.y-20,`🌈 +${bpts}`,"#c084fc",14);
        }
        // Draw beam line as particles
        for(let d=20;d<260;d+=22){
          particlesRef.current.push({type:"dot",x:hit.x+Math.cos(ba)*d,y:hit.y+Math.sin(ba)*d,
            color:`hsl(${(b*120+d*0.5)%360},100%,70%)`,alpha:0.8,size:3,vx:0,vy:0,born:performance.now(),duration:400});
        }
      }
      const basePts=Math.round(300*combo*feverMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-34,`🌈 PRISM! ${beamHits} beam hits! +${basePts}`,"#f0abfc",20);
      spawnParticles(hit.x,hit.y,"#f0abfc",16,"spark");
      sfx(beamHits>=3?"legendary":"chainBonus");vibrate([10,6,14]);
      unlock("prismbomb_tap");
      if(beamHits>=3)unlock("prismbomb_triple");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgPB=levelCfgRef.current;
      if(cfgPB){if(gs.score>=cfgPB.scoreGoal&&(!cfgPB.modifier||checkModGoal(cfgPB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ICE CAGE — freezes nearby targets for 4s; tap to auto-collect all when frozen
    if(hit.type==="icecage"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const CAGE_R=140;
      const frozen=targetsRef.current.filter(t=>!t.dying&&t.type!=="boss"&&t.type!=="bomb"&&Math.hypot(t.x-hit.x,t.y-hit.y)<CAGE_R);
      // Freeze all nearby targets
      frozen.forEach(t=>{t._frozenUntil=Date.now()+4000;t._anchorFrozenVx=t.vx;t._anchorFrozenVy=t.vy;t.vx=0;t.vy=0;});
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const cageBonus=frozen.length*120;
      const basePts=Math.round((400+cageBonus)*combo*feverMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#7dd3fc",size:CAGE_R*1.3,born:performance.now(),duration:700,alpha:0.7});
      spawnPopup(hit.x,hit.y-34,`🧊 CAGE! ${frozen.length} frozen! +${basePts}`,"#7dd3fc",20);
      spawnParticles(hit.x,hit.y,"#bae6fd",18,"spark");
      sfx(frozen.length>=3?"legendary":"bossKill");vibrate([12,8,16]);
      unlock("icecage_tap");
      if(frozen.length>=3)unlock("icecage_full");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgIC=levelCfgRef.current;
      if(cfgIC){if(gs.score>=cfgIC.scoreGoal&&(!cfgIC.modifier||checkModGoal(cfgIC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SOLAR FLARE — smaller = more points (1200→120 as it grows)
    if(hit.type==="solarflare"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const growPct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime*0.9));
      const flareBase=Math.round(1200*(1-growPct*0.9)+120); // 1200 small → 132 big
      const pts=Math.round(flareBase*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const isEarlyTap=growPct<0.25;
      const col=isEarlyTap?"#fde68a":"#f97316";
      spawnPopup(hit.x,hit.y-30,(isEarlyTap?`☀️ SOLAR BURST! +${pts}`:`☀️ Flare! +${pts}`),col,isEarlyTap?22:16);
      spawnParticles(hit.x,hit.y,col,isEarlyTap?20:10,"spark");
      if(isEarlyTap){sfx("legendary");vibrate([12,8,18]);setScreenShake(true);setTimeout(()=>setScreenShake(false),200);}
      else{sfx("tap");vibrate(8);}
      unlock("solarflare_tap");
      if(isEarlyTap)unlock("solarflare_early");
      mascotHappyRef.current+=2;
      updateMissions(gs.sessionStats);
      const cfgSF=levelCfgRef.current;
      if(cfgSF){if(gs.score>=cfgSF.scoreGoal&&(!cfgSF.modifier||checkModGoal(cfgSF.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MAGMA — defuse for 800pts before it destroys a target
    if(hit.type==="magma"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(800*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-32,`🌋 COOLED! +${pts}`,"#f97316",20);
      spawnParticles(hit.x,hit.y,"#f97316",16,"spark");spawnParticles(hit.x,hit.y,"#fbbf24",8,"dot");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#f97316",size:180,born:performance.now(),duration:600,alpha:0.5});
      sfx("bossKill");vibrate([12,8,16]);
      unlock("magma_tap");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgMg=levelCfgRef.current;
      if(cfgMg){if(gs.score>=cfgMg.scoreGoal&&(!cfgMg.modifier||checkModGoal(cfgMg.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CHAIN COLLECTOR — auto-grabs 3 nearest normal/special targets
    if(hit.type==="chaincollector"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // Find 3 nearest non-boss, non-bomb, non-mine targets
      const eligible=targetsRef.current
        .filter(t=>!t.dying&&t.type!=="boss"&&t.type!=="bomb"&&t.type!=="timebomb"&&t.type!=="chaincollector")
        .sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y))
        .slice(0,3);
      let chainScore=300;
      eligible.forEach((t,i)=>{
        setTimeout(()=>{
          const rMult=t.rarity?.mult||1;
          const tpts=Math.round(rMult*200*combo*feverMult);
          chainScore+=tpts;gs.score+=tpts;
          spawnParticles(t.x,t.y,"#34d399",10,"spark");
          spawnPopup(t.x,t.y-20,`⛓️ +${tpts}`,"#34d399",14);
          targetsRef.current=targetsRef.current.filter(q=>q.id!==t.id);
          setHud(h=>({...h,score:gsRef.current?.score||h.score}));
        },i*120);
      });
      const basePts=Math.round(300*combo*feverMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-34,`⛓️ CHAIN! ${eligible.length} caught!`,"#34d399",20);
      spawnParticles(hit.x,hit.y,"#6ee7b7",18,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#34d399",size:200,born:performance.now(),duration:600,alpha:0.5});
      sfx(eligible.length>=3?"bossKill":"chainBonus");vibrate([10,6,14]);
      unlock("chain_collect");
      if(eligible.length>=3)unlock("chain_triple");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgCC=levelCfgRef.current;
      if(cfgCC){if(gs.score>=cfgCC.scoreGoal&&(!cfgCC.modifier||checkModGoal(cfgCC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STARDUST — releases 8 collectible star particles that drift downward
    if(hit.type==="stardust"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const canvas=canvasRef.current;const cw6=canvas?.width||390,ch6=canvas?.height||700;
      // Spawn 8 star pieces
      for(let i=0;i<8;i++){
        const a=(i/8)*Math.PI*2;const spd=1.4+Math.random()*1.0;
        const sx=Math.max(30,Math.min(cw6-30,hit.x+Math.cos(a)*30));
        const sy=Math.max(100,Math.min(ch6-60,hit.y+Math.sin(a)*30));
        targetsRef.current.push({
          id:Math.random().toString(36).slice(2),type:"star_piece",
          x:sx,y:sy,radius:BASE_R*0.6,color:"#fbbf24",glow:"#fde68a",
          rarity:RARITY.RARE,lifetime:2000,spawnedAt:Date.now(),born:performance.now(),
          moving:true,ghost:false,vx:Math.cos(a)*spd*0.5,vy:0.4+Math.random()*0.3,trail:[],
          hitsLeft:1,maxHits:1,dying:null,_dustId:hit.id
        });
      }
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const basePts=Math.round(200*combo*feverMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,"⭐ STARDUST! Catch the stars!","#fde68a",18);
      spawnParticles(hit.x,hit.y,"#fde68a",20,"spark");
      sfx("chainBonus");vibrate([10,5,12]);
      unlock("stardust_tap");
      updateMissions(gs.sessionStats);
      const cfgSD=levelCfgRef.current;
      if(cfgSD){if(gs.score>=cfgSD.scoreGoal&&(!cfgSD.modifier||checkModGoal(cfgSD.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STAR_PIECE — tiny collectible from Stardust; 80pts each
    if(hit.type==="star_piece"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(8,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(80*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-18,`⭐ +${pts}`,"#fde68a",12);
      spawnParticles(hit.x,hit.y,"#fbbf24",7,"spark");
      sfx("tap");vibrate(5);
      // Check if player caught 5+ stars from same stardust
      const siblingsLeft=targetsRef.current.filter(t=>!t.dying&&t.type==="star_piece"&&t._dustId===hit._dustId).length;
      if(siblingsLeft<=2)unlock("stardust_shower");
      updateMissions(gs.sessionStats);
      const cfgSP=levelCfgRef.current;
      if(cfgSP){if(gs.score>=cfgSP.scoreGoal&&(!cfgSP.modifier||checkModGoal(cfgSP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // BLOOM — explodes into 5 petal targets; catch each petal for 100pts
    if(hit.type==="bloom"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const canvas=canvasRef.current;const cw5=canvas?.width||390,ch5=canvas?.height||700;
      // Spawn 5 flying petals
      const petalColors=["#ff6b9d","#ff9e9e","#ffd1dc","#ffb347","#ff85c1"];
      for(let i=0;i<5;i++){
        const a=(i/5)*Math.PI*2-Math.PI/2;const spd=1.8+Math.random()*1.2;
        const px=Math.max(30,Math.min(cw5-30,hit.x+Math.cos(a)*40));
        const py=Math.max(120,Math.min(ch5-30,hit.y+Math.sin(a)*40));
        targetsRef.current.push({
          id:Math.random().toString(36).slice(2),type:"petal",
          x:px,y:py,radius:BASE_R*0.55,color:petalColors[i],glow:petalColors[i],
          rarity:RARITY.RARE,lifetime:1800,spawnedAt:Date.now(),born:performance.now(),
          moving:true,ghost:false,vx:Math.cos(a)*spd,vy:Math.sin(a)*spd,trail:[],
          hitsLeft:1,maxHits:1,dying:null,_bloomId:hit.id
        });
      }
      spawnParticles(hit.x,hit.y,"#ff6b9d",18,"spark");spawnParticles(hit.x,hit.y,"#ffd700",8,"dot");
      spawnPopup(hit.x,hit.y-28,"🌸 BLOOM! Catch petals!","#ff6b9d",18);
      sfx("chainBonus");vibrate([10,5,12]);
      unlock("bloom_tap");
      gs.streak++;gs.lastTapTime=Date.now();
      updateMissions(gs.sessionStats);
      const cfgBl=levelCfgRef.current;
      if(cfgBl){if(gs.score>=cfgBl.scoreGoal&&(!cfgBl.modifier||checkModGoal(cfgBl.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PETAL — mini target from Bloom; 100 pts each
    if(hit.type==="petal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(8,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(100*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-20,`🌸 +${pts}`,"#ff6b9d",13);
      spawnParticles(hit.x,hit.y,"#ff6b9d",8,"spark");
      sfx("tap");vibrate(6);
      // Check if player caught all petals from same bloom
      const sibling=targetsRef.current.filter(t=>!t.dying&&t.type==="petal"&&t._bloomId===hit._bloomId).length;
      if(sibling===0)unlock("bloom_harvest");
      updateMissions(gs.sessionStats);
      const cfgPt=levelCfgRef.current;
      if(cfgPt){if(gs.score>=cfgPt.scoreGoal&&(!cfgPt.modifier||checkModGoal(cfgPt.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // REFLECTOR — reverses all moving targets' velocity + 600pts + bonus per reversed
    if(hit.type==="reflector"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      let reversedCount=0;
      targetsRef.current.forEach(t=>{
        if(!t.dying&&t.moving&&t.type!=="boss"){
          t.vx*=-1;t.vy*=-1;reversedCount++;
          spawnParticles(t.x,t.y,"#22d3ee",4,"spark");
        }
      });
      const pts=Math.round((600+reversedCount*80)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#22d3ee",size:300,born:performance.now(),duration:700,alpha:0.7});
      spawnPopup(hit.x,hit.y-34,`🪞 REFLECT! +${pts} (${reversedCount}×)`,"#22d3ee",20);
      spawnParticles(hit.x,hit.y,"#67e8f9",16,"spark");
      sfx(reversedCount>=5?"legendary":"bossKill");vibrate([12,6,16]);
      unlock("reflector_tap");
      if(reversedCount>=5)unlock("reflector_chaos");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgRf=levelCfgRef.current;
      if(cfgRf){if(gs.score>=cfgRf.scoreGoal&&(!cfgRf.modifier||checkModGoal(cfgRf.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VORTEX — swirling portal; bonus per target it consumed + pts
    if(hit.type==="vortex"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const consumed=hit._vtxConsumed||0;
      const pts=Math.round((400+consumed*150)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#7c3aed",size:200,born:performance.now(),duration:700,alpha:0.7});
      spawnPopup(hit.x,hit.y-34,`🌀 VORTEX! +${pts}${consumed>0?` (${consumed} consumed)`:""}`, "#a78bfa",20);
      spawnParticles(hit.x,hit.y,"#a78bfa",20,"spark");
      sfx(consumed>=3?"legendary":"bossKill");vibrate([12,8,16]);
      unlock("vortex_tap");
      if(consumed>=3)unlock("vortex_feast");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgVtx=levelCfgRef.current;
      if(cfgVtx){if(gs.score>=cfgVtx.scoreGoal&&(!cfgVtx.modifier||checkModGoal(cfgVtx.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PIXEL — tiny 8-bit target; 800+ pts, tiny hitbox
    if(hit.type==="pixel"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const ageMs=Date.now()-hit.spawnedAt;
      const isSpeedrun=ageMs<500;
      const pts=Math.round((isSpeedrun?1400:800)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isSpeedrun?`🟩 SPEEDRUN! +${pts}`:`🟩 PIXEL! +${pts}`,"#00ff88",isSpeedrun?22:18);
      spawnParticles(hit.x,hit.y,"#00ff88",14,"spark");
      sfx(isSpeedrun?"legendary":"chainBonus");vibrate([8,4,8]);
      unlock("pixel_tap");
      if(isSpeedrun)unlock("pixel_speedrun");
      mascotHappyRef.current+=2;
      updateMissions(gs.sessionStats);
      const cfgPx=levelCfgRef.current;
      if(cfgPx){if(gs.score>=cfgPx.scoreGoal&&(!cfgPx.modifier||checkModGoal(cfgPx.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NEON SIGN — flashes 3 colors; score = 300/600/1200 based on phase (red/cyan/gold)
    if(hit.type==="neon"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const phase=Math.floor((Date.now()-hit.spawnedAt)/900)%3;
      const phasePts=[300,600,1200];const phaseLabels=["💡 TAP","💡 NEON!","⭐ JACKPOT!"];
      const phaseColors=["#ff2244","#00ccff","#ffcc00"];
      const pts=Math.round(phasePts[phase]*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-30,`${phaseLabels[phase]} +${pts}`,phaseColors[phase],phase===2?24:18);
      spawnParticles(hit.x,hit.y,phaseColors[phase],14,"spark");
      if(phase===2){particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#ffcc00",size:180,born:performance.now(),duration:600,alpha:0.6});}
      sfx(phase===2?"legendary":phase===1?"chainBonus":"tap");vibrate(phase===2?[15,8,20]:8);
      unlock("neon_tap");
      if(phase===2)unlock("neon_gold");
      mascotHappyRef.current+=(phase+1);
      updateMissions(gs.sessionStats);
      const cfgNe=levelCfgRef.current;
      if(cfgNe){if(gs.score>=cfgNe.scoreGoal&&(!cfgNe.modifier||checkModGoal(cfgNe.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LANTERN — chain reaction: bonus pts per nearest 2 targets it lights up
    if(hit.type==="lantern"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // Chain to 2 nearest normal targets
      const chainTargets=targetsRef.current
        .filter(t=>!t.dying&&t.type!=="boss"&&t.type!=="bomb"&&t.type!=="timebomb")
        .sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y))
        .slice(0,2);
      chainTargets.forEach((t,i)=>{
        setTimeout(()=>{
          if(!t.dying){
            t.dying=performance.now();
            const cpts=Math.round(180*combo*feverMult);gsRef.current.score+=cpts;
            spawnParticles(t.x,t.y,"#fbbf24",10,"spark");
            spawnPopup(t.x,t.y-18,`🏮 +${cpts}`,"#fbbf24",14);
          }
        },i*180);
      });
      const basePts=Math.round((250+chainTargets.length*100)*combo*feverMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-32,`🏮 LANTERN! +${basePts}${chainTargets.length>0?` → ${chainTargets.length} chain!`:""}`, "#fbbf24",20);
      spawnParticles(hit.x,hit.y,"#fbbf24",16,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#fbbf24",size:160,born:performance.now(),duration:600,alpha:0.5});
      sfx(chainTargets.length>=2?"chainBonus":"tap");vibrate([10,5,10]);
      unlock("lantern_tap");
      if(chainTargets.length>=2)unlock("lantern_chain");
      mascotHappyRef.current+=2;
      updateMissions(gs.sessionStats);
      const cfgLn=levelCfgRef.current;
      if(cfgLn){if(gs.score>=cfgLn.scoreGoal&&(!cfgLn.modifier||checkModGoal(cfgLn.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // DRIFT — wavy target; pts scale with distance from center
    if(hit.type==="drift"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const canvas=canvasRef.current;const cw8=canvas?.width||390,ch8=canvas?.height||700;
      const distPct=Math.min(1,Math.hypot(hit.x-cw8/2,hit.y-ch8/2)/Math.hypot(cw8/2,ch8/2));
      const isMaxDrift=distPct>=0.9;
      const pts=Math.round((150+Math.round(distPct*850))*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const hue=Math.round(180+distPct*120);
      spawnPopup(hit.x,hit.y-28,isMaxDrift?`🌊 MAX DRIFT! +${pts}`:`🌊 DRIFT +${pts}`,`hsl(${hue},90%,65%)`,isMaxDrift?22:16);
      spawnParticles(hit.x,hit.y,`hsl(${hue},90%,65%)`,12,"spark");
      if(isMaxDrift){sfx("legendary");vibrate([14,8,18]);particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#38bdf8",size:160,born:performance.now(),duration:600,alpha:0.5});}
      else{sfx("tap");vibrate(8);}
      unlock("drift_tap");
      if(isMaxDrift)unlock("drift_max");
      mascotHappyRef.current+=2;
      updateMissions(gs.sessionStats);
      const cfgDr=levelCfgRef.current;
      if(cfgDr){if(gs.score>=cfgDr.scoreGoal&&(!cfgDr.modifier||checkModGoal(cfgDr.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SMOKE RING — expands outward; pts based on how early caught (less expanded = more pts)
    if(hit.type==="smoke_ring"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const expandPct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||3000));
      const isEarly=expandPct<0.4;
      const pts=Math.round(Math.max(60,(500*(1-expandPct)))*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-24,isEarly?`💨 QUICK! +${pts}`:`💨 +${pts}`,"#94a3b8",isEarly?20:15);
      spawnParticles(hit.x,hit.y,"#94a3b8",10,"spark");
      sfx(isEarly?"chainBonus":"tap");vibrate(isEarly?[10,4,10]:6);
      unlock("smoke_tap");
      if(isEarly)unlock("smoke_early");
      mascotHappyRef.current+=(isEarly?3:1);updateMissions(gs.sessionStats);
      const cfgSm=levelCfgRef.current;
      if(cfgSm){if(gs.score>=cfgSm.scoreGoal&&(!cfgSm.modifier||checkModGoal(cfgSm.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CHAIN BOMB — defuse it to also clear nearest bomb; risky but 1500pts
    if(hit.type==="chain_bomb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // Find nearest bomb
      const nearBomb=targetsRef.current.filter(t=>!t.dying&&t.type==="bomb")
        .sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y))[0];
      if(nearBomb){
        nearBomb.dying=performance.now();
        spawnParticles(nearBomb.x,nearBomb.y,"#4ade80",12,"spark");
        spawnPopup(nearBomb.x,nearBomb.y-20,"💚 DEFUSED!","#4ade80",14);
      }
      const pts=Math.round(1500*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#f97316",size:200,born:performance.now(),duration:600,alpha:0.5});
      spawnPopup(hit.x,hit.y-34,`🔗 CHAIN DEFUSE! +${pts}`,"#fde68a",22);
      spawnParticles(hit.x,hit.y,"#f97316",18,"spark");
      sfx("legendary");vibrate([12,6,16]);
      unlock("chainbomb_tap");unlock("chainbomb_safe");
      mascotHappyRef.current+=4;updateMissions(gs.sessionStats);
      const cfgCB=levelCfgRef.current;
      if(cfgCB){if(gs.score>=cfgCB.scoreGoal&&(!cfgCB.modifier||checkModGoal(cfgCB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // WARP GATE — teleports all non-boss targets to random positions + 400pts
    if(hit.type==="warp_gate"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const canvas=canvasRef.current;const cw11=canvas?.width||390,ch11=canvas?.height||700;
      let warped=0;
      targetsRef.current.forEach(t=>{
        if(t.dying||t.type==="boss")return;
        warped++;
        t.x=t.radius+20+Math.random()*(cw11-t.radius*2-40);
        t.y=t.radius+100+Math.random()*(ch11-t.radius*2-120);
        spawnParticles(t.x,t.y,"#06b6d4",4,"spark");
      });
      const pts=Math.round((400+warped*30)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#06b6d4",size:300,born:performance.now(),duration:800,alpha:0.6});
      spawnPopup(hit.x,hit.y-34,`🌀 WARP! +${pts} (${warped} targets)`,"#06b6d4",20);
      spawnParticles(hit.x,hit.y,"#67e8f9",18,"spark");
      sfx(warped>=5?"legendary":"bossKill");vibrate([12,6,14]);
      unlock("warpgate_tap");
      if(warped>=5)unlock("warpgate_chaos");
      mascotHappyRef.current+=3;updateMissions(gs.sessionStats);
      const cfgWG=levelCfgRef.current;
      if(cfgWG){if(gs.score>=cfgWG.scoreGoal&&(!cfgWG.modifier||checkModGoal(cfgWG.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MIRAGE — decoy target! Only 50pts; but unlock achievement for spotting it
    if(hit.type==="mirage"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const pts=50;gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-22,"🏜️ MIRAGE! +50","#f59e0b",15);
      spawnParticles(hit.x,hit.y,"#fbbf24",8,"spark");
      sfx("tap");vibrate(4);
      unlock("mirage_tap");
      updateMissions(gs.sessionStats);
      const cfgMr=levelCfgRef.current;
      if(cfgMr){if(gs.score>=cfgMr.scoreGoal&&(!cfgMr.modifier||checkModGoal(cfgMr.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SUPERNOVA — 200pts base + spawns 5 mini-orbs (100pts each); collect = Star Field
    if(hit.type==="supernova"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(200*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      // Spawn 5 mini-orbs
      gs._supernovaSession=(gs._supernovaSession||0)+1;
      gs._supernovaMinis=0;
      for(let i=0;i<5;i++){
        const sa=(i/5)*Math.PI*2+Math.random()*0.4;
        const sv2=2.0+Math.random()*1.4;
        targetsRef.current.push({
          id:Math.random().toString(36).slice(2),type:"supernova_mini",
          x:hit.x+Math.cos(sa)*20,y:hit.y+Math.sin(sa)*20,
          radius:hit.radius*0.45,color:"#fbbf24",glow:"#fef9c3",
          lifetime:2000,spawnedAt:Date.now(),born:performance.now(),
          moving:true,vx:Math.cos(sa)*sv2,vy:Math.sin(sa)*sv2,
          ghost:false,trail:[],hitsLeft:1,maxHits:1,dying:null,
          _hue:(i*72),_snovaSession:gs._supernovaSession,
        });
      }
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius*0.6,maxR:hit.radius*3.5,born:performance.now(),color:"#fbbf24",alpha:0.7,life:500});
      spawnPopup(hit.x,hit.y-28,`💫 NOVA! +${pts}`,"#fbbf24",20);
      spawnParticles(hit.x,hit.y,"#fef9c3",14,"spark");
      sfx("chainBonus");vibrate([8,4,12,4,8]);
      unlock("supernova_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSN=levelCfgRef.current;
      if(cfgSN){if(gs.score>=cfgSN.scoreGoal&&(!cfgSN.modifier||checkModGoal(cfgSN.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SUPERNOVA MINI — 100pts; collecting 4+ unlocks Star Field
    if(hit.type==="supernova_mini"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(100*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs._supernovaMinis=(gs._supernovaMinis||0)+1;
      spawnPopup(hit.x,hit.y-14,`⭐ +${pts}`,"#fbbf24",13);
      spawnParticles(hit.x,hit.y,"#fbbf24",5,"spark");
      sfx("tap");vibrate(4);
      if(gs._supernovaMinis>=4)unlock("supernova_collect");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSNM=levelCfgRef.current;
      if(cfgSNM){if(gs.score>=cfgSNM.scoreGoal&&(!cfgSNM.modifier||checkModGoal(cfgSNM.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL MATRIX — 3-tap: 200pts → 400pts → 800pts per layer; all 3 = full shatter
    if(hit.type==="crystal_matrix"){
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const layer=hit.hitsLeft||1;
      const layerPts=[200,400,800];
      const pts=Math.round(layerPts[layer-1]*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      hit.hitsLeft=Math.max(0,hit.hitsLeft-1);
      if(hit.hitsLeft<=0){
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        spawnPopup(hit.x,hit.y-26,`✨ SHATTERED! +${pts}`,"#7dd3fc",22);
        spawnParticles(hit.x,hit.y,"#38bdf8",16,"spark");
        sfx("chainBonus");vibrate([10,4,14,4,10]);
        unlock("crystalmatrix_shatter");
      } else {
        spawnPopup(hit.x,hit.y-20,`💎 ×${4-hit.hitsLeft} +${pts}`,"#38bdf8",16);
        spawnParticles(hit.x,hit.y,"#7dd3fc",8,"spark");
        sfx("tap");vibrate(7);
      }
      unlock("crystalmatrix_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCM=levelCfgRef.current;
      if(cfgCM){if(gs.score>=cfgCM.scoreGoal&&(!cfgCM.modifier||checkModGoal(cfgCM.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PLASMA STORM — 200pts + 80pts per nearby target electrified; 5+ = Thunder King
    if(hit.type==="plasma_storm"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const electrified=hit._stormCount||0;
      const pts=Math.round((200+electrified*80)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      // electrify nearby targets (make them glow for visual only)
      spawnPopup(hit.x,hit.y-26,electrified>=5?`⛈️ THUNDER KING! +${pts}`:`⚡ STORM! +${pts}`,electrified>=5?"#a5f3fc":"#22d3ee",electrified>=5?24:16);
      spawnParticles(hit.x,hit.y,"#22d3ee",electrified>=5?16:8,"spark");
      if(electrified>=5)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:hit.radius*4.5,born:performance.now(),color:"#06b6d4",alpha:0.7,life:500});
      sfx(electrified>=5?"chainBonus":"tap");vibrate(electrified>=5?[10,4,14,4,10]:7);
      unlock("plasmastorm_tap");
      if(electrified>=5)unlock("plasmastorm_surge");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPS=levelCfgRef.current;
      if(cfgPS){if(gs.score>=cfgPS.scoreGoal&&(!cfgPS.modifier||checkModGoal(cfgPS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NEXUS GATE — 300pts; teleports nearby targets through to paired gate position
    if(hit.type==="nexus_gate"){
      const pairGate=targetsRef.current.find(t=>t.type==="nexus_gate"&&t.id!==hit.id&&!t.dying);
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      let teleported=0;
      if(pairGate){
        const nearby=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb"&&t.type!=="timebomb"&&Math.hypot(t.x-hit.x,t.y-hit.y)<100);
        nearby.slice(0,5).forEach(t=>{
          t.x=pairGate.x+(Math.random()-0.5)*40;t.y=pairGate.y+(Math.random()-0.5)*40;
          teleported++;spawnParticles(t.x,t.y,"#6366f1",5,"spark");
        });
        targetsRef.current=targetsRef.current.filter(t=>t.id!==pairGate.id);
      }
      gs._nexusActive=false;
      const pts=Math.round((300+teleported*80)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,teleported>=3?`🌀 PORTAL! +${pts}`:`🔵 GATE! +${pts}`,teleported>=3?"#a5b4fc":"#6366f1",teleported>=3?22:16);
      spawnParticles(hit.x,hit.y,"#818cf8",teleported>=3?14:8,"spark");
      sfx(teleported>=3?"chainBonus":"tap");vibrate(teleported>=3?[8,4,10,4,8]:7);
      unlock("nexusgate_tap");
      if(teleported>=3)unlock("nexusgate_chain");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNG=levelCfgRef.current;
      if(cfgNG){if(gs.score>=cfgNG.scoreGoal&&(!cfgNG.modifier||checkModGoal(cfgNG.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TWILIGHT ORB — 200pts normally; 600pts when near-invisible (agePct >= 0.85)
    if(hit.type==="twilight_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4500));
      const isCatch=agePct>=0.85;
      const pts=Math.round((isCatch?600:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isCatch?`🌑 SHADOW CATCH! +${pts}`:`🌙 TWILIGHT! +${pts}`,isCatch?"#ffd700":"#818cf8",isCatch?24:16);
      spawnParticles(hit.x,hit.y,isCatch?"#ffd700":"#a5b4fc",isCatch?14:8,"spark");
      sfx(isCatch?"legendary":"tap");vibrate(isCatch?[10,4,14,4,10]:7);
      unlock("twilight_tap");
      if(isCatch)unlock("twilight_catch");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTO=levelCfgRef.current;
      if(cfgTO){if(gs.score>=cfgTO.scoreGoal&&(!cfgTO.modifier||checkModGoal(cfgTO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LIFE ORB — gives +1 life (up to max); 100pts if already at max lives
    if(hit.type==="life_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const maxLives=levelCfgRef.current?.lives||3;
      if(gs.lives<maxLives+2){
        gs.lives=Math.min(maxLives+2,gs.lives+1);
        spawnPopup(hit.x,hit.y-26,"❤️ +1 LIFE!","#4ade80",22);
        spawnParticles(hit.x,hit.y,"#4ade80",12,"spark");
        sfx("coin");vibrate([8,4,12,4,8]);
      } else {
        const pts=Math.round(100*(gs.feverActive?2:1));
        gs.score+=pts;
        spawnPopup(hit.x,hit.y-22,`❤️ +${pts}pts`,"#86efac",16);
        spawnParticles(hit.x,hit.y,"#4ade80",8,"spark");
        sfx("tap");vibrate(7);
      }
      gs.streak++;gs.lastTapTime=Date.now();
      unlock("lifeorb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLO=levelCfgRef.current;
      if(cfgLO){if(gs.score>=cfgLO.scoreGoal&&(!cfgLO.modifier||checkModGoal(cfgLO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SCORE AMP ORB — tap it = 200pts; all nearby taps count scored at 2× while it lives
    if(hit.type==="score_amp"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // Count how many targets were nearby (benefited from amp)
      const nearCount=targetsRef.current.filter(t=>!t.dying&&Math.hypot(t.x-hit.x,t.y-hit.y)<200).length;
      const pts=Math.round(200*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,nearCount>=3?`📢 TRIPLE AMP! +${pts}`:`📣 AMP! +${pts}`,nearCount>=3?"#ffd700":"#f43f5e",nearCount>=3?24:16);
      spawnParticles(hit.x,hit.y,"#fda4af",10,"spark");
      sfx("tap");vibrate(7);
      unlock("scoreamp_tap");
      if(nearCount>=3)unlock("scoreamp_multi");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSA=levelCfgRef.current;
      if(cfgSA){if(gs.score>=cfgSA.scoreGoal&&(!cfgSA.modifier||checkModGoal(cfgSA.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NIGHT ORB — starts dark, brightens; pts scale with brightness (50→400pts)
    if(hit.type==="night_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4500));
      const isPeak=agePct>=0.95;
      const pts=Math.round((50+350*agePct)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isPeak?`☀️ FULL DAYLIGHT! +${pts}`:`🌅 DAWN! +${pts}`,isPeak?"#ffd700":"#a78bfa",isPeak?24:16);
      spawnParticles(hit.x,hit.y,isPeak?"#ffd700":"#c4b5fd",isPeak?16:8,"spark");
      sfx(isPeak?"legendary":"tap");vibrate(isPeak?[10,4,14,4,10]:7);
      unlock("nightorb_tap");
      if(isPeak)unlock("nightorb_bright");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNO=levelCfgRef.current;
      if(cfgNO){if(gs.score>=cfgNO.scoreGoal&&(!cfgNO.modifier||checkModGoal(cfgNO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SHIFTING TARGET — 5 phases cycling every 600ms; pts scale with phase
    if(hit.type==="shift_target"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const phase=Math.floor((Date.now()-hit.spawnedAt)/600)%5;
      const phasePts=[100,300,600,1200,2500];
      const phaseLabels=["NORMAL","RARE","EPIC","LEGENDARY","BOSS"];
      const phaseColors=["#a3e635","#38bdf8","#c084fc","#f472b6","#ffd700"];
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(phasePts[phase]*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const label=phaseLabels[phase];
      spawnPopup(hit.x,hit.y-26,`🔄 ${label}! +${pts}`,phaseColors[phase],phase>=3?22:16);
      spawnParticles(hit.x,hit.y,phaseColors[phase],6+phase*2,"spark");
      sfx(phase>=3?"legendary":phase>=2?"epic":"tap");vibrate(phase>=3?[10,5,15,5,10]:7);
      unlock("shift_tap");
      if(phase===3)unlock("shift_legendary");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgST=levelCfgRef.current;
      if(cfgST){if(gs.score>=cfgST.scoreGoal&&(!cfgST.modifier||checkModGoal(cfgST.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LUCKY WHEEL — spinning number (1-20); pts = 100 × number
    if(hit.type==="lucky_wheel"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const num=1+Math.floor((Date.now()-hit.spawnedAt)/100)%20;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(100*num*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const isMax=num>=18;
      spawnPopup(hit.x,hit.y-26,isMax?`🎰 JACKPOT! ×${num} = +${pts}`:`🎡 ×${num} = +${pts}`,isMax?"#ffd700":"#f59e0b",isMax?24:16);
      spawnParticles(hit.x,hit.y,isMax?"#ffd700":"#f59e0b",isMax?14:6,"spark");
      sfx(isMax?"jackpot":"coin");vibrate(isMax?[10,5,20,5,10]:5);
      unlock("luckwheel_tap");
      if(num===20)unlock("luckwheel_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLW=levelCfgRef.current;
      if(cfgLW){if(gs.score>=cfgLW.scoreGoal&&(!cfgLW.modifier||checkModGoal(cfgLW.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ABYSS ORB — 300pts if tapped; penalty on expiry handled separately
    if(hit.type==="abyss_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(300*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,`🌑 DEFUSED! +${pts}`,"#a78bfa",18);
      spawnParticles(hit.x,hit.y,"#8b5cf6",10,"spark");
      sfx("tap");vibrate([5,3,10]);
      unlock("abyss_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAO=levelCfgRef.current;
      if(cfgAO){if(gs.score>=cfgAO.scoreGoal&&(!cfgAO.modifier||checkModGoal(cfgAO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // COSMIC PORTAL — next tap anywhere gives 5× pts
    if(hit.type==="cosmic_portal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs._portalNextTap=true;
      gs.score+=50;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,"🌀 PORTAL OPEN! Next tap ×5!","#c4b5fd",20);
      spawnParticles(hit.x,hit.y,"#a78bfa",12,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#a78bfa",alpha:0.6,life:600});
      sfx("powerUp");vibrate([8,4,8,4,15]);
      unlock("portal_tap2");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    // SUNFLOWER ORB — pts = 50 × remaining petals
    if(hit.type==="sunflower_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const petals=hit._petals||Math.max(1,12-Math.floor((Date.now()-hit.spawnedAt)/300));
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(50*petals*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const isFull=petals>=12;
      spawnPopup(hit.x,hit.y-26,isFull?`🌞 FULL BLOOM! +${pts}`:`🌻 ×${petals} = +${pts}`,isFull?"#ffd700":"#fbbf24",isFull?24:16);
      spawnParticles(hit.x,hit.y,"#facc15",8+petals,"spark");
      sfx(isFull?"legendary":"tap");vibrate(isFull?[10,5,15]:5);
      unlock("sunflower_tap");
      if(isFull)unlock("sunflower_full");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSFO=levelCfgRef.current;
      if(cfgSFO){if(gs.score>=cfgSFO.scoreGoal&&(!cfgSFO.modifier||checkModGoal(cfgSFO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TOXIC CLOUD — clears cloud for 350pts; also handle penalty for nearby normal taps
    if(hit.type==="toxic_cloud"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(350*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,`☣️ CLEARED! +${pts}`,"#4ade80",20);
      spawnParticles(hit.x,hit.y,"#4ade80",12,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#4ade80",alpha:0.5,life:600});
      sfx("tap");vibrate([6,3,8]);
      unlock("toxic_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTC=levelCfgRef.current;
      if(cfgTC){if(gs.score>=cfgTC.scoreGoal&&(!cfgTC.modifier||checkModGoal(cfgTC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LASER PRISM — 300pts; charges nearby targets
    if(hit.type==="laser_prism"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(300*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,`🔆 PRISM! +${pts}`,"#f43f5e",18);
      spawnParticles(hit.x,hit.y,"#f43f5e",10,"spark");
      sfx("tap");vibrate([5,3,8]);
      unlock("laserprism_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLP=levelCfgRef.current;
      if(cfgLP){if(gs.score>=cfgLP.scoreGoal&&(!cfgLP.modifier||checkModGoal(cfgLP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ECHO STAR — 500pts; echoes already spawned in draw loop
    if(hit.type==="echo_star"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(500*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`⭐ ECHO STAR! +${pts}`,"#ffd700",22);
      spawnParticles(hit.x,hit.y,"#ffd700",12,"spark");
      sfx("epic");vibrate([8,4,12]);
      unlock("echostar_tap");
      // Check if all echoes collected too
      const groupId=hit._echoGroup||hit.id;
      const remaining=targetsRef.current.filter(t=>t.type==="echo_point"&&t._echoGroup===groupId&&!t.dying);
      if(remaining.length===0)unlock("echostar_all");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgES=levelCfgRef.current;
      if(cfgES){if(gs.score>=cfgES.scoreGoal&&(!cfgES.modifier||checkModGoal(cfgES.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ECHO POINT — 150pts; track group for echostar_all
    if(hit.type==="echo_point"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=150;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-18,"⭐ +150","#fbbf24",12);
      spawnParticles(hit.x,hit.y,"#fbbf24",5,"spark");
      sfx("tap");vibrate(3);
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgEP=levelCfgRef.current;
      if(cfgEP){if(gs.score>=cfgEP.scoreGoal&&(!cfgEP.modifier||checkModGoal(cfgEP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // RUNE CHAIN — 3 sequential rune taps (100/300/700pts)
    if(hit.type==="rune_chain"){
      const runeIdx=hit.maxHits-(hit.hitsLeft||1);
      const runePts=[100,300,700];
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(runePts[runeIdx]*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      hit.hitsLeft--;
      spawnPopup(hit.x,hit.y-24-runeIdx*8,`🔮 RUNE ${runeIdx+1}! +${pts}`,["#a78bfa","#f472b6","#ffd700"][runeIdx],14+runeIdx*2);
      spawnParticles(hit.x,hit.y,["#a78bfa","#f472b6","#ffd700"][runeIdx],6+runeIdx*3,"spark");
      sfx("tap");vibrate(4+runeIdx*2);
      unlock("runechain_tap");
      if(hit.hitsLeft<=0){
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        spawnPopup(hit.x,hit.y-60,"⚗️ CHAIN COMPLETE!","#ffd700",22);
        sfx("epic");vibrate([10,5,20]);unlock("runechain_max");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgRC=levelCfgRef.current;
      if(cfgRC){if(gs.score>=cfgRC.scoreGoal&&(!cfgRC.modifier||checkModGoal(cfgRC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PLASMA WEB — 200pts + 80pts per web connection
    if(hit.type==="plasma_web"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const webCount=hit._webCount||0;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((200+80*webCount)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,webCount>=5?`🌐 WEB KING! +${pts}`:`🕸️ ×${webCount} = +${pts}`,webCount>=5?"#ffd700":"#7c3aed",webCount>=5?22:16);
      spawnParticles(hit.x,hit.y,"#a78bfa",8+webCount*2,"spark");
      sfx(webCount>=5?"epic":"tap");vibrate(webCount>=5?[10,5,10]:5);
      unlock("plasmaweb_tap");
      if(webCount>=5)unlock("plasmaweb_dense");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPW=levelCfgRef.current;
      if(cfgPW){if(gs.score>=cfgPW.scoreGoal&&(!cfgPW.modifier||checkModGoal(cfgPW.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ASTRAL ORB — 800pts if tapped within 0.3s of teleport; else 300pts
    if(hit.type==="astral_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const isFresh=hit._justTeleported&&(Date.now()-hit._justTeleported)<300;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isFresh?800:300)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isFresh?`⚡ INSTANT! +${pts}`:`✨ ASTRAL +${pts}`,isFresh?"#ffd700":"#818cf8",isFresh?24:16);
      spawnParticles(hit.x,hit.y,isFresh?"#ffd700":"#818cf8",isFresh?16:8,"spark");
      sfx(isFresh?"legendary":"tap");vibrate(isFresh?[12,6,18]:6);
      unlock("astral_tap");
      if(isFresh)unlock("astral_instant");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAO2=levelCfgRef.current;
      if(cfgAO2){if(gs.score>=cfgAO2.scoreGoal&&(!cfgAO2.modifier||checkModGoal(cfgAO2.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL CLOCK — pts = 100 × segments filled (max 8)
    if(hit.type==="crystal_clock"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const segments=Math.min(8,Math.floor((Date.now()-hit.spawnedAt)/(hit.lifetime||6000)*8));
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(Math.max(100,100*segments)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const isFull=segments>=8;
      spawnPopup(hit.x,hit.y-28,isFull?`👑 FULL CRYSTAL! +${pts}`:`💎 ×${segments} = +${pts}`,isFull?"#ffd700":"#0ea5e9",isFull?24:16);
      spawnParticles(hit.x,hit.y,isFull?"#ffd700":"#67e8f9",isFull?16:8,"spark");
      sfx(isFull?"legendary":"tap");vibrate(isFull?[12,6,20]:6);
      unlock("crystal_clock_tap");
      if(isFull)unlock("crystal_clock_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCC=levelCfgRef.current;
      if(cfgCC){if(gs.score>=cfgCC.scoreGoal&&(!cfgCC.modifier||checkModGoal(cfgCC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // METEOR SHOWER — 400pts + spawns 3 flaming meteor shards
    if(hit.type==="meteor_shower"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(400*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-30,`☄️ METEOR! +${pts}`,"#f97316",22);
      spawnParticles(hit.x,hit.y,"#fbbf24",14,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:160,born:performance.now(),color:"#f97316",alpha:0.65,life:500});
      const cw8=canvasRef.current?.width||360;const ch8=canvasRef.current?.height||640;
      for(let i=0;i<3;i++){
        const ma=(i/3)*Math.PI*2+Math.random()*0.6;const md=60+Math.random()*40;
        const mx=Math.max(35,Math.min(cw8-35,hit.x+Math.cos(ma)*md));
        const my=Math.max(35,Math.min(ch8-35,hit.y+Math.sin(ma)*md));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"meteor_shard",
          rarity:RARITY.RARE,x:mx,y:my,radius:22,color:"#f97316",glow:"#fed7aa",
          lifetime:2200,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
          worldId:hit.worldId,worldColor:hit.worldColor,bossPattern:null,bossPhase:1,cx:mx,cy:my,
          _meteorGroup:hit.id,_meteorIdx:i});
      }
      sfx("chainBonus");vibrate([8,4,10,4,8]);
      unlock("meteor_shower_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgMS=levelCfgRef.current;
      if(cfgMS){if(gs.score>=cfgMS.scoreGoal&&(!cfgMS.modifier||checkModGoal(cfgMS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // METEOR SHARD — 120pts; track group for achievement
    if(hit.type==="meteor_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=120;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-18,"🔥 SHARD +120","#f97316",12);
      spawnParticles(hit.x,hit.y,"#fbbf24",5,"spark");
      sfx("tap");vibrate(4);
      const groupId=hit._meteorGroup;
      if(groupId){
        const remaining=targetsRef.current.filter(t=>t.type==="meteor_shard"&&t._meteorGroup===groupId&&!t.dying);
        if(remaining.length===0)unlock("meteor_shower_all");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgMSh=levelCfgRef.current;
      if(cfgMSh){if(gs.score>=cfgMSh.scoreGoal&&(!cfgMSh.modifier||checkModGoal(cfgMSh.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GOLDEN GATE — window of opportunity: fast tap = 800pts, late = 200pts
    if(hit.type==="golden_gate"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||800));
      const isFast=agePct<0.4;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isFast?800:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isFast?`🏆 LIGHTNING! +${pts}`:`🥅 GATE +${pts}`,isFast?"#ffd700":"#a16207",isFast?24:16);
      spawnParticles(hit.x,hit.y,isFast?"#ffd700":"#ca8a04",isFast?18:6,"spark");
      sfx(isFast?"legendary":"tap");vibrate(isFast?[12,6,18,6,12]:5);
      unlock("golden_gate_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGG=levelCfgRef.current;
      if(cfgGG){if(gs.score>=cfgGG.scoreGoal&&(!cfgGG.modifier||checkModGoal(cfgGG.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PULSE RING — sweet-spot tap (between 35%-65% of pulse cycle) = 700pts, else 200pts
    if(hit.type==="pulse_ring"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const ringPct=(Date.now()-hit.spawnedAt)%(hit.lifetime||4000)/(hit.lifetime||4000);
      const isSweet=ringPct>=0.35&&ringPct<=0.65;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isSweet?700:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isSweet?`💥 SWEET SPOT! +${pts}`:`🔵 PULSE +${pts}`,isSweet?"#22d3ee":"#6b7280",isSweet?24:16);
      spawnParticles(hit.x,hit.y,isSweet?"#22d3ee":"#9ca3af",isSweet?16:6,"spark");
      if(isSweet)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:140,born:performance.now(),color:"#22d3ee",alpha:0.6,life:500});
      sfx(isSweet?"epic":"tap");vibrate(isSweet?[10,5,10]:4);
      unlock("pulse_ring_tap");
      if(isSweet)unlock("pulse_ring_sweet");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPR=levelCfgRef.current;
      if(cfgPR){if(gs.score>=cfgPR.scoreGoal&&(!cfgPR.modifier||checkModGoal(cfgPR.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CLOCKWORK BOMB — 5-hit defuse; each hit = 100pts × (1+gear); all 5 = 1000 bonus
    if(hit.type==="clockwork_bomb"){
      const gear=hit.maxHits-hit.hitsLeft; // 0–4 (gears removed so far)
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(100*(1+gear)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      hit.hitsLeft--;
      spawnPopup(hit.x,hit.y-26-gear*6,`⚙️ GEAR ${gear+1}! +${pts}`,"#f59e0b",14+gear*2);
      spawnParticles(hit.x,hit.y,"#fcd34d",4+gear*2,"spark");
      sfx("tap");vibrate(4+gear*3);
      unlock("clockwork_tap");
      if(hit.hitsLeft<=0){
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const bonusPts=Math.round(1000*combo*feverMult);
        gs.score+=bonusPts;
        spawnPopup(hit.x,hit.y-60,`💣 DEFUSED! +${bonusPts}`,"#ffd700",26);
        for(let i=0;i<20;i++)spawnParticles(hit.x,hit.y,"#fcd34d",1,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#f59e0b",alpha:0.7,life:700});
        sfx("epic");vibrate([12,6,20,6,12]);
        unlock("clockwork_max");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCK=levelCfgRef.current;
      if(cfgCK){if(gs.score>=cfgCK.scoreGoal&&(!cfgCK.modifier||checkModGoal(cfgCK.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NEBULA VORTEX — pts scale with growth (200 at start, 800 at full)
    if(hit.type==="nebula_vortex"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const growPct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||5000));
      const isBig=growPct>=0.8;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((200+Math.floor(growPct*600))*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isBig?`🌌 COSMIC! +${pts}`:`🌌 NEBULA +${pts}`,isBig?"#ffd700":"#a78bfa",isBig?24:16);
      spawnParticles(hit.x,hit.y,isBig?"#ffd700":"#818cf8",isBig?18:8,"spark");
      if(isBig)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#818cf8",alpha:0.65,life:700});
      sfx(isBig?"legendary":"tap");vibrate(isBig?[12,6,20]:6);
      unlock("nebula_vortex_tap");
      if(isBig)unlock("nebula_vortex_big");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNV=levelCfgRef.current;
      if(cfgNV){if(gs.score>=cfgNV.scoreGoal&&(!cfgNV.modifier||checkModGoal(cfgNV.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // FROST HEX — 200pts base + 100 per frozen target
    if(hit.type==="frost_hex"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const frozenCount=hit._frozenCount||0;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((200+100*frozenCount)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,frozenCount>=3?`🧊 ARCTIC BLAST! +${pts}`:`❄️ FROZEN ×${frozenCount} +${pts}`,frozenCount>=3?"#38bdf8":"#7dd3fc",frozenCount>=3?24:16);
      spawnParticles(hit.x,hit.y,"#bae6fd",8+frozenCount*3,"spark");
      sfx(frozenCount>=3?"epic":"tap");vibrate(frozenCount>=3?[10,5,10]:4);
      unlock("frost_hex_tap");
      if(frozenCount>=3)unlock("frost_hex_freeze");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgFH=levelCfgRef.current;
      if(cfgFH){if(gs.score>=cfgFH.scoreGoal&&(!cfgFH.modifier||checkModGoal(cfgFH.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LIGHTNING ORB — 300pts base + chain to up to 5 nearby normals for 80pts each
    if(hit.type==="lightning_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const chainTargets=targetsRef.current.filter(o=>!o.dying&&o.type==="normal"&&Math.hypot(o.x-hit.x,o.y-hit.y)<200).slice(0,5);
      chainTargets.forEach((o,i)=>{
        o.dying=performance.now();
        spawnParticles(o.x,o.y,"#facc15",5,"spark");
        spawnPopup(o.x,o.y-16,"⚡+80","#fcd34d",10);
      });
      const chainBonus=chainTargets.length*80;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((300+chainBonus)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-30,chainTargets.length>=3?`🌩️ CHAIN ×${chainTargets.length}! +${pts}`:`⚡ ZAP! +${pts}`,chainTargets.length>=3?"#ffd700":"#fcd34d",chainTargets.length>=3?24:16);
      spawnParticles(hit.x,hit.y,"#facc15",12,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:150,born:performance.now(),color:"#fcd34d",alpha:0.6,life:500});
      sfx(chainTargets.length>=3?"epic":"tap");vibrate(chainTargets.length>=3?[10,5,15,5,10]:5);
      unlock("lightning_orb_tap");
      if(chainTargets.length>=3)unlock("lightning_orb_chain");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLO=levelCfgRef.current;
      if(cfgLO){if(gs.score>=cfgLO.scoreGoal&&(!cfgLO.modifier||checkModGoal(cfgLO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STAR BURST — 200pts early, 600pts at full expansion (>85%)
    if(hit.type==="star_burst"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const expandPct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4500));
      const isFull=expandPct>=0.85;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isFull?600:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isFull?`🌟 SUPERNOVA! +${pts}`:`💫 BURST +${pts}`,isFull?"#ffd700":"#fbbf24",isFull?26:16);
      spawnParticles(hit.x,hit.y,isFull?"#ffd700":"#fbbf24",isFull?20:8,"spark");
      if(isFull)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#ffd700",alpha:0.7,life:700});
      sfx(isFull?"legendary":"tap");vibrate(isFull?[14,6,22,6,14]:5);
      unlock("starburst_tap");
      if(isFull)unlock("starburst_full");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSBst=levelCfgRef.current;
      if(cfgSBst){if(gs.score>=cfgSBst.scoreGoal&&(!cfgSBst.modifier||checkModGoal(cfgSBst.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL SPIRE — shatters into 4 shards on tap; pts = 200 + 100×chargeLevel
    if(hit.type==="crystal_spire"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const csCharge=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4000));
      const chargeLevel=Math.floor(csCharge*5);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((200+100*chargeLevel)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-30,`🔷 SHATTER! +${pts}`,"#67e8f9",20);
      spawnParticles(hit.x,hit.y,"#a5f3fc",12,"spark");
      const cw9=canvasRef.current?.width||360;const ch9=canvasRef.current?.height||640;
      for(let i=0;i<4;i++){
        const sa=(i/4)*Math.PI*2;const sd=55+Math.random()*30;
        const sx=Math.max(30,Math.min(cw9-30,hit.x+Math.cos(sa)*sd));
        const sy=Math.max(30,Math.min(ch9-30,hit.y+Math.sin(sa)*sd));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"crystal_spire_shard",
          rarity:RARITY.RARE,x:sx,y:sy,radius:18,color:"#a5f3fc",glow:"#cffafe",
          lifetime:1800,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
          worldId:hit.worldId,worldColor:hit.worldColor,bossPattern:null,bossPhase:1,cx:sx,cy:sy,
          _spireGroup:hit.id,_spireIdx:i});
      }
      sfx("chainBonus");vibrate([8,4,10]);
      unlock("crystalspire_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCSp=levelCfgRef.current;
      if(cfgCSp){if(gs.score>=cfgCSp.scoreGoal&&(!cfgCSp.modifier||checkModGoal(cfgCSp.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL SPIRE SHARD — 80pts; track group for achievement
    if(hit.type==="crystal_spire_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=80;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-16,"💎 SHARD +80","#a5f3fc",11);
      spawnParticles(hit.x,hit.y,"#67e8f9",4,"spark");
      sfx("tap");vibrate(3);
      const groupId=hit._spireGroup;
      if(groupId){
        const remaining=targetsRef.current.filter(t=>t.type==="crystal_spire_shard"&&t._spireGroup===groupId&&!t.dying);
        if(remaining.length===0)unlock("crystalspire_all");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCSSh=levelCfgRef.current;
      if(cfgCSSh){if(gs.score>=cfgCSSh.scoreGoal&&(!cfgCSSh.modifier||checkModGoal(cfgCSSh.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PHASE GATE — only tappable when solid (first 900ms of 1800ms cycle)
    if(hit.type==="phase_gate"){
      const cycleMs=1800;const solidMs=900;
      const phase=(Date.now()-hit.spawnedAt)%cycleMs;
      const isSolid=phase<solidMs;
      if(!isSolid){return;} // ghost phase — pass-through, no feedback
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const isClutch=phase>solidMs*0.85; // last 15% of solid phase
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isClutch?800:350)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isClutch?`🌀 CLUTCH! +${pts}`:`🌀 PHASE +${pts}`,isClutch?"#ffd700":"#22d3ee",isClutch?24:16);
      spawnParticles(hit.x,hit.y,isClutch?"#ffd700":"#67e8f9",isClutch?16:8,"spark");
      sfx(isClutch?"legendary":"tap");vibrate(isClutch?[12,6,18,6,12]:5);
      unlock("phasegate_tap");
      if(isClutch)unlock("phasegate_clutch");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPGt=levelCfgRef.current;
      if(cfgPGt){if(gs.score>=cfgPGt.scoreGoal&&(!cfgPGt.modifier||checkModGoal(cfgPGt.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VOID PULSAR — 500pts early (<30% shrink), 150pts late; no tap = -150pts
    if(hit.type==="void_pulsar"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const vpShrink=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||3500));
      const isEarly=vpShrink<0.3;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isEarly?500:150)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isEarly?`🎯 VOID SNIPER! +${pts}`:`🌌 VOID +${pts}`,isEarly?"#ffd700":"#a78bfa",isEarly?24:16);
      spawnParticles(hit.x,hit.y,isEarly?"#ffd700":"#7c3aed",isEarly?16:8,"spark");
      sfx(isEarly?"epic":"tap");vibrate(isEarly?[10,5,15]:5);
      unlock("voidpulsar_tap");
      if(isEarly)unlock("voidpulsar_early");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVP=levelCfgRef.current;
      if(cfgVP){if(gs.score>=cfgVP.scoreGoal&&(!cfgVP.modifier||checkModGoal(cfgVP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NOVA CANNON — charges up; on tap: 400pts + shockwave clears nearby targets (100pts each)
    if(hit.type==="nova_cannon"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const ncCharge=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4500));
      const BLAST_R2=ncCharge>=0.8?220:100;
      const blasted=targetsRef.current.filter(o=>!o.dying&&o.type==="normal"&&Math.hypot(o.x-hit.x,o.y-hit.y)<BLAST_R2).slice(0,8);
      blasted.forEach(o=>{o.dying=performance.now();spawnParticles(o.x,o.y,"#f97316",6,"spark");});
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((400+blasted.length*100)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-32,blasted.length>=4?`🔥 MEGA BLAST! +${pts}`:`💥 CANNON! +${pts}`,blasted.length>=4?"#ffd700":"#fb923c",blasted.length>=4?26:18);
      spawnParticles(hit.x,hit.y,"#fbbf24",14+blasted.length*2,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:BLAST_R2,born:performance.now(),color:"#f97316",alpha:0.75,life:700});
      sfx(blasted.length>=4?"epic":"tap");vibrate(blasted.length>=4?[12,6,20,6,12]:6);
      unlock("novacannon_tap");
      if(blasted.length>=4)unlock("novacannon_mega");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNC=levelCfgRef.current;
      if(cfgNC){if(gs.score>=cfgNC.scoreGoal&&(!cfgNC.modifier||checkModGoal(cfgNC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // HEX GRID — 6 hexes, each tap fills one; all 6 = jackpot
    if(hit.type==="hex_grid"){
      const prevMask=hit._tappedMask||0;
      // Find which hex was tapped (based on pointer position relative to target)
      const hexIdx=((hit._nextHex||0)%6);
      hit._nextHex=(hit._nextHex||0)+1;
      hit._tappedMask=(prevMask|(1<<hexIdx));
      const isComplete=hit._tappedMask===63; // all 6 bits set
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const tapPts=Math.round(80*(hexIdx+1)*combo*feverMult);
      gs.score+=tapPts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-20-hexIdx*6,`🔷 HEX ${hexIdx+1}! +${tapPts}`,"#a5b4fc",12+hexIdx*2);
      spawnParticles(hit.x,hit.y,"#818cf8",4+hexIdx,"spark");
      sfx("tap");vibrate(4+hexIdx*2);
      unlock("hexgrid_tap");
      if(isComplete){
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const bonusPts=Math.round(1200*combo*feverMult);
        gs.score+=bonusPts;
        spawnPopup(hit.x,hit.y-70,`👑 HEX COMPLETE! +${bonusPts}`,"#ffd700",28);
        for(let i=0;i<24;i++)spawnParticles(hit.x,hit.y,"#ffd700",1,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:220,born:performance.now(),color:"#818cf8",alpha:0.7,life:800});
        sfx("epic");vibrate([14,6,22,6,14]);
        unlock("hexgrid_full");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgHG=levelCfgRef.current;
      if(cfgHG){if(gs.score>=cfgHG.scoreGoal&&(!cfgHG.modifier||checkModGoal(cfgHG.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // QUANTUM TUNNEL — tap either endpoint; both tapped = achievement
    if(hit.type==="quantum_tunnel_a"||hit.type==="quantum_tunnel_b"){
      const isA=hit.type==="quantum_tunnel_a";
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      // Remove partner too
      const partner=targetsRef.current.find(t=>t.id===hit._partnerId);
      if(partner)partner.dying=performance.now();
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(350*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`${isA?"🔵":"🔴"} TUNNEL! +${pts}`,isA?"#22d3ee":"#f472b6",20);
      spawnParticles(hit.x,hit.y,isA?"#22d3ee":"#f472b6",12,"spark");
      sfx("tap");vibrate([6,3,10]);
      unlock("qtunnel_tap");
      // Check if we tapped the B portal (shows player tapped both)
      if(!isA||gs._qtunnelTappedA){unlock("qtunnel_both");}
      if(isA)gs._qtunnelTappedA=true;
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgQT=levelCfgRef.current;
      if(cfgQT){if(gs.score>=cfgQT.scoreGoal&&(!cfgQT.modifier||checkModGoal(cfgQT.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // INFERNO ORB — pts = 100 + 100×heatStacks; max heat (8) = legendary
    if(hit.type==="inferno_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const heat=hit._heatStacks||0;
      const isMax=heat>=8;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((100+100*heat)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isMax?`👑 MAX HEAT! +${pts}`:`🔥 HEAT ×${heat} +${pts}`,isMax?"#ffd700":"#f97316",isMax?26:18);
      spawnParticles(hit.x,hit.y,isMax?"#ffd700":"#fbbf24",isMax?20:8,"spark");
      if(isMax)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#f97316",alpha:0.7,life:700});
      sfx(isMax?"legendary":"tap");vibrate(isMax?[14,7,22,7,14]:6);
      unlock("inferno_tap");
      if(isMax)unlock("inferno_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgIO=levelCfgRef.current;
      if(cfgIO){if(gs.score>=cfgIO.scoreGoal&&(!cfgIO.modifier||checkModGoal(cfgIO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CHRONO SPHERE — 400pts + 80pts per slowed target count
    if(hit.type==="chrono_sphere"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const slowed=hit._slowedCount||0;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((400+80*slowed)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,slowed>=3?`⌛ TIME SURGE! +${pts}`:`⏱ CHRONO ×${slowed} +${pts}`,slowed>=3?"#ffd700":"#818cf8",slowed>=3?24:16);
      spawnParticles(hit.x,hit.y,"#a5b4fc",8+slowed*2,"spark");
      sfx(slowed>=3?"epic":"tap");vibrate(slowed>=3?[10,5,15]:4);
      unlock("chrono_tap");
      if(slowed>=3)unlock("chrono_surge");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCS2=levelCfgRef.current;
      if(cfgCS2){if(gs.score>=cfgCS2.scoreGoal&&(!cfgCS2.modifier||checkModGoal(cfgCS2.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PRISMATIC LENS — 800pts if tapped on focus color; 200pts otherwise
    if(hit.type==="prismatic_lens"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const colorIdx=Math.floor((Date.now()-hit.spawnedAt)/300)%6;
      const isFocus=colorIdx===(hit._focusIdx||0);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isFocus?800:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isFocus?`🎯 FOCUS! +${pts}`:`🔮 PRISM +${pts}`,isFocus?"#ffd700":"#818cf8",isFocus?24:16);
      spawnParticles(hit.x,hit.y,isFocus?"#ffd700":"#818cf8",isFocus?18:6,"spark");
      sfx(isFocus?"legendary":"tap");vibrate(isFocus?[12,6,18]:5);
      unlock("prism_lens_tap");
      if(isFocus)unlock("prism_lens_focus");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPL=levelCfgRef.current;
      if(cfgPL){if(gs.score>=cfgPL.scoreGoal&&(!cfgPL.modifier||checkModGoal(cfgPL.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SHADOW MIRROR — 600pts if active (last tap was recent); 100pts otherwise
    if(hit.type==="shadow_mirror"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const hasLastTap=gs._lastTapX!==undefined&&(Date.now()-gs._lastTapTime)<3000;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((hasLastTap?600:100)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,hasLastTap?`🪞 REFLECTION! +${pts}`:`🌑 DARK MIRROR +${pts}`,hasLastTap?"#f0abfc":"#6b7280",hasLastTap?22:14);
      spawnParticles(hit.x,hit.y,hasLastTap?"#f472b6":"#6b7280",hasLastTap?14:5,"spark");
      sfx(hasLastTap?"epic":"tap");vibrate(hasLastTap?[10,5,12]:4);
      unlock("shadow_mirror_tap");
      if(hasLastTap)unlock("shadow_mirror_hit");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSMi=levelCfgRef.current;
      if(cfgSMi){if(gs.score>=cfgSMi.scoreGoal&&(!cfgSMi.modifier||checkModGoal(cfgSMi.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PLASMA COMET — 1000pts at apex (40%-65% of lifetime); 250pts otherwise
    if(hit.type==="plasma_comet"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const age=(Date.now()-hit.spawnedAt)/(hit.lifetime||2500);
      const isApex=age>0.4&&age<0.65;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isApex?1000:250)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-30,isApex?`⭐ APEX! +${pts}`:`☄️ COMET +${pts}`,isApex?"#ffd700":"#c084fc",isApex?26:16);
      spawnParticles(hit.x,hit.y,isApex?"#ffd700":"#a855f7",isApex?20:8,"spark");
      if(isApex)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#c084fc",alpha:0.7,life:700});
      sfx(isApex?"legendary":"tap");vibrate(isApex?[14,7,22,7,14]:5);
      unlock("plasma_comet_tap");unlock("plasmacomet_tap");
      if(isApex)unlock("plasma_comet_apex");
      if(age<0.3)unlock("plasmacomet_early");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPC=levelCfgRef.current;
      if(cfgPC){if(gs.score>=cfgPC.scoreGoal&&(!cfgPC.modifier||checkModGoal(cfgPC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ABYSSAL GATE — 500pts + 100pts per drain; tap it down for massive reward
    if(hit.type==="abyssal_gate"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const drainCount=hit._drainCount||0;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((500+100*drainCount)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-30,drainCount>=5?`🌑 ABYSS FEAST! +${pts}`:`⚫ GATE +${drainCount} +${pts}`,drainCount>=5?"#ffd700":"#a78bfa",drainCount>=5?26:18);
      spawnParticles(hit.x,hit.y,"#a78bfa",10+drainCount*2,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#7c3aed",alpha:0.65,life:700});
      sfx(drainCount>=5?"legendary":"tap");vibrate(drainCount>=5?[14,7,22,7,14]:6);
      unlock("abyssal_tap");
      if(drainCount>=5)unlock("abyssal_drain");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAG=levelCfgRef.current;
      if(cfgAG){if(gs.score>=cfgAG.scoreGoal&&(!cfgAG.modifier||checkModGoal(cfgAG.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // WARP BALL — 800pts if caught within 0.3s of warp; 250pts otherwise
    if(hit.type==="warp_ball"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const nowWB2=Date.now();
      const justWarped=hit._justWarped&&(nowWB2-hit._justWarped)<300;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((justWarped?800:250)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,justWarped?`🌟 WARP CATCH! +${pts}`:`🌀 WARP +${pts}`,justWarped?"#ffd700":"#38bdf8",justWarped?24:16);
      spawnParticles(hit.x,hit.y,justWarped?"#ffd700":"#38bdf8",justWarped?18:8,"spark");
      sfx(justWarped?"legendary":"tap");vibrate(justWarped?[12,6,18]:5);
      unlock("warpball_tap");
      if(justWarped)unlock("warpball_instant");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgWB=levelCfgRef.current;
      if(cfgWB){if(gs.score>=cfgWB.scoreGoal&&(!cfgWB.modifier||checkModGoal(cfgWB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL SPIKE — 500pts from safe arc; -1 life from danger zone
    if(hit.type==="crystal_spike"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const dangerArc=hit._dangerArc||0;
      // Determine tap angle relative to target center
      const tapAngle=Math.atan2(ty-hit.y,tx-hit.x);
      const angleDiff=Math.abs(((tapAngle-dangerArc+Math.PI*3)%(Math.PI*2))-Math.PI);
      const isDanger=angleDiff<1.0; // within 1 radian of danger arc
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      if(isDanger){
        gs.score=Math.max(0,gs.score-50);gs.streak=0;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-24,"💎 OUCH! −50","#ef4444",16);
        spawnParticles(hit.x,hit.y,"#ef4444",8,"spark");
        sfx("miss");vibrate([12,6,12]);
        unlock("crystalspike_ow");
        gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
        return;
      }
      const pts=Math.round(500*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`💎 SAFE! +${pts}`,"#a5f3fc",20);
      spawnParticles(hit.x,hit.y,"#67e8f9",12,"spark");
      sfx("tap");vibrate([6,3,8]);
      unlock("crystalspike_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCS3=levelCfgRef.current;
      if(cfgCS3){if(gs.score>=cfgCS3.scoreGoal&&(!cfgCS3.modifier||checkModGoal(cfgCS3.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ENERGY CORE — green (phase 0) = 500pts; yellow (phase 1) = 300pts; red (phase 2) = risky 800pts but skip 1 life check
    if(hit.type==="energy_core"){
      const cycleMs=1400;
      const phase=Math.floor((Date.now()-hit.spawnedAt)/cycleMs)%3;
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const basePts=[500,300,800][phase];
      const pts=Math.round(basePts*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const phaseNames=["✅ GREEN! ","⚠️ YELLOW! ","☢️ RED RISK! "];
      const phaseColors=["#4ade80","#facc15","#ef4444"];
      spawnPopup(hit.x,hit.y-28,`${phaseNames[phase]}+${pts}`,phaseColors[phase],phase===2?22:18);
      spawnParticles(hit.x,hit.y,phaseColors[phase],8+phase*3,"spark");
      sfx(phase===2?"epic":"tap");vibrate(phase===2?[10,5,15]:4);
      unlock(phase===0?"energycore_green":phase===2?"energycore_red":"energycore_green");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgEC=levelCfgRef.current;
      if(cfgEC){if(gs.score>=cfgEC.scoreGoal&&(!cfgEC.modifier||checkModGoal(cfgEC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ARCANE CIRCLE — 700pts if correct rune shown; 150pts otherwise
    if(hit.type==="arcane_circle"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const cycleMs=500;
      const runeIdx=Math.floor((Date.now()-hit.spawnedAt)/cycleMs)%5;
      const isMatch=runeIdx===(hit._targetRune||0);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isMatch?700:150)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isMatch?`⚡ RUNE MATCH! +${pts}`:`🔮 ARCANE +${pts}`,isMatch?"#ffd700":"#818cf8",isMatch?24:16);
      spawnParticles(hit.x,hit.y,isMatch?"#ffd700":"#818cf8",isMatch?18:6,"spark");
      sfx(isMatch?"legendary":"tap");vibrate(isMatch?[12,6,18,6,12]:4);
      unlock("arcane_tap");
      if(isMatch)unlock("arcane_match");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAC=levelCfgRef.current;
      if(cfgAC){if(gs.score>=cfgAC.scoreGoal&&(!cfgAC.modifier||checkModGoal(cfgAC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STELLAR DRIFT — pts = 100 + 2×driftDist (max 600)
    if(hit.type==="stellar_drift"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const spawnX=hit._spawnX||hit.x;const spawnY=hit._spawnY||hit.y;
      const driftDist=Math.hypot(hit.x-spawnX,hit.y-spawnY);
      const isFar=driftDist>=200;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(Math.min(600,100+Math.floor(driftDist*2))*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isFar?`⭐ FAR STAR! +${pts}`:`🌠 DRIFT +${pts}`,isFar?"#ffd700":"#a5f3fc",isFar?24:16);
      spawnParticles(hit.x,hit.y,isFar?"#ffd700":"#67e8f9",isFar?16:8,"spark");
      sfx(isFar?"epic":"tap");vibrate(isFar?[10,5,12]:4);
      unlock("stellar_tap");
      if(isFar)unlock("stellar_far");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSD=levelCfgRef.current;
      if(cfgSD){if(gs.score>=cfgSD.scoreGoal&&(!cfgSD.modifier||checkModGoal(cfgSD.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // BUBBLE WAVE — 300pts early, 600pts clutch (>80%); burst (expiry) = -1 life
    if(hit.type==="bubble_wave"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const bwGrow=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4000));
      const isClutch=bwGrow>0.8;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isClutch?600:300)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isClutch?`💥 CLUTCH POP! +${pts}`:`🫧 POP! +${pts}`,isClutch?"#ffd700":"#38bdf8",isClutch?24:16);
      spawnParticles(hit.x,hit.y,isClutch?"#ffd700":"#38bdf8",isClutch?18:10,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:100+bwGrow*100,born:performance.now(),color:"#38bdf8",alpha:0.55,life:500});
      sfx(isClutch?"epic":"tap");vibrate(isClutch?[10,5,12]:4);
      unlock("bubblewave_tap");
      if(isClutch)unlock("bubblewave_clutch");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgBW=levelCfgRef.current;
      if(cfgBW){if(gs.score>=cfgBW.scoreGoal&&(!cfgBW.modifier||checkModGoal(cfgBW.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // IRON SHIELD — 3-hit: each hit scores 100pts; broken = +500pts
    if(hit.type==="iron_shield"){
      const nowIS=Date.now();
      // Must tap within 0.8s window between hits
      if(hit._lastShieldTap&&nowIS-hit._lastShieldTap>800){
        // Reset — tapped too slow
        hit.hitsLeft=hit.maxHits||3;
        spawnPopup(hit.x,hit.y-22,"🛡️ RESET!","#94a3b8",14);
        sfx("miss");vibrate([4,2,4]);
        return;
      }
      hit._lastShieldTap=nowIS;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(100*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      hit.hitsLeft--;
      spawnPopup(hit.x,hit.y-22-((hit.maxHits||3)-hit.hitsLeft-1)*6,`🛡️ HIT! +${pts}`,"#94a3b8",13);
      spawnParticles(hit.x,hit.y,"#e2e8f0",5,"spark");
      sfx("tap");vibrate(5);
      unlock("ironshield_tap");
      if(hit.hitsLeft<=0){
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const breakPts=Math.round(500*combo*feverMult);
        gs.score+=breakPts;
        spawnPopup(hit.x,hit.y-60,`💪 BROKEN! +${breakPts}`,"#ffd700",26);
        for(let i=0;i<16;i++)spawnParticles(hit.x,hit.y,"#f1f5f9",1,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#94a3b8",alpha:0.65,life:700});
        sfx("epic");vibrate([12,6,18,6,12]);
        unlock("ironshield_break");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgIS=levelCfgRef.current;
      if(cfgIS){if(gs.score>=cfgIS.scoreGoal&&(!cfgIS.modifier||checkModGoal(cfgIS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TIME ECHO — 400pts; track count for achievement
    if(hit.type==="time_echo"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(400*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,`👻 ECHO! +${pts}`,"#a78bfa",20);
      spawnParticles(hit.x,hit.y,"#a78bfa",12,"spark");
      sfx("tap");vibrate([6,3,9]);
      gs._echoCount=(gs._echoCount||0)+1;
      unlock("timeecho_tap");
      if(gs._echoCount>=3)unlock("timeecho_perfect");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTE=levelCfgRef.current;
      if(cfgTE){if(gs.score>=cfgTE.scoreGoal&&(!cfgTE.modifier||checkModGoal(cfgTE.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GRAVITY ORB MK2 — 300pts + 100×pullCount; pulls all nearby targets into it permanently
    if(hit.type==="gravity_orb2"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const pullCount=hit._pullCount||0;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((300+100*pullCount)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-30,pullCount>=5?`⚫ EVENT HORIZON! +${pts}`:`🌑 BLACK HOLE ×${pullCount} +${pts}`,pullCount>=5?"#ffd700":"#a78bfa",pullCount>=5?26:18);
      spawnParticles(hit.x,hit.y,"#7c3aed",12+pullCount*2,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:240,born:performance.now(),color:"#4c1d95",alpha:0.75,life:800});
      sfx(pullCount>=5?"legendary":"tap");vibrate(pullCount>=5?[14,7,22,7,14]:6);
      unlock("gravity2_tap");
      if(pullCount>=5)unlock("gravity2_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGO2=levelCfgRef.current;
      if(cfgGO2){if(gs.score>=cfgGO2.scoreGoal&&(!cfgGO2.modifier||checkModGoal(cfgGO2.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PIXEL SWARM — tap core for 50pts + deploys 8 pixel units
    if(hit.type==="pixel_swarm"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=50;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-24,"📡 SWARM! Collect all!","#f472b6",16);
      spawnParticles(hit.x,hit.y,"#f9a8d4",8,"spark");
      sfx("chainBonus");vibrate([5,3,5,3,5]);
      unlock("pixelswarm_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    // PIXEL UNIT — 80pts each; collect all 8 for achievement
    if(hit.type==="pixel_unit"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=80;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-14,"🎨 +80","#f472b6",11);
      spawnParticles(hit.x,hit.y,"#f9a8d4",4,"spark");
      sfx("tap");vibrate(3);
      const groupId=hit._pixelGroup;
      if(groupId){
        const remaining=targetsRef.current.filter(t=>t.type==="pixel_unit"&&t._pixelGroup===groupId&&!t.dying);
        if(remaining.length===0)unlock("pixelswarm_full");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPU=levelCfgRef.current;
      if(cfgPU){if(gs.score>=cfgPU.scoreGoal&&(!cfgPU.modifier||checkModGoal(cfgPU.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CHRONO CRYSTAL — phase 1: freeze on tap; phase 2: double pts bonus tap
    if(hit.type==="chrono_crystal"){
      const phase=hit._chronoPhase||"active";
      if(phase==="active"){
        // First tap within 3s — freeze the crystal
        const ageMs=Date.now()-hit.spawnedAt;
        if(ageMs<3000){
          hit._chronoPhase="frozen";
          hit._frozenAt=Date.now();
          hit.lifetime=hit.lifetime+(3000-(Date.now()-hit.spawnedAt))+3000; // extend to give freeze window
          spawnPopup(hit.x,hit.y-24,"⏳ FROZEN! Tap again!","#7dd3fc",18);
          spawnParticles(hit.x,hit.y,"#bae6fd",10,"spark");
          sfx("freeze");vibrate([5,3,8,3,5]);
          unlock("chrono_tap");
          gs.streak++;gs.lastTapTime=Date.now();
          gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
          return; // don't remove target yet
        } else {
          // Tapped after 3s — normal behavior, small pts
          targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
          const pts=100;gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
          spawnPopup(hit.x,hit.y-20,`🔮 +${pts}`,"#818cf8",14);
          spawnParticles(hit.x,hit.y,"#818cf8",6,"spark");
          sfx("tap");vibrate(5);
          unlock("chrono_tap");
          gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
          const cfgCC=levelCfgRef.current;
          if(cfgCC){if(gs.score>=cfgCC.scoreGoal&&(!cfgCC.modifier||checkModGoal(cfgCC.modifier,gs))){endLevel(true);return;}}
          return;
        }
      } else if(phase==="frozen"){
        // Second tap in frozen phase — double pts!
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const combo=Math.min(10,1+Math.floor(gs.streak/5));
        const feverMult=gs.feverActive?2:1;
        const pts=Math.round(400*combo*feverMult);
        gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-30,`⏳ DOUBLE TAP! +${pts}`,"#7dd3fc",24);
        spawnParticles(hit.x,hit.y,"#7dd3fc",14,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#7dd3fc",alpha:0.7,life:600});
        sfx("epic");vibrate([10,5,15,5,10]);
        unlock("chrono_bonus");
        gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
        const cfgCC2=levelCfgRef.current;
        if(cfgCC2){if(gs.score>=cfgCC2.scoreGoal&&(!cfgCC2.modifier||checkModGoal(cfgCC2.modifier,gs))){endLevel(true);return;}}
        return;
      }
    }
    // RUNE BURST — fires 5 rune blasts at nearest normals, 60 pts each; self 150 pts
    if(hit.type==="rune_burst"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      gs.score+=Math.round(150*combo*feverMult);gs.streak++;gs.lastTapTime=Date.now();
      spawnParticles(hit.x,hit.y,"#c084fc",12,"spark");
      sfx("chainBonus");vibrate([4,3,4,3,4,3,4]);
      // Fire 5 blasts at nearest normals
      const nearest5=targetsRef.current.filter(o=>!o.dying&&o.type==="normal")
        .sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y)).slice(0,5);
      let blastCount=0;
      nearest5.forEach(o=>{
        o.dying=performance.now();blastCount++;
        const bpts=Math.round(60*combo*feverMult);
        gs.score+=bpts;
        spawnParticles(o.x,o.y,"#c084fc",6,"spark");
        spawnPopup(o.x,o.y-14,`🔮+${bpts}`,"#c084fc",12);
      });
      spawnPopup(hit.x,hit.y-28,`🔮 RUNE BURST! ×${blastCount}+150`,"#c084fc",20);
      unlock("runeburst_tap");
      if(blastCount>=5)unlock("runeburst_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgRB=levelCfgRef.current;
      if(cfgRB){if(gs.score>=cfgRB.scoreGoal&&(!cfgRB.modifier||checkModGoal(cfgRB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL GOLEM — 3 HP; 3rd hit = full bonus 500 pts × combo
    if(hit.type==="crystal_golem"){
      hit.hitsLeft--;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      if(hit.hitsLeft<=0){
        // Final hit!
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const pts=Math.round(500*combo*feverMult);
        gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-32,"💎 GOLEM SLAIN! +"+pts,"#86efac",26);
        spawnParticles(hit.x,hit.y,"#86efac",18,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:220,born:performance.now(),color:"#86efac",alpha:0.7,life:700});
        sfx("bossKill");vibrate([14,7,22,7,14]);
        unlock("golem_destroy");
      } else {
        // Partial hit
        const pts=Math.round(100*combo*feverMult);
        gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-20,`${hit.hitsLeft===2?"🟢":"⚪"} HIT! +${pts} (${hit.hitsLeft} HP left)`,"#86efac",16);
        spawnParticles(hit.x,hit.y,"#86efac",8,"spark");
        sfx("tap");vibrate([5,3,8]);
        if(hit.hitsLeft===2)unlock("golem_tap");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCG=levelCfgRef.current;
      if(cfgCG){if(gs.score>=cfgCG.scoreGoal&&(!cfgCG.modifier||checkModGoal(cfgCG.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // FREEZE ORB — freezes all normal targets for 3s; 200 pts × combo
    if(hit.type==="freeze_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo43a=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult43a=gs.feverActive?2:1;
      const pts43a=Math.round(200*combo43a*feverMult43a);
      gs.score+=pts43a;gs.streak++;gs.lastTapTime=Date.now();
      // Freeze all normals for 3s
      const freezeExp43=Date.now()+3000;
      targetsRef.current.filter(o=>!o.dying&&o.type==="normal").forEach(o=>{
        o._anchorFrozenVx=o.vx;o._anchorFrozenVy=o.vy;o.vx=0;o.vy=0;o._frozenUntil=freezeExp43;
      });
      gs._freezeOrbExpiry=freezeExp43;gs._freezeOrbNormalTaps=0;
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:240,born:performance.now(),color:"#a5f3fc",alpha:0.7,life:700});
      spawnPopup(hit.x,hit.y-32,"🧊 FREEZE ORB! +"+pts43a,"#a5f3fc",26);
      spawnParticles(hit.x,hit.y,"#a5f3fc",16,"spark");
      sfx("freeze");vibrate([8,4,12,4,8]);
      unlock("freeze_orb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgFO43=levelCfgRef.current;
      if(cfgFO43){if(gs.score>=cfgFO43.scoreGoal&&(!cfgFO43.modifier||checkModGoal(cfgFO43.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TWIN COMET — tap one to kill both for 300 pts; or just 100 pts if alone
    if(hit.type==="twin_comet"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo43b=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult43b=gs.feverActive?2:1;
      const partner43=targetsRef.current.find(o=>!o.dying&&o.type==="twin_comet"&&o._twinGroup===hit._twinGroup);
      if(partner43){
        partner43.dying=performance.now();
        const pts43b=Math.round(300*combo43b*feverMult43b);
        gs.score+=pts43b;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-32,"☄️☄️ TWIN KILL! +"+pts43b,"#fb923c",26);
        spawnParticles(hit.x,hit.y,"#fb923c",16,"spark");
        spawnParticles(partner43.x,partner43.y,"#fb923c",12,"spark");
        sfx("chainBonus");vibrate([10,5,14,5,10]);
        unlock("twincomet_clean");
      } else {
        const pts43c=Math.round(100*combo43b*feverMult43b);
        gs.score+=pts43c;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-20,"☄️ COMET +"+pts43c,"#fb923c",18);
        spawnParticles(hit.x,hit.y,"#fb923c",10,"spark");
        sfx("tap");vibrate([6,3,8]);
      }
      unlock("twincomet_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTC43=levelCfgRef.current;
      if(cfgTC43){if(gs.score>=cfgTC43.scoreGoal&&(!cfgTC43.modifier||checkModGoal(cfgTC43.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PLASMA VORTEX — tap at phase ≥0.75 (peak) for 320 pts; otherwise 120 pts
    if(hit.type==="plasma_vortex"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const phase44=hit._vortexPhase||0;
      const combo44=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult44=gs.feverActive?2:1;
      const isPeak=phase44>=0.75;
      const pts44=Math.round((isPeak?320:120)*combo44*feverMult44);
      gs.score+=pts44;gs.streak++;gs.lastTapTime=Date.now();
      if(isPeak){
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:230,born:performance.now(),color:"#c084fc",alpha:0.75,life:700});
        spawnPopup(hit.x,hit.y-32,"🌀✨ PEAK! +"+pts44,"#c084fc",26);
        spawnParticles(hit.x,hit.y,"#c084fc",18,"spark");
        sfx("epic");vibrate([10,5,14,5,10]);
        unlock("plasmavortex_peak");
      } else {
        spawnPopup(hit.x,hit.y-20,"🌀 VORTEX +"+pts44,"#a78bfa",18);
        spawnParticles(hit.x,hit.y,"#a78bfa",10,"spark");
        sfx("tap");vibrate([6,3,8]);
      }
      unlock("plasmavortex_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPV44=levelCfgRef.current;
      if(cfgPV44){if(gs.score>=cfgPV44.scoreGoal&&(!cfgPV44.modifier||checkModGoal(cfgPV44.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STAR BOMB — tap for 250 pts × combo + blast normals within 160px; or auto-detonates on expiry
    if(hit.type==="star_bomb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo44b=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult44b=gs.feverActive?2:1;
      const blastNormals=targetsRef.current.filter(o=>!o.dying&&o.type==="normal"&&Math.hypot(o.x-hit.x,o.y-hit.y)<160).slice(0,5);
      blastNormals.forEach(o=>{o.dying=performance.now();gs.score+=Math.round(50*combo44b*feverMult44b);spawnParticles(o.x,o.y,"#fbbf24",6,"spark");});
      const basePts=Math.round(250*combo44b*feverMult44b);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#fbbf24",alpha:0.8,life:600});
      spawnPopup(hit.x,hit.y-32,`💥⭐ STAR BOMB! +${basePts}${blastNormals.length?" ×"+(blastNormals.length+1):""}` ,"#fbbf24",24);
      spawnParticles(hit.x,hit.y,"#fde68a",20,"spark");
      sfx("epic");vibrate([12,6,18,6,12]);
      unlock("starbomb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSB44=levelCfgRef.current;
      if(cfgSB44){if(gs.score>=cfgSB44.scoreGoal&&(!cfgSB44.modifier||checkModGoal(cfgSB44.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NOVA STAR — tap to sweep nearby normals; bonus if 3+ scored
    if(hit.type==="nova_star"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo46=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult46=gs.feverActive?2:1;
      const swept46=targetsRef.current.filter(o=>!o.dying&&o.type==="normal"&&Math.hypot(o.x-hit.x,o.y-hit.y)<160).slice(0,6);
      swept46.forEach(o=>{o.dying=performance.now();gs.score+=Math.round(60*combo46*feverMult46);spawnParticles(o.x,o.y,"#fbbf24",5,"spark");});
      const basePts46=Math.round(180*combo46*feverMult46);
      gs.score+=basePts46;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:220,born:performance.now(),color:"#fde68a",alpha:0.75,life:650});
      spawnPopup(hit.x,hit.y-32,`🌟 NOVA SWEEP! +${basePts46}${swept46.length?" ×"+(swept46.length+1):""}` ,"#fbbf24",24);
      spawnParticles(hit.x,hit.y,"#fde68a",18,"spark");
      sfx("epic");vibrate([10,5,14,5,10]);
      unlock("novastar_tap");
      if(swept46.length>=3)unlock("novastar_sweep");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNS46=levelCfgRef.current;
      if(cfgNS46){if(gs.score>=cfgNS46.scoreGoal&&(!cfgNS46.modifier||checkModGoal(cfgNS46.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ICE PHOENIX — tap before hatch (2.5s) for freeze+250pts; after hatch = 100 pts
    if(hit.type==="ice_phoenix"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const hatchPct46=hit._hatchPct||0;
      const combo46b=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult46b=gs.feverActive?2:1;
      const isEgg=hatchPct46<1;
      const pts46=Math.round((isEgg?250:100)*combo46b*feverMult46b);
      gs.score+=pts46;gs.streak++;gs.lastTapTime=Date.now();
      if(isEgg){
        // Freeze all normals for 2.5s
        const freezeExp46=Date.now()+2500;
        targetsRef.current.filter(o=>!o.dying&&o.type==="normal").forEach(o=>{
          o._anchorFrozenVx=o.vx;o._anchorFrozenVy=o.vy;o.vx=0;o.vy=0;o._frozenUntil=freezeExp46;
        });
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#a5f3fc",alpha:0.7,life:600});
        spawnPopup(hit.x,hit.y-32,"🧊🐦 CRYO! +"+pts46,"#a5f3fc",24);
        spawnParticles(hit.x,hit.y,"#a5f3fc",16,"spark");
        sfx("freeze");vibrate([8,4,12,4,8]);
      } else {
        spawnPopup(hit.x,hit.y-20,"🐦 HATCHED! +"+pts46,"#60a5fa",18);
        spawnParticles(hit.x,hit.y,"#60a5fa",10,"spark");
        sfx("tap");vibrate([5,3,7]);
      }
      unlock("icephoenix_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgIP46=levelCfgRef.current;
      if(cfgIP46){if(gs.score>=cfgIP46.scoreGoal&&(!cfgIP46.modifier||checkModGoal(cfgIP46.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SPIRAL ORB — bonus pts based on spiral distance (75%+ travel = epic bonus)
    if(hit.type==="spiral_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const spiralPct47=hit._spiralPct||0;
      const combo47=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult47=gs.feverActive?2:1;
      const isFar=spiralPct47>=0.75;
      const pts47=Math.round((isFar?280:120)*combo47*feverMult47);
      gs.score+=pts47;gs.streak++;gs.lastTapTime=Date.now();
      if(isFar){
        spawnPopup(hit.x,hit.y-32,"🌀💫 FAR SPIRAL! +"+pts47,"#34d399",24);
        spawnParticles(hit.x,hit.y,"#34d399",16,"spark");
        sfx("epic");vibrate([10,5,14,5,10]);
        unlock("spiralorb_far");
      } else {
        spawnPopup(hit.x,hit.y-20,"🌀 SPIRAL +"+pts47,"#6ee7b7",18);
        spawnParticles(hit.x,hit.y,"#6ee7b7",10,"spark");
        sfx("tap");vibrate([5,3,7]);
      }
      unlock("spiralorb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSO47=levelCfgRef.current;
      if(cfgSO47){if(gs.score>=cfgSO47.scoreGoal&&(!cfgSO47.modifier||checkModGoal(cfgSO47.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // DARK PULSE — pay -20 pts; receive 2 bonus normal targets worth 200 pts each
    if(hit.type==="dark_pulse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const gs47=gs;
      gs47.score=Math.max(0,gs47.score-20);
      gs47.streak++;gs47.lastTapTime=Date.now();
      // Spawn 2 juicy bonus normals
      const cw47d=canvasRef.current?.width||360;const ch47d=canvasRef.current?.height||640;
      const bonus47Spawned=[];
      for(let i=0;i<2;i++){
        const bx47=Math.max(30,Math.min(cw47d-30,hit.x+(i===0?-80:80)+Math.random()*40-20));
        const by47=Math.max(30,Math.min(ch47d-30,hit.y+Math.random()*60-30));
        const bn47={id:Math.random().toString(36).slice(2),type:"normal",
          rarity:RARITY.EPIC,x:bx47,y:by47,radius:BASE_R*1.2,color:"#a78bfa",glow:"#c4b5fd",
          lifetime:3000,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,trail:[],hitsLeft:1,maxHits:1,dying:null,
          _darkBonus:200,_darkGroup:hit.id,cx:bx47,cy:by47,worldId:hit.worldId,worldColor:hit.worldColor,bossPattern:null,bossPhase:1,pwrType:null};
        targetsRef.current.push(bn47);
        bonus47Spawned.push(bn47);
      }
      spawnPopup(hit.x,hit.y-28,"🌑 DARK DEAL! -20 → 2×200","#a78bfa",20);
      spawnParticles(hit.x,hit.y,"#7c3aed",14,"spark");
      sfx("chainBonus");vibrate([8,4,8,4,8]);
      unlock("darkpulse_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgDP47=levelCfgRef.current;
      if(cfgDP47){if(gs.score>=cfgDP47.scoreGoal&&(!cfgDP47.modifier||checkModGoal(cfgDP47.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LUNAR ECLIPSE — max size (>0.8) = 350 pts; shrinking = reduced
    if(hit.type==="lunar_eclipse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const sizePct48=hit._sizePct||0;
      const combo48=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult48=gs.feverActive?2:1;
      const isMax=sizePct48>=0.8;
      const pts48=Math.round((isMax?350:Math.max(60,Math.round(350*sizePct48)))*combo48*feverMult48);
      gs.score+=pts48;gs.streak++;gs.lastTapTime=Date.now();
      if(isMax){
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:220,born:performance.now(),color:"#fbbf24",alpha:0.75,life:650});
        spawnPopup(hit.x,hit.y-32,"🌕 FULL MOON! +"+pts48,"#fbbf24",26);
        spawnParticles(hit.x,hit.y,"#fbbf24",18,"spark");
        sfx("epic");vibrate([10,5,14,5,10]);
        unlock("lunareclipse_max");
      } else {
        spawnPopup(hit.x,hit.y-20,"🌑 ECLIPSE +"+pts48,"#94a3b8",18);
        spawnParticles(hit.x,hit.y,"#94a3b8",10,"spark");
        sfx("tap");vibrate([5,3,7]);
      }
      unlock("lunareclipse_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLE48=levelCfgRef.current;
      if(cfgLE48){if(gs.score>=cfgLE48.scoreGoal&&(!cfgLE48.modifier||checkModGoal(cfgLE48.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CHAIN WALL — 100 pts; if tapped within 1.5s of last tap, streak preserved; else breaks
    if(hit.type==="chain_wall"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo48b=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult48b=gs.feverActive?2:1;
      const pts48b=Math.round(100*combo48b*feverMult48b);
      const inChain=gs.lastTapTime&&(Date.now()-gs.lastTapTime<1500);
      gs.score+=pts48b;
      if(inChain){
        gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-24,"🧱 WALL PASS! +"+pts48b,"#fbbf24",20);
        spawnParticles(hit.x,hit.y,"#fbbf24",12,"spark");
        sfx("chainBonus");vibrate([6,3,8,3,6]);
      } else {
        gs.streak=0;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-20,"🧱 BLOCKED! +"+pts48b,"#ef4444",18);
        spawnParticles(hit.x,hit.y,"#ef4444",10,"spark");
        sfx("miss");vibrate([10,5,10]);
        setMascotMood("sad");setTimeout(()=>setMascotMood("idle"),1000);
      }
      unlock("chainwall_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCW48=levelCfgRef.current;
      if(cfgCW48){if(gs.score>=cfgCW48.scoreGoal&&(!cfgCW48.modifier||checkModGoal(cfgCW48.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // BOUNCE WEB — snares up to 3 nearest moving targets for 2s; 150 pts base
    if(hit.type==="bounce_web"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo49w=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult49w=gs.feverActive?2:1;
      const pts49w=Math.round(150*combo49w*feverMult49w);
      gs.score+=pts49w;gs.streak++;gs.lastTapTime=Date.now();
      // Snare up to 3 nearest moving targets
      const movers=targetsRef.current.filter(o=>!o.dying&&o.moving&&o.type!=="boss").sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y)).slice(0,3);
      movers.forEach(o=>{o._snaredUntil=Date.now()+2000;o._savedVx=o.vx;o._savedVy=o.vy;o.vx=0;o.vy=0;});
      spawnPopup(hit.x,hit.y-24,`🕸️ WEB! +${pts49w}`,"#a855f7",20);
      spawnParticles(hit.x,hit.y,"#c084fc",14,"spark");
      sfx("chainBonus");vibrate([5,3,8,3,5]);
      unlock("bounceweb_tap");
      if(movers.length>=3)unlock("bounceweb_snare");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgBW49=levelCfgRef.current;
      if(cfgBW49){if(gs.score>=cfgBW49.scoreGoal&&(!cfgBW49.modifier||checkModGoal(cfgBW49.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // METEOR SHOWER — tap to release 4 mini-meteors that auto-score after 1.5s each
    if(hit.type==="meteor_shower"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo49m=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult49m=gs.feverActive?2:1;
      const tapPts49m=Math.round(80*combo49m*feverMult49m);
      gs.score+=tapPts49m;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-24,`☄️ METEOR! +${tapPts49m}`,"#fb923c",20);
      spawnParticles(hit.x,hit.y,"#fb923c",14,"spark");
      sfx("feverStart");vibrate([6,3,9,3,6]);
      // Spawn 4 mini-meteors as fast-moving targets
      const cw49m=canvasRef.current?.width||360;const ch49m=canvasRef.current?.height||640;
      for(let i=0;i<4;i++){
        const ang49m=(i/4)*Math.PI*2+Math.random()*0.5;
        const mmx=Math.max(30,Math.min(cw49m-30,hit.x+Math.cos(ang49m)*60));
        const mmy=Math.max(30,Math.min(ch49m-30,hit.y+Math.sin(ang49m)*60));
        const miniMeteor={id:Math.random().toString(36).slice(2),type:"normal",
          rarity:RARITY.RARE,x:mmx,y:mmy,radius:BASE_R*0.55,
          color:"#fb923c",glow:"#ffedd5",lifetime:1800,spawnedAt:Date.now(),
          born:performance.now(),moving:true,ghost:false,
          vx:(Math.random()-0.5)*2.5,vy:(Math.random()-0.5)*2.5,
          trail:null,hitsLeft:1,maxHits:1,dying:null,
          worldId:hit.worldId,worldColor:hit.worldColor,
          bossPattern:null,bossPhase:1,cx:mmx,cy:mmy,
          anticipateMs:0,pwrType:null,_meteorBonus:Math.round(80*combo49m*feverMult49m),_meteorGroup:hit.id};
        targetsRef.current.push(miniMeteor);
      }
      unlock("meteorshower_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgMS49=levelCfgRef.current;
      if(cfgMS49){if(gs.score>=cfgMS49.scoreGoal&&(!cfgMS49.modifier||checkModGoal(cfgMS49.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PRISMATIC ORB — tap; if current hue within ±30° of target hue: 3× pts bonus
    if(hit.type==="prismatic_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo50p=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult50p=gs.feverActive?2:1;
      const basePts50p=Math.round(120*combo50p*feverMult50p);
      const curHue50=hit._curHue||0,targetHue50=hit._targetHue||0;
      const hueDiff=Math.abs(((curHue50-targetHue50+540)%360)-180);
      const isMatch=hueDiff<=30;
      const pts50p=isMatch?basePts50p*3:basePts50p;
      gs.score+=pts50p;gs.streak++;gs.lastTapTime=Date.now();
      if(isMatch){
        spawnPopup(hit.x,hit.y-28,`🔮 TRUE COLOR! ×3 +${pts50p}`,"#f0abfc",22);
        spawnParticles(hit.x,hit.y,"#e879f9",16,"spark");
        sfx("legendary");vibrate([5,3,8,3,5,3,8]);
        unlock("prismatic_bonus");
      } else {
        spawnPopup(hit.x,hit.y-22,`🔮 PRISM +${pts50p}`,"#e879f9",18);
        spawnParticles(hit.x,hit.y,"#e879f9",10,"spark");
        sfx("tap");vibrate([5,3,7]);
      }
      unlock("prismatic_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPO50=levelCfgRef.current;
      if(cfgPO50){if(gs.score>=cfgPO50.scoreGoal&&(!cfgPO50.modifier||checkModGoal(cfgPO50.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VOID TETHER — tap one; spawns linked partner nearby; tap partner = jackpot 500 pts
    if(hit.type==="void_tether"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo50v=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult50v=gs.feverActive?2:1;
      const cw50v=canvasRef.current?.width||360;const ch50v=canvasRef.current?.height||640;
      if(hit._tetherGroup){
        // Second tap — jackpot!
        const pts50v=Math.round(500*combo50v*feverMult50v);
        gs.score+=pts50v;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-28,`🌑 TETHER JACKPOT! +${pts50v}`,"#a78bfa",24);
        spawnParticles(hit.x,hit.y,"#a78bfa",20,"spark");
        sfx("jackpot");vibrate([8,4,12,4,8,4,12]);
        // Kill the first one too if still alive
        targetsRef.current=targetsRef.current.filter(t=>t._tetherGroup!==hit._tetherGroup);
      } else {
        // First tap — spawn linked partner
        const pts50v=Math.round(80*combo50v*feverMult50v);
        gs.score+=pts50v;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-22,`🌑 TETHER! +${pts50v}`,"#6d28d9",18);
        spawnParticles(hit.x,hit.y,"#a78bfa",10,"spark");
        sfx("chainBonus");vibrate([5,3,8]);
        const grpId50v=hit.id;
        const px50v=Math.max(40,Math.min(cw50v-40,hit.x+(Math.random()-0.5)*120));
        const py50v=Math.max(40,Math.min(ch50v-40,hit.y+(Math.random()-0.5)*120));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"void_tether",
          rarity:RARITY.EPIC,x:px50v,y:py50v,radius:BASE_R*1.15,
          color:"#6d28d9",glow:"#ede9fe",lifetime:3000,spawnedAt:Date.now(),
          born:performance.now(),moving:false,ghost:false,vx:0,vy:0,
          trail:null,hitsLeft:1,maxHits:1,dying:null,
          worldId:hit.worldId,worldColor:hit.worldColor,
          bossPattern:null,bossPhase:1,cx:px50v,cy:py50v,
          anticipateMs:0,pwrType:null,_tetherGroup:grpId50v});
      }
      unlock("voidtether_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVT50=levelCfgRef.current;
      if(cfgVT50){if(gs.score>=cfgVT50.scoreGoal&&(!cfgVT50.modifier||checkModGoal(cfgVT50.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL PULSAR — tap; if pulsePct>0.8 = legendary pts (×4)
    if(hit.type==="crystal_pulsar"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo51c=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult51c=gs.feverActive?2:1;
      const pPct=hit._pulsePct||0;
      const isPeak=pPct>0.8;
      const basePts51c=Math.round(100*combo51c*feverMult51c);
      const pts51c=isPeak?basePts51c*4:basePts51c;
      gs.score+=pts51c;gs.streak++;gs.lastTapTime=Date.now();
      if(isPeak){
        spawnPopup(hit.x,hit.y-28,`💎⚡ PEAK PULSE! ×4 +${pts51c}`,"#38bdf8",22);
        spawnParticles(hit.x,hit.y,"#e0f2fe",18,"spark");
        sfx("legendary");vibrate([5,3,8,3,5,3,8]);
        unlock("crystalpulsar_peak");
      } else {
        spawnPopup(hit.x,hit.y-22,`💎 PULSAR +${pts51c}`,"#38bdf8",18);
        spawnParticles(hit.x,hit.y,"#38bdf8",10,"spark");
        sfx("tap");vibrate([5,3,7]);
      }
      unlock("crystalpulsar_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCP51=levelCfgRef.current;
      if(cfgCP51){if(gs.score>=cfgCP51.scoreGoal&&(!cfgCP51.modifier||checkModGoal(cfgCP51.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SHADOW ECHO — dark: 60 pts; bright echo: 300 pts bonus
    if(hit.type==="shadow_echo"||hit.type==="shadow_echo_bright"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo51s=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult51s=gs.feverActive?2:1;
      const isEchoBright=hit.type==="shadow_echo_bright";
      const pts51s=isEchoBright?Math.round(300*combo51s*feverMult51s):Math.round(60*combo51s*feverMult51s);
      gs.score+=pts51s;gs.streak++;gs.lastTapTime=Date.now();
      if(isEchoBright){
        spawnPopup(hit.x,hit.y-28,`🌕 ECHO BONUS! +${pts51s}`,"#fbbf24",22);
        spawnParticles(hit.x,hit.y,"#fbbf24",16,"spark");
        sfx("rare");vibrate([6,3,8,3,6]);
      } else {
        spawnPopup(hit.x,hit.y-22,`🌒 SHADOW +${pts51s}`,"#6366f1",18);
        spawnParticles(hit.x,hit.y,"#6366f1",10,"spark");
        sfx("tap");vibrate([5,3,7]);
      }
      unlock("shadowecho_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSE51=levelCfgRef.current;
      if(cfgSE51){if(gs.score>=cfgSE51.scoreGoal&&(!cfgSE51.modifier||checkModGoal(cfgSE51.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // FLUX RING — expanding ring; thickness drives pts: early=big, late=small
    if(hit.type==="flux_ring"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo52f=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult52f=gs.feverActive?2:1;
      const agePct52=hit._agePct||0;
      const isEarly=agePct52<0.35;
      const ptsScale52=Math.max(0.2,1-agePct52*0.85);
      const pts52f=Math.round(200*ptsScale52*combo52f*feverMult52f);
      gs.score+=pts52f;gs.streak++;gs.lastTapTime=Date.now();
      if(isEarly){
        spawnPopup(hit.x,hit.y-28,`⭕ RING RUSH! +${pts52f}`,"#fde68a",22);
        spawnParticles(hit.x,hit.y,"#fbbf24",14,"spark");
        sfx("rare");vibrate([6,3,8,3,6]);
        unlock("fluxring_early");
      } else {
        spawnPopup(hit.x,hit.y-22,`⭕ RING +${pts52f}`,"#06b6d4",18);
        spawnParticles(hit.x,hit.y,"#67e8f9",10,"spark");
        sfx("tap");vibrate([5,3,7]);
      }
      unlock("fluxring_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgFR52=levelCfgRef.current;
      if(cfgFR52){if(gs.score>=cfgFR52.scoreGoal&&(!cfgFR52.modifier||checkModGoal(cfgFR52.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // QUANTUM FLARE — tap: spawn 3 decoys + 1 real; tapping decoy = 20pts; real = 300pts
    if(hit.type==="quantum_flare"||hit.type==="quantum_flare_decoy"){
      const isDecoy=hit.type==="quantum_flare_decoy";
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo52q=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult52q=gs.feverActive?2:1;
      if(isDecoy){
        // Decoy tap: small pts, don't kill real flares of same group
        const pts52qd=Math.round(20*combo52q*feverMult52q);
        gs.score+=pts52qd;
        spawnPopup(hit.x,hit.y-22,`❓ DECOY +${pts52qd}`,"#818cf8",16);
        spawnParticles(hit.x,hit.y,"#c7d2fe",8,"spark");
        sfx("tap");vibrate([3,2,5]);
      } else {
        // Real flare tap: big pts + kill all decoys of this group
        const pts52qr=Math.round(300*combo52q*feverMult52q);
        gs.score+=pts52qr;gs.streak++;gs.lastTapTime=Date.now();
        if(hit._flareGroup)targetsRef.current=targetsRef.current.filter(t=>t._flareGroup!==hit._flareGroup);
        spawnPopup(hit.x,hit.y-28,`🌟 QUANTUM FLARE! +${pts52qr}`,"#f0abfc",22);
        spawnParticles(hit.x,hit.y,"#f0abfc",16,"spark");
        sfx("legendary");vibrate([6,3,8,3,6]);
        // Spawn decoys on tap
        const cw52q=canvasRef.current?.width||360;const ch52q=canvasRef.current?.height||640;
        const grpId52q=hit.id;
        for(let i=0;i<3;i++){
          const ang52q=(i/3)*Math.PI*2+Math.random()*0.5;
          const dx52q=Math.max(30,Math.min(cw52q-30,hit.x+Math.cos(ang52q)*80));
          const dy52q=Math.max(30,Math.min(ch52q-30,hit.y+Math.sin(ang52q)*80));
          targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"quantum_flare_decoy",
            rarity:RARITY.UNCOMMON,x:dx52q,y:dy52q,radius:BASE_R*1.1,
            color:"#818cf8",glow:"#c7d2fe",lifetime:2500,spawnedAt:Date.now(),
            born:performance.now(),moving:false,ghost:false,vx:0,vy:0,
            trail:null,hitsLeft:1,maxHits:1,dying:null,
            worldId:hit.worldId,worldColor:hit.worldColor,
            bossPattern:null,bossPhase:1,cx:dx52q,cy:dy52q,
            anticipateMs:0,pwrType:null,_flareGroup:grpId52q});
        }
        unlock("quantumflare_tap");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgQF52=levelCfgRef.current;
      if(cfgQF52){if(gs.score>=cfgQF52.scoreGoal&&(!cfgQF52.modifier||checkModGoal(cfgQF52.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NEBULA CRYSTAL — 3 hits: each reduces pts; 3rd hit = jackpot 600pts
    if(hit.type==="nebula_crystal"){
      const combo53n=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult53n=gs.feverActive?2:1;
      const phase53n=(hit.maxHits||3)-(hit.hitsLeft||1);
      hit.hitsLeft=Math.max(0,(hit.hitsLeft||1)-1);
      const isKill=hit.hitsLeft<=0;
      if(isKill){
        // 3rd hit jackpot
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const pts53n=Math.round(600*combo53n*feverMult53n);
        gs.score+=pts53n;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-28,`💜 NEBULA CORE! +${pts53n}`,"#e9d5ff",24);
        spawnParticles(hit.x,hit.y,"#a855f7",20,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#7e22ce",size:130,born:performance.now(),duration:600,alpha:0.8});
        sfx("legendary");vibrate([8,4,12,4,8,4,12]);
        unlock("nebulacrystal_3");
      } else {
        // Phase crack
        const ptsPerPhase=[150,100];
        const pts53n=Math.round((ptsPerPhase[phase53n]||80)*combo53n*feverMult53n);
        gs.score+=pts53n;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-22,`💜 CRACK! +${pts53n}`,"#c084fc",18);
        spawnParticles(hit.x,hit.y,"#c084fc",10,"spark");
        sfx("tap");vibrate([5,3,8]);
      }
      unlock("nebulacrystal_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNC53=levelCfgRef.current;
      if(cfgNC53){if(gs.score>=cfgNC53.scoreGoal&&(!cfgNC53.modifier||checkModGoal(cfgNC53.modifier,gs))){endLevel(true);return;}}
      if(!isKill)return; // stays on screen for more hits
      return;
    }
    // TIME SHARD — visible phase = 2× pts; faded phase = normal pts
    if(hit.type==="time_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo53t=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult53t=gs.feverActive?2:1;
      const visPct53=hit._visPct||0.5;
      const isVisible=visPct53>0.7;
      const basePts53t=Math.round(100*combo53t*feverMult53t);
      const pts53t=isVisible?basePts53t*2:basePts53t;
      gs.score+=pts53t;gs.streak++;gs.lastTapTime=Date.now();
      if(isVisible){
        spawnPopup(hit.x,hit.y-28,`⏳ TIME BURST! ×2 +${pts53t}`,"#818cf8",22);
        spawnParticles(hit.x,hit.y,"#c7d2fe",14,"spark");
        sfx("rare");vibrate([6,3,8,3,6]);
      } else {
        spawnPopup(hit.x,hit.y-22,`⏳ SHARD +${pts53t}`,"#818cf8",16);
        spawnParticles(hit.x,hit.y,"#818cf8",8,"spark");
        sfx("tap");vibrate([4,2,6]);
      }
      unlock("timeshard_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTS53=levelCfgRef.current;
      if(cfgTS53){if(gs.score>=cfgTS53.scoreGoal&&(!cfgTS53.modifier||checkModGoal(cfgTS53.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GRAVITY NODE — tap releases all pulled targets (scored) + 50pts each
    if(hit.type==="gravity_node"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo54g=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult54g=gs.feverActive?2:1;
      const pulledIds=hit._pulledTargets||new Set();
      const pulled=targetsRef.current.filter(o=>pulledIds.has(o.id)&&!o.dying);
      const pullBonus=Math.round(pulled.length*50*combo54g*feverMult54g);
      const basePts54g=Math.round(100*combo54g*feverMult54g);
      const totalPts=basePts54g+pullBonus;
      gs.score+=totalPts;gs.streak++;gs.lastTapTime=Date.now();
      // Release all pulled targets with scatter
      pulled.forEach(o=>{const a=Math.random()*Math.PI*2;o.vx=Math.cos(a)*1.5;o.vy=Math.sin(a)*1.5;o.moving=true;});
      spawnPopup(hit.x,hit.y-28,`🌑 NODE! +${totalPts}`,"#a855f7",22);
      spawnParticles(hit.x,hit.y,"#c084fc",16,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#4c1d95",size:160,born:performance.now(),duration:600,alpha:0.7});
      sfx("chainBonus");vibrate([8,4,12,4,8]);
      unlock("gravitynode_tap");
      if(pulled.length>=4)unlock("gravitynode_pull");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGN54=levelCfgRef.current;
      if(cfgGN54){if(gs.score>=cfgGN54.scoreGoal&&(!cfgGN54.modifier||checkModGoal(cfgGN54.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SOLAR RING — tap: closeness to ring edge = better score
    if(hit.type==="solar_ring"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo59s=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult59s=gs.feverActive?2:1;
      const ringR59=hit._ringR||hit.radius;
      const tapDist59=Math.hypot(tapX-hit.x,tapY-hit.y);
      const edgeDist59=Math.abs(tapDist59-ringR59);
      const edgePct59=Math.max(0,1-edgeDist59/(ringR59*0.4));
      const isEdge59=edgePct59>0.6;
      const mult59s=Math.round(1+edgePct59*3); // 1× to 4×
      const pts59s=Math.round(120*mult59s*combo59s*feverMult59s);
      gs.score+=pts59s;gs.streak++;gs.lastTapTime=Date.now();
      if(isEdge59){
        spawnPopup(hit.x,hit.y-36,`🌞 RING EDGE ×${mult59s} +${pts59s}`,"#fbbf24",26);
        spawnParticles(hit.x,hit.y,"#fde68a",14,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:ringR59,maxR:ringR59*2,born:performance.now(),color:"#fbbf24",alpha:0.6,life:600});
        sfx("chainBonus");vibrate([8,4,12]);
        unlock("solarring_edge");
      } else {
        spawnPopup(hit.x,hit.y-26,`☀️ SOLAR +${pts59s}`,"#fbbf24",18);
        spawnParticles(hit.x,hit.y,"#fbbf24",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("solarring_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSR59=levelCfgRef.current;
      if(cfgSR59){if(gs.score>=cfgSR59.scoreGoal&&(!cfgSR59.modifier||checkModGoal(cfgSR59.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CHRONO VORTEX — tap gives time-freeze bonus (slows everything for 1.5s)
    if(hit.type==="chrono_vortex"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo59c=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult59c=gs.feverActive?2:1;
      const slowed59c=hit._slowedCount||0;
      const pts59c=Math.round((180+slowed59c*40)*combo59c*feverMult59c);
      gs.score+=pts59c;gs.streak++;gs.lastTapTime=Date.now();
      // Apply global slow for 1.5s
      targetsRef.current.forEach(o=>{if(!o.dying&&o.moving){o.vx=(o.vx||0)*0.3;o.vy=(o.vy||0)*0.3;}});
      spawnPopup(hit.x,hit.y-32,`⏳ VORTEX ×${slowed59c+1} +${pts59c}`,"#38bdf8",24);
      spawnParticles(hit.x,hit.y,"#7dd3fc",12,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#38bdf8",alpha:0.6,life:700});
      sfx("chainBonus");vibrate([6,3,10,3,6]);
      unlock("chronovortex_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCV59=levelCfgRef.current;
      if(cfgCV59){if(gs.score>=cfgCV59.scoreGoal&&(!cfgCV59.modifier||checkModGoal(cfgCV59.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // BONUS ORB — simple collect for +50pts
    if(hit.type==="bonus_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const pts66bo=Math.round(50*(gs.feverActive?2:1));
      gs.score+=pts66bo;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-18,`🎁 +${pts66bo}`,"#10b981",14);
      spawnParticles(hit.x,hit.y,"#6ee7b7",6,"dot");
      sfx("coin");vibrate(5);
      // Track bonus wave collection
      const bwRef=hit._bwRef;
      if(bwRef){
        bwRef.collected=(bwRef.collected||0)+1;
        bwRef.orbs=bwRef.orbs.filter(id=>id!==hit.id);
        if(bwRef.collected>=5)unlock("bonus_wave_all");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgBO66=levelCfgRef.current;
      if(cfgBO66){if(gs.score>=cfgBO66.scoreGoal&&(!cfgBO66.modifier||checkModGoal(cfgBO66.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // COSMIC WEB — center tap: jackpot if all nodes tapped; else partial score
    if(hit.type==="cosmic_web"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo66cw=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult66cw=gs.feverActive?2:1;
      const allNodesDone66=(hit._nodesLeft||6)===0;
      if(allNodesDone66){
        const pts66cw=Math.round(800*combo66cw*feverMult66cw);
        gs.score+=pts66cw;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-40,`🕸️ WEB JACKPOT! +${pts66cw}`,"#fbbf24",32);
        spawnParticles(hit.x,hit.y,"#fde68a",18,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:240,born:performance.now(),color:"#06b6d4",alpha:0.8,life:800});
        sfx("jackpot");vibrate([15,6,25,6,15]);
        unlock("cosmicweb_all");
      } else {
        const pts66cw2=Math.round((100+(hit._nodesTapped||0)*30)*combo66cw*feverMult66cw);
        gs.score+=pts66cw2;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-28,`🕸️ WEB +${pts66cw2}`,"#06b6d4",20);
        spawnParticles(hit.x,hit.y,"#67e8f9",10,"dot");
        sfx("tap");vibrate(6);
        // Remove remaining nodes
        if(hit._nodeIds)targetsRef.current=targetsRef.current.filter(o=>!hit._nodeIds.includes(o.id));
      }
      unlock("cosmicweb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCW66=levelCfgRef.current;
      if(cfgCW66){if(gs.score>=cfgCW66.scoreGoal&&(!cfgCW66.modifier||checkModGoal(cfgCW66.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // COSMIC NODE — tap reduces parent's node count; all 6 = unlock jackpot
    if(hit.type==="cosmic_node"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo66cn=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult66cn=gs.feverActive?2:1;
      const pts66cn=Math.round(60*combo66cn*feverMult66cn);
      gs.score+=pts66cn;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-18,`🔵 NODE +${pts66cn}`,"#38bdf8",14);
      spawnParticles(hit.x,hit.y,"#67e8f9",6,"dot");
      sfx("tap");vibrate(5);
      // Update parent
      const parentCW66=targetsRef.current.find(o=>o.id===hit._parentId&&o.type==="cosmic_web");
      if(parentCW66){
        parentCW66._nodesLeft=Math.max(0,(parentCW66._nodesLeft||6)-1);
        parentCW66._nodesTapped=(parentCW66._nodesTapped||0)+1;
        if(parentCW66._nodesLeft===0){
          spawnPopup(parentCW66.x,parentCW66.y-26,"🕸️ TAP CENTER!","#fbbf24",18);
          sfx("comboNote",8);vibrate([5,3,10]);
        }
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCN66=levelCfgRef.current;
      if(cfgCN66){if(gs.score>=cfgCN66.scoreGoal&&(!cfgCN66.modifier||checkModGoal(cfgCN66.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // WARP ORB — mid-warp tap = crit bonus
    if(hit.type==="glitch_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo72go=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult72go=gs.feverActive?2:1;
      const visible72=!!(hit._visible72);
      const mult72go=visible72?2:1;
      const pts72go=Math.round(95*mult72go*combo72go*feverMult72go);
      gs.score+=pts72go;gs.streak++;gs.lastTapTime=Date.now();
      if(visible72){
        spawnPopup(hit.x,hit.y-30,`👾 VISIBLE ×2 +${pts72go}`,"#34d399",26);
        spawnParticles(hit.x,hit.y,"#6ee7b7",14,"spark");
        sfx("chainBonus");vibrate([6,4,8,4,6]);
        unlock("glitchorb_vis");
      } else {
        spawnPopup(hit.x,hit.y-22,`👾 GLITCH +${pts72go}`,"#10b981",18);
        spawnParticles(hit.x,hit.y,"#a7f3d0",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("glitchorb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGO72=levelCfgRef.current;
      if(cfgGO72){if(gs.score>=cfgGO72.scoreGoal&&(!cfgGO72.modifier||checkModGoal(cfgGO72.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="solar_flare72"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo72sf=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult72sf=gs.feverActive?2:1;
      const brightPct72=hit._brightPct72||0;
      const atPeak72=brightPct72>0.85;
      const mult72sf=atPeak72?3:1;
      const pts72sf=Math.round(105*mult72sf*combo72sf*feverMult72sf);
      gs.score+=pts72sf;gs.streak++;gs.lastTapTime=Date.now();
      if(atPeak72){
        spawnPopup(hit.x,hit.y-34,`☀️ PEAK ×3 +${pts72sf}`,"#fbbf24",28);
        spawnParticles(hit.x,hit.y,"#fde68a",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#fbbf24",alpha:0.6,life:650});
        sfx("legendary");vibrate([8,4,12,4,8]);
        unlock("solarflare72_peak");
      } else {
        spawnPopup(hit.x,hit.y-24,`☀️ +${pts72sf}`,"#f59e0b",18);
        spawnParticles(hit.x,hit.y,"#fcd34d",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("solarflare72_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSF72=levelCfgRef.current;
      if(cfgSF72){if(gs.score>=cfgSF72.scoreGoal&&(!cfgSF72.modifier||checkModGoal(cfgSF72.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="crystal_burst"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo71cb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult71cb=gs.feverActive?2:1;
      const pts71cb=Math.round(60*combo71cb*feverMult71cb);
      gs.score+=pts71cb;gs.streak++;gs.lastTapTime=Date.now();
      // Spawn 4 shards
      const cw71cb=canvasRef.current?.width||360;const ch71cb=canvasRef.current?.height||640;
      const shardIds71=[];
      for(let si=0;si<4;si++){
        const sa=si*(Math.PI/2)+Math.PI/4;
        const sx71=Math.max(20,Math.min(cw71cb-20,hit.x+Math.cos(sa)*55));
        const sy71=Math.max(20,Math.min(ch71cb-20,hit.y+Math.sin(sa)*55));
        const sid71=Math.random().toString(36).slice(2);
        shardIds71.push(sid71);
        targetsRef.current.push({id:sid71,type:"crystal_shard71",rarity:null,
          x:sx71,y:sy71,radius:BASE_R*0.45,color:"#67e8f9",glow:"#cffafe",
          lifetime:2500,spawnedAt:Date.now(),born:performance.now(),
          moving:false,ghost:false,vx:0,vy:0,trail:null,
          hitsLeft:1,maxHits:1,dying:null,worldId:null,worldColor:null,
          bossPattern:null,bossPhase:1,cx:sx71,cy:sy71,anticipateMs:0,pwrType:null,
          _shardHue:si*90,_parentBurst:hit.id});
      }
      hit._shardIds=shardIds71;hit._shardsTapped=0;
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:110,born:performance.now(),color:"#67e8f9",alpha:0.6,life:500});
      spawnParticles(hit.x,hit.y,"#a5f3fc",16,"spark");
      spawnPopup(hit.x,hit.y-28,`💎 BURST +${pts71cb}`,"#67e8f9",22);
      sfx("chainBonus");vibrate([6,4,8]);
      unlock("crystalburst_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCB71=levelCfgRef.current;
      if(cfgCB71){if(gs.score>=cfgCB71.scoreGoal&&(!cfgCB71.modifier||checkModGoal(cfgCB71.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="crystal_shard71"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const pts71sh=Math.round(25*(gs.feverActive?2:1));
      gs.score+=pts71sh;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-18,`💎+${pts71sh}`,"#67e8f9",16);
      spawnParticles(hit.x,hit.y,"#a5f3fc",6,"dot");
      sfx("tap");vibrate(3);
      // Check if all 4 shards tapped
      const parentCB71=targetsRef.current.find(o=>o.type==="crystal_burst"&&o._shardIds?.includes(hit.id));
      if(parentCB71){
        parentCB71._shardsTapped=(parentCB71._shardsTapped||0)+1;
        if(parentCB71._shardsTapped>=4){unlock("crystalburst_all");}
      } else {
        // No parent (already gone) — check if sibling shards remain
        const siblingShards=targetsRef.current.filter(o=>o.type==="crystal_shard71"&&o._parentBurst===hit._parentBurst);
        if(siblingShards.length===0)unlock("crystalburst_all");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    if(hit.type==="time_rift"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo71tr=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult71tr=gs.feverActive?2:1;
      const timeLeft71=hit._effectiveTimeLeft||5000;
      const clutch71=timeLeft71<1000;
      const mult71tr=clutch71?4:1;
      const pts71tr=Math.round(120*mult71tr*combo71tr*feverMult71tr);
      gs.score+=pts71tr;gs.streak++;gs.lastTapTime=Date.now();
      if(clutch71){
        spawnPopup(hit.x,hit.y-38,`⏱️ CLUTCH ×4 +${pts71tr}`,"#fbbf24",30);
        spawnParticles(hit.x,hit.y,"#fde68a",18,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#a78bfa",alpha:0.7,life:700});
        sfx("jackpot");vibrate([10,4,16,4,10]);
        unlock("timerift_clutch");
      } else {
        spawnPopup(hit.x,hit.y-24,`⏱️ RIFT +${pts71tr}`,"#a78bfa",20);
        spawnParticles(hit.x,hit.y,"#ddd6fe",10,"dot");
        sfx("tap");vibrate(6);
      }
      unlock("timerift_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTR71=levelCfgRef.current;
      if(cfgTR71){if(gs.score>=cfgTR71.scoreGoal&&(!cfgTR71.modifier||checkModGoal(cfgTR71.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="arc_bloom"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo70ab=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult70ab=gs.feverActive?2:1;
      const bloomPct70=hit._bloomPct||0;
      const fullyOpen70=bloomPct70>0.85;
      const mult70ab=fullyOpen70?3:1;
      const pts70ab=Math.round(100*mult70ab*combo70ab*feverMult70ab);
      gs.score+=pts70ab;gs.streak++;gs.lastTapTime=Date.now();
      if(fullyOpen70){
        spawnPopup(hit.x,hit.y-34,`🌸 BLOOM ×3 +${pts70ab}`,"#f9a8d4",28);
        spawnParticles(hit.x,hit.y,"#fce7f3",16,"spark");
        sfx("legendary");vibrate([6,4,10,4]);
        unlock("arcbloom_full");
      } else {
        spawnPopup(hit.x,hit.y-24,`🌸 +${pts70ab}`,"#f472b6",18);
        spawnParticles(hit.x,hit.y,"#fda4af",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("arcbloom_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAB70=levelCfgRef.current;
      if(cfgAB70){if(gs.score>=cfgAB70.scoreGoal&&(!cfgAB70.modifier||checkModGoal(cfgAB70.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="neon_bolt"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo70nb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult70nb=gs.feverActive?2:1;
      const pts70nb=Math.round(90*combo70nb*feverMult70nb);
      gs.score+=pts70nb;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`⚡ BOLT +${pts70nb}`,"#34d399",22);
      spawnParticles(hit.x,hit.y,"#6ee7b7",12,"spark");
      sfx("chainBonus");vibrate([5,3,8]);
      unlock("neonbolt_tap");unlock("neonbolt_fast");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNB70=levelCfgRef.current;
      if(cfgNB70){if(gs.score>=cfgNB70.scoreGoal&&(!cfgNB70.modifier||checkModGoal(cfgNB70.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="prism_clock"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo69pc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult69pc=gs.feverActive?2:1;
      const matched69pc=!!(hit._matched);
      const mult69pc=matched69pc?3:1;
      const pts69pc=Math.round(105*mult69pc*combo69pc*feverMult69pc);
      gs.score+=pts69pc;gs.streak++;gs.lastTapTime=Date.now();
      if(matched69pc){
        spawnPopup(hit.x,hit.y-34,`🕐 MATCH ×3 +${pts69pc}`,"#818cf8",28);
        spawnParticles(hit.x,hit.y,"#e0e7ff",14,"spark");
        sfx("legendary");vibrate([6,4,10,4,6]);
        unlock("prismclock_match");
      } else {
        spawnPopup(hit.x,hit.y-24,`🕐 +${pts69pc}`,"#6366f1",18);
        spawnParticles(hit.x,hit.y,"#c7d2fe",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("prismclock_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPC69=levelCfgRef.current;
      if(cfgPC69){if(gs.score>=cfgPC69.scoreGoal&&(!cfgPC69.modifier||checkModGoal(cfgPC69.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="void_pulse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo69vp=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult69vp=gs.feverActive?2:1;
      const scalePct69=hit._scalePct||0;
      const atMax69=scalePct69>0.85;
      const mult69vp=atMax69?3:1;
      const pts69vp=Math.round(110*mult69vp*combo69vp*feverMult69vp);
      gs.score+=pts69vp;gs.streak++;gs.lastTapTime=Date.now();
      if(atMax69){
        spawnPopup(hit.x,hit.y-34,`🌑 MAX ×3 +${pts69vp}`,"#a855f7",28);
        spawnParticles(hit.x,hit.y,"#c4b5fd",14,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:170,born:performance.now(),color:"#7c3aed",alpha:0.6,life:650});
        sfx("chainBonus");vibrate([8,4,12]);
        unlock("voidpulse_max");
      } else {
        spawnPopup(hit.x,hit.y-24,`🌑 +${pts69vp}`,"#7c3aed",18);
        spawnParticles(hit.x,hit.y,"#ddd6fe",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("voidpulse_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVP69=levelCfgRef.current;
      if(cfgVP69){if(gs.score>=cfgVP69.scoreGoal&&(!cfgVP69.modifier||checkModGoal(cfgVP69.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="chain_nova"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo68cn=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult68cn=gs.feverActive?2:1;
      // Ring explosion — score all targets within 100px
      const ringTargets68=targetsRef.current.filter(o=>!o.dying&&o.type!=="bomb"&&o.type!=="boss"&&Math.hypot(o.x-hit.x,o.y-hit.y)<100);
      ringTargets68.forEach(o=>{o.dying=performance.now();});
      const ringBonus68=Math.round(ringTargets68.length*50*feverMult68cn);
      const pts68cn=Math.round(120*combo68cn*feverMult68cn)+ringBonus68;
      gs.score+=pts68cn;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#f43f5e",alpha:0.7,life:700});
      spawnParticles(hit.x,hit.y,"#fca5a5",16,"spark");
      if(ringTargets68.length>=3){
        spawnPopup(hit.x,hit.y-38,`💥 NOVA ×${ringTargets68.length} +${pts68cn}`,"#f43f5e",28);
        sfx("chainBonus");vibrate([8,4,12,4,8]);
        unlock("chainnova_ring");
      } else {
        spawnPopup(hit.x,hit.y-28,`💥 NOVA +${pts68cn}`,"#f43f5e",20);
        sfx("tap");vibrate(7);
      }
      unlock("chainnova_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCN68=levelCfgRef.current;
      if(cfgCN68){if(gs.score>=cfgCN68.scoreGoal&&(!cfgCN68.modifier||checkModGoal(cfgCN68.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="mirror_flash"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      // Also remove the decoy
      if(hit._decoyId)targetsRef.current=targetsRef.current.filter(t=>t.id!==hit._decoyId);
      const combo68mf=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult68mf=gs.feverActive?2:1;
      const pts68mf=Math.round(140*combo68mf*feverMult68mf);
      gs.score+=pts68mf;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-34,`🪞 REAL! ×2 +${pts68mf}`,"#f0abfc",28);
      spawnParticles(hit.x,hit.y,"#fae8ff",14,"spark");
      sfx("legendary");vibrate([6,4,10,4,6]);
      unlock("mirrorflash_tap");unlock("mirrorflash_true");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgMF68=levelCfgRef.current;
      if(cfgMF68){if(gs.score>=cfgMF68.scoreGoal&&(!cfgMF68.modifier||checkModGoal(cfgMF68.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="mirror_decoy"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const pts68md=0;
      gs.streak=Math.max(0,gs.streak-2); // penalty: lose combo
      spawnPopup(hit.x,hit.y-24,"❌ DECOY!","#6366f1",20);
      spawnParticles(hit.x,hit.y,"#c7d2fe",8,"dot");
      sfx("miss");vibrate([30,10,30]);
      // Also unlock tap achievement
      unlock("mirrorflash_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    if(hit.type==="dark_mirror"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const opPct67dm=hit._opacityPct||0;
      const peakHit67=opPct67dm>0.85;
      const combo67dm=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult67dm=gs.feverActive?2:1;
      const mult67dm=peakHit67?3:1;
      const pts67dm=Math.round(110*mult67dm*combo67dm*feverMult67dm);
      gs.score+=pts67dm;gs.streak++;gs.lastTapTime=Date.now();
      if(peakHit67){
        spawnPopup(hit.x,hit.y-34,`🪞 PEAK ×3 +${pts67dm}`,"#a78bfa",28);
        spawnParticles(hit.x,hit.y,"#ede9fe",14,"spark");
        sfx("legendary");vibrate([8,4,12]);
        unlock("darkmirror_peak");
      } else {
        spawnPopup(hit.x,hit.y-24,`🪞 +${pts67dm}`,"#7c3aed",18);
        spawnParticles(hit.x,hit.y,"#c4b5fd",8,"dot");
        sfx("tap");vibrate(6);
      }
      unlock("darkmirror_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgDM67=levelCfgRef.current;
      if(cfgDM67){if(gs.score>=cfgDM67.scoreGoal&&(!cfgDM67.modifier||checkModGoal(cfgDM67.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="storm_ball"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const spPct67sb=hit._speedPct||0;
      const earlyHit67=spPct67sb<0.5;
      const combo67sb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult67sb=gs.feverActive?2:1;
      const mult67sb=earlyHit67?2.5:1;
      const pts67sb=Math.round(100*mult67sb*combo67sb*feverMult67sb);
      gs.score+=pts67sb;gs.streak++;gs.lastTapTime=Date.now();
      if(earlyHit67){
        spawnPopup(hit.x,hit.y-34,`⚡ EARLY ×2.5 +${pts67sb}`,"#fbbf24",28);
        spawnParticles(hit.x,hit.y,"#fde68a",14,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:160,born:performance.now(),color:"#fbbf24",alpha:0.6,life:600});
        sfx("chainBonus");vibrate([6,4,10]);
        unlock("stormball_early");
      } else {
        spawnPopup(hit.x,hit.y-24,`⚡ STORM +${pts67sb}`,"#f59e0b",18);
        spawnParticles(hit.x,hit.y,"#fcd34d",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("stormball_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSB67=levelCfgRef.current;
      if(cfgSB67){if(gs.score>=cfgSB67.scoreGoal&&(!cfgSB67.modifier||checkModGoal(cfgSB67.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="heal_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo67ho=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult67ho=gs.feverActive?2:1;
      const pts67ho=Math.round(25*combo67ho*feverMult67ho);
      gs.score+=pts67ho;gs.streak++;gs.lastTapTime=Date.now();
      if(gs.lives<(levelCfgRef.current?.lives||3))gs.lives++;
      spawnPopup(hit.x,hit.y-24,`💚 HEAL +❤️`,"#22c55e",20);
      spawnParticles(hit.x,hit.y,"#86efac",10,"dot");
      sfx("powerUp");vibrate(5);
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgHO67=levelCfgRef.current;
      if(cfgHO67){if(gs.score>=cfgHO67.scoreGoal&&(!cfgHO67.modifier||checkModGoal(cfgHO67.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="warp_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo66wo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult66wo=gs.feverActive?2:1;
      const isWarping66=!!(hit._warping);
      const mult66wo=isWarping66?4:1;
      const pts66wo=Math.round(120*mult66wo*combo66wo*feverMult66wo);
      gs.score+=pts66wo;gs.streak++;gs.lastTapTime=Date.now();
      if(isWarping66){
        spawnPopup(hit.x,hit.y-34,`🌀 MID-WARP ×4 +${pts66wo}`,"#fbbf24",28);
        spawnParticles(hit.x,hit.y,"#fde68a",14,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:190,born:performance.now(),color:"#6366f1",alpha:0.7,life:650});
        sfx("chainBonus");vibrate([8,4,14,4,8]);
        unlock("warporb_crit");
      } else {
        spawnPopup(hit.x,hit.y-24,`🌀 WARP +${pts66wo}`,"#6366f1",18);
        spawnParticles(hit.x,hit.y,"#a5b4fc",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("warporb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgWO66=levelCfgRef.current;
      if(cfgWO66){if(gs.score>=cfgWO66.scoreGoal&&(!cfgWO66.modifier||checkModGoal(cfgWO66.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ARCANE RUNE — first tap primes; second matching symbol tap = jackpot
    if(hit.type==="arcane_rune"){
      const combo65ar=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult65ar=gs.feverActive?2:1;
      if(!hit._primed){
        // First tap — prime the rune
        hit._primed=true;
        hit._primedAt=Date.now();
        const pts65ar=Math.round(80*combo65ar*feverMult65ar);
        gs.score+=pts65ar;gs.streak++;gs.lastTapTime=Date.now();
        const symbols65=["🔥","💧","🌿","⚡"];
        spawnPopup(hit.x,hit.y-24,`🔮 ${symbols65[hit._symbol||0]} PRIMED!`,"#a78bfa",18);
        spawnParticles(hit.x,hit.y,"#c4b5fd",8,"dot");
        sfx("tap");vibrate([5,3,8]);
        unlock("arcanerune_tap");
      } else {
        // Second tap (within 3s) — jackpot!
        const withinWindow65=(Date.now()-(hit._primedAt||0))<3000;
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        if(withinWindow65){
          const pts65ar2=Math.round(500*combo65ar*feverMult65ar);
          gs.score+=pts65ar2;gs.streak++;gs.lastTapTime=Date.now();
          spawnPopup(hit.x,hit.y-36,`🔮 RUNE MATCH! +${pts65ar2}`,"#fbbf24",28);
          spawnParticles(hit.x,hit.y,"#fde68a",16,"spark");
          particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:220,born:performance.now(),color:"#a78bfa",alpha:0.7,life:700});
          sfx("jackpot");vibrate([12,6,20,6,12]);
          unlock("arcanerune_match");
        } else {
          const pts65ar3=Math.round(120*combo65ar*feverMult65ar);
          gs.score+=pts65ar3;gs.streak++;gs.lastTapTime=Date.now();
          spawnPopup(hit.x,hit.y-26,`🔮 RUNE +${pts65ar3}`,"#818cf8",18);
          sfx("rare");vibrate(6);
        }
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAR65=levelCfgRef.current;
      if(cfgAR65){if(gs.score>=cfgAR65.scoreGoal&&(!cfgAR65.modifier||checkModGoal(cfgAR65.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PULSE GEM — tap at pulse peak (pulsePct>0.85) for 4× score
    if(hit.type==="pulse_gem"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo65pg=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult65pg=gs.feverActive?2:1;
      const pulsePct65=hit._pulsePct||0;
      const isPeak65=pulsePct65>0.85;
      const mult65pg=isPeak65?4:Math.max(1,Math.round(1+pulsePct65*2));
      const pts65pg=Math.round(100*mult65pg*combo65pg*feverMult65pg);
      gs.score+=pts65pg;gs.streak++;gs.lastTapTime=Date.now();
      if(isPeak65){
        spawnPopup(hit.x,hit.y-34,`💎 PEAK ×4 +${pts65pg}`,"#fbbf24",28);
        spawnParticles(hit.x,hit.y,"#fde68a",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:190,born:performance.now(),color:"#f472b6",alpha:0.7,life:650});
        sfx("chainBonus");vibrate([8,4,14]);
        unlock("pulsegem_peak");
      } else {
        spawnPopup(hit.x,hit.y-24,`💎 PULSE ×${mult65pg} +${pts65pg}`,"#f472b6",18);
        spawnParticles(hit.x,hit.y,"#f9a8d4",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("pulsegem_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPG65=levelCfgRef.current;
      if(cfgPG65){if(gs.score>=cfgPG65.scoreGoal&&(!cfgPG65.modifier||checkModGoal(cfgPG65.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // HUE ORB — tap scores; match = bonus
    if(hit.type==="hue_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo64ho=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult64ho=gs.feverActive?2:1;
      const matched64=hit._matched||false;
      const mult64ho=matched64?3:1;
      const pts64ho=Math.round(100*mult64ho*combo64ho*feverMult64ho);
      gs.score+=pts64ho;gs.streak++;gs.lastTapTime=Date.now();
      if(matched64){
        spawnPopup(hit.x,hit.y-34,`🌈 MATCH! ×3 +${pts64ho}`,"#fbbf24",26);
        spawnParticles(hit.x,hit.y,"#fde68a",14,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#fbbf24",alpha:0.6,life:600});
        sfx("chainBonus");vibrate([8,4,12]);
        unlock("hue_orb_match");
      } else {
        spawnPopup(hit.x,hit.y-24,`🌑 HUE +${pts64ho}`,"#a78bfa",18);
        spawnParticles(hit.x,hit.y,"#c4b5fd",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("hue_orb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgHO64=levelCfgRef.current;
      if(cfgHO64){if(gs.score>=cfgHO64.scoreGoal&&(!cfgHO64.modifier||checkModGoal(cfgHO64.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LIGHTNING CHAIN 64 — tap chains to up to 5 nearest targets
    if(hit.type==="lightning_chain_64"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo64lc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult64lc=gs.feverActive?2:1;
      const pts64lc=Math.round(100*combo64lc*feverMult64lc);
      gs.score+=pts64lc;gs.streak++;gs.lastTapTime=Date.now();
      // Chain to nearest targets
      const chain64=targetsRef.current.filter(o=>!o.dying&&o.type!=="bomb"&&o.type!=="boss").sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y)).slice(0,5);
      let chainBonus64=0;
      chain64.forEach((o,i)=>{
        const delay64=i*80;
        setTimeout(()=>{
          if(o&&!o.dying){
            o.dying=performance.now();
            const cb=Math.round(60*(i+1)*(gs.feverActive?2:1));
            chainBonus64+=cb;
            gs.score+=cb;
            spawnParticles(o.x,o.y,"#facc15",6,"spark");
          }
        },delay64);
      });
      hit._chainCount=chain64.length;
      spawnPopup(hit.x,hit.y-30,`⚡ CHAIN ×${chain64.length} +${pts64lc}`,"#facc15",24);
      spawnParticles(hit.x,hit.y,"#fef9c3",12,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#facc15",alpha:0.65,life:650});
      sfx("chainBonus");vibrate([8,4,12,4,8]);
      unlock("lchain_64_tap");
      if(chain64.length>=5)unlock("lchain_64_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLC64=levelCfgRef.current;
      if(cfgLC64){if(gs.score>=cfgLC64.scoreGoal&&(!cfgLC64.modifier||checkModGoal(cfgLC64.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NEBULA CLOCK — tap scores + spawns dots; dots each score
    if(hit.type==="nebula_clock"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo63nc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult63nc=gs.feverActive?2:1;
      const pts63nc=Math.round(150*combo63nc*feverMult63nc);
      gs.score+=pts63nc;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`🌌 NEBULA +${pts63nc}`,"#818cf8",22);
      spawnParticles(hit.x,hit.y,"#c4b5fd",12,"spark");
      sfx("rare");vibrate([6,4,8]);
      unlock("nebulaclock_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNC63=levelCfgRef.current;
      if(cfgNC63){if(gs.score>=cfgNC63.scoreGoal&&(!cfgNC63.modifier||checkModGoal(cfgNC63.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NEBULA DOT — orbiting child dot; all 5 = jackpot
    if(hit.type==="nebula_dot"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo63nd=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult63nd=gs.feverActive?2:1;
      const pts63nd=Math.round(80*combo63nd*feverMult63nd);
      gs.score+=pts63nd;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-18,`🌌 DOT +${pts63nd}`,"#a78bfa",14);
      spawnParticles(hit.x,hit.y,"#c4b5fd",6,"dot");
      sfx("tap");vibrate(5);
      // Check jackpot
      const parentId63=hit._parentId;
      if(parentId63){
        const parentNC=targetsRef.current.find(o=>o.id===parentId63);
        if(parentNC){
          parentNC._dotsLeft=(parentNC._dotsLeft||5)-1;
          const remainDots63=targetsRef.current.filter(o=>o._parentId===parentId63&&o.type==="nebula_dot"&&!o.dying&&o.id!==hit.id).length;
          if(remainDots63===0&&parentNC._dotsLeft<=0){
            const jackpot63=Math.round(700*combo63nd*feverMult63nd);
            gs.score+=jackpot63;
            spawnPopup(hit.x,hit.y-46,`🌌 JACKPOT! +${jackpot63}`,"#fbbf24",30);
            particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:40,maxR:240,born:performance.now(),color:"#818cf8",alpha:0.8,life:800});
            sfx("jackpot");vibrate([12,6,20,6,12]);
            unlock("nebulaclock_all");
            targetsRef.current=targetsRef.current.filter(o=>o.id!==parentId63);
          }
        }
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgND63=levelCfgRef.current;
      if(cfgND63){if(gs.score>=cfgND63.scoreGoal&&(!cfgND63.modifier||checkModGoal(cfgND63.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ICE SHATTER — tap 3 times to shatter; score grows each hit
    if(hit.type==="ice_shatter"){
      const combo63is=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult63is=gs.feverActive?2:1;
      hit.hitsLeft=(hit.hitsLeft||3)-1;
      const hitNum63=3-hit.hitsLeft;
      const pts63is=Math.round(hitNum63*100*combo63is*feverMult63is);
      gs.score+=pts63is;gs.streak++;gs.lastTapTime=Date.now();
      if(hit.hitsLeft<=0){
        // Shattered!
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        spawnPopup(hit.x,hit.y-32,`🧊 SHATTERED! +${pts63is}`,"#67e8f9",26);
        spawnParticles(hit.x,hit.y,"#e0f2fe",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#67e8f9",alpha:0.7,life:700});
        sfx("chainBonus");vibrate([10,5,15,5,10]);
        unlock("iceshatter_done");
      } else {
        spawnPopup(hit.x,hit.y-22,`🧊 HIT! +${pts63is} (${hit.hitsLeft} left)`,"#38bdf8",16);
        spawnParticles(hit.x,hit.y,"#7dd3fc",8,"dot");
        sfx("tap");vibrate(6);
      }
      unlock("iceshatter_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgIS63=levelCfgRef.current;
      if(cfgIS63){if(gs.score>=cfgIS63.scoreGoal&&(!cfgIS63.modifier||checkModGoal(cfgIS63.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PLASMA BURST — tap fires 4 bolts in cardinal directions
    if(hit.type==="plasma_burst"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo62pb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult62pb=gs.feverActive?2:1;
      const pts62pb=Math.round(100*combo62pb*feverMult62pb);
      gs.score+=pts62pb;gs.streak++;gs.lastTapTime=Date.now();
      // Spawn 4 plasma bolts
      const cw62=canvasRef.current?.width||360;const ch62=canvasRef.current?.height||640;
      const boltIds62=[];
      for(let bi=0;bi<4;bi++){
        const ang62=bi*Math.PI/2;
        const bid62=Math.random().toString(36).slice(2);
        boltIds62.push(bid62);
        targetsRef.current.push({id:bid62,type:"plasma_bolt",rarity:null,
          x:Math.max(20,Math.min(cw62-20,hit.x+Math.cos(ang62)*30)),
          y:Math.max(20,Math.min(ch62-20,hit.y+Math.sin(ang62)*30)),
          radius:BASE_R*0.5,color:"#f97316",glow:"#ffedd5",
          lifetime:2000,spawnedAt:Date.now(),born:performance.now(),
          moving:true,ghost:false,
          vx:Math.cos(ang62)*1.8,vy:Math.sin(ang62)*1.8,
          trail:null,hitsLeft:1,maxHits:1,dying:null,
          worldId:hit.worldId,worldColor:hit.worldColor,
          bossPattern:null,bossPhase:1,cx:hit.x,cy:hit.y,anticipateMs:0,pwrType:null,
          _boltParentId:hit.id,_boltIdx:bi});
      }
      hit._boltsFired=true;hit._boltIds=boltIds62;
      spawnPopup(hit.x,hit.y-28,`💥 BURST +${pts62pb}`,"#f97316",22);
      spawnParticles(hit.x,hit.y,"#fdba74",12,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:160,born:performance.now(),color:"#f97316",alpha:0.6,life:500});
      sfx("epic");vibrate([8,4,12]);
      unlock("plasmaburst_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPB62=levelCfgRef.current;
      if(cfgPB62){if(gs.score>=cfgPB62.scoreGoal&&(!cfgPB62.modifier||checkModGoal(cfgPB62.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PLASMA BOLT — child bolt from plasma burst
    if(hit.type==="plasma_bolt"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo62bo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult62bo=gs.feverActive?2:1;
      const pts62bo=Math.round(80*combo62bo*feverMult62bo);
      gs.score+=pts62bo;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-20,`⚡ BOLT +${pts62bo}`,"#fb923c",16);
      spawnParticles(hit.x,hit.y,"#fdba74",7,"dot");
      sfx("tap");vibrate(5);
      // Check if all 4 bolts collected
      const parentId62=hit._boltParentId;
      if(parentId62){
        const remainBolts=targetsRef.current.filter(o=>o._boltParentId===parentId62&&o.type==="plasma_bolt"&&!o.dying).length;
        if(remainBolts===0){
          const allBonus62=Math.round(200*combo62bo*feverMult62bo);
          gs.score+=allBonus62;
          spawnPopup(hit.x,hit.y-44,`💥 ALL BOLTS! +${allBonus62}`,"#fbbf24",26);
          sfx("jackpot");vibrate([10,5,15,5,10]);
          unlock("plasmaburst_all");
        }
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgBO62=levelCfgRef.current;
      if(cfgBO62){if(gs.score>=cfgBO62.scoreGoal&&(!cfgBO62.modifier||checkModGoal(cfgBO62.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STAR CLOCK — countdown 5→0; tap at 0 = jackpot, else pts=countdown×20
    if(hit.type==="star_clock"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo62sc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult62sc=gs.feverActive?2:1;
      const countdown62=hit._countdown||0;
      const isZero62=countdown62<=0;
      if(isZero62){
        const pts62sc=Math.round(800*combo62sc*feverMult62sc);
        gs.score+=pts62sc;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-36,`⭐ ZERO HOUR! +${pts62sc}`,"#fbbf24",30);
        spawnParticles(hit.x,hit.y,"#fde68a",18,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:220,born:performance.now(),color:"#fbbf24",alpha:0.8,life:800});
        sfx("jackpot");vibrate([15,6,25,6,15]);
        unlock("starclock_zero");
      } else {
        const pts62sc2=Math.round(countdown62*30*combo62sc*feverMult62sc);
        gs.score+=pts62sc2;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-26,`⭐ CLOCK ×${countdown62} +${pts62sc2}`,"#38bdf8",20);
        spawnParticles(hit.x,hit.y,"#7dd3fc",10,"dot");
        sfx("tap");vibrate(6);
      }
      unlock("starclock_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSC62=levelCfgRef.current;
      if(cfgSC62){if(gs.score>=cfgSC62.scoreGoal&&(!cfgSC62.modifier||checkModGoal(cfgSC62.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VORTEX STAR 61 — 3 zones: inner=1×, mid=2×, outer=4×
    if(hit.type==="vortex_star_61"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo61vs=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult61vs=gs.feverActive?2:1;
      const tapDist61=Math.hypot(tapX-hit.x,tapY-hit.y);
      hit._lastTapDist=tapDist61;
      const zone61=tapDist61>hit.radius*2?2:tapDist61>hit.radius*1.4?1:0;
      const zoneMults=[1,2,4];
      const zoneMult61=zoneMults[zone61]||1;
      const pts61vs=Math.round(100*zoneMult61*combo61vs*feverMult61vs);
      gs.score+=pts61vs;gs.streak++;gs.lastTapTime=Date.now();
      const zoneNames=["INNER","MID","OUTER"];
      const zoneColors=["#ef4444","#f59e0b","#4ade80"];
      spawnPopup(hit.x,hit.y-30,`⭐ ${zoneNames[zone61]} ×${zoneMult61} +${pts61vs}`,zoneColors[zone61]||"#fbbf24",zone61===2?28:20);
      spawnParticles(hit.x,hit.y,"#fde68a",10,"spark");
      sfx(zone61===2?"chainBonus":"tap");vibrate(zone61===2?[8,4,12]:5);
      unlock("vortexstar_61_tap");
      if(zone61===2)unlock("vortexstar_61_out");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVS61=levelCfgRef.current;
      if(cfgVS61){if(gs.score>=cfgVS61.scoreGoal&&(!cfgVS61.modifier||checkModGoal(cfgVS61.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // QUANTUM DRIFT — first tap freezes; second tap in window = bonus
    if(hit.type==="quantum_drift"){
      const combo61qd=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult61qd=gs.feverActive?2:1;
      const frozen61=hit._frozenUntil&&Date.now()<hit._frozenUntil;
      if(frozen61){
        // Tap while frozen — jackpot
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const pts61qd=Math.round(300*combo61qd*feverMult61qd);
        gs.score+=pts61qd;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-34,`❄️ DRIFT FROZEN! +${pts61qd}`,"#38bdf8",28);
        spawnParticles(hit.x,hit.y,"#e0f2fe",14,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#38bdf8",alpha:0.65,life:650});
        sfx("chainBonus");vibrate([8,4,12,4,8]);
        unlock("qdrift_freeze");
      } else {
        // First tap — freeze the target
        hit._frozenUntil=Date.now()+2500;
        const pts61qd2=Math.round(80*combo61qd*feverMult61qd);
        gs.score+=pts61qd2;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-24,`⚛️ DRIFT STOP! +${pts61qd2}`,"#a78bfa",18);
        spawnParticles(hit.x,hit.y,"#c4b5fd",8,"dot");
        sfx("tap");vibrate([5,3,8]);
      }
      unlock("qdrift_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgQD61=levelCfgRef.current;
      if(cfgQD61){if(gs.score>=cfgQD61.scoreGoal&&(!cfgQD61.modifier||checkModGoal(cfgQD61.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PRISM NOVA — main tap spawns shards; all 3 shards = jackpot
    if(hit.type==="prism_nova"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo60pn=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult60pn=gs.feverActive?2:1;
      const pts60pn=Math.round(150*combo60pn*feverMult60pn);
      gs.score+=pts60pn;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`🌈 PRISM NOVA! +${pts60pn}`,"#f472b6",24);
      spawnParticles(hit.x,hit.y,"#f0abfc",14,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#f472b6",alpha:0.6,life:600});
      sfx("epic");vibrate([8,4,8]);
      unlock("prismnova_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPN60=levelCfgRef.current;
      if(cfgPN60){if(gs.score>=cfgPN60.scoreGoal&&(!cfgPN60.modifier||checkModGoal(cfgPN60.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PRISM SHARD — child of prism_nova; all 3 = jackpot
    if(hit.type==="prism_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo60ps=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult60ps=gs.feverActive?2:1;
      const pts60ps=Math.round(120*combo60ps*feverMult60ps);
      gs.score+=pts60ps;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-24,`💎 SHARD! +${pts60ps}`,"#a855f7",18);
      spawnParticles(hit.x,hit.y,`hsl(${hit._shardHue||0},100%,60%)`,10,"spark");
      sfx("rare");vibrate(6);
      // Check if parent exists and track collected shards
      const parentId60=hit._parentId;
      if(parentId60){
        const parentPrism60=targetsRef.current.find(o=>o.id===parentId60)||null;
        if(parentPrism60){
          parentPrism60._shardsTapped=(parentPrism60._shardsTapped||0)+1;
          const siblingShards60=targetsRef.current.filter(o=>o._parentId===parentId60&&o.type==="prism_shard"&&!o.dying&&o.id!==hit.id);
          if(siblingShards60.length===0){
            // All 3 shards collected — jackpot
            const jackpot60=Math.round(600*combo60ps*feverMult60ps);
            gs.score+=jackpot60;
            spawnPopup(hit.x,hit.y-50,`🌈 JACKPOT! +${jackpot60}`,"#fbbf24",30);
            particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:240,born:performance.now(),color:"#fbbf24",alpha:0.8,life:800});
            sfx("jackpot");vibrate([12,6,20,6,12]);
            unlock("prismnova_shards");
            targetsRef.current=targetsRef.current.filter(o=>o.id!==parentId60);
          }
        }
      }
      unlock("prismnova_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPS60=levelCfgRef.current;
      if(cfgPS60){if(gs.score>=cfgPS60.scoreGoal&&(!cfgPS60.modifier||checkModGoal(cfgPS60.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ECHO BLAST — first tap spawns ring; taps within ring area = bonus
    if(hit.type==="echo_blast"){
      const combo60eb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult60eb=gs.feverActive?2:1;
      if(!hit._ringSpawned){
        // First tap — spawn echo ring, give base score
        hit._ringSpawned=true;
        hit._echoRing=true;
        hit._echoRingR=hit.radius*2.2;
        hit._echoRingSpawnAt=Date.now();
        const pts60eb=Math.round(80*combo60eb*feverMult60eb);
        gs.score+=pts60eb;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-24,`🔊 ECHO RING! +${pts60eb}`,"#8b5cf6",20);
        spawnParticles(hit.x,hit.y,"#c4b5fd",10,"dot");
        sfx("tap");vibrate([5,3,8]);
        unlock("echoblast_tap");
        gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
        const cfgEB60a=levelCfgRef.current;
        if(cfgEB60a){if(gs.score>=cfgEB60a.scoreGoal&&(!cfgEB60a.modifier||checkModGoal(cfgEB60a.modifier,gs))){endLevel(true);return;}}
        return;
      } else {
        // Second tap (within ring) — bonus
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const pts60eb2=Math.round(200*combo60eb*feverMult60eb);
        gs.score+=pts60eb2;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-32,`🔊 RING RIDER +${pts60eb2}`,"#f59e0b",24);
        spawnParticles(hit.x,hit.y,"#fde68a",12,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit._echoRingR||80,maxR:200,born:performance.now(),color:"#f59e0b",alpha:0.65,life:650});
        sfx("chainBonus");vibrate([8,4,12]);
        unlock("echoblast_ring");
        gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
        const cfgEB60b=levelCfgRef.current;
        if(cfgEB60b){if(gs.score>=cfgEB60b.scoreGoal&&(!cfgEB60b.modifier||checkModGoal(cfgEB60b.modifier,gs))){endLevel(true);return;}}
        return;
      }
    }
    // NOVA CRYSTAL — 3-stage charge; stage 2 = explosive bonus
    if(hit.type==="nova_crystal"){
      const combo58n=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult58n=gs.feverActive?2:1;
      const stage58n=hit._stage||0;
      const isFull58n=stage58n>=2;
      if(isFull58n){
        // Full charge — explosive finale
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const pts58n=Math.round(500*combo58n*feverMult58n);
        gs.score+=pts58n;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-36,`💎 FULL NOVA! +${pts58n}`,"#fbbf24",28);
        spawnParticles(hit.x,hit.y,"#fde68a",18,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:220,born:performance.now(),color:"#a78bfa",alpha:0.7,life:700});
        sfx("legendary");vibrate([12,6,20,6,12]);
        unlock("novacrystal_full");
      } else {
        // Partial charge — advance stage
        hit.hitsLeft=(hit.hitsLeft||3)-1;
        hit._stage=Math.min(2,(hit._stage||0)+1);
        const pts58n2=Math.round((80+stage58n*60)*combo58n*feverMult58n);
        gs.score+=pts58n2;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-26,`💙 CHARGE ${stage58n+1}/3 +${pts58n2}`,"#60a5fa",18);
        spawnParticles(hit.x,hit.y,"#93c5fd",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("novacrystal_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNC58=levelCfgRef.current;
      if(cfgNC58){if(gs.score>=cfgNC58.scoreGoal&&(!cfgNC58.modifier||checkModGoal(cfgNC58.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PHASE SHIFT — only tappable when solid (cyclePct < 0.5)
    if(hit.type==="phase_shift"){
      const combo58p=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult58p=gs.feverActive?2:1;
      const isSolid58p=hit._isSolid!==undefined?hit._isSolid:true;
      if(!isSolid58p){
        // Tapped in ghost phase — near-miss, no penalty but no score
        spawnPopup(hit.x,hit.y-20,"👻 PHASED OUT!","#818cf8",14);
        spawnParticles(hit.x,hit.y,"#c7d2fe",5,"dot");
        sfx("miss");
        return;
      }
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const pts58p=Math.round(200*combo58p*feverMult58p);
      gs.score+=pts58p;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`👁️ SOLID! +${pts58p}`,"#f0abfc",20);
      spawnParticles(hit.x,hit.y,"#f0abfc",10,"spark");
      sfx("tap");vibrate(6);
      unlock("phaseshift_tap");unlock("phaseshift_solid");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPS58=levelCfgRef.current;
      if(cfgPS58){if(gs.score>=cfgPS58.scoreGoal&&(!cfgPS58.modifier||checkModGoal(cfgPS58.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STELLAR ORB — grows over 6s; tap at >85% size for legendary pts
    if(hit.type==="stellar_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo57s=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult57s=gs.feverActive?2:1;
      const sizePct57s=hit._sizePct||0;
      const isMax57s=sizePct57s>0.85;
      const mult57s=isMax57s?4:1;
      const pts57s=Math.round(200*mult57s*combo57s*feverMult57s);
      gs.score+=pts57s;gs.streak++;gs.lastTapTime=Date.now();
      if(isMax57s){
        spawnPopup(hit.x,hit.y-36,`🌟 STELLAR MAX! ×4 +${pts57s}`,"#ffd700",26);
        spawnParticles(hit.x,hit.y,"#fde68a",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#fbbf24",alpha:0.7,life:700});
        sfx("legendary");vibrate([14,7,22,7,14]);
        unlock("stellarorb_max");
      } else {
        spawnPopup(hit.x,hit.y-26,`⭐ STELLAR +${pts57s}`,"#fbbf24",18);
        spawnParticles(hit.x,hit.y,"#fbbf24",8,"spark");
        sfx("tap");vibrate(5);
      }
      unlock("stellarorb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSO57=levelCfgRef.current;
      if(cfgSO57){if(gs.score>=cfgSO57.scoreGoal&&(!cfgSO57.modifier||checkModGoal(cfgSO57.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // RUNE GATE — tap A then B within 2s for jackpot
    if(hit.type==="rune_gate_a"||hit.type==="rune_gate_b"){
      const combo57r=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult57r=gs.feverActive?2:1;
      if(hit.type==="rune_gate_a"){
        // First tap — prime the gate
        hit._primed=true;hit._primedAt=Date.now();
        // Prime the partner too
        const partnerB57=targetsRef.current.find(o=>o.id===hit._partnerId&&!o.dying);
        if(partnerB57)partnerB57._primed=true;
        const pts57r=Math.round(80*combo57r*feverMult57r);
        gs.score+=pts57r;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-26,"✨ GATE A — TAP B!","#818cf8",18);
        spawnParticles(hit.x,hit.y,"#818cf8",8,"dot");
        sfx("tap");vibrate(5);
        unlock("runegate_tap");
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      } else {
        // Second tap — check if gate A was primed within 2s
        const partnerA57=targetsRef.current.find(o=>o.id===hit._partnerId&&!o.dying);
        const withinWindow57=partnerA57?._primed||hit._primed;
        const primedAt57=partnerA57?._primedAt||(hit._primedAt||0);
        const inTime57=Date.now()-primedAt57<2000;
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id&&t.id!==hit._partnerId);
        if(withinWindow57&&inTime57){
          const jackpot57=Math.round(700*combo57r*feverMult57r);
          gs.score+=jackpot57;gs.streak++;gs.lastTapTime=Date.now();
          spawnPopup(hit.x,hit.y-36,`🔮 RUNE CHAIN! +${jackpot57}`,"#fbbf24",28);
          spawnParticles(hit.x,hit.y,"#e879f9",16,"spark");
          particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#818cf8",alpha:0.7,life:700});
          sfx("chainBonus");vibrate([8,4,12,4,8]);
          unlock("runegate_chain");
        } else {
          const pts57rb=Math.round(80*combo57r*feverMult57r);
          gs.score+=pts57rb;gs.streak++;gs.lastTapTime=Date.now();
          spawnPopup(hit.x,hit.y-26,`🔮 GATE B +${pts57rb}`,"#f472b6",18);
          spawnParticles(hit.x,hit.y,"#f472b6",8,"dot");
          sfx("tap");vibrate(5);
        }
        unlock("runegate_tap");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgRG57=levelCfgRef.current;
      if(cfgRG57){if(gs.score>=cfgRG57.scoreGoal&&(!cfgRG57.modifier||checkModGoal(cfgRG57.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ARC PULSE — tap in arc sweet zone for 3× pts
    if(hit.type==="arc_pulse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo56a=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult56a=gs.feverActive?2:1;
      const inZone56a=hit._inZone||false;
      const mult56a=inZone56a?3:1;
      const pts56a=Math.round(160*mult56a*combo56a*feverMult56a);
      gs.score+=pts56a;gs.streak++;gs.lastTapTime=Date.now();
      if(inZone56a){
        spawnPopup(hit.x,hit.y-32,`⚡ ARC ZONE! ×3 +${pts56a}`,"#fbbf24",24);
        spawnParticles(hit.x,hit.y,"#fde68a",14,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:160,born:performance.now(),color:"#38bdf8",alpha:0.6,life:600});
        sfx("chainBonus");vibrate([8,4,12]);
        unlock("arcpulse_arc");
      } else {
        spawnPopup(hit.x,hit.y-26,`💠 ARC +${pts56a}`,"#38bdf8",16);
        spawnParticles(hit.x,hit.y,"#7dd3fc",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("arcpulse_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAP56=levelCfgRef.current;
      if(cfgAP56){if(gs.score>=cfgAP56.scoreGoal&&(!cfgAP56.modifier||checkModGoal(cfgAP56.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VOID BLOOM — tap main bloom to activate, then tap 4 petals for jackpot
    if(hit.type==="void_bloom"||hit.type==="void_petal"){
      const combo56v=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult56v=gs.feverActive?2:1;
      if(hit.type==="void_bloom"){
        const pts56v=Math.round(100*combo56v*feverMult56v);
        gs.score+=pts56v;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-26,`🌀 BLOOM TAP +${pts56v}`,"#c084fc",16);
        spawnParticles(hit.x,hit.y,"#c084fc",8,"dot");
        sfx("tap");vibrate(5);
        unlock("voidbloom_tap");
        // If petals not spawned yet, trigger immediate bloom
        if(!hit._petalsSpawned){
          hit._petalsSpawned=false; // RAF will trigger at 1.5s naturally
        }
      } else {
        // Petal tapped
        const mainBloom56=targetsRef.current.find(o=>o.id===hit._bloomGroupId&&!o.dying);
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const pts56p=Math.round(80*combo56v*feverMult56v);
        gs.score+=pts56p;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-22,`🌸 +${pts56p}`,"#e879f9",14);
        spawnParticles(hit.x,hit.y,"#e879f9",6,"dot");
        sfx("tap");vibrate(4);
        if(mainBloom56){
          if(!mainBloom56._bloomWindow)mainBloom56._bloomWindow=Date.now();
          mainBloom56._bloomTaps=(mainBloom56._bloomTaps||0)+1;
          // Check if all 4 petals collected
          const allGone=mainBloom56._petalIds&&mainBloom56._petalIds.every(pid=>{
            return !targetsRef.current.find(o=>o.id===pid&&!o.dying);
          });
          const withinWindow=mainBloom56._bloomWindow&&(Date.now()-mainBloom56._bloomWindow)<2000;
          if(allGone&&withinWindow){
            targetsRef.current=targetsRef.current.filter(t=>t.id!==mainBloom56.id);
            const jackpot56=Math.round(600*combo56v*feverMult56v);
            gs.score+=jackpot56;
            spawnPopup(mainBloom56.x,mainBloom56.y-38,`🌸×4 FULL BLOOM! +${jackpot56}`,"#fbbf24",28);
            spawnParticles(mainBloom56.x,mainBloom56.y,"#e879f9",16,"spark");
            particlesRef.current.push({type:"shockwave",x:mainBloom56.x,y:mainBloom56.y,r:mainBloom56.radius,maxR:200,born:performance.now(),color:"#c084fc",alpha:0.7,life:800});
            sfx("legendary");vibrate([8,4,12,4,8]);
            unlock("voidbloom_full");
            const cfgVB56=levelCfgRef.current;
            if(cfgVB56){if(gs.score>=cfgVB56.scoreGoal&&(!cfgVB56.modifier||checkModGoal(cfgVB56.modifier,gs))){endLevel(true);return;}}
            return;
          }
        }
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVB56b=levelCfgRef.current;
      if(cfgVB56b){if(gs.score>=cfgVB56b.scoreGoal&&(!cfgVB56b.modifier||checkModGoal(cfgVB56b.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL WEB — tap main + 2 nodes within 1.5s for chain jackpot
    if(hit.type==="crystal_web"||hit.type==="crystal_web_node"){
      const combo55c=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult55c=gs.feverActive?2:1;
      if(hit.type==="crystal_web"){
        // Tap main web orb — register tap start, 150pts
        hit._webTaps=(hit._webTaps||0)+1;
        hit._webStartTime=Date.now();
        const pts55c=Math.round(150*combo55c*feverMult55c);
        gs.score+=pts55c;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-28,`🔷 WEB +${pts55c}`,"#22d3ee",16);
        spawnParticles(hit.x,hit.y,"#22d3ee",6,"dot");
        sfx("tap");vibrate(5);
        unlock("crystalweb_tap");
      } else {
        // Tap a crystal_web_node — find main web
        const mainWeb55=targetsRef.current.find(o=>o.id===hit._webGroupId&&!o.dying);
        hit._tapped55=true;
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const pts55n=Math.round(100*combo55c*feverMult55c);
        gs.score+=pts55n;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-24,`💎 +${pts55n}`,"#67e8f9",14);
        spawnParticles(hit.x,hit.y,"#67e8f9",5,"dot");
        sfx("tap");vibrate(4);
        if(mainWeb55){
          mainWeb55._webTaps=(mainWeb55._webTaps||0)+1;
          // Check if all 3 parts tapped (main + 2 nodes) within 1.5s
          const allNodesTapped=mainWeb55._nodeIds&&mainWeb55._nodeIds.every(nid=>{
            const nd=targetsRef.current.find(o=>o.id===nid);
            return !nd||nd._tapped55;
          });
          const withinWindow=mainWeb55._webStartTime&&(Date.now()-mainWeb55._webStartTime)<1500;
          if(allNodesTapped&&withinWindow&&mainWeb55._webTaps>=3){
            // JACKPOT — all 3 in time
            targetsRef.current=targetsRef.current.filter(t=>t.id!==mainWeb55.id&&!mainWeb55._nodeIds?.includes(t.id));
            const jackpotPts=Math.round(800*combo55c*feverMult55c);
            gs.score+=jackpotPts;
            spawnPopup(mainWeb55.x,mainWeb55.y-36,`🔷×3 CHAIN! +${jackpotPts}`,"#fbbf24",28);
            spawnParticles(mainWeb55.x,mainWeb55.y,"#22d3ee",16,"spark");
            particlesRef.current.push({type:"shockwave",x:mainWeb55.x,y:mainWeb55.y,r:mainWeb55.radius,maxR:200,born:performance.now(),color:"#06b6d4",alpha:0.7,life:700});
            sfx("chainBonus");vibrate([8,4,12,4,8]);
            unlock("crystalweb_chain");
            const cfgCW55=levelCfgRef.current;
            if(cfgCW55){if(gs.score>=cfgCW55.scoreGoal&&(!cfgCW55.modifier||checkModGoal(cfgCW55.modifier,gs))){endLevel(true);return;}}
            return;
          }
        }
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCW55b=levelCfgRef.current;
      if(cfgCW55b){if(gs.score>=cfgCW55b.scoreGoal&&(!cfgCW55b.modifier||checkModGoal(cfgCW55b.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LIGHTNING CAGE — score 80pts per trapped normal target inside cage radius
    if(hit.type==="lightning_cage"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo54l=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult54l=gs.feverActive?2:1;
      const cageR54l=hit.radius*2.5;
      const trapped54l=targetsRef.current.filter(o=>!o.dying&&o.type==="normal"&&Math.hypot(o.x-hit.x,o.y-hit.y)<cageR54l);
      const cagePts=Math.round((80*trapped54l.length+100)*combo54l*feverMult54l);
      gs.score+=cagePts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`⚡ CAGE! ×${trapped54l.length} +${cagePts}`,"#fbbf24",22);
      spawnParticles(hit.x,hit.y,"#fde68a",14,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#fbbf24",size:cageR54l*2,born:performance.now(),duration:500,alpha:0.7});
      sfx("chainBonus");vibrate([6,3,8,3,6]);
      unlock("lightningcage_tap");
      if(trapped54l.length>=5)unlock("lightningcage_all");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLC54=levelCfgRef.current;
      if(cfgLC54){if(gs.score>=cfgLC54.scoreGoal&&(!cfgLC54.modifier||checkModGoal(cfgLC54.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GLOW SHARD — splits into 3 colorful fragments; each fragment is tappable for bonus
    if(hit.type==="glow_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo45=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult45=gs.feverActive?2:1;
      const pts45=Math.round(120*combo45*feverMult45);
      gs.score+=pts45;gs.streak++;gs.lastTapTime=Date.now();
      // Spawn 3 glowing fragments
      const cw45=canvasRef.current?.width||360;const ch45=canvasRef.current?.height||640;
      const fragColors=["#f0abfc","#67e8f9","#fde68a"];
      const grpId45=hit.id;
      for(let i=0;i<3;i++){
        const ang=i*(Math.PI*2/3)+Math.random()*0.5;
        const dist45=40+Math.random()*40;
        const fx=Math.max(20,Math.min(cw45-20,hit.x+Math.cos(ang)*dist45));
        const fy=Math.max(20,Math.min(ch45-20,hit.y+Math.sin(ang)*dist45));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"glow_fragment",
          rarity:RARITY.RARE,x:fx,y:fy,radius:BASE_R*0.5,color:fragColors[i],glow:"#fdf4ff",
          lifetime:2500,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,trail:[],hitsLeft:1,maxHits:1,dying:null,
          _fragColor:fragColors[i],_shardGroup:grpId45,cx:fx,cy:fy,worldId:hit.worldId,worldColor:hit.worldColor,bossPattern:null,bossPhase:1,pwrType:null});
      }
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#f0abfc",alpha:0.65,life:600});
      spawnPopup(hit.x,hit.y-28,"✨ SHARD SPLIT! +"+pts45,"#f0abfc",22);
      spawnParticles(hit.x,hit.y,"#f0abfc",14,"spark");
      sfx("chainBonus");vibrate([8,4,12,4,8]);
      unlock("glowshard_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGS45=levelCfgRef.current;
      if(cfgGS45){if(gs.score>=cfgGS45.scoreGoal&&(!cfgGS45.modifier||checkModGoal(cfgGS45.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GLOW FRAGMENT — mini fragment from glow shard; 80 pts, bonus if all 3 collected
    if(hit.type==="glow_fragment"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo45b=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult45b=gs.feverActive?2:1;
      const pts45b=Math.round(80*combo45b*feverMult45b);
      gs.score+=pts45b;gs.streak++;gs.lastTapTime=Date.now();
      spawnParticles(hit.x,hit.y,hit._fragColor||"#f0abfc",8,"spark");
      spawnPopup(hit.x,hit.y-14,`✨+${pts45b}`,"#f0abfc",14);
      sfx("tap");vibrate([4,3,6]);
      // Check if all fragments from same group collected
      const remaining45=targetsRef.current.filter(o=>!o.dying&&o.type==="glow_fragment"&&o._shardGroup===hit._shardGroup);
      if(remaining45.length===0){
        gs.score+=200;
        spawnPopup(hit.x,hit.y-32,"✨✨✨ FULL SET! +200","#fde68a",22);
        sfx("legendary");unlock("glowshard_full");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGF45=levelCfgRef.current;
      if(cfgGF45){if(gs.score>=cfgGF45.scoreGoal&&(!cfgGF45.modifier||checkModGoal(cfgGF45.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // QUANTUM LEECH — reclaim 150 pts + all drained pts; drain increases over time
    if(hit.type==="quantum_leech"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo45c=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult45c=gs.feverActive?2:1;
      const drainedBack=hit._drainedTotal||0;
      const pts45c=Math.round((150+drainedBack)*combo45c*feverMult45c);
      gs.score+=pts45c;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-32,`🧿 RECLAIMED! +${pts45c}${drainedBack?" (+"+(drainedBack)+" drain)":""}` ,"#4ade80",22);
      spawnParticles(hit.x,hit.y,"#4ade80",14,"spark");
      sfx("chainBonus");vibrate([8,4,12,4,8]);
      unlock("quantumleech_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgQL45=levelCfgRef.current;
      if(cfgQL45){if(gs.score>=cfgQL45.scoreGoal&&(!cfgQL45.modifier||checkModGoal(cfgQL45.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ENERGY ORB (energy_orb2) — charges; 80%+ tap boosts all normals 2× for 4s
    if(hit.type==="energy_orb2"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const chargePct=hit._chargePct||0;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const isCharged=chargePct>=0.8;
      const pts=Math.round((isCharged?350:100)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      if(isCharged){
        // Boost all normal targets for 4s
        const boostExpiry=Date.now()+4000;
        targetsRef.current.filter(o=>!o.dying&&o.type==="normal").forEach(o=>{o._energyBoosted=boostExpiry;});
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:260,born:performance.now(),color:"#06b6d4",alpha:0.7,life:700});
        spawnPopup(hit.x,hit.y-32,"🔋 FULL CHARGE! +"+pts,"#fde68a",26);
        spawnParticles(hit.x,hit.y,"#06b6d4",16,"spark");
        sfx("epic");vibrate([12,6,18,6,12]);
        unlock("energy_orb_surge");
      } else {
        spawnPopup(hit.x,hit.y-20,`⚡ ENERGY +${pts}`,"#06b6d4",16);
        spawnParticles(hit.x,hit.y,"#06b6d4",8,"spark");
        sfx("tap");vibrate(6);
      }
      unlock("energy_orb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgEO=levelCfgRef.current;
      if(cfgEO){if(gs.score>=cfgEO.scoreGoal&&(!cfgEO.modifier||checkModGoal(cfgEO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // COMET TRAIL — bonus pts per trail dot near the tap point
    if(hit.type==="comet_trail"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const trailPts=((hit._trailPos||[]).filter(pt2=>Math.hypot(pt2.x-tx,pt2.y-ty)<80)).length;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((180+trailPts*30)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-24,trailPts>=4?`☄️ TRAIL MAX! +${pts}`:`☄️ COMET ×${trailPts} +${pts}`,trailPts>=4?"#fde68a":"#f97316",trailPts>=4?24:16);
      spawnParticles(hit.x,hit.y,"#f97316",8+trailPts*2,"spark");
      sfx(trailPts>=4?"epic":"tap");vibrate(trailPts>=4?[8,4,14]:5);
      unlock("comettrail_tap");
      if(trailPts>=4)unlock("comettrail_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCT=levelCfgRef.current;
      if(cfgCT){if(gs.score>=cfgCT.scoreGoal&&(!cfgCT.modifier||checkModGoal(cfgCT.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STAR CLUSTER — burst into 5 sparkle mini-targets
    if(hit.type==="star_cluster"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=60;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-22,"✨ CLUSTER! Collect stars!","#fbbf24",16);
      spawnParticles(hit.x,hit.y,"#fde68a",10,"spark");
      sfx("chainBonus");vibrate([4,3,4,3,4]);
      unlock("starcluster_tap");
      // Spawn 5 sparkle mini-targets
      const cw99=canvasRef.current?.width||360;const ch99=canvasRef.current?.height||640;
      const grpSC=hit.id;
      for(let i=0;i<5;i++){
        const pa99=i*(Math.PI*2/5);const pd99=55+Math.random()*25;
        const px99=Math.max(15,Math.min(cw99-15,hit.x+Math.cos(pa99)*pd99));
        const py99=Math.max(15,Math.min(ch99-15,hit.y+Math.sin(pa99)*pd99));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"star_sparkle",
          rarity:null,x:px99,y:py99,radius:11,color:"#fbbf24",glow:"#fffbeb",
          lifetime:3000,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
          worldId:hit.worldId,worldColor:hit.worldColor,bossPattern:null,bossPhase:1,cx:px99,cy:py99,
          _sparkleGroup:grpSC,_sparkleIdx:i});
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    // STAR SPARKLE — 70pts each; collect all 5 for achievement
    if(hit.type==="star_sparkle"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=70;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-14,"⭐ +70","#fbbf24",11);
      spawnParticles(hit.x,hit.y,"#fde68a",4,"spark");
      sfx("tap");vibrate(3);
      const grpSP=hit._sparkleGroup;
      if(grpSP){
        const remainSP=targetsRef.current.filter(t=>t.type==="star_sparkle"&&t._sparkleGroup===grpSP&&!t.dying);
        if(remainSP.length===0)unlock("starcluster_full");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSP=levelCfgRef.current;
      if(cfgSP){if(gs.score>=cfgSP.scoreGoal&&(!cfgSP.modifier||checkModGoal(cfgSP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VOID CRYSTAL — score scales inversely with size (smaller = more pts)
    if(hit.type==="void_crystal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const sizePct=hit._voidSizePct||1;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const basePts=Math.round((80+400*(1-sizePct))*combo*feverMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      const isSmall=sizePct<=0.3;
      spawnPopup(hit.x,hit.y-24,isSmall?`🔮 TINY! +${basePts}`:`💜 VOID CRYSTAL +${basePts}`,isSmall?"#fde68a":"#a78bfa",isSmall?24:16);
      spawnParticles(hit.x,hit.y,"#7c3aed",isSmall?14:8,"spark");
      if(isSmall)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:160,born:performance.now(),color:"#7c3aed",alpha:0.7,life:500});
      sfx(isSmall?"epic":"tap");vibrate(isSmall?[10,5,14]:5);
      unlock("voidcrystal_tap");
      if(isSmall)unlock("voidcrystal_tiny");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVC=levelCfgRef.current;
      if(cfgVC){if(gs.score>=cfgVC.scoreGoal&&(!cfgVC.modifier||checkModGoal(cfgVC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NEON PULSE — bright tap = 3× pts; dim tap = base pts
    if(hit.type==="neon_pulse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const brightPct=hit._brightPct||0;
      const isBright=brightPct>0.65;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const basePts=isBright?Math.round(250*combo*feverMult):Math.round(60*combo*feverMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-22,isBright?`💡 BRIGHT! ×3 +${basePts}`:`🔅 DIM +${basePts}`,isBright?"#fde68a":"#38bdf8",isBright?22:14);
      spawnParticles(hit.x,hit.y,isBright?"#fde68a":"#38bdf8",isBright?12:6,"spark");
      sfx(isBright?"epic":"tap");vibrate(isBright?[8,4,12]:4);
      unlock("neonpulse_tap");
      if(isBright)unlock("neonpulse_bright");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNP=levelCfgRef.current;
      if(cfgNP){if(gs.score>=cfgNP.scoreGoal&&(!cfgNP.modifier||checkModGoal(cfgNP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MIRROR TWIN — tap both halves within 4s for full bonus
    if(hit.type==="mirror_twin"){
      const groupMT=hit._mirrorGroup||hit.id;
      const partner88=targetsRef.current.find(t=>t._mirrorGroup===groupMT&&t.id!==hit.id&&!t.dying);
      if(partner88&&partner88._mirrorTapped){
        // Partner already tapped — second tap! Full bonus
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id&&t.id!==partner88.id);
        const combo=Math.min(10,1+Math.floor(gs.streak/5));
        const feverMult=gs.feverActive?2:1;
        const pts=Math.round(450*combo*feverMult);
        gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-30,`🪞 DOUBLE MIRROR! +${pts}`,"#34d399",24);
        spawnParticles(hit.x,hit.y,"#34d399",12,"spark");
        spawnParticles(partner88.x,partner88.y,"#f9a8d4",12,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#34d399",alpha:0.7,life:600});
        sfx("epic");vibrate([12,6,18,6,12]);
        unlock("mirrortwin_tap");unlock("mirrortwin_both");
      } else if(partner88){
        // First tap
        hit._mirrorTapped=true;hit._mirrorTapTime=Date.now();
        gs.score+=100;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-20,"🪞 TAP MIRROR!","#34d399",16);
        spawnParticles(hit.x,hit.y,"#34d399",8,"spark");
        sfx("tap");vibrate(5);
        unlock("mirrortwin_tap");
        // Remove this target from view but keep in array for partner check
        setTimeout(()=>{targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);},4000);
      } else {
        // Solo tap
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        gs.score+=100;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-18,"🪞 +100","#34d399",14);
        spawnParticles(hit.x,hit.y,"#34d399",6,"spark");
        sfx("tap");vibrate(4);
        unlock("mirrortwin_tap");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgMT88=levelCfgRef.current;
      if(cfgMT88){if(gs.score>=cfgMT88.scoreGoal&&(!cfgMT88.modifier||checkModGoal(cfgMT88.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GEMINI PAIR — tap both within 3s for full bonus
    if(hit.type==="gemini_pair"){
      const groupG=hit._geminiGroup||hit.id;
      const partner=targetsRef.current.find(t=>t._geminiGroup===groupG&&t.id!==hit.id&&!t.dying);
      if(partner&&partner._geminiTapped){
        // Partner already tapped — this is the second tap! Full bonus
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id&&t.id!==partner.id);
        const combo=Math.min(10,1+Math.floor(gs.streak/5));
        const feverMult=gs.feverActive?2:1;
        const timeDiff=Date.now()-partner._geminiTapTime;
        const pts=Math.round(500*combo*feverMult);
        gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-30,`♊ PERFECT PAIR! +${pts}`,"#fde68a",24);
        spawnParticles(hit.x,hit.y,"#a78bfa",14,"spark");
        spawnParticles(partner.x,partner.y,"#a78bfa",14,"spark");
        particlesRef.current.push({type:"shockwave",x:(hit.x+partner.x)/2,y:(hit.y+partner.y)/2,r:20,maxR:220,born:performance.now(),color:"#a78bfa",alpha:0.7,life:600});
        sfx("epic");vibrate([12,6,18,6,12]);
        unlock("gemini_tap");unlock("gemini_combo");
      } else if(partner&&!partner._geminiTapped){
        // First tap — mark it, start countdown
        hit._geminiTapped=true;hit._geminiTapTime=Date.now();
        gs.score+=100;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-20,"♊ TAP PARTNER!","#a78bfa",16);
        spawnParticles(hit.x,hit.y,"#c4b5fd",8,"spark");
        sfx("tap");vibrate(5);
        unlock("gemini_tap");
        // Set expiry for solo: if partner not tapped in 3s, score only 100
        setTimeout(()=>{
          const stillAlive=targetsRef.current.find(t=>t.id===hit.id&&!t.dying);
          if(stillAlive&&!partner._geminiTapped){
            targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
          }
        },3000);
      } else {
        // Solo tap (partner already gone or doesn't exist)
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        gs.score+=100;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-18,"♊ +100","#a78bfa",14);
        spawnParticles(hit.x,hit.y,"#a78bfa",6,"spark");
        sfx("tap");vibrate(4);
        unlock("gemini_tap");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGP=levelCfgRef.current;
      if(cfgGP){if(gs.score>=cfgGP.scoreGoal&&(!cfgGP.modifier||checkModGoal(cfgGP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // AURORA RING — burst nearby targets based on current ring expansion
    if(hit.type==="aurora_ring"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const expandPct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||5000));
      const ringRadius=hit.radius*(1+expandPct*3.5)*1.0; // use expanded radius for burst
      const burstTargets=targetsRef.current.filter(o=>!o.dying&&o.type==="normal"&&Math.hypot(o.x-hit.x,o.y-hit.y)<=ringRadius+20);
      let bonusPts=0;
      burstTargets.forEach(o=>{
        o.dying=performance.now();bonusPts+=50;
        spawnParticles(o.x,o.y,"#38bdf8",6,"spark");
        spawnPopup(o.x,o.y-14,"💥+50","#38bdf8",11);
      });
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const basePts=Math.round((150+bonusPts)*combo*feverMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,burstTargets.length>=5?`🌌 AURORA MAX! +${basePts}`:`🌌 AURORA ×${burstTargets.length} +${basePts}`,burstTargets.length>=5?"#fde68a":"#38bdf8",burstTargets.length>=5?24:18);
      spawnParticles(hit.x,hit.y,"#38bdf8",16,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:Math.max(180,ringRadius),born:performance.now(),color:"#38bdf8",alpha:0.65,life:700});
      sfx(burstTargets.length>=5?"epic":"chainBonus");vibrate(burstTargets.length>=5?[12,6,18]:6);
      unlock("aurora_ring_tap");
      if(burstTargets.length>=5)unlock("aurora_ring_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAR=levelCfgRef.current;
      if(cfgAR){if(gs.score>=cfgAR.scoreGoal&&(!cfgAR.modifier||checkModGoal(cfgAR.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LIGHTNING CHAIN — chain-kills 3 nearest normals for 80pts each + 250 for self
    if(hit.type==="lightning_chain"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      gs.score+=Math.round(250*combo*feverMult);
      gs.streak++;gs.lastTapTime=Date.now();
      spawnParticles(hit.x,hit.y,"#fbbf24",12,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#fbbf24",alpha:0.7,life:500});
      // Chain to 3 nearest normals
      const chainTargets=targetsRef.current.filter(o=>!o.dying&&o.type==="normal")
        .sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y)).slice(0,3);
      let chainCount=0;
      chainTargets.forEach(o=>{
        o.dying=performance.now();chainCount++;
        const cpts=Math.round(80*combo*feverMult);
        gs.score+=cpts;
        spawnParticles(o.x,o.y,"#fde68a",6,"spark");
        spawnPopup(o.x,o.y-14,`⚡+${cpts}`,"#fbbf24",12);
      });
      spawnPopup(hit.x,hit.y-28,chainCount>=3?`⚡ CHAIN MAX! +${Math.round(250*combo*feverMult)}`:`⚡ CHAIN ×${chainCount} +${Math.round(250*combo*feverMult)}`,"#fbbf24",20);
      sfx("chainBonus");vibrate([6,3,8,3,6]);
      unlock("lchain_tap");
      if(chainCount>=3)unlock("lchain_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLC=levelCfgRef.current;
      if(cfgLC){if(gs.score>=cfgLC.scoreGoal&&(!cfgLC.modifier||checkModGoal(cfgLC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MAGMA SEED — collect core first; lava drops give 60pts each
    if(hit.type==="magma_seed"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(180*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-24,"🌋 MAGMA SEED! Collect drops!","#ef4444",18);
      spawnParticles(hit.x,hit.y,"#f97316",12,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#ef4444",alpha:0.7,life:500});
      sfx("chainBonus");vibrate([6,3,6,3,10]);
      unlock("magmaseed_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    // LAVA DROP — 60pts each; collect all 5 for achievement
    if(hit.type==="lava_drop"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=60;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-14,"🔥 +60","#f97316",11);
      spawnParticles(hit.x,hit.y,"#f97316",4,"spark");
      sfx("tap");vibrate(3);
      const groupIdLD=hit._magmaGroup;
      if(groupIdLD){
        const remainLD=targetsRef.current.filter(t=>t.type==="lava_drop"&&t._magmaGroup===groupIdLD&&!t.dying);
        if(remainLD.length===0)unlock("magmaseed_full");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLD=levelCfgRef.current;
      if(cfgLD){if(gs.score>=cfgLD.scoreGoal&&(!cfgLD.modifier||checkModGoal(cfgLD.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PRISM SHARD — splits into 3 mini shards; collect all for achievement
    if(hit.type==="prism_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=80;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-22,"💎 SPLIT!","#f0abfc",16);
      spawnParticles(hit.x,hit.y,"#f0abfc",10,"spark");
      sfx("chainBonus");vibrate([4,3,4,3,4]);
      unlock("prism_shard_tap");
      // Spawn 3 mini shards in a triangle
      const cw33=canvasRef.current?.width||360;const ch33=canvasRef.current?.height||640;
      const groupId33=hit.id;
      for(let i=0;i<3;i++){
        const pa33=(i/3)*Math.PI*2-Math.PI/6;const pd33=50+Math.random()*30;
        const px33=Math.max(20,Math.min(cw33-20,hit.x+Math.cos(pa33)*pd33));
        const py33=Math.max(20,Math.min(ch33-20,hit.y+Math.sin(pa33)*pd33));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"prism_mini",
          rarity:null,x:px33,y:py33,radius:13,color:"#f0abfc",glow:"#fae8ff",
          lifetime:3000,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
          worldId:hit.worldId,worldColor:hit.worldColor,bossPattern:null,bossPhase:1,cx:px33,cy:py33,
          _prismGroup:groupId33,_prismIdx:i});
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    // PRISM MINI — 60pts each; collect all 3 for achievement
    if(hit.type==="prism_mini"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=60;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-14,`✨ +60`,"#f0abfc",11);
      spawnParticles(hit.x,hit.y,"#f0abfc",4,"spark");
      sfx("tap");vibrate(3);
      const groupId33=hit._prismGroup;
      if(groupId33){
        const remaining33=targetsRef.current.filter(t=>t.type==="prism_mini"&&t._prismGroup===groupId33&&!t.dying);
        if(remaining33.length===0)unlock("prism_shard_full");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPM=levelCfgRef.current;
      if(cfgPM){if(gs.score>=cfgPM.scoreGoal&&(!cfgPM.modifier||checkModGoal(cfgPM.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TEMPORAL ORB — +200pts base; bonus if tapped right after rewind
    if(hit.type==="temporal_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const justRewound=hit._justRewound&&Date.now()-hit._justRewound<500;
      const pts=Math.round((justRewound?450:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,justRewound?`⏪ REWIND TAP! +${pts}`:`🌀 TEMPORAL +${pts}`,justRewound?"#f59e0b":"#38bdf8",justRewound?24:16);
      spawnParticles(hit.x,hit.y,justRewound?"#fde68a":"#38bdf8",justRewound?14:8,"spark");
      if(justRewound){
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#38bdf8",alpha:0.7,life:500});
        sfx("epic");vibrate([10,5,15]);
      } else {sfx("tap");vibrate(6);}
      unlock("temporal_tap");
      if(justRewound)unlock("temporal_rewind");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTO2=levelCfgRef.current;
      if(cfgTO2){if(gs.score>=cfgTO2.scoreGoal&&(!cfgTO2.modifier||checkModGoal(cfgTO2.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VOID SEED — score scales with growth; detonates at max
    if(hit.type==="void_seed"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||8000));
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const basePts=Math.round(50+300*agePct);
      const pts=Math.round(basePts*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,agePct>=0.9?`🌸 FULL BLOOM! +${pts}`:`🌱 VOID SEED +${pts}`,agePct>=0.9?"#fde68a":"#c084fc",agePct>=0.9?24:16);
      spawnParticles(hit.x,hit.y,"#7c3aed",8+Math.round(agePct*8),"spark");
      if(agePct>=0.9){
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#7c3aed",alpha:0.7,life:500});
        sfx("legendary");vibrate([12,6,18,6,12]);
      } else {
        sfx("tap");vibrate(6);
      }
      unlock("voidseed_tap");
      if(agePct>=0.9)unlock("voidseed_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVS=levelCfgRef.current;
      if(cfgVS){if(gs.score>=cfgVS.scoreGoal&&(!cfgVS.modifier||checkModGoal(cfgVS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SHADOW TWIN — real tap = 400pts; shadow clone tap = -50pts
    if(hit.type==="shadow_twin"){
      // Remove both real and shadow clone
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id&&t._realId!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(400*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const isFirst=!gs._shadowTwinHit;gs._shadowTwinHit=true;
      spawnPopup(hit.x,hit.y-26,`👥 REAL! +${pts}`,"#f472b6",20);
      spawnParticles(hit.x,hit.y,"#f472b6",10,"spark");
      sfx("tap");vibrate([5,3,8]);
      unlock("shadowtwin_tap");
      if(isFirst)unlock("shadowtwin_lucky");
      gs._shadowTwinHit=false;
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSTw=levelCfgRef.current;
      if(cfgSTw){if(gs.score>=cfgSTw.scoreGoal&&(!cfgSTw.modifier||checkModGoal(cfgSTw.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="shadow_clone2"){
      // Tapped the fake — penalty; remove both
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id&&t.id!==hit._realId);
      gs.score=Math.max(0,gs.score-50);gs.streak=0;gs.lastTapTime=Date.now();
      gs._shadowTwinHit=true;
      spawnPopup(hit.x,hit.y-22,"👤 SHADOW! −50","#6b7280",16);
      spawnParticles(hit.x,hit.y,"#4b5563",6,"spark");
      sfx("miss");vibrate([8,4,8]);
      gs._shadowTwinHit=false;
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    // PHOTON BURST — 600pts if visible (on), 0pts if invisible (off)
    if(hit.type==="photon_burst"){
      const cycleMs=300;const isOn=Math.floor((Date.now()-hit.spawnedAt)/cycleMs)%2===0;
      if(!isOn){return;} // invisible = miss (no feedback)
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(600*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,`💡 FLASH! +${pts}`,"#ffd700",20);
      spawnParticles(hit.x,hit.y,"#facc15",12,"spark");
      sfx("epic");vibrate([8,4,12]);
      unlock("photon_tap");unlock("photon_precise");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPB=levelCfgRef.current;
      if(cfgPB){if(gs.score>=cfgPB.scoreGoal&&(!cfgPB.modifier||checkModGoal(cfgPB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VAULT ORB — claim banked pts (100pts/s banked)
    if(hit.type==="vault_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const secsAlive=(Date.now()-hit.spawnedAt)/1000;
      const banked=Math.max(100,Math.floor(secsAlive*100));
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(banked*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const isRich=banked>=400;
      spawnPopup(hit.x,hit.y-28,isRich?`💰 JACKPOT! +${pts}`:`🏦 VAULT +${pts}`,isRich?"#ffd700":"#4ade80",isRich?24:16);
      spawnParticles(hit.x,hit.y,isRich?"#ffd700":"#4ade80",isRich?16:8,"spark");
      sfx(isRich?"jackpot":"coin");vibrate(isRich?[10,5,20]:6);
      unlock("vault_tap");
      if(banked>=500)unlock("vault_rich");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVO=levelCfgRef.current;
      if(cfgVO){if(gs.score>=cfgVO.scoreGoal&&(!cfgVO.modifier||checkModGoal(cfgVO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NANO SWARM — spawns 6 nano units on tap
    if(hit.type==="nano_swarm"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=50;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,"🦠 SWARM DEPLOYED!","#34d399",16);
      spawnParticles(hit.x,hit.y,"#34d399",8,"spark");
      const cw4=canvasRef.current?.width||360;const ch4=canvasRef.current?.height||640;
      for(let i=0;i<6;i++){
        const na=(i/6)*Math.PI*2;const nd=60+Math.random()*40;
        const nx=Math.max(30,Math.min(cw4-30,hit.x+Math.cos(na)*nd));
        const ny=Math.max(30,Math.min(ch4-30,hit.y+Math.sin(na)*nd));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"nano_unit",
          rarity:RARITY.UNCOMMON,x:nx,y:ny,radius:18,color:"#34d399",glow:"#6ee7b7",
          lifetime:1800,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
          worldId:hit.worldId,worldColor:hit.worldColor,bossPattern:null,bossPhase:1,cx:nx,cy:ny,
          _swarmGroup:hit.id,_nanoIdx:i});
      }
      sfx("chainBonus");vibrate([5,3,5,3,5]);
      unlock("nanoswarm_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    // NANO UNIT — 80pts; track for achievement
    if(hit.type==="nano_unit"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=80;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-14,"🦠 +80","#34d399",11);
      spawnParticles(hit.x,hit.y,"#34d399",4,"spark");
      sfx("tap");vibrate(3);
      const groupId=hit._swarmGroup;
      if(groupId){
        const remaining=targetsRef.current.filter(t=>t.type==="nano_unit"&&t._swarmGroup===groupId&&!t.dying);
        if(remaining.length===0)unlock("nanoswarm_all");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNU=levelCfgRef.current;
      if(cfgNU){if(gs.score>=cfgNU.scoreGoal&&(!cfgNU.modifier||checkModGoal(cfgNU.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PRISM PULSE — match active color for 600pts, else 120pts
    if(hit.type==="prism_pulse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const colorIdx=Math.floor((Date.now()-hit.spawnedAt)/300)%6;
      const targetIdx=hit._prismTargetIdx||0;
      const isMatch=colorIdx===targetIdx;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isMatch?600:120)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isMatch?`🎯 MATCH! +${pts}`:`🌈 PRISM +${pts}`,isMatch?"#ffd700":"#e879f9",isMatch?22:16);
      spawnParticles(hit.x,hit.y,isMatch?"#ffd700":"#e879f9",isMatch?14:6,"spark");
      sfx(isMatch?"epic":"tap");vibrate(isMatch?[10,5,10]:5);
      unlock("prismpulse_tap");
      if(isMatch)unlock("prismpulse_match");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPP=levelCfgRef.current;
      if(cfgPP){if(gs.score>=cfgPP.scoreGoal&&(!cfgPP.modifier||checkModGoal(cfgPP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // DARK MATTER — 800pts if tapped blind (< 2s), else 300pts
    if(hit.type==="dark_matter"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const isBlind=(Date.now()-hit.spawnedAt)<2000;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isBlind?800:300)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isBlind?`🧠 SIXTH SENSE! +${pts}`:`⚫ FOUND! +${pts}`,isBlind?"#ffd700":"#a78bfa",isBlind?24:16);
      spawnParticles(hit.x,hit.y,isBlind?"#ffd700":"#7c3aed",isBlind?16:8,"spark");
      sfx(isBlind?"legendary":"tap");vibrate(isBlind?[12,6,18,6,12]:6);
      unlock("darkmatter_tap");
      if(isBlind)unlock("darkmatter_blind");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgDM=levelCfgRef.current;
      if(cfgDM){if(gs.score>=cfgDM.scoreGoal&&(!cfgDM.modifier||checkModGoal(cfgDM.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // COMET BLAST — 500pts + spawns 4 small blast shards
    if(hit.type==="comet_blast"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(500*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`☄️ COMET! +${pts}`,"#f97316",20);
      spawnParticles(hit.x,hit.y,"#fbbf24",18,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:200,born:performance.now(),color:"#f97316",alpha:0.7,life:600});
      // Spawn 4 blast shards
      const cw2=canvasRef.current?.width||360;const ch2=canvasRef.current?.height||640;
      for(let i=0;i<4;i++){
        const ba=(i/4)*Math.PI*2;
        const bx=Math.max(40,Math.min(cw2-40,hit.x+Math.cos(ba)*80));
        const by=Math.max(40,Math.min(ch2-40,hit.y+Math.sin(ba)*80));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"comet_shard",
          rarity:RARITY.RARE,x:bx,y:by,radius:20,color:"#fbbf24",glow:"#fef9c3",
          lifetime:2000,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
          moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
          worldId:hit.worldId,worldColor:hit.worldColor,bossPattern:null,bossPhase:1,cx:bx,cy:by,
          _shardGroup:hit.id,_shardIdx:i});
      }
      sfx("chainBonus");vibrate([8,4,12,4,8]);
      unlock("cometblast_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCB=levelCfgRef.current;
      if(cfgCB){if(gs.score>=cfgCB.scoreGoal&&(!cfgCB.modifier||checkModGoal(cfgCB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // COMET SHARD — 100pts; track group collection for achievement
    if(hit.type==="comet_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs.score+=100;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-18,"☄️ SHARD +100","#fbbf24",12);
      spawnParticles(hit.x,hit.y,"#fbbf24",5,"spark");
      sfx("tap");vibrate(3);
      // Check if all 4 shards from same group collected
      const groupId=hit._shardGroup;
      if(groupId){
        const remaining=targetsRef.current.filter(t=>t.type==="comet_shard"&&t._shardGroup===groupId&&!t.dying);
        if(remaining.length===0)unlock("cometblast_all");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCS=levelCfgRef.current;
      if(cfgCS){if(gs.score>=cfgCS.scoreGoal&&(!cfgCS.modifier||checkModGoal(cfgCS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TIME PRISON — releases caged targets and scores 150pts × caged count
    if(hit.type==="time_prison"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const caged=hit._caged||[];
      // Release all caged targets by clearing their _imprisoned flag
      targetsRef.current.forEach(t=>{if(caged.includes(t.id)){t._imprisoned=null;}});
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((100+150*caged.length)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,caged.length>=3?`🔓 MASS RELEASE! +${pts}`:`🔒 FREE ×${caged.length} +${pts}`,caged.length>=3?"#ffd700":"#0ea5e9",caged.length>=3?24:16);
      spawnParticles(hit.x,hit.y,"#7dd3fc",10+caged.length*3,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:180,born:performance.now(),color:"#0ea5e9",alpha:0.6,life:600});
      sfx(caged.length>=3?"epic":"tap");vibrate(caged.length>=3?[10,5,15]:5);
      unlock("timeprison_tap");
      if(caged.length>=3)unlock("timeprison_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTP=levelCfgRef.current;
      if(cfgTP){if(gs.score>=cfgTP.scoreGoal&&(!cfgTP.modifier||checkModGoal(cfgTP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SPIRAL BOMB — jackpot at full spiral (>85% fill); else 250pts; miss = -1 life (expiry)
    if(hit.type==="spiral_bomb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const fillPct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4000));
      const isFull=fillPct>0.85;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isFull?1000:250)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isFull?`💥 FULL SPIRAL! +${pts}`:`🌀 SPIRAL +${pts}`,isFull?"#ffd700":"#f97316",isFull?24:16);
      spawnParticles(hit.x,hit.y,isFull?"#ffd700":"#f97316",isFull?18:8,"spark");
      sfx(isFull?"epic":"tap");vibrate(isFull?[10,5,15]:5);
      unlock("spiralbomb_tap");
      if(isFull)unlock("spiralbomb_full");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSB=levelCfgRef.current;
      if(cfgSB){if(gs.score>=cfgSB.scoreGoal&&(!cfgSB.modifier||checkModGoal(cfgSB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CHAIN ORB — 4-tap chain: each tap scores 100/200/400/800pts; must tap within 800ms between taps
    if(hit.type==="chain_orb"){
      const nowCO=Date.now();
      if(!hit._lastChainTap||nowCO-hit._lastChainTap<800){
        hit._lastChainTap=nowCO;
        const tapIndex=hit.maxHits-hit.hitsLeft; // 0,1,2,3
        const chainPts=[100,200,400,800];
        const combo=Math.min(10,1+Math.floor(gs.streak/5));
        const feverMult=gs.feverActive?2:1;
        const pts=Math.round(chainPts[tapIndex]*combo*feverMult);
        gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        hit.hitsLeft--;
        spawnPopup(hit.x,hit.y-26-tapIndex*8,`⛓️ ×${tapIndex+1} +${pts}`,`hsl(${200+tapIndex*50},90%,60%)`,14+tapIndex*2);
        spawnParticles(hit.x,hit.y,`hsl(${200+tapIndex*50},90%,60%)`,5+tapIndex*2,"spark");
        sfx("tap");vibrate(4+tapIndex*2);
        unlock("chainorb_tap");
        if(hit.hitsLeft<=0){
          targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
          spawnPopup(hit.x,hit.y-60,"🔗 CHAIN COMPLETE!","#ffd700",22);
          for(let i=0;i<16;i++)spawnParticles(hit.x,hit.y,"#ffd700",1,"spark");
          sfx("epic");vibrate([10,5,20]);
          unlock("chainorb_max");
        }
      } else {
        // Chain timed out — reset hitsLeft
        hit.hitsLeft=hit.maxHits;
        spawnPopup(hit.x,hit.y-20,"⛓️ CHAIN RESET","#9ca3af",12);
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCO=levelCfgRef.current;
      if(cfgCO){if(gs.score>=cfgCO.scoreGoal&&(!cfgCO.modifier||checkModGoal(cfgCO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // BOLT CATCHER — pts = 400; bonus if tapped within 0.5s
    if(hit.type==="bolt_catcher"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const elapsed=(Date.now()-hit.spawnedAt)/1000;
      const isFast=elapsed<0.5;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((isFast?700:400)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isFast?`⚡ LIGHTNING FAST! +${pts}`:`⚡ CAUGHT! +${pts}`,isFast?"#ffd700":"#f59e0b",isFast?22:16);
      spawnParticles(hit.x,hit.y,isFast?"#ffd700":"#fbbf24",isFast?14:8,"spark");
      sfx(isFast?"epic":"tap");vibrate(isFast?[10,5,10]:5);
      unlock("bolt_tap");
      if(isFast)unlock("bolt_fast");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgBC=levelCfgRef.current;
      if(cfgBC){if(gs.score>=cfgBC.scoreGoal&&(!cfgBC.modifier||checkModGoal(cfgBC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GEM HARVEST — 200pts base + 120pts per gem collected
    if(hit.type==="gem_harvest"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const gems=hit._gemCount||0;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((200+gems*120)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,gems>=5?`👑 GEM TYCOON! +${pts}`:`💎 ×${gems} = +${pts}`,gems>=5?"#ffd700":"#34d399",gems>=5?24:16);
      spawnParticles(hit.x,hit.y,"#34d399",8+gems*2,"spark");
      sfx(gems>=5?"legendary":"coin");vibrate(gems>=5?[10,5,15]:5);
      unlock("gemharvest_tap");
      if(gems>=5)unlock("gemharvest_rich");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGH=levelCfgRef.current;
      if(cfgGH){if(gs.score>=cfgGH.scoreGoal&&(!cfgGH.modifier||checkModGoal(cfgGH.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MIRROR ECHO — collect reflected clone for bonus pts
    if(hit.type==="mirror_echo"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const ePts=hit._mirrorPts||150;
      gs.score+=ePts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-22,`🌀 ECHO +${ePts}`,"#c7d2fe",16);
      spawnParticles(hit.x,hit.y,"#818cf8",8,"spark");
      sfx("coin");vibrate(4);
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgME=levelCfgRef.current;
      if(cfgME){if(gs.score>=cfgME.scoreGoal&&(!cfgME.modifier||checkModGoal(cfgME.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // FLAME RING — sweet spot (0.3-0.6 ring life remaining) = 800pts; else 200pts
    if(hit.type==="flame_ring"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4000));
      const ringPct=1-agePct;
      const inSweet=ringPct>0.3&&ringPct<0.6;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round((inSweet?800:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,inSweet?`🔥 SWEET SPOT! +${pts}`:`🔥 RING! +${pts}`,inSweet?"#ffd700":"#f97316",inSweet?22:16);
      spawnParticles(hit.x,hit.y,inSweet?"#ffd700":"#f97316",inSweet?14:6,"spark");
      sfx(inSweet?"epic":"tap");vibrate(inSweet?[10,5,10]:5);
      unlock("flamering_tap");
      if(inSweet)unlock("flamering_sweet");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgFR=levelCfgRef.current;
      if(cfgFR){if(gs.score>=cfgFR.scoreGoal&&(!cfgFR.modifier||checkModGoal(cfgFR.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ICE CLOCK — 5s countdown; pts = 200 × secs remaining
    if(hit.type==="ice_clock"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const elapsed=(Date.now()-hit.spawnedAt)/1000;
      const secsLeft=Math.max(0,Math.ceil(5-elapsed));
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(200*secsLeft*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,secsLeft>=5?`⏰ MAX TIME! +${pts}`:`🧊 ×${secsLeft}s = +${pts}`,secsLeft>=4?"#7dd3fc":"#ef4444",secsLeft>=5?22:16);
      spawnParticles(hit.x,hit.y,secsLeft>=4?"#7dd3fc":"#ef4444",8,"spark");
      sfx(secsLeft>=5?"legendary":"tap");vibrate(5);
      unlock("iceclock_tap");
      if(secsLeft>=5)unlock("iceclock_5");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgIC=levelCfgRef.current;
      if(cfgIC){if(gs.score>=cfgIC.scoreGoal&&(!cfgIC.modifier||checkModGoal(cfgIC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // HOLO DECOY — real=400pts; decoy=-100pts
    if(hit.type==="holo_decoy"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const isReal=hit._holoReal!==false;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      if(isReal){
        const pts=Math.round(400*combo*feverMult);
        gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-26,`✅ REAL! +${pts}`,"#22d3ee",20);
        spawnParticles(hit.x,hit.y,"#22d3ee",10,"spark");
        sfx("tap");vibrate([5,3,8]);
        unlock("holodecoy_tap");unlock("holodecoy_real");
      } else {
        const drain=100;gs.score=Math.max(0,gs.score-drain);
        gs.streak=0;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-26,"👻 FAKE! −100","#f472b6",18);
        spawnParticles(hit.x,hit.y,"#f472b6",8,"spark");
        sfx("miss");vibrate([12,6,12]);
        unlock("holodecoy_tap");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgHD=levelCfgRef.current;
      if(cfgHD){if(gs.score>=cfgHD.scoreGoal&&(!cfgHD.modifier||checkModGoal(cfgHD.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MIRROR GATE — activates mirror mode; next tapped target gets a free clone
    if(hit.type==="mirror_gate"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      gs._mirrorGateActive=true;
      gs.score+=75;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,"🪞 MIRROR GATE! Next tap clones!","#818cf8",20);
      spawnParticles(hit.x,hit.y,"#c7d2fe",12,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:160,born:performance.now(),color:"#818cf8",alpha:0.6,life:500});
      sfx("powerUp");vibrate([6,3,10,3,6]);
      unlock("mirrorgate_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    // OMEGA ORB — 3000pts base (always legendary); screen flash if 20+ combo
    if(hit.type==="omega_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const isAwakened=gs.streak>=20;
      const pts=Math.round((isAwakened?5000:3000)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-32,isAwakened?`🔱 OMEGA AWAKENED! +${pts}`:`Ω OMEGA! +${pts}`,isAwakened?"#ffd700":"#fbbf24",isAwakened?30:24);
      for(let i=0;i<30;i++)spawnParticles(hit.x,hit.y,"#ffd700",1,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:350,born:performance.now(),color:"#ffd700",alpha:0.9,life:900});
      sfx("jackpot");vibrate([20,10,30,10,40,10,50]);
      setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),500);
      unlock("omega_tap");
      if(isAwakened)unlock("omega_legendary");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgOM=levelCfgRef.current;
      if(cfgOM){if(gs.score>=cfgOM.scoreGoal&&(!cfgOM.modifier||checkModGoal(cfgOM.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // THUNDER BALL — 250pts + charges up to 3 nearby targets for 2× pts for 2s
    if(hit.type==="thunder_ball"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const chargeDist=180;
      const chargeTargets=targetsRef.current.filter(t=>!t.dying&&t.type==="normal"&&Math.hypot(t.x-hit.x,t.y-hit.y)<chargeDist).slice(0,3);
      chargeTargets.forEach(t=>{t._thunderCharged=true;t._thunderExpiry=Date.now()+2000;spawnParticles(t.x,t.y,"#fbbf24",4,"spark");});
      const chargeCount=chargeTargets.length;
      const pts=Math.round((250+chargeCount*50)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,chargeCount>=3?`🌩️ CHAIN! +${pts}`:`⚡ CHARGED×${chargeCount}! +${pts}`,chargeCount>=3?"#ffd700":"#f59e0b",chargeCount>=3?22:16);
      spawnParticles(hit.x,hit.y,"#fbbf24",10,"spark");
      sfx(chargeCount>=3?"chainBonus":"tap");vibrate(chargeCount>=3?[10,4,14,4,10]:7);
      unlock("thunderball_tap");
      if(chargeCount>=3)unlock("thunderball_chain");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTB=levelCfgRef.current;
      if(cfgTB){if(gs.score>=cfgTB.scoreGoal&&(!cfgTB.modifier||checkModGoal(cfgTB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // COSMIC AURA — 300pts + activates LUCKY power-up for 5s
    if(hit.type==="cosmic_aura"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(300*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      // Activate LUCKY for 5s
      luckyRef.current="active";
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LUCKY");
      activePwrRef.current.push({type:"LUCKY",endsAt:Date.now()+5000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(hit.x,hit.y-28,"🌌 COSMIC AURA! +LUCKY","#818cf8",22);
      spawnParticles(hit.x,hit.y,"#c7d2fe",14,"spark");
      sfx("lucky");vibrate([8,4,12,4,8]);
      setNotif("🌌 Cosmic Aura! Lucky mode for 5s!");
      unlock("cosmicaura_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCA=levelCfgRef.current;
      if(cfgCA){if(gs.score>=cfgCA.scoreGoal&&(!cfgCA.modifier||checkModGoal(cfgCA.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // COMBO RUSH — pts = 80 × current streak (min 80, capped at 50 streak)
    if(hit.type==="combo_rush"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const feverMult=gs.feverActive?2:1;
      const streakVal=Math.max(1,Math.min(50,gs.streak));
      const isRush=streakVal>=30;
      const pts=Math.round(80*streakVal*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isRush?`💥 RUSH FRENZY! +${pts}`:`🔥 COMBO×${streakVal} +${pts}`,isRush?"#ffd700":"#f97316",isRush?26:16);
      spawnParticles(hit.x,hit.y,isRush?"#ffd700":"#fb923c",isRush?16:8,"spark");
      sfx(isRush?"chainBonus":"tap");vibrate(isRush?[10,4,14,4,10]:7);
      unlock("comborush_tap");
      if(isRush)unlock("comborush_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCR=levelCfgRef.current;
      if(cfgCR){if(gs.score>=cfgCR.scoreGoal&&(!cfgCR.modifier||checkModGoal(cfgCR.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NEON CRYSTAL — pts depend on current phase (cold=50, warm=200, hot=800)
    if(hit.type==="neon_crystal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const phase=Math.floor((Date.now()-hit.spawnedAt)/800)%3;
      const phasePts=[50,200,800];const phaseNames=["🧊 COLD","🌡️ WARM","🔥 HOT"];const phaseCols=["#67e8f9","#fb923c","#ffd700"];
      const isHot=phase===2;
      const pts=Math.round(phasePts[phase]*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,`${phaseNames[phase]}! +${pts}`,phaseCols[phase],isHot?24:16);
      spawnParticles(hit.x,hit.y,phaseCols[phase],isHot?16:8,"spark");
      sfx(isHot?"jackpot":phase===1?"legendary":"tap");vibrate(isHot?[10,4,14,4,10]:7);
      unlock("neoncrystal_tap");
      if(isHot)unlock("neoncrystal_hot");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNC=levelCfgRef.current;
      if(cfgNC){if(gs.score>=cfgNC.scoreGoal&&(!cfgNC.modifier||checkModGoal(cfgNC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SONAR PING — 200pts + 100pts per target within current ring radius
    if(hit.type==="sonar_ping"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // Largest current ring
      const rings=hit._pings||[];
      const maxRing=rings.length>0?Math.max(...rings):0;
      const pingR=hit.radius*0.6+maxRing*(hit.radius*4);
      const inRange=targetsRef.current.filter(t=>!t.dying&&t.type!=="boss"&&Math.hypot(t.x-hit.x,t.y-hit.y)<pingR);
      const pingCount=inRange.length;
      const isCluster=pingCount>=4;
      const pts=Math.round((200+pingCount*100)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isCluster?`📶 CLUSTER! +${pts}`:`📡 PING×${pingCount} +${pts}`,isCluster?"#ffd700":"#4ade80",isCluster?24:16);
      spawnParticles(hit.x,hit.y,"#86efac",8+pingCount*2,"spark");
      sfx(isCluster?"chainBonus":"tap");vibrate(7);
      unlock("sonarping_tap");
      if(isCluster)unlock("sonarping_cluster");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSP2=levelCfgRef.current;
      if(cfgSP2){if(gs.score>=cfgSP2.scoreGoal&&(!cfgSP2.modifier||checkModGoal(cfgSP2.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GRAVITY BOMB — defuse for 350pts; bonus if in top half of screen
    if(hit.type==="gravity_bomb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const ch=canvasRef.current?.height||700;
      const isEarly=hit.y<ch*0.45;
      const pts=Math.round((isEarly?600:300)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isEarly?`🎯 MID-AIR! +${pts}`:`🧲 DEFUSED! +${pts}`,isEarly?"#ffd700":"#f97316",isEarly?26:18);
      spawnParticles(hit.x,hit.y,isEarly?"#ffd700":"#fb923c",isEarly?16:10,"spark");
      sfx(isEarly?"jackpot":"bossKill");vibrate(isEarly?[12,6,18,6,12]:8);
      unlock("gravitybomb_tap");
      if(isEarly)unlock("gravitybomb_early");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGB=levelCfgRef.current;
      if(cfgGB){if(gs.score>=cfgGB.scoreGoal&&(!cfgGB.modifier||checkModGoal(cfgGB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SPEED NOVA — pts scale with current size (0–400pts based on growth)
    if(hit.type==="speed_nova"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const sizePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4000));
      const isMax=sizePct>=0.9;
      const pts=Math.round((50+350*sizePct)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isMax?`🌟 FULL NOVA! +${pts}`:`💫 NOVA! +${pts}`,isMax?"#ffd700":"#c084fc",isMax?24:16);
      spawnParticles(hit.x,hit.y,isMax?"#ffd700":"#e9d5ff",isMax?16:8,"spark");
      sfx(isMax?"legendary":"tap");vibrate(isMax?[10,4,14,4,10]:7);
      unlock("speednova_tap");
      if(isMax)unlock("speednova_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSN=levelCfgRef.current;
      if(cfgSN){if(gs.score>=cfgSN.scoreGoal&&(!cfgSN.modifier||checkModGoal(cfgSN.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // QUANTUM SPLIT — first tap splits it into 2 quantum_half targets
    if(hit.type==="quantum_split"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // Spawn 2 quantum halves in opposite directions
      const splitId=`qs_${Math.random().toString(36).slice(2)}`;
      const angles=[Math.PI*0.35,Math.PI*1.35];
      angles.forEach((ang,i)=>{
        const halfR=hit.radius*0.75;
        const hx=hit.x+Math.cos(ang)*halfR*1.8,hy=hit.y+Math.sin(ang)*halfR*1.8;
        targetsRef.current.push({
          id:Math.random().toString(36).slice(2),type:"quantum_half",
          x:hx,y:hy,radius:halfR,color:"#3b82f6",glow:"#bfdbfe",
          rarity:RARITY.RARE,lifetime:2000,spawnedAt:Date.now(),born:performance.now(),
          moving:true,ghost:false,vx:Math.cos(ang)*0.8,vy:Math.sin(ang)*0.8,trail:[],
          hitsLeft:1,maxHits:1,dying:null,_splitGroup:splitId,_splitIdx:i,
        });
      });
      gs._qSplitGroup=splitId;gs._qSplitCollected=0;
      gs.score+=Math.round(50*combo*feverMult);gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,"⚛️ SPLIT!","#60a5fa",18);
      spawnParticles(hit.x,hit.y,"#93c5fd",10,"spark");
      sfx("tap");vibrate(7);
      unlock("qsplit_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      return;
    }
    // QUANTUM HALF — each half worth 200pts; collecting both unlocks achievement
    if(hit.type==="quantum_half"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(200*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      if(gs._qSplitGroup===hit._splitGroup){
        gs._qSplitCollected=(gs._qSplitCollected||0)+1;
        if(gs._qSplitCollected>=2)unlock("qsplit_collect");
      }
      spawnPopup(hit.x,hit.y-22,"⚛️ HALF! +"+pts,"#60a5fa",16);
      spawnParticles(hit.x,hit.y,"#93c5fd",8,"spark");
      sfx("tap");vibrate(7);
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgQH=levelCfgRef.current;
      if(cfgQH){if(gs.score>=cfgQH.scoreGoal&&(!cfgQH.modifier||checkModGoal(cfgQH.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // FROST BOLT — 150pts; 3× if >70% lifetime remaining
    if(hit.type==="frost_bolt"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const timeLeftPct=1-(Date.now()-hit.spawnedAt)/(hit.lifetime||2200);
      const isEarly=timeLeftPct>=0.7;
      const pts=Math.round((isEarly?450:150)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isEarly?`🧊 SPEED FREEZE! +${pts}`:`❄️ BOLT! +${pts}`,isEarly?"#ffd700":"#67e8f9",isEarly?24:16);
      spawnParticles(hit.x,hit.y,"#a5f3fc",isEarly?14:7,"spark");
      sfx(isEarly?"legendary":"tap");vibrate(isEarly?[10,4,14,4,10]:7);
      unlock("frostbolt_tap");
      if(isEarly)unlock("frostbolt_speed");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgFB=levelCfgRef.current;
      if(cfgFB){if(gs.score>=cfgFB.scoreGoal&&(!cfgFB.modifier||checkModGoal(cfgFB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VORTEX STAR — 50pts × star count (0–8); max stars = achievement
    if(hit.type==="vortex_star"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const starCount=hit._starCount||Math.min(8,Math.floor((Date.now()-hit.spawnedAt)/500));
      const isMax=starCount>=7;
      const pts=Math.round((50+50*starCount)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isMax?`💫 FULL ORBIT! +${pts}`:`🌟 ×${starCount} +${pts}`,isMax?"#ffd700":"#fbbf24",isMax?24:16);
      spawnParticles(hit.x,hit.y,"#fde68a",6+starCount,"spark");
      sfx(isMax?"chainBonus":"tap");vibrate(isMax?[10,4,14,4,10]:7);
      unlock("vortexstar_tap");
      if(isMax)unlock("vortexstar_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVS=levelCfgRef.current;
      if(cfgVS){if(gs.score>=cfgVS.scoreGoal&&(!cfgVS.modifier||checkModGoal(cfgVS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PIXEL BURST — 3-tap target (100/300/700pts per tap); fully un-pixelated = achievement
    if(hit.type==="pixel_burst"){
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      hit.hitsLeft=(hit.hitsLeft||3)-1;
      const tapIdx=3-hit.hitsLeft;
      const tapPts=[100,300,700];
      const isFull=hit.hitsLeft<=0;
      const pts=Math.round(tapPts[tapIdx-1]*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      if(isFull){
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        spawnPopup(hit.x,hit.y-28,`🖥️ FULL RES! +${pts}`,"#ffd700",26);
        spawnParticles(hit.x,hit.y,"#f472b6",18,"spark");
        sfx("jackpot");vibrate([10,4,14,4,10]);
        unlock("pixelburst_full");
      } else {
        hit.dying=null; // ensure it stays alive
        spawnPopup(hit.x,hit.y-22,`🎮 ×${tapIdx}/3 +${pts}`,"#a78bfa",16);
        spawnParticles(hit.x,hit.y,"#818cf8",6,"spark");
        sfx("tap");vibrate(7);
      }
      unlock("pixelburst_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPX=levelCfgRef.current;
      if(cfgPX){if(gs.score>=cfgPX.scoreGoal&&(!cfgPX.modifier||checkModGoal(cfgPX.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STAR FORGE — 3 charge tiers: <80%=100pts, 80-95%=500pts, 95%+=2000pts
    if(hit.type==="star_forge"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const chargePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4500));
      const tier=chargePct>=0.95?2:chargePct>=0.8?1:0;
      const tierPts=[100,500,2000];const tierNames=["⭐ FORGED","🔥 HOT","💥 MAX CHARGE"];const tierCols=["#818cf8","#f59e0b","#ffd700"];
      const pts=Math.round(tierPts[tier]*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`${tierNames[tier]}! +${pts}`,tierCols[tier],tier===2?28:tier===1?22:16);
      spawnParticles(hit.x,hit.y,tierCols[tier],tier===2?20:tier===1?12:6,"spark");
      if(tier===2){particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:220,born:performance.now(),color:"#ffd700",alpha:0.7,life:700});}
      sfx(tier===2?"jackpot":tier===1?"legendary":"tap");vibrate(tier===2?[15,8,20,8,25]:tier===1?[10,4,14,4,10]:7);
      unlock("starforge_tap");
      if(tier===2)unlock("starforge_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSF=levelCfgRef.current;
      if(cfgSF){if(gs.score>=cfgSF.scoreGoal&&(!cfgSF.modifier||checkModGoal(cfgSF.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MAGIC DICE — pts = 100 × face value (1–6); re-rolls every 600ms
    if(hit.type==="magic_dice"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const face=1+Math.floor((Date.now()-hit.spawnedAt)/600)%6;
      const isSix=face===6;
      const pts=Math.round(100*face*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isSix?`🎰 LUCKY 6! +${pts}`:`🎲 ×${face}! +${pts}`,isSix?"#ffd700":"#c084fc",isSix?24:16);
      spawnParticles(hit.x,hit.y,isSix?"#ffd700":"#a855f7",isSix?16:6+face,"spark");
      sfx(isSix?"jackpot":face>=5?"legendary":"tap");vibrate(isSix?[10,4,14,4,10]:7);
      unlock("magicdice_tap");
      if(isSix)unlock("magicdice_six");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgMD=levelCfgRef.current;
      if(cfgMD){if(gs.score>=cfgMD.scoreGoal&&(!cfgMD.modifier||checkModGoal(cfgMD.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // WORMHOLE TARGET — 300pts + teleports all targets to random positions
    if(hit.type==="wormhole_tgt"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const cw=canvasRef.current?.width||400,ch=canvasRef.current?.height||700;
      let teleported=0;
      targetsRef.current.forEach(t=>{
        if(t.dying||t.type==="boss")return;
        t.x=40+Math.random()*(cw-80);t.y=50+Math.random()*(ch-100);
        teleported++;
        spawnParticles(t.x,t.y,"#06b6d4",3,"spark");
      });
      const isTotal=teleported>=6;
      const pts=Math.round((300+teleported*40)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isTotal?`🌌 TOTAL CHAOS! +${pts}`:`🌀 WORMHOLE! +${pts}`,isTotal?"#ffd700":"#06b6d4",isTotal?26:18);
      spawnParticles(hit.x,hit.y,"#38bdf8",14,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:250,born:performance.now(),color:"#06b6d4",alpha:0.6,life:600});
      sfx(isTotal?"chainBonus":"tap");vibrate(isTotal?[10,5,15,5,10]:8);
      unlock("wormhole_tap");
      if(isTotal)unlock("wormhole_chaos");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgWT=levelCfgRef.current;
      if(cfgWT){if(gs.score>=cfgWT.scoreGoal&&(!cfgWT.modifier||checkModGoal(cfgWT.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SHADOW PULSE — 150pts bright phase; 500pts dark phase
    if(hit.type==="shadow_pulse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const isDark=Math.floor((Date.now()-hit.spawnedAt)/800)%2===0;
      const pts=Math.round((isDark?500:150)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isDark?`🌚 INTO THE DARK! +${pts}`:`👁️ PULSE! +${pts}`,isDark?"#ffd700":"#8b5cf6",isDark?24:16);
      spawnParticles(hit.x,hit.y,isDark?"#ffd700":"#c4b5fd",isDark?14:8,"spark");
      sfx(isDark?"legendary":"tap");vibrate(isDark?[10,4,14,4,10]:7);
      unlock("shadowpulse_tap");
      if(isDark)unlock("shadowpulse_dark");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSP=levelCfgRef.current;
      if(cfgSP){if(gs.score>=cfgSP.scoreGoal&&(!cfgSP.modifier||checkModGoal(cfgSP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ECLIPSE ORB — 150pts sun phase; 500pts moon phase
    if(hit.type==="eclipse_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const isMoon=Math.floor((Date.now()-hit.spawnedAt)/800)%2===1;
      const pts=Math.round((isMoon?500:150)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isMoon?`🌙 MOONSHOT! +${pts}`:`☀️ SOLAR! +${pts}`,isMoon?"#818cf8":"#f59e0b",isMoon?24:16);
      spawnParticles(hit.x,hit.y,isMoon?"#c7d2fe":"#fde68a",isMoon?14:8,"spark");
      sfx(isMoon?"legendary":"tap");vibrate(isMoon?[10,4,14,4,10]:7);
      unlock("eclipse_tap");
      if(isMoon)unlock("eclipse_moon");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgEO=levelCfgRef.current;
      if(cfgEO){if(gs.score>=cfgEO.scoreGoal&&(!cfgEO.modifier||checkModGoal(cfgEO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SPIN BLOOM — 50pts × petals remaining (1–8)
    if(hit.type==="spin_bloom"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4000));
      const petalsLeft=Math.max(1,Math.ceil(8*(1-agePct*0.9)));
      const isFull=petalsLeft>=7;
      const pts=Math.round(50*petalsLeft*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isFull?`🌺 FULL BLOOM! +${pts}`:`🌸 ×${petalsLeft} +${pts}`,isFull?"#ffd700":"#f472b6",isFull?24:16);
      spawnParticles(hit.x,hit.y,"#f9a8d4",6+petalsLeft,"spark");
      sfx(isFull?"legendary":"tap");vibrate(isFull?[10,4,14,4,10]:7);
      unlock("spinbloom_tap");
      if(isFull)unlock("spinbloom_full");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSB=levelCfgRef.current;
      if(cfgSB){if(gs.score>=cfgSB.scoreGoal&&(!cfgSB.modifier||checkModGoal(cfgSB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TESLA NODE — claims all banked pts (80pts per auto-zapped target)
    if(hit.type==="tesla_node"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const bank=hit._bank||0;
      const isRich=bank>=400;
      const pts=Math.round((100+bank)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isRich?`🔋 POWER SURGE! +${pts}`:`⚡ TESLA! +${pts}`,isRich?"#ffd700":"#38bdf8",isRich?24:16);
      spawnParticles(hit.x,hit.y,"#7dd3fc",isRich?16:8,"spark");
      sfx(isRich?"chainBonus":"tap");vibrate(isRich?[10,4,14,4,10]:7);
      unlock("teslanode_tap");
      if(isRich)unlock("teslanode_rich");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTN=levelCfgRef.current;
      if(cfgTN){if(gs.score>=cfgTN.scoreGoal&&(!cfgTN.modifier||checkModGoal(cfgTN.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // RUNE CIRCLE — pts depend on active rune; legendary (✨ idx=5) = jackpot
    if(hit.type==="rune_circle"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const runeIdx=Math.floor((Date.now()-hit.spawnedAt)/550)%6;
      const runePts=[100,200,350,500,750,1000];
      const runeNames=["🔮 MYSTICAL","🌟 STELLAR","⚗️ ALCHEMIST","📿 SACRED","🔯 HEXAGRAM","✨ LEGENDARY"];
      const runeCols=["#a78bfa","#fbbf24","#34d399","#f472b6","#60a5fa","#ffd700"];
      const isLegendary=runeIdx===5;
      const pts=Math.round(runePts[runeIdx]*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`${runeNames[runeIdx]}! +${pts}`,runeCols[runeIdx],isLegendary?26:16);
      spawnParticles(hit.x,hit.y,runeCols[runeIdx],isLegendary?18:8,"spark");
      sfx(isLegendary?"jackpot":runeIdx>=4?"legendary":"tap");vibrate(isLegendary?[12,6,18,6,12]:7);
      unlock("runecircle_tap");
      if(isLegendary)unlock("runecircle_jackpot");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgRC=levelCfgRef.current;
      if(cfgRC){if(gs.score>=cfgRC.scoreGoal&&(!cfgRC.modifier||checkModGoal(cfgRC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SOLAR ORBIT — 200pts + 100pts per planet in alignment (top arc, angle near 0°)
    if(hit.type==="solar_orbit"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const speeds=[0.0055,0.0038,0.0071];
      const aligned=speeds.filter(sp=>{
        const a=((performance.now())*sp)%(Math.PI*2);
        // Planet is "aligned" if near top arc (within 60° of 270° = -90° = up)
        const diff=Math.abs(((a+Math.PI*1.5)%(Math.PI*2))-Math.PI);
        return diff<Math.PI/3;
      }).length;
      const pts=Math.round((200+aligned*100)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,aligned===3?`🌌 ALIGNMENT! +${pts}`:`🪐 ORBIT×${aligned} +${pts}`,aligned===3?"#ffd700":"#f59e0b",aligned===3?24:16);
      spawnParticles(hit.x,hit.y,"#fbbf24",6+aligned*3,"spark");
      sfx(aligned===3?"chainBonus":"tap");vibrate(7);
      unlock("solarorbit_tap");
      if(aligned===3)unlock("solarorbit_align");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSO=levelCfgRef.current;
      if(cfgSO){if(gs.score>=cfgSO.scoreGoal&&(!cfgSO.modifier||checkModGoal(cfgSO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CHROMA SHIFT — pts depend on active color at tap moment
    if(hit.type==="chroma_shift"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const colorIdx=Math.floor((Date.now()-hit.spawnedAt)/400)%6;
      const chromaPts=[80,150,250,400,600,1000];
      const chromaNames=["🔴 RED","🔵 BLUE","🟢 GREEN","🟣 PURPLE","🩵 CYAN","🥇 GOLD"];
      const chromaCols=["#ef4444","#3b82f6","#22c55e","#a855f7","#06b6d4","#ffd700"];
      const basePts=chromaPts[colorIdx];
      const pts=Math.round(basePts*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const isGold=colorIdx===5;
      spawnPopup(hit.x,hit.y-26,`${chromaNames[colorIdx]}! +${pts}`,chromaCols[colorIdx],isGold?26:16);
      spawnParticles(hit.x,hit.y,chromaCols[colorIdx],isGold?16:8,"spark");
      sfx(isGold?"jackpot":colorIdx>=4?"legendary":"tap");vibrate(isGold?[10,4,14,4,10]:7);
      unlock("chroma_tap");
      if(isGold)unlock("chroma_gold");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCS=levelCfgRef.current;
      if(cfgCS){if(gs.score>=cfgCS.scoreGoal&&(!cfgCS.modifier||checkModGoal(cfgCS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NEON PULSAR — 150pts normally; 3× pts at peak size (phasePct > 0.88)
    if(hit.type==="neon_pulsar"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const cycleMs=1200;
      const phase=(Date.now()-hit.spawnedAt)%cycleMs/cycleMs;
      const phasePct=Math.sin(phase*Math.PI);
      const isPeak=phasePct>0.88;
      const pts=Math.round((isPeak?450:150)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isPeak?`⚡ PEAK POWER! +${pts}`:`💡 PULSAR! +${pts}`,isPeak?"#ffd700":"#e879f9",isPeak?24:16);
      spawnParticles(hit.x,hit.y,isPeak?"#ffd700":"#e879f9",isPeak?14:7,"spark");
      sfx(isPeak?"legendary":"tap");vibrate(isPeak?[10,4,14,4,10]:7);
      unlock("pulsar_tap");
      if(isPeak)unlock("pulsar_peak");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNP=levelCfgRef.current;
      if(cfgNP){if(gs.score>=cfgNP.scoreGoal&&(!cfgNP.modifier||checkModGoal(cfgNP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // DRIFT MINE — defuse for 350pts; clutch bonus if <600ms left
    if(hit.type==="drift_mine"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const timeLeft=(hit.spawnedAt+hit.lifetime)-Date.now();
      const isClutch=timeLeft<600;
      const pts=Math.round((isClutch?700:350)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isClutch?`💣 CLUTCH! +${pts}`:`💣 DEFUSED! +${pts}`,isClutch?"#ffd700":"#f87171",isClutch?26:18);
      spawnParticles(hit.x,hit.y,isClutch?"#ffd700":"#f87171",isClutch?16:10,"spark");
      sfx(isClutch?"jackpot":"bossKill");vibrate(isClutch?[12,6,18,6,12]:8);
      unlock("driftmine_tap");
      if(isClutch)unlock("driftmine_clutch");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgDM=levelCfgRef.current;
      if(cfgDM){if(gs.score>=cfgDM.scoreGoal&&(!cfgDM.modifier||checkModGoal(cfgDM.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ARC LIGHT — 200pts + 80pts per nearby connected target (max 4 arcs)
    if(hit.type==="arc_light"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const arcDist=190;
      const connected=targetsRef.current.filter(t=>!t.dying&&Math.hypot(t.x-hit.x,t.y-hit.y)<arcDist);
      const arcCount=Math.min(4,connected.length);
      const pts=Math.round((200+80*arcCount)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,arcCount>=4?`⚡ WEB! +${pts}`:`⚡ ARC×${arcCount}! +${pts}`,arcCount>=4?"#ffd700":"#facc15",arcCount>=4?22:16);
      spawnParticles(hit.x,hit.y,"#facc15",6+arcCount*2,"spark");
      sfx(arcCount>=4?"chainBonus":"tap");vibrate(7);
      unlock("arclight_tap");
      if(arcCount>=4)unlock("arclight_web");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAL=levelCfgRef.current;
      if(cfgAL){if(gs.score>=cfgAL.scoreGoal&&(!cfgAL.modifier||checkModGoal(cfgAL.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PULSE CHAIN — 300pts + destroys up to 5 nearby normals for 120pts each
    if(hit.type==="pulse_chain"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const chainDist=155;
      const victims=targetsRef.current.filter(t=>!t.dying&&t.type==="normal"&&Math.hypot(t.x-hit.x,t.y-hit.y)<chainDist).slice(0,5);
      const chainCount=victims.length;
      victims.forEach(v=>{
        v.dying=performance.now();
        spawnParticles(v.x,v.y,"#f97316",5,"spark");
        spawnPopup(v.x,v.y-16,"+120","#f97316",13);
        gs.score+=Math.round(120*feverMult);
      });
      const basePts=Math.round(300*combo*feverMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      const totalPts=basePts+Math.round(120*feverMult*chainCount);
      spawnPopup(hit.x,hit.y-28,chainCount>=3?`🔗 CHAIN BURST! +${totalPts}`:`🔗 CHAIN×${chainCount} +${totalPts}`,chainCount>=3?"#ffd700":"#f97316",chainCount>=3?24:16);
      spawnParticles(hit.x,hit.y,"#fb923c",10,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:chainDist,born:performance.now(),color:"#f97316",alpha:0.5,life:500});
      sfx(chainCount>=3?"chainBonus":"tap");vibrate(chainCount>=3?[8,4,14,4,8]:7);
      unlock("pulsechain_tap");
      if(chainCount>=3)unlock("pulsechain_burst");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPC=levelCfgRef.current;
      if(cfgPC){if(gs.score>=cfgPC.scoreGoal&&(!cfgPC.modifier||checkModGoal(cfgPC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GHOST ORB — 200pts normally; 600pts when nearly invisible (agePct >= 0.9)
    if(hit.type==="ghost_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4200));
      const isPhantom=agePct>=0.9;
      const psPwr80=activePwrRef.current.find(p=>p.type==="PHANTOM_STRIKE"&&p.endsAt>Date.now());
      const psMult80=psPwr80?2:1;
      const pts=Math.round((isPhantom?600:200)*combo*feverMult*psMult80);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      if(psPwr80){
        psPwr80.ghostHits=(psPwr80.ghostHits||0)+1;
        const nearPS80=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-hit.x,o.y-hit.y)<110).slice(0,2);
        nearPS80.forEach(o=>{gs.score+=30;o.dying=performance.now();spawnParticles(o.x,o.y,"#a78bfa",5,"spark");});
        spawnPopup(hit.x,hit.y-36,`👻⚡ ×2 +${pts}`,"#a78bfa",24);
        unlock("phantom_strike_hit");
      } else {
        spawnPopup(hit.x,hit.y-26,isPhantom?`👻 PHANTOM! +${pts}`:`👻 GHOST! +${pts}`,isPhantom?"#ffd700":"#c4b5fd",isPhantom?24:16);
      }
      spawnParticles(hit.x,hit.y,isPhantom?"#ffd700":"#8b5cf6",isPhantom?14:8,"spark");
      sfx(isPhantom?"legendary":"tap");vibrate(isPhantom?[10,4,14,4,10]:7);
      unlock("ghostorb_tap");
      if(isPhantom)unlock("ghostorb_phantom");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGO=levelCfgRef.current;
      if(cfgGO){if(gs.score>=cfgGO.scoreGoal&&(!cfgGO.modifier||checkModGoal(cfgGO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TIME FRACTURE — 400pts + freezes all targets 2.5s; bonus pts per frozen target
    if(hit.type==="time_fracture"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const frozenTargets=targetsRef.current.filter(t=>!t.dying&&t.type!=="boss"&&t.type!=="bomb");
      const frozenCount=frozenTargets.length;
      // Freeze movement using existing FREEZE powerup mechanism
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="FREEZE");
      activePwrRef.current.push({type:"FREEZE",endsAt:Date.now()+2500});
      setActivePwrDisp([...activePwrRef.current]);
      const pts=Math.round((400+frozenCount*60)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-30,frozenCount>=5?`⏳ TEMPORAL FEAST! +${pts}`:`⏳ FREEZE! +${pts}`,frozenCount>=5?"#ffd700":"#06b6d4",frozenCount>=5?26:18);
      spawnParticles(hit.x,hit.y,"#06b6d4",12,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:280,born:performance.now(),color:"#06b6d4",alpha:0.55,life:600});
      sfx("feverStart");vibrate([10,5,20,5,10]);
      unlock("timefracture_tap");
      if(frozenCount>=5)unlock("timefracture_haul");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTF=levelCfgRef.current;
      if(cfgTF){if(gs.score>=cfgTF.scoreGoal&&(!cfgTF.modifier||checkModGoal(cfgTF.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VENOM ORB — antidote (500pts) if tapped within 1s; otherwise 50pts × age decay
    if(hit.type==="venom_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const ageSec=(Date.now()-hit.spawnedAt)/1000;
      const isAntidote=ageSec<1.0;
      const pts=Math.round((isAntidote?500:Math.max(50,200-ageSec*40))*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isAntidote?`💚 ANTIDOTE! +${pts}`:`☠️ VENOM! +${pts}`,isAntidote?"#4ade80":"#86efac",isAntidote?22:16);
      spawnParticles(hit.x,hit.y,isAntidote?"#4ade80":"#86efac",isAntidote?14:6,"spark");
      sfx(isAntidote?"chainBonus":"tap");vibrate(isAntidote?[8,4,10,4,8]:7);
      unlock("venomorb_tap");
      if(isAntidote)unlock("venomorb_antidote");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVO=levelCfgRef.current;
      if(cfgVO){if(gs.score>=cfgVO.scoreGoal&&(!cfgVO.modifier||checkModGoal(cfgVO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // THUNDER EGG — 150pts at phase 1; 400pts phase 2; 900pts phase 3 (full crack)
    if(hit.type==="thunder_egg"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const phase=Math.min(3,Math.floor((Date.now()-hit.spawnedAt)/1200));
      const phasePts=[150,400,900];
      const pts=Math.round((phasePts[Math.min(2,phase)]||150)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const isMax=phase>=3;
      spawnPopup(hit.x,hit.y-26,isMax?`⚡ HATCHED! +${pts}`:`🥚 CRACK×${phase+1}! +${pts}`,isMax?"#fef08a":"#fbbf24",isMax?22:16);
      spawnParticles(hit.x,hit.y,"#fef08a",isMax?16:8,"spark");
      if(isMax)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius*0.8,maxR:hit.radius*4,born:performance.now(),color:"#fbbf24",alpha:0.75,life:500});
      sfx(isMax?"chainBonus":"tap");vibrate(isMax?[10,4,14,4,10]:7);
      unlock("thunderegg_tap");
      if(isMax)unlock("thunderegg_crack");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTE=levelCfgRef.current;
      if(cfgTE){if(gs.score>=cfgTE.scoreGoal&&(!cfgTE.modifier||checkModGoal(cfgTE.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MOON GLYPH — 200pts base; jackpot 750pts when symbol = ☽ (index 0)
    if(hit.type==="moon_glyph"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const symIdx=Math.floor((Date.now()-hit.spawnedAt)/600)%5;
      const isMoon=symIdx===0;
      const pts=Math.round((isMoon?750:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isMoon?`🌙 MOON MATCH! +${pts}`:`☽ GLYPH! +${pts}`,isMoon?"#c7d2fe":"#818cf8",isMoon?22:16);
      spawnParticles(hit.x,hit.y,"#818cf8",isMoon?14:8,"spark");
      sfx(isMoon?"jackpot":"tap");vibrate(isMoon?[10,4,14,4,10]:7);
      unlock("moonglyph_tap");
      if(isMoon)unlock("moonglyph_match");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgMG=levelCfgRef.current;
      if(cfgMG){if(gs.score>=cfgMG.scoreGoal&&(!cfgMG.modifier||checkModGoal(cfgMG.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // COMET TAIL — 100pts base; +15pts per 100ms of life remaining (early = big pts)
    if(hit.type==="comet_tail"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const lifeMs=(Date.now()-hit.spawnedAt);
      const lifeLeft=Math.max(0,(hit.lifetime||2400)-lifeMs);
      const agePct=Math.min(1,lifeMs/(hit.lifetime||2400));
      const earlyBonus=Math.floor(lifeLeft/100)*15;
      const isEarly=agePct<0.5;
      const pts=Math.round((100+earlyBonus)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isEarly?`🔥 BLAZING! +${pts}`:`☄️ COMET! +${pts}`,isEarly?"#fef9c3":"#fbbf24",isEarly?22:16);
      spawnParticles(hit.x,hit.y,"#fbbf24",isEarly?14:8,"spark");
      sfx(isEarly?"chainBonus":"tap");vibrate(isEarly?[8,4,10,4,8]:7);
      unlock("comettail_tap");
      if(isEarly)unlock("comettail_early");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCT=levelCfgRef.current;
      if(cfgCT){if(gs.score>=cfgCT.scoreGoal&&(!cfgCT.modifier||checkModGoal(cfgCT.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // EMBER RING — 150pts base; 800pts jackpot at max expansion (agePct >= 0.85)
    if(hit.type==="ember_ring"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||3200));
      const isMax=agePct>=0.85;
      const pts=Math.round((isMax?800:150+agePct*350)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isMax?`🌋 FULL BLAZE! +${pts}`:`🔥 EMBER! +${pts}`,isMax?"#ffd700":"#f97316",isMax?22:16);
      spawnParticles(hit.x,hit.y,isMax?"#ffd700":"#f97316",isMax?14:8,"spark");
      sfx(isMax?"jackpot":"tap");vibrate(isMax?[10,4,14,4,10]:7);
      unlock("emberring_tap");
      if(isMax)unlock("emberring_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgER=levelCfgRef.current;
      if(cfgER){if(gs.score>=cfgER.scoreGoal&&(!cfgER.modifier||checkModGoal(cfgER.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ACCUMULATOR — 150pts × stack multiplier (1–8× based on expired targets nearby)
    if(hit.type==="accumulator"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const stacks=hit._stacks||0;
      const stackMult=Math.min(8,1+stacks);
      const isMax=stacks>=7;
      const pts=Math.round(150*stackMult*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isMax?`💥 OVERFLOW! +${pts}`:`📈 ×${stackMult} ACCUM! +${pts}`,isMax?"#ffd700":"#3b82f6",isMax?22:16);
      spawnParticles(hit.x,hit.y,"#93c5fd",isMax?16:8,"spark");
      sfx(isMax?"chainBonus":"tap");vibrate(isMax?[10,4,14,4,10]:7);
      unlock("accumulator_tap");
      if(isMax)unlock("accumulator_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAC=levelCfgRef.current;
      if(cfgAC){if(gs.score>=cfgAC.scoreGoal&&(!cfgAC.modifier||checkModGoal(cfgAC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // JACKPOT TARGET — guaranteed 2000pts base (once per level)
    if(hit.type==="jackpot_target"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(2000*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-30,`🎰 JACKPOT! +${pts}`,"#ffd700",28);
      for(let i=0;i<22;i++)spawnParticles(hit.x,hit.y,"#ffd700",1,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius*0.8,maxR:hit.radius*5,born:performance.now(),color:"#ffd700",alpha:0.8,life:700});
      sfx("jackpot");vibrate([15,8,20,8,25,8,30]);
      unlock("jackpot_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgJT=levelCfgRef.current;
      if(cfgJT){if(gs.score>=cfgJT.scoreGoal&&(!cfgJT.modifier||checkModGoal(cfgJT.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PHASE ORB — pts depend on current phase: attack=150, defense=250, bonus=700
    if(hit.type==="phase_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const phase=Math.floor((Date.now()-hit.spawnedAt)/1200)%3;
      const phaseBase=[150,250,700];const phaseNames=["⚔️ ATTACK","🛡 DEFEND","💰 BONUS!"];
      const phaseColors=["#ef4444","#3b82f6","#ffd700"];
      const pts=Math.round(phaseBase[phase]*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,`${phaseNames[phase]} +${pts}`,phaseColors[phase],phase===2?24:16);
      spawnParticles(hit.x,hit.y,phaseColors[phase],phase===2?14:8,"spark");
      sfx(phase===2?"jackpot":"tap");vibrate(phase===2?[10,4,14,4,10]:7);
      unlock("phaseorb_tap");
      if(phase===2)unlock("phaseorb_bonus");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPO=levelCfgRef.current;
      if(cfgPO){if(gs.score>=cfgPO.scoreGoal&&(!cfgPO.modifier||checkModGoal(cfgPO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // RIPPLE WAVE — 200pts + 100pts per target hit by any live ring at moment of tap
    if(hit.type==="ripple_wave"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      let ringHits=0;
      (hit._ripRings||[]).forEach(ringAge=>{
        const ringR=ringAge*hit.radius*2.5;
        targetsRef.current.forEach(t=>{
          if(!t.dying&&t.type!=="bomb"&&t.type!=="timebomb"&&Date.now()>=t.spawnedAt){
            const dist=Math.hypot(t.x-hit.x,t.y-hit.y);
            if(Math.abs(dist-ringR)<20)ringHits++;
          }
        });
      });
      const pts=Math.round((200+ringHits*100)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,ringHits>=3?`🔗 CHAIN! +${pts}`:`🌊 RIPPLE! +${pts}`,ringHits>=3?"#67e8f9":"#06b6d4",ringHits>=3?22:16);
      spawnParticles(hit.x,hit.y,"#67e8f9",ringHits>=3?14:8,"spark");
      sfx(ringHits>=3?"chainBonus":"tap");vibrate(ringHits>=3?[8,4,10,4,8]:7);
      unlock("ripplewave_tap");
      if(ringHits>=3)unlock("ripplewave_chain");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgRW=levelCfgRef.current;
      if(cfgRW){if(gs.score>=cfgRW.scoreGoal&&(!cfgRW.modifier||checkModGoal(cfgRW.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LASER GRID — 200pts base; jackpot if any beam is within 20° of north
    if(hit.type==="laser_grid"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const elapsed=(Date.now()-hit.spawnedAt)/1000;
      const rot=elapsed*0.014*1000;
      // Check if any of 4 beams is near north (top — angle = n*π/2 near 0 or 2π)
      let isAligned=false;
      for(let i=0;i<4;i++){const a=((rot+(i/4)*Math.PI*2)%(Math.PI*2));if(a<0.35||a>Math.PI*2-0.35)isAligned=true;}
      const pts=Math.round((isAligned?850:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isAligned?`🔴 LOCKED! +${pts}`:`🎯 GRID! +${pts}`,isAligned?"#fda4af":"#f43f5e",isAligned?22:16);
      spawnParticles(hit.x,hit.y,"#f43f5e",isAligned?14:8,"spark");
      sfx(isAligned?"jackpot":"tap");vibrate(isAligned?[10,4,14,4,10]:7);
      unlock("lasergrid_tap");
      if(isAligned)unlock("lasergrid_beam");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLG=levelCfgRef.current;
      if(cfgLG){if(gs.score>=cfgLG.scoreGoal&&(!cfgLG.modifier||checkModGoal(cfgLG.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // BOUNCING ORB — 200pts × bounce multiplier (each wall bounce = double, max ×8)
    if(hit.type==="bouncing_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const bounces=hit._bounces||0;
      const bounceMult=Math.min(8,1+bounces);
      const pts=Math.round(200*bounceMult*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const isMax=bounces>=7;
      spawnPopup(hit.x,hit.y-26,isMax?`💫 MAX BOUNCE! +${pts}`:`⚽ ×${bounceMult} ORB! +${pts}`,isMax?"#ffd700":"#4ade80",isMax?22:16);
      spawnParticles(hit.x,hit.y,"#4ade80",isMax?14:8,"spark");
      sfx(isMax?"jackpot":"tap");vibrate(isMax?[10,4,14,4,10]:7);
      unlock("bouncing_tap");
      if(isMax)unlock("bouncing_4x");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgBO=levelCfgRef.current;
      if(cfgBO){if(gs.score>=cfgBO.scoreGoal&&(!cfgBO.modifier||checkModGoal(cfgBO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TIME CAPSULE — 100pts normally; 1000pts jackpot if tapped in golden window (agePct 0.5-0.7)
    if(hit.type==="time_capsule"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||5000));
      const isGolden=agePct>=0.5&&agePct<=0.7;
      const pts=Math.round((isGolden?1000:100)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isGolden?`🥇 GOLDEN! +${pts}`:`⏳ CAPSULE! +${pts}`,isGolden?"#ffd700":"#f59e0b",isGolden?24:16);
      spawnParticles(hit.x,hit.y,isGolden?"#ffd700":"#f59e0b",isGolden?16:8,"spark");
      if(isGolden)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius*0.8,maxR:hit.radius*4,born:performance.now(),color:"#ffd700",alpha:0.7,life:600});
      sfx(isGolden?"jackpot":"tap");vibrate(isGolden?[10,5,16,5,10]:7);
      unlock("timecapsule_tap");
      if(isGolden)unlock("timecapsule_golden");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTCap=levelCfgRef.current;
      if(cfgTCap){if(gs.score>=cfgTCap.scoreGoal&&(!cfgTCap.modifier||checkModGoal(cfgTCap.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NETHER SHARD — score = 100 × power multiplier (1–10); at max power = 1000pts base
    if(hit.type==="nether_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||5000));
      const powerMult=Math.round(1+agePct*9);
      const isMax=powerMult>=10;
      const pts=Math.round(100*powerMult*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const col=`hsl(${270+agePct*60},80%,80%)`;
      spawnPopup(hit.x,hit.y-26,isMax?`⚫ NETHER MAX! +${pts}`:`🔮 ×${powerMult} NETHER! +${pts}`,col,isMax?24:16);
      spawnParticles(hit.x,hit.y,col,isMax?16:8,"spark");
      if(isMax)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius*0.9,maxR:hit.radius*5,born:performance.now(),color:"#a78bfa",alpha:0.8,life:600});
      sfx(isMax?"feverStart":"tap");vibrate(isMax?[12,5,18,5,12]:7);
      unlock("nethershard_tap");
      if(isMax)unlock("nethershard_max");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNS=levelCfgRef.current;
      if(cfgNS){if(gs.score>=cfgNS.scoreGoal&&(!cfgNS.modifier||checkModGoal(cfgNS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STAR RAIN DROP — 200pts bonus star; spawned by STAR_RAIN power-up
    if(hit.type==="star_rain_drop"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(200*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-18,`⭐ +${pts}`,"#ffd700",15);
      spawnParticles(hit.x,hit.y,"#ffd700",8,"spark");
      sfx("starEarn");vibrate(5);
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSR=levelCfgRef.current;
      if(cfgSR){if(gs.score>=cfgSR.scoreGoal&&(!cfgSR.modifier||checkModGoal(cfgSR.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SOLAR BURST — AoE; 200pts base + 150pts per extra target within 1.5× radius
    if(hit.type==="solar_burst"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const aoeR=hit.radius*1.5;
      const aoeTargets=targetsRef.current.filter(t=>
        !t.dying&&t.type!=="bomb"&&t.type!=="timebomb"&&Date.now()>=t.spawnedAt&&
        Math.hypot(t.x-hit.x,t.y-hit.y)<aoeR
      );
      aoeTargets.forEach(t=>{t.dying=performance.now();spawnParticles(t.x,t.y,"#fbbf24",5,"spark");});
      const isAoe=aoeTargets.length>=4;
      const pts=Math.round((200+aoeTargets.length*150)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isAoe?`🌟 SUNSTRIKE! +${pts}`:`☀️ SOLAR! +${pts}`,isAoe?"#fef9c3":"#fbbf24",isAoe?24:18);
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:aoeR*1.4,born:performance.now(),color:"#fbbf24",alpha:0.65,life:500});
      sfx(isAoe?"chainBonus":"tap");vibrate(isAoe?[8,4,14,4,8]:8);
      unlock("solarburst_tap");
      if(isAoe)unlock("solarburst_aoe");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSBr=levelCfgRef.current;
      if(cfgSBr){if(gs.score>=cfgSBr.scoreGoal&&(!cfgSBr.modifier||checkModGoal(cfgSBr.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // FROST COMET — on tap: 250pts + spawns 3 ice shards that scatter and can freeze normal targets
    if(hit.type==="frost_comet"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(250*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      // spawn 3 ice shards that scatter; they freeze nearby targets on impact
      for(let i=0;i<3;i++){
        const sa=(i/3)*Math.PI*2+Math.random()*0.3;
        const sv2=2.5+Math.random()*1.2;
        const shardId=Math.random().toString(36).slice(2);
        targetsRef.current.push({
          id:shardId,type:"frost_shard",
          x:hit.x+Math.cos(sa)*15,y:hit.y+Math.sin(sa)*15,
          radius:hit.radius*0.35,color:"#bae6fd",glow:"#7dd3fc",
          lifetime:1600,spawnedAt:Date.now(),born:performance.now(),
          moving:true,vx:Math.cos(sa)*sv2,vy:Math.sin(sa)*sv2,
          ghost:false,trail:[],hitsLeft:1,maxHits:1,dying:null,
        });
      }
      spawnPopup(hit.x,hit.y-26,`❄️ COMET! +${pts}`,"#7dd3fc",18);
      spawnParticles(hit.x,hit.y,"#bae6fd",12,"spark");
      sfx("chainBonus");vibrate([5,3,8,3,5]);
      unlock("frostcomet_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgFC=levelCfgRef.current;
      if(cfgFC){if(gs.score>=cfgFC.scoreGoal&&(!cfgFC.modifier||checkModGoal(cfgFC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // FROST SHARD — 120pts; on contact with a normal target it freezes it
    if(hit.type==="frost_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(120*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      // check if any normal target nearby to freeze
      const nearby=targetsRef.current.find(t=>!t.dying&&t.type==="normal"&&Math.hypot(t.x-hit.x,t.y-hit.y)<60);
      if(nearby){nearby.vx=0;nearby.vy=0;nearby.moving=false;nearby._frozen=true;unlock("frostcomet_freeze");}
      spawnPopup(hit.x,hit.y-14,`🧊 +${pts}`,"#bae6fd",13);
      spawnParticles(hit.x,hit.y,"#7dd3fc",5,"spark");
      sfx("tap");vibrate(3);
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgFSh=levelCfgRef.current;
      if(cfgFSh){if(gs.score>=cfgFSh.scoreGoal&&(!cfgFSh.modifier||checkModGoal(cfgFSh.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ENERGY WEB — base 200pts + 120pts per linked node in range
    if(hit.type==="energy_web"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const nodes=hit._webNodes||0;
      const pts=Math.round((200+nodes*120)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,nodes>=5?`🔗 FULL NETWORK! +${pts}`:`🕸️ WEB! +${pts}`,nodes>=5?"#a7f3d0":"#34d399",nodes>=5?22:16);
      spawnParticles(hit.x,hit.y,"#34d399",nodes>=5?14:8,"spark");
      sfx(nodes>=5?"chainBonus":"tap");vibrate(nodes>=5?[8,4,10,4,8]:7);
      unlock("energyweb_tap");
      if(nodes>=5)unlock("energyweb_nodes");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgEW=levelCfgRef.current;
      if(cfgEW){if(gs.score>=cfgEW.scoreGoal&&(!cfgEW.modifier||checkModGoal(cfgEW.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VOID RIFT — base 300pts + 200pts per target it devoured while alive
    if(hit.type==="void_rift"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const devoured=hit._devouredCount||0;
      const pts=Math.round((300+devoured*200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,devoured>=5?`💀 SINGULARITY! +${pts}`:`🌀 RIFT! +${pts}`,devoured>=5?"#c4b5fd":"#7c3aed",devoured>=5?26:18);
      for(let i=0;i<(devoured>=5?18:8);i++)spawnParticles(hit.x,hit.y,"#a78bfa",1,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:hit.radius*5,born:performance.now(),color:"#7c3aed",alpha:0.8,life:600});
      sfx(devoured>=5?"feverStart":"tap");vibrate(devoured>=5?[12,5,18,5,12]:8);
      unlock("voidrift_tap");
      if(devoured>=5)unlock("voidrift_feast");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVR=levelCfgRef.current;
      if(cfgVR){if(gs.score>=cfgVR.scoreGoal&&(!cfgVR.modifier||checkModGoal(cfgVR.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // QUANTUM LEAP — normal 200pts; jackpot 700pts if tapped within 300ms of a teleport
    if(hit.type==="quantum_leap"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const msSinceTeleport=hit._qlLastJump?Date.now()-hit._qlLastJump:9999;
      const isBlink=msSinceTeleport<300;
      const pts=Math.round((isBlink?700:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isBlink?`⚡ BLINK! +${pts}`:`🔮 LEAP! +${pts}`,isBlink?"#c7d2fe":"#818cf8",isBlink?22:16);
      spawnParticles(hit.x,hit.y,"#818cf8",isBlink?14:8,"spark");
      sfx(isBlink?"jackpot":"tap");vibrate(isBlink?[10,4,14,4,10]:7);
      unlock("qleap_tap");
      if(isBlink)unlock("qleap_between");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgQL=levelCfgRef.current;
      if(cfgQL){if(gs.score>=cfgQL.scoreGoal&&(!cfgQL.modifier||checkModGoal(cfgQL.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MAGMA BURST — pts 200 at cold; 900 at white-hot (agePct > 0.7)
    if(hit.type==="magma_burst"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4000));
      const isWhiteHot=agePct>=0.7;
      const pts=Math.round((isWhiteHot?900:150+Math.round(agePct*450))*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const col=isWhiteHot?"#fefce8":"#ef4444";
      spawnPopup(hit.x,hit.y-26,isWhiteHot?`🔥 WHITE HOT! +${pts}`:`🌋 MAGMA! +${pts}`,col,isWhiteHot?24:16);
      spawnParticles(hit.x,hit.y,col,isWhiteHot?16:8,"spark");
      if(isWhiteHot)particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius*0.9,maxR:hit.radius*3.5,born:performance.now(),color:"#fbbf24",alpha:0.7,life:450});
      sfx(isWhiteHot?"jackpot":"tap");vibrate(isWhiteHot?[10,4,14,4,10]:7);
      unlock("magma_burst_tap");
      if(isWhiteHot)unlock("magma_burst_hot");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgMB=levelCfgRef.current;
      if(cfgMB){if(gs.score>=cfgMB.scoreGoal&&(!cfgMB.modifier||checkModGoal(cfgMB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // LIGHTNING ROD — 150pts base + 60pts per attracted target at tap; bonus if 5+
    if(hit.type==="lightning_rod"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const attracted=hit._rodAttract||0;
      const pts=Math.round((150+attracted*60)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,attracted>=5?`⛈️ MEGA SURGE! +${pts}`:`⚡ STRIKE! +${pts}`,attracted>=5?"#fef08a":"#fbbf24",attracted>=5?22:16);
      spawnParticles(hit.x,hit.y,"#fef08a",attracted>=5?16:8,"spark");
      sfx(attracted>=5?"chainBonus":"tap");vibrate(attracted>=5?[8,4,12,4,8]:7);
      unlock("lightning_rod_tap");
      if(attracted>=5)unlock("lightning_rod_surge");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLR=levelCfgRef.current;
      if(cfgLR){if(gs.score>=cfgLR.scoreGoal&&(!cfgLR.modifier||checkModGoal(cfgLR.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PRISM GATE — spawns 3 colored mini-shards that scatter; the actual tap gives 150pts + shard pts
    if(hit.type==="prism_gate"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(150*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      // spawn 3 colored shards that scatter
      const shardColors=[["#f87171","#ef4444"],["#4ade80","#16a34a"],["#60a5fa","#2563eb"]];
      shardColors.forEach(([col,glow],i)=>{
        const sa=(i/3)*Math.PI*2+Math.random()*0.4;
        const sv2=2.2+Math.random()*1.0;
        targetsRef.current.push({
          id:Math.random().toString(36).slice(2),type:"prism_shard",
          x:hit.x+Math.cos(sa)*20,y:hit.y+Math.sin(sa)*20,
          radius:hit.radius*0.38,color:col,glow,lifetime:1800,
          spawnedAt:Date.now(),born:performance.now(),
          moving:true,vx:Math.cos(sa)*sv2,vy:Math.sin(sa)*sv2,
          ghost:false,trail:[],hitsLeft:1,maxHits:1,dying:null,
          _shardIdx:i,_prismParent:hit.id,
        });
      });
      gs._prismShardCatch=0;
      spawnPopup(hit.x,hit.y-26,`🔷 PRISM! +${pts}`,"#67e8f9",18);
      spawnParticles(hit.x,hit.y,"#67e8f9",10,"spark");
      sfx("tap");vibrate([5,3,5,3,5]);
      unlock("prismgate_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPG=levelCfgRef.current;
      if(cfgPG){if(gs.score>=cfgPG.scoreGoal&&(!cfgPG.modifier||checkModGoal(cfgPG.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PRISM SHARD — catch scattered prism shards; 200pts each; all 3 = tri-color bonus
    if(hit.type==="prism_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(200*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs._prismShardCatch=(gs._prismShardCatch||0)+1;
      spawnPopup(hit.x,hit.y-14,`🌈 +${pts}`,"#67e8f9",13);
      spawnParticles(hit.x,hit.y,hit.color||"#67e8f9",6,"spark");
      sfx("tap");vibrate(4);
      if(gs._prismShardCatch>=3){
        const triPts=Math.round(500*combo*feverMult);
        gs.score+=triPts;
        spawnPopup(hit.x,hit.y-28,`🌈 TRI-COLOR! +${triPts}`,"#ffd700",22);
        sfx("chainBonus");vibrate([8,4,12,4,8]);
        unlock("prismgate_triple");
      }
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPS=levelCfgRef.current;
      if(cfgPS){if(gs.score>=cfgPS.scoreGoal&&(!cfgPS.modifier||checkModGoal(cfgPS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // BUBBLE CLUSTER — pop all 5 bubbles; 120pts per bubble + 300pt combo if all hit
    if(hit.type==="bubble_cluster"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const bubblesPopped=5;const allBonus=300;
      const pts=Math.round((bubblesPopped*120+allBonus)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      for(let i=0;i<5;i++){spawnParticles(hit.x+(Math.random()-0.5)*hit.radius*1.2,hit.y+(Math.random()-0.5)*hit.radius*1.2,"#7dd3fc",3,"spark");}
      spawnPopup(hit.x,hit.y-26,`🫧 BURST! +${pts}`,"#7dd3fc",20);
      sfx("chainBonus");vibrate([4,2,4,2,4,2,4,2,4]);
      unlock("bubble_tap");unlock("bubble_all");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgBC=levelCfgRef.current;
      if(cfgBC){if(gs.score>=cfgBC.scoreGoal&&(!cfgBC.modifier||checkModGoal(cfgBC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // VOID SHARD — reclaims all absorbed pts + 200 base; bonus if absorbed >= 200
    if(hit.type==="void_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const absorbed=hit._absorbedPts||0;
      const pts=Math.round((200+absorbed)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,absorbed>=200?`💫 ABSORBED! +${pts}`:`🌑 SHARD! +${pts}`,absorbed>=200?"#38bdf8":"#0ea5e9",absorbed>=200?22:16);
      spawnParticles(hit.x,hit.y,"#38bdf8",12,"spark");
      sfx(absorbed>=200?"chainBonus":"tap");vibrate(absorbed>=200?[8,4,10,4,8]:7);
      unlock("voidshard_tap");
      if(absorbed>=200)unlock("voidshard_rich");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVS=levelCfgRef.current;
      if(cfgVS){if(gs.score>=cfgVS.scoreGoal&&(!cfgVS.modifier||checkModGoal(cfgVS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // STASIS FIELD — jackpot 800pts if tapped before the ring expands (agePct < 0.3)
    if(hit.type==="stasis_field"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const agePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||2500));
      const isCore=agePct<0.3;
      const pts=Math.round((isCore?800:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isCore?`🎯 CORE! +${pts}`:`❄️ STASIS! +${pts}`,isCore?"#ffd700":"#38bdf8",isCore?24:16);
      spawnParticles(hit.x,hit.y,isCore?"#ffd700":"#38bdf8",isCore?14:8,"spark");
      sfx(isCore?"jackpot":"tap");vibrate(isCore?[10,5,14,5,10]:7);
      unlock("stasis_tap");
      if(isCore)unlock("stasis_core");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSF=levelCfgRef.current;
      if(cfgSF){if(gs.score>=cfgSF.scoreGoal&&(!cfgSF.modifier||checkModGoal(cfgSF.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTALLINE — score based on how many facets are lit at tap time (7 = full spectrum)
    if(hit.type==="crystalline"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const elapsed=(Date.now()-hit.spawnedAt)/1000;
      const rot=elapsed*0.008*1000;
      // how many facets face the viewer: based on spin angle alignment
      const visibleFacets=Math.round(4+3*Math.abs(Math.sin(rot)));
      const isFullSpectrum=visibleFacets>=7;
      const pts=Math.round((180+visibleFacets*40)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isFullSpectrum?`✨ FULL FACET! +${pts}`:`💎 CRYSTAL! +${pts}`,isFullSpectrum?"#f0abfc":"#c084fc",isFullSpectrum?22:16);
      spawnParticles(hit.x,hit.y,"#e9d5ff",isFullSpectrum?14:8,"spark");
      sfx(isFullSpectrum?"feverStart":"tap");vibrate(isFullSpectrum?[8,4,10,4,8]:7);
      unlock("crystalline_tap");
      if(isFullSpectrum)unlock("crystalline_facets");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCR=levelCfgRef.current;
      if(cfgCR){if(gs.score>=cfgCR.scoreGoal&&(!cfgCR.modifier||checkModGoal(cfgCR.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GRAVITY WELL — pts scale with how many targets it pulled in over its lifetime
    if(hit.type==="gravity_well"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pulledCount=(hit._gwPulled||new Set()).size;
      const pts=Math.round((200+pulledCount*90)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,pulledCount>=4?`🌌 TIDAL! +${pts}`:`🌀 WELL! +${pts}`,pulledCount>=4?"#c4b5fd":"#a78bfa",pulledCount>=4?22:16);
      spawnParticles(hit.x,hit.y,"#a78bfa",12,"spark");
      sfx(pulledCount>=4?"chainBonus":"tap");vibrate(pulledCount>=4?[8,5,12,5,8]:7);
      unlock("gravwell_tap");
      if(pulledCount>=4)unlock("gravwell_feast");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGW=levelCfgRef.current;
      if(cfgGW){if(gs.score>=cfgGW.scoreGoal&&(!cfgGW.modifier||checkModGoal(cfgGW.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SPECTRUM ORB — pts multiplier based on hue; yellow phase (~60°) = 3× jackpot
    if(hit.type==="spectrum_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const elapsed=(Date.now()-hit.spawnedAt);
      const hue=(elapsed*0.22)%360;
      const isGolden=hue>=45&&hue<=75;
      const hueMult=isGolden?3.0:0.8+((Math.sin(hue*Math.PI/180)+1)/2)*1.5;
      const pts=Math.round(220*hueMult*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isGolden?`✨ GOLDEN! +${pts}`:`🌈 SPECTRUM! +${pts}`,isGolden?"#ffd700":"#e879f9",isGolden?24:16);
      spawnParticles(hit.x,hit.y,isGolden?"#ffd700":"#e879f9",isGolden?16:8,"spark");
      sfx(isGolden?"jackpot":"tap");vibrate(isGolden?[10,5,15,5,10]:7);
      unlock("spectrum_tap");
      if(isGolden)unlock("spectrum_gold");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSO=levelCfgRef.current;
      if(cfgSO){if(gs.score>=cfgSO.scoreGoal&&(!cfgSO.modifier||checkModGoal(cfgSO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SHARD STORM — jackpot 900pts when shards align (sin of rotSpeed > 0.92)
    if(hit.type==="shard_storm"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const elapsed=(Date.now()-hit.spawnedAt)/1000;
      const rotSpeed=elapsed*0.012;
      const alignScore=Math.abs(Math.sin(rotSpeed));
      const isAligned=alignScore>0.92;
      const pts=Math.round((isAligned?900:220)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isAligned?`💠 ALIGNED! +${pts}`:`❄️ STORM! +${pts}`,isAligned?"#ffd700":"#67e8f9",isAligned?24:16);
      spawnParticles(hit.x,hit.y,isAligned?"#ffd700":"#a5f3fc",isAligned?16:8,"spark");
      sfx(isAligned?"jackpot":"tap");vibrate(isAligned?[10,4,14,4,10]:7);
      unlock("shardstorm_tap");
      if(isAligned)unlock("shardstorm_align");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSS=levelCfgRef.current;
      if(cfgSS){if(gs.score>=cfgSS.scoreGoal&&(!cfgSS.modifier||checkModGoal(cfgSS.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PULSE NOVA — big pts only during the release window (chargePct >= 1.0 → releasing)
    if(hit.type==="pulse_nova"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const chargePct=Math.min(1,(Date.now()-hit.spawnedAt)/1500);
      const isReleasing=chargePct>=1.0;
      const pts=Math.round((isReleasing?850:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isReleasing?`🔥 NOVA! +${pts}`:`💥 PULSE! +${pts}`,isReleasing?"#fb7185":"#fda4af",isReleasing?24:16);
      for(let i=0;i<(isReleasing?14:8);i++)spawnParticles(hit.x,hit.y,"#fb7185",1,"spark");
      if(isReleasing){particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius*0.8,maxR:hit.radius*4,born:performance.now(),color:"#fb7185",alpha:0.7,life:500});}
      sfx(isReleasing?"feverStart":"tap");vibrate(isReleasing?[12,5,18,5,12]:7);
      unlock("pulsenova_tap");
      if(isReleasing)unlock("pulsenova_release");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPN=levelCfgRef.current;
      if(cfgPN){if(gs.score>=cfgPN.scoreGoal&&(!cfgPN.modifier||checkModGoal(cfgPN.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SURGE BALL — base 250pts; chains lightning to up to 3 nearest non-bomb targets (+100pts each)
    if(hit.type==="surge_ball"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // find nearest non-bomb targets for chain
      const candidates=targetsRef.current.filter(t=>t.type!=="bomb"&&t.type!=="chain_bomb"&&!t.dying&&Date.now()>=t.spawnedAt);
      candidates.sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y));
      const chainTargets=candidates.slice(0,3);
      const chainPts=chainTargets.length*100;
      const basePts=250;
      const pts=Math.round((basePts+chainPts)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      // visual chain arcs to each chained target
      chainTargets.forEach(ct=>{
        ct.dying=performance.now();
        spawnParticles(ct.x,ct.y,"#facc15",6,"spark");
        spawnPopup(ct.x,ct.y-16,"⚡+100","#facc15",12);
      });
      spawnPopup(hit.x,hit.y-26,`⚡ SURGE! +${pts}`,"#facc15",18);
      spawnParticles(hit.x,hit.y,"#fef08a",12,"spark");
      sfx("chainBonus");vibrate([8,4,8,4,8]);
      unlock("surgeball_tap");
      if(chainTargets.length>=3)unlock("surgeball_chain");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSB=levelCfgRef.current;
      if(cfgSB){if(gs.score>=cfgSB.scoreGoal&&(!cfgSB.modifier||checkModGoal(cfgSB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TICKING CLOCK — base 200pts; jackpot 800pts if hand is within 15° of 12 o'clock on tap
    if(hit.type==="ticking_clock"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const elapsed=(Date.now()-hit.spawnedAt)/1000;
      const totalSecs=(hit.lifetime||4000)/1000;
      const handAngleDeg=((elapsed/totalSecs)*360)%360;
      const isJackpot=handAngleDeg<=15||handAngleDeg>=345;
      const pts=Math.round((isJackpot?800:200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,isJackpot?`🕐 JACKPOT! +${pts}`:`🕐 TICK! +${pts}`,isJackpot?"#ffd700":"#94a3b8",isJackpot?22:16);
      spawnParticles(hit.x,hit.y,isJackpot?"#ffd700":"#94a3b8",isJackpot?14:8,"spark");
      sfx(isJackpot?"jackpot":"tap");vibrate(isJackpot?[10,5,15,5,10]:7);
      unlock("clock_tap");
      if(isJackpot)unlock("clock_jackpot");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTC=levelCfgRef.current;
      if(cfgTC){if(gs.score>=cfgTC.scoreGoal&&(!cfgTC.modifier||checkModGoal(cfgTC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // ECHO PULSE — collects all currently-live rings; 80pts per ring; base 150pts
    if(hit.type==="echo_pulse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const ringCount=(hit._rings||[]).length;
      const pts=Math.round((150+ringCount*80)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-26,ringCount>=3?`◎ RING BURST! +${pts}`:`◎ PULSE! +${pts}`,"#c084fc",ringCount>=3?22:16);
      for(let i=0;i<Math.max(6,ringCount*3);i++)spawnParticles(hit.x,hit.y,"#e879f9",1,"spark");
      sfx(ringCount>=3?"feverStart":"tap");vibrate(ringCount>=3?[5,3,10,3,5]:6);
      unlock("echopulse_tap");
      if(ringCount>=3)unlock("echopulse_bonus");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgEP=levelCfgRef.current;
      if(cfgEP){if(gs.score>=cfgEP.scoreGoal&&(!cfgEP.modifier||checkModGoal(cfgEP.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SOLAR DRONE — scores 300pts normally; 700pts if it was "mid-orbit" (speed > threshold)
    if(hit.type==="solar_drone"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const speed=Math.hypot(hit.vx||0,hit.vy||0);
      const isMidOrbit=speed>0.9;
      const pts=Math.round((isMidOrbit?700:300)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isMidOrbit?`☀️ SOLAR HIT! +${pts}`:`☀️ DRONE! +${pts}`,"#fbbf24",isMidOrbit?22:16);
      spawnParticles(hit.x,hit.y,"#fbbf24",14,"spark");
      sfx(isMidOrbit?"chainBonus":"tap");vibrate(isMidOrbit?[10,5,12]:8);
      unlock("solar_drone_tap");
      if(isMidOrbit)unlock("solar_drone_orbit");
      mascotHappyRef.current+=2;updateMissions(gs.sessionStats);
      const cfgSD=levelCfgRef.current;
      if(cfgSD){if(gs.score>=cfgSD.scoreGoal&&(!cfgSD.modifier||checkModGoal(cfgSD.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL CLUSTER — shatters all 4 crystals simultaneously; 100pts each + bonus
    if(hit.type==="crystal_cluster"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const crystalCount=4;const perCrystal=100;
      const pts=Math.round((perCrystal*crystalCount+200)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      // Spawn crystal shard visuals
      for(let i=0;i<8;i++){
        const a=(i/8)*Math.PI*2;
        particlesRef.current.push({type:"dot",x:hit.x+Math.cos(a)*hit.radius*0.6,y:hit.y+Math.sin(a)*hit.radius*0.6,
          color:["#38bdf8","#818cf8","#34d399","#f472b6"][i%4],alpha:1,size:5,
          vx:Math.cos(a)*1.5,vy:Math.sin(a)*1.5,born:performance.now(),duration:800});
      }
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#ffffff",size:160,born:performance.now(),duration:500,alpha:0.6});
      spawnPopup(hit.x,hit.y-34,`💠 CLUSTER SMASH! +${pts}`,"#38bdf8",22);
      spawnParticles(hit.x,hit.y,"#818cf8",20,"spark");
      sfx("legendary");vibrate([12,6,16]);
      unlock("cluster_tap");unlock("cluster_rainbow");
      mascotHappyRef.current+=4;updateMissions(gs.sessionStats);
      const cfgCC=levelCfgRef.current;
      if(cfgCC){if(gs.score>=cfgCC.scoreGoal&&(!cfgCC.modifier||checkModGoal(cfgCC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // SWARM — pops the entire swarm for 500pts; tracks session kills for master
    if(hit.type==="swarm"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(500*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gsRef.current._swarmKills=(gsRef.current._swarmKills||0)+1;
      spawnPopup(hit.x,hit.y-30,`🐝 SWARM! +${pts}`,"#fb923c",20);
      spawnParticles(hit.x,hit.y,"#f97316",18,"spark");
      sfx("chainBonus");vibrate([10,5,10]);
      unlock("swarm_tap");
      if((gsRef.current._swarmKills||0)>=3)unlock("swarm_master");
      mascotHappyRef.current+=3;updateMissions(gs.sessionStats);
      const cfgSw=levelCfgRef.current;
      if(cfgSw){if(gs.score>=cfgSw.scoreGoal&&(!cfgSw.modifier||checkModGoal(cfgSw.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CELESTIAL ORB — large slow target; 600pts; tracks session count for legend
    if(hit.type==="celestial"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(600*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gsRef.current._celestialHits=(gsRef.current._celestialHits||0)+1;
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#93c5fd",size:240,born:performance.now(),duration:700,alpha:0.5});
      spawnPopup(hit.x,hit.y-34,`🌠 CELESTIAL! +${pts}`,"#93c5fd",22);
      spawnParticles(hit.x,hit.y,"#1e40af",20,"spark");spawnParticles(hit.x,hit.y,"#93c5fd",8,"dot");
      sfx("legendary");vibrate([14,8,18]);
      unlock("celestial_tap");
      if((gsRef.current._celestialHits||0)>=5)unlock("celestial_legend");
      mascotHappyRef.current+=4;updateMissions(gs.sessionStats);
      const cfgCe=levelCfgRef.current;
      if(cfgCe){if(gs.score>=cfgCe.scoreGoal&&(!cfgCe.modifier||checkModGoal(cfgCe.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // FRACTAL — splits into 2 smaller fractals on tap (depth 0→1); depth 1 fractals score directly
    if(hit.type==="fractal"||hit.type==="fractal_mini"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const depth=hit._fractalDepth||0;
      if(depth<1){
        // Split into 2 mini fractals
        const canvas=canvasRef.current;const cw10=canvas?.width||390,ch10=canvas?.height||700;
        const basePts=Math.round(150*combo*feverMult);gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
        for(let i=0;i<2;i++){
          const a=Math.random()*Math.PI*2;const dist=40+Math.random()*30;
          const sx=Math.max(BASE_R+10,Math.min(cw10-BASE_R-10,hit.x+Math.cos(a+i*Math.PI)*dist));
          const sy=Math.max(BASE_R+100,Math.min(ch10-BASE_R-20,hit.y+Math.sin(a+i*Math.PI)*dist));
          targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"fractal_mini",
            x:sx,y:sy,radius:Math.round(BASE_R*0.7),color:hit.color,glow:hit.glow,
            lifetime:2500,spawnedAt:Date.now(),born:performance.now(),moving:true,ghost:false,
            vx:(Math.random()-0.5)*1.5,vy:(Math.random()-0.5)*1.5,trail:[],
            hitsLeft:1,maxHits:1,dying:null,rarity:null,_fractalDepth:1});
        }
        spawnPopup(hit.x,hit.y-26,`🔷 FRACTAL SPLIT! +${basePts}`,"#818cf8",18);
        spawnParticles(hit.x,hit.y,"#818cf8",14,"spark");sfx("chainBonus");vibrate([8,4,8]);
        unlock("fractal_tap");
      } else {
        // Mini fractal — score directly
        const pts=Math.round(250*combo*feverMult);gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-20,`💠 +${pts}`,"#6366f1",16);
        spawnParticles(hit.x,hit.y,"#6366f1",10,"spark");sfx("tap");vibrate(6);
        unlock("fractal_tap");unlock("fractal_cascade");
      }
      mascotHappyRef.current+=2;updateMissions(gs.sessionStats);
      const cfgFr=levelCfgRef.current;
      if(cfgFr){if(gs.score>=cfgFr.scoreGoal&&(!cfgFr.modifier||checkModGoal(cfgFr.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // PLASMA — charges up over lifetime; tap at 70-90% charge for surge jackpot
    if(hit.type==="plasma"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const chargePct=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime||4000));
      const isSurge=chargePct>=0.7&&chargePct<=0.9;
      const pts=Math.round((isSurge?1300:Math.round(300+chargePct*600))*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-30,isSurge?`⚡ PLASMA SURGE! +${pts}`:`⚡ PLASMA! +${pts}`,isSurge?"#00ffcc":"#22d3ee",isSurge?22:17);
      spawnParticles(hit.x,hit.y,isSurge?"#00ffcc":"#22d3ee",isSurge?20:12,"spark");
      if(isSurge){particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#00ffcc",size:200,born:performance.now(),duration:700,alpha:0.6});sfx("legendary");vibrate([14,8,20]);}
      else{sfx("chainBonus");vibrate(10);}
      unlock("plasma_tap");
      if(isSurge)unlock("plasma_surge");
      mascotHappyRef.current+=(isSurge?5:2);
      updateMissions(gs.sessionStats);
      const cfgPl=levelCfgRef.current;
      if(cfgPl){if(gs.score>=cfgPl.scoreGoal&&(!cfgPl.modifier||checkModGoal(cfgPl.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // TIMEWARP TARGET — extends all targets' remaining lifetime by 2s
    if(hit.type==="timewarp_tgt"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      let extended=0;
      targetsRef.current.forEach(t=>{
        if(!t.dying&&t.type!=="boss"){t.spawnedAt+=2000;t.lifetime+=2000;extended++;}
      });
      const pts=Math.round((300+extended*50)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#818cf8",size:280,born:performance.now(),duration:800,alpha:0.4});
      spawnPopup(hit.x,hit.y-34,`⏱️ +2s ALL! +${pts} (${extended} targets)`,"#c7d2fe",20);
      spawnParticles(hit.x,hit.y,"#818cf8",16,"spark");
      sfx("powerUp");vibrate([10,6,10,6,14]);
      unlock("timewarp_target_tap");
      if(extended>=5)unlock("timewarp_hoard");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgTw=levelCfgRef.current;
      if(cfgTw){if(gs.score>=cfgTw.scoreGoal&&(!cfgTw.modifier||checkModGoal(cfgTw.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // THUNDERCLAP — shockwave on tap knocks back all nearby targets
    if(hit.type==="thunderclap"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const CLAP_R=200;
      // Knock all nearby non-boss targets away from tap point
      let knocked=0;
      targetsRef.current.forEach(t=>{
        if(t.dying||t.type==="boss"||t.type==="bomb"||t.type==="timebomb")return;
        const dx=t.x-hit.x,dy=t.y-hit.y;
        const dist=Math.hypot(dx,dy)||1;
        if(dist<CLAP_R){
          knocked++;const force=3.5*(1-dist/CLAP_R);
          t.vx=(dx/dist)*force+(Math.random()-0.5)*0.5;t.vy=(dy/dist)*force+(Math.random()-0.5)*0.5;
          t.moving=true;spawnParticles(t.x,t.y,"#fde047",4,"spark");
        }
      });
      const pts=Math.round((350+knocked*90)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#fde047",size:CLAP_R,born:performance.now(),duration:600,alpha:0.8});
      spawnPopup(hit.x,hit.y-34,`💥 THUNDERCLAP! +${pts} (${knocked} knocked)`, "#fde047",20);
      spawnParticles(hit.x,hit.y,"#4338ca",20,"spark");
      setScreenShake(true);setTimeout(()=>setScreenShake(false),250);
      sfx(knocked>=4?"legendary":"bossKill");vibrate([15,8,20]);
      unlock("thunderclap_tap");
      if(knocked>=4)unlock("thunderclap_surge");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgTC=levelCfgRef.current;
      if(cfgTC){if(gs.score>=cfgTC.scoreGoal&&(!cfgTC.modifier||checkModGoal(cfgTC.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MIRROR SHARD — 3-tap diamond; each hit worth more (100/300/800pts)
    if(hit.type==="mirror_shard"){
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const tapPts=[100,300,800];const tapIdx=hit.maxHits-hit.hitsLeft; // 0=first, 1=second, 2=third
      if(hit.hitsLeft>1){
        hit.hitsLeft--;
        const pts=Math.round(tapPts[tapIdx]*combo*feverMult);gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        spawnParticles(hit.x,hit.y,"#bae6fd",8,"spark");
        spawnPopup(hit.x,hit.y-24,`💎 +${pts} (${hit.hitsLeft} left!)`,"#e0f2fe",16);
        sfx("tap");vibrate(6);
        unlock("mirror_tap");
      } else {
        // Final tap — big shatter
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const pts=Math.round(tapPts[2]*combo*feverMult);gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-34,`💎 SHATTERED! +${pts}`,"#e0f2fe",24);
        spawnParticles(hit.x,hit.y,"#bae6fd",24,"spark");spawnParticles(hit.x,hit.y,"#ffffff",8,"dot");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#bae6fd",size:160,born:performance.now(),duration:600,alpha:0.6});
        sfx("legendary");vibrate([14,6,18]);
        unlock("mirror_tap");unlock("mirror_triple");
        mascotHappyRef.current+=5;
      }
      updateMissions(gs.sessionStats);
      const cfgMs=levelCfgRef.current;
      if(cfgMs){if(gs.score>=cfgMs.scoreGoal&&(!cfgMs.modifier||checkModGoal(cfgMs.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // RUNE STONE — score based on which rune phase when tapped; power runes = jackpot
    if(hit.type==="rune_stone"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const phase=Math.floor((Date.now()-hit.spawnedAt)/1500)%8;
      const runes=["ᚱ","ᚦ","ᚨ","ᚲ","ᚷ","ᛁ","ᛃ","ᛗ"];
      const isPowerRune=phase===0||phase===1; // ᚱ or ᚦ = power runes
      const rune=runes[phase];
      const pts=Math.round((isPowerRune?1100:400)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-32,isPowerRune?`${rune} POWER RUNE! +${pts}`:`${rune} RUNE! +${pts}`,"#d8b4fe",isPowerRune?24:18);
      spawnParticles(hit.x,hit.y,"#a855f7",isPowerRune?20:12,"spark");
      if(isPowerRune){particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#a855f7",size:180,born:performance.now(),duration:600,alpha:0.5});sfx("legendary");vibrate([14,7,18]);}
      else{sfx("chainBonus");vibrate(8);}
      unlock("rune_tap");
      if(isPowerRune)unlock("rune_power");
      mascotHappyRef.current+=(isPowerRune?5:2);
      updateMissions(gs.sessionStats);
      const cfgRs=levelCfgRef.current;
      if(cfgRs){if(gs.score>=cfgRs.scoreGoal&&(!cfgRs.modifier||checkModGoal(cfgRs.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // NEBULA — cosmic cloud; releases 3-4 star pieces + scores base pts
    if(hit.type==="nebula"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const canvas=canvasRef.current;const cw9=canvas?.width||390,ch9=canvas?.height||700;
      const pieceCount=3+Math.floor(Math.random()*2); // 3-4 star pieces
      for(let i=0;i<pieceCount;i++){
        const a=(i/pieceCount)*Math.PI*2+Math.random()*0.5;
        const dist=40+Math.random()*60;
        const sx=Math.max(20,Math.min(cw9-20,hit.x+Math.cos(a)*dist));
        const sy=Math.max(100,Math.min(ch9-20,hit.y+Math.sin(a)*dist));
        setTimeout(()=>{
          targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"star_piece",
            x:sx,y:sy,radius:Math.round(BASE_R*0.6),color:"#818cf8",glow:"#6d28d9",
            lifetime:1800,spawnedAt:Date.now(),born:performance.now(),moving:false,ghost:false,
            vx:0,vy:0.5+Math.random()*0.5,trail:[],hitsLeft:1,maxHits:1,dying:null,rarity:null});
        },i*100);
      }
      const pts=Math.round((320+pieceCount*60)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#818cf8",size:180,born:performance.now(),duration:600,alpha:0.5});
      spawnPopup(hit.x,hit.y-34,`🌌 NEBULA! +${pts} → ${pieceCount} stars!`,"#818cf8",20);
      spawnParticles(hit.x,hit.y,"#c4b5fd",18,"spark");
      sfx("chainBonus");vibrate([10,6,12]);
      unlock("nebula_tap");
      if(pieceCount>=4)unlock("nebula_cluster");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgNb=levelCfgRef.current;
      if(cfgNb){if(gs.score>=cfgNb.scoreGoal&&(!cfgNb.modifier||checkModGoal(cfgNb.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // WISP — nearly invisible when moving; huge pts if caught while stationary
    if(hit.type==="wisp"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const isMoving=hit.moving&&Math.hypot(hit.vx||0,hit.vy||0)>0.1;
      const isStill=!isMoving;
      const pts=Math.round((isStill?1500:280)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isStill?`✨ PERFECT STILLNESS! +${pts}`:`🕯️ WISP! +${pts}`,"#e0f2fe",isStill?24:16);
      spawnParticles(hit.x,hit.y,"#bae6fd",isStill?20:10,"spark");
      if(isStill){sfx("legendary");vibrate([15,8,20]);particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#e0f2fe",size:200,born:performance.now(),duration:700,alpha:0.5});}
      else{sfx("tap");vibrate(8);}
      unlock("wisp_tap");
      if(isStill)unlock("wisp_still");
      mascotHappyRef.current+=(isStill?5:2);
      updateMissions(gs.sessionStats);
      const cfgWs=levelCfgRef.current;
      if(cfgWs){if(gs.score>=cfgWs.scoreGoal&&(!cfgWs.modifier||checkModGoal(cfgWs.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // METEOR — fast target; bonus pts if caught early in its trajectory
    if(hit.type==="meteor"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const ageMs=Date.now()-hit.spawnedAt;
      const lifeRatio=ageMs/(hit.lifetime||2600);
      const isEarly=lifeRatio<0.3;
      const pts=Math.round((isEarly?1100:600)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,isEarly?`☄️ EARLY CATCH! +${pts}`:`☄️ METEOR! +${pts}`,"#fde68a",isEarly?22:16);
      spawnParticles(hit.x,hit.y,"#f97316",16,"spark");
      spawnParticles(hit.x,hit.y,"#fde68a",8,"dot");
      if(isEarly){sfx("chainBonus");vibrate([12,6,16]);}else{sfx("tap");vibrate(10);}
      unlock("meteor_tap");
      if(isEarly)unlock("meteor_early");
      mascotHappyRef.current+=2;
      updateMissions(gs.sessionStats);
      const cfgMt=levelCfgRef.current;
      if(cfgMt){if(gs.score>=cfgMt.scoreGoal&&(!cfgMt.modifier||checkModGoal(cfgMt.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // AURA RING — outer edge (>70% radius) = jackpot 1200pts; center = 400pts
    if(hit.type==="aura"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // Determine tap zone: how far from center vs radius
      const canvas=canvasRef.current;
      // We need tap position relative to target center; use hit.x/y as center approximation
      // The dx,dy from the hit detection gives us the hit point
      const tapDistFromCenter=Math.hypot(tx-hit.x,ty-hit.y);
      const edgeRatio=tapDistFromCenter/hit.radius;
      const isEdge=edgeRatio>0.6;
      const pts=Math.round((isEdge?1200:400)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const hue=Math.round((Date.now()*0.08)%360);
      spawnPopup(hit.x,hit.y-32,isEdge?`🎯 EDGE SHOT! +${pts}`:`🔆 AURA! +${pts}`,`hsl(${hue},90%,65%)`,isEdge?22:17);
      spawnParticles(hit.x,hit.y,`hsl(${hue},90%,65%)`,16,"spark");
      if(isEdge){particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:`hsl(${hue},90%,65%)`,size:180,born:performance.now(),duration:600,alpha:0.6});sfx("chainBonus");vibrate([12,6,16]);}
      else{sfx("tap");vibrate(8);}
      unlock("aura_tap");
      if(isEdge)unlock("aura_edge");
      mascotHappyRef.current+=(isEdge?4:2);
      updateMissions(gs.sessionStats);
      const cfgAu=levelCfgRef.current;
      if(cfgAu){if(gs.score>=cfgAu.scoreGoal&&(!cfgAu.modifier||checkModGoal(cfgAu.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // QUANTUM — 3-tap target; teleports to random pos on each hit, big score on 3rd tap
    if(hit.type==="quantum"){
      const canvas=canvasRef.current;const cw2=canvas?.width||390,ch2=canvas?.height||700;
      if(hit.hitsLeft>1){
        // Teleport to new random position
        hit.hitsLeft--;
        const margin=hit.radius+20;
        hit.x=margin+Math.random()*(cw2-margin*2);
        hit.y=(hit.radius+100)+Math.random()*(ch2-(hit.radius+100)-margin-30);
        spawnParticles(hit.x,hit.y,"#a78bfa",8,"spark");
        spawnPopup(hit.x,hit.y-30,`🔀 QUANTUM! ${hit.hitsLeft} more!`,"#a78bfa",14);
        sfx("tap");vibrate(6);
      } else {
        // Final hit — big score
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const combo=Math.min(10,1+Math.floor(gs.streak/5));
        const feverMult=gs.feverActive?2:1;
        const pts=Math.round(900*combo*feverMult);
        gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-34,`⚛️ STABILIZED! +${pts}`,"#c4b5fd",24);
        spawnParticles(hit.x,hit.y,"#a78bfa",20,"spark");spawnParticles(hit.x,hit.y,"#ffffff",8,"dot");
        sfx("legendary");vibrate([15,8,20]);
        unlock("quantum_tap");unlock("quantum_stable");
        mascotHappyRef.current+=4;
      }
      updateMissions(gs.sessionStats);
      const cfgQ=levelCfgRef.current;
      if(cfgQ){if(gs.score>=cfgQ.scoreGoal&&(!cfgQ.modifier||checkModGoal(cfgQ.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CLOCKWORK — slows all targets to 30% speed for 4s when tapped
    if(hit.type==="clockwork"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(450*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      // Apply time-slow: use SLOW power-up mechanic + extra lifetime on all targets
      activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SLOW");
      activePwrRef.current.push({type:"SLOW",endsAt:Date.now()+4000});
      setActivePwrDisp([...activePwrRef.current]);
      spawnPopup(hit.x,hit.y-32,`⏰ TIME SLOW! +${pts}`,"#94a3b8",20);
      spawnParticles(hit.x,hit.y,"#94a3b8",16,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#94a3b8",size:300,born:performance.now(),duration:700,alpha:0.5});
      sfx("powerUp");vibrate([10,8,14]);
      unlock("clockwork_tap");
      mascotHappyRef.current+=2;
      updateMissions(gs.sessionStats);
      const cfgCW=levelCfgRef.current;
      if(cfgCW){if(gs.score>=cfgCW.scoreGoal&&(!cfgCW.modifier||checkModGoal(cfgCW.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // THUNDERBOLT — tap to catch lightning; big pts; clutch bonus if nearly off-screen
    if(hit.type==="thunderbolt"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const canvas=canvasRef.current;const ch=canvas?.height||700;
      const nearBottom=hit.y>(ch*0.75);// caught it near the bottom = clutch
      const pts=Math.round((nearBottom?1200:700)*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const label=nearBottom?`⚡ CLUTCH! +${pts}`:`⚡ LIGHTNING! +${pts}`;
      const col=nearBottom?"#fef08a":"#facc15";
      spawnPopup(hit.x,hit.y-30,label,col,nearBottom?22:17);
      spawnParticles(hit.x,hit.y,col,16,"spark");
      if(nearBottom){sfx("chainBonus");vibrate([15,8,20]);setScreenShake(true);setTimeout(()=>setScreenShake(false),220);}
      else{sfx("tap");vibrate(10);}
      unlock("thunderbolt_tap");
      if(nearBottom)unlock("thunderbolt_clutch");
      mascotHappyRef.current+=2;
      updateMissions(gs.sessionStats);
      const cfgTbolt=levelCfgRef.current;
      if(cfgTbolt){if(gs.score>=cfgTbolt.scoreGoal&&(!cfgTbolt.modifier||checkModGoal(cfgTbolt.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GRAVITY ORB — tap to score + release all pulled targets; bonus per pulled target
    if(hit.type==="gravityorb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      // Count how many targets are near the orb (they were being pulled)
      const nearby=targetsRef.current.filter(t=>!t.dying&&t.type!=="boss"&&Math.hypot(t.x-hit.x,t.y-hit.y)<180).length;
      const pullBonus=nearby*80;
      const basePts=500+pullBonus;
      const pts=Math.round(basePts*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      // Release pulled targets: push them away from orb center
      targetsRef.current.forEach(t=>{
        if(!t.dying&&t.type!=="boss"&&Math.hypot(t.x-hit.x,t.y-hit.y)<220){
          const dx=t.x-hit.x,dy=t.y-hit.y;const dist=Math.hypot(dx,dy)||1;
          t.vx=(dx/dist)*1.5+(Math.random()-0.5)*0.5;t.vy=(dy/dist)*1.5+(Math.random()-0.5)*0.5;
        }
      });
      const label=nearby>0?`🌐 GRAVITY! +${pts} (${nearby} pulled)`:`🌐 GRAVITY ORB! +${pts}`;
      spawnPopup(hit.x,hit.y-34,label,"#a78bfa",nearby>3?22:18);
      spawnParticles(hit.x,hit.y,"#7c3aed",20,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#7c3aed",size:220,born:performance.now(),duration:800,alpha:0.6});
      sfx(nearby>3?"legendary":"bossKill");vibrate([12,8,16]);
      unlock("gravityorb_tap");
      if(nearby>=4)unlock("gravityorb_cluster");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgGO=levelCfgRef.current;
      if(cfgGO){if(gs.score>=cfgGO.scoreGoal&&(!cfgGO.modifier||checkModGoal(cfgGO.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL BALL — guarantees legendary rarity on next 2 spawns; scores base pts
    if(hit.type==="crystalball"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const pts=Math.round(400*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs._luckyTaps=2; // next 2 spawns will be legendary (reuses lucky spawn logic if available)
      luckyRef.current="active"; // briefly activate lucky mode for 2 spawns
      setTimeout(()=>{
        if(gsRef.current&&!gsRef.current._crystalBallUsed)gsRef.current._crystalBallUsed=false;
        if(luckyRef.current==="active"){luckyRef.current=null;setLuckyMode(false);}
      },4000);
      setLuckyMode(true);
      spawnPopup(hit.x,hit.y-34,`🔮 CRYSTAL! +${pts} Legendary incoming!`,"#c084fc",20);
      spawnParticles(hit.x,hit.y,"#e879f9",18,"spark");spawnParticles(hit.x,hit.y,"#ffd700",8,"dot");
      sfx("lucky");vibrate([12,8,12,8,20]);
      unlock("crystalball_tap");
      mascotHappyRef.current+=4;
      updateMissions(gs.sessionStats);
      const cfgCB=levelCfgRef.current;
      if(cfgCB){if(gs.score>=cfgCB.scoreGoal&&(!cfgCB.modifier||checkModGoal(cfgCB.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // FIREFLY — tiny fast; 500 base pts, bonus if caught within 0.8s of spawn
    if(hit.type==="firefly"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(12,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const ageMs=Date.now()-hit.spawnedAt;
      const freshBonus=ageMs<800?2:1; // double pts if caught fast
      const pts=Math.round(500*combo*feverMult*freshBonus);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const label=freshBonus===2?"✨ SWIFT CATCH! +"+pts:"✨ Firefly! +"+pts;
      spawnPopup(hit.x,hit.y-30,label,"#84ef63",freshBonus===2?20:16);
      spawnParticles(hit.x,hit.y,"#bbf7d0",14,"spark");spawnParticles(hit.x,hit.y,"#ffffff",6,"dot");
      sfx("tap");if(freshBonus===2){sfx("chainBonus");vibrate([10,5,15]);}else vibrate(8);
      unlock("firefly_tap");
      if(freshBonus===2&&(gs._swiftCatches||0)===0){gs._swiftCatches=1;unlock("firefly_swift");}
      else if(freshBonus===2)gs._swiftCatches=(gs._swiftCatches||0)+1;
      mascotHappyRef.current+=2;
      updateMissions(gs.sessionStats);
      const cfgFf=levelCfgRef.current;
      if(cfgFf){if(gs.score>=cfgFf.scoreGoal&&(!cfgFf.modifier||checkModGoal(cfgFf.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // GEODE — 2-tap rock; first crack reveals gem, second awards big score + coins
    if(hit.type==="geode"){
      if(hit.hitsLeft===2){
        // First tap — crack the shell
        hit.hitsLeft=1;hit._geodeCracked=true;
        hit.color="#a855f7";hit.glow="#7c3aed";
        spawnParticles(hit.x,hit.y,"#94a3b8",12,"spark");
        spawnPopup(hit.x,hit.y-26,"🪨 CRACKED! Tap again!","#94a3b8",15);
        sfx("tap");vibrate([8,12,8]);
      } else {
        // Second tap — collect the gem
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const combo=Math.min(10,1+Math.floor(gs.streak/5));
        const feverMult=gs.feverActive?2:1;
        const pts=Math.round(650*combo*feverMult);
        const coinReward=3+Math.floor(Math.random()*4);
        gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
        saveRef.current.coins=(saveRef.current.coins||0)+coinReward;
        saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+coinReward;
        spawnPopup(hit.x,hit.y-30,`💎 GEM! +${pts} +${coinReward}🪙`,"#c084fc",22);
        spawnParticles(hit.x,hit.y,"#c084fc",18,"spark");spawnParticles(hit.x,hit.y,"#ffd700",10,"dot");
        sfx("legendary");vibrate([12,8,20]);
        unlock("geode_gem");
        mascotHappyRef.current+=4;
        updateMissions(gs.sessionStats);
        const cfgGd=levelCfgRef.current;
        if(cfgGd){if(gs.score>=cfgGd.scoreGoal&&(!cfgGd.modifier||checkModGoal(cfgGd.modifier,gs))){endLevel(true);return;}}
      }
      unlock("geode_crack");
      return;
    }
    // TIME BOMB — tap to defuse; 800 pts base; clutch bonus if < 0.4s left
    if(hit.type==="timebomb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const timeLeftMs=Math.max(0,(hit.spawnedAt+hit.lifetime)-Date.now());
      const clutch=timeLeftMs<400;
      const basePts=clutch?1400:800;
      const pts=Math.round(basePts*combo*feverMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      const label=clutch?`💣 CLUTCH DEFUSE! +${pts}`:`💣 DEFUSED! +${pts}`;
      const col=clutch?"#fbbf24":"#f97316";
      spawnPopup(hit.x,hit.y-32,label,col,clutch?22:18);
      spawnParticles(hit.x,hit.y,col,16,"spark");
      if(clutch){spawnParticles(hit.x,hit.y,"#ffffff",8,"dot");sfx("legendary");vibrate([15,8,20]);setScreenShake(true);setTimeout(()=>setScreenShake(false),250);}
      else{sfx("bossKill");vibrate([10,6,12]);}
      unlock("timebomb_defuse");
      if(clutch)unlock("timebomb_clutch");
      mascotHappyRef.current+=3;
      updateMissions(gs.sessionStats);
      const cfgTb=levelCfgRef.current;
      if(cfgTb){if(gs.score>=cfgTb.scoreGoal&&(!cfgTb.modifier||checkModGoal(cfgTb.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // BEACON — stationary amplifier; tap to collect and score nearby bonus
    if(hit.type==="beacon"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const BEACON_R=110;
      const nearbyCount=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb"&&Math.hypot(t.x-hit.x,t.y-hit.y)<BEACON_R).length;
      const basePts=Math.round((150+nearbyCount*60)*combo*feverMult*prestigeMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      // Remove beacon boost flag from nearby targets
      targetsRef.current.forEach(t=>{if(t._beaconBoost)t._beaconBoost=false;});
      spawnParticles(hit.x,hit.y,"#fbbf24",16,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#fbbf24",size:BEACON_R*1.5,born:performance.now(),duration:600,alpha:0.5});
      spawnPopup(hit.x,hit.y-44,`🔆 BEACON! ×${nearbyCount} nearby +${basePts}`,"#fbbf24",nearbyCount>3?24:20);
      sfx(nearbyCount>3?"legendary":"epic");
      if(nearbyCount>3){setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);}
      vibrate([15,8,20,8,25]);
      unlock("beacon_tap");
      if(nearbyCount>=4)unlock("beacon_surge");
      mascotHappyRef.current+=2+nearbyCount;
      const cfgBn=levelCfgRef.current;
      if(cfgBn){if(gs.score>=cfgBn.scoreGoal&&(!cfgBn.modifier||checkModGoal(cfgBn.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // SHADOW — tap leaves a shadow_clone at same position; clone gives bonus if caught
    if(hit.type==="shadow"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const basePts=Math.round(100*combo*feverMult*prestigeMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      // Spawn shadow clone at same position — 1.5s window
      targetsRef.current.push({
        id:Math.random().toString(36).slice(2),type:"shadow_clone",
        x:hit.x,y:hit.y,radius:BASE_R*0.9,color:"#4338ca",glow:"#6366f1",
        rarity:RARITY.RARE,lifetime:1500,spawnedAt:Date.now(),born:performance.now(),
        moving:false,ghost:false,vx:0,vy:0,trail:[],hitsLeft:1,maxHits:1,dying:null
      });
      spawnParticles(hit.x,hit.y,"#6366f1",12,"spark");
      spawnPopup(hit.x,hit.y-36,`🕶️ SHADOW! +${basePts} — catch clone!`,"#818cf8",20);
      sfx("rare");vibrate([12,6,12]);
      unlock("shadow_tap");
      mascotHappyRef.current+=2;
      const cfgSh=levelCfgRef.current;
      if(cfgSh){if(gs.score>=cfgSh.scoreGoal&&(!cfgSh.modifier||checkModGoal(cfgSh.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // SHADOW CLONE — bonus tap after a shadow target; short window
    if(hit.type==="shadow_clone"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const cloneBonus=Math.round(300*combo*feverMult*prestigeMult);
      gs.score+=cloneBonus;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      spawnParticles(hit.x,hit.y,"#c7d2fe",14,"spark");
      spawnPopup(hit.x,hit.y-40,`👤 CLONE CAUGHT! +${cloneBonus}`,"#c7d2fe",22);
      sfx("epic");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);
      vibrate([15,8,25]);
      unlock("shadow_clone_catch");
      mascotHappyRef.current+=4;
      const cfgSc=levelCfgRef.current;
      if(cfgSc){if(gs.score>=cfgSc.scoreGoal&&(!cfgSc.modifier||checkModGoal(cfgSc.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // PORTAL — teleports all targets to random new positions; base score + chaos bonus
    if(hit.type==="portal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const canvas=canvasRef.current;const cw=canvas?.width||390,ch=canvas?.height||700;
      const teleported=targetsRef.current.filter(t=>!t.dying&&t.type!=="boss"&&t.type!=="bomb").length;
      const basePts=Math.round((100+teleported*30)*combo*feverMult*prestigeMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      // Teleport all non-boss targets
      targetsRef.current.forEach(t=>{
        if(t.dying||t.type==="boss"||t.type==="bomb")return;
        const newX=t.radius+Math.random()*(cw-t.radius*2);
        const newY=t.radius+95+Math.random()*(ch-t.radius-100);
        spawnParticles(t.x,t.y,"#818cf8",6,"spark");
        t.x=newX;t.y=newY;
        if(t.trail)t.trail=[];
      });
      spawnParticles(hit.x,hit.y,"#818cf8",20,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#818cf8",size:hit.radius*3.5,born:performance.now(),duration:800,alpha:0.7});
      spawnPopup(hit.x,hit.y-48,`🌀 PORTAL! ×${teleported} teleported! +${basePts}`,"#818cf8",teleported>5?24:20);
      sfx("legendary");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);
      setScreenShake(true);setTimeout(()=>setScreenShake(false),350);
      vibrate([18,8,18,8,35]);
      unlock("portal_tap");
      if(teleported>=5)unlock("portal_chaos");
      mascotHappyRef.current+=2+Math.floor(teleported/2);
      const cfgP=levelCfgRef.current;
      if(cfgP){if(gs.score>=cfgP.scoreGoal&&(!cfgP.modifier||checkModGoal(cfgP.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // PARTICLE BOMB — explodes into 12 scoreable spark particles on tap
    if(hit.type==="particlebomb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const basePts=Math.round(80*combo*feverMult*prestigeMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      const canvas=canvasRef.current;const cw=canvas?.width||390,ch=canvas?.height||700;
      // Explosion of spark particles that fly outward and give points when they expire
      const BOMB_SPARKS=12;
      let sparkPts=0;
      for(let i=0;i<BOMB_SPARKS;i++){
        const angle=(i/BOMB_SPARKS)*Math.PI*2+Math.random()*0.3;
        const speed=2+Math.random()*3;
        const sparkColor=i%3===0?"#ffed4a":i%3===1?"#ff8c00":"#f43f5e";
        const travelTime=600+Math.random()*400;
        const landX=Math.max(30,Math.min(cw-30,hit.x+Math.cos(angle)*speed*travelTime*0.05));
        const landY=Math.max(100,Math.min(ch-30,hit.y+Math.sin(angle)*speed*travelTime*0.05));
        particlesRef.current.push({
          type:"dot",x:hit.x,y:hit.y,
          vx:Math.cos(angle)*speed,vy:Math.sin(angle)*speed,
          color:sparkColor,alpha:1,scale:1.5,born:performance.now(),duration:travelTime
        });
        setTimeout(()=>{
          const spts=Math.round(20*combo*feverMult*prestigeMult);
          sparkPts+=spts;gs.score+=spts;gs.sessionStats.score=gs.score;
          spawnParticles(landX,landY,sparkColor,4,"spark");
          spawnPopup(landX,landY-16,`+${spts}`,"#ff8c00",10);
        },travelTime);
      }
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#ff4500",size:hit.radius*2.5,born:performance.now(),duration:600,alpha:0.8});
      spawnParticles(hit.x,hit.y,"#ffed4a",16,"spark");
      spawnPopup(hit.x,hit.y-42,`💥 BOMB! +${basePts} +sparks!`,"#ff8c00",20);
      sfx("bossKill");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);
      vibrate([20,8,20,8,40]);
      unlock("particlebomb_tap");
      mascotHappyRef.current+=3;
      const cfgBomb=levelCfgRef.current;
      if(cfgBomb){if(gs.score>=cfgBomb.scoreGoal&&(!cfgBomb.modifier||checkModGoal(cfgBomb.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // VOLTAGE — chain-zaps up to 3 nearby normal targets when tapped
    if(hit.type==="voltage"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const basePts=Math.round(120*combo*feverMult*prestigeMult);
      gs.score+=basePts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      spawnParticles(hit.x,hit.y,"#fbbf24",14,"spark");
      spawnPopup(hit.x,hit.y-36,`⚡ VOLTAGE! +${basePts}`,"#fbbf24",20);
      sfx("chainBonus");vibrate([10,5,20,5,10]);
      // Chain-zap up to 3 nearby targets within 100px
      const CHAIN_R=100;
      const nearby=targetsRef.current
        .filter(t=>!t.dying&&(t.type==="normal"||t.type==="phantom")&&Math.hypot(t.x-hit.x,t.y-hit.y)<CHAIN_R)
        .slice(0,3);
      nearby.forEach((zt,i)=>{
        setTimeout(()=>{
          if(!zt.dying){
            zt.dying=performance.now();
            const zpts=Math.round(60*combo*feverMult*prestigeMult);
            gs.score+=zpts;gs.sessionStats.score=gs.score;
            spawnParticles(zt.x,zt.y,"#facc15",8,"spark");
            spawnPopup(zt.x,zt.y-20,`⚡+${zpts}`,"#fbbf24",12);
            sfx("comboNote",gs.streak+i);
          }
        },i*120);
      });
      if(nearby.length>0)setNotif(`⚡ Chain zap! ×${nearby.length} targets!`);
      unlock("voltage_tap");
      mascotHappyRef.current+=1+nearby.length;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // VOID — absorbs up to 5 nearby targets for massive bonus score
    if(hit.type==="void"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const canvas=canvasRef.current;
      const VOID_R=120;
      const absorbed=targetsRef.current.filter(
        t=>!t.dying&&t.type!=="boss"&&t.type!=="bomb"&&t.type!=="siphon"&&Math.hypot(t.x-hit.x,t.y-hit.y)<VOID_R
      ).slice(0,5);
      // Remove absorbed targets
      const absorbedIds=new Set(absorbed.map(t=>t.id));
      targetsRef.current=targetsRef.current.filter(t=>!absorbedIds.has(t.id));
      const absorbBonus=absorbed.length*150;
      const basePts=Math.round((300+absorbBonus)*combo*feverMult*prestigeMult);
      gs.score+=basePts;gs.streak+=Math.min(3,absorbed.length);gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;gs.sessionStats.rareHits++;
      // Implosion particle effect
      absorbed.forEach(t=>{
        spawnParticles(t.x,t.y,"#a855f7",8,"spark");
      });
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#7c3aed",size:hit.radius*3,born:performance.now(),duration:700,alpha:0.8});
      spawnParticles(hit.x,hit.y,"#c084fc",18,"spark");
      spawnPopup(hit.x,hit.y-46,absorbed.length>0?`🌀 VOID! +${basePts} (×${absorbed.length+1} abs!)`:`🌀 VOID! +${basePts}`,"#c084fc",absorbed.length>2?26:22);
      if(absorbed.length>=3){sfx("legendary");setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),800);setScreenShake(true);setTimeout(()=>setScreenShake(false),400);}
      else{sfx("epic");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);}
      vibrate([20,8,20,8,40]);
      unlock("void_tap");
      if(absorbed.length>=3)unlock("void_master");
      mascotHappyRef.current+=2+absorbed.length;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // HOMING — scores based on how close it is to center when tapped
    if(hit.type==="homing"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const canvas=canvasRef.current;const cw=canvas?.width||390,ch=canvas?.height||700;
      const centerDist=Math.hypot(hit.x-cw/2,hit.y-ch/2);
      const maxDist=Math.hypot(cw/2,ch/2);
      const proximityBonus=Math.max(0,1-centerDist/maxDist); // 0=edge, 1=center
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round((60+proximityBonus*140)*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("comboNote",gs.streak);vibrate([10,5,10]);
      spawnParticles(hit.x,hit.y,"#34d399",12,"spark");
      const prxLabel=proximityBonus>0.8?"🎯 BULLSEYE!":proximityBonus>0.5?"✅ CLOSE!":"🟡 NEAR";
      spawnPopup(hit.x,hit.y-28,`${prxLabel} +${pts}`,"#4ade80",15+Math.floor(proximityBonus*6));
      if(proximityBonus>0.85)unlock("homing_center");
      unlock("homing_tap");
      mascotHappyRef.current++;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // VANISHING — bonus points if hit while invisible (blind timing)
    if(hit.type==="vanishing"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const cycleLen=800;
      const phase=(performance.now()%cycleLen)/cycleLen;
      const isVisible=phase<0.4;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const basePts=isVisible?70:200; // blind hit = 200 base pts!
      const pts=Math.round(basePts*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx(isVisible?"comboNote":"legendary");vibrate(isVisible?[10,6,10]:[20,10,20,10,40]);
      spawnParticles(hit.x,hit.y,"#67e8f9",isVisible?8:16,"spark");
      const label=isVisible?`👻 VANISH! +${pts}`:`🎯 BLIND HIT! +${pts}`;
      const color=isVisible?"#bae6fd":"#ffd700";
      spawnPopup(hit.x,hit.y-30,label,color,isVisible?14:20);
      if(!isVisible){sfx("jackpot");unlock("vanishing_blind");}
      unlock("vanishing_tap");
      mascotHappyRef.current++;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // DIVIDER — scores points then splits into 2 medium moving targets
    if(hit.type==="divider"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(70*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("rare");vibrate([12,6,12]);
      const canvas=canvasRef.current;const cw=canvas?.width||390,ch=canvas?.height||700;
      spawnParticles(hit.x,hit.y,"#fb923c",12,"spark");
      spawnPopup(hit.x,hit.y-28,`÷ SPLIT! +${pts}`,"#fdba74",15);
      // Spawn 2 medium sub-targets flying outward
      [0,1].forEach(si=>{
        const angle=(si===0?-0.4:0.4)+Math.random()*0.3-0.15;
        const speed=1.8+Math.random()*1.2;
        const subX=Math.max(hit.radius+20,Math.min(cw-hit.radius-20,hit.x));
        const subY=Math.max(hit.radius+100,Math.min(ch-hit.radius-20,hit.y));
        const subRar=RARITY.find(r=>r.name==="common");
        targetsRef.current.push({
          id:Math.random().toString(36).slice(2),type:"normal",
          x:subX,y:subY,radius:Math.round(hit.radius*0.62),
          color:"#fdba74",glow:"#f97316",rarity:subRar||RARITY[0],
          lifetime:2800,spawnedAt:Date.now(),born:performance.now(),
          moving:true,ghost:false,vx:Math.cos(angle)*speed,vy:Math.sin(angle)*speed,
          trail:[],hitsLeft:1,maxHits:1,dying:null,_isShard:true,
        });
      });
      unlock("divider_tap");
      mascotHappyRef.current++;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // RAGE — scores more points the more enraged (higher rageLevel) it is
    if(hit.type==="rage"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const rageLevel=Math.min(1,(Date.now()-hit.spawnedAt)/(hit.lifetime*0.9));
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round((80+rageLevel*220)*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      const rageColor=rageLevel>0.7?"#dc2626":rageLevel>0.4?"#f97316":"#fbbf24";
      sfx("bossKill");vibrate([20,10,20]);
      spawnParticles(hit.x,hit.y,rageColor,14,"spark");
      spawnParticles(hit.x,hit.y,"#fca5a5",6,"dot");
      const rageLabel=rageLevel>0.8?"😡 MAX RAGE!":rageLevel>0.5?"😠 ENRAGED!":"😤 ANGRY!";
      spawnPopup(hit.x,hit.y-30,`${rageLabel} +${pts}`,rageColor,16+Math.floor(rageLevel*6));
      if(rageLevel>=0.8)unlock("rage_max");
      else unlock("rage_tap");
      mascotHappyRef.current++;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // CRYSTAL — shatters into 3 scoreable shards flying outward
    if(hit.type==="crystal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(80*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("epic");vibrate([15,8,15]);
      const canvas=canvasRef.current;const cw=canvas?.width||390,ch=canvas?.height||700;
      spawnParticles(hit.x,hit.y,"#bfdbfe",12,"spark");
      spawnParticles(hit.x,hit.y,"#60a5fa",6,"dot");
      spawnPopup(hit.x,hit.y-30,`💎 CRYSTAL +${pts}`,"#93c5fd",16);
      // Spawn 3 shards flying outward
      const shardBorn=performance.now();
      const groupId=Math.random().toString(36).slice(2);
      [0,1,2].forEach(si=>{
        const angle=(si/3)*Math.PI*2+Math.random()*0.5;
        const flyDist=55+Math.random()*45;
        const sx=Math.max(hit.radius+20,Math.min(cw-hit.radius-20,hit.x+Math.cos(angle)*flyDist));
        const sy=Math.max(hit.radius+100,Math.min(ch-hit.radius-20,hit.y+Math.sin(angle)*flyDist));
        targetsRef.current.push({
          id:Math.random().toString(36).slice(2),type:"crystal_shard",
          x:sx,y:sy,radius:18,color:"#93c5fd",glow:"#3b82f6",
          rarity:hit.rarity,lifetime:2000,spawnedAt:Date.now(),born:shardBorn,
          moving:false,ghost:false,vx:0,vy:0,trail:[],
          hitsLeft:1,maxHits:1,dying:null,_shardBorn:shardBorn,_shardGroup:groupId,
        });
      });
      gs._crystalGroupId=groupId;gs._crystalShardsHit=0;
      unlock("crystal_shatter");
      mascotHappyRef.current++;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // CRYSTAL SHARD — bonus score after crystal shatter
    if(hit.type==="crystal_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round(45*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("comboNote",gs.streak);vibrate(8);
      spawnParticles(hit.x,hit.y,"#bfdbfe",7,"spark");
      spawnPopup(hit.x,hit.y-24,`✨ SHARD +${pts}`,"#bfdbfe",13);
      // Check if this is the third shard in the group
      if(hit._shardGroup&&gs._crystalGroupId===hit._shardGroup){
        gs._crystalShardsHit=(gs._crystalShardsHit||0)+1;
        if(gs._crystalShardsHit>=3){
          const bonus=Math.round(200*combo*feverMult*prestigeMult);
          gs.score+=bonus;
          spawnPopup(hit.x,hit.y-50,`💠 FULL CRYSTAL! +${bonus}`,"#60a5fa",20);
          sfx("legendary");vibrate([20,10,20,10,40]);
          unlock("crystal_chain");
        }
      }
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // TORNADO — scrambles nearby target positions in a vortex, then scores
    if(hit.type==="tornado"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const VORTEX_R=120;
      let vortexCount=0;
      targetsRef.current.forEach(t=>{
        if(t.dying||t.type==="boss")return;
        const dx=t.x-hit.x,dy=t.y-hit.y;
        const dist=Math.hypot(dx,dy);
        if(dist<VORTEX_R&&dist>0){
          const angle=Math.atan2(dy,dx)+Math.PI/2; // perpendicular (tangential kick)
          const force=((VORTEX_R-dist)/VORTEX_R)*40;
          t.x=Math.max(t.radius+10,Math.min(canvasRef.current?.width||390-t.radius-10,t.x+Math.cos(angle)*force));
          t.y=Math.max(t.radius+100,Math.min(canvasRef.current?.height||700-t.radius-10,t.y+Math.sin(angle)*force));
          vortexCount++;
        }
      });
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const pts=Math.round((150+vortexCount*30)*combo*feverMult*prestigeMult);
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      hit.dying=performance.now();
      sfx("chainBonus");vibrate([10,5,15,5,10]);
      spawnParticles(hit.x,hit.y,"#67e8f9",25,"spark");
      spawnPopup(hit.x,hit.y-30,`🌀 VORTEX! +${pts}`,"#67e8f9",18);
      if(vortexCount>=3)spawnPopup(hit.x,hit.y-52,`×${vortexCount} SCATTER!`,"#06b6d4",13);
      setEpicFlash(true);setTimeout(()=>setEpicFlash(false),400);
      unlock("tornado_catch");
      mascotHappyRef.current++;
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // LOOT CHEST — drops 3-5 coins and bonus points on tap
    if(hit.type==="lootchest"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const coinDrop=3+Math.floor(Math.random()*3); // 3-5 coins
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const isMultiplierL=activePwrRef.current.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
      const pts=Math.round(80*combo*feverMult*prestigeMult*(isMultiplierL?3:1));
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      saveRef.current.coins=(saveRef.current.coins||0)+coinDrop;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+coinDrop;
      hit.dying=performance.now();
      sfx("coin");sfx("treasure");vibrate([10,5,20,5,30]);
      spawnParticles(hit.x,hit.y,"#ffd700",25,"spark");
      spawnParticles(hit.x,hit.y,"#fef3c7",10,"dot");
      spawnPopup(hit.x,hit.y-28,`🪙 ×${coinDrop} +${pts}`,"#ffd700",20);
      // Cascade: 3 coin popups raining down
      for(let i=0;i<coinDrop;i++){
        const ox=(Math.random()-0.5)*60,oy=-(20+i*18);
        setTimeout(()=>spawnPopup(hit.x+ox,hit.y+oy,"🪙","#ffd700",14),i*80);
      }
      unlock("loot_chest");
      mascotHappyRef.current+=2;
      debounceSave();
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // MIMIC — copies last tapped rarity for its score
    if(hit.type==="mimic"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      // Use last rarity mult (default UNCOMMON if no recent hit)
      const lastMult=gs.lastRarityMult||RARITY.UNCOMMON.mult;
      const lastColor=gs.lastRarityColor||RARITY.UNCOMMON.color;
      const lastName=gs.lastRarityName||"uncommon";
      const pts=Math.round(lastMult*combo*feverMult*prestigeMult*1.3); // 1.3× mimic bonus
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      if(lastName!=="common")gs.sessionStats.rareHits++;
      // Visual: mimic morphs into the copied rarity color
      hit.dying=performance.now();
      sfx(lastName==="legendary"?"legendary":lastName==="epic"?"epic":lastName==="rare"?"rare":"uncommon");
      vibrate([10,8,10]);
      spawnParticles(hit.x,hit.y,lastColor,30,"spark");
      spawnParticles(hit.x,hit.y,"#ffffff",10,"dot");
      spawnPopup(hit.x,hit.y-22,`🪞 MIMIC! +${pts}`,lastColor,18);
      sfx("comboNote",gs.streak);
      unlock("mimic_hit");
      // Save coins
      saveRef.current.coins=(saveRef.current.coins||0)+Math.max(1,Math.floor(pts*0.09));
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+Math.max(1,Math.floor(pts*0.09));
      // Win check
      const cfg2=levelCfgRef.current;
      if(cfg2){const scoreWin=gs.score>=cfg2.scoreGoal;if(scoreWin&&(!cfg2.modifier||checkModGoal(cfg2.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // ANCHOR — freezes all current targets for 3 seconds
    if(hit.type==="anchor"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const pts=Math.round(120*(gs.feverActive?2:1));
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      // Freeze all current targets
      const freezeUntil=Date.now()+3000;
      targetsRef.current.forEach(t=>{t._anchorFrozenVx=t.vx;t._anchorFrozenVy=t.vy;t.vx=0;t.vy=0;t._frozenUntil=freezeUntil;});
      gs._anchorFreezeUntil=freezeUntil;
      sfx("powerUp");vibrate([10,8,20,8,10]);
      spawnParticles(hit.x,hit.y,"#06b6d4",22,"dot");
      spawnParticles(hit.x,hit.y,"#67e8f9",10,"spark");
      spawnPopup(hit.x,hit.y-24,"⚓ FROZEN! +"+pts,"#06b6d4",20);
      setScreenShake(false); // not shake — calm freeze effect
      sfx("comboNote",gs.streak);
      const cfg=levelCfgRef.current;
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // SPLITTER — splits into 3 small targets
    if(hit.type==="splitter"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const pts=Math.round(80*(gs.feverActive?2:1));
      gs.score+=pts;gs.streak++;gs.lastTapTime=Date.now();
      gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
      sfx("tap");vibrate([5,5,15]);
      spawnParticles(hit.x,hit.y,"#f97316",18,"spark");
      spawnPopup(hit.x,hit.y-24,"💥 SPLIT! +"+pts,"#f97316",18);
      // Spawn 3 small normal targets radiating outward
      const cfg=levelCfgRef.current;
      for(let i=0;i<3;i++){
        const ang=i*Math.PI*2/3+Math.random()*0.4;
        const sr=BASE_R*0.75;
        const sx=Math.max(sr+8,Math.min((canvasRef.current?.width||360)-sr-8,hit.x+Math.cos(ang)*55));
        const sy=Math.max(sr+8,Math.min((canvasRef.current?.height||720)-sr-8,hit.y+Math.sin(ang)*55));
        const shard={
          id:Math.random().toString(36).slice(2),type:"normal",
          rarity:RARITY.UNCOMMON,color:RARITY.UNCOMMON.color,glow:RARITY.UNCOMMON.glow,
          x:sx,y:sy,radius:sr,
          lifetime:Math.max(1500,(cfg?.targetLifetime||2500)*0.7),
          spawnedAt:Date.now(),born:performance.now(),
          moving:true,vx:Math.cos(ang)*1.2,vy:Math.sin(ang)*1.2,
          ghost:false,hitsLeft:1,maxHits:1,trail:[],
          worldId:cfg?.world||1,worldColor:cfg?.worldColor||"#ff6030",
          _isShard:true, // reward is halved for shards
        };
        targetsRef.current.push(shard);
      }
      sfx("comboNote",gs.streak);
      if(cfg){if(gs.score>=cfg.scoreGoal&&(!cfg.modifier||checkModGoal(cfg.modifier,gs))){endLevel(true);return;}}
      return;
    }

    // TREASURE CHEST — jackpot variable reward
    if(hit.type==="treasure"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const cfg=levelCfgRef.current;
      const combo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult=gs.feverActive?2:1;
      const rewards=[80,120,200,350,600,1000];
      const reward=rewards[Math.floor(Math.random()*rewards.length)]*combo*feverMult;
      const coins=Math.floor(reward*0.25);
      gs.score+=reward;
      saveRef.current.coins=(saveRef.current.coins||0)+coins;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+coins;
      gs.sessionStats.score=gs.score;
      sfx("jackpot");vibrate([20,15,20,15,40,15,80]);
      spawnParticles(hit.x,hit.y,"#ffd700",60,"spark");
      spawnParticles(hit.x,hit.y,"#ff6030",20,"spark");
      spawnPopup(hit.x,hit.y-28,`💰 +${reward}!`,"#ffd700",22);
      setTimeout(()=>spawnPopup(hit.x,hit.y+10,`+${coins}🪙`,"#ffc800",15),300);
      setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),1100);
      setScreenShake(true);setTimeout(()=>setScreenShake(false),500);
      const cfg2=levelCfgRef.current;
      if(cfg2){const scoreWin=gs.score>=cfg2.scoreGoal;if(scoreWin&&(!cfg2.modifier||checkModGoal(cfg2.modifier,gs))){endLevel(true);return;}}
      return;
    }
    // MYSTERY BOX — slot machine reveal
    if(hit.type==="mystery"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      sfx("jackpot");vibrate([20,15,20,15,40]);
      gs.mysteryPause=Date.now()+1600; // prevent new mystery during reveal
      // Pick weighted prize (LUCK_SURGE: only rare+ prizes)
      const luckSurge=activePwrRef.current.some(p=>p.type==="LUCK_SURGE"&&p.endsAt>Date.now());
      const prizePool=luckSurge?MYSTERY_PRIZES.filter(p=>p.weight<=30):MYSTERY_PRIZES;
      const total=(prizePool.length?prizePool:MYSTERY_PRIZES).reduce((s,p)=>s+p.weight,0);
      let rand=Math.random()*total;
      let prize=(prizePool.length?prizePool:MYSTERY_PRIZES)[prizePool.length?prizePool.length-1:MYSTERY_PRIZES.length-1];
      for(const p of (prizePool.length?prizePool:MYSTERY_PRIZES)){rand-=p.weight;if(rand<=0){prize=p;break;}}
      // Build 3 reels — last one is always the actual prize
      const fakeReel=()=>Math.floor(Math.random()*MYSTERY_PRIZES.length);
      const prizeIdx=MYSTERY_PRIZES.indexOf(prize);
      setMysteryReveal({phase:0,reels:[fakeReel(),fakeReel(),prizeIdx],prize,x:hit.x,y:hit.y});
      showMascotSpeech("mystery");
      setTimeout(()=>setMysteryReveal(r=>r?{...r,phase:1}:null),420);
      setTimeout(()=>{setMysteryReveal(r=>r?{...r,phase:2}:null);sfx("coin");vibrate(15);},820);
      setTimeout(()=>{setMysteryReveal(r=>r?{...r,phase:3}:null);sfx("coin");vibrate(15);},1200);
      setTimeout(()=>{
        const gs2=gsRef.current;
        if(gs2){
          if(prize.type==="points"||prize.type==="jackpot"){
            gs2.score+=prize.value;
            spawnPopup(hit.x,hit.y-50,`🎁 +${prize.value}pts!`,"#ffd700",prize.type==="jackpot"?26:22);
            if(prize.type==="jackpot"){setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),1200);vibrate([30,15,60]);}
          } else if(prize.type==="coins"){
            saveRef.current.coins=(saveRef.current.coins||0)+prize.value;flushSave();
            spawnPopup(hit.x,hit.y-50,`🪙 +${prize.value}!`,"#ffd700",22);
          } else if(prize.type==="life"){
            gs2.lives=Math.min(MAX_LIVES,gs2.lives+1);
            spawnPopup(hit.x,hit.y-50,"❤️ EXTRA LIFE!","#ef4444",20);
          }
        }
        setMysteryReveal(null);
        sfx("levelComplete");vibrate(40);
        showSpeech(prize.type==="jackpot"?"JACKPOT!! YESSS!! 🌈":"Lucky! Lucky! ⭐");
      },1650);
      return;
    }
    // BOMB
    if(hit.type==="nebula_core"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo91nc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult91nc=gs.feverActive?2:1;
      const isCenter91=hitDist<hit.radius*0.2;
      const pts91nc=Math.round((isCenter91?270:85)*combo91nc*feverMult91nc);
      gs.score+=pts91nc;gs.streak++;gs.lastTapTime=Date.now();
      if(isCenter91){
        spawnPopup(hit.x,hit.y-36,`🌌 CENTER ×3 +${pts91nc}`,"#818cf8",26);
        spawnParticles(hit.x,hit.y,"#e0e7ff",14,"spark");
        sfx("legendary");vibrate([5,3,12,3,5]);
        unlock("nebulacore_center");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌌 +${pts91nc}`,"#6366f1",20);
        spawnParticles(hit.x,hit.y,"#c7d2fe",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("nebulacore_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNC91=levelCfgRef.current;
      if(cfgNC91){if(gs.score>=cfgNC91.scoreGoal&&(!cfgNC91.modifier||checkModGoal(cfgNC91.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="dusk_sphere"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo91to=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult91to=gs.feverActive?2:1;
      const isDusk91=(hit._toPhase91||0)>0.65;
      const pts91to=Math.round((isDusk91?180:80)*combo91to*feverMult91to);
      gs.score+=pts91to;gs.streak++;gs.lastTapTime=Date.now();
      if(isDusk91){
        spawnPopup(hit.x,hit.y-36,`🌅 DUSK! ×2 +${pts91to}`,"#c4b5fd",26);
        spawnParticles(hit.x,hit.y,"#ede9fe",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("twilightorb_dusk");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌄 +${pts91to}`,"#a78bfa",20);
        spawnParticles(hit.x,hit.y,"#f3e8ff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("twilightorb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTO91=levelCfgRef.current;
      if(cfgTO91){if(gs.score>=cfgTO91.scoreGoal&&(!cfgTO91.modifier||checkModGoal(cfgTO91.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="omega_heart"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo100oh=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult100oh=gs.feverActive?2:1;
      const isSync100=(hit._ohSync100||0)>0.9;
      const pts100oh=Math.round((isSync100?500:120)*combo100oh*feverMult100oh);
      gs.score+=pts100oh;gs.streak++;gs.lastTapTime=Date.now();
      if(isSync100){
        spawnPopup(hit.x,hit.y-44,`♾️ OMEGA SYNC ×5! +${pts100oh}`,"#ec4899",34);
        spawnParticles(hit.x,hit.y,"#fce7f3",22,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#ec4899",size:200,born:performance.now(),duration:1000,alpha:0.7});
        sfx("legendary");setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),1000);vibrate([12,5,20,5,12,5,20]);
        unlock("omegaheart_sync");
      } else {
        spawnPopup(hit.x,hit.y-30,`💜 +${pts100oh}`,"#f9a8d4",22);
        spawnParticles(hit.x,hit.y,"#fce7f3",10,"dot");
        sfx("epic");vibrate([8,4,12]);
      }
      unlock("omegaheart_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgOH100=levelCfgRef.current;
      if(cfgOH100){if(gs.score>=cfgOH100.scoreGoal&&(!cfgOH100.modifier||checkModGoal(cfgOH100.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="nexus_gem"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo100ng=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult100ng=gs.feverActive?2:1;
      const isCenter100=hitDist<hit.radius*0.2;
      const pts100ng=Math.round((isCenter100?400:110)*combo100ng*feverMult100ng);
      gs.score+=pts100ng;gs.streak++;gs.lastTapTime=Date.now();
      if(isCenter100){
        spawnPopup(hit.x,hit.y-40,`💎 GEM CORE ×4! +${pts100ng}`,"#38bdf8",32);
        spawnParticles(hit.x,hit.y,"#e0f2fe",20,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#38bdf8",size:160,born:performance.now(),duration:800,alpha:0.6});
        sfx("legendary");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),700);vibrate([10,5,16,5,10,5,16]);
        unlock("nexusgem_core");
      } else {
        spawnPopup(hit.x,hit.y-28,`🌐 +${pts100ng}`,"#7dd3fc",22);
        spawnParticles(hit.x,hit.y,"#e0f2fe",8,"dot");
        sfx("epic");vibrate([8,4,12]);
      }
      unlock("nexusgem_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgNG100=levelCfgRef.current;
      if(cfgNG100){if(gs.score>=cfgNG100.scoreGoal&&(!cfgNG100.modifier||checkModGoal(cfgNG100.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="plasma_halo"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo99ph=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult99ph=gs.feverActive?2:1;
      const isSweet99=hitDist>hit.radius*0.55&&hitDist<hit.radius*0.92;
      const pts99ph=Math.round((isSweet99?270:90)*combo99ph*feverMult99ph);
      gs.score+=pts99ph;gs.streak++;gs.lastTapTime=Date.now();
      if(isSweet99){
        spawnPopup(hit.x,hit.y-36,`💜 SWEETSPOT ×3! +${pts99ph}`,"#e879f9",28);
        spawnParticles(hit.x,hit.y,"#f5d0fe",16,"spark");
        sfx("legendary");vibrate([8,4,14,4,8]);
        unlock("plasmahalo_sweet");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔱 +${pts99ph}`,"#a855f7",20);
        spawnParticles(hit.x,hit.y,"#ede9fe",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("plasmahalo_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgPH99=levelCfgRef.current;
      if(cfgPH99){if(gs.score>=cfgPH99.scoreGoal&&(!cfgPH99.modifier||checkModGoal(cfgPH99.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="glitch_node"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo99gn=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult99gn=gs.feverActive?2:1;
      const isBurst99=((hit._gnBurst99||0)>0.6&&(hit._gnBurst99||0)<0.8);
      const pts99gn=Math.round((isBurst99?180:80)*combo99gn*feverMult99gn);
      gs.score+=pts99gn;gs.streak++;gs.lastTapTime=Date.now();
      if(isBurst99){
        spawnPopup(hit.x,hit.y-36,`💾 BURST ×2! +${pts99gn}`,"#4ade80",26);
        spawnParticles(hit.x,hit.y,"#dcfce7",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("glitchnode_burst");
      } else {
        spawnPopup(hit.x,hit.y-26,`⚡ +${pts99gn}`,"#34d399",20);
        spawnParticles(hit.x,hit.y,"#d1fae5",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("glitchnode_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGN99=levelCfgRef.current;
      if(cfgGN99){if(gs.score>=cfgGN99.scoreGoal&&(!cfgGN99.modifier||checkModGoal(cfgGN99.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="shadow_crystal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo101sc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult101sc=gs.feverActive?2:1;
      const isDark101=((hit._scDark101||0)>0.65&&(hit._scDark101||0)<0.85);
      const pts101sc=Math.round((isDark101?270:90)*combo101sc*feverMult101sc);
      gs.score+=pts101sc;gs.streak++;gs.lastTapTime=Date.now();
      if(isDark101){
        spawnPopup(hit.x,hit.y-38,`🖤 DARK CORE ×3! +${pts101sc}`,"#7c3aed",28);
        spawnParticles(hit.x,hit.y,"#ede9fe",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#7c3aed",size:130,born:performance.now(),duration:700,alpha:0.5});
        sfx("epic");vibrate([8,4,12,4,8]);
        unlock("shadowcrystal_dark");
      } else {
        spawnPopup(hit.x,hit.y-26,`💎 +${pts101sc}`,"#a78bfa",20);
        spawnParticles(hit.x,hit.y,"#f5f3ff",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("shadowcrystal_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgSC101=levelCfgRef.current;
      if(cfgSC101){if(gs.score>=cfgSC101.scoreGoal&&(!cfgSC101.modifier||checkModGoal(cfgSC101.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="tide_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo101to=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult101to=gs.feverActive?2:1;
      const isCrest101=((hit._toCrest101||0)>0.75&&(hit._toCrest101||0)<0.9);
      const pts101to=Math.round((isCrest101?220:100)*combo101to*feverMult101to);
      gs.score+=pts101to;gs.streak++;gs.lastTapTime=Date.now();
      if(isCrest101){
        spawnPopup(hit.x,hit.y-36,`🌊 WAVE CREST ×2! +${pts101to}`,"#22d3ee",26);
        spawnParticles(hit.x,hit.y,"#cffafe",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("tideorb_crest");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔮 +${pts101to}`,"#67e8f9",20);
        spawnParticles(hit.x,hit.y,"#ecfeff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("tideorb_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgTO101=levelCfgRef.current;
      if(cfgTO101){if(gs.score>=cfgTO101.scoreGoal&&(!cfgTO101.modifier||checkModGoal(cfgTO101.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="astral_core"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo111ac=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult111ac=gs.feverActive?2:1;
      const isAlign111=((hit._acAlign111||0)>0.8&&(hit._acAlign111||0)<0.92);
      const pts111ac=Math.round((isAlign111?270:90)*combo111ac*feverMult111ac);
      gs.score+=pts111ac;gs.streak++;gs.lastTapTime=Date.now();
      if(isAlign111){
        spawnPopup(hit.x,hit.y-38,`🌟 ASTRAL ALIGN ×3! +${pts111ac}`,"#818cf8",28);
        spawnParticles(hit.x,hit.y,"#e0e7ff",16,"spark");
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("astralcore_align");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔮 +${pts111ac}`,"#6366f1",20);
        spawnParticles(hit.x,hit.y,"#e0e7ff",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("astralcore_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgAC111=levelCfgRef.current;
      if(cfgAC111){if(gs.score>=cfgAC111.scoreGoal&&(!cfgAC111.modifier||checkModGoal(cfgAC111.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="light_bolt"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo111lb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult111lb=gs.feverActive?2:1;
      const isCharged111=(hit._lbCharge111||0)>0.88;
      const pts111lb=Math.round((isCharged111?270:90)*combo111lb*feverMult111lb);
      gs.score+=pts111lb;gs.streak++;gs.lastTapTime=Date.now();
      if(isCharged111){
        spawnPopup(hit.x,hit.y-38,`⚡ FULL CHARGE ×3! +${pts111lb}`,"#eab308",28);
        spawnParticles(hit.x,hit.y,"#fef9c3",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#eab308",size:120,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("lightbolt_charged");
      } else {
        spawnPopup(hit.x,hit.y-26,`💡 +${pts111lb}`,"#fbbf24",20);
        spawnParticles(hit.x,hit.y,"#fef3c7",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("lightbolt_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgLB111=levelCfgRef.current;
      if(cfgLB111){if(gs.score>=cfgLB111.scoreGoal&&(!cfgLB111.modifier||checkModGoal(cfgLB111.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="solar_eye"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo112se=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult112se=gs.feverActive?2:1;
      const isOpen112=(hit._seOpen112||0)>0.88;
      const pts112se=Math.round((isOpen112?270:90)*combo112se*feverMult112se);
      gs.score+=pts112se;gs.streak++;gs.lastTapTime=Date.now();
      if(isOpen112){
        spawnPopup(hit.x,hit.y-38,`☀️ EYE OPEN ×3! +${pts112se}`,"#fbbf24",28);
        spawnParticles(hit.x,hit.y,"#fef3c7",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#fbbf24",size:120,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("solareye_open");
      } else {
        spawnPopup(hit.x,hit.y-26,`👁️ +${pts112se}`,"#f59e0b",20);
        spawnParticles(hit.x,hit.y,"#fef9c3",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("solareye_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgSE112=levelCfgRef.current;
      if(cfgSE112){if(gs.score>=cfgSE112.scoreGoal&&(!cfgSE112.modifier||checkModGoal(cfgSE112.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="void_serpent"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo112vs=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult112vs=gs.feverActive?2:1;
      const isVert112=(hit._vsVert112||0)>0.85;
      const pts112vs=Math.round((isVert112?180:90)*combo112vs*feverMult112vs);
      gs.score+=pts112vs;gs.streak++;gs.lastTapTime=Date.now();
      if(isVert112){
        spawnPopup(hit.x,hit.y-38,`🌑 SERPENT STRIKE ×2! +${pts112vs}`,"#7c3aed",28);
        spawnParticles(hit.x,hit.y,"#ddd6fe",14,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#7c3aed",size:115,born:performance.now(),duration:650,alpha:0.5});
        sfx("epic");vibrate([5,3,9,3,5]);
        unlock("voidserpent_vert");
      } else {
        spawnPopup(hit.x,hit.y-26,`🐍 +${pts112vs}`,"#8b5cf6",20);
        spawnParticles(hit.x,hit.y,"#ede9fe",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("voidserpent_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgVS112=levelCfgRef.current;
      if(cfgVS112){if(gs.score>=cfgVS112.scoreGoal&&(!cfgVS112.modifier||checkModGoal(cfgVS112.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="moon_petal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo113mp=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult113mp=gs.feverActive?2:1;
      const isBloom113=(hit._mpBloom113||0)>0.88;
      const pts113mp=Math.round((isBloom113?270:90)*combo113mp*feverMult113mp);
      gs.score+=pts113mp;gs.streak++;gs.lastTapTime=Date.now();
      if(isBloom113){
        spawnPopup(hit.x,hit.y-38,`🌸 FULL BLOOM ×3! +${pts113mp}`,"#a78bfa",28);
        spawnParticles(hit.x,hit.y,"#ede9fe",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#a78bfa",size:120,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("moonpetal_bloom");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌙 +${pts113mp}`,"#c4b5fd",20);
        spawnParticles(hit.x,hit.y,"#ede9fe",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("moonpetal_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgMP113=levelCfgRef.current;
      if(cfgMP113){if(gs.score>=cfgMP113.scoreGoal&&(!cfgMP113.modifier||checkModGoal(cfgMP113.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="star_dust2"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo113sd=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult113sd=gs.feverActive?2:1;
      const hasTrail113=(hit._sd2Trail113||0)>0.75;
      const pts113sd=Math.round((hasTrail113?180:90)*combo113sd*feverMult113sd);
      gs.score+=pts113sd;gs.streak++;gs.lastTapTime=Date.now();
      if(hasTrail113){
        spawnPopup(hit.x,hit.y-38,`✨ DUST TRAIL ×2! +${pts113sd}`,"#fbbf24",28);
        spawnParticles(hit.x,hit.y,"#fef3c7",14,"spark");
        sfx("epic");vibrate([5,3,9,3,5]);
        unlock("stardust2_trail");
      } else {
        spawnPopup(hit.x,hit.y-26,`⭐ +${pts113sd}`,"#f59e0b",20);
        spawnParticles(hit.x,hit.y,"#fef9c3",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("stardust2_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgSD113=levelCfgRef.current;
      if(cfgSD113){if(gs.score>=cfgSD113.scoreGoal&&(!cfgSD113.modifier||checkModGoal(cfgSD113.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="ice_nova2"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo114in=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult114in=gs.feverActive?2:1;
      const isFreeze114=(hit._in2Frz114||0)>0.88;
      const pts114in=Math.round((isFreeze114?270:90)*combo114in*feverMult114in);
      gs.score+=pts114in;gs.streak++;gs.lastTapTime=Date.now();
      if(isFreeze114){
        spawnPopup(hit.x,hit.y-38,`❄️ DEEP FREEZE ×3! +${pts114in}`,"#93c5fd",28);
        spawnParticles(hit.x,hit.y,"#dbeafe",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#93c5fd",size:130,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("icenova2_freeze");
      } else {
        spawnPopup(hit.x,hit.y-26,`💎 +${pts114in}`,"#60a5fa",20);
        spawnParticles(hit.x,hit.y,"#dbeafe",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("icenova2_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgIN114=levelCfgRef.current;
      if(cfgIN114){if(gs.score>=cfgIN114.scoreGoal&&(!cfgIN114.modifier||checkModGoal(cfgIN114.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="arc_gem"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo114ag=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult114ag=gs.feverActive?2:1;
      const isCharged114=(hit._agCharge114||0)>0.88;
      const pts114ag=Math.round((isCharged114?270:90)*combo114ag*feverMult114ag);
      gs.score+=pts114ag;gs.streak++;gs.lastTapTime=Date.now();
      if(isCharged114){
        spawnPopup(hit.x,hit.y-38,`⚡ ARC CHARGED ×3! +${pts114ag}`,"#34d399",28);
        spawnParticles(hit.x,hit.y,"#d1fae5",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#34d399",size:120,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("arcgem_charged");
      } else {
        spawnPopup(hit.x,hit.y-26,`💎 +${pts114ag}`,"#10b981",20);
        spawnParticles(hit.x,hit.y,"#d1fae5",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("arcgem_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgAG114=levelCfgRef.current;
      if(cfgAG114){if(gs.score>=cfgAG114.scoreGoal&&(!cfgAG114.modifier||checkModGoal(cfgAG114.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="flame_orb2"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo115fo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult115fo=gs.feverActive?2:1;
      const isPeak115=(hit._fo2Heat115||0)>0.88;
      const pts115fo=Math.round((isPeak115?270:90)*combo115fo*feverMult115fo);
      gs.score+=pts115fo;gs.streak++;gs.lastTapTime=Date.now();
      if(isPeak115){
        spawnPopup(hit.x,hit.y-38,`🔥 FLAME PEAK ×3! +${pts115fo}`,"#f97316",28);
        spawnParticles(hit.x,hit.y,"#ffedd5",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#f97316",size:125,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("flameorb2_peak");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔮 +${pts115fo}`,"#fb923c",20);
        spawnParticles(hit.x,hit.y,"#fed7aa",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("flameorb2_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgFO115=levelCfgRef.current;
      if(cfgFO115){if(gs.score>=cfgFO115.scoreGoal&&(!cfgFO115.modifier||checkModGoal(cfgFO115.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="thor_gem"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo115tg=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult115tg=gs.feverActive?2:1;
      const isStorm115=((hit._tgStorm115||0)>0.75&&(hit._tgStorm115||0)<0.92);
      const pts115tg=Math.round((isStorm115?180:90)*combo115tg*feverMult115tg);
      gs.score+=pts115tg;gs.streak++;gs.lastTapTime=Date.now();
      if(isStorm115){
        spawnPopup(hit.x,hit.y-38,`⚡ STORM STRIKE ×2! +${pts115tg}`,"#fbbf24",28);
        spawnParticles(hit.x,hit.y,"#fef3c7",14,"spark");
        sfx("epic");vibrate([5,3,9,3,5]);
        unlock("thorgem_storm");
      } else {
        spawnPopup(hit.x,hit.y-26,`💎 +${pts115tg}`,"#f59e0b",20);
        spawnParticles(hit.x,hit.y,"#fef9c3",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("thorgem_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgTG115=levelCfgRef.current;
      if(cfgTG115){if(gs.score>=cfgTG115.scoreGoal&&(!cfgTG115.modifier||checkModGoal(cfgTG115.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="cosmic_lens"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo116cl=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult116cl=gs.feverActive?2:1;
      const isFocus116=(hit._clFocus116||0)>0.87;
      const pts116cl=Math.round((isFocus116?270:90)*combo116cl*feverMult116cl);
      gs.score+=pts116cl;gs.streak++;gs.lastTapTime=Date.now();
      if(isFocus116){
        spawnPopup(hit.x,hit.y-38,`🔭 COSMIC FOCUS ×3! +${pts116cl}`,"#818cf8",28);
        spawnParticles(hit.x,hit.y,"#e0e7ff",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#818cf8",size:120,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("cosmiclens_focus");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌌 +${pts116cl}`,"#6366f1",20);
        spawnParticles(hit.x,hit.y,"#e0e7ff",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("cosmiclens_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgCL116=levelCfgRef.current;
      if(cfgCL116){if(gs.score>=cfgCL116.scoreGoal&&(!cfgCL116.modifier||checkModGoal(cfgCL116.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="nebula_burst2"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo116nb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult116nb=gs.feverActive?2:1;
      const isNova116=(hit._nb2Nova116||0)>0.88;
      const pts116nb=Math.round((isNova116?270:90)*combo116nb*feverMult116nb);
      gs.score+=pts116nb;gs.streak++;gs.lastTapTime=Date.now();
      if(isNova116){
        spawnPopup(hit.x,hit.y-38,`🌠 NOVA RELEASE ×3! +${pts116nb}`,"#c084fc",28);
        spawnParticles(hit.x,hit.y,"#f3e8ff",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#c084fc",size:130,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("nebulaburst2_nova");
      } else {
        spawnPopup(hit.x,hit.y-26,`💫 +${pts116nb}`,"#a855f7",20);
        spawnParticles(hit.x,hit.y,"#f3e8ff",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("nebulaburst2_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgNB116=levelCfgRef.current;
      if(cfgNB116){if(gs.score>=cfgNB116.scoreGoal&&(!cfgNB116.modifier||checkModGoal(cfgNB116.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="dark_pulsar"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo110dp=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult110dp=gs.feverActive?2:1;
      const isBurst110=((hit._dpBurst110||0)>0.82&&(hit._dpBurst110||0)<0.95);
      const pts110dp=Math.round((isBurst110?270:90)*combo110dp*feverMult110dp);
      gs.score+=pts110dp;gs.streak++;gs.lastTapTime=Date.now();
      if(isBurst110){
        spawnPopup(hit.x,hit.y-38,`💫 PULSAR BURST ×3! +${pts110dp}`,"#a855f7",28);
        spawnParticles(hit.x,hit.y,"#ede9fe",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#7c3aed",size:130,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("darkpulsar_burst");
      } else {
        spawnPopup(hit.x,hit.y-26,`🖤 +${pts110dp}`,"#7c3aed",20);
        spawnParticles(hit.x,hit.y,"#f3e8ff",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("darkpulsar_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgDP110=levelCfgRef.current;
      if(cfgDP110){if(gs.score>=cfgDP110.scoreGoal&&(!cfgDP110.modifier||checkModGoal(cfgDP110.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="crystal_bomb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo110cb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult110cb=gs.feverActive?2:1;
      const lifePct110=(Date.now()-hit.spawnedAt)/hit.lifetime;
      const isSafe110=lifePct110<0.7;
      const pts110cb=Math.round((isSafe110?200:85)*combo110cb*feverMult110cb);
      gs.score+=pts110cb;gs.streak++;gs.lastTapTime=Date.now();
      if(isSafe110){
        spawnPopup(hit.x,hit.y-36,`💎 SAFE DEFUSE ×2! +${pts110cb}`,"#34d399",26);
        spawnParticles(hit.x,hit.y,"#d1fae5",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("crystalbomb_safe");
      } else {
        spawnPopup(hit.x,hit.y-26,`💣 +${pts110cb}`,"#f87171",20);
        spawnParticles(hit.x,hit.y,"#fee2e2",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("crystalbomb_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgCB110=levelCfgRef.current;
      if(cfgCB110){if(gs.score>=cfgCB110.scoreGoal&&(!cfgCB110.modifier||checkModGoal(cfgCB110.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="glacier_spike"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo109gs=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult109gs=gs.feverActive?2:1;
      const isTip109=hitDist<hit.radius*0.12;
      const pts109gs=Math.round((isTip109?270:90)*combo109gs*feverMult109gs);
      gs.score+=pts109gs;gs.streak++;gs.lastTapTime=Date.now();
      if(isTip109){
        spawnPopup(hit.x,hit.y-38,`🧊 SPIKE TIP ×3! +${pts109gs}`,"#7dd3fc",28);
        spawnParticles(hit.x,hit.y,"#e0f2fe",16,"spark");
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("glacierspike_tip");
      } else {
        spawnPopup(hit.x,hit.y-26,`⚡ +${pts109gs}`,"#38bdf8",20);
        spawnParticles(hit.x,hit.y,"#e0f2fe",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("glacierspike_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgGS109=levelCfgRef.current;
      if(cfgGS109){if(gs.score>=cfgGS109.scoreGoal&&(!cfgGS109.modifier||checkModGoal(cfgGS109.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="plasma_pulse2"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo109pp=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult109pp=gs.feverActive?2:1;
      const isPeak109=((hit._pp2Peak109||0)>0.88&&(hit._pp2Peak109||0)<0.97);
      const pts109pp=Math.round((isPeak109?270:90)*combo109pp*feverMult109pp);
      gs.score+=pts109pp;gs.streak++;gs.lastTapTime=Date.now();
      if(isPeak109){
        spawnPopup(hit.x,hit.y-38,`💜 PULSE CREST ×3! +${pts109pp}`,"#c084fc",28);
        spawnParticles(hit.x,hit.y,"#f3e8ff",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#c084fc",size:120,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("plasmapulse2_peak");
      } else {
        spawnPopup(hit.x,hit.y-26,`〰️ +${pts109pp}`,"#a855f7",20);
        spawnParticles(hit.x,hit.y,"#ede9fe",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("plasmapulse2_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgPP109=levelCfgRef.current;
      if(cfgPP109){if(gs.score>=cfgPP109.scoreGoal&&(!cfgPP109.modifier||checkModGoal(cfgPP109.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="quantum_burst2"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo108qb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult108qb=gs.feverActive?2:1;
      const pulsePct108=(hit._qb2Pulse108||0);
      const isTiny108=pulsePct108<0.15||pulsePct108>0.92;
      const pts108qb=Math.round((isTiny108?270:90)*combo108qb*feverMult108qb);
      gs.score+=pts108qb;gs.streak++;gs.lastTapTime=Date.now();
      if(isTiny108){
        spawnPopup(hit.x,hit.y-38,`⚛️ QUANTUM TINY ×3! +${pts108qb}`,"#7c3aed",28);
        spawnParticles(hit.x,hit.y,"#ede9fe",16,"spark");
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("quantumburst2_tiny");
      } else {
        spawnPopup(hit.x,hit.y-26,`💥 +${pts108qb}`,"#a855f7",20);
        spawnParticles(hit.x,hit.y,"#f3e8ff",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("quantumburst2_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgQB108=levelCfgRef.current;
      if(cfgQB108){if(gs.score>=cfgQB108.scoreGoal&&(!cfgQB108.modifier||checkModGoal(cfgQB108.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="nebula_ring"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo108nr=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult108nr=gs.feverActive?2:1;
      const isOuter108=hitDist>hit.radius*0.75;
      const pts108nr=Math.round((isOuter108?200:90)*combo108nr*feverMult108nr);
      gs.score+=pts108nr;gs.streak++;gs.lastTapTime=Date.now();
      if(isOuter108){
        spawnPopup(hit.x,hit.y-36,`🌌 RING EDGE ×2! +${pts108nr}`,"#818cf8",26);
        spawnParticles(hit.x,hit.y,"#e0e7ff",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("nebularing_outer");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔱 +${pts108nr}`,"#6366f1",20);
        spawnParticles(hit.x,hit.y,"#e0e7ff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("nebularing_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgNR108=levelCfgRef.current;
      if(cfgNR108){if(gs.score>=cfgNR108.scoreGoal&&(!cfgNR108.modifier||checkModGoal(cfgNR108.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="flux_ring2"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo107fr=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult107fr=gs.feverActive?2:1;
      const isInner107=hitDist<hit.radius*0.5;
      const pts107fr=Math.round((isInner107?200:85)*combo107fr*feverMult107fr);
      gs.score+=pts107fr;gs.streak++;gs.lastTapTime=Date.now();
      if(isInner107){
        spawnPopup(hit.x,hit.y-36,`〰️ RING CORE ×2! +${pts107fr}`,"#3b82f6",26);
        spawnParticles(hit.x,hit.y,"#dbeafe",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("fluxring2_inner");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔵 +${pts107fr}`,"#60a5fa",20);
        spawnParticles(hit.x,hit.y,"#eff6ff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("fluxring2_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgFR107=levelCfgRef.current;
      if(cfgFR107){if(gs.score>=cfgFR107.scoreGoal&&(!cfgFR107.modifier||checkModGoal(cfgFR107.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="solar_vortex"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo107sv=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult107sv=gs.feverActive?2:1;
      const isEye107=hitDist<hit.radius*0.25;
      const pts107sv=Math.round((isEye107?270:90)*combo107sv*feverMult107sv);
      gs.score+=pts107sv;gs.streak++;gs.lastTapTime=Date.now();
      if(isEye107){
        spawnPopup(hit.x,hit.y-38,`☀️ VORTEX EYE ×3! +${pts107sv}`,"#f59e0b",28);
        spawnParticles(hit.x,hit.y,"#fef3c7",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#f59e0b",size:130,born:performance.now(),duration:700,alpha:0.5});
        sfx("legendary");vibrate([6,3,12,3,6]);
        unlock("solarvortex_eye");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌀 +${pts107sv}`,"#fbbf24",20);
        spawnParticles(hit.x,hit.y,"#fef9c3",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("solarvortex_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgSV107=levelCfgRef.current;
      if(cfgSV107){if(gs.score>=cfgSV107.scoreGoal&&(!cfgSV107.modifier||checkModGoal(cfgSV107.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="drift_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo106do=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult106do=gs.feverActive?2:1;
      const isStill106=((hit._doStill106||0)>0.47&&(hit._doStill106||0)<0.53);
      const pts106do=Math.round((isStill106?200:90)*combo106do*feverMult106do);
      gs.score+=pts106do;gs.streak++;gs.lastTapTime=Date.now();
      if(isStill106){
        spawnPopup(hit.x,hit.y-36,`⏸️ DRIFT STILL ×2! +${pts106do}`,"#6366f1",26);
        spawnParticles(hit.x,hit.y,"#e0e7ff",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("driftorb_still");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌀 +${pts106do}`,"#818cf8",20);
        spawnParticles(hit.x,hit.y,"#e0e7ff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("driftorb_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgDO106=levelCfgRef.current;
      if(cfgDO106){if(gs.score>=cfgDO106.scoreGoal&&(!cfgDO106.modifier||checkModGoal(cfgDO106.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="chrono_star"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo106cs=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult106cs=gs.feverActive?2:1;
      const isApex106=((hit._csApex106||0)>0.85&&(hit._csApex106||0)<0.97);
      const pts106cs=Math.round((isApex106?270:90)*combo106cs*feverMult106cs);
      gs.score+=pts106cs;gs.streak++;gs.lastTapTime=Date.now();
      if(isApex106){
        spawnPopup(hit.x,hit.y-38,`⭐ TIME APEX ×3! +${pts106cs}`,"#c084fc",28);
        spawnParticles(hit.x,hit.y,"#f3e8ff",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#c084fc",size:120,born:performance.now(),duration:700,alpha:0.5});
        sfx("epic");vibrate([6,3,12,3,6]);
        unlock("chronostar_apex");
      } else {
        spawnPopup(hit.x,hit.y-26,`⏱️ +${pts106cs}`,"#a78bfa",20);
        spawnParticles(hit.x,hit.y,"#ede9fe",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("chronostar_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgCS106=levelCfgRef.current;
      if(cfgCS106){if(gs.score>=cfgCS106.scoreGoal&&(!cfgCS106.modifier||checkModGoal(cfgCS106.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="prism_coil"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo105pc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult105pc=gs.feverActive?2:1;
      const isSync105=((hit._pcSync105||0)>0.75&&(hit._pcSync105||0)<0.9);
      const pts105pc=Math.round((isSync105?270:90)*combo105pc*feverMult105pc);
      gs.score+=pts105pc;gs.streak++;gs.lastTapTime=Date.now();
      if(isSync105){
        spawnPopup(hit.x,hit.y-38,`🌈 COIL SYNC ×3! +${pts105pc}`,"#a855f7",28);
        spawnParticles(hit.x,hit.y,"#f3e8ff",16,"spark");
        sfx("epic");vibrate([6,3,12,3,6]);
        unlock("prismcoil_sync");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌀 +${pts105pc}`,"#c084fc",20);
        spawnParticles(hit.x,hit.y,"#ede9fe",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("prismcoil_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgPC105=levelCfgRef.current;
      if(cfgPC105){if(gs.score>=cfgPC105.scoreGoal&&(!cfgPC105.modifier||checkModGoal(cfgPC105.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="star_veil"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo105sv=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult105sv=gs.feverActive?2:1;
      const isReveal105=((hit._svReveal105||0)>0.55&&(hit._svReveal105||0)<0.72);
      const pts105sv=Math.round((isReveal105?200:90)*combo105sv*feverMult105sv);
      gs.score+=pts105sv;gs.streak++;gs.lastTapTime=Date.now();
      if(isReveal105){
        spawnPopup(hit.x,hit.y-36,`⭐ REVEAL ×2! +${pts105sv}`,"#fde68a",26);
        spawnParticles(hit.x,hit.y,"#fef9c3",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("starveil_reveal");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌫️ +${pts105sv}`,"#fbbf24",20);
        spawnParticles(hit.x,hit.y,"#fef3c7",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("starveil_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgSV105=levelCfgRef.current;
      if(cfgSV105){if(gs.score>=cfgSV105.scoreGoal&&(!cfgSV105.modifier||checkModGoal(cfgSV105.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="frost_halo"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo104fh=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult104fh=gs.feverActive?2:1;
      const isFreeze104=((hit._fhFreeze104||0)>0.78&&(hit._fhFreeze104||0)<0.93);
      const pts104fh=Math.round((isFreeze104?270:90)*combo104fh*feverMult104fh);
      gs.score+=pts104fh;gs.streak++;gs.lastTapTime=Date.now();
      if(isFreeze104){
        spawnPopup(hit.x,hit.y-38,`❄️ DEEP FREEZE ×3! +${pts104fh}`,"#93c5fd",28);
        spawnParticles(hit.x,hit.y,"#dbeafe",16,"spark");
        sfx("epic");vibrate([6,3,12,3,6]);
        unlock("frosthalo_freeze");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔱 +${pts104fh}`,"#bfdbfe",20);
        spawnParticles(hit.x,hit.y,"#eff6ff",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("frosthalo_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgFH104=levelCfgRef.current;
      if(cfgFH104){if(gs.score>=cfgFH104.scoreGoal&&(!cfgFH104.modifier||checkModGoal(cfgFH104.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="ember_clock"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo104ec=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult104ec=gs.feverActive?2:1;
      const lifePct104=(Date.now()-hit.spawnedAt)/hit.lifetime;
      const isClutch104=lifePct104>0.8;
      const pts104ec=Math.round((isClutch104?200:85)*combo104ec*feverMult104ec);
      gs.score+=pts104ec;gs.streak++;gs.lastTapTime=Date.now();
      if(isClutch104){
        spawnPopup(hit.x,hit.y-36,`🔥 CLUTCH BURN ×2! +${pts104ec}`,"#ef4444",26);
        spawnParticles(hit.x,hit.y,"#fee2e2",14,"spark");
        sfx("epic");vibrate([8,4,12,4,8]);
        unlock("emberclock_clutch");
      } else {
        spawnPopup(hit.x,hit.y-26,`🕐 +${pts104ec}`,"#f97316",20);
        spawnParticles(hit.x,hit.y,"#ffedd5",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("emberclock_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgEC104=levelCfgRef.current;
      if(cfgEC104){if(gs.score>=cfgEC104.scoreGoal&&(!cfgEC104.modifier||checkModGoal(cfgEC104.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="nova_spire"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo103ns=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult103ns=gs.feverActive?2:1;
      const isPeak103=((hit._nsPeak103||0)>0.82&&(hit._nsPeak103||0)<0.96);
      const pts103ns=Math.round((isPeak103?270:90)*combo103ns*feverMult103ns);
      gs.score+=pts103ns;gs.streak++;gs.lastTapTime=Date.now();
      if(isPeak103){
        spawnPopup(hit.x,hit.y-38,`💥 SPIRE PEAK ×3! +${pts103ns}`,"#f59e0b",28);
        spawnParticles(hit.x,hit.y,"#fef3c7",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#f59e0b",size:120,born:performance.now(),duration:700,alpha:0.5});
        sfx("epic");vibrate([6,3,12,3,6]);
        unlock("novaspire_peak");
      } else {
        spawnPopup(hit.x,hit.y-26,`🗼 +${pts103ns}`,"#fbbf24",20);
        spawnParticles(hit.x,hit.y,"#fef9c3",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("novaspire_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgNS103=levelCfgRef.current;
      if(cfgNS103){if(gs.score>=cfgNS103.scoreGoal&&(!cfgNS103.modifier||checkModGoal(cfgNS103.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="echo_lens"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo103el=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult103el=gs.feverActive?2:1;
      const isFocus103=((hit._elFocus103||0)>0.6&&(hit._elFocus103||0)<0.8);
      const pts103el=Math.round((isFocus103?200:90)*combo103el*feverMult103el);
      gs.score+=pts103el;gs.streak++;gs.lastTapTime=Date.now();
      if(isFocus103){
        spawnPopup(hit.x,hit.y-36,`📡 FOCUS ×2! +${pts103el}`,"#22d3ee",26);
        spawnParticles(hit.x,hit.y,"#cffafe",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("echolens_focus");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔮 +${pts103el}`,"#67e8f9",20);
        spawnParticles(hit.x,hit.y,"#ecfeff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("echolens_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgEL103=levelCfgRef.current;
      if(cfgEL103){if(gs.score>=cfgEL103.scoreGoal&&(!cfgEL103.modifier||checkModGoal(cfgEL103.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="crystal_petal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo102cp=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult102cp=gs.feverActive?2:1;
      const isAlign102=((hit._cpAlign102||0)>0.8&&(hit._cpAlign102||0)<0.95);
      const pts102cp=Math.round((isAlign102?270:90)*combo102cp*feverMult102cp);
      gs.score+=pts102cp;gs.streak++;gs.lastTapTime=Date.now();
      if(isAlign102){
        spawnPopup(hit.x,hit.y-38,`🌸 PETAL ALIGN ×3! +${pts102cp}`,"#f43f5e",28);
        spawnParticles(hit.x,hit.y,"#ffe4e6",16,"spark");
        sfx("epic");vibrate([6,3,12,3,6]);
        unlock("crystalpetal_align");
      } else {
        spawnPopup(hit.x,hit.y-26,`💎 +${pts102cp}`,"#fb7185",20);
        spawnParticles(hit.x,hit.y,"#fecdd3",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("crystalpetal_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgCP102=levelCfgRef.current;
      if(cfgCP102){if(gs.score>=cfgCP102.scoreGoal&&(!cfgCP102.modifier||checkModGoal(cfgCP102.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="twilight_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo102ts=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult102ts=gs.feverActive?2:1;
      const isDusk102=((hit._tsDusk102||0)>0.5&&(hit._tsDusk102||0)<0.75);
      const pts102ts=Math.round((isDusk102?200:90)*combo102ts*feverMult102ts);
      gs.score+=pts102ts;gs.streak++;gs.lastTapTime=Date.now();
      if(isDusk102){
        spawnPopup(hit.x,hit.y-36,`🌌 TWILIGHT ×2! +${pts102ts}`,"#818cf8",26);
        spawnParticles(hit.x,hit.y,"#e0e7ff",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("twilightshard_dusk");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔮 +${pts102ts}`,"#a5b4fc",20);
        spawnParticles(hit.x,hit.y,"#e0e7ff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("twilightshard_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgTS102=levelCfgRef.current;
      if(cfgTS102){if(gs.score>=cfgTS102.scoreGoal&&(!cfgTS102.modifier||checkModGoal(cfgTS102.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="lunar_rune"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo98lr=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult98lr=gs.feverActive?2:1;
      const isFull98=(hit._lrMoon98||0)>0.85;
      const pts98lr=Math.round((isFull98?270:90)*combo98lr*feverMult98lr);
      gs.score+=pts98lr;gs.streak++;gs.lastTapTime=Date.now();
      if(isFull98){
        spawnPopup(hit.x,hit.y-36,`🌕 FULL MOON ×3! +${pts98lr}`,"#fde68a",28);
        spawnParticles(hit.x,hit.y,"#fef9c3",16,"spark");
        sfx("legendary");vibrate([8,4,14,4,8]);
        unlock("lunarrune_full");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌙 +${pts98lr}`,"#fbbf24",20);
        spawnParticles(hit.x,hit.y,"#fef3c7",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("lunarrune_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgLR98=levelCfgRef.current;
      if(cfgLR98){if(gs.score>=cfgLR98.scoreGoal&&(!cfgLR98.modifier||checkModGoal(cfgLR98.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="storm_seed"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo98ss=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult98ss=gs.feverActive?2:1;
      const isGrown98=(hit._ssGrow98||0)>0.85;
      const pts98ss=Math.round((isGrown98?180:80)*combo98ss*feverMult98ss);
      gs.score+=pts98ss;gs.streak++;gs.lastTapTime=Date.now();
      if(isGrown98){
        spawnPopup(hit.x,hit.y-36,`🌩️ STORM GROWN ×2! +${pts98ss}`,"#60a5fa",26);
        spawnParticles(hit.x,hit.y,"#dbeafe",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("stormseed_grow");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌱 +${pts98ss}`,"#93c5fd",20);
        spawnParticles(hit.x,hit.y,"#eff6ff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("stormseed_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSS98=levelCfgRef.current;
      if(cfgSS98){if(gs.score>=cfgSS98.scoreGoal&&(!cfgSS98.modifier||checkModGoal(cfgSS98.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="warp_glyph"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo97wg=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult97wg=gs.feverActive?2:1;
      const isGlow97=(hit._wgGlow97||0)>0.7;
      const pts97wg=Math.round((isGlow97?270:90)*combo97wg*feverMult97wg);
      gs.score+=pts97wg;gs.streak++;gs.lastTapTime=Date.now();
      if(isGlow97){
        spawnPopup(hit.x,hit.y-36,`🔮 PORTAL ×3! +${pts97wg}`,"#e879f9",28);
        spawnParticles(hit.x,hit.y,"#fae8ff",16,"spark");
        sfx("legendary");vibrate([8,4,14,4,8]);
        unlock("warpglyph_portal");
      } else {
        spawnPopup(hit.x,hit.y-26,`✨ +${pts97wg}`,"#c084fc",20);
        spawnParticles(hit.x,hit.y,"#f5d0fe",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("warpglyph_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgWG97=levelCfgRef.current;
      if(cfgWG97){if(gs.score>=cfgWG97.scoreGoal&&(!cfgWG97.modifier||checkModGoal(cfgWG97.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="ether_bolt"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo97eb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult97eb=gs.feverActive?2:1;
      const ebChain97=(hit._ebChain97||0)+1;
      hit._ebChain97=ebChain97;
      const isChain97=ebChain97>=3;
      const pts97eb=Math.round((isChain97?180:80)*combo97eb*feverMult97eb);
      gs.score+=pts97eb;gs.streak++;gs.lastTapTime=Date.now();
      // Propagate chain count to next ether_bolt target
      const nextEB=targetsRef.current.find(t=>t.type==="ether_bolt"&&t.id!==hit.id&&!t.dying);
      if(nextEB)nextEB._ebChain97=ebChain97;
      if(isChain97){
        spawnPopup(hit.x,hit.y-32,`⚡ CHAIN ×2! +${pts97eb}`,"#38bdf8",26);
        spawnParticles(hit.x,hit.y,"#e0f2fe",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("etherbolt_chain");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌀 +${pts97eb} (${ebChain97})`,"#7dd3fc",20);
        spawnParticles(hit.x,hit.y,"#cffafe",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("etherbolt_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgEB97=levelCfgRef.current;
      if(cfgEB97){if(gs.score>=cfgEB97.scoreGoal&&(!cfgEB97.modifier||checkModGoal(cfgEB97.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="cosmic_dust"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo96cd=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult96cd=gs.feverActive?2:1;
      const isDense96=(hit._cdDensity96||0)>0.7;
      const pts96cd=Math.round((isDense96?160:80)*combo96cd*feverMult96cd);
      gs.score+=pts96cd;gs.streak++;gs.lastTapTime=Date.now();
      if(isDense96){
        spawnPopup(hit.x,hit.y-32,`✨ DENSE ×2! +${pts96cd}`,"#818cf8",26);
        spawnParticles(hit.x,hit.y,"#e0e7ff",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("cosmicdust_cloud");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌌 +${pts96cd}`,"#c4b5fd",20);
        spawnParticles(hit.x,hit.y,"#ede9fe",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("cosmicdust_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCD96=levelCfgRef.current;
      if(cfgCD96){if(gs.score>=cfgCD96.scoreGoal&&(!cfgCD96.modifier||checkModGoal(cfgCD96.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="nova_bell"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo96nb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult96nb=gs.feverActive?2:1;
      const isRing96=(hit._nbRes96||0)>0.75;
      const pts96nb=Math.round((isRing96?270:80)*combo96nb*feverMult96nb);
      gs.score+=pts96nb;gs.streak++;gs.lastTapTime=Date.now();
      if(isRing96){
        spawnPopup(hit.x,hit.y-36,`🔔 RING ×3! +${pts96nb}`,"#fbbf24",28);
        spawnParticles(hit.x,hit.y,"#fef9c3",16,"spark");
        sfx("legendary");vibrate([8,4,14,4,8]);
        unlock("novabell_ring");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔕 +${pts96nb}`,"#fde68a",20);
        spawnParticles(hit.x,hit.y,"#fef3c7",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("novabell_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgNB96=levelCfgRef.current;
      if(cfgNB96){if(gs.score>=cfgNB96.scoreGoal&&(!cfgNB96.modifier||checkModGoal(cfgNB96.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="prismatic_eye"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo95pe=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult95pe=gs.feverActive?2:1;
      const isGaze95=(hit._peg95||0)>0.7;
      const pts95pe=Math.round((isGaze95?270:90)*combo95pe*feverMult95pe);
      gs.score+=pts95pe;gs.streak++;gs.lastTapTime=Date.now();
      if(isGaze95){
        spawnPopup(hit.x,hit.y-36,`👁️ GAZE ×3! +${pts95pe}`,"#e879f9",28);
        spawnParticles(hit.x,hit.y,"#fae8ff",16,"spark");
        sfx("legendary");vibrate([8,4,14,4,8]);
        unlock("prismaticeye_gaze");
      } else {
        spawnPopup(hit.x,hit.y-26,`👁️ +${pts95pe}`,"#c084fc",20);
        spawnParticles(hit.x,hit.y,"#f5d0fe",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("prismaticeye_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgPE95=levelCfgRef.current;
      if(cfgPE95){if(gs.score>=cfgPE95.scoreGoal&&(!cfgPE95.modifier||checkModGoal(cfgPE95.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="thunder_crown"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo95tc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult95tc=gs.feverActive?2:1;
      const isFull95=(hit._tcCharge95||0)>0.85;
      const pts95tc=Math.round((isFull95?270:90)*combo95tc*feverMult95tc);
      gs.score+=pts95tc;gs.streak++;gs.lastTapTime=Date.now();
      if(isFull95){
        spawnPopup(hit.x,hit.y-36,`⚡ FULL CHARGE ×3! +${pts95tc}`,"#fef08a",28);
        spawnParticles(hit.x,hit.y,"#fef9c3",16,"spark");
        sfx("legendary");vibrate([10,5,16,5,10]);
        unlock("thundercrown_zap");
      } else {
        spawnPopup(hit.x,hit.y-26,`👑 +${pts95tc}`,"#fbbf24",20);
        spawnParticles(hit.x,hit.y,"#fef3c7",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("thundercrown_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgTC95=levelCfgRef.current;
      if(cfgTC95){if(gs.score>=cfgTC95.scoreGoal&&(!cfgTC95.modifier||checkModGoal(cfgTC95.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="solar_beam"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo94sb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult94sb=gs.feverActive?2:1;
      const isCenter94=hitDist<hit.radius*0.25;
      const pts94sb=Math.round((isCenter94?270:90)*combo94sb*feverMult94sb);
      gs.score+=pts94sb;gs.streak++;gs.lastTapTime=Date.now();
      if(isCenter94){
        spawnPopup(hit.x,hit.y-36,`☀️ BEAM CENTER ×3! +${pts94sb}`,"#fef08a",28);
        spawnParticles(hit.x,hit.y,"#fef9c3",16,"spark");
        sfx("legendary");vibrate([8,4,14,4,8]);
        unlock("solarbeam_center");
      } else {
        spawnPopup(hit.x,hit.y-26,`☀️ +${pts94sb}`,"#fbbf24",20);
        spawnParticles(hit.x,hit.y,"#fef3c7",8,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("solarbeam_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgSB94=levelCfgRef.current;
      if(cfgSB94){if(gs.score>=cfgSB94.scoreGoal&&(!cfgSB94.modifier||checkModGoal(cfgSB94.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="ice_prism"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo94ip=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult94ip=gs.feverActive?2:1;
      const isSplit94=(hit._ipSplit94||0)>0.5&&(hit._ipSplit94||0)<0.7;
      const pts94ip=Math.round((isSplit94?180:80)*combo94ip*feverMult94ip);
      gs.score+=pts94ip;gs.streak++;gs.lastTapTime=Date.now();
      if(isSplit94){
        spawnPopup(hit.x,hit.y-36,`🌈 SPLIT ×2! +${pts94ip}`,"#38bdf8",26);
        spawnParticles(hit.x,hit.y,"#e0f2fe",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("iceprism_split");
      } else {
        spawnPopup(hit.x,hit.y-26,`🧊 +${pts94ip}`,"#7dd3fc",20);
        spawnParticles(hit.x,hit.y,"#e0f2fe",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("iceprism_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgIP94=levelCfgRef.current;
      if(cfgIP94){if(gs.score>=cfgIP94.scoreGoal&&(!cfgIP94.modifier||checkModGoal(cfgIP94.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="star_pulse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo93sp=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult93sp=gs.feverActive?2:1;
      const isSync93=(hit._spPulse93||0)>0.88;
      const pts93sp=Math.round((isSync93?360:80)*combo93sp*feverMult93sp);
      gs.score+=pts93sp;gs.streak++;gs.lastTapTime=Date.now();
      if(isSync93){
        spawnPopup(hit.x,hit.y-40,`⭐ SYNC ×4! +${pts93sp}`,"#fef08a",30);
        spawnParticles(hit.x,hit.y,"#fef9c3",18,"spark");
        sfx("legendary");setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),700);vibrate([8,4,16,4,8]);
        unlock("starpulse_sync");
      } else {
        spawnPopup(hit.x,hit.y-26,`⭐ +${pts93sp}`,"#fde68a",20);
        spawnParticles(hit.x,hit.y,"#fef3c7",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("starpulse_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgSP93=levelCfgRef.current;
      if(cfgSP93){if(gs.score>=cfgSP93.scoreGoal&&(!cfgSP93.modifier||checkModGoal(cfgSP93.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="void_nova"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo93vn=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult93vn=gs.feverActive?2:1;
      const isBurst93=(hit._vnCollapse93||0)>0.75;
      const pts93vn=Math.round((isBurst93?270:80)*combo93vn*feverMult93vn);
      gs.score+=pts93vn;gs.streak++;gs.lastTapTime=Date.now();
      if(isBurst93){
        spawnPopup(hit.x,hit.y-36,`🌑 NOVA BURST ×3! +${pts93vn}`,"#a78bfa",28);
        spawnParticles(hit.x,hit.y,"#ede9fe",16,"spark");
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#7c3aed",size:120,born:performance.now(),duration:600,alpha:0.6});
        sfx("legendary");vibrate([8,4,14,4,8]);
        unlock("voidnova_burst");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌑 +${pts93vn}`,"#6d28d9",20);
        spawnParticles(hit.x,hit.y,"#ede9fe",7,"dot");
        sfx("rare");vibrate(5);
      }
      unlock("voidnova_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgVN93=levelCfgRef.current;
      if(cfgVN93){if(gs.score>=cfgVN93.scoreGoal&&(!cfgVN93.modifier||checkModGoal(cfgVN93.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="flame_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo92fo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult92fo=gs.feverActive?2:1;
      const isPeak92=(hit._foIntensity92||0)>0.8;
      const pts92fo=Math.round((isPeak92?270:80)*combo92fo*feverMult92fo);
      gs.score+=pts92fo;gs.streak++;gs.lastTapTime=Date.now();
      if(isPeak92){
        spawnPopup(hit.x,hit.y-36,`🔥 PEAK ×3! +${pts92fo}`,"#ef4444",28);
        spawnParticles(hit.x,hit.y,"#fca5a5",16,"spark");
        sfx("legendary");vibrate([8,4,14,4,8]);
        unlock("flameorb_super");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌡️ +${pts92fo}`,"#f97316",20);
        spawnParticles(hit.x,hit.y,"#ffedd5",7,"dot");
        sfx("rare");vibrate(6);
      }
      unlock("flameorb_tap");
      gs.sessionStats.tapsTotal++;gs.sessionStats.rareHits++;updateMissions(gs.sessionStats);
      const cfgFO92=levelCfgRef.current;
      if(cfgFO92){if(gs.score>=cfgFO92.scoreGoal&&(!cfgFO92.modifier||checkModGoal(cfgFO92.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="crystal_shell"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo92cs=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult92cs=gs.feverActive?2:1;
      const isCore92=(hit._cscrack92||0)>0.7;
      const pts92cs=Math.round((isCore92?180:80)*combo92cs*feverMult92cs);
      gs.score+=pts92cs;gs.streak++;gs.lastTapTime=Date.now();
      if(isCore92){
        spawnPopup(hit.x,hit.y-36,`💎 CORE ×2! +${pts92cs}`,"#818cf8",26);
        spawnParticles(hit.x,hit.y,"#e0e7ff",14,"spark");
        sfx("epic");vibrate([6,3,10,3,6]);
        unlock("crystalshell_core");
      } else {
        spawnPopup(hit.x,hit.y-26,`🐚 +${pts92cs}`,"#a78bfa",20);
        spawnParticles(hit.x,hit.y,"#ede9fe",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("crystalshell_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCS92=levelCfgRef.current;
      if(cfgCS92){if(gs.score>=cfgCS92.scoreGoal&&(!cfgCS92.modifier||checkModGoal(cfgCS92.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="spiral_bead"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo90sb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult90sb=gs.feverActive?2:1;
      const isCenter90=hitDist<hit.radius*0.22; // center spiral
      const pts90sb=Math.round((isCenter90?270:80)*combo90sb*feverMult90sb);
      gs.score+=pts90sb;gs.streak++;gs.lastTapTime=Date.now();
      if(isCenter90){
        spawnPopup(hit.x,hit.y-36,`🌀 CENTER ×3 +${pts90sb}`,"#e879f9",26);
        spawnParticles(hit.x,hit.y,"#f5d0fe",14,"spark");
        sfx("legendary");vibrate([5,3,12,3,5]);
        unlock("spiralbead_center");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌀 +${pts90sb}`,"#a855f7",20);
        spawnParticles(hit.x,hit.y,"#fae8ff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("spiralbead_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSB90=levelCfgRef.current;
      if(cfgSB90){if(gs.score>=cfgSB90.scoreGoal&&(!cfgSB90.modifier||checkModGoal(cfgSB90.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="quantum_flux"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo90qf=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult90qf=gs.feverActive?2:1;
      const isStable90=(hit._qfFlux90||0)<0.35;
      const pts90qf=Math.round((isStable90?180:80)*combo90qf*feverMult90qf);
      gs.score+=pts90qf;gs.streak++;gs.lastTapTime=Date.now();
      if(isStable90){
        spawnPopup(hit.x,hit.y-36,`⚛️ STABLE ×2 +${pts90qf}`,"#6ee7b7",26);
        spawnParticles(hit.x,hit.y,"#d1fae5",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("quantumflux_stable");
      } else {
        spawnPopup(hit.x,hit.y-26,`💥 +${pts90qf}`,"#ef4444",20);
        spawnParticles(hit.x,hit.y,"#fecaca",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("quantumflux_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgQF90=levelCfgRef.current;
      if(cfgQF90){if(gs.score>=cfgQF90.scoreGoal&&(!cfgQF90.modifier||checkModGoal(cfgQF90.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="nebula_drop"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo89nd=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult89nd=gs.feverActive?2:1;
      const pts89nd=Math.round(70*combo89nd*feverMult89nd);
      gs.score+=pts89nd;gs.streak++;gs.lastTapTime=Date.now();
      // Track nebula drop collection
      gs._nebulaCount89=(gs._nebulaCount89||0)+1;
      if(gs._nebulaCount89>=5){
        const collectBonus89=Math.round(200*feverMult89nd);
        gs.score+=collectBonus89;
        spawnPopup(hit.x,hit.y-38,`🌌 COLLECT ×5 +${collectBonus89}`,"#818cf8",22);
        spawnParticles(hit.x,hit.y,"#c7d2fe",12,"spark");
        sfx("chainBonus");vibrate([5,3,8,3,5]);
        unlock("nebuladrop_collect");
        gs._nebulaCount89=0;
      } else {
        spawnPopup(hit.x,hit.y-26,`🌌 +${pts89nd} (${gs._nebulaCount89}/5)`,"#818cf8",18);
        spawnParticles(hit.x,hit.y,"#e0e7ff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("nebuladrop_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgND89=levelCfgRef.current;
      if(cfgND89){if(gs.score>=cfgND89.scoreGoal&&(!cfgND89.modifier||checkModGoal(cfgND89.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="pulse_gate"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo89pg=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult89pg=gs.feverActive?2:1;
      const isOpen89=(hit._pgOpen89||0)>0.6;
      const pts89pg=Math.round((isOpen89?180:80)*combo89pg*feverMult89pg);
      gs.score+=pts89pg;gs.streak++;gs.lastTapTime=Date.now();
      if(isOpen89){
        spawnPopup(hit.x,hit.y-36,`🚪 OPEN! ×2 +${pts89pg}`,"#67e8f9",26);
        spawnParticles(hit.x,hit.y,"#cffafe",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("pulsegate_open");
      } else {
        spawnPopup(hit.x,hit.y-26,`🚪 +${pts89pg}`,"#22d3ee",20);
        spawnParticles(hit.x,hit.y,"#a5f3fc",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("pulsegate_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPG89=levelCfgRef.current;
      if(cfgPG89){if(gs.score>=cfgPG89.scoreGoal&&(!cfgPG89.modifier||checkModGoal(cfgPG89.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="frost_gem"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo88fg=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult88fg=gs.feverActive?2:1;
      const pts88fg=Math.round(85*combo88fg*feverMult88fg);
      gs.score+=pts88fg;gs.streak++;gs.lastTapTime=Date.now();
      // Freeze all bombs for 2s
      targetsRef.current.filter(o=>o.type==="bomb"&&!o.dying).forEach(b=>{
        b._frozenUntil88=(b._frozenUntil88||0);
        b._frozenUntil88=Date.now()+2000;
        b.lifetime=(b.lifetime||2000)+2000;
        spawnPopup(b.x,b.y-16,"❄️ FROZEN","#93c5fd",12);
      });
      spawnPopup(hit.x,hit.y-30,`💎 +${pts88fg} ❄️ FREEZE`,"#3b82f6",22);
      spawnParticles(hit.x,hit.y,"#bfdbfe",12,"spark");
      sfx("tap");vibrate([5,3,8]);
      unlock("frostgem_tap");unlock("frostgem_freeze");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgFG88=levelCfgRef.current;
      if(cfgFG88){if(gs.score>=cfgFG88.scoreGoal&&(!cfgFG88.modifier||checkModGoal(cfgFG88.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="heat_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo88ho=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult88ho=gs.feverActive?3:1; // 3× during fever
      const pts88ho=Math.round((gs.feverActive?270:90)*combo88ho);
      gs.score+=pts88ho;gs.streak++;gs.lastTapTime=Date.now();
      if(gs.feverActive){
        spawnPopup(hit.x,hit.y-36,`🌟 IGNITE! ×3 +${pts88ho}`,"#fde047",26);
        spawnParticles(hit.x,hit.y,"#fef3c7",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("heatorb_hot");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔴 +${pts88ho}`,"#f97316",20);
        spawnParticles(hit.x,hit.y,"#fed7aa",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("heatorb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgHO88=levelCfgRef.current;
      if(cfgHO88){if(gs.score>=cfgHO88.scoreGoal&&(!cfgHO88.modifier||checkModGoal(cfgHO88.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="thunder_bead"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo87tb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult87tb=gs.feverActive?2:1;
      const pts87tb=Math.round(75*combo87tb*feverMult87tb);
      gs.score+=pts87tb;gs.streak++;gs.lastTapTime=Date.now();
      // Track thunder bead chain
      const now87tb=Date.now();
      gs._tbChain87=(gs._tbChain87||[]).filter(t87=>now87tb-t87<3000);
      gs._tbChain87.push(now87tb);
      const chain87=gs._tbChain87.length;
      if(chain87>=3){
        const chainBonus87=Math.round(200*feverMult87tb);
        gs.score+=chainBonus87;
        spawnPopup(hit.x,hit.y-38,`⚡ CHAIN ×${chain87} +${chainBonus87}`,"#fde047",22);
        spawnParticles(hit.x,hit.y,"#fef9c3",12,"spark");
        sfx("chainBonus");vibrate([5,3,8,3,5]);
        unlock("thunderbead_chain");
        gs._tbChain87=[];
      } else {
        spawnPopup(hit.x,hit.y-26,`⚡ +${pts87tb}`,"#eab308",20);
        spawnParticles(hit.x,hit.y,"#fef08a",7,"spark");
        sfx("tap");vibrate(5);
      }
      unlock("thunderbead_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgTB87=levelCfgRef.current;
      if(cfgTB87){if(gs.score>=cfgTB87.scoreGoal&&(!cfgTB87.modifier||checkModGoal(cfgTB87.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="mirror_star"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo87ms=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult87ms=gs.feverActive?2:1;
      const hasReflect87=(gs.streak||0)>=5;
      const pts87ms=Math.round((hasReflect87?200:80)*combo87ms*feverMult87ms);
      gs.score+=pts87ms;gs.streak++;gs.lastTapTime=Date.now();
      if(hasReflect87){
        spawnPopup(hit.x,hit.y-36,`🪞 REFLECT! +${pts87ms}`,"#e2e8f0",26);
        spawnParticles(hit.x,hit.y,"#f1f5f9",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("mirrorstar_match");
      } else {
        spawnPopup(hit.x,hit.y-26,`⭐ +${pts87ms}`,"#94a3b8",20);
        spawnParticles(hit.x,hit.y,"#e2e8f0",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("mirrorstar_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgMS87=levelCfgRef.current;
      if(cfgMS87){if(gs.score>=cfgMS87.scoreGoal&&(!cfgMS87.modifier||checkModGoal(cfgMS87.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="aurora_wave"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo86aw=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult86aw=gs.feverActive?2:1;
      const isCrest86=(hit._awCrest86||0)>0.75;
      const pts86aw=Math.round((isCrest86?180:80)*combo86aw*feverMult86aw);
      gs.score+=pts86aw;gs.streak++;gs.lastTapTime=Date.now();
      if(isCrest86){
        spawnPopup(hit.x,hit.y-36,`🌊 CREST! ×2 +${pts86aw}`,"#86efac",26);
        spawnParticles(hit.x,hit.y,"#dcfce7",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("aurorawave_crest");
      } else {
        spawnPopup(hit.x,hit.y-26,`〰️ +${pts86aw}`,"#22c55e",20);
        spawnParticles(hit.x,hit.y,"#bbf7d0",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("aurorawave_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAW86=levelCfgRef.current;
      if(cfgAW86){if(gs.score>=cfgAW86.scoreGoal&&(!cfgAW86.modifier||checkModGoal(cfgAW86.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="crystal_prism"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo86cp=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult86cp=gs.feverActive?2:1;
      const basePts86cp=Math.round(90*combo86cp*feverMult86cp);
      // Spawn 3 score fragment mini-targets
      const canvas86cp=canvasRef.current;const cw86=canvas86cp?.width||360;const ch86=canvas86cp?.height||640;
      const fragBonus86=Math.round(50*combo86cp*feverMult86cp);
      gs.score+=basePts86cp;gs.streak++;gs.lastTapTime=Date.now();
      // Spawn 3 tiny score popups in random directions = fragment effect
      for(let fi=0;fi<3;fi++){
        const angle86=fi*(Math.PI*2/3)+Math.random()*0.5;
        const dist86=30+Math.random()*25;
        const fx86=Math.max(20,Math.min(cw86-20,hit.x+Math.cos(angle86)*dist86));
        const fy86=Math.max(20,Math.min(ch86-60,hit.y+Math.sin(angle86)*dist86));
        setTimeout(()=>{
          const gsF86=gsRef.current;if(gsF86&&gsF86.lives>0){
            gsF86.score+=fragBonus86;
            spawnPopup(fx86,fy86,`💫+${fragBonus86}`,"#67e8f9",14);
            spawnParticles(fx86,fy86,"#a5f3fc",4,"spark");
          }
        },200+fi*150);
      }
      spawnPopup(hit.x,hit.y-36,`🔷 +${basePts86cp}`,"#06b6d4",24);
      spawnParticles(hit.x,hit.y,"#cffafe",12,"spark");
      sfx("chainBonus");vibrate([4,3,8,3,4]);
      unlock("crystalprism_tap");unlock("crystalprism_split");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCP86=levelCfgRef.current;
      if(cfgCP86){if(gs.score>=cfgCP86.scoreGoal&&(!cfgCP86.modifier||checkModGoal(cfgCP86.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="plasma_ring"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo85pr=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult85pr=gs.feverActive?2:1;
      // Inside = hitDist < r*0.45 (inner glow zone)
      const isInside85=hitDist<hit.radius*0.45;
      const pts85pr=Math.round((isInside85?200:90)*combo85pr*feverMult85pr);
      gs.score+=pts85pr;gs.streak++;gs.lastTapTime=Date.now();
      if(isInside85){
        spawnPopup(hit.x,hit.y-36,`💿 INSIDE! ×2 +${pts85pr}`,"#c084fc",26);
        spawnParticles(hit.x,hit.y,"#f5d0fe",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("plasmaring_inside");
      } else {
        spawnPopup(hit.x,hit.y-26,`💿 +${pts85pr}`,"#a855f7",20);
        spawnParticles(hit.x,hit.y,"#e9d5ff",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("plasmaring_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPR85=levelCfgRef.current;
      if(cfgPR85){if(gs.score>=cfgPR85.scoreGoal&&(!cfgPR85.modifier||checkModGoal(cfgPR85.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="void_star"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo85vs=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult85vs=gs.feverActive?2:1;
      // Core = hitDist < r*0.2
      const isCore85=hitDist<hit.radius*0.20;
      const pts85vs=Math.round((isCore85?270:90)*combo85vs*feverMult85vs);
      gs.score+=pts85vs;gs.streak++;gs.lastTapTime=Date.now();
      if(isCore85){
        spawnPopup(hit.x,hit.y-36,`⭐ CORE! ×3 +${pts85vs}`,"#c4b5fd",26);
        spawnParticles(hit.x,hit.y,"#ede9fe",14,"spark");
        sfx("legendary");vibrate([5,3,12,3,5]);
        unlock("voidstar_core");
      } else {
        spawnPopup(hit.x,hit.y-26,`⭐ +${pts85vs}`,"#7c3aed",20);
        spawnParticles(hit.x,hit.y,"#ddd6fe",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("voidstar_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVS85=levelCfgRef.current;
      if(cfgVS85){if(gs.score>=cfgVS85.scoreGoal&&(!cfgVS85.modifier||checkModGoal(cfgVS85.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="ice_dagger"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo84id=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult84id=gs.feverActive?2:1;
      const isTip84=hitDist<hit.radius*0.22; // tip zone
      const pts84id=Math.round((isTip84?240:80)*combo84id*feverMult84id);
      gs.score+=pts84id;gs.streak++;gs.lastTapTime=Date.now();
      if(isTip84){
        spawnPopup(hit.x,hit.y-36,`🗡️ TIP! ×3 +${pts84id}`,"#93c5fd",26);
        spawnParticles(hit.x,hit.y,"#e0f2fe",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("icedagger_tip");
      } else {
        spawnPopup(hit.x,hit.y-26,`🗡️ +${pts84id}`,"#3b82f6",20);
        spawnParticles(hit.x,hit.y,"#bfdbfe",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("icedagger_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgID84=levelCfgRef.current;
      if(cfgID84){if(gs.score>=cfgID84.scoreGoal&&(!cfgID84.modifier||checkModGoal(cfgID84.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="shadow_flare"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo84sf=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult84sf=gs.feverActive?2:1;
      const isBright84=(hit._sfBright84||0)>0.75;
      const pts84sf=Math.round((isBright84?160:80)*combo84sf*feverMult84sf);
      gs.score+=pts84sf;gs.streak++;gs.lastTapTime=Date.now();
      if(isBright84){
        spawnPopup(hit.x,hit.y-36,`☀️ BRIGHT! ×2 +${pts84sf}`,"#fde68a",26);
        spawnParticles(hit.x,hit.y,"#fef3c7",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("shadowflare_bright");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌑 +${pts84sf}`,"#f59e0b",20);
        spawnParticles(hit.x,hit.y,"#fef9c3",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("shadowflare_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSF84=levelCfgRef.current;
      if(cfgSF84){if(gs.score>=cfgSF84.scoreGoal&&(!cfgSF84.modifier||checkModGoal(cfgSF84.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="nova_drift"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo83nd=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult83nd=gs.feverActive?2:1;
      const isCenter83=hitDist<(hit.radius*(0.25*(1-(hit._ndAge83||0)*0.6))*1.5+hit.radius*0.2);
      const pts83nd=Math.round((isCenter83?180:80)*combo83nd*feverMult83nd);
      gs.score+=pts83nd;gs.streak++;gs.lastTapTime=Date.now();
      if(isCenter83){
        spawnPopup(hit.x,hit.y-34,`💥 CENTER ×2 +${pts83nd}`,"#fb7185",26);
        spawnParticles(hit.x,hit.y,"#ffe4e6",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("novadrift_center");
      } else {
        spawnPopup(hit.x,hit.y-26,`💥 +${pts83nd}`,"#f43f5e",20);
        spawnParticles(hit.x,hit.y,"#fecdd3",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("novadrift_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgND83=levelCfgRef.current;
      if(cfgND83){if(gs.score>=cfgND83.scoreGoal&&(!cfgND83.modifier||checkModGoal(cfgND83.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="energy_pulse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo83ep=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult83ep=gs.feverActive?2:1;
      const charge83=(hit._epCharge83||0);
      const isMax83=charge83>=0.9;
      const pts83ep=Math.round((isMax83?300:Math.round(50+charge83*200))*combo83ep*feverMult83ep);
      gs.score+=pts83ep;gs.streak++;gs.lastTapTime=Date.now();
      if(isMax83){
        spawnPopup(hit.x,hit.y-34,`⚡ MAX CHARGE +${pts83ep}`,"#7dd3fc",26);
        spawnParticles(hit.x,hit.y,"#e0f2fe",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("energypulse_max");
      } else {
        spawnPopup(hit.x,hit.y-26,`⚡ +${pts83ep}`,"#0ea5e9",20);
        spawnParticles(hit.x,hit.y,"#bae6fd",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("energypulse_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgEP83=levelCfgRef.current;
      if(cfgEP83){if(gs.score>=cfgEP83.scoreGoal&&(!cfgEP83.modifier||checkModGoal(cfgEP83.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="solar_petal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo82sp=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult82sp=gs.feverActive?2:1;
      const pts82sp=Math.round(65*combo82sp*feverMult82sp);
      gs.score+=pts82sp;gs.streak++;gs.lastTapTime=Date.now();
      const now82sp=Date.now();
      gs._solarPetalChain82=(gs._solarPetalChain82||[]).filter(t82=>now82sp-t82<5000);
      gs._solarPetalChain82.push(now82sp);
      const chain82=gs._solarPetalChain82.length;
      if(chain82>=3){
        gs.score+=150;
        spawnPopup(hit.x,hit.y-38,`☀️ CHAIN ×${chain82} +150`,"#fbbf24",22);
        spawnParticles(hit.x,hit.y,"#fef3c7",12,"spark");
        sfx("chainBonus");vibrate([4,3,8,3,4]);
        unlock("solarpetal_chain");
      } else {
        spawnPopup(hit.x,hit.y-26,`☀️ +${pts82sp}`,"#fbbf24",20);
        spawnParticles(hit.x,hit.y,"#fef9c3",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("solarpetal_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSPT82=levelCfgRef.current;
      if(cfgSPT82){if(gs.score>=cfgSPT82.scoreGoal&&(!cfgSPT82.modifier||checkModGoal(cfgSPT82.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="cosmic_shard"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo82cs=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult82cs=gs.feverActive?2:1;
      const pts82cs=Math.round(80*combo82cs*feverMult82cs);
      gs.score+=pts82cs;gs.streak++;gs.lastTapTime=Date.now();
      // Track which color tiers were tapped
      const now82cs=Date.now();
      if(!gs._cosmicShardSet82||now82cs-(gs._cosmicShardSetAt82||0)>8000){gs._cosmicShardSet82=new Set();gs._cosmicShardSetAt82=now82cs;}
      gs._cosmicShardSet82.add(hit._csColor82||0);
      if(gs._cosmicShardSet82.size>=3){
        gs.score+=300;
        spawnPopup(hit.x,hit.y-40,`💠 ALIGNED! +300`,"#f0abfc",26);
        spawnParticles(hit.x,hit.y,"#e879f9",16,"spark");
        sfx("legendary");vibrate([6,4,12,4,6]);
        gs._cosmicShardSet82=new Set(); // reset
        unlock("cosmicshard_align");
      } else {
        const COLS82=["🔴","🟣","🔵"];
        spawnPopup(hit.x,hit.y-26,`💠 +${pts82cs} (${gs._cosmicShardSet82.size}/3)`,["#f43f5e","#8b5cf6","#0ea5e9"][hit._csColor82||0],20);
        spawnParticles(hit.x,hit.y,"#ede9fe",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("cosmicshard_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCS82=levelCfgRef.current;
      if(cfgCS82){if(gs.score>=cfgCS82.scoreGoal&&(!cfgCS82.modifier||checkModGoal(cfgCS82.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="crystal_wing_l"||hit.type==="crystal_wing_r"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo81cw=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult81cw=gs.feverActive?2:1;
      // Check if partner was tapped recently (within 2.5s)
      const partner81=targetsRef.current.find(o=>o.id===hit._cwPartner81&&!o.dying);
      const partnerTime81=partner81?._cwTapTime81||null;
      const pairBonus81=partnerTime81&&(Date.now()-partnerTime81<2500);
      const pts81cw=Math.round((pairBonus81?250:80)*combo81cw*feverMult81cw);
      gs.score+=pts81cw;gs.streak++;gs.lastTapTime=Date.now();
      // Record tap time on self; if partner is alive, mark its _cwTapTime81
      if(partner81)partner81._cwTapTime81=Date.now();
      if(pairBonus81){
        spawnPopup(hit.x,hit.y-36,`💎 PAIR! +${pts81cw}`,"#818cf8",26);
        spawnParticles(hit.x,hit.y,"#c7d2fe",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("crystalwing_pair");
      } else {
        spawnPopup(hit.x,hit.y-24,`💎 +${pts81cw}`,"#38bdf8",18);
        spawnParticles(hit.x,hit.y,"#e0f2fe",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("crystalwing_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgCW81=levelCfgRef.current;
      if(cfgCW81){if(gs.score>=cfgCW81.scoreGoal&&(!cfgCW81.modifier||checkModGoal(cfgCW81.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="storm_cloud"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo81sc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult81sc=gs.feverActive?2:1;
      const isBolt81=!!(hit._scBolt81);
      const pts81sc=Math.round((isBolt81?160:70)*combo81sc*feverMult81sc);
      gs.score+=pts81sc;gs.streak++;gs.lastTapTime=Date.now();
      if(isBolt81){
        spawnPopup(hit.x,hit.y-36,`⚡ BOLT ×2 +${pts81sc}`,"#fbbf24",26);
        spawnParticles(hit.x,hit.y,"#fde68a",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("stormcloud_bolt");
      } else {
        spawnPopup(hit.x,hit.y-26,`⛈️ +${pts81sc}`,"#94a3b8",20);
        spawnParticles(hit.x,hit.y,"#e2e8f0",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("stormcloud_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSC81=levelCfgRef.current;
      if(cfgSC81){if(gs.score>=cfgSC81.scoreGoal&&(!cfgSC81.modifier||checkModGoal(cfgSC81.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="arc_node"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo80an=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult80an=gs.feverActive?2:1;
      const pts80an=Math.round(80*combo80an*feverMult80an);
      gs.score+=pts80an;gs.streak++;gs.lastTapTime=Date.now();
      // Arc to 2 nearest normal targets
      const arcTargets80=targetsRef.current.filter(o=>!o.dying&&o.type==="normal"&&Math.hypot(o.x-hit.x,o.y-hit.y)<200).slice(0,2);
      arcTargets80.forEach(o=>{
        const arcPts80=Math.round(25*combo80an*feverMult80an);
        gs.score+=arcPts80;o.dying=performance.now();
        spawnParticles(o.x,o.y,"#93c5fd",6,"spark");
        spawnPopup(o.x,o.y-14,`⚡+${arcPts80}`,"#60a5fa",12);
      });
      spawnPopup(hit.x,hit.y-28,`⚡ ARC +${pts80an}`,"#3b82f6",20);
      spawnParticles(hit.x,hit.y,"#bfdbfe",10,"spark");
      sfx("chainBonus");vibrate([5,3,8,3,5]);
      unlock("arcnode_tap");
      if(arcTargets80.length>=2)unlock("arcnode_link");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgAN80=levelCfgRef.current;
      if(cfgAN80){if(gs.score>=cfgAN80.scoreGoal&&(!cfgAN80.modifier||checkModGoal(cfgAN80.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="prismatic_burst"){
      const pbPhase80=hit._pbPhase80||0;
      const pbHits80=hit._pbHitPhases80||(hit._pbHitPhases80=new Set());
      pbHits80.add(pbPhase80);
      const combo80pb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult80pb=gs.feverActive?2:1;
      const allFive80=pbHits80.size>=5;
      if(allFive80){
        // Jackpot! Remove target
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const pts80pb=Math.round(600*combo80pb*feverMult80pb);
        gs.score+=pts80pb;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-40,`🌈 FULL SPECTRUM +${pts80pb}`,"#fde68a",28);
        spawnParticles(hit.x,hit.y,"#fde68a",18,"spark");
        sfx("legendary");vibrate([8,4,16,4,8]);
        setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),1000);
        unlock("prismaticburst_all");
      } else {
        // Partial hit — score partial pts, target stays for more phases
        const pts80pb=Math.round(80*combo80pb*feverMult80pb);
        gs.score+=pts80pb;gs.streak++;gs.lastTapTime=Date.now();
        const PHASE_COLS=["#ef4444","#f97316","#eab308","#22c55e","#8b5cf6"];
        spawnPopup(hit.x,hit.y-28,`🌈 +${pts80pb} (${pbHits80.size}/5)`,PHASE_COLS[pbPhase80],20);
        spawnParticles(hit.x,hit.y,PHASE_COLS[pbPhase80],8,"spark");
        sfx("tap");vibrate(5);
      }
      unlock("prismaticburst_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPB80=levelCfgRef.current;
      if(cfgPB80){if(gs.score>=cfgPB80.scoreGoal&&(!cfgPB80.modifier||checkModGoal(cfgPB80.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="moon_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo79mo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult79mo=gs.feverActive?2:1;
      const isFull79=(hit._moonPct79||0)>0.85;
      const pts79mo=Math.round(75*(isFull79?2:1)*combo79mo*feverMult79mo);
      gs.score+=pts79mo;gs.streak++;gs.lastTapTime=Date.now();
      if(isFull79){
        spawnPopup(hit.x,hit.y-34,`🌕 FULL MOON ×2 +${pts79mo}`,"#fde68a",26);
        spawnParticles(hit.x,hit.y,"#fef3c7",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("moon_orb_phase");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌙 +${pts79mo}`,"#fde68a",20);
        spawnParticles(hit.x,hit.y,"#fef9c3",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("moon_orb_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgMO79=levelCfgRef.current;
      if(cfgMO79){if(gs.score>=cfgMO79.scoreGoal&&(!cfgMO79.modifier||checkModGoal(cfgMO79.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="lava_core"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo79lc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult79lc=gs.feverActive?2:1;
      const pts79lc=Math.round(70*combo79lc*feverMult79lc);
      gs.score+=pts79lc;gs.streak++;gs.lastTapTime=Date.now();
      // Chain tracking
      const now79lc=Date.now();
      gs._lavaCoreChain=(gs._lavaCoreChain||[]).filter(t79=>now79lc-t79<4000);
      gs._lavaCoreChain.push(now79lc);
      const chain79=gs._lavaCoreChain.length;
      if(chain79>=3){
        gs.score+=200;
        spawnPopup(hit.x,hit.y-38,`🌋 CHAIN ×${chain79} +200`,"#f97316",22);
        spawnParticles(hit.x,hit.y,"#fed7aa",14,"spark");
        sfx("chainBonus");vibrate([5,3,10,3,5]);
        unlock("lava_core_chain");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌋 +${pts79lc}`,"#f97316",20);
        spawnParticles(hit.x,hit.y,"#ffedd5",7,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("lava_core_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLC79=levelCfgRef.current;
      if(cfgLC79){if(gs.score>=cfgLC79.scoreGoal&&(!cfgLC79.modifier||checkModGoal(cfgLC79.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="sun_petal"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo78sp=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult78sp=gs.feverActive?2:1;
      const bloom78=(hit._bloomPct78||0);
      const isBloom78=bloom78>=0.85;
      const mult78sp=isBloom78?3:bloom78>0.5?2:1;
      const pts78sp=Math.round(70*mult78sp*combo78sp*feverMult78sp);
      gs.score+=pts78sp;gs.streak++;gs.lastTapTime=Date.now();
      if(isBloom78){
        spawnPopup(hit.x,hit.y-34,`🌸 BLOOM ×3 +${pts78sp}`,"#fde68a",26);
        spawnParticles(hit.x,hit.y,"#fef3c7",16,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("sunpetal_bloom");
      } else {
        spawnPopup(hit.x,hit.y-26,`🌸 +${pts78sp}`,"#fbbf24",20);
        spawnParticles(hit.x,hit.y,"#fef9c3",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("sunpetal_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSP78=levelCfgRef.current;
      if(cfgSP78){if(gs.score>=cfgSP78.scoreGoal&&(!cfgSP78.modifier||checkModGoal(cfgSP78.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="ghost_bell"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo78gb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult78gb=gs.feverActive?2:1;
      const isSilent78=!!(hit._gbSilent78);
      const mult78gb=isSilent78?3:1;
      const pts78gb=Math.round(85*mult78gb*combo78gb*feverMult78gb);
      gs.score+=pts78gb;gs.streak++;gs.lastTapTime=Date.now();
      if(isSilent78){
        spawnPopup(hit.x,hit.y-36,`🔔 SILENT ×3 +${pts78gb}`,"#fde68a",28);
        spawnParticles(hit.x,hit.y,"#fef3c7",16,"spark");
        sfx("legendary");vibrate([6,4,14,4,6]);
        setEpicFlash(true);setTimeout(()=>setEpicFlash(false),800);
        unlock("ghostbell_silent");
      } else {
        spawnPopup(hit.x,hit.y-26,`🔔 +${pts78gb}`,"#fde68a",20);
        spawnParticles(hit.x,hit.y,"#fef9c3",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("ghostbell_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgGB78=levelCfgRef.current;
      if(cfgGB78){if(gs.score>=cfgGB78.scoreGoal&&(!cfgGB78.modifier||checkModGoal(cfgGB78.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="plasma_disk"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo77pd=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult77pd=gs.feverActive?2:1;
      const isEdge77=hitDist>(hit.radius*0.7); // hot edge ring zone
      const mult77pd=isEdge77?2:1;
      const pts77pd=Math.round(75*mult77pd*combo77pd*feverMult77pd);
      gs.score+=pts77pd;gs.streak++;gs.lastTapTime=Date.now();
      if(isEdge77){
        spawnPopup(hit.x,hit.y-34,`💿 EDGE ×2 +${pts77pd}`,"#fde68a",26);
        spawnParticles(hit.x,hit.y,"#fde68a",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("plasmadisk_edge");
      } else {
        spawnPopup(hit.x,hit.y-26,`💿 +${pts77pd}`,"#f97316",20);
        spawnParticles(hit.x,hit.y,"#fed7aa",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("plasmadisk_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgPD77=levelCfgRef.current;
      if(cfgPD77){if(gs.score>=cfgPD77.scoreGoal&&(!cfgPD77.modifier||checkModGoal(cfgPD77.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="void_ring"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo77vr=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult77vr=gs.feverActive?2:1;
      const isInner77=hitDist<(hit.radius*0.3); // center void gap
      const mult77vr=isInner77?2:1;
      const pts77vr=Math.round(80*mult77vr*combo77vr*feverMult77vr);
      gs.score+=pts77vr;gs.streak++;gs.lastTapTime=Date.now();
      if(isInner77){
        spawnPopup(hit.x,hit.y-34,`⭕ INNER ×2 +${pts77vr}`,"#c084fc",26);
        spawnParticles(hit.x,hit.y,"#a78bfa",14,"spark");
        sfx("epic");vibrate([5,3,10,3,5]);
        unlock("voidring_inner");
      } else {
        spawnPopup(hit.x,hit.y-26,`⭕ +${pts77vr}`,"#7c3aed",20);
        spawnParticles(hit.x,hit.y,"#ddd6fe",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("voidring_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgVR77=levelCfgRef.current;
      if(cfgVR77){if(gs.score>=cfgVR77.scoreGoal&&(!cfgVR77.modifier||checkModGoal(cfgVR77.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="ether_orb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo76eo=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult76eo=gs.feverActive?2:1;
      const pts76eo=Math.round(60*combo76eo*feverMult76eo);
      gs.score+=pts76eo;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-28,`🌫️ SPLIT +${pts76eo}`,"#2dd4bf",20);
      spawnParticles(hit.x,hit.y,"#99f6e4",10,"spark");
      sfx("tap");vibrate(6);
      unlock("etherOrb_tap");
      // Spawn 2 ether_copy children at offset positions
      const w76=canvasRef.current?.width||400,h76=canvasRef.current?.height||700;
      const BASE_R76=Math.round(Math.min(w76,h76)*0.062);
      const offsets76=[{dx:-60,dy:-30},{dx:60,dy:-30}];
      offsets76.forEach((off,idx76)=>{
        const cx=Math.max(BASE_R76+10,Math.min(w76-BASE_R76-10,hit.x+off.dx));
        const cy=Math.max(BASE_R76+100,Math.min(h76-BASE_R76-10,hit.y+off.dy));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"ether_copy",x:cx,y:cy,
          radius:Math.round(BASE_R76*0.6),color:"#2dd4bf",glow:"#ccfbf1",rarity:RARITY[0],
          lifetime:3000,spawnedAt:Date.now(),born:performance.now(),
          moving:false,ghost:false,vx:0,vy:0,trail:[],hitsLeft:1,maxHits:1,dying:null,
          anticipateMs:0,_ethHue76:idx76===0?175:145,_ethParent76:hit.id});
      });
      unlock("etherOrb_copies");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgEO76=levelCfgRef.current;
      if(cfgEO76){if(gs.score>=cfgEO76.scoreGoal&&(!cfgEO76.modifier||checkModGoal(cfgEO76.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="ether_copy"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo76ec=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult76ec=gs.feverActive?2:1;
      const pts76ec=Math.round(40*combo76ec*feverMult76ec);
      gs.score+=pts76ec;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-20,`✨ +${pts76ec}`,"#6ee7b7",16);
      spawnParticles(hit.x,hit.y,"#99f6e4",6,"dot");
      sfx("tap");vibrate(4);
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgEC76=levelCfgRef.current;
      if(cfgEC76){if(gs.score>=cfgEC76.scoreGoal&&(!cfgEC76.modifier||checkModGoal(cfgEC76.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="quantum_blast"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo76qb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult76qb=gs.feverActive?2:1;
      const isTiny76=(hit._qbScale76||0.5)<0.2;
      const basePts76qb=isTiny76?400:80;
      const pts76qb=Math.round(basePts76qb*combo76qb*feverMult76qb);
      gs.score+=pts76qb;gs.streak++;gs.lastTapTime=Date.now();
      if(isTiny76){
        spawnPopup(hit.x,hit.y-36,`⚛️ TINY ×5 +${pts76qb}`,"#fde68a",28);
        spawnParticles(hit.x,hit.y,"#fde68a",18,"spark");
        sfx("legendary");vibrate([8,4,16,4,8]);
        setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),1000);
        unlock("qblast_tiny");
      } else {
        spawnPopup(hit.x,hit.y-26,`💜 +${pts76qb}`,"#c084fc",20);
        spawnParticles(hit.x,hit.y,"#f5d0fe",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("qblast_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgQB76=levelCfgRef.current;
      if(cfgQB76){if(gs.score>=cfgQB76.scoreGoal&&(!cfgQB76.modifier||checkModGoal(cfgQB76.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="nexus_core"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo75nc=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult75nc=gs.feverActive?2:1;
      const pts75nc=Math.round(80*combo75nc*feverMult75nc);
      gs.score+=pts75nc;gs.streak++;gs.lastTapTime=Date.now();
      const isEdge75=hitDist>hit.radius*0.8; // tapped near edge of hit zone
      spawnPopup(hit.x,hit.y-28,`💠 CORE +${pts75nc}`,"#38bdf8",22);
      spawnParticles(hit.x,hit.y,"#bae6fd",12,"spark");
      sfx("tap");vibrate(5);
      unlock("nexuscore_tap");
      if(isEdge75)unlock("nexuscore_edge");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgNC75=levelCfgRef.current;
      if(cfgNC75){if(gs.score>=cfgNC75.scoreGoal&&(!cfgNC75.modifier||checkModGoal(cfgNC75.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="blink_star"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo75bs=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult75bs=gs.feverActive?2:1;
      const justBlinked75=!!(hit._justBlinked75);
      const mult75bs=justBlinked75?3:1;
      const pts75bs=Math.round(95*mult75bs*combo75bs*feverMult75bs);
      gs.score+=pts75bs;gs.streak++;gs.lastTapTime=Date.now();
      if(justBlinked75){
        spawnPopup(hit.x,hit.y-34,`⭐ BLINK ×3 +${pts75bs}`,"#fde68a",28);
        spawnParticles(hit.x,hit.y,"#fde68a",16,"spark");
        sfx("legendary");vibrate([6,4,12,4,6]);
        unlock("blinkstar_crit");
      } else {
        spawnPopup(hit.x,hit.y-24,`⭐ +${pts75bs}`,"#f472b6",18);
        spawnParticles(hit.x,hit.y,"#fce7f3",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("blinkstar_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgBS75=levelCfgRef.current;
      if(cfgBS75){if(gs.score>=cfgBS75.scoreGoal&&(!cfgBS75.modifier||checkModGoal(cfgBS75.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="omega_pulse"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo74op=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult74op=gs.feverActive?2:1;
      const elapsed74op=Date.now()-hit.spawnedAt;
      const fast74=elapsed74op<2000;
      const pts74op=Math.round(300*combo74op*feverMult74op);
      gs.score+=pts74op;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-34,`Ω OMEGA +${pts74op}`,"#a78bfa",32);
      spawnParticles(hit.x,hit.y,"#ddd6fe",20,"spark");
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:280,born:performance.now(),color:"#7c3aed",alpha:0.7,life:900});
      sfx("legendary");vibrate([12,4,20,4,12]);
      unlock("omegapulse_tap");
      if(fast74)unlock("omegapulse_fast");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgOP74=levelCfgRef.current;
      if(cfgOP74){if(gs.score>=cfgOP74.scoreGoal&&(!cfgOP74.modifier||checkModGoal(cfgOP74.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="spiral_orb74"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo74so=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult74so=gs.feverActive?2:1;
      const atCenter74=!!(hit._atCenter74);
      const mult74so=atCenter74?2:1;
      const pts74so=Math.round(90*mult74so*combo74so*feverMult74so);
      gs.score+=pts74so;gs.streak++;gs.lastTapTime=Date.now();
      if(atCenter74){
        spawnPopup(hit.x,hit.y-30,`🌀 CENTER ×2 +${pts74so}`,"#fbbf24",26);
        spawnParticles(hit.x,hit.y,"#fde68a",14,"spark");
        sfx("chainBonus");vibrate([6,4,10]);
        unlock("spiralorb74_center");
      } else {
        spawnPopup(hit.x,hit.y-22,`🌀 +${pts74so}`,"#22d3ee",18);
        spawnParticles(hit.x,hit.y,"#a5f3fc",8,"dot");
        sfx("tap");vibrate(5);
      }
      unlock("spiralorb74_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgSO74=levelCfgRef.current;
      if(cfgSO74){if(gs.score>=cfgSO74.scoreGoal&&(!cfgSO74.modifier||checkModGoal(cfgSO74.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="light_bomb"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo73lb=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult73lb=gs.feverActive?2:1;
      const pts73lb=Math.round(70*combo73lb*feverMult73lb);
      gs.score+=pts73lb;gs.streak++;gs.lastTapTime=Date.now();
      spawnPopup(hit.x,hit.y-24,`💡 SAFE! +${pts73lb}`,"#fbbf24",20);
      spawnParticles(hit.x,hit.y,"#fde68a",12,"spark");
      sfx("powerUp");vibrate(5);
      // Track for chain achievement
      const gs73lb=gsRef.current;
      gs73lb._lightBombCount=(gs73lb._lightBombCount||0)+1;
      unlock("lightbomb_tap");
      if(gs73lb._lightBombCount>=3)unlock("lightbomb_chain");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgLB73=levelCfgRef.current;
      if(cfgLB73){if(gs.score>=cfgLB73.scoreGoal&&(!cfgLB73.modifier||checkModGoal(cfgLB73.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="frost_nova"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      const combo73fn=Math.min(10,1+Math.floor(gs.streak/5));
      const feverMult73fn=gs.feverActive?2:1;
      // Freeze all other targets for 1.5s
      const frozenTargets73=targetsRef.current.filter(o=>!o.dying&&o.type!=="bomb"&&o.type!=="boss");
      const freezeUntil73=Date.now()+1500;
      frozenTargets73.forEach(o=>{o._frozenUntil=freezeUntil73;o._frozenColor="#93c5fd";});
      const frozenCount73=frozenTargets73.length;
      const pts73fn=Math.round((40+frozenCount73*30)*combo73fn*feverMult73fn);
      gs.score+=pts73fn;gs.streak++;gs.lastTapTime=Date.now();
      if(frozenCount73>=3){
        spawnPopup(hit.x,hit.y-34,`❄️ FREEZE ×${frozenCount73} +${pts73fn}`,"#93c5fd",28);
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:250,born:performance.now(),color:"#93c5fd",alpha:0.5,life:700});
        spawnParticles(hit.x,hit.y,"#bfdbfe",16,"spark");
        sfx("chainBonus");vibrate([8,4,12,4,8]);
        unlock("frostnova_freeze");
      } else {
        spawnPopup(hit.x,hit.y-24,`❄️ +${pts73fn}`,"#60a5fa",20);
        spawnParticles(hit.x,hit.y,"#bfdbfe",10,"dot");
        sfx("tap");vibrate(6);
      }
      unlock("frostnova_tap");
      gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
      const cfgFN73=levelCfgRef.current;
      if(cfgFN73){if(gs.score>=cfgFN73.scoreGoal&&(!cfgFN73.modifier||checkModGoal(cfgFN73.modifier,gs))){endLevel(true);return;}}
      return;
    }
    if(hit.type==="bomb"){
      // PHASE_BURST — score bomb safely, consume one charge
      {const pbPwr68=activePwrRef.current.find(p=>p.type==="PHASE_BURST"&&(p.charges||0)>0&&p.endsAt>Date.now());
      if(pbPwr68){
        pbPwr68.charges=(pbPwr68.charges||1)-1;
        if(pbPwr68.charges<=0)activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PHASE_BURST");
        setActivePwrDisp([...activePwrRef.current]);
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        const phasePts68=Math.round(80*(gs.feverActive?2:1));
        gs.score+=phasePts68;gs.streak++;gs.lastTapTime=Date.now();
        spawnPopup(hit.x,hit.y-28,`💠 PHASED +${phasePts68}`,"#38bdf8",20);
        spawnParticles(hit.x,hit.y,"#7dd3fc",10,"spark");
        sfx("powerUp");vibrate([4,3,8]);
        unlock("phase_burst_bomb");
        gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
        return;
      }}
      // GHOST_MODE or PHANTOM_TOUCH — bombs are intangible; just flash and stay
      if(activePwrRef.current.some(p=>(p.type==="PHANTOM_TOUCH"||p.type==="GHOST_MODE")&&p.endsAt>Date.now())){
        spawnParticles(hit.x,hit.y,"#818cf8",6,"spark");
        spawnPopup(hit.x,hit.y-18,"👻 GHOST!","#818cf8",13);
        sfx("tap");unlock("ghost_mode_use");return;
      }
      // Double-tap defuse: tapping same bomb twice within 200ms defuses it for 25pts
      const nowBomb=Date.now();
      if(hit._firstTap&&(nowBomb-hit._firstTap)<200){
        // Defuse! Remove without penalty
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        sfx("rare");vibrate([10,5,10]);
        spawnParticles(hit.x,hit.y,"#4ade80",14,"spark");
        spawnPopup(hit.x,hit.y-22,"💚 DEFUSED! +25","#4ade80",16);
        gs.score+=25;gs.sessionStats.score=gs.score;
        unlock("bomb_defuse");
        return;
      }
      hit._firstTap=nowBomb;
      setTimeout(()=>{if(hit._firstTap===nowBomb)hit._firstTap=null;},200);
      // LIFE_BOMB — next bomb tap restores 1 life
      {const lbPwr=activePwrRef.current.find(p=>p.type==="LIFE_BOMB"&&(p.charges||0)>0&&p.endsAt>Date.now());
      if(lbPwr){
        lbPwr.charges=0;activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_BOMB");setActivePwrDisp([...activePwrRef.current]);
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        gs.lives=Math.min(5,gs.lives+1);
        spawnPopup(hit.x,hit.y-28,"💣❤️ +1 LIFE!","#f87171",22);
        spawnParticles(hit.x,hit.y,"#fca5a5",12,"spark");
        sfx("coin");vibrate([4,2,8]);
        unlock("life_bomb_proc");
        gs.sessionStats.tapsTotal++;updateMissions(gs.sessionStats);
        const cfgLB=levelCfgRef.current;
        if(cfgLB){if(gs.score>=cfgLB.scoreGoal&&(!cfgLB.modifier||checkModGoal(cfgLB.modifier,gs))){endLevel(true);return;}}
        return;
      }}
      // ORBIT_SHIELD — absorbs one bomb tap
      {const orbSh=activePwrRef.current.find(p=>p.type==="ORBIT_SHIELD"&&(p.charges||0)>0&&p.endsAt>Date.now());
      if(orbSh){
        orbSh.charges=0;activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="ORBIT_SHIELD");setActivePwrDisp([...activePwrRef.current]);
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        spawnPopup(hit.x,hit.y-22,"🛸🛡 ORBIT SHIELD!","#93c5fd",20);
        spawnParticles(hit.x,hit.y,"#bfdbfe",14,"spark");
        sfx("rare");vibrate([5,3,8]);
        unlock("orbit_shield_abs");
        return;
      }}
      // Normal bomb hit
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      sfx("bombHit");vibrate(55);spawnParticles(hit.x,hit.y,"#ef4444",16,"spark");
      const hasShield=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>Date.now());
      if(hasShield){
        activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");
        setActivePwrDisp([...activePwrRef.current]);
        spawnPopup(hit.x,hit.y,"SHIELD!","#60a5fa",17);vibrate([5,15,5]);
      } else {
        const berserkerActive=gs._berserkerEndsAt&&Date.now()<gs._berserkerEndsAt;
        const livesToLose=berserkerActive?2:1;
        gs.lives=Math.max(0,gs.lives-livesToLose);gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);
        spawnPopup(hit.x,hit.y,berserkerActive?"💥 BOOM! -2 LIVES!":"BOOM!","#ef4444",berserkerActive?20:19);
        setScreenShake(true);setTimeout(()=>setScreenShake(false),450);vibrate([45,20,45]);
        if(gs.lives<=0){endLevel(false);return;}
      }
      return;
    }
    // POWERUP
    if(hit.type==="powerup"){
      targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
      activatePowerUp(hit.x,hit.y,hit.pwrType);sfx("coin");return;
    }
    // BOSS
    if(hit.type==="boss"){
      hit.hitsLeft--;sfx("bossHit");vibrate(22);spawnParticles(hit.x,hit.y,hit.color,10,"spark");
      // Phase 2 transition at 66% HP (or 2/3 hits remaining for 3-hit bosses)
      const hpPct=hit.hitsLeft/(hit.maxHits||1);
      if(hpPct<=0.66&&hpPct>0.33&&hit.bossPhase===1){
        hit.bossPhase=2;
        sfx("bossPhase");vibrate([20,10,20]);
        spawnPopup(hit.x,hit.y-50,"💨 PHASE 2!",hit.color||"#ff6030",18);
        const taunt2=BOSS_TAUNTS.phase2[Math.floor(Math.random()*BOSS_TAUNTS.phase2.length)];
        setBossTaunt({text:taunt2,phase:2});setTimeout(()=>setBossTaunt(null),3500);
        setScreenShake(true);setTimeout(()=>setScreenShake(false),350);
        // New random direction, also remember new anchor for orbit pattern
        const a2=Math.random()*Math.PI*2;
        hit.vx=Math.cos(a2)*1.5;hit.vy=Math.sin(a2)*1.5;
        hit.cx=hit.x;hit.cy=hit.y;
        // Spawn 2 mini bombs as backup
        const cfgNow=levelCfgRef.current;
        if(cfgNow&&!cfgNow.isBoss){
          for(let i=0;i<2;i++){
            const ang=Math.random()*Math.PI*2;const dist=60+Math.random()*30;
            targetsRef.current.push({
              id:Math.random().toString(36).slice(2),type:"bomb",
              x:hit.x+Math.cos(ang)*dist,y:hit.y+Math.sin(ang)*dist,
              radius:BASE_R,color:"#ef4444",glow:"#dc2626",
              lifetime:Math.min(3500,cfgNow.targetLifetime*0.55),
              spawnedAt:Date.now(),born:performance.now(),
              moving:false,hitsLeft:1,maxHits:1,
              worldId:cfgNow.world,worldColor:cfgNow.worldColor,
            });
          }
        }
      }
      // Rage mode on last hit (phase 3)
      if(hit.hitsLeft===1&&!hit.rage){
        hit.rage=true;hit.bossPhase=3;hit.moving=true;hit.vx=(Math.random()-0.5)*4;hit.vy=(Math.random()-0.5)*4;
        hit.cx=hit.x;hit.cy=hit.y;
        sfx("bossPhase");vibrate([30,15,30,15,60]);
        spawnPopup(hit.x,hit.y-50,"⚠️ RAGE MODE!","#ff0000",20);
        const taunt3=BOSS_TAUNTS.phase3[Math.floor(Math.random()*BOSS_TAUNTS.phase3.length)];
        setBossTaunt({text:taunt3,phase:3});setTimeout(()=>setBossTaunt(null),4000);
        setScreenShake(true);setTimeout(()=>setScreenShake(false),500);
        // Speed up spawning
        if(levelCfgRef.current)levelCfgRef.current._rageSpawn=true;
      }
      if(hit.hitsLeft<=0){
        targetsRef.current=targetsRef.current.filter(t=>t.id!==hit.id);
        sfx("jackpot");vibrate([35,20,35,20,60,20,80]);
        // Confetti explosion in world colors
        const wc=hit.worldColor||hit.color;
        const rainbowColors=["#ff6030","#ffd700","#34d399","#60a5fa","#f472b6","#a78bfa","#ff6b35"];
        rainbowColors.forEach(c=>spawnParticles(hit.x,hit.y,c,18,"spark"));
        spawnParticles(hit.x,hit.y,"#ffffff",25,"dot");
        setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),1200);
        setScreenShake(true);setTimeout(()=>setScreenShake(false),600);
        const pts=800*Math.min(10,1+Math.floor(gs.streak/5))*(gs.feverActive?2:1);
        gs.score+=pts;gs.sessionStats.bossKills=(gs.sessionStats.bossKills||0)+1;
        _bgBlast={x:hit.x,y:hit.y,at:performance.now()}; // explode bg particles
        spawnPopup(hit.x,hit.y-28,`🏆 BOSS! +${pts}`,"#ffd700",24);
        const dyingTaunt=BOSS_TAUNTS.dying[Math.floor(Math.random()*BOSS_TAUNTS.dying.length)];
        setBossTaunt({text:dyingTaunt,phase:"dying"});setTimeout(()=>setBossTaunt(null),3000);
        unlock("boss_kill");
        // Boss loot drop — guaranteed rare reward
        {
          const totalW=BOSS_LOOT.reduce((s,l)=>s+l.weight,0);
          let r=Math.random()*totalW,loot=BOSS_LOOT[BOSS_LOOT.length-1];
          for(const l of BOSS_LOOT){r-=l.weight;if(r<=0){loot=l;break;}}
          const amount=loot.min===loot.max?loot.min:Math.floor(loot.min+Math.random()*(loot.max-loot.min+1));
          const sv2=saveRef.current;
          if(loot.type==="coins"){sv2.coins+=amount;sv2.totalCoins=(sv2.totalCoins||0)+amount;sfx("coin");}
          else if(loot.type==="xp"){sv2.xp=(sv2.xp||0)+amount;}
          else if(loot.type==="fragment"){
            sv2.bossFragments=(sv2.bossFragments||0)+1;
            if(sv2.bossFragments>=10){
              // Every 10 fragments unlock a random unowned accessory
              const allAcc=["hat","crown","glasses","halo","bow","star"];
              const owned=sv2.ownedAccessories||{};
              const avail=allAcc.filter(id=>!Object.keys(owned).some(k=>k.endsWith(":"+id)));
              if(avail.length>0){
                const pick=avail[Math.floor(Math.random()*avail.length)];
                const key=(sv2.mascotId||"dragon")+":"+pick;
                sv2.ownedAccessories={...owned,[key]:true};
                sv2.bossFragments-=10;
                // Celebration — rainbow flash + particles + notification
                setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),1000);
                sfx("jackpot");vibrate([20,10,20,10,40,10,60]);
                const cx=canvasRef.current?canvasRef.current.width/2:180;
                const cy=canvasRef.current?canvasRef.current.height/2:360;
                ["#c084fc","#ffd700","#ff6030","#34d399","#60a5fa","#f472b6"].forEach(c=>spawnParticles(cx,cy,c,14,"spark"));
                spawnParticles(cx,cy,"#ffffff",20,"dot");
                setTimeout(()=>setNotif(`🎁 ACCESSORY UNLOCKED: ${pick}! 💜`),400);
              } else {
                // No more accessories to unlock — convert to coins
                sv2.coins=(sv2.coins||0)+150;sv2.totalCoins=(sv2.totalCoins||0)+150;
                sv2.bossFragments-=10;
                setTimeout(()=>setNotif("💰 Fragments → +150 coins! (all accessories owned)"),400);
              }
            }
          }
          sv2.bossLootHistory=[{type:loot.type,amount,date:getTodayKey()},...(sv2.bossLootHistory||[])].slice(0,20);
          debounceSave();
          const lootLabel=loot.type==="fragment"?`${loot.emoji} FRAGMENT x1 (${sv2.bossFragments||1}/10)`:`${loot.emoji} ${loot.label} +${amount}`;
          setTimeout(()=>spawnPopup(hit.x,hit.y-70,lootLabel,loot.color,18),200);
          setTimeout(()=>{sfx("unlock");vibrate([15,10,30]);},350);
        }
        // BONUS ROUND — free spins equivalent (non-boss levels only)
        {const cfgB=levelCfgRef.current;
        if(cfgB&&!cfgB.isBoss&&!gs.bonusRoundActive){
          gs.bonusRoundActive=true;setBonusRound(true);
          spawnPopup(hit.x,hit.y-65,"🌟 BONUS ROUND!","#ffd700",20);
          sfx("lucky");vibrate([25,15,25,15,55]);
          showMascotSpeech("bonus");
          setTimeout(()=>{if(gsRef.current)gsRef.current.bonusRoundActive=false;setBonusRound(false);},9000);
        }}
        // Check modifier goal
        const cfg=levelCfgRef.current;
        if(cfg?.modifier?.type==="boss_kill"&&gs.sessionStats.bossKills>=1&&gs.score>=cfg.scoreGoal){endLevel(true);return;}
        if(cfg?.modifier?.type==="final_boss"){endLevel(true);return;}
      }
      return;
    }
    // NORMAL — squish then remove
    hit.dying=performance.now();
    gs.streak++;
    gs.lastTapTime=Date.now();
    // Log hit for adaptive difficulty
    if(gs._hitLog){gs._hitLog.push({t:Date.now(),hit:true});if(gs._hitLog.length>80)gs._hitLog.shift();}
    setStreakDecaying(false);
    const multSurge=activePwrRef.current.some(p=>p.type==="MULTIPLIER_SURGE"&&p.endsAt>Date.now());
    const combo=Math.min(multSurge?20:10,Math.round((1+Math.floor(gs.streak/5))*(multSurge?2:1)));
    const isDouble=activePwrRef.current.some(p=>p.type==="DOUBLE"&&p.endsAt>Date.now());
    const isMultiplier=activePwrRef.current.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
    const feverMult=gs.feverActive?2:1;
    const timeLeft=1-(Date.now()-hit.spawnedAt)/hit.lifetime;
    const ceLevel=(saveRef.current.skills||{}).critical_eye||0;
    const perfectZone=0.38*(ceLevel===3?1.35:ceLevel===2?1.20:ceLevel===1?1.10:1.0);
    const isPerfect=hitDist<hit.radius*perfectZone&&timeLeft>0.36&&timeLeft<0.67;
    const isLastBreath=timeLeft<0.08&&hit.type==="normal"; // caught in last 8% of life
    // Prestige score multiplier (+5% per prestige level, max 5 prestiges = +25%)
    const prestigeMult=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
    const shardMult=hit._isShard?0.5:1; // splitter shards award half points
    const poisonMult=gs._poisonedUntil&&Date.now()<gs._poisonedUntil?0.5:1; // poison debuff
    if(poisonMult<1)gs._poisonedUntil=null; // consume debuff after one hit
    const scoreBoostMult=(gs._scoreBoostTaps||0)>0?5:1;
    if(scoreBoostMult>1){gs._scoreBoostTaps--;if(gs._scoreBoostTaps<=0)setNotif("×5 boost ended!");}
    // Beacon bonus: if any active beacon within 110px, target scores 2×
    const BEACON_RANGE=110;
    const beaconActive=targetsRef.current.some(t=>t.type==="beacon"&&!t.dying&&Math.hypot(t.x-hit.x,t.y-hit.y)<BEACON_RANGE);
    const beaconMult=beaconActive?2:1;
    const overclockMult=(gs._overclockEndsAt&&Date.now()<gs._overclockEndsAt)?2:1;
    // Supercell boost: if this target was electrified by a Supercell, 2× pts
    const supercellMult=(hit._supercellBoosted&&Date.now()<hit._supercellBoosted)?2:1;
    if(supercellMult>1){spawnParticles(hit.x,hit.y,"#7dd3fc",6,"spark");}
    // Berserker: all normal targets 5× pts
    const berserkerMult=(gs._berserkerEndsAt&&Date.now()<gs._berserkerEndsAt)?5:1;
    if(berserkerMult>1){spawnParticles(hit.x,hit.y,"#ef4444",6,"spark");}
    // Bounty Board bonus: matching rarity gives 2-5× extra points
    const bounty=bountyRef.current;
    const bountyHit=bounty&&bounty.expiresAt>Date.now()&&hit.rarity?.name===bounty.rarity;
    const bountyMult=bountyHit?bounty.mult:1;
    const megaTapMult=(()=>{const mt=activePwrRef.current.find(p=>p.type==="MEGA_TAP"&&p.endsAt>Date.now());if(!mt||!gs._megaTapCharges||gs._megaTapCharges<=0)return 1;gs._megaTapCharges--;mt.charges=gs._megaTapCharges;if(gs._megaTapCharges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="MEGA_TAP");setActivePwrDisp([...activePwrRef.current]);}return 3;})();
    const thunderMult=(hit._thunderCharged&&hit._thunderExpiry&&Date.now()<hit._thunderExpiry)?2:1;
    if(thunderMult===2){spawnPopup(hit.x,hit.y-32,"⚡ CHARGED! ×2","#fbbf24",14);}
    // Laser Prism charge bonus
    const laserBonus=(hit._laserCharged&&hit._laserExpiry&&Date.now()<hit._laserExpiry)?150:0;
    if(laserBonus>0){spawnPopup(hit.x,hit.y-36,"🔆 LASER! +150","#f43f5e",13);unlock("laserprism_chain");}
    // Toxic Cloud penalty — nearby cloud penalizes this tap
    const nearbyToxic=targetsRef.current.find(t=>t.type==="toxic_cloud"&&!t.dying&&Math.hypot(t.x-hit.x,t.y-hit.y)<180);
    if(nearbyToxic&&!activePwrRef.current.some(p=>p.type==="SCORE_GUARD"&&p.endsAt>Date.now())){
      gs.score=Math.max(0,gs.score-50);spawnPopup(hit.x,hit.y-28,"☣️ TOXIC! −50","#4ade80",12);
      gs._toxicPenalties=(gs._toxicPenalties||0)+1;
      if(gs._toxicPenalties>=3)unlock("toxic_zone");
    }
    const scoreAmpMult=targetsRef.current.some(t=>t.type==="score_amp"&&!t.dying&&Math.hypot(t.x-hit.x,t.y-hit.y)<220)?2:1;
    const portalMult=(()=>{if(!gs._portalNextTap)return 1;gs._portalNextTap=false;spawnPopup(hit.x,hit.y-42,"💫 PORTAL ×5!","#c4b5fd",22);unlock("portal_bonus2");return 5;})();
    const goldenMult=(()=>{const gt=activePwrRef.current.find(p=>p.type==="GOLDEN_TOUCH"&&p.endsAt>Date.now());if(!gt||!gs._goldenCharges||gs._goldenCharges<=0)return 1;gs._goldenCharges--;gt.charges=gs._goldenCharges;if(gs._goldenCharges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="GOLDEN_TOUCH");setActivePwrDisp([...activePwrRef.current]);}spawnPopup(hit.x,hit.y-48,"✨ GOLDEN!","#ffd700",12);return RARITY.LEGENDARY.mult/hit.rarity.mult;})();
    const scoreX2Mult=activePwrRef.current.some(p=>p.type==="SCORE_X2"&&p.endsAt>Date.now())?2:1;
    const energyBoostMult=(hit._energyBoosted&&Date.now()<hit._energyBoosted)?2:1;
    if(energyBoostMult>1){spawnPopup(hit.x,hit.y-44,"🔋 BOOSTED! ×2","#06b6d4",12);}
    const pts=Math.round((hit.rarity.mult*combo*feverMult*(isDouble?2:1)*(isMultiplier?3:1)*(isPerfect?1.5:1)*(isLastBreath?1.5:1)*prestigeMult*shardMult*poisonMult*scoreBoostMult*beaconMult*overclockMult*bountyMult*supercellMult*berserkerMult*megaTapMult*thunderMult*scoreAmpMult*portalMult*goldenMult+laserBonus)*scoreX2Mult*energyBoostMult);
    gs.score+=pts;
    // Swipe Boost — rapid-tap stacking bonus
    {const swb=activePwrRef.current.find(p=>p.type==="SWIPE_BOOST"&&p.endsAt>Date.now());
    if(swb){
      const nowSWB=Date.now();
      if(nowSWB-swb.lastTap<=500){swb.stack=(swb.stack||0)+1;}
      else{swb.stack=1;}
      swb.lastTap=nowSWB;
      const swbBonus=swb.stack*50;
      gs.score+=swbBonus;
      if(swb.stack>1)spawnPopup(hit.x,hit.y-34,`💨 ×${swb.stack} +${swbBonus}`,"#67e8f9",12);
    }}
    // Rush Mode — if this target was rushed (gold), apply 2× bonus
    if(hit._rushedUntil&&Date.now()<hit._rushedUntil){
      gs.score+=pts; // double pts (add pts again)
      spawnPopup(hit.x,hit.y-32,"⚡ RUSH ×2!","#fcd34d",14);
    }
    // Chain Burst — +75 bonus per tap when streak >= 3
    if(gs.streak>=3&&activePwrRef.current.some(p=>p.type==="CHAIN_BURST"&&p.endsAt>Date.now())){
      gs.score+=75;
      spawnPopup(hit.x,hit.y-22,"+75 BURST","#f97316",12);
      spawnParticles(hit.x,hit.y,"#fb923c",4,"spark");
    }
    // Coin Rush — 30% chance to award 1 coin per tap
    if(activePwrRef.current.some(p=>p.type==="COIN_RUSH"&&p.endsAt>Date.now())&&Math.random()<0.3){
      saveRef.current.coins=(saveRef.current.coins||0)+1;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+1;
      debounceSave();
      spawnPopup(hit.x,hit.y-30,"🪙+1","#fcd34d",11);
    }
    // Coin Magnet — always award 2 coins per tap
    {const cmMag=activePwrRef.current.find(p=>p.type==="COIN_MAGNET"&&p.endsAt>Date.now());
    if(cmMag){
      const magCoins=2;
      saveRef.current.coins=(saveRef.current.coins||0)+magCoins;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+magCoins;
      cmMag.coinsCollected=(cmMag.coinsCollected||0)+magCoins;
      debounceSave();
      spawnPopup(hit.x,hit.y-32,"🧲🪙+2","#fbbf24",11);
      if(cmMag.coinsCollected>=20)unlock("coin_magnet_rich");
    }}
    // Twin Strike — auto-score nearest non-hit normal target for 50% pts
    if(activePwrRef.current.some(p=>p.type==="TWIN_STRIKE"&&p.endsAt>Date.now())){
      const twin=targetsRef.current.find(t=>t.id!==hit.id&&!t.dying&&t.type==="normal"&&Math.hypot(t.x-hit.x,t.y-hit.y)<300);
      if(twin){
        const twinPts=Math.round(pts*0.5);
        gs.score+=twinPts;
        twin.dying=performance.now();
        spawnParticles(twin.x,twin.y,"#f472b6",8,"spark");
        spawnPopup(twin.x,twin.y-18,`👥 TWIN +${twinPts}`,"#f472b6",12);
      }
    }
    // Echo Shot — auto-hit nearest other normal for 70 pts
    if(activePwrRef.current.some(p=>p.type==="ECHO_SHOT"&&p.endsAt>Date.now())){
      const echo=targetsRef.current.find(t=>t.id!==hit.id&&!t.dying&&t.type==="normal");
      if(echo){
        const echoPts=Math.round(70*(gs.feverActive?2:1));
        gs.score+=echoPts;
        echo.dying=performance.now();
        spawnParticles(echo.x,echo.y,"#38bdf8",8,"spark");
        spawnPopup(echo.x,echo.y-18,`📡+${echoPts}`,"#7dd3fc",12);
      }
    }
    // Dark Pulse bonus tracking
    if(hit._darkBonus&&hit._darkGroup){
      const remDark=targetsRef.current.filter(o=>!o.dying&&o._darkGroup===hit._darkGroup);
      if(remDark.length===0)unlock("darkpulse_bonus");
      spawnPopup(hit.x,hit.y-12,"🌑 BONUS!","#a78bfa",11);
    }
    // Freeze Orb bonus tracking
    if(gs._freezeOrbExpiry&&Date.now()<gs._freezeOrbExpiry){
      gs._freezeOrbNormalTaps=(gs._freezeOrbNormalTaps||0)+1;
      if(gs._freezeOrbNormalTaps>=3)unlock("freeze_orb_bonus");
    }
    // Tempo Sync — bright phase (500ms/s) = 2× pts
    if(activePwrRef.current.some(p=>p.type==="TEMPO_SYNC"&&p.endsAt>Date.now())){
      const cycleMs48=Date.now()%1000;
      if(cycleMs48<500){
        gs.score+=pts;
        spawnPopup(hit.x,hit.y-26,"🎵×2!","#f0abfc",12);
      }
    }
    // Gold Rush — all normal taps worth 2× pts
    if(activePwrRef.current.some(p=>p.type==="GOLD_RUSH"&&p.endsAt>Date.now())){
      gs.score+=pts; // double pts
      spawnPopup(hit.x,hit.y-24,`🥇 ×2!`,"#ffd700",11);
    }
    // Mirror Maze — ghost-tap at mirrored position
    if(activePwrRef.current.some(p=>p.type==="MIRROR_MAZE"&&p.endsAt>Date.now())){
      const cw47=canvasRef.current?.width||360;
      const mirX47=cw47-hit.x;
      const nearMirror47=targetsRef.current.find(t=>t.id!==hit.id&&!t.dying&&t.type==="normal"&&Math.hypot(t.x-mirX47,t.y-hit.y)<60);
      if(nearMirror47){
        const mirPts47=Math.round(pts*0.6);
        gs.score+=mirPts47;
        nearMirror47.dying=performance.now();
        spawnParticles(nearMirror47.x,nearMirror47.y,"#a78bfa",8,"spark");
        spawnPopup(nearMirror47.x,nearMirror47.y-18,`🪞+${mirPts47}`,"#a78bfa",12);
      }
    }
    // Crit Boost — every 3rd tap is a 3× critical hit
    {const critPwr=activePwrRef.current.find(p=>p.type==="CRIT_BOOST"&&p.endsAt>Date.now());
    if(critPwr){
      critPwr.tapCount=(critPwr.tapCount||0)+1;
      if(critPwr.tapCount%3===0){
        const critBns=pts*2; // total 3× (pts already added, so add 2× more)
        gs.score+=critBns;
        spawnPopup(hit.x,hit.y-36,`💥 CRIT! ×3 +${critBns}`,"#ef4444",18);
        spawnParticles(hit.x,hit.y,"#fca5a5",10,"spark");
        setScreenShake(true);setTimeout(()=>setScreenShake(false),120);
        unlock("crit_boost_proc");
      }
    }}
    // Phantom Burst — each tap spawns a bonus phantom for +30 pts
    if(activePwrRef.current.some(p=>p.type==="PHANTOM_BURST"&&p.endsAt>Date.now())){
      const phBns=Math.round(30*(gs.feverActive?2:1));
      gs.score+=phBns;
      spawnParticles(hit.x+Math.random()*60-30,hit.y+Math.random()*40-20,"#c084fc",5,"spark");
      spawnPopup(hit.x+20,hit.y-28,`👻+${phBns}`,"#c084fc",11);
    }
    // Rarity Boost — track legendary taps for achievement
    {const rbPwr=activePwrRef.current.find(p=>p.type==="RARITY_BOOST"&&p.endsAt>Date.now());
    if(rbPwr&&hit.rarity===RARITY.LEGENDARY){
      rbPwr.legCount=(rbPwr.legCount||0)+1;
      if(rbPwr.legCount>=3)unlock("rarity_boost_leg");
    }}
    // Streak Storm — at streak ≥10, bonus pts equal to streak count
    if(gs.streak>=10&&activePwrRef.current.some(p=>p.type==="STREAK_STORM"&&p.endsAt>Date.now())){
      const stormBns=gs.streak;
      gs.score+=stormBns;
      spawnPopup(hit.x,hit.y-36,`⛈️ ×${gs.streak} +${stormBns}`,"#67e8f9",13);
      unlock("streak_storm_bonus");
    }
    // Score Echo — every 5th tap echoes for 2× last pts
    {const sEcho=activePwrRef.current.find(p=>p.type==="SCORE_ECHO"&&p.endsAt>Date.now());
    if(sEcho){
      sEcho.lastPts=pts;
      sEcho.tapCount=(sEcho.tapCount||0)+1;
      if(sEcho.tapCount%5===0){
        const echoBns=pts*2;
        gs.score+=echoBns;
        spawnPopup(hit.x,hit.y-38,`📢 ECHO ×2! +${echoBns}`,"#fde68a",18);
        spawnParticles(hit.x,hit.y,"#fbbf24",10,"spark");
        unlock("score_echo_proc");
      }
    }}
    // Life Leech — every 10 taps restores 1 life (max 5)
    {const llPwr=activePwrRef.current.find(p=>p.type==="LIFE_LEECH"&&p.endsAt>Date.now());
    if(llPwr){
      llPwr.tapCount=(llPwr.tapCount||0)+1;
      if(llPwr.tapCount%10===0&&gs.lives<5){
        gs.lives=Math.min(5,gs.lives+1);
        spawnPopup(hit.x,hit.y-36,`❤️🩸 +1 LIFE`,"#f87171",18);
        spawnParticles(hit.x,hit.y,"#fca5a5",10,"spark");
        sfx("coin");vibrate([4,2,8]);
        unlock("life_leech_proc");
      }
    }}
    // Chain Magnet — schedule auto-pull of nearest target within 600ms
    {const cmPwr=activePwrRef.current.find(p=>p.type==="CHAIN_MAGNET"&&p.endsAt>Date.now());
    if(cmPwr){
      cmPwr.lastPullAt=Date.now();
      cmPwr.pullCount=(cmPwr.pullCount||0)+1;
      if(cmPwr.pullCount>=5)unlock("chain_magnet_pull");
    }}
    // Score Surge — doubles multiplier every 5 consecutive taps (max ×8)
    {const ssPwr=activePwrRef.current.find(p=>p.type==="SCORE_SURGE"&&p.endsAt>Date.now());
    if(ssPwr){
      ssPwr.tapCount=(ssPwr.tapCount||0)+1;
      if(ssPwr.tapCount%5===0){
        ssPwr.mult=Math.min(8,ssPwr.mult*2);
        spawnPopup(hit.x,hit.y-38,`💹 ×${ssPwr.mult} SURGE!`,"#4ade80",18);
        spawnParticles(hit.x,hit.y,"#86efac",10,"spark");
        if(ssPwr.mult>=8)unlock("score_surge_peak");
      }
      if(ssPwr.mult>1){
        const surgeBns=Math.round(pts*(ssPwr.mult-1));
        gs.score+=surgeBns;
        if(ssPwr.mult>=4)spawnPopup(hit.x+18,hit.y-26,`💹+${surgeBns}`,"#4ade80",11);
      }
    }}
    // Echo Wave — shockwave scores 25pts per nearby non-bomb target
    if(activePwrRef.current.some(p=>p.type==="ECHO_WAVE"&&p.endsAt>Date.now())){
      const nearbyEW=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="bomb"&&Math.hypot(o.x-hit.x,o.y-hit.y)<140);
      if(nearbyEW.length>0){
        const ewBns=nearbyEW.length*25;
        gs.score+=ewBns;
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#67e8f9",size:160,born:performance.now(),duration:500,alpha:0.7});
        spawnPopup(hit.x,hit.y-38,`〰️ WAVE ×${nearbyEW.length} +${ewBns}`,"#67e8f9",16);
        if(nearbyEW.length>=3)unlock("echo_wave_multi");
      }
    }
    // Phase Lock — 2× score while active; track total gained for haul achievement
    {const plPwr=activePwrRef.current.find(p=>p.type==="PHASE_LOCK"&&p.endsAt>Date.now());
    if(plPwr){
      gs.score+=pts; // double pts (pts already added, add again)
      plPwr.scoreGained=(plPwr.scoreGained||0)+pts;
      spawnPopup(hit.x+20,hit.y-30,`🔒 ×2!`,"#a78bfa",11);
      if(plPwr.scoreGained>=500)unlock("phase_lock_haul");
    }}
    // Split Shot — auto-tap 1 additional nearest normal target for 50pts
    {const ssPwr53=activePwrRef.current.find(p=>p.type==="SPLIT_SHOT"&&p.endsAt>Date.now());
    if(ssPwr53){
      const extras=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type==="normal").sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y));
      if(extras.length>0){
        const extraTarget=extras[0];
        extraTarget.dying=performance.now();
        const splitBns=Math.round(50*(gs.feverActive?2:1));
        gs.score+=splitBns;
        spawnParticles(extraTarget.x,extraTarget.y,"#fbbf24",8,"spark");
        spawnPopup(extraTarget.x,extraTarget.y-18,`🎯 SPLIT +${splitBns}`,"#fbbf24",12);
        ssPwr53.autoCount=(ssPwr53.autoCount||0)+1;
        if(ssPwr53.autoCount>=3)unlock("split_shot_double");
      }
    }}
    // Turbo Tap — ×3 score for next 3 taps
    {const ttPwr=activePwrRef.current.find(p=>p.type==="TURBO_TAP"&&(p.charges||0)>0&&p.endsAt>Date.now());
    if(ttPwr){
      const turboBns=pts*2; // pts already added, add ×2 more = ×3 total
      gs.score+=turboBns;
      ttPwr.charges=(ttPwr.charges||1)-1;
      ttPwr.used=(ttPwr.used||0)+1;
      spawnPopup(hit.x+22,hit.y-30,`⚡×3 +${pts+turboBns}`,"#fbbf24",14);
      if(ttPwr.charges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="TURBO_TAP");setActivePwrDisp([...activePwrRef.current]);}
      if(ttPwr.used>=3)unlock("turbo_tap_triple");
    }}
    // BLAST RADIUS — each tap scores all targets within 80px
    {const brPwr=activePwrRef.current.find(p=>p.type==="BLAST_RADIUS"&&(p.charges||0)>0&&p.endsAt>Date.now());
    if(brPwr){
      const BR_RANGE=80;
      const near60=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="bomb"&&Math.hypot(o.x-tapX,o.y-tapY)<BR_RANGE);
      if(near60.length>0){
        const aeoPts60=Math.round(near60.length*60*(gs.feverActive?2:1));
        gs.score+=aeoPts60;
        near60.forEach(o=>{o.dying=performance.now();});
        spawnPopup(tapX,tapY-36,`💥 AOE ×${near60.length} +${aeoPts60}`,"#f97316",20);
        particlesRef.current.push({type:"shockwave",x:tapX,y:tapY,r:10,maxR:BR_RANGE,born:performance.now(),color:"#f97316",alpha:0.6,life:500});
        if(near60.length>=2)unlock("blast_radius_aoe");
      }
      brPwr.charges=(brPwr.charges||1)-1;
      if(brPwr.charges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BLAST_RADIUS");setActivePwrDisp([...activePwrRef.current]);}
    }}
    // SCORE ACCELERATOR — each tap adds +10% more score, stacking up to 8 taps
    {const saPwr=activePwrRef.current.find(p=>p.type==="SCORE_ACCELERATOR"&&p.endsAt>Date.now());
    if(saPwr){
      saPwr.tapCount=(saPwr.tapCount||0)+1;
      const accelMult60=Math.min(8,saPwr.tapCount)*0.1;
      const accelBonus60=Math.round(pts*accelMult60);
      if(accelBonus60>0){gs.score+=accelBonus60;spawnPopup(hit.x+26,hit.y-28,`📈×${(1+accelMult60).toFixed(1)} +${accelBonus60}`,"#34d399",13);}
      if(saPwr.tapCount>=8&&!saPwr.peakReached){saPwr.peakReached=true;unlock("score_accel_peak");}
      if(saPwr.tapCount>=8){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_ACCELERATOR");setActivePwrDisp([...activePwrRef.current]);}
    }}
    // SCORE_SPIKE — 5× on first tap in each 2s window
    {const spPwr=activePwrRef.current.find(p=>p.type==="SCORE_SPIKE"&&p.endsAt>Date.now());
    if(spPwr){
      const now67=Date.now();
      const windowIdx67=Math.floor(now67/2000);
      if(windowIdx67!==spPwr.lastWindow){
        spPwr.lastWindow=windowIdx67;
        const spikeBonus67=pts*4; // pts already added; add 4× more = 5× total
        gs.score+=spikeBonus67;
        spPwr.spikeCount=(spPwr.spikeCount||0)+1;
        spawnPopup(hit.x,hit.y-38,`📊 SPIKE! ×5 +${spikeBonus67}`,"#f43f5e",22);
        sfx("rare");vibrate([6,3,10]);
        unlock("score_spike_proc");
      }
    }}
    // RICOCHET_BLAST — auto-score nearest neighbor on this tap
    {const rbPwr74=activePwrRef.current.find(p=>p.type==="RICOCHET_BLAST"&&(p.charges||0)>0&&p.endsAt>Date.now());
    if(rbPwr74){
      const neighbor74=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="bomb"&&o.type!=="boss").sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y))[0];
      if(neighbor74){
        neighbor74.dying=performance.now();
        const ricoPts74=Math.round(60*(gs.feverActive?2:1));
        gs.score+=ricoPts74;
        spawnPopup(neighbor74.x,neighbor74.y-18,`🎯 RICO +${ricoPts74}`,"#fb923c",16);
        spawnParticles(neighbor74.x,neighbor74.y,"#fed7aa",8,"spark");
        unlock("ricochet_blast_nb");
        // Draw a tracer line particle
        particlesRef.current.push({type:"tracer74",x:hit.x,y:hit.y,tx:neighbor74.x,ty:neighbor74.y,born:performance.now(),life:300,color:"#fb923c"});
      }
      rbPwr74.charges=(rbPwr74.charges||4)-1;
      if(rbPwr74.charges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="RICOCHET_BLAST");setActivePwrDisp([...activePwrRef.current]);}
      else{setActivePwrDisp([...activePwrRef.current]);}
    }}
    // SCORE_AMPLIFY — add 10% of current score as bonus per tap
    {const saPwr73=activePwrRef.current.find(p=>p.type==="SCORE_AMPLIFY"&&p.endsAt>Date.now());
    if(saPwr73){
      const ampBonus73=Math.min(500,Math.round(gs.score*0.1));
      if(ampBonus73>0){
        gs.score+=ampBonus73;
        saPwr73.amplifyTotal=(saPwr73.amplifyTotal||0)+ampBonus73;
        spawnPopup(hit.x-20,hit.y-10,`📈+${ampBonus73}`,"#a3e635",14);
        if(saPwr73.amplifyTotal>=300)unlock("score_amplify_big");
      }
    }}
    // CRIT_SURGE — 30% chance for 3× tap score
    {const csPwr78=activePwrRef.current.find(p=>p.type==="CRIT_SURGE"&&p.endsAt>Date.now());
    if(csPwr78&&Math.random()<0.30){
      const critBase78=Math.round(50*Math.min(10,1+Math.floor((gs.streak||0)/5))*(gs.feverActive?2:1));
      const critBonus78=Math.round(critBase78*2); // +2× (total 3×)
      gs.score+=critBonus78;
      csPwr78.critCount=(csPwr78.critCount||0)+1;
      spawnPopup(hit.x,hit.y-32,`💥 CRIT ×3 +${critBonus78}`,"#fb923c",22);
      spawnParticles(hit.x,hit.y,"#fed7aa",8,"spark");
      if(csPwr78.critCount>=5)unlock("crit_surge_big");
    }}
    // LIFE_PULSE — every 5th tap restores 1 life
    {const lpPwr78=activePwrRef.current.find(p=>p.type==="LIFE_PULSE"&&p.endsAt>Date.now());
    if(lpPwr78){
      lpPwr78.tapCount=(lpPwr78.tapCount||0)+1;
      if(lpPwr78.tapCount%5===0){
        const gsLP=gsRef.current;const cfgLP=levelCfgRef.current;
        const maxLP=cfgLP?.lives||3;
        if(gsLP&&gsLP.lives<maxLP){
          gsLP.lives++;
          lpPwr78.livesGiven=(lpPwr78.livesGiven||0)+1;
          spawnPopup(hit.x,hit.y-28,"💓 +1 LIFE","#fb7185",18);
          if(gs.streak>=10)unlock("life_pulse_full");
        }
      }
    }}
    // ECHO_BURST — echo the tap score after 0.4s
    {const ebPwr82=activePwrRef.current.find(p=>p.type==="ECHO_BURST"&&p.endsAt>Date.now());
    if(ebPwr82){
      const echoPts82=Math.round(30*Math.min(10,1+Math.floor((gs.streak||0)/5))*(gs.feverActive?2:1));
      setTimeout(()=>{
        const gs82=gsRef.current;if(gs82&&activePwrRef.current.find(p=>p.type==="ECHO_BURST"&&p.endsAt>Date.now())){
          gs82.score+=echoPts82;
          const pwr82=activePwrRef.current.find(p=>p.type==="ECHO_BURST");
          if(pwr82){pwr82.totalBonus=(pwr82.totalBonus||0)+echoPts82;if(pwr82.totalBonus>=500)unlock("echo_burst_big");}
          spawnPopup(hit.x+(Math.random()<0.5?25:-25),hit.y-20,`🔊+${echoPts82}`,"#e879f9",12);
        }
      },400);
    }}
    // SCORE_TRAIL — each tap adds 10% of score gained since last tap
    {const stPwr84=activePwrRef.current.find(p=>p.type==="SCORE_TRAIL"&&p.endsAt>Date.now());
    if(stPwr84){
      const scoreDiff84=gs.score-(stPwr84.lastScore||0);
      if(scoreDiff84>0){
        const trailBonus84=Math.max(1,Math.round(scoreDiff84*0.1));
        gs.score+=trailBonus84;
        stPwr84.lastScore=gs.score;
        stPwr84.totalAdded=(stPwr84.totalAdded||0)+trailBonus84;
        spawnPopup(hit.x+20,hit.y-14,`✨+${trailBonus84}`,"#a3e635",12);
        if(stPwr84.totalAdded>=600)unlock("score_trail_big");
      } else {
        stPwr84.lastScore=gs.score;
      }
    }}
    // BURST_FEVER — extra score bonus per tap (on top of fever ×2, makes ×3 effective)
    {const bfPwr84=activePwrRef.current.find(p=>p.type==="BURST_FEVER"&&p.endsAt>Date.now());
    if(bfPwr84){
      const bfBase84=Math.round(25*Math.min(10,1+Math.floor((gs.streak||0)/5)));
      gs.score+=bfBase84;
      bfPwr84.totalScore=(bfPwr84.totalScore||0)+bfBase84;
      spawnPopup(hit.x-20,hit.y-16,`🌡️+${bfBase84}`,"#f43f5e",12);
      if(bfPwr84.totalScore>=2000)unlock("burst_fever_peak");
    }}
    // SCORE_VAULT — bank 15% of each tap's score; releases all at 10s end
    {const svPwr86=activePwrRef.current.find(p=>p.type==="SCORE_VAULT"&&p.endsAt>Date.now());
    if(svPwr86){
      const bank86=Math.max(1,Math.round(pts*0.15));
      svPwr86.banked=(svPwr86.banked||0)+bank86;
      spawnPopup(hit.x+18,hit.y-10,`🏦+${bank86}`,"#fbbf24",11);
    }}
    // STREAK_ECHO — bonus pts = streak×3 per tap
    {const sePwr86=activePwrRef.current.find(p=>p.type==="STREAK_ECHO"&&p.endsAt>Date.now());
    if(sePwr86){
      const seBonus86=Math.max(1,(gs.streak||0)*3)*(gs.feverActive?2:1);
      gs.score+=seBonus86;
      sePwr86.totalBonus=(sePwr86.totalBonus||0)+seBonus86;
      spawnPopup(hit.x-22,hit.y-12,`🔥×${gs.streak||1} +${seBonus86}`,"#f97316",12);
      if(sePwr86.totalBonus>=1000)unlock("streak_echo_peak");
    }}
    // NOVA_PULSE — every 4 taps fire a score nova
    // TIME_SHIELD — extend nearest expiring target by 1s
    // PHANTOM_RUSH — ghost-tap a random target on every tap
    // STREAK_BLAZE — bonus = streak²/10 on every tap
    // FOCUS_BLAST — detonate nearest target within 100px
    {const fbPwr91=activePwrRef.current.find(p=>p.type==="FOCUS_BLAST"&&p.endsAt>Date.now());
    if(fbPwr91){
      const nearest91=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="bomb"&&o.type!=="boss").filter(o=>Math.hypot(o.x-hit.x,o.y-hit.y)<100)[0];
      if(nearest91){
        const blastPts91=Math.round((nearest91.rarity?.mult||1)*50*(gs.feverActive?2:1));
        gs.score+=blastPts91;nearest91.dying=performance.now();
        spawnParticles(nearest91.x,nearest91.y,"#fca5a5",8,"spark");
        spawnPopup(nearest91.x,nearest91.y-16,`🎯 +${blastPts91}`,"#f43f5e",14);
        sfx("comboNote",2);
        fbPwr91.hits=(fbPwr91.hits||0)+1;
        if(fbPwr91.hits>=5)unlock("focus_blast_big");
      }
    }}
    // COIN_CHAIN — earn 1–5 coins per tap (scaling with tap count mod 5)
    {const ccPwr91=activePwrRef.current.find(p=>p.type==="COIN_CHAIN"&&p.endsAt>Date.now());
    if(ccPwr91){
      ccPwr91.tapCounter=(ccPwr91.tapCounter||0)+1;
      const chainCoins91=Math.min(5,1+Math.floor(ccPwr91.tapCounter/5));
      saveRef.current.coins=(saveRef.current.coins||0)+chainCoins91;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+chainCoins91;
      ccPwr91.totalCoins=(ccPwr91.totalCoins||0)+chainCoins91;
      spawnPopup(hit.x+16,hit.y-8,`🪙+${chainCoins91}`,"#fbbf24",11);
      debounceSave();
      if(ccPwr91.totalCoins>=50)unlock("coin_chain_max");
    }}
    // SCORE_ECHO2 — echo 30% of tap pts as bonus after 0.5s
    {const se2Pwr92=activePwrRef.current.find(p=>p.type==="SCORE_ECHO2"&&p.endsAt>Date.now());
    if(se2Pwr92&&(pts||0)>0){
      const echo92=Math.max(1,Math.round((pts||100)*0.3));
      setTimeout(()=>{
        const gs2=gsRef.current;if(!gs2)return;
        gs2.score+=echo92;
        spawnPopup(hit.x+20,hit.y-20,`📊+${echo92}`,"#a78bfa",11);
      },500);
      se2Pwr92.totalBonus=(se2Pwr92.totalBonus||0)+echo92;
      if(se2Pwr92.totalBonus>=500)unlock("score_echo_big");
    }}
    // SCORE_OMEGA — build multiplier per tap
    {const soPwr100=activePwrRef.current.find(p=>p.type==="SCORE_OMEGA"&&p.endsAt>Date.now());
    if(soPwr100&&(pts||0)>0){
      soPwr100.tapCount=(soPwr100.tapCount||0)+1;
      soPwr100.mult=Math.min(10,1+Math.floor(soPwr100.tapCount/3));
      const omegaBonus100=Math.round((pts||0)*(soPwr100.mult-1));
      if(omegaBonus100>0){
        gs.score+=omegaBonus100;
        spawnPopup(hit.x+18,hit.y-16,`Ω×${soPwr100.mult} +${omegaBonus100}`,"#a855f7",11);
      }
      if(soPwr100.mult>=10)unlock("score_omega_max");
    }}
    // ARC_SURGE — chain to nearest target within 120px for +40pts
    {const asPwr101=activePwrRef.current.find(p=>p.type==="ARC_SURGE"&&p.endsAt>Date.now());
    if(asPwr101){
      const nearest101=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="bomb"&&o.type!=="boss").sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y))[0];
      if(nearest101&&Math.hypot(nearest101.x-hit.x,nearest101.y-hit.y)<120){
        const arcPts101=Math.round(40*(gs.feverActive?2:1));
        gs.score+=arcPts101;nearest101.dying=performance.now();
        spawnParticles(nearest101.x,nearest101.y,"#38bdf8",8,"spark");
        spawnPopup(nearest101.x,nearest101.y-16,`⚡+${arcPts101}`,"#38bdf8",14);
        asPwr101.chainCount=(asPwr101.chainCount||0)+1;
        if(asPwr101.chainCount>=8)unlock("arc_surge_chain");
      }
    }}
    // RELIC_PULSE — every 5th tap grants +streak×10 pts
    {const rpPwr101=activePwrRef.current.find(p=>p.type==="RELIC_PULSE"&&p.endsAt>Date.now());
    if(rpPwr101){
      rpPwr101.tapCount=(rpPwr101.tapCount||0)+1;
      if(rpPwr101.tapCount%5===0){
        const relicBonus101=Math.round((gs.streak||1)*10*(gs.feverActive?2:1));
        gs.score+=relicBonus101;
        rpPwr101.totalBonus=(rpPwr101.totalBonus||0)+relicBonus101;
        spawnPopup(hit.x,hit.y-30,`🏺💓 +${relicBonus101}`,"#a78bfa",18);
        spawnParticles(hit.x,hit.y,"#ede9fe",8,"spark");
        sfx("comboNote",3);
        if(rpPwr101.totalBonus>=400)unlock("relic_pulse_max");
      }
    }}
    // NOVA_CASCADE — tap count² pts bonus per tap
    {const ncPwr111=activePwrRef.current.find(p=>p.type==="NOVA_CASCADE"&&p.endsAt>Date.now());
    if(ncPwr111){
      ncPwr111.tapCount=(ncPwr111.tapCount||0)+1;
      const cascadeBonus111=Math.round((ncPwr111.tapCount**2)*(gs.feverActive?1.5:1));
      if(cascadeBonus111>0){
        gs.score+=cascadeBonus111;
        ncPwr111.totalBonus=(ncPwr111.totalBonus||0)+cascadeBonus111;
        if(cascadeBonus111>10)spawnPopup(hit.x+20,hit.y-14,`💥${ncPwr111.tapCount}²+${cascadeBonus111}`,"#f43f5e",11);
        if(ncPwr111.totalBonus>=800)unlock("nova_cascade_max");
      }
    }}
    // SOLAR_PULSE — +15pts per active target on screen per tap
    {const spPwr112=activePwrRef.current.find(p=>p.type==="SOLAR_PULSE"&&p.endsAt>Date.now());
    if(spPwr112){
      const solarTargets112=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb").length;
      const solarBonus112=solarTargets112*15*(gs.feverActive?2:1);
      if(solarBonus112>0){
        gs.score+=solarBonus112;
        spPwr112.totalBonus=(spPwr112.totalBonus||0)+solarBonus112;
        if(solarBonus112>20)spawnPopup(hit.x+18,hit.y-12,`☀️+${solarBonus112}`,"#fbbf24",11);
        if(spPwr112.totalBonus>=600)unlock("solar_pulse_max");
      }
    }}
    // VOID_CHAIN — chain nearest non-hit target for 50% bonus
    {const vcPwr112=activePwrRef.current.find(p=>p.type==="VOID_CHAIN"&&p.endsAt>Date.now());
    if(vcPwr112){
      const chainCandidates112=targetsRef.current.filter(t=>!t.dying&&t.id!==hit.id&&t.type!=="bomb"&&t.type!=="boss");
      if(chainCandidates112.length>0){
        let nearest112=null,nearD112=Infinity;
        chainCandidates112.forEach(t=>{
          const d=Math.hypot(t.x-hit.x,t.y-hit.y);
          if(d<nearD112){nearD112=d;nearest112=t;}
        });
        if(nearest112&&nearD112<200){
          const chainBonus112=Math.round(pts*0.5);
          gs.score+=chainBonus112;
          vcPwr112.chainCount=(vcPwr112.chainCount||0)+1;
          spawnPopup(nearest112.x,nearest112.y-14,`🌑⛓️+${chainBonus112}`,"#7c3aed",12);
          spawnParticles(nearest112.x,nearest112.y,"#ddd6fe",4,"dot");
          if(vcPwr112.chainCount>=10)unlock("void_chain_max");
        }
      }
    }}
    // QUANTUM_ECHO — 40% of previous tap bonus as echo
    {const qePwr116=activePwrRef.current.find(p=>p.type==="QUANTUM_ECHO"&&p.endsAt>Date.now());
    if(qePwr116){
      const echoBonus116=Math.round((qePwr116.lastPts||0)*0.4*(gs.feverActive?1.5:1));
      if(echoBonus116>0){
        gs.score+=echoBonus116;
        qePwr116.echoCount=(qePwr116.echoCount||0)+1;
        spawnPopup(hit.x+14,hit.y-24,`🔮+${echoBonus116}`,"#818cf8",10);
        if(qePwr116.echoCount>=8)unlock("quantum_echo_max");
      }
      qePwr116.lastPts=pts;
    }}
    // FROST_CROWN — score += streak² per tap
    {const fcPwr116=activePwrRef.current.find(p=>p.type==="FROST_CROWN"&&p.endsAt>Date.now());
    if(fcPwr116){
      const crownBonus116=Math.round((gs.streak**2)*(gs.feverActive?1.5:1));
      if(crownBonus116>0){
        gs.score+=crownBonus116;
        fcPwr116.totalBonus=(fcPwr116.totalBonus||0)+crownBonus116;
        if(crownBonus116>20)spawnPopup(hit.x-14,hit.y-22,`❄️👑+${crownBonus116}`,"#7dd3fc",10);
        if(fcPwr116.totalBonus>=1000)unlock("frost_crown_max");
      }
    }}
    // NEXUS_WAVE — bonus = pts × active target count / 5
    {const nwPwr115=activePwrRef.current.find(p=>p.type==="NEXUS_WAVE"&&p.endsAt>Date.now());
    if(nwPwr115){
      const nwTargets115=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb").length;
      const nwBonus115=Math.round(pts*nwTargets115/5*(gs.feverActive?1.5:1));
      if(nwBonus115>0){
        gs.score+=nwBonus115;
        nwPwr115.totalBonus=(nwPwr115.totalBonus||0)+nwBonus115;
        if(nwBonus115>15)spawnPopup(hit.x-16,hit.y-28,`🌐×${nwTargets115}+${nwBonus115}`,"#0ea5e9",11);
        if(nwPwr115.totalBonus>=900)unlock("nexus_wave_max");
      }
    }}
    // PRISM_ECHO — every 3rd tap echoes last score ×1.5
    {const pePwr114=activePwrRef.current.find(p=>p.type==="PRISM_ECHO"&&p.endsAt>Date.now());
    if(pePwr114){
      pePwr114.tapCount=(pePwr114.tapCount||0)+1;
      pePwr114.lastPts=(pePwr114.lastPts||0)+pts;
      if(pePwr114.tapCount%3===0){
        const echoBonus114=Math.round(pePwr114.lastPts*0.5*(gs.feverActive?1.5:1));
        if(echoBonus114>0){
          gs.score+=echoBonus114;
          pePwr114.echoCount=(pePwr114.echoCount||0)+1;
          spawnPopup(hit.x-18,hit.y-28,`🌈🔊+${echoBonus114}`,"#ec4899",13);
          if(pePwr114.echoCount>=6)unlock("prism_echo_max");
        }
        pePwr114.lastPts=0;
      }
    }}
    // VOLT_SURGE — streak×10 flat bonus per tap
    {const vsPwr114=activePwrRef.current.find(p=>p.type==="VOLT_SURGE"&&p.endsAt>Date.now());
    if(vsPwr114){
      const voltBonus114=Math.round(gs.streak*10*(gs.feverActive?2:1));
      if(voltBonus114>0){
        gs.score+=voltBonus114;
        vsPwr114.totalBonus=(vsPwr114.totalBonus||0)+voltBonus114;
        if(voltBonus114>30)spawnPopup(hit.x+16,hit.y-16,`⚡+${voltBonus114}`,"#f59e0b",10);
        if(vsPwr114.totalBonus>=800)unlock("volt_surge_max");
      }
    }}
    // EMBER_WAVE — every 4th tap fires a wave for ×streak bonus
    {const ewPwr113=activePwrRef.current.find(p=>p.type==="EMBER_WAVE"&&p.endsAt>Date.now());
    if(ewPwr113){
      ewPwr113.tapCount=(ewPwr113.tapCount||0)+1;
      if(ewPwr113.tapCount%4===0){
        const waveBonus113=Math.round(gs.streak*20*(gs.feverActive?1.5:1));
        if(waveBonus113>0){
          gs.score+=waveBonus113;
          ewPwr113.totalBonus=(ewPwr113.totalBonus||0)+waveBonus113;
          spawnPopup(hit.x,hit.y-30,`🔥〰️ WAVE +${waveBonus113}`,"#f97316",20);
          spawnParticles(hit.x,hit.y,"#fed7aa",8,"spark");
          if(ewPwr113.totalBonus>=700)unlock("ember_wave_max");
        }
      }
    }}
    // SCORE_QUAKE — every 5th tap shakes pts from all targets
    {const sqPwr110=activePwrRef.current.find(p=>p.type==="SCORE_QUAKE"&&p.endsAt>Date.now());
    if(sqPwr110){
      sqPwr110.tapCount=(sqPwr110.tapCount||0)+1;
      if(sqPwr110.tapCount%5===0){
        const quakeTargets=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb"&&t.type!=="boss");
        const quakeBonus110=Math.round(quakeTargets.length*25*(gs.feverActive?2:1));
        if(quakeBonus110>0){
          gs.score+=quakeBonus110;
          sqPwr110.totalBonus=(sqPwr110.totalBonus||0)+quakeBonus110;
          setScreenShake(true);setTimeout(()=>setScreenShake(false),400);
          spawnPopup(hit.x,hit.y-32,`🌍💥 +${quakeBonus110}`,"#78716c",18);
          if(sqPwr110.totalBonus>=700)unlock("score_quake_max");
        }
      }
    }}
    // SHIMMER_COIN — rare/epic/legendary targets drop bonus coins
    {const smPwr109=activePwrRef.current.find(p=>p.type==="SHIMMER_COIN"&&p.endsAt>Date.now());
    if(smPwr109&&hit.rarity){
      const rarityName=(hit.rarity.name||"").toUpperCase();
      const shimCoins109=rarityName==="LEGENDARY"?20:rarityName==="EPIC"?10:rarityName==="RARE"?5:0;
      if(shimCoins109>0){
        saveRef.current.coins=(saveRef.current.coins||0)+shimCoins109;
        saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+shimCoins109;
        smPwr109.totalCoins=(smPwr109.totalCoins||0)+shimCoins109;
        spawnPopup(hit.x+14,hit.y-10,`✨+${shimCoins109}🪙`,"#fbbf24",12);
        debounceSave();
        if(smPwr109.totalCoins>=100)unlock("shimmer_coin_max");
      }
    }}
    // FROST_LANCE — pierce up to 2 nearby targets for +50pts each
    {const flPwr108=activePwrRef.current.find(p=>p.type==="FROST_LANCE"&&p.endsAt>Date.now());
    if(flPwr108){
      const pierceTargets108=targetsRef.current.filter(t=>t.id!==hit.id&&!t.dying&&t.type!=="bomb"&&t.type!=="boss").sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y)).slice(0,2);
      if(pierceTargets108.length>0){
        const lancePts108=Math.round(50*pierceTargets108.length*(gs.feverActive?2:1));
        gs.score+=lancePts108;
        pierceTargets108.forEach(t=>{t.dying=performance.now();spawnParticles(t.x,t.y,"#bfdbfe",6,"spark");});
        spawnPopup(hit.x,hit.y-24,`❄️🗡️ +${lancePts108}`,"#93c5fd",14);
        flPwr108.pierced=(flPwr108.pierced||0)+pierceTargets108.length;
        if(flPwr108.pierced>=6)unlock("frost_lance_max");
      }
    }}
    // LIFE_MATRIX — restore life when low, every 5s
    {const lmPwr108=activePwrRef.current.find(p=>p.type==="LIFE_MATRIX"&&p.endsAt>Date.now());
    if(lmPwr108&&(Date.now()-lmPwr108.lastCheck)>=5000){
      lmPwr108.lastCheck=Date.now();
      if(gs.lives<3){
        gs.lives=Math.min(5,gs.lives+1);
        lmPwr108.totalRestored=(lmPwr108.totalRestored||0)+1;
        spawnPopup(hit.x,hit.y-30,`❤️🔲 MATRIX +1`,"#f43f5e",16);
        spawnParticles(hit.x,hit.y,"#fecaca",6,"spark");
        sfx("comboNote",2);vibrate([5,3,8]);
        if(lmPwr108.totalRestored>=4)unlock("life_matrix_max");
      }
    }}
    // MAGMA_WAVE — AoE wave every 4s
    {const mwPwr107=activePwrRef.current.find(p=>p.type==="MAGMA_WAVE"&&p.endsAt>Date.now());
    if(mwPwr107&&(Date.now()-mwPwr107.lastWave)>=4000){
      mwPwr107.lastWave=Date.now();
      const magmaTargets=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb"&&t.type!=="boss");
      const magmaBonus107=Math.round(magmaTargets.length*30*(gs.feverActive?2:1));
      magmaTargets.forEach(t=>{spawnParticles(t.x,t.y,"#fca5a5",3,"spark");});
      gs.score+=magmaBonus107;
      mwPwr107.totalBonus=(mwPwr107.totalBonus||0)+magmaBonus107;
      if(magmaBonus107>0){
        particlesRef.current.push({type:"shockwave",x:canvasRef.current?.width/2||200,y:canvasRef.current?.height/2||350,color:"#dc2626",size:260,born:performance.now(),duration:700,alpha:0.4});
        spawnPopup(hit.x,hit.y-28,`🌋+${magmaBonus107}`,"#dc2626",16);
      }
      if(mwPwr107.totalBonus>=400)unlock("magma_wave_max");
    }}
    // COSMIC_ECHO — echo pts from 0.6s ago
    {const cePwr107=activePwrRef.current.find(p=>p.type==="COSMIC_ECHO"&&p.endsAt>Date.now());
    if(cePwr107&&(pts||0)>0){
      cePwr107.recentPts=cePwr107.recentPts||[];
      // Check if we have a pts from ~0.6s ago to echo
      const echoEntry=cePwr107.recentPts.find(e=>Date.now()-e.t>=500&&Date.now()-e.t<800);
      if(echoEntry){
        const echoBonus107=Math.round(echoEntry.pts*(gs.feverActive?1.5:1));
        gs.score+=echoBonus107;
        cePwr107.totalBonus=(cePwr107.totalBonus||0)+echoBonus107;
        spawnPopup(hit.x-18,hit.y-18,`🌌+${echoBonus107}`,"#818cf8",11);
        if(cePwr107.totalBonus>=500)unlock("cosmic_echo_max");
      }
      cePwr107.recentPts.push({pts:pts||0,t:Date.now()});
      if(cePwr107.recentPts.length>20)cePwr107.recentPts=cePwr107.recentPts.slice(-20);
    }}
    // INFERNO_CHAIN — quick consecutive taps double inferno bonus (max ×8)
    {const icPwr106=activePwrRef.current.find(p=>p.type==="INFERNO_CHAIN"&&p.endsAt>Date.now());
    if(icPwr106&&(pts||0)>0){
      const now106=Date.now();
      if(now106-icPwr106.lastTap<800){
        icPwr106.mult=Math.min(8,(icPwr106.mult||1)*2);
      } else {
        icPwr106.mult=1;
      }
      icPwr106.lastTap=now106;
      const infernoBonus106=Math.round((pts||0)*(icPwr106.mult-1)*(gs.feverActive?1.5:1));
      if(infernoBonus106>0){
        gs.score+=infernoBonus106;
        spawnPopup(hit.x+20,hit.y-12,`🔥×${icPwr106.mult} +${infernoBonus106}`,"#ef4444",11);
      }
      if(icPwr106.mult>=8)unlock("inferno_chain_max");
    }}
    // SPECTRAL_COIN — check if tapping near a spectral coin
    {const spPwr106=activePwrRef.current.find(p=>p.type==="SPECTRAL_COIN"&&p.endsAt>Date.now());
    if(spPwr106&&spPwr106.pool){
      spPwr106.pool.forEach(sc=>{
        if(!sc.collected&&Math.hypot(hit.x-sc.x,hit.y-sc.y)<35){
          sc.collected=true;
          saveRef.current.coins=(saveRef.current.coins||0)+5;
          saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+5;
          spPwr106.totalCoins=(spPwr106.totalCoins||0)+5;
          spawnPopup(sc.x,sc.y-16,`👻+5🪙`,"#c084fc",13);
          sfx("coin");debounceSave();
          if(spPwr106.totalCoins>=80)unlock("spectral_coin_max");
        }
      });
    }}
    // SCORE_AVALANCHE — grows multiplier per tap
    {const saPwr105=activePwrRef.current.find(p=>p.type==="SCORE_AVALANCHE"&&p.endsAt>Date.now());
    if(saPwr105&&(pts||0)>0){
      saPwr105.mult=Math.min(12,((saPwr105.mult||1)+0.5));
      const avalancheBonus105=Math.round((pts||0)*(saPwr105.mult-1)*(gs.feverActive?1.5:1));
      gs.score+=avalancheBonus105;
      saPwr105.totalBonus=(saPwr105.totalBonus||0)+avalancheBonus105;
      if(avalancheBonus105>0)spawnPopup(hit.x+22,hit.y-12,`🏔️×${saPwr105.mult.toFixed(1)} +${avalancheBonus105}`,"#94a3b8",11);
      if(saPwr105.totalBonus>=600)unlock("score_avalanche_max");
    }}
    // COIN_VORTEX — earn coins based on proximity to canvas center
    {const cvPwr104=activePwrRef.current.find(p=>p.type==="COIN_VORTEX"&&p.endsAt>Date.now());
    if(cvPwr104){
      const cw104=canvasRef.current?.width||360;const ch104=canvasRef.current?.height||700;
      const dist104=Math.hypot(hit.x-cw104/2,hit.y-ch104/2);
      const maxDist104=Math.hypot(cw104/2,ch104/2);
      const prox104=Math.max(0,1-dist104/maxDist104);
      const vCoins104=Math.round(2+prox104*3);
      saveRef.current.coins=(saveRef.current.coins||0)+vCoins104;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+vCoins104;
      cvPwr104.totalCoins=(cvPwr104.totalCoins||0)+vCoins104;
      spawnPopup(hit.x+14,hit.y-10,`🌀+${vCoins104}🪙`,"#fbbf24",11);
      debounceSave();
      if(cvPwr104.totalCoins>=60)unlock("coin_vortex_max");
    }}
    // STAR_CHAIN — consecutive taps within 1.5s each stack +20pts
    {const scPwr103=activePwrRef.current.find(p=>p.type==="STAR_CHAIN"&&p.endsAt>Date.now());
    if(scPwr103){
      const now103=Date.now();
      if(now103-scPwr103.lastTap<1500){
        scPwr103.chain=(scPwr103.chain||0)+1;
      } else {
        scPwr103.chain=1;
      }
      scPwr103.lastTap=now103;
      const chainBonus103=Math.round(scPwr103.chain*20*(gs.feverActive?2:1));
      gs.score+=chainBonus103;
      scPwr103.totalLinks=(scPwr103.totalLinks||0)+1;
      spawnPopup(hit.x+20,hit.y-12,`⭐×${scPwr103.chain} +${chainBonus103}`,"#fbbf24",11);
      if(scPwr103.totalLinks>=6)unlock("star_chain_max");
    }}
    // VOID_BLOOM — AoE +60pts when 3+ targets cluster within 80px
    {const vbPwr103=activePwrRef.current.find(p=>p.type==="VOID_BLOOM"&&p.endsAt>Date.now());
    if(vbPwr103){
      const cluster103=targetsRef.current.filter(t=>t.id!==hit.id&&!t.dying&&t.type!=="bomb"&&Math.hypot(t.x-hit.x,t.y-hit.y)<80);
      if(cluster103.length>=2){
        const vbBonus103=Math.round(60*(gs.feverActive?2:1));
        gs.score+=vbBonus103;
        vbPwr103.totalBonus=(vbPwr103.totalBonus||0)+vbBonus103;
        spawnPopup(hit.x,hit.y-30,`🌑 AoE +${vbBonus103}`,"#a855f7",16);
        spawnParticles(hit.x,hit.y,"#ede9fe",8,"spark");
        if(vbPwr103.totalBonus>=500)unlock("void_bloom_max");
      }
    }}
    // LUNAR_BLOOM — every 3rd tap spawns 2 ghost bonus targets
    {const lbPwr102=activePwrRef.current.find(p=>p.type==="LUNAR_BLOOM"&&p.endsAt>Date.now());
    if(lbPwr102){
      lbPwr102.tapCount=(lbPwr102.tapCount||0)+1;
      if(lbPwr102.tapCount%3===0){
        const cw=canvasRef.current?.width||360;const ch=canvasRef.current?.height||700;
        for(let gi=0;gi<2;gi++){
          const gx=50+Math.random()*(cw-100);const gy=80+Math.random()*(ch-200);
          targetsRef.current.push({id:"lb102_"+Date.now()+gi,type:"normal",x:gx,y:gy,radius:22,color:"#c084fc",glow:"#f3e8ff",rarity:{name:"RARE",color:"#c084fc",glow:"#f3e8ff",mult:1,size:1},lifetime:2000,spawnedAt:Date.now(),born:performance.now(),moving:false,ghost:true,vx:0,vy:0,trail:[],hitsLeft:1,maxHits:1,dying:null,_lunarGhost:true,pts:30});
          lbPwr102.ghostsSpawned=(lbPwr102.ghostsSpawned||0)+1;
        }
        spawnPopup(hit.x,hit.y-28,`🌙🌸 BLOOM!`,"#c084fc",14);
        if(lbPwr102.ghostsSpawned>=10)unlock("lunar_bloom_max");
      }
    }}
    // FEVER_BLOOM — re-bloom fever when it expires if not used yet
    {const fbPwr99=activePwrRef.current.find(p=>p.type==="FEVER_BLOOM"&&p.endsAt>Date.now()&&!p.rebloomedOnce);
    if(fbPwr99&&!gs.feverActive){
      fbPwr99.rebloomedOnce=true;
      gs.feverActive=true;gs.feverTimeLeft=4000;
      gs.streak=Math.max(gs.streak,FEVER_STREAK);
      setFeverBorder(true);sfx("feverStart");vibrate([8,4,12]);
      spawnPopup(hit.x,hit.y-40,`🌸 RE-BLOOM FEVER!`,"#f43f5e",20);
      gs.sessionStats.feverCount=(gs.sessionStats.feverCount||0)+1;
      unlock("fever_bloom_peak");
    }}
    // COIN_CASCADE — cascading coin multiplier per tap
    {const ccasPwr99=activePwrRef.current.find(p=>p.type==="COIN_CASCADE"&&p.endsAt>Date.now());
    if(ccasPwr99){
      const cascCoins99=ccasPwr99.multiplier;
      saveRef.current.coins=(saveRef.current.coins||0)+cascCoins99;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+cascCoins99;
      ccasPwr99.totalCoins=(ccasPwr99.totalCoins||0)+cascCoins99;
      ccasPwr99.multiplier=Math.min(10,ccasPwr99.multiplier+1);
      spawnPopup(hit.x+14,hit.y-10,`🪙×${cascCoins99}`,"#fbbf24",10);
      debounceSave();
      if(ccasPwr99.totalCoins>=60)unlock("coin_cascade_max");
    }}
    // BLAZE_TRAIL — drop flame at tap pos; check targets near recent flames
    {const btPwr98=activePwrRef.current.find(p=>p.type==="BLAZE_TRAIL"&&p.endsAt>Date.now());
    if(btPwr98){
      btPwr98.flames=(btPwr98.flames||[]);
      btPwr98.flames.push({x:hit.x,y:hit.y,expiresAt:Date.now()+1000});
      btPwr98.flames=btPwr98.flames.filter(f=>f.expiresAt>Date.now());
      // Check if any non-hit target is inside a flame
      const nearFlame=targetsRef.current.filter(t=>t.id!==hit.id&&!t.dying&&t.type!=="bomb"&&t.type!=="boss").filter(t=>btPwr98.flames.some(f=>Math.hypot(t.x-f.x,t.y-f.y)<50));
      if(nearFlame.length>0){
        const blazeBonus98=nearFlame.length*15*(gs.feverActive?2:1);
        gs.score+=blazeBonus98;
        nearFlame.forEach(t=>spawnParticles(t.x,t.y,"#fed7aa",4,"spark"));
        spawnPopup(hit.x,hit.y-20,`🔥+${blazeBonus98}`,"#f97316",12);
        btPwr98.totalBonus=(btPwr98.totalBonus||0)+blazeBonus98;
        if(btPwr98.totalBonus>=300)unlock("blaze_trail_long");
      }
    }}
    // COIN_PULSE — grant coins every 3s
    {const cpPwr98=activePwrRef.current.find(p=>p.type==="COIN_PULSE"&&p.endsAt>Date.now());
    if(cpPwr98&&(Date.now()-cpPwr98.lastPulse)>=3000){
      cpPwr98.lastPulse=Date.now();
      const pulseCoins98=3+Math.floor(Math.random()*4);
      saveRef.current.coins=(saveRef.current.coins||0)+pulseCoins98;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+pulseCoins98;
      cpPwr98.totalCoins=(cpPwr98.totalCoins||0)+pulseCoins98;
      spawnPopup(hit.x,hit.y-36,`💓+${pulseCoins98}🪙`,"#fbbf24",16);
      spawnParticles(hit.x,hit.y,"#fef3c7",pulseCoins98,"dot");
      sfx("coin");debounceSave();
      if(cpPwr98.totalCoins>=40)unlock("coin_pulse_rich");
    }}
    // SCORE_FRENZY — double all score while active
    {const sfPwr97=activePwrRef.current.find(p=>p.type==="SCORE_FRENZY"&&p.endsAt>Date.now());
    if(sfPwr97&&(pts||0)>0){
      const frenzyBonus97=Math.max(1,Math.round((pts||0)*1));
      gs.score+=frenzyBonus97;
      sfPwr97.totalBonus=(sfPwr97.totalBonus||0)+frenzyBonus97;
      spawnPopup(hit.x+16,hit.y-8,`📊×2 +${frenzyBonus97}`,"#ef4444",11);
      if(sfPwr97.totalBonus>=500)unlock("score_frenzy_big");
    }}
    // LIFE_BEACON — check if lives at 1 and trigger hasn't fired yet
    {const lbPwr97=activePwrRef.current.find(p=>p.type==="LIFE_BEACON"&&!p.triggered);
    if(lbPwr97&&gs.lives<=1){
      lbPwr97.triggered=true;
      gs.lives=Math.min(5,gs.lives+3);
      spawnPopup(hit.x,hit.y-40,`❤️🔦 BEACON! +3 LIVES`,"#f43f5e",24);
      spawnParticles(hit.x,hit.y,"#fecaca",12,"spark");
      sfx("comboNote",4);vibrate([8,4,8,4,8]);
      setActivePwrDisp([...activePwrRef.current]);
      unlock("life_beacon_save");
    }}
    // GOLD_FRENZY — earn 2× rarity mult coins per tap
    {const gfPwr96=activePwrRef.current.find(p=>p.type==="GOLD_FRENZY"&&p.endsAt>Date.now());
    if(gfPwr96){
      const goldCoins96=Math.max(2,Math.round((hit.rarity?.mult||1)*2));
      saveRef.current.coins=(saveRef.current.coins||0)+goldCoins96;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+goldCoins96;
      gfPwr96.totalCoins=(gfPwr96.totalCoins||0)+goldCoins96;
      spawnPopup(hit.x+14,hit.y-10,`🥇+${goldCoins96}`,"#fbbf24",12);
      debounceSave();
      if(gfPwr96.totalCoins>=50)unlock("gold_frenzy_rich");
    }}
    // PHANTOM_COIN — check if tap is near a phantom coin position
    {const pcPwr96=activePwrRef.current.find(p=>p.type==="PHANTOM_COIN"&&p.endsAt>Date.now());
    if(pcPwr96&&pcPwr96.coins){
      pcPwr96.coins.forEach(pc=>{
        if(!pc.collected&&Math.hypot(hit.x-pc.x,hit.y-pc.y)<40){
          pc.collected=true;
          saveRef.current.coins=(saveRef.current.coins||0)+20;
          saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+20;
          spawnPopup(pc.x,pc.y-20,`👻🪙 +20`,"#a78bfa",16);
          spawnParticles(pc.x,pc.y,"#ede9fe",8,"spark");
          sfx("coin");
          pcPwr96.collected=(pcPwr96.collected||0)+1;
          debounceSave();
          if(pcPwr96.collected>=5)unlock("phantom_coin_haul");
        }
      });
    }}
    // STREAK_NOVA — fire nova when streak reaches 20+
    {const snPwr95=activePwrRef.current.find(p=>p.type==="STREAK_NOVA"&&p.endsAt>Date.now());
    if(snPwr95){
      snPwr95.bestStreak=Math.max(snPwr95.bestStreak||0,gs.streak);
      if(gs.streak>=20&&!snPwr95.novaFired){
        snPwr95.novaFired=true;
        const novaHits95=targetsRef.current.filter(t=>!t.dying&&t.type!=="bomb"&&t.type!=="boss");
        const novaPts95=novaHits95.length*30*(gs.feverActive?2:1);
        novaHits95.forEach(t=>{t.dying=performance.now();spawnParticles(t.x,t.y,"#fef3c7",6,"spark");});
        gs.score+=novaPts95;
        if(novaPts95>0){spawnPopup(hit.x,hit.y-50,`⛓️💥 NOVA! +${novaPts95}`,"#f59e0b",28);particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#f59e0b",size:200,born:performance.now(),duration:800,alpha:0.6});}
        sfx("legendary");vibrate([12,6,20,6,12]);
        unlock("streak_nova_peak");
        // Allow re-fire after 5s cooldown
        setTimeout(()=>{const snRef=activePwrRef.current.find(p=>p.type==="STREAK_NOVA");if(snRef)snRef.novaFired=false;},5000);
      }
    }}
    // TEMPO_ECHO — every 2nd tap echoes the previous tap position for bonus pts
    {const tePwr94=activePwrRef.current.find(p=>p.type==="TEMPO_ECHO"&&p.endsAt>Date.now());
    if(tePwr94){
      tePwr94.tapCount=(tePwr94.tapCount||0)+1;
      if(tePwr94.tapCount%2===0&&tePwr94.lastPos){
        const echoPts94=Math.round(60*(gs.feverActive?2:1));
        gs.score+=echoPts94;
        spawnParticles(tePwr94.lastPos.x,tePwr94.lastPos.y,"#c4b5fd",6,"spark");
        spawnPopup(tePwr94.lastPos.x,tePwr94.lastPos.y-14,`🎵+${echoPts94}`,"#a78bfa",12);
        tePwr94.beatCount=(tePwr94.beatCount||0)+1;
        sfx("comboNote",2);
        if(tePwr94.beatCount>=10)unlock("tempo_echo_beat");
      }
      tePwr94.lastPos={x:hit.x,y:hit.y};
    }}
    // COIN_BLOOM — every 4th tap blooms 4–8 coins
    {const cbPwr94=activePwrRef.current.find(p=>p.type==="COIN_BLOOM"&&p.endsAt>Date.now());
    if(cbPwr94){
      cbPwr94.tapCount=(cbPwr94.tapCount||0)+1;
      if(cbPwr94.tapCount%4===0){
        const bloomCoins94=4+Math.floor(Math.random()*5);
        saveRef.current.coins=(saveRef.current.coins||0)+bloomCoins94;
        saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+bloomCoins94;
        cbPwr94.totalCoins=(cbPwr94.totalCoins||0)+bloomCoins94;
        spawnPopup(hit.x,hit.y-32,`🌸+${bloomCoins94}🪙`,"#fbbf24",16);
        spawnParticles(hit.x,hit.y,"#fef3c7",bloomCoins94,"dot");
        sfx("coin");debounceSave();
        if(cbPwr94.totalCoins>=30)unlock("coin_bloom_full");
      }
    }}
    // COMBO_SURGE2 — every 3rd tap grants extra streak-based pts
    {const cs2Pwr93=activePwrRef.current.find(p=>p.type==="COMBO_SURGE2"&&p.endsAt>Date.now());
    if(cs2Pwr93){
      cs2Pwr93.tapCycle=(cs2Pwr93.tapCycle||0)+1;
      if(cs2Pwr93.tapCycle%3===0){
        cs2Pwr93.extraMult=Math.min(15,(cs2Pwr93.extraMult||1)+1);
        const surgePts93=Math.round(gs.streak*cs2Pwr93.extraMult*(gs.feverActive?2:1));
        gs.score+=surgePts93;
        spawnPopup(hit.x+20,hit.y-20,`🔥×${cs2Pwr93.extraMult} +${surgePts93}`,"#f97316",13);
        if(cs2Pwr93.extraMult>=15)unlock("combo_surge2_peak");
      }
    }}
    // LIFE_SPIRAL — every 5 taps restore 1 life (max 5)
    {const lsPwr93=activePwrRef.current.find(p=>p.type==="LIFE_SPIRAL"&&p.endsAt>Date.now());
    if(lsPwr93){
      lsPwr93.tapCount=(lsPwr93.tapCount||0)+1;
      if(lsPwr93.tapCount%5===0&&gs.lives<5){
        gs.lives=Math.min(5,gs.lives+1);
        lsPwr93.livesGained=(lsPwr93.livesGained||0)+1;
        spawnPopup(hit.x,hit.y-32,`❤️🌀 +1 LIFE`,"#f43f5e",18);
        spawnParticles(hit.x,hit.y,"#fecaca",8,"spark");
        sfx("comboNote",3);vibrate([8,4,8]);
        if(lsPwr93.livesGained>=3)unlock("life_spiral_max");
      }
    }}
    {const sbzPwr90=activePwrRef.current.find(p=>p.type==="STREAK_BLAZE"&&p.endsAt>Date.now());
    if(sbzPwr90){
      const streak90=gs.streak||0;
      const blazeBonus90=Math.max(1,Math.round(streak90*streak90/10))*(gs.feverActive?2:1);
      gs.score+=blazeBonus90;
      sbzPwr90.totalBonus=(sbzPwr90.totalBonus||0)+blazeBonus90;
      sbzPwr90.peakStreak=Math.max(sbzPwr90.peakStreak||0,streak90);
      spawnPopup(hit.x+18,hit.y-14,`🔥${streak90}²+${blazeBonus90}`,"#f97316",12);
      if(sbzPwr90.peakStreak>=30)unlock("streak_blaze_max");
    }}
    // LIFE_BLOOM — no per-tap; bloom handled at activation timeout
    {const lbPwr90=activePwrRef.current.find(p=>p.type==="LIFE_BLOOM"&&p.endsAt>Date.now());
    if(lbPwr90&&lbPwr90.bloomed){unlock("life_bloom_max");}}
    {const prPwr89=activePwrRef.current.find(p=>p.type==="PHANTOM_RUSH"&&p.endsAt>Date.now());
    if(prPwr89){
      const phTargets89=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="bomb"&&o.type!=="boss");
      if(phTargets89.length>0){
        const randPh89=phTargets89[Math.floor(Math.random()*phTargets89.length)];
        const phPts89=Math.round(20*(gs.feverActive?2:1));
        gs.score+=phPts89;
        prPwr89.totalBonus=(prPwr89.totalBonus||0)+phPts89;
        spawnPopup(randPh89.x,randPh89.y-14,`👻+${phPts89}`,"#818cf8",12);
        spawnParticles(randPh89.x,randPh89.y,"#c7d2fe",4,"dot");
        if(prPwr89.totalBonus>=1800)unlock("phantom_rush_big");
      }
    }}
    {const tsPwr88=activePwrRef.current.find(p=>p.type==="TIME_SHIELD"&&p.endsAt>Date.now());
    if(tsPwr88){
      const now88ts=Date.now();
      const expiring88=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="bomb"&&o.type!=="boss").sort((a,b)=>{
        const remA=(a.spawnedAt+(a.lifetime||2000))-now88ts;
        const remB=(b.spawnedAt+(b.lifetime||2000))-now88ts;
        return remA-remB;
      })[0];
      if(expiring88&&((expiring88.spawnedAt+(expiring88.lifetime||2000))-now88ts)<2000){
        expiring88.lifetime=(expiring88.lifetime||2000)+1000;
        tsPwr88.saves=(tsPwr88.saves||0)+1;
        spawnPopup(expiring88.x,expiring88.y-18,"⏱️ +1s","#60a5fa",13);
        if(tsPwr88.saves>=1)unlock("time_shield_save");
      }
    }}
    // SCORE_BLITZ — double score bonus on every tap
    {const sbPwr88=activePwrRef.current.find(p=>p.type==="SCORE_BLITZ"&&p.endsAt>Date.now());
    if(sbPwr88){
      const blitzBonus88=Math.round(pts*(gs.feverActive?1.5:1));
      gs.score+=blitzBonus88;
      sbPwr88.totalBonus=(sbPwr88.totalBonus||0)+blitzBonus88;
      // Extra streak increment for ×2 combo speed
      if((gs.streak||0)<50)gs.streak++;
      spawnPopup(hit.x+20,hit.y-18,`⚡×2 +${blitzBonus88}`,"#f59e0b",13);
      if(sbPwr88.totalBonus>=2000)unlock("score_blitz_peak");
    }}
    {const npPwr87=activePwrRef.current.find(p=>p.type==="NOVA_PULSE"&&p.endsAt>Date.now());
    if(npPwr87){
      npPwr87.tapCounter=(npPwr87.tapCounter||0)+1;
      if(npPwr87.tapCounter%4===0){
        const novaBonus87=Math.round((gs.streak||1)*10*(gs.feverActive?2:1));
        gs.score+=novaBonus87;
        npPwr87.totalBonus=(npPwr87.totalBonus||0)+novaBonus87;
        spawnPopup(hit.x,hit.y-32,`💥 NOVA +${novaBonus87}`,"#f43f5e",18);
        spawnParticles(hit.x,hit.y,"#fecdd3",10,"spark");
        sfx("comboNote",3);
        if(npPwr87.totalBonus>=1500)unlock("nova_pulse_big");
      }
    }}
    // COIN_ECHO — double all coin earnings while active (handled at coin award sites via this per-tap check)
    {const cePwr87=activePwrRef.current.find(p=>p.type==="COIN_ECHO"&&p.endsAt>Date.now());
    if(cePwr87){
      // Earn 1 echo coin per tap as passive
      const echoCoin87=1;
      saveRef.current.coins=(saveRef.current.coins||0)+echoCoin87;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+echoCoin87;
      cePwr87.totalCoins=(cePwr87.totalCoins||0)+echoCoin87;
      spawnPopup(hit.x+16,hit.y-8,`🪙+${echoCoin87}`,"#fbbf24",11);
      debounceSave();
      if(cePwr87.totalCoins>=60)unlock("coin_echo_rich");
    }}
    // CHAIN_SNAP — auto-score nearest nearly-expired normal target
    {const csPwr85=activePwrRef.current.find(p=>p.type==="CHAIN_SNAP"&&p.endsAt>Date.now());
    if(csPwr85){
      const now85cs=Date.now();
      const snapTarget85=targetsRef.current.find(o=>o.id!==hit.id&&!o.dying&&(o.type==="normal"||o.type==="treasure")&&((now85cs-o.spawnedAt)/(o.lifetime||2000))>0.80);
      if(snapTarget85){
        const snapPts85=Math.round((snapTarget85.rarity?.mult||1)*40*(gs.feverActive?2:1));
        gs.score+=snapPts85;snapTarget85.dying=performance.now();
        csPwr85.snapCount=(csPwr85.snapCount||0)+1;
        spawnParticles(snapTarget85.x,snapTarget85.y,"#7dd3fc",6,"spark");
        spawnPopup(snapTarget85.x,snapTarget85.y-16,`🔗 SNAP +${snapPts85}`,"#38bdf8",13);
        if(csPwr85.snapCount>=3)unlock("chain_snap_proc");
      }
    }}
    // LIFE_ECHO — every 5 taps restore +1 life (up to max)
    {const lePwr85=activePwrRef.current.find(p=>p.type==="LIFE_ECHO"&&p.endsAt>Date.now());
    if(lePwr85){
      lePwr85.tapCounter=(lePwr85.tapCounter||0)+1;
      if(lePwr85.tapCounter%5===0){
        const gsLE85=gsRef.current;const cfgLE85=levelCfgRef.current;
        const maxLivesLE85=cfgLE85?.lives||3;
        if(gsLE85&&gsLE85.lives<maxLivesLE85){
          gsLE85.lives++;
          spawnPopup(hit.x,hit.y-30,"❤️ ECHO +1","#fb7185",18);
          lePwr85.triggered=(lePwr85.triggered||0)+1;
          if(lePwr85.triggered>=1)unlock("life_echo_trigger");
        }
      }
    }}
    // COIN_SURGE — each tap earns 1–3 bonus coins
    {const coinSPwr80=activePwrRef.current.find(p=>p.type==="COIN_SURGE"&&p.endsAt>Date.now());
    if(coinSPwr80){
      const coinEarned80=1+Math.floor(Math.random()*3);
      saveRef.current.coins=(saveRef.current.coins||0)+coinEarned80;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+coinEarned80;
      coinSPwr80.totalCoins=(coinSPwr80.totalCoins||0)+coinEarned80;
      spawnPopup(hit.x+18,hit.y-8,`🪙+${coinEarned80}`,"#fbbf24",12);
      debounceSave();
      if(coinSPwr80.totalCoins>=50)unlock("coin_surge_rich");
    }}
    // CHAIN_WAVE — auto-score nearest valid target on each tap
    {const cwPwr79=activePwrRef.current.find(p=>p.type==="CHAIN_WAVE"&&p.endsAt>Date.now());
    if(cwPwr79){
      const nearest79=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="bomb"&&o.type!=="boss").sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y))[0];
      if(nearest79){
        const chainPts79=Math.round(30*Math.min(10,1+Math.floor((gs.streak||0)/5)));
        gs.score+=chainPts79;nearest79.dying=performance.now();
        spawnParticles(nearest79.x,nearest79.y,"#7dd3fc",6,"spark");
        spawnPopup(nearest79.x,nearest79.y-18,`🌊+${chainPts79}`,"#38bdf8",13);
        cwPwr79.chainCount=(cwPwr79.chainCount||0)+1;
        if(cwPwr79.chainCount>=3)unlock("chain_wave_chain");
      }
    }}
    // SCORE_OVERDRIVE — multiply pts by streak (capped ×5)
    {const odPwr79=activePwrRef.current.find(p=>p.type==="SCORE_OVERDRIVE"&&p.endsAt>Date.now());
    if(odPwr79){
      const odMult79=Math.min(5,1+Math.floor((gs.streak||0)/5));
      if(odMult79>1){
        const odBase79=Math.round(50*Math.min(10,1+Math.floor((gs.streak||0)/5))*(gs.feverActive?2:1));
        const odBonus79=Math.round(odBase79*(odMult79-1));
        gs.score+=odBonus79;
        odPwr79.totalAdded=(odPwr79.totalAdded||0)+odBonus79;
        spawnPopup(hit.x+22,hit.y-14,`📈×${odMult79} +${odBonus79}`,"#f87171",13);
        if(odPwr79.totalAdded>=2000)unlock("score_od_peak");
      }
    }}
    // TEMPO_BURST — streak×2 bonus pts on every tap
    {const tbPwr77=activePwrRef.current.find(p=>p.type==="TEMPO_BURST"&&p.endsAt>Date.now());
    if(tbPwr77){
      const tempoBonus77=Math.max(2,(gs.streak||0)*2)*(gs.feverActive?2:1);
      gs.score+=tempoBonus77;
      tbPwr77.totalBonus=(tbPwr77.totalBonus||0)+tempoBonus77;
      spawnPopup(hit.x-18,hit.y-12,`🎵+${tempoBonus77}`,"#e879f9",13);
      if(tbPwr77.totalBonus>=1000)unlock("tempo_burst_peak");
    }}
    // SCORE_FLOOD — add +50 bonus on every tap
    {const sfPwr71=activePwrRef.current.find(p=>p.type==="SCORE_FLOOD"&&p.endsAt>Date.now());
    if(sfPwr71){
      const floodBonus71=50*(gs.feverActive?2:1);
      gs.score+=floodBonus71;
      sfPwr71.floodTotal=(sfPwr71.floodTotal||0)+floodBonus71;
      spawnPopup(hit.x+20,hit.y-10,`🌊+${floodBonus71}`,"#0ea5e9",14);
      if(sfPwr71.floodTotal>=500)unlock("score_flood_big");
    }}
    // LIFE_LEECH_PLUS — restore 1 life every 3 taps
    {const llpPwr71=activePwrRef.current.find(p=>p.type==="LIFE_LEECH_PLUS"&&p.endsAt>Date.now());
    if(llpPwr71){
      llpPwr71.tapCounter=(llpPwr71.tapCounter||0)+1;
      if(llpPwr71.tapCounter%3===0){
        const gsLlp=gsRef.current;const cfgLlp=levelCfgRef.current;
        const maxLives71=cfgLlp?.lives||3;
        if(gsLlp&&gsLlp.lives<maxLives71){
          gsLlp.lives++;
          spawnPopup(hit.x,hit.y-30,"❤️ +1","#f87171",18);
          llpPwr71.livesRestored=(llpPwr71.livesRestored||0)+1;
          if(llpPwr71.livesRestored>=3)unlock("life_leech_plus_3");
        }
      }
    }}
    // ECHO_CHAIN — schedule an echo tap 0.5s later at same position for 60% of current pts
    {const ecPwr70=activePwrRef.current.find(p=>p.type==="ECHO_CHAIN"&&p.endsAt>Date.now());
    if(ecPwr70){
      const echoX70=hit.x;const echoY70=hit.y;const echoPts70=Math.round(pts*0.6);
      setTimeout(()=>{
        const gsEcho=gsRef.current;
        if(!gsEcho||gsEcho.lives<=0)return;
        gsEcho.score+=echoPts70;
        spawnPopup(echoX70,echoY70-20,`🔊 ECHO +${echoPts70}`,"#22d3ee",16);
        spawnParticles(echoX70,echoY70,"#a5f3fc",6,"dot");
        sfx("comboNote",2);
        ecPwr70.echoCount=(ecPwr70.echoCount||0)+1;
        unlock("echo_chain_proc");
      },500);
    }}
    // CHAIN_LOCK — each tap adds previous tap's score to current
    {const clPwr69=activePwrRef.current.find(p=>p.type==="CHAIN_LOCK"&&(p.charges||0)>0&&p.endsAt>Date.now());
    if(clPwr69){
      const chainBonus69=clPwr69.lastPts||0;
      gs.score+=chainBonus69;
      clPwr69.lastPts=pts+chainBonus69; // next tap gets this tap's total
      clPwr69.chainTotal=(clPwr69.chainTotal||0)+1;
      clPwr69.charges=(clPwr69.charges||5)-1;
      if(chainBonus69>0){
        spawnPopup(hit.x,hit.y-42,`🔐 +${chainBonus69} CHAIN`,"#f59e0b",18);
      }
      if(clPwr69.charges<=0){
        activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="CHAIN_LOCK");
        setActivePwrDisp([...activePwrRef.current]);
        unlock("chain_lock_max");
      } else {
        setActivePwrDisp([...activePwrRef.current]);
      }
    }}
    // VORTEX_TAP — each tap pulls nearby targets toward tap point
    {const vtPwr=activePwrRef.current.find(p=>p.type==="VORTEX_TAP"&&p.endsAt>Date.now());
    if(vtPwr){
      const vortexRange68=100;
      const pulled68=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="bomb"&&o.type!=="boss"&&Math.hypot(o.x-tapX,o.y-tapY)<vortexRange68);
      if(pulled68.length>0){
        pulled68.forEach(o=>{
          const ang=Math.atan2(tapY-o.y,tapX-o.x);
          o.x+=Math.cos(ang)*25;o.y+=Math.sin(ang)*25;
          o.x=Math.max(o.radius,Math.min((canvasRef.current?.width||360)-o.radius,o.x));
          o.y=Math.max(o.radius,Math.min((canvasRef.current?.height||640)-o.radius,o.y));
        });
        vtPwr.totalPulled=(vtPwr.totalPulled||0)+pulled68.length;
        particlesRef.current.push({type:"shockwave",x:tapX,y:tapY,r:10,maxR:vortexRange68,born:performance.now(),color:"#818cf8",alpha:0.4,life:400});
        if(pulled68.length>=3){spawnPopup(tapX,tapY-30,"🌀 PULL!","#818cf8",16);unlock("vortex_tap_pull");}
      }
    }}
    // TAP_FURY — force 2× multiplier when active
    {const tfPwr=activePwrRef.current.find(p=>p.type==="TAP_FURY"&&p.endsAt>Date.now());
    if(tfPwr&&!gs.feverActive){
      const furyBonus66=pts; // pts already at 1× — add another pts = 2×
      gs.score+=furyBonus66;
      tfPwr.bonusScore=(tfPwr.bonusScore||0)+furyBonus66;
      if(tfPwr.bonusScore>=500)unlock("tap_fury_big");
    }}
    // MAGNET_BLITZ — auto-collect nearest non-bomb within 120px
    {const mbPwr=activePwrRef.current.find(p=>p.type==="MAGNET_BLITZ"&&p.endsAt>Date.now());
    if(mbPwr){
      const closest65=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="bomb"&&o.type!=="boss"&&Math.hypot(o.x-hit.x,o.y-hit.y)<120).sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y))[0];
      if(closest65){
        targetsRef.current=targetsRef.current.filter(o=>o.id!==closest65.id);
        const blitzPts65=Math.round(50*(gs.feverActive?2:1));
        gs.score+=blitzPts65;
        spawnParticles(closest65.x,closest65.y,"#fbcfe8",5,"dot");
        spawnPopup(closest65.x,closest65.y-16,`🧲 +${blitzPts65}`,"#ec4899",11);
        mbPwr.pullCount=(mbPwr.pullCount||0)+1;
        if(mbPwr.pullCount>=10)unlock("magnet_blitz_pull");
      }
    }}
    // CRIT_STORM — 20% chance of 3× crit
    {const csPwr=activePwrRef.current.find(p=>p.type==="CRIT_STORM"&&p.endsAt>Date.now());
    if(csPwr&&Math.random()<0.20){
      const critBonus65=pts*2; // pts already added; add 2× more = 3× total
      gs.score+=critBonus65;
      csPwr.critCount=(csPwr.critCount||0)+1;
      spawnPopup(hit.x,hit.y-36,`💥 CRIT! ×3 +${critBonus65}`,"#f43f5e",20);
      sfx("epic");vibrate([6,3,10]);
      unlock("crit_storm_proc");
    }}
    // SCORE_DOUBLER — next 3 targets score ×2
    {const sdPwr=activePwrRef.current.find(p=>p.type==="SCORE_DOUBLER"&&(p.charges||0)>0&&p.endsAt>Date.now());
    if(sdPwr){
      gs.score+=pts; // already added pts; add again = ×2
      sdPwr.charges=(sdPwr.charges||1)-1;
      sdPwr.used=(sdPwr.used||0)+1;
      spawnPopup(hit.x+24,hit.y-26,`×2 +${pts}`,"#34d399",12);
      if(sdPwr.charges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_DOUBLER");setActivePwrDisp([...activePwrRef.current]);}
      if((sdPwr.used||0)>=3)unlock("score_doubler_tri");
    }}
    // COMBO_ECHO — each tap auto-hits nearest normal target when combo≥3
    {const cePwr=activePwrRef.current.find(p=>p.type==="COMBO_ECHO"&&p.endsAt>Date.now());
    if(cePwr&&gs.streak>=3){
      const nearest63=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type==="normal").sort((a,b)=>Math.hypot(a.x-hit.x,a.y-hit.y)-Math.hypot(b.x-hit.x,b.y-hit.y))[0];
      if(nearest63){
        targetsRef.current=targetsRef.current.filter(o=>o.id!==nearest63.id);
        const echoPts63=Math.round(60*(gs.feverActive?2:1));
        gs.score+=echoPts63;
        spawnPopup(nearest63.x,nearest63.y-18,`🔊 ECHO +${echoPts63}`,"#a78bfa",13);
        spawnParticles(nearest63.x,nearest63.y,"#c4b5fd",5,"dot");
        cePwr.echoCount=(cePwr.echoCount||0)+1;
        if(cePwr.echoCount>=1)unlock("combo_echo_proc");
      }
    }}
    // STREAK_SURGE — bonus pts = pts×(streak/5) on each tap, 10 charges
    {const ssPwr=activePwrRef.current.find(p=>p.type==="STREAK_SURGE"&&(p.charges||0)>0&&p.endsAt>Date.now());
    if(ssPwr){
      const streak62=gs.streak||0;
      const surgeMult62=Math.max(0,streak62/5);
      const surgeBonus62=Math.round(pts*surgeMult62);
      if(surgeBonus62>0){gs.score+=surgeBonus62;spawnPopup(hit.x-24,hit.y-28,`🔥+${surgeBonus62}`,"#fb923c",12);}
      ssPwr.charges=(ssPwr.charges||1)-1;
      if(streak62>=20&&!ssPwr.peaked){ssPwr.peaked=true;unlock("streak_surge_big");}
      if(ssPwr.charges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="STREAK_SURGE");setActivePwrDisp([...activePwrRef.current]);}
    }}
    // LIFE_CHAIN — every 5 taps restore 1 life
    {const lcPwr=activePwrRef.current.find(p=>p.type==="LIFE_CHAIN"&&p.endsAt>Date.now());
    if(lcPwr&&(lcPwr.healsGiven||0)<3){
      lcPwr.tapCount=(lcPwr.tapCount||0)+1;
      if(lcPwr.tapCount%5===0){
        gs.lives=Math.min(5,gs.lives+1);
        lcPwr.healsGiven=(lcPwr.healsGiven||0)+1;
        spawnPopup(hit.x,hit.y-38,"❤️ LIFE CHAIN!","#f87171",18);
        sfx("tap");vibrate([5,3,5]);
        if(lcPwr.healsGiven>=3){unlock("life_chain_heal");activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_CHAIN");setActivePwrDisp([...activePwrRef.current]);}
      }
    }}
    // SCORE_MIRROR — bank all score gains for end payout
    {const smPwr=activePwrRef.current.find(p=>p.type==="SCORE_MIRROR"&&p.endsAt>Date.now());
    if(smPwr){
      smPwr.mirrorBank=(smPwr.mirrorBank||0)+pts;
    }}
    // Chain Fever — 5 taps in 2s triggers instant fever
    {const cfPwr=activePwrRef.current.find(p=>p.type==="CHAIN_FEVER"&&p.endsAt>Date.now());
    if(cfPwr&&!cfPwr.triggered){
      const now58=Date.now();
      cfPwr.chainTaps=[...cfPwr.chainTaps.filter(t58=>now58-t58<2000),now58];
      if(cfPwr.chainTaps.length>=5){
        cfPwr.triggered=true;
        gs.feverActive=true;gs.feverTimeLeft=5000;
        setFeverBorder(true);setTimeout(()=>setFeverBorder(false),5000);
        spawnPopup(hit.x,hit.y-40,"⛓️🔥 CHAIN FEVER!","#f97316",26);
        sfx("feverStart");vibrate([8,4,12,4,8]);
        unlock("chain_fever_proc");
        // Reset so it can trigger again if still active
        setTimeout(()=>{if(cfPwr)cfPwr.triggered=false;cfPwr.chainTaps=[];},5000);
      }
    }}
    // Magnet Storm — pull ALL non-boss targets toward tap point
    {const msPwr=activePwrRef.current.find(p=>p.type==="MAGNET_STORM"&&p.endsAt>Date.now());
    if(msPwr){
      const pullDist57=160;
      const pulledTargets57=targetsRef.current.filter(o=>o.id!==hit.id&&!o.dying&&o.type!=="boss"&&o.type!=="bomb");
      pulledTargets57.forEach(o=>{
        const dx57=hit.x-o.x,dy57=hit.y-o.y;
        const dist57=Math.hypot(dx57,dy57)||1;
        const force57=Math.min(pullDist57,dist57)*0.15;
        o.vx=(o.vx||0)+(dx57/dist57)*force57;o.vy=(o.vy||0)+(dy57/dist57)*force57;
        o.moving=true;
      });
      msPwr.maxPull=Math.max(msPwr.maxPull||0,pulledTargets57.length);
      if(msPwr.maxPull>=5)unlock("magnet_storm_pull");
    }}
    // Nova Boost — every 3rd tap fires a mini nova shockwave (+150pts)
    {const nbPwr=activePwrRef.current.find(p=>p.type==="NOVA_BOOST"&&p.endsAt>Date.now());
    if(nbPwr){
      nbPwr.novaCount=(nbPwr.novaCount||0)+1;
      if(nbPwr.novaCount%3===0){
        const novaPts=150;
        gs.score+=novaPts;
        particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:hit.radius,maxR:130,born:performance.now(),color:"#38bdf8",alpha:0.6,life:500});
        spawnPopup(hit.x,hit.y-40,"💥🌟 NOVA!","#38bdf8",22);
        sfx("chainBonus");
        unlock("nova_boost_proc");
      }
    }}
    // Mirror Gate — clone this normal target at mirrored position
    if(gs._mirrorGateActive){
      gs._mirrorGateActive=false;
      const cw=canvasRef.current?.width||360;const ch=canvasRef.current?.height||640;
      const mirrorX=cw-hit.x;const mirrorY=hit.y;
      targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"mirror_echo",
        rarity:hit.rarity,x:mirrorX,y:mirrorY,radius:hit.radius*0.85,color:"#c7d2fe",glow:"#818cf8",
        lifetime:2200,spawnedAt:Date.now(),born:performance.now(),anticipateMs:0,
        moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
        worldId:hit.worldId,worldColor:hit.worldColor,bossPattern:null,bossPhase:1,cx:mirrorX,cy:mirrorY,
        _mirrorPts:Math.round(pts*0.5)});
      spawnPopup(mirrorX,mirrorY-20,"🌀 ECHO!","#c7d2fe",14);
      unlock("mirrorgate_echo");
    }
    if(bountyHit){
      spawnPopup(hit.x,hit.y-38,`🎯 BOUNTY! ×${bounty.mult} +${pts}`,bounty.color,22);
      spawnParticles(hit.x,hit.y,bounty.color,16,"spark");
      sfx("chainBonus");vibrate([12,6,18]);
      gs._bountyHits=(gs._bountyHits||0)+1;
      if(gs._bountyHits>=3)unlock("bounty_hit");
      // Reset bounty after hit — next one in 8s
      bountyRef.current=null;setBountyData(null);
      if(bountyTimerRef.current)clearTimeout(bountyTimerRef.current);
      bountyTimerRef.current=setTimeout(()=>{
        if(!gsRef.current||gsRef.current.lives<=0)return;
        const BOUNTY_POOL2=[{rarity:"UNCOMMON",icon:"⚪",mult:2,color:"#94a3b8"},{rarity:"RARE",icon:"🔵",mult:3,color:"#60a5fa"},{rarity:"EPIC",icon:"🟣",mult:4,color:"#c084fc"},{rarity:"LEGENDARY",icon:"🟡",mult:5,color:"#ffd700"}];
        const b=BOUNTY_POOL2[Math.floor(Math.random()*BOUNTY_POOL2.length)];
        const bd={...b,expiresAt:Date.now()+35000};bountyRef.current=bd;setBountyData(bd);
        bountyTimerRef.current=setTimeout(()=>{bountyRef.current=null;setBountyData(null);},35000);
      },8000);
    }
    if(scoreBoostMult>1){spawnParticles(hit.x,hit.y,"#f43f5e",8,"spark");}
    // Prestige aura — golden shockwave on each tap when prestige ≥ 1
    if((saveRef.current.prestigeLevel||0)>=1){
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,color:"#ffd700",
        size:hit.radius*1.2,born:performance.now(),duration:500,alpha:0.55+(saveRef.current.prestigeLevel||0)*0.06});
    }
    // Proximity chain bonus — if tapped within 300ms AND within 80px of last tap
    const nowMs=Date.now();
    if(gs._lastTapX!==undefined&&gs._lastTapY!==undefined&&gs._lastTapMs&&
       (nowMs-gs._lastTapMs)<300&&Math.hypot(hit.x-gs._lastTapX,hit.y-gs._lastTapY)<80){
      const chainPts=Math.round(pts*0.4);
      if(chainPts>0){
        gs.score+=chainPts;
        spawnPopup(hit.x,hit.y-42,`🔗 CHAIN +${chainPts}`,"#a78bfa",13);
      }
    }
    gs._lastTapX=hit.x;gs._lastTapY=hit.y;gs._lastTapMs=nowMs;
    if(gs._magnetFieldEndsAt&&Date.now()<gs._magnetFieldEndsAt){gs._magnetFieldX=hit.x;gs._magnetFieldY=hit.y;}
    // Tap Frenzy — 5 hits within 2s triggers bonus
    if(!gs._recentTaps)gs._recentTaps=[];
    gs._recentTaps.push(nowMs);
    gs._recentTaps=gs._recentTaps.filter(t=>nowMs-t<2000);
    if(gs._recentTaps.length>=5&&!gs._frenzyAt){
      gs._frenzyAt=nowMs;
      const frenzyBonus=Math.round(150*(gs.feverActive?2:1)*(1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05)));
      gs.score+=frenzyBonus;
      const canvas=canvasRef.current;
      spawnPopup(canvas?.width/2||195,(canvas?.height||700)*0.38,`⚡ TAP FRENZY! +${frenzyBonus}`,"#f97316",22);
      sfx("chainBonus");vibrate([15,8,15,8,30]);
      setTimeout(()=>{if(gsRef.current)gsRef.current._frenzyAt=null;},2000);
      unlock("tap_frenzy");
    }
    // Lucky Streak — 7 consecutive hits without any miss gives coin jackpot
    gs._luckyTaps=(gs._luckyTaps||0)+1;
    if(gs._luckyTaps===7){
      gs._luckyTaps=0;
      const luckyCoins=10+Math.floor(Math.random()*15);
      saveRef.current.coins=(saveRef.current.coins||0)+luckyCoins;
      saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+luckyCoins;
      debounceSave();
      const canvas=canvasRef.current;
      spawnPopup(canvas?.width/2||195,(canvas?.height||700)*0.32,`🍀 LUCKY STREAK! +${luckyCoins}🪙`,"#ffd700",20);
      sfx("coin");vibrate([8,6,8,6,15]);
      unlock("lucky_streak");
    }
    // Track last rarity for Mimic targets
    gs.lastRarityMult=hit.rarity.mult;
    gs.lastRarityColor=hit.color;
    gs.lastRarityName=hit.rarity.name;
    // 9B — Mascot happiness — session tap milestone triggers dialogue + bonus
    mascotHappyRef.current=(mascotHappyRef.current||0)+1;
    const happyCount=mascotHappyRef.current;
    if(happyCount===25||happyCount===50||happyCount===75){
      const happyMascot=MASCOTS.find(m=>m.id===(saveRef.current.mascotId||"dragon"));
      const msgs=happyMascot?.speeches?.happy||["Great job!"];
      showMascotSpeech("happy");
      if(happyCount===50){
        // 50-tap milestone: MASCOT POWER! 2× pts for 6s (via OVERCLOCK)
        gs._overclockEndsAt=Date.now()+6000;
        activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="OVERCLOCK");
        activePwrRef.current.push({type:"OVERCLOCK",endsAt:Date.now()+6000});
        setActivePwrDisp([...activePwrRef.current]);
        const canvas5=canvasRef.current;
        spawnPopup(canvas5?.width/2||195,(canvas5?.height||700)*0.38,`${happyMascot?.emoji?.happy||"🌟"} MASCOT POWER! ×2 PTS!`,"#fbbf24",22);
        spawnParticles(canvas5?.width/2||195,(canvas5?.height||700)*0.5,"#fbbf24",22,"spark");
        sfx("feverStart");vibrate([15,8,15,8,25]);setMascotBounce(true);setTimeout(()=>setMascotBounce(false),1200);
        setNotif(`${happyMascot?.emoji?.happy||"🌟"} ${happyMascot?.name} is THRILLED! ×2 pts for 6s!`);
      }
      if(happyCount===75){
        // 75-tap milestone: instant 3× score multiplier for 5s (handled via feverMult override)
        spawnPopup(hit.x,hit.y-55,"🌈 MASCOT FEVER! +100","#ff00ff",18);
        gs.score+=100;sfx("mascotNote",25,440,"sine");vibrate([20,10,20,10,40]);
      }
    }
    // Mascot XP (level 5 = +5% coin bonus)
    const mascotIdNow=saveRef.current.mascotId||"dragon";
    const mXP=saveRef.current.mascotXP||(saveRef.current.mascotXP={});
    const mLvlBefore=Math.min(20,Math.floor((mXP[mascotIdNow]||0)/500));
    mXP[mascotIdNow]=(mXP[mascotIdNow]||0)+Math.max(1,Math.floor(pts*0.01));
    const mLvlNow=Math.min(20,Math.floor((mXP[mascotIdNow]||0)/500));
    // Evolution at LV5/10/15/20 — show popup with new perk
    if(mLvlNow>mLvlBefore&&[5,10,15,20].includes(mLvlNow)){
      const EVO_PERKS={5:"💰 +5% coins",10:"✨ Aura unlocked",15:"⚡ Fast dance",20:"👑 MAX rainbow aura"};
      const m=MASCOTS.find(mm=>mm.id===mascotIdNow);
      sfx("unlock");vibrate([40,20,40,20,80]);
      setNotif(`🌟 ${m?.name||"Mascot"} → LV${mLvlNow}! ${EVO_PERKS[mLvlNow]}`);
      setTimeout(()=>setNotif(null),3000);
      if(mLvlNow>=10)unlock("mascot_lv10");
      // Auto-equip star at LV5, crown at LV20
      const owned={...(saveRef.current.ownedAccessories||{})};
      const equipped={...(saveRef.current.mascotAccessories||{})};
      if(mLvlNow===5){owned[`${mascotIdNow}:star`]=true;equipped[mascotIdNow]=equipped[mascotIdNow]||"star";}
      if(mLvlNow===20){owned[`${mascotIdNow}:crown`]=true;equipped[mascotIdNow]="crown";}
      saveRef.current.ownedAccessories=owned;
      saveRef.current.mascotAccessories=equipped;
    }
    const _skl=saveRef.current.skills||{};
    const coinSkill=_skl.coin_magnet||0;
    const coinSkillMult=coinSkill===3?1.2:coinSkill===2?1.1:coinSkill===1?1.05:1;
    const goldRushActive=gs._goldRushEndsAt&&Date.now()<gs._goldRushEndsAt;
    const coinMult=(mLvlNow>=5?1.05:1)*coinSkillMult*(goldRushActive?3:1);
    const baseCoin=Math.max(1,Math.floor(pts*0.09*coinMult));
    const bountyCoin=hit._isBounty?baseCoin*2:0; // Bounty target gives 3× coins (base + 2× bonus)
    saveRef.current.coins=(saveRef.current.coins||0)+baseCoin+bountyCoin;
    saveRef.current.totalCoins=(saveRef.current.totalCoins||0)+baseCoin+bountyCoin;
    if(bountyCoin>0){spawnPopup(hit.x,hit.y-44,`👑 BOUNTY! +${bountyCoin}🪙`,"#ffd700",15);sfx("coin");unlock("bounty_hit");}
    if(goldRushActive&&baseCoin>0&&Math.random()<0.25){spawnPopup(hit.x,hit.y-50,`🥇 ×3 GOLD!`,"#ffd700",13);}
    // Trigger Gold Rush — rare chance on legendary or loot chest hit
    if(!goldRushActive&&!gs._goldRushEndsAt&&(hit.rarity?.name==="legendary"||hit.type==="lootchest")&&Math.random()<0.12){
      gs._goldRushEndsAt=Date.now()+9000;
      setGoldRushMode(true);setTimeout(()=>setGoldRushMode(false),9000);
      sfx("jackpot");vibrate([30,15,30,15,60]);
      const canvas=canvasRef.current;
      spawnPopup(canvas?.width/2||195,canvas?.height/3||230,"🥇 GOLD RUSH! 3× COINS","#ffd700",22);
      unlock("gold_rush");
    }

    gs.sessionStats.tapsTotal++;gs.sessionStats.score=gs.score;
    if(hit.rarity.name!=="common")gs.sessionStats.rareHits++;
    if(gs.streak>gs.sessionStats.bestCombo)gs.sessionStats.bestCombo=gs.streak;
    if(isPerfect){gs.sessionStats.perfectTaps=(gs.sessionStats.perfectTaps||0)+1;}

    // First-tap fever: if very first tap of a level hits rare+, grant 3s mini-fever
    if(gs.sessionStats.tapsTotal===1&&!gs.feverActive&&(hit.rarity.name==="rare"||hit.rarity.name==="epic"||hit.rarity.name==="legendary")){
      gs.feverActive=true;gs.feverTimeLeft=3000;
      setFeverBorder(true);sfx("feverStart");vibrate([25,12,25]);
      spawnPopup(hit.x,hit.y-40,"🔥 FIRST TAP FEVER!","#f97316",20);
      unlock("first_tap_fever");
    }

    // New personal best streak notification
    if(gs.streak>5&&gs.streak>(saveRef.current.bestStreak||0)&&!gs._streakPBNotified){
      gs._streakPBNotified=gs.streak;
      spawnPopup(hit.x,hit.y-58,`🏆 NEW STREAK PB: ${gs.streak}×!`,"#34d399",15);
      saveRef.current.bestStreak=gs.streak;
    }
    // Activate streak shield at streak 10
    if(gs.streak===10&&!streakShRef.current){streakShRef.current=true;setStreakShieldActive(true);sfx("lucky");}
    // Combo Shield auto-recharge at streak 15 (if shield was lost)
    if(gs.streak===15&&!streakShRef.current){
      streakShRef.current=true;setStreakShieldActive(true);sfx("lucky");
      const canvas3=canvasRef.current;
      spawnPopup(canvas3?.width/2||195,(canvas3?.height||700)*0.28,"🛡 SHIELD RECHARGED!","#fbbf24",18);
      vibrate([15,8,15]);
    }
    // Fury Mode: streak ≥35 triggers a purple border effect + bonus
    if(gs.streak===35){
      const canvas3=canvasRef.current;const cw35=canvas3?.width||390;const ch35=canvas3?.height||700;
      const furyColors=["#a855f7","#7c3aed","#f97316","#ffd700"];
      furyColors.forEach((c,i)=>setTimeout(()=>spawnParticles(cw35/2,ch35/2,c,14,"spark"),i*65));
      sfx("legendary");vibrate([25,10,25,10,50]);
      spawnPopup(cw35/2,ch35*0.22,"🌑 FURY MODE!","#a855f7",24);
      unlock("fury_mode");
    }
    // Mascot mood
    if(gs.streak>=35)setMascotMood("fire");
    else if(gs.streak>=20)setMascotMood("fire");
    else if(gs.streak>=10)setMascotMood("excited");
    else if(gs.streak>=5)setMascotMood("happy");
    else setMascotMood("idle");
    // Mascot bounce on every hit
    mascotBouncePlay();
    // Streak milestone burst celebrations + speech
    {const MILESTONES=[{n:5,label:"🔥 ON FIRE!",color:"#fbbf24"},{n:10,label:"⚡ UNSTOPPABLE!",color:"#f97316"},{n:20,label:"💥 LEGENDARY!",color:"#ef4444"},{n:25,label:"🌟 SUPERSTAR!",color:"#c084fc"},{n:30,label:"🌈 GODLIKE!!!",color:"#ff00ff"},{n:50,label:"👑 TRANSCENDENT!",color:"#ffd700"}];
    const ms=MILESTONES.find(m=>m.n===gs.streak);
    if(ms){
      setStreakBurst(ms);setTimeout(()=>setStreakBurst(null),ms.n>=50?1800:1200);showMascotSpeech("streak");
      // 25x: shockwave ring explosion
      if(ms.n===25){
        const canvas=canvasRef.current;const cw2=canvas?canvas.width:390;const ch2=canvas?canvas.height:700;
        const cols=["#c084fc","#a78bfa","#60a5fa","#fbbf24"];
        cols.forEach((c,i)=>setTimeout(()=>spawnParticles(cw2/2,ch2/2,c,12,"spark"),i*55));
        spawnParticles(cw2/2,ch2/2,"#fff",8,"shockwave");
        sfx("chainBonus");vibrate([20,10,20,10,45]);
        setEpicFlash(true);setTimeout(()=>setEpicFlash(false),500);
      }
      // 50x: rainbow explosion across the whole canvas
      if(ms.n>=50){
        const canvas=canvasRef.current;const cw2=canvas?canvas.width:390;const ch2=canvas?canvas.height:700;
        const colors=["#ff6030","#ffd700","#34d399","#60a5fa","#f472b6","#a78bfa","#ff6b35"];
        colors.forEach((c,i)=>setTimeout(()=>spawnParticles(cw2/2,ch2/2,c,15,"spark"),i*80));
        sfx("jackpot");vibrate([30,10,30,10,30,10,80]);
        setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),1000);
      }
    }}

    // Musical pentatonic scale note (most addictive mechanic!) — rising melody as streak grows
    sfx("comboNote", gs.streak);

    // Check personal best mid-game — firework burst when record is broken
    if(gs.score>saveRef.current.highScore&&gs.score>200&&!newRecord){
      setNewRecord(true);setTimeout(()=>setNewRecord(false),2000);
      const canvas=canvasRef.current;const cw3=canvas?canvas.width:390;const ch3=canvas?canvas.height:700;
      const fwColors=["#ffd700","#ff6b35","#34d399","#60a5fa","#f472b6","#a78bfa"];
      // 3 firework bursts at different positions
      [[cw3*0.25,ch3*0.3],[cw3*0.75,ch3*0.25],[cw3*0.5,ch3*0.2]].forEach(([fx,fy],i)=>{
        setTimeout(()=>spawnParticles(fx,fy,fwColors[i*2]||"#ffd700",22,"spark"),i*180);
      });
      sfx("jackpot");vibrate([20,10,20,10,20]);
    }

    // Score milestones — mini celebration at key point thresholds
    {const SCORE_MILESTONES=[1000,2500,5000,10000,20000,50000];
    const prevScore=gs.score-pts;
    const crossed=SCORE_MILESTONES.find(m=>prevScore<m&&gs.score>=m);
    if(crossed&&!cfg?.isZen){
      const canvas2=canvasRef.current;const cw4=canvas2?.width||390,ch4=canvas2?.height||700;
      const isEpic=crossed>=10000;
      const milestoneColor=isEpic?"#ffd700":crossed>=5000?"#c084fc":crossed>=2500?"#f97316":"#34d399";
      spawnPopup(cw4/2,ch4*0.28,`🏅 ${crossed.toLocaleString()} PTS!`,milestoneColor,isEpic?24:20);
      spawnParticles(cw4/2,ch4*0.3,milestoneColor,isEpic?20:12,"spark");
      if(isEpic){sfx("legendary");setEpicFlash(true);setTimeout(()=>setEpicFlash(false),400);unlock("score_10k");if(crossed>=25000)unlock("score_25k");if(crossed>=50000)unlock("score_50k");if(crossed>=100000)unlock("score_100k");if(crossed>=250000)unlock("score_250k");if(crossed>=500000)unlock("score_500k");if(crossed>=1000000)unlock("score_1m");}
      else sfx("chainBonus");
      vibrate(isEpic?[15,8,15,8,25]:[10,5,15]);
    }}

    // SFX + particles
    if(hit.rarity.name==="legendary"){
      sfx("legendary");vibrate([30,15,30,15,60,15,90]);spawnParticles(hit.x,hit.y,hit.color,55,"spark");
      spawnParticles(hit.x,hit.y,"#ffffff",20,"dot");
      setEpicFlash(true);setTimeout(()=>setEpicFlash(false),650);
      setLegendaryFlash(true);setTimeout(()=>setLegendaryFlash(false),900);
      unlock("legendary");
    } else if(hit.rarity.name==="epic"){
      sfx("epic");vibrate([22,30,22]);spawnParticles(hit.x,hit.y,hit.color,35,"spark");
      spawnParticles(hit.x,hit.y,hit.color+"88",12,"dot");
      setEpicFlash(true);setTimeout(()=>setEpicFlash(false),420);unlock("first_epic");
    } else if(hit.rarity.name==="rare"){
      sfx("rare");vibrate([20,20]);spawnParticles(hit.x,hit.y,hit.color,22,"dot");unlock("first_rare");
    } else if(hit.rarity.name==="uncommon"){
      sfx("uncommon");vibrate(14);spawnParticles(hit.x,hit.y,hit.color,12,"dot");
    } else {
      vibrate(8);spawnParticles(hit.x,hit.y,hit.color,8,"dot");
    }
    // ── CHAIN REACTIONS — Rare+ targets can chain-detonate nearby normal targets ──
    const chainChance=hit.rarity.name==="legendary"?1.0:hit.rarity.name==="epic"?0.55:hit.rarity.name==="rare"?0.22:0;
    if(chainChance>0){
      const chainRange=hit.radius*4.2+90;
      const candidates=targetsRef.current.filter(t=>
        t!==hit&&!t.dying&&!t.anticipateMs||(t.anticipateMs&&(performance.now()-t.born)>=t.anticipateMs)
      ).filter(t=>t.type==="normal"&&t.id!==hit.id&&!t.dying);
      let chained=0;
      candidates.forEach(t=>{
        if(chained>=4)return;
        const dist=Math.hypot(t.x-hit.x,t.y-hit.y);
        if(dist>chainRange)return;
        if(Math.random()>chainChance)return;
        chained++;
        // Lightning arc from hit to chained target
        particlesRef.current.push({type:"arc",x:hit.x,y:hit.y,x2:t.x,y2:t.y,
          color:hit.color,size:3,born:performance.now(),duration:280,alpha:1});
        // Detonate
        t.dying=performance.now();
        const chainPts=Math.max(1,Math.floor((t.rarity?.mult||1)*combo*feverMult*0.5*prestigeMult));
        gs.score+=chainPts;
        spawnParticles(t.x,t.y,t.color,14,"dot");
        spawnPopup(t.x,t.y-18,`+${chainPts} CHAIN!`,hit.color,12);
      });
      if(chained>0){
        sfx("chainBonus");vibrate([8,4,8]);
        // Chain count announcement popup
        const chainLabel=chained>=4?"⚡⚡⚡ CHAIN x"+chained+"!":chained>=2?"⚡⚡ CHAIN x"+chained+"!":"⚡ CHAIN!";
        setChainFlash({label:chainLabel,at:Date.now()});
      }
      if(chained>=4)unlock("chain_4");
    }
    if(isPerfect){
      sfx("perfect");vibrate([8,8,8]);spawnPopup(hit.x,hit.y-22,"✨ PERFECT!","#fbbf24",18);
      setPerfectFlash(true);setTimeout(()=>setPerfectFlash(false),350);unlock("perfect_tap");
      // Golden star burst trail for perfect taps
      const trailColors=["#ffd700","#fbbf24","#fffbe6","#f59e0b","#ffffff"];
      const _pnow=performance.now();
      for(let i=0;i<20;i++){
        const ang=Math.random()*Math.PI*2,spd=1.5+Math.random()*3.5;
        particlesRef.current.push({
          type:"trail",x:hit.x,y:hit.y,
          vx:Math.cos(ang)*spd,vy:Math.sin(ang)*spd,
          color:trailColors[i%trailColors.length],
          born:_pnow,duration:380+Math.random()*360,
          size:2+Math.random()*4,alpha:1,
        });
      }
      // Ring shockwave in gold
      particlesRef.current.push({type:"shockwave",x:hit.x,y:hit.y,r:1,color:"#ffd700",born:_pnow,duration:500,alpha:0.85});
    } else if(hit.rarity.label){
      spawnPopup(hit.x,hit.y,hit.rarity.label,hit.color,14);
    }
    // Last-Breath bonus visual
    if(isLastBreath){
      spawnPopup(hit.x,hit.y-38,"⏰ LAST BREATH! ×1.5","#fb923c",15);
      sfx("nearMiss"); // distinctive sound for near-miss catch
    }
    // Combo label
    const cl=COMBO_LABELS.find(([n])=>gs.streak>=n);
    if(cl){setComboLabel(cl[1]);clearTimeout(window.__clt);window.__clt=setTimeout(()=>setComboLabel(""),1300);}
    // Achievements
    unlock("first_tap");
    if(gs.streak>=5)unlock("streak_5");
    if(gs.streak>=10)unlock("streak_10");if(gs.streak>=25)unlock("streak_25");if(gs.streak>=50)unlock("streak_50");
    if(gs.streak>=15)unlock("combo_15");
    if(gs.score>=2000)unlock("score_2000");
    // Score milestone celebration — every 500 points
    const prevM=Math.floor((gs.score-pts)/500);
    const newM=Math.floor(gs.score/500);
    if(newM>prevM&&newM>0){
      const milestoneScore=newM*500;
      spawnParticles(hit.x,hit.y,"#ffd700",20,"spark");
      spawnPopup(hit.x,hit.y-50,`🏅 ${milestoneScore.toLocaleString()}!`,"#ffd700",17);
    }
    // Fever
    if(gs.streak>=FEVER_STREAK&&!gs.feverActive){
      const frLvl=(saveRef.current.skills||{}).fever_rush||0;
      const feverMult2=frLvl===3?1.6:frLvl===2?1.4:frLvl===1?1.2:1.0;
      gs.feverActive=true;gs.feverTimeLeft=Math.round(FEVER_DUR*feverMult2);setFeverBorder(true);sfx("feverStart");vibrate([35,20,35,20,65]);
      audioRef.current?.setBgMusicFever?.(true);
      spawnPopup(hit.x,hit.y-45,"🌡 FEVER!","#fbbf24",21);unlock("fever_mode");setMascotMood("fever");showMascotSpeech("fever");
      gs.sessionStats.feverCount=(gs.sessionStats.feverCount||0)+1;
    }
    // Check level win conditions
    const cfg=levelCfgRef.current;
    if(cfg){
      const scoreWin=gs.score>=cfg.scoreGoal;
      const modWin=!cfg.modifier||checkModGoal(cfg.modifier,gs);
      if(scoreWin&&modWin){endLevel(true);return;}
    }
    updateMissions({...gs.sessionStats,bestCombo:gs.streak});
    debounceSave();
  },[sfx,spawnParticles,spawnPopup,activatePowerUp,unlock,updateMissions,debounceSave,endLevel]);

  function checkModGoal(mod,gs){
    if(!mod)return true;
    switch(mod.type){
      case"boss_kill": return(gs.sessionStats.bossKills||0)>=1;
      case"combo_20":  return(gs.sessionStats.bestCombo||0)>=20;
      case"combo_40":  return(gs.sessionStats.bestCombo||0)>=40;
      case"fever_2":   return(gs.sessionStats.feverCount||0)>=2;
      case"fever_3":   return(gs.sessionStats.feverCount||0)>=3;
      case"no_miss":   return true; // enforced by losing life
      default: return true;
    }
  }

  // Countdown
  const runCountdown=useCallback((cb)=>{
    let n=3;
    const tick=()=>{
      setCountdownVal(n);sfx("countdown");
      if(n===0){setCountdownVal("GO!");sfx("go");setTimeout(()=>{setCountdownVal(null);cb();},600);return;}
      n--;setTimeout(tick,900);
    };
    tick();
  },[sfx]);

  // Start game
  const startGame=useCallback((levelId,shopCart=[],infCfg=null)=>{
    const baseCfg=infCfg||getLevelConfig(levelId);
    // Apply speed mode multiplier
    const speedMult=saveRef.current.speedMode||1.0;
    const cfg={...baseCfg,
      spawnInterval:Math.round(baseCfg.spawnInterval/speedMult),
      targetLifetime:Math.round(baseCfg.targetLifetime/speedMult),
    };
    levelCfgRef.current=cfg;
    // Sync module-level flags
    _hapticOn=saveRef.current.hapticEnabled!==false;
    _colorblindOn=!!saveRef.current.colorblindMode;
    _hapticIntensity=saveRef.current.hapticIntensity??70;
    initMissions();
    initBgParts(cfg.world);
    targetsRef.current=[];particlesRef.current=[];activePwrRef.current=[];ripplesRef.current=[];tapTrailRef.current=[];
    spawnTimer.current=0;pausedRef.current=false;setPaused(false);
    mascotHappyRef.current=0; // reset session happiness
    streakShRef.current=false;setStreakShieldActive(false);setNewRecord(false);setCloseBanner(false);setRushMode(false);setBossTaunt(null);setGoldRushMode(false);
    luckyRef.current=null;if(luckyTimer.current)clearTimeout(luckyTimer.current);
    // Schedule first lucky event 45-70 seconds in
    luckyTimer.current=setTimeout(()=>triggerLucky(),45000+Math.random()*25000);
    // Bounty Board — first bounty after 12 seconds, then every 35s
    bountyRef.current=null;setBountyData(null);
    if(bountyTimerRef.current)clearTimeout(bountyTimerRef.current);
    const BOUNTY_POOL=[
      {rarity:"UNCOMMON",icon:"⚪",mult:2,color:"#94a3b8"},
      {rarity:"RARE",icon:"🔵",mult:3,color:"#60a5fa"},
      {rarity:"EPIC",icon:"🟣",mult:4,color:"#c084fc"},
      {rarity:"LEGENDARY",icon:"🟡",mult:5,color:"#ffd700"},
    ];
    const pickBounty=()=>{
      if(!gsRef.current||gsRef.current.lives<=0)return;
      const b=BOUNTY_POOL[Math.floor(Math.random()*BOUNTY_POOL.length)];
      const bd={...b,expiresAt:Date.now()+35000};
      bountyRef.current=bd;setBountyData(bd);
      bountyTimerRef.current=setTimeout(()=>{bountyRef.current=null;setBountyData(null);bountyTimerRef.current=setTimeout(pickBounty,5000);},35000);
    };
    bountyTimerRef.current=setTimeout(pickBounty,12000);

    const extraLife=shopCart.includes("extra_life");
    const headStart=shopCart.includes("head_start");
    const shieldStart=shopCart.includes("shield_start");
    const powerPack=shopCart.includes("power_pack");
    const feverPotion=shopCart.includes("fever_potion");
    const xpBomb=shopCart.includes("xp_bomb");
    const streakSaver=shopCart.includes("streak_saver");
    const timeWarpStart=shopCart.includes("time_warp_start");
    const doubleSpawn=shopCart.includes("double_spawn");

    const isRescue=rescuedRef.current;rescuedRef.current=false;
    tensionRef.current=0;setTensionLevel(0);setBonusRound(false);setMysteryReveal(null);
    setStreakDecaying(false);if(decayTimerRef.current){clearInterval(decayTimerRef.current);decayTimerRef.current=null;}
    // Mascot level bonuses
    const mascotId=saveRef.current.mascotId||"dragon";
    const mascotXP=saveRef.current.mascotXP||{};
    const mLevel=Math.min(20,Math.floor((mascotXP[mascotId]||0)/500));
    const hasStreakShield=mLevel>=10;
    const headStartBonus=(mLevel>=15?100:0)+(headStart?300:0);
    const frLvl2=(saveRef.current.skills||{}).fever_rush||0;
    const feverMult2=frLvl2===3?1.6:frLvl2===2?1.4:frLvl2===1?1.2:1.0;
    gsRef.current={
      score:isRescue?400:headStartBonus>0?headStartBonus:0,
      lives:isRescue?MAX_LIVES:Math.min(MAX_LIVES,cfg.lives+(extraLife?1:0)),
      streak:feverPotion?FEVER_STREAK:0,
      feverActive:!!feverPotion, feverTimeLeft:feverPotion?Math.round(FEVER_DUR*feverMult2*1.5):0,
      startTime:Date.now(), lastTapTime:Date.now(),
      sessionStats:{tapsTotal:0,rareHits:0,bestCombo:0,score:0,feverCount:feverPotion?1:0,powerupCollected:0,bossKills:0,perfectTaps:0,missedTargets:0},
      _hitLog:[], // [{t:timestamp, hit:bool}] for adaptive difficulty
      _adaptNextEval:Date.now()+4000, // first eval after 4 seconds
      _adaptMult:1.0, // current adaptive multiplier (0.7-1.3)
      _xpBombActive:!!xpBomb, // doubles XP at level end
      _streakSaverActive:!!streakSaver, // saves streak once on miss
      _timeWarpEndsAt:timeWarpStart?Date.now()+6000:null,
      _doubleSpawnActive:!!doubleSpawn,
      _lightBombCount:0, // tracks light bombs tapped this level
    };
    if(hasStreakShield&&!isRescue){streakShRef.current=true;setStreakShieldActive(true);}
    if(shieldStart)activePwrRef.current=[{type:"SHIELD",endsAt:Date.now()+25000}];
    if(powerPack){const pt=["SLOW","DOUBLE","SHIELD","FREEZE"];const ty=pt[Math.floor(Math.random()*pt.length)];if(!activePwrRef.current.find(p=>p.type===ty))activePwrRef.current.push({type:ty,endsAt:Date.now()+12000});}
    if(feverPotion){setFeverBorder(true);sfx("feverStart");}
    if(timeWarpStart){activePwrRef.current.push({type:"TIME_WARP",endsAt:Date.now()+6000});setTimeout(()=>setNotif("⏱️ Time Warp active!"),600);}
    if(doubleSpawn){setTimeout(()=>setNotif("🌊 Target Rush! 2× spawns!"),600);}
    setActivePwrDisp([...activePwrRef.current]);
    setHud({score:headStart?300:0,lives:gsRef.current.lives,streak:0,fever:false,coins:saveRef.current.coins,timeLeft:null,modGoal:cfg.modifier?.desc||null});
    setLevelCompleteData(null);setGameOverData(null);setEpicFlash(false);setFeverBorder(false);setComboLabel("");
    setCartItems([]);setScreen("playing");setMascotMood("idle");setMascotSpeech(null);setMascotBounce(false);
    if(soundOn&&audioRef.current?.startBgMusic)audioRef.current.startBgMusic(cfg.world);
    // 11B — Boss warning on levels ending in 9 (boss next)
    if(!cfg.isInfinity&&!cfg.isZen&&typeof levelId==="number"&&levelId%10===9){
      const nextWld=WORLDS[Math.floor(levelId/10)%10];
      setTimeout(()=>setNotif(`👑 Next level: ${nextWld?.emoji||""} BOSS BATTLE!`),2200);
    }
    runCountdown(()=>{lastTickRef.current=performance.now();rafRef.current=requestAnimationFrame(gl=>gameLoopFn(gl));});
  },[initMissions,initBgParts,runCountdown,soundOn]); // eslint-disable-line

  // Lucky golden event — all targets turn legendary for 7 seconds
  const triggerLucky=useCallback(()=>{
    if(!gsRef.current||pausedRef.current)return;
    luckyRef.current="active";setLuckyMode(true);
    sfx("lucky");
    spawnPopup(window.innerWidth/2,window.innerHeight/2-60,"⭐ LUCKY STARS! ⭐","#ffd700",24);
    setTimeout(()=>{luckyRef.current=null;setLuckyMode(false);
      // Schedule next lucky event 50-80s later
      luckyTimer.current=setTimeout(()=>triggerLucky(),50000+Math.random()*30000);
    },7000);
  },[sfx,spawnPopup]);// eslint-disable-line
  // Keep ref in sync for forward-reference calls from activatePowerUp
  React.useEffect(()=>{triggerLuckyRef.current=triggerLucky;},[triggerLucky]);

  // Pause
  const togglePause=useCallback(()=>{
    if(!gsRef.current)return;
    const next=!pausedRef.current;pausedRef.current=next;setPaused(next);
    if(!next){lastTickRef.current=performance.now();rafRef.current=requestAnimationFrame(gl=>gameLoopFn(gl));}
  },[]);// eslint-disable-line

  useEffect(()=>()=>{if(rafRef.current)cancelAnimationFrame(rafRef.current);},[]);

  // Menu canvas loop
  useEffect(()=>{
    if(screen!=="menu"&&screen!=="levelmap"&&screen!=="shop"&&screen!=="levelcomplete"&&screen!=="gameover"&&screen!=="spinwheel"&&screen!=="missions"&&screen!=="achievements"&&screen!=="leaderboard"&&screen!=="settings"&&screen!=="infinity"&&screen!=="gauntlet"&&screen!=="mascotcollection"&&screen!=="weekly"&&screen!=="tournament"&&screen!=="skilltree"&&screen!=="zen"&&screen!=="timeattack")return;
    let raf;
    const loop=(ts)=>{
      const canvas=canvasRef.current;if(!canvas)return;
      const ctx=canvas.getContext("2d");
      const th=THEMES.find(t=>t.id===(saveRef.current.themeId||"neon"))||THEMES[0];
      drawBg(ctx,canvas.width,canvas.height,th.accent,th.grid,ts,false);
      drawBgParticles(ctx,bgPartsRef.current,th.accent,false);
      drawVignette(ctx,canvas.width,canvas.height,th.accent);
      drawRipples(ctx,ripplesRef.current);
      raf=requestAnimationFrame(loop);
    };
    raf=requestAnimationFrame(loop);return()=>cancelAnimationFrame(raf);
  },[screen]);

  // Game loop function (stored in ref to avoid stale closure issues)
  const gameLoopFn=useCallback((ts)=>{
    const gs=gsRef.current;
    if(!gs||pausedRef.current)return;
    const dt=Math.min(100,ts-lastTickRef.current);lastTickRef.current=ts;
    const canvas=canvasRef.current;if(!canvas)return;
    const ctx=canvas.getContext("2d");
    const w=canvas.width,h=canvas.height;
    const fever=gs.feverActive;
    const cfg=levelCfgRef.current;
    const th=THEMES.find(t=>t.id===(saveRef.current.themeId||"neon"))||THEMES[0];
    // Use world grid color when available
    const gridColor=cfg?WORLDS[cfg.world-1].grid:th.grid;
    const accentColor=cfg?WORLDS[cfg.world-1].color:th.accent;

    drawBg(ctx,w,h,accentColor,gridColor,ts,fever,cfg?cfg.world:0);
    drawBgParticles(ctx,bgPartsRef.current,accentColor,fever,cfg?cfg.world:0);
    drawRipples(ctx,ripplesRef.current);
    drawTapTrail(ctx,tapTrailRef.current,accentColor,gsRef.current?.streak||0);

    // Particles
    const pnow=performance.now();
    particlesRef.current=particlesRef.current.filter(p=>{
      const age=pnow-p.born;if(age>=p.duration)return false;
      if(p.type!=="shockwave"&&p.type!=="popup"){
        p.x+=p.vx*(p.type==="spark"?1:0.91);p.y+=p.vy*(p.type==="spark"?1:0.91);p.vy+=p.type==="spark"?0.22:0.12;
      }
      drawParticle(ctx,p,pnow);return true;
    });

    // Targets
    const now=Date.now();let lostLife=false;
    // Restore anchor-frozen target velocities when freeze expires
    const anchorFreezeActive=gsRef.current&&gsRef.current._anchorFreezeUntil&&gsRef.current._anchorFreezeUntil>now;
    if(!anchorFreezeActive){
      targetsRef.current.forEach(t=>{
        if(t._frozenUntil&&t._frozenUntil<=now){
          t.vx=t._anchorFrozenVx||0;t.vy=t._anchorFrozenVy||0;
          delete t._anchorFrozenVx;delete t._anchorFrozenVy;delete t._frozenUntil;
        }
      });
    }
    targetsRef.current=targetsRef.current.filter(t=>{
      // Move (skip if anchor-frozen)
      if(t._frozenUntil&&t._frozenUntil>now){drawTarget(ctx,t,ts);return true;}
      if(t.moving&&!activePwrRef.current.some(p=>p.type==="FREEZE"&&p.endsAt>now)){
        if(t.type==="boss"&&t.bossPattern!=null){
          // Boss-specific movement patterns, scaled by phase speed
          const phaseSpeed=t.bossPhase===3?1.8:t.bossPhase===2?1.35:1.0;
          const margin=t.radius+5;
          if(t.bossPattern===0){
            // Orbit around spawn anchor
            const orbR=Math.min(80,Math.min(w,h)*0.18);
            const omega=0.0011*phaseSpeed;
            t.bossAngle=(t.bossAngle||0)+omega*dt;
            t.x=(t.cx||t.x)+Math.cos(t.bossAngle)*orbR;
            t.y=(t.cy||t.y)+Math.sin(t.bossAngle)*orbR*0.7;
          } else if(t.bossPattern===1){
            // Bounce (default)
            t.x+=t.vx*dt*0.056*phaseSpeed;t.y+=t.vy*dt*0.056*phaseSpeed;
            if(t.x<margin||t.x>w-margin){t.vx*=-1;t.x=Math.max(margin,Math.min(w-margin,t.x));}
            if(t.y<t.radius+95||t.y>h-margin){t.vy*=-1;t.y=Math.max(t.radius+95,Math.min(h-margin,t.y));}
          } else if(t.bossPattern===2){
            // Vertical wave drift
            t.x+=t.vx*dt*0.056*phaseSpeed;
            t.y=(t.cy||t.y)+Math.sin((performance.now()-t.born)*0.0025*phaseSpeed)*45;
            if(t.x<margin||t.x>w-margin){t.vx*=-1;t.x=Math.max(margin,Math.min(w-margin,t.x));}
          } else {
            // Zigzag — sudden direction change every ~700ms
            t.bossZigT=(t.bossZigT||0)+dt;
            if(t.bossZigT>700/phaseSpeed){
              const a=Math.random()*Math.PI*2;const sp=1.2*phaseSpeed;
              t.vx=Math.cos(a)*sp;t.vy=Math.sin(a)*sp;t.bossZigT=0;
            }
            t.x+=t.vx*dt*0.056;t.y+=t.vy*dt*0.056;
            if(t.x<margin||t.x>w-margin){t.vx*=-1;t.x=Math.max(margin,Math.min(w-margin,t.x));}
            if(t.y<t.radius+95||t.y>h-margin){t.vy*=-1;t.y=Math.max(t.radius+95,Math.min(h-margin,t.y));}
          }
        } else {
          const timeWarpMult=gs._timeWarpEndsAt&&Date.now()<gs._timeWarpEndsAt?0.2:1;
          // Comet and Ice Comet fly straight without bouncing
          if(t.type==="comet"||t.type==="icecomet"){
            t.x+=t.vx*dt*0.072*timeWarpMult;t.y+=t.vy*dt*0.072*timeWarpMult;
            // Off-screen removal (handled by expiry)
          } else
          // Rage target accelerates over its lifetime
          if(t.type==="rage"){
            const rl=Math.min(1,(now-t.spawnedAt)/(t.lifetime*0.9));
            const speedScale=0.056*(1+rl*3.5)*timeWarpMult; // up to 4.5× faster at max rage
            t.x+=t.vx*dt*speedScale;t.y+=t.vy*dt*speedScale;
          } else if(t.type==="homing"){
            // Homing: gently steers toward screen center
            const cx2=w/2,cy2=Math.min(h*0.52,h-80);
            const dx2=cx2-t.x,dy2=cy2-t.y;
            const dist2=Math.hypot(dx2,dy2)||1;
            const steer=0.018*timeWarpMult;
            t.vx+=dx2/dist2*steer;t.vy+=dy2/dist2*steer;
            const spd=Math.hypot(t.vx,t.vy);if(spd>1.4){t.vx=t.vx/spd*1.4;t.vy=t.vy/spd*1.4;}
            t.x+=t.vx*dt*0.056*timeWarpMult;t.y+=t.vy*dt*0.056*timeWarpMult;
          } else {
            t.x+=t.vx*dt*0.056*timeWarpMult;t.y+=t.vy*dt*0.056*timeWarpMult;
          }
          // MAGNET FIELD — all non-boss targets drift toward last tap position
          if(gs._magnetFieldEndsAt&&Date.now()<gs._magnetFieldEndsAt&&t.type!=="boss"&&t.type!=="bomb"&&t.type!=="siphon"){
            const mfx=gs._magnetFieldX||w/2,mfy=gs._magnetFieldY||h/2;
            const mdx=mfx-t.x,mdy=mfy-t.y;
            const mDist=Math.hypot(mdx,mdy)||1;
            if(mDist>30){
              const pull=0.012;
              t.vx+=mdx/mDist*pull;t.vy+=mdy/mDist*pull;
              const spd=Math.hypot(t.vx,t.vy);if(spd>1.8){t.vx=t.vx/spd*1.8;t.vy=t.vy/spd*1.8;}
            }
          }
          const margin=t.radius+5;
          if(t.x<margin||t.x>w-margin){t.vx*=-1;t.x=Math.max(margin,Math.min(w-margin,t.x));}
          if(t.y<t.radius+95||t.y>h-margin){t.vy*=-1;t.y=Math.max(t.radius+95,Math.min(h-margin,t.y));}
        }
        if(t.trail){t.trail.push({x:t.x,y:t.y});if(t.trail.length>12)t.trail.shift();}
      }
      // Glitch teleport — jumps to a random valid position every 0.9s
      if(t.type==="glitch"&&!t.dying){
        if(!t._lastGlitch)t._lastGlitch=now;
        if(now-t._lastGlitch>=900){
          t._lastGlitch=now;
          const margin=t.radius+10;
          t.x=margin+Math.random()*(w-margin*2);
          t.y=(t.radius+100)+Math.random()*(h-(t.radius+100)-margin);
          spawnParticles(t.x,t.y,"#00ff88",5,"spark");
        }
      }
      // Edge Danger Zone — stationary normal targets within 20px of screen edges flash and explode after 1.5s
      if(!t.moving&&!t.dying&&t.type==="normal"&&!t._isShard&&!cfg?.isZen){
        const EDGE=22;
        const nearEdge=t.x<EDGE||t.x>w-EDGE||t.y<t.radius+95+EDGE||t.y>h-EDGE;
        if(nearEdge){
          if(!t._edgeWarnAt)t._edgeWarnAt=now;
          const edgeAge=now-t._edgeWarnAt;
          if(edgeAge>=1500){
            // Explode! Cost a life if no shield
            spawnParticles(t.x,t.y,"#ef4444",18,"spark");
            spawnPopup(t.x,t.y-24,"⚠️ EDGE!","#ef4444",16);
            sfx("miss");vibrate([20,10,20]);
            const hasShieldE=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now);
            if(hasShieldE){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);}
            else if(streakShRef.current){streakShRef.current=false;setStreakShieldActive(false);sfx("shieldBreak");}
            else{gs.lives--;gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);setMascotMood("sad");lostLife=true;}
            return false;
          }
        } else {
          t._edgeWarnAt=null;
        }
      }
      // Pinwheel — circular orbit around a fixed anchor point
      if(t.type==="pinwheel"&&!t.dying){
        if(!t._orbitAngle)t._orbitAngle=Math.random()*Math.PI*2;
        if(!t._orbitCx){t._orbitCx=t.x;t._orbitCy=Math.max(h*0.3,Math.min(h*0.7,t.y));}
        if(!t._orbitR)t._orbitR=50+Math.random()*60;
        t._orbitAngle+=0.025*dt;
        t.x=t._orbitCx+Math.cos(t._orbitAngle)*t._orbitR;
        t.y=t._orbitCy+Math.sin(t._orbitAngle)*t._orbitR*0.5; // elliptical
        t.x=Math.max(t.radius+10,Math.min(w-t.radius-10,t.x));
        t.y=Math.max(t.radius+95,Math.min(h-t.radius-10,t.y));
      }
      // Supercell — electrifies nearby normal targets (gives them _supercellBoosted flag)
      if(t.type==="supercell"&&!t.dying){
        const CELL_R=160;
        if(!t._lastCellZap||now-t._lastCellZap>2000){
          t._lastCellZap=now;
          targetsRef.current.forEach(other=>{
            if(other===t||other.dying||other.type!=="normal"||other._supercellBoosted)return;
            if(Math.hypot(other.x-t.x,other.y-t.y)<CELL_R){
              other._supercellBoosted=Date.now()+6000;
              spawnParticles(other.x,other.y,"#7dd3fc",5,"spark");
            }
          });
        }
      }
      // Solar Flare — grows in radius over lifetime
      if(t.type==="solarflare"&&!t.dying){
        const growPct=Math.min(1,(now-t.spawnedAt)/(t.lifetime*0.9));
        t.radius=Math.round((BASE_R*1.0)*(1+growPct*1.8)); // grows up to 2.8× base
      }
      // Magma — slowly homes in on nearest normal target
      if(t.type==="magma"&&!t.dying){
        const nearest=targetsRef.current
          .filter(o=>o!==t&&!o.dying&&o.type==="normal"&&!o._isShard)
          .sort((a,b)=>Math.hypot(a.x-t.x,a.y-t.y)-Math.hypot(b.x-t.x,b.y-t.y))[0];
        if(nearest){
          const dx=nearest.x-t.x,dy=nearest.y-t.y;const dist=Math.hypot(dx,dy)||1;
          t.vx=(t.vx||0)+(dx/dist)*0.02*dt;t.vy=(t.vy||0)+(dy/dist)*0.02*dt;
          const spd=Math.hypot(t.vx,t.vy);if(spd>1.2){t.vx=t.vx/spd*1.2;t.vy=t.vy/spd*1.2;}
          // If magma touches a normal target, destroy it!
          if(dist<t.radius+nearest.radius){
            spawnParticles(nearest.x,nearest.y,"#dc2626",12,"spark");
            spawnPopup(nearest.x,nearest.y-20,"🌋 EATEN!","#dc2626",14);
            nearest.dying=performance.now();sfx("bombHit");vibrate([15,8,15]);
          }
        }
      }
      // Quantum — micro-teleports every 1.8s when alive (between player taps)
      if(t.type==="quantum"&&!t.dying){
        if(!t._lastQTeleport)t._lastQTeleport=now;
        if(now-t._lastQTeleport>=1800){
          t._lastQTeleport=now;
          const margin=t.radius+20;
          t.x=margin+Math.random()*(w-margin*2);
          t.y=(t.radius+100)+Math.random()*(h-(t.radius+100)-margin-30);
          spawnParticles(t.x,t.y,"#a78bfa",4,"spark");
        }
      }
      // Gravity Orb — pulls ALL non-bomb targets toward its center
      if(t.type==="gravityorb"&&!t.dying){
        const GORB_R=160,GORB_STR=0.022;
        targetsRef.current.forEach(other=>{
          if(other===t||other.dying||other.type==="boss"||other.type==="bomb"||other.type==="timebomb")return;
          const dx=t.x-other.x,dy=t.y-other.y;
          const dist=Math.hypot(dx,dy);
          if(dist>0&&dist<GORB_R){
            const force=GORB_STR*(1-dist/GORB_R)*dt;
            if(!other._gravLocked){other.vx=(other.vx||0)+(dx/dist)*force*2;other.vy=(other.vy||0)+(dy/dist)*force*2;other.moving=true;}
          }
        });
      }
      // Vortex — pulls ALL non-bomb targets inward; counts pulled for bonus scoring
      if(t.type==="vortex"&&!t.dying){
        const VTX_R=150,VTX_STR=0.028;
        let pulled=0;
        targetsRef.current.forEach(other=>{
          if(other===t||other.dying||other.type==="boss"||other.type==="bomb"||other.type==="timebomb"||other.type==="vortex")return;
          const dx=t.x-other.x,dy=t.y-other.y;
          const dist=Math.hypot(dx,dy);
          if(dist>0&&dist<VTX_R){
            pulled++;const force=VTX_STR*(1-dist/VTX_R)*dt;
            other.vx=(other.vx||0)+(dx/dist)*force*2.4;other.vy=(other.vy||0)+(dy/dist)*force*2.4;other.moving=true;
            // If a target is pulled into the vortex core, consume it for bonus pts
            if(dist<t.radius*0.6&&!other._vtxConsumed){
              other._vtxConsumed=true;other.dying=performance.now();
              t._vtxConsumed=(t._vtxConsumed||0)+1;
              const bonusPts=120;gsRef.current.score+=bonusPts;
              spawnParticles(other.x,other.y,"#a78bfa",8,"spark");
              spawnPopup(other.x,other.y-12,`+${bonusPts}`,"#a78bfa",13);
            }
          }
        });
        t._vtxPulled=pulled;
      }
      // Accumulator — radius grows with stacks (max 8 stacks = 2× base radius)
      if(t.type==="accumulator"&&!t.dying){
        const sc=Math.min(8,t._stacks||0);
        // radius grows with stacks
        t.radius=t._baseAccRadius?(t._baseAccRadius*(1+sc*0.12)):t.radius;
      }
      // Bouncing Orb — counts wall bounces for score multiplier (max 8×)
      if(t.type==="bouncing_orb"&&!t.dying){
        const cw=canvasRef.current?.width||390;const ch=canvasRef.current?.height||700;
        const margin=t.radius+4;
        let bounced=false;
        if(t.x-margin<=0||t.x+margin>=cw){t.vx=-t.vx;t.x=Math.max(margin,Math.min(cw-margin,t.x));bounced=true;}
        if(t.y-margin<=90||t.y+margin>=ch-margin){t.vy=-t.vy;t.y=Math.max(90+margin,Math.min(ch-margin,t.y));bounced=true;}
        if(bounced){t._bounces=(t._bounces||0)+1;spawnParticles(t.x,t.y,"#4ade80",3,"spark");}
      }
      // Energy Web — counts how many non-bomb targets are within web range
      if(t.type==="energy_web"&&!t.dying){
        let nodes=0;
        targetsRef.current.forEach(other=>{
          if(other===t||other.dying||other.type==="boss"||other.type==="bomb"||other.type==="timebomb")return;
          const dist=Math.hypot(other.x-t.x,other.y-t.y);
          if(dist<160)nodes++;
        });
        t._webNodes=nodes;
      }
      // Void Rift — devours nearby targets into itself for massive pts
      if(t.type==="void_rift"&&!t.dying){
        const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
        const VR_R=80+agePct*120,VR_STR=0.035+agePct*0.04;
        targetsRef.current.forEach(other=>{
          if(other===t||other.dying||other.type==="boss"||other.type==="bomb"||other.type==="timebomb"||other.type==="void_rift")return;
          const dx=t.x-other.x,dy=t.y-other.y;
          const dist=Math.hypot(dx,dy);
          if(dist>0&&dist<VR_R){
            const force=VR_STR*(1-dist/VR_R)*dt;
            other.vx=(other.vx||0)+(dx/dist)*force*3.5;other.vy=(other.vy||0)+(dy/dist)*force*3.5;other.moving=true;
            // devour into core
            if(dist<t.radius*0.7&&!other._vrDevoured){
              other._vrDevoured=true;other.dying=performance.now();
              t._devouredCount=(t._devouredCount||0)+1;
              const bonus=150;gsRef.current.score+=bonus;
              spawnParticles(other.x,other.y,"#7c3aed",6,"spark");
              spawnPopup(other.x,other.y-12,`+${bonus}`,"#a78bfa",11);
            }
          }
        });
      }
      // Quantum Leap — teleports to a new random canvas position every 1200ms
      if(t.type==="quantum_leap"&&!t.dying){
        if(!t._qlLastJump)t._qlLastJump=t.spawnedAt;
        if(now-t._qlLastJump>1200){
          const cw=canvasRef.current?.width||390;const ch=canvasRef.current?.height||700;
          t._qlLastJump=now;
          t.x=t.radius*1.2+Math.random()*(cw-t.radius*2.4);
          t.y=90+t.radius*1.2+Math.random()*(ch-90-t.radius*2.4);
          t._qlTrailAlpha=1.0;
          spawnParticles(t.x,t.y,"#818cf8",5,"spark");
        }
        if(t._qlTrailAlpha>0)t._qlTrailAlpha=Math.max(0,t._qlTrailAlpha-dt*0.003);
      }
      // Lightning Rod — attracts nearby normal targets; counts attracted for scoring
      if(t.type==="lightning_rod"&&!t.dying){
        const LR_R=140,LR_STR=0.022;
        let attracted=0;
        targetsRef.current.forEach(other=>{
          if(other===t||other.dying||other.type==="boss"||other.type==="bomb"||other.type==="timebomb"||other.type==="lightning_rod")return;
          const dx=t.x-other.x,dy=t.y-other.y;
          const dist=Math.hypot(dx,dy);
          if(dist>0&&dist<LR_R){
            attracted++;
            const force=LR_STR*(1-dist/LR_R)*dt;
            other.vx=(other.vx||0)+(dx/dist)*force*1.8;other.vy=(other.vy||0)+(dy/dist)*force*1.8;other.moving=true;
          }
        });
        t._rodAttract=attracted;
      }
      // Tesla Node — auto-zaps nearest normal target every 1200ms, banking 80pts
      if(t.type==="tesla_node"&&!t.dying){
        const nowT=Date.now();
        if(!t._lastZapAt||nowT-t._lastZapAt>1200){
          t._lastZapAt=nowT;
          let nearest=null,nearDist=Infinity;
          targetsRef.current.forEach(other=>{
            if(other===t||other.dying||other.type!=="normal")return;
            const d=Math.hypot(other.x-t.x,other.y-t.y);
            if(d<200&&d<nearDist){nearDist=d;nearest=other;}
          });
          if(nearest){
            nearest.dying=performance.now();
            t._bank=(t._bank||0)+80;
            spawnParticles(nearest.x,nearest.y,"#38bdf8",4,"spark");
            // Arc particle line toward node
            particlesRef.current.push({type:"dot",x:(nearest.x+t.x)/2,y:(nearest.y+t.y)/2,vx:0,vy:-0.8,alpha:0.9,size:3,color:"#7dd3fc",born:performance.now(),life:350});
          }
        }
      }
      // Plasma Storm — tracks how many targets are within shock range
      if(t.type==="plasma_storm"&&!t.dying){
        let nearby=0;
        targetsRef.current.forEach(other=>{
          if(other===t||other.dying||other.type==="boss"||other.type==="bomb"||other.type==="timebomb")return;
          if(Math.hypot(other.x-t.x,other.y-t.y)<150)nearby++;
        });
        t._stormCount=nearby;
      }
      // Void Shard — absorbs a few pts from nearby normal targets every 600ms
      if(t.type==="void_shard"&&!t.dying){
        const now2=Date.now();
        if(!t._lastAbsorbAt||now2-t._lastAbsorbAt>600){
          t._lastAbsorbAt=now2;
          targetsRef.current.forEach(other=>{
            if(other===t||other.dying||other.type!=="normal"||Date.now()<other.spawnedAt)return;
            const dist=Math.hypot(other.x-t.x,other.y-t.y);
            if(dist<120){
              const stolen=5;
              t._absorbedPts=(t._absorbedPts||0)+stolen;
            }
          });
        }
      }
      // Gravity Well — pulls nearby targets with increasing force as it ages
      if(t.type==="gravity_well"&&!t.dying){
        const agePct=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||4200));
        const GW_R=130+agePct*60,GW_STR=0.015+agePct*0.03;
        targetsRef.current.forEach(other=>{
          if(other===t||other.dying||other.type==="boss"||other.type==="bomb"||other.type==="timebomb"||other.type==="gravity_well")return;
          const dx=t.x-other.x,dy=t.y-other.y;
          const dist=Math.hypot(dx,dy);
          if(dist>0&&dist<GW_R){
            const force=GW_STR*(1-dist/GW_R)*dt;
            other.vx=(other.vx||0)+(dx/dist)*force*2.0;other.vy=(other.vy||0)+(dy/dist)*force*2.0;other.moving=true;
            if(!t._gwPulled)t._gwPulled=new Set();
            t._gwPulled.add(other.id);
          }
        });
      }
      // Wisp — alternates between drifting and stationary every 2.2s
      if(t.type==="wisp"&&!t.dying){
        if(!t._wispPhaseAt)t._wispPhaseAt=now;
        if(now-t._wispPhaseAt>2200){
          t._wispPhaseAt=now;
          if(t.moving){t._preMoveVx=t.vx;t._preMoveVy=t.vy;t.vx=0;t.vy=0;t.moving=false;}
          else{t.vx=t._preMoveVx||(Math.random()-0.5)*1.2;t.vy=t._preMoveVy||(Math.random()-0.5)*1.2;t.moving=true;}
        }
      }
      // Drift — waves path; no extra logic needed beyond bounce (vx/vy set at spawn)
      // MAGNET_PULL power-up — attract targets toward last tap position
      if(activePwrRef.current.some(p=>p.type==="MAGNET_PULL"&&p.endsAt>Date.now())&&t.type==="normal"&&!t.dying&&!t.moving){
        const gs2=gsRef.current;
        if(gs2?._lastTapX!==undefined&&gs2?._lastTapY!==undefined){
          const dx2=gs2._lastTapX-t.x,dy2=gs2._lastTapY-t.y;
          const dist2=Math.hypot(dx2,dy2);
          if(dist2>20&&dist2<250){const force=0.008*(1-dist2/250)*dt;t.x+=dx2/dist2*force*50;t.y+=dy2/dist2*force*50;}
        }
      }
      // BOUNCE WEB snare release — restore velocity after 2s
      if(t._snaredUntil&&now>t._snaredUntil){
        t.vx=t._savedVx||((Math.random()-0.5)*1.4);t.vy=t._savedVy||((Math.random()-0.5)*1.4);
        t.moving=true;delete t._snaredUntil;delete t._savedVx;delete t._savedVy;
      }
      // FREEZE_ZONE — slow targets near last tap
      if(activePwrRef.current.some(p=>p.type==="FREEZE_ZONE"&&p.endsAt>Date.now())&&t.type==="normal"&&!t.dying&&t.moving){
        const gs3=gsRef.current;
        if(gs3?._lastTapX!==undefined&&Math.hypot(gs3._lastTapX-t.x,gs3._lastTapY-t.y)<120){
          t.vx*=0.3;t.vy*=0.3;
        }
      }
      // SCORE_X2 handled in pts calc
      // RUSH_MODE — mark a random normal target as gold every 3s
      {const pRush=activePwrRef.current.find(p=>p.type==="RUSH_MODE"&&p.endsAt>now);
      if(pRush&&now-pRush.lastRush>=3000){
        pRush.lastRush=now;
        const normals=targetsRef.current.filter(o=>!o.dying&&o.type==="normal"&&!o._rushedUntil);
        if(normals.length>0){
          const picked=normals[Math.floor(Math.random()*normals.length)];
          picked._rushedUntil=now+1200;
          spawnParticles(picked.x,picked.y,"#fcd34d",8,"spark");
        }
      }}
      // SCORE_PULSE — tick every 1s, grant +50 pts
      // QUANTUM LEECH — drains -10 pts/s from score while alive
      if(t.type==="quantum_leech"&&!t.dying){
        if(!t._lastDrainTick)t._lastDrainTick=now;
        if(now-t._lastDrainTick>=1000){
          t._lastDrainTick=now;
          const gs45=gsRef.current;
          if(gs45&&gs45.score>0){
            gs45.score=Math.max(0,gs45.score-10);
            spawnPopup(t.x,t.y-18,"🧿 -10","#4ade80",11);
          }
        }
      }
      {const pPulse=activePwrRef.current.find(p=>p.type==="SCORE_PULSE"&&p.endsAt>now);
      if(pPulse&&now-pPulse.lastTick>=1000){
        pPulse.lastTick=now;
        const gs5=gsRef.current;if(gs5){gs5.score+=50;}
        spawnPopup(w/2,h*0.3+Math.random()*30-15,"💫 +50","#fbbf24",13);
      }}
      // SCORE_RAIN — random score drops every ~600ms
      {const sRain=activePwrRef.current.find(p=>p.type==="SCORE_RAIN"&&p.endsAt>now);
      if(sRain&&now-(sRain.lastDrop||0)>=600){
        sRain.lastDrop=now;
        const gs5r=gsRef.current;
        if(gs5r){
          const rainPts=20+Math.floor(Math.random()*61);
          gs5r.score+=rainPts;
          const rx=w*0.15+Math.random()*(w*0.7);
          const ry=h*0.1+Math.random()*(h*0.4);
          spawnPopup(rx,ry,`🌧️⭐ +${rainPts}`,"#fde68a",13);
          spawnParticles(rx,ry,"#fbbf24",4,"spark");
        }
      }}
      // FEVER_LOCK — enforce fever active while power lasts
      {const flPwr62=activePwrRef.current.find(p=>p.type==="FEVER_LOCK"&&p.endsAt>now);
      if(flPwr62){
        if(!gs.feverActive){
          gs.feverActive=true;gs.feverTimeLeft=Math.max(100,flPwr62.endsAt-now);
          if(!flPwr62.triggered){flPwr62.triggered=true;unlock("fever_lock_save");}
        }
      }}
      // SCORE_FREEZE — enforce score floor each tick
      {const sfPwr57=activePwrRef.current.find(p=>p.type==="SCORE_FREEZE"&&p.endsAt>now);
      if(sfPwr57&&gs.score<sfPwr57.scoreFloor){
        const diff57=sfPwr57.scoreFloor-gs.score;
        gs.score=sfPwr57.scoreFloor;
        spawnPopup(w*0.5,h*0.35,`❄️🔒 +${diff57} FROZEN`,"#67e8f9",14);
        unlock("score_freeze_save");
      }}
      // PHASE_LOCK — restore target velocities when expired
      if(t._prePhaseLockVx!==undefined&&!activePwrRef.current.some(p=>p.type==="PHASE_LOCK"&&p.endsAt>now)){
        t.vx=t._prePhaseLockVx;t.vy=t._prePhaseLockVy;t.moving=!!(t.vx||t.vy);
        delete t._prePhaseLockVx;delete t._prePhaseLockVy;
      }
      // SCORE_SURGE — reset mult if streak broken
      {const ssPwr50=activePwrRef.current.find(p=>p.type==="SCORE_SURGE"&&p.endsAt>now);
      if(ssPwr50&&(gsRef.current?.streak||0)===0&&ssPwr50.mult>1){
        ssPwr50.mult=1;ssPwr50.tapCount=0;
        spawnPopup(w/2,h*0.35,"💹 SURGE RESET","#ef4444",14);
      }}
      // CHAIN_MAGNET — auto-tap nearest normal target 600ms after last player tap (once per tap)
      {const cmPwr50=activePwrRef.current.find(p=>p.type==="CHAIN_MAGNET"&&p.endsAt>now);
      if(cmPwr50&&cmPwr50.lastPullAt&&!cmPwr50._autoFired&&now-cmPwr50.lastPullAt>=600){
        cmPwr50._autoFired=true;
        if(!t.dying&&t.type==="normal"){
          const gs50c=gsRef.current;
          const txRef=gs50c?._lastTapX||w/2,tyRef=gs50c?._lastTapY||h/2;
          const nearestCM=targetsRef.current.filter(o=>!o.dying&&o.type==="normal").sort((a,b)=>Math.hypot(a.x-txRef,a.y-tyRef)-Math.hypot(b.x-txRef,b.y-tyRef))[0];
          if(nearestCM&&nearestCM.id===t.id){
            t.dying=performance.now();
            if(gs50c){gs50c.score+=80;gs50c.streak++;}
            spawnParticles(t.x,t.y,"#34d399",8,"spark");
            spawnPopup(t.x,t.y-20,"🧲 PULL! +80","#34d399",13);
          }
        }
      }
      if(cmPwr50&&cmPwr50._autoFired&&now-cmPwr50.lastPullAt>=700){cmPwr50._autoFired=false;}}
      // PERFECT_AIM — auto-hit nearest normal target every 2s (5 charges)
      if(!t.dying&&t.type==="normal"){
        const pAim=activePwrRef.current.find(p=>p.type==="PERFECT_AIM"&&p.endsAt>now&&(p.charges||0)>0);
        if(pAim&&(!pAim.lastAutoHit||now-pAim.lastAutoHit>2000)){
          // Only process for the nearest normal target
          const nearest=targetsRef.current.filter(o=>!o.dying&&o.type==="normal").sort((a,b)=>Math.hypot(a.x-(gsRef.current?._lastTapX||w/2),a.y-(gsRef.current?._lastTapY||h/2))-Math.hypot(b.x-(gsRef.current?._lastTapX||w/2),b.y-(gsRef.current?._lastTapY||h/2)))[0];
          if(nearest&&nearest.id===t.id){
            pAim.lastAutoHit=now;pAim.charges=(pAim.charges||1)-1;
            t.dying=performance.now();
            const gs4=gsRef.current;if(gs4){gs4.score+=150;gs4.streak++;gs4.lastTapTime=now;}
            spawnParticles(t.x,t.y,"#22d3ee",10,"spark");
            spawnPopup(t.x,t.y-20,`🎯 AUTO +150!`,"#22d3ee",14);
            if(pAim.charges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PERFECT_AIM");setActivePwrDisp([...activePwrRef.current]);}
            else setActivePwrDisp([...activePwrRef.current]);
          }
        }
      }
      // SHADOW ECHO — after 1.5s spawn a bright echo at offset position
      if(t.type==="shadow_echo"&&!t.dying&&!t._echoSpawned&&(Date.now()-t.spawnedAt)>=1500){
        t._echoSpawned=true;
        const cw51=canvasRef.current?.width||360;const ch51=canvasRef.current?.height||640;
        const ex=Math.max(30,Math.min(cw51-30,t.x+(Math.random()-0.5)*100));
        const ey=Math.max(30,Math.min(ch51-30,t.y+(Math.random()-0.5)*100));
        targetsRef.current.push({id:Math.random().toString(36).slice(2),type:"shadow_echo_bright",
          rarity:RARITY.EPIC,x:ex,y:ey,radius:BASE_R*1.1,
          color:"#fbbf24",glow:"#fde68a",lifetime:2000,spawnedAt:Date.now(),
          born:performance.now(),moving:false,ghost:false,vx:0,vy:0,
          trail:null,hitsLeft:1,maxHits:1,dying:null,
          worldId:t.worldId,worldColor:t.worldColor,
          bossPattern:null,bossPhase:1,cx:ex,cy:ey,
          anticipateMs:0,pwrType:null,_shadowEchoParent:t.id});
        spawnPopup(ex,ey-22,"🌕 ECHO!","#fbbf24",14);
      }
      // SPIRAL ORB — spiral outward from spawn point
      if(t.type==="spiral_orb"&&!t.dying){
        if(!t._spiralCx){t._spiralCx=t.x;t._spiralCy=t.y;t._spiralAngle=Math.random()*Math.PI*2;t._spiralRadius=30;}
        t._spiralAngle+=0.04*dt;
        t._spiralRadius=Math.min(150,t._spiralRadius+0.3*dt);
        const cw47s=canvasRef.current?.width||360;const ch47s=canvasRef.current?.height||640;
        t.x=Math.max(t.radius,Math.min(cw47s-t.radius,t._spiralCx+Math.cos(t._spiralAngle)*t._spiralRadius));
        t.y=Math.max(t.radius,Math.min(ch47s-t.radius,t._spiralCy+Math.sin(t._spiralAngle)*t._spiralRadius));
        t._spiralPct=t._spiralRadius/150;
      }
      // NEBULA CLOCK — spawn 5 orbiting dots after 1s
      if(t.type==="nebula_clock"&&!t.dying&&!t._dotsSpawned&&(Date.now()-t.spawnedAt)>=1000){
        t._dotsSpawned=true;t._dotsLeft=5;
        const cw63=canvasRef.current?.width||360;const ch63=canvasRef.current?.height||640;
        const dotIds63=[];
        for(let di=0;di<5;di++){
          const ang63=di*(Math.PI*2/5);
          const did63=Math.random().toString(36).slice(2);
          dotIds63.push(did63);
          const orbitR63=50+Math.random()*30;
          targetsRef.current.push({id:did63,type:"nebula_dot",rarity:null,
            x:Math.max(20,Math.min(cw63-20,t.x+Math.cos(ang63)*orbitR63)),
            y:Math.max(20,Math.min(ch63-20,t.y+Math.sin(ang63)*orbitR63)),
            radius:BASE_R*0.4,color:"#a78bfa",glow:"#ede9fe",
            lifetime:3500,spawnedAt:Date.now(),born:performance.now(),
            moving:false,ghost:false,vx:0,vy:0,trail:null,
            hitsLeft:1,maxHits:1,dying:null,worldId:t.worldId,worldColor:t.worldColor,
            bossPattern:null,bossPhase:1,cx:t.x,cy:t.y,anticipateMs:0,pwrType:null,
            _parentId:t.id,_orbitR:orbitR63,_orbitAng:ang63,_dotIdx:di});
        }
        t._dotIds=dotIds63;
        spawnPopup(t.x,t.y-28,"🌌 DOTS!","#818cf8",14);
      }
      // Nebula dots orbit the parent clock
      if(t.type==="nebula_dot"&&!t.dying){
        const parentNC63=targetsRef.current.find(o=>o.id===t._parentId);
        if(parentNC63){
          t._orbitAng=(t._orbitAng||0)+0.02*dt;
          const cw63d=canvasRef.current?.width||360;const ch63d=canvasRef.current?.height||640;
          t.x=Math.max(20,Math.min(cw63d-20,parentNC63.x+Math.cos(t._orbitAng)*(t._orbitR||50)));
          t.y=Math.max(20,Math.min(ch63d-20,parentNC63.y+Math.sin(t._orbitAng)*(t._orbitR||50)));
        }
      }
      // STAR CLOCK — auto-remove when countdown hits 0 with a miss penalty
      if(t.type==="star_clock"&&!t.dying&&(t._countdown||0)<=0&&(Date.now()-t.spawnedAt)>5000){
        t.dying=performance.now();
        const gs62sc=gsRef.current;
        if(gs62sc){gs62sc.lives=Math.max(0,gs62sc.lives-1);}
        spawnPopup(t.x,t.y-24,"⏰ MISSED!","#ef4444",18);
      }
      // STORM BALL — accelerates over lifetime; bounces off walls
      if(t.type==="storm_ball"&&!t.dying&&t.moving){
        const speedPct67sb=Math.min(1,(Date.now()-t.spawnedAt)/(t.lifetime||5000));
        const speed67sb=0.5+speedPct67sb*2.5;
        if(!t._sbAngle)t._sbAngle=Math.random()*Math.PI*2;
        t.vx=Math.cos(t._sbAngle)*speed67sb;t.vy=Math.sin(t._sbAngle)*speed67sb;
        const cw67sb=canvasRef.current?.width||360;const ch67sb=canvasRef.current?.height||640;
        if(t.x<t.radius||t.x>cw67sb-t.radius){t._sbAngle=Math.PI-t._sbAngle;t.x=Math.max(t.radius,Math.min(cw67sb-t.radius,t.x));}
        if(t.y<t.radius||t.y>ch67sb-t.radius){t._sbAngle=-t._sbAngle;t.y=Math.max(t.radius,Math.min(ch67sb-t.radius,t.y));}
      }
      // BLINK STAR — teleports every 0.7s; track blink time for crit window
      if(t.type==="blink_star"&&!t.dying){
        if(!t._nextBlink75)t._nextBlink75=Date.now()+700;
        if(Date.now()>=t._nextBlink75){
          const cw75b=canvasRef.current?.width||360;const ch75b=canvasRef.current?.height||640;
          t.x=Math.max(t.radius,Math.min(cw75b-t.radius,30+Math.random()*(cw75b-60)));
          t.y=Math.max(t.radius,Math.min(ch75b-t.radius,30+Math.random()*(ch75b-60)));
          t._nextBlink75=Date.now()+700;
          t._blinkAt75=Date.now(); // record blink time for crit window
        }
        t._justBlinked75=t._blinkAt75&&(Date.now()-t._blinkAt75)<250;
      }
      // SPIRAL ORB 74 — spiral motion around spawn point
      if(t.type==="spiral_orb74"&&!t.dying){
        const elapsed74=(Date.now()-t.spawnedAt)/1000;
        const spiralR74=40+elapsed74*12; // expanding spiral
        const angle74=elapsed74*2.5; // angular velocity
        const cx74=t.cx||t.x;const cy74=t.cy||t.y;
        if(!t._cx74){t._cx74=t.x;t._cy74=t.y;}
        t.x=t._cx74+Math.cos(angle74)*spiralR74;
        t.y=t._cy74+Math.sin(angle74)*spiralR74;
        const cw74s=canvasRef.current?.width||360;const ch74s=canvasRef.current?.height||640;
        t.x=Math.max(t.radius,Math.min(cw74s-t.radius,t.x));
        t.y=Math.max(t.radius,Math.min(ch74s-t.radius,t.y));
        t._atCenter74=spiralR74<25; // center zone = first ~0.5s
      }
      // NEON BOLT — fast straight-line motion; exits = despawn (no life loss)
      if(t.type==="neon_bolt"&&!t.dying&&t.moving){
        if(!t._boltAngle70){
          t._boltAngle70=Math.random()*Math.PI*2;
          t.vx=Math.cos(t._boltAngle70)*3.5;t.vy=Math.sin(t._boltAngle70)*3.5;
        }
        const cw70nb=canvasRef.current?.width||360;const ch70nb=canvasRef.current?.height||640;
        if(t.x<-t.radius||t.x>cw70nb+t.radius||t.y<-t.radius||t.y>ch70nb+t.radius){
          t.dying=performance.now(); // exits without life penalty
        }
      }
      // WARP ORB — teleports to random pos every 1.5s
      if(t.type==="warp_orb"&&!t.dying){
        if(!t._nextWarp66)t._nextWarp66=Date.now()+1500;
        const timeToWarp66=t._nextWarp66-Date.now();
        t._warping=timeToWarp66<200; // "warping" window 200ms before teleport
        if(Date.now()>=t._nextWarp66){
          const cw66w=canvasRef.current?.width||360;const ch66w=canvasRef.current?.height||640;
          t.x=Math.max(30,Math.min(cw66w-30,30+Math.random()*(cw66w-60)));
          t.y=Math.max(30,Math.min(ch66w-30,30+Math.random()*(ch66w-60)));
          t._nextWarp66=Date.now()+1500+Math.random()*500;
          t._warping=true;
          setTimeout(()=>{if(t)t._warping=false;},200);
        }
      }
      // QUANTUM DRIFT — random direction changes every 1.5s when not frozen
      if(t.type==="quantum_drift"&&!t.dying){
        const frozen61=t._frozenUntil&&Date.now()<t._frozenUntil;
        if(!frozen61){
          if(!t._driftTimer61||Date.now()>t._driftTimer61){
            const ang61=Math.random()*Math.PI*2;
            t.vx=Math.cos(ang61)*(0.6+Math.random()*0.8);
            t.vy=Math.sin(ang61)*(0.6+Math.random()*0.8);
            t.moving=true;
            t._driftTimer61=Date.now()+1200+Math.random()*600;
          }
        } else {
          t.vx=0;t.vy=0;t.moving=false;
        }
      }
      // PRISM NOVA — after 1.2s split into 3 colored shards
      if(t.type==="prism_nova"&&!t.dying&&!t._shardsSpawned&&(Date.now()-t.spawnedAt)>=1200){
        t._shardsSpawned=true;
        const cw60=canvasRef.current?.width||360;const ch60=canvasRef.current?.height||640;
        const shardIds60=[];
        const shardHues60=[0,120,240];
        for(let si=0;si<3;si++){
          const ang60=(si/3)*Math.PI*2;
          const sid60=Math.random().toString(36).slice(2);
          shardIds60.push(sid60);
          const sx60=Math.max(25,Math.min(cw60-25,t.x+Math.cos(ang60)*55));
          const sy60=Math.max(25,Math.min(ch60-25,t.y+Math.sin(ang60)*55));
          targetsRef.current.push({id:sid60,type:"prism_shard",rarity:null,
            x:sx60,y:sy60,
            radius:BASE_R*0.65,color:`hsl(${shardHues60[si]},100%,60%)`,glow:`hsl(${shardHues60[si]},100%,80%)`,
            lifetime:3500,spawnedAt:Date.now(),born:performance.now(),
            moving:false,ghost:false,vx:0,vy:0,trail:null,
            hitsLeft:1,maxHits:1,dying:null,worldId:t.worldId,worldColor:t.worldColor,
            bossPattern:null,bossPhase:1,cx:sx60,cy:sy60,anticipateMs:0,pwrType:null,
            _shardHue:shardHues60[si],_parentId:t.id});
        }
        t._shardIds=shardIds60;
        t._shardsTapped=0;
        spawnPopup(t.x,t.y-26,"🌈 SHARDS!","#f472b6",14);
      }
      // ECHO BLAST — decay echo ring after 1.5s
      if(t.type==="echo_blast"&&t._echoRing&&t._echoRingSpawnAt){
        if((Date.now()-t._echoRingSpawnAt)>=1500){t._echoRing=false;}
      }
      // CHRONO VORTEX — slow nearby targets each tick
      if(t.type==="chrono_vortex"&&!t.dying){
        const CV_R=180;
        let slowedCount59=0;
        targetsRef.current.forEach(other=>{
          if(other===t||other.dying||other.type==="boss")return;
          const dist59=Math.hypot(other.x-t.x,other.y-t.y);
          if(dist59<CV_R&&other.moving){
            other.vx=(other.vx||0)*0.97;other.vy=(other.vy||0)*0.97;
            slowedCount59++;
          }
        });
        t._slowedCount=slowedCount59;
        if(slowedCount59>=3&&!t._slowAchUnlocked){t._slowAchUnlocked=true;unlock("chronovortex_slow");}
      }
      // VOID BLOOM — spawn 4 petals after 1.5s
      if(t.type==="void_bloom"&&!t.dying&&!t._petalsSpawned&&(Date.now()-t.spawnedAt)>=1500){
        t._petalsSpawned=true;
        const cw56=canvasRef.current?.width||360;const ch56=canvasRef.current?.height||640;
        const petalIds56=[];
        for(let pi=0;pi<4;pi++){
          const ang56=(pi/4)*Math.PI*2;
          const pVx56=Math.cos(ang56)*(0.8+Math.random()*0.5);
          const pVy56=Math.sin(ang56)*(0.8+Math.random()*0.5);
          const pid56=Math.random().toString(36).slice(2);
          petalIds56.push(pid56);
          targetsRef.current.push({id:pid56,type:"void_petal",rarity:null,
            x:Math.max(25,Math.min(cw56-25,t.x)),y:Math.max(25,Math.min(ch56-25,t.y)),
            radius:t.radius*0.55,color:"#e879f9",glow:"#fae8ff",
            lifetime:2500,spawnedAt:Date.now(),born:performance.now(),
            moving:true,ghost:false,vx:pVx56,vy:pVy56,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
            worldId:t.worldId,worldColor:t.worldColor,bossPattern:null,bossPhase:1,cx:t.x,cy:t.y,
            _bloomGroupId:t.id});
        }
        t._petalIds=petalIds56;t._bloomTaps=0;t._bloomWindow=null;
        spawnPopup(t.x,t.y-30,"🌀 BLOOM!","#c084fc",18);
      }
      // CRYSTAL WEB — spawn 2 web nodes after 800ms
      if(t.type==="crystal_web"&&!t.dying&&!t._nodeIds&&(Date.now()-t.spawnedAt)>=800){
        const cw55=canvasRef.current?.width||360;const ch55=canvasRef.current?.height||640;
        const grpId55=t.id;
        const nodeIds55=[];
        for(let ni=0;ni<2;ni++){
          const ang55=(ni/2)*Math.PI*2+Math.PI/4;
          const dist55=60+Math.random()*30;
          const nx55=Math.max(25,Math.min(cw55-25,t.x+Math.cos(ang55)*dist55));
          const ny55=Math.max(25,Math.min(ch55-25,t.y+Math.sin(ang55)*dist55));
          const nid55=Math.random().toString(36).slice(2);
          nodeIds55.push(nid55);
          targetsRef.current.push({id:nid55,type:"crystal_web_node",rarity:null,
            x:nx55,y:ny55,radius:t.radius*0.7,color:"#22d3ee",glow:"#67e8f9",
            lifetime:t.lifetime-(Date.now()-t.spawnedAt+800),
            spawnedAt:Date.now(),born:performance.now(),
            moving:false,ghost:false,vx:0,vy:0,pwrType:null,hitsLeft:1,maxHits:1,trail:null,
            worldId:t.worldId,worldColor:t.worldColor,bossPattern:null,bossPhase:1,cx:nx55,cy:ny55,
            _webGroupId:grpId55,_tapped55:false});
        }
        t._nodeIds=nodeIds55;t._webTaps=0;t._webStartTime=null;
      }
      // GRAVITY NODE — pull nearby normal targets toward it
      if(t.type==="gravity_node"&&!t.dying){
        if(!t._pulledTargets)t._pulledTargets=new Set();
        const GN_R=150;const GN_STR=0.025;
        targetsRef.current.forEach(other=>{
          if(other===t||other.dying||other.type!=="normal")return;
          const dx54=t.x-other.x,dy54=t.y-other.y;
          const dist54=Math.hypot(dx54,dy54);
          if(dist54>0&&dist54<GN_R){
            const force=GN_STR*(1-dist54/GN_R)*dt;
            other.vx=(other.vx||0)+(dx54/dist54)*force*50;
            other.vy=(other.vy||0)+(dy54/dist54)*force*50;
            other.moving=true;t._pulledTargets.add(other.id);
          }
        });
      }
      // MAGNET_ORBIT — orbit all non-boss targets around canvas center
      if(!t.dying&&t.type!=="boss"&&t.type!=="bomb"&&activePwrRef.current.some(p=>p.type==="MAGNET_ORBIT"&&p.endsAt>now)){
        const cx44=w/2,cy44=h/2;
        const dx44=t.x-cx44,dy44=t.y-cy44;
        const dist44=Math.hypot(dx44,dy44)||1;
        const orbitSpd=0.9*dt;
        t.x+=(-dy44/dist44)*orbitSpd;t.y+=(dx44/dist44)*orbitSpd;
        t.x=Math.max(t.radius,Math.min(w-t.radius,t.x));t.y=Math.max(t.radius,Math.min(h-t.radius,t.y));
      }
      // GRAVITY_WELL — pull all targets toward canvas center
      if(!t.dying&&activePwrRef.current.some(p=>p.type==="GRAVITY_WELL"&&p.endsAt>now)){
        const cx7=w/2,cy7=h/2;
        const dx7=cx7-t.x,dy7=cy7-t.y;
        const dist7=Math.hypot(dx7,dy7);
        if(dist7>30){
          t.x+=dx7/dist7*1.2*dt;t.y+=dy7/dist7*1.2*dt;
        }
      }
      // 10B — Magnet pull: attract nearby normal targets toward this magnet
      if(t.type==="magnet"&&!t.dying){
        const PULL_R=110,PULL_STR=0.018;
        targetsRef.current.forEach(other=>{
          if(other===t||other.dying||other.type!=="normal")return;
          const dx=t.x-other.x,dy=t.y-other.y;
          const dist=Math.hypot(dx,dy);
          if(dist>0&&dist<PULL_R){
            const force=PULL_STR*(1-dist/PULL_R)*dt;
            other.x+=dx/dist*force*2;
            other.y+=dy/dist*force*2;
          }
        });
      }
      // Squish (dying) animation — plays for 120ms then removes
      if(t.dying){
        const age=pnow-t.dying;
        if(age>=120)return false;
        const p=age/120;
        const sx=1+Math.sin(p*Math.PI)*0.55;
        const sy=1-Math.sin(p*Math.PI)*0.42;
        ctx.save();ctx.translate(t.x,t.y);ctx.scale(sx,sy);ctx.translate(-t.x,-t.y);
        ctx.globalAlpha=1-p*0.8;
        drawTarget(ctx,t,ts);
        ctx.restore();ctx.globalAlpha=1;
        return true;
      }
      // Expiry
      if((now-t.spawnedAt)>=t.lifetime){
        if(t.type==="powerup"||t.type==="bomb"||t.type==="boss"||t.type==="treasure")return false;
        // TIME_STOP — freeze normal target expiry (bombs and bosses still expire)
        if(activePwrRef.current.some(p=>p.type==="TIME_STOP"&&p.endsAt>now)&&t.type!=="drift_mine"&&t.type!=="timebomb"&&t.type!=="siphon"&&t.type!=="poison"){return true;}
        // PHOENIX expired — respawns once as phoenix2 (risen form)
        if(t.type==="phoenix"&&!t._hasRisen){
          spawnParticles(t.x,t.y,"#f97316",12,"spark");
          targetsRef.current.push({
            id:Math.random().toString(36).slice(2),type:"phoenix2",
            x:t.x,y:t.y,radius:BASE_R*1.4,color:"#c084fc",glow:"#7c3aed",
            rarity:RARITY.EPIC,lifetime:3000,spawnedAt:Date.now(),born:performance.now(),
            moving:t.moving,ghost:false,vx:t.vx*-0.5,vy:t.vy*-0.5,trail:[],
            hitsLeft:1,maxHits:1,dying:null,_hasRisen:true
          });
          return false; // remove original
        }
        // THUNDERBOLT exits screen — costs 1 life (it "struck" without being caught)
        if(t.type==="thunderbolt"){
          spawnParticles(t.x,Math.min(h-20,t.y),"#facc15",14,"spark");
          spawnPopup(t.x,Math.min(h-40,t.y-20),"⚡ MISSED! -1 LIFE","#facc15",16);
          sfx("miss");vibrate([20,10,20]);
          const hasShTb=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now)||activePwrRef.current.find(p=>p.type==="SHIELD_WALL"&&p.endsAt>now);
          if(hasShTb&&typeof hasShTb==="object"){
            const sw=hasShTb;sw.hitsLeft=(sw.hitsLeft||1)-1;
            if(sw.hitsLeft<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_WALL");setActivePwrDisp([...activePwrRef.current]);}
          } else if(typeof hasShTb==="boolean"&&hasShTb){
            activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);
          } else{gs.lives=Math.max(0,gs.lives-1);gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);lostLife=true;}
          return false;
        }
        // REFLECT_WAVE — bomb expiry scores instead of penalty
        if(t.type==="bomb"&&activePwrRef.current.some(p=>p.type==="REFLECT_WAVE"&&p.endsAt>now)){
          const nearbyRW=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<150);
          const rwPts=nearbyRW.length*100+50;
          gs.score+=rwPts;
          particlesRef.current.push({type:"shockwave",x:t.x,y:t.y,color:"#f87171",size:180,born:performance.now(),duration:500,alpha:0.7});
          spawnPopup(t.x,t.y-22,`💥🔄 REFLECT +${rwPts}`,"#f87171",16);
          spawnParticles(t.x,t.y,"#fca5a5",10,"spark");
          unlock("reflect_wave_hit");
          return false;
        }
        // TIME BOMB detonation — costs 2 lives, removes nearby targets, screen shake
        if(t.type==="timebomb"){
          const BLAST_R=120;
          const blasted=targetsRef.current.filter(o=>o!==t&&!o.dying&&Math.hypot(o.x-t.x,o.y-t.y)<BLAST_R&&o.type!=="boss");
          blasted.forEach(o=>{o.dying=performance.now();spawnParticles(o.x,o.y,"#f97316",6,"spark");});
          spawnParticles(t.x,t.y,"#f97316",35,"spark");spawnParticles(t.x,t.y,"#ef4444",20,"dot");
          particlesRef.current.push({type:"shockwave",x:t.x,y:t.y,color:"#f97316",size:BLAST_R*1.4,born:performance.now(),duration:700,alpha:0.85});
          spawnPopup(t.x,t.y-32,"💣 BOOM! −2 LIVES","#ef4444",20);
          sfx("miss");vibrate([30,15,30,15,40]);setScreenShake(true);setTimeout(()=>setScreenShake(false),500);
          const hasShTB=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now);
          if(hasShTB){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);}
          else{gs.lives=Math.max(0,gs.lives-2);gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);lostLife=true;}
          if(gs.lives<=0){endLevel(false);return false;}
          return false;
        }
        // SCORE_MAGNET — any normal target expiry gives 30% of base points
        if((t.type==="normal"||t.type==="ether_copy")&&!t._vrDevoured){
          const smPwr76=activePwrRef.current.find(p=>p.type==="SCORE_MAGNET"&&p.endsAt>now);
          if(smPwr76){
            const basePts=Math.round((t.rarity?.mult||1)*50);
            const magnetPts=Math.max(15,Math.round(basePts*0.3));
            gs.score+=magnetPts;smPwr76.savedCount=(smPwr76.savedCount||0)+1;
            spawnPopup(t.x,t.y-18,`🧲 +${magnetPts}`,"#6ee7b7",12);
            if(smPwr76.savedCount>=1)unlock("score_magnet_save");
            return false; // no life penalty
          }
        }
        // SCORE_LEECH — normal target expiry gives +30pts instead of penalty
        if(t.type==="normal"&&!t._vrDevoured){
          const slPwr=activePwrRef.current.find(p=>p.type==="SCORE_LEECH"&&p.endsAt>now);
          if(slPwr){
            gs.score+=30;slPwr.totalGained=(slPwr.totalGained||0)+30;
            spawnPopup(t.x,t.y-18,"🔋 +30","#a3e635",12);
            if(slPwr.totalGained>=150)unlock("score_leech_gain");
            return false; // skip normal life penalty
          }
        }
        // ACCUMULATOR — any normal target expiry near it adds a stack
        if(t.type==="normal"&&!t._vrDevoured){
          targetsRef.current.forEach(acc=>{
            if(acc.type==="accumulator"&&!acc.dying&&Math.hypot(t.x-acc.x,t.y-acc.y)<180){
              acc._stacks=(acc._stacks||0)+1;
              if(!acc._baseAccRadius)acc._baseAccRadius=acc.radius;
            }
          });
        }
        // GRAVITY BOMB expired (hit the floor) — explodes! −1 life + screen shake
        if(t.type==="gravity_bomb"){
          spawnParticles(t.x,t.y,"#ef4444",22,"spark");spawnParticles(t.x,t.y,"#fbbf24",12,"dot");
          particlesRef.current.push({type:"shockwave",x:t.x,y:t.y,color:"#ef4444",size:140,born:performance.now(),duration:600,alpha:0.8});
          spawnPopup(t.x,t.y-30,"🧲 CRASH! −1 LIFE","#ef4444",20);
          sfx("miss");vibrate([20,10,25]);setScreenShake(true);setTimeout(()=>setScreenShake(false),400);
          const hasShGB=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now)||activePwrRef.current.find(p=>p.type==="SHIELD_WALL"&&p.endsAt>now);
          if(hasShGB&&typeof hasShGB==="object"){
            const sw=hasShGB;sw.hitsLeft=(sw.hitsLeft||1)-1;
            if(sw.hitsLeft<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_WALL");setActivePwrDisp([...activePwrRef.current]);}
          } else if(typeof hasShGB==="boolean"&&hasShGB){
            activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);
          } else{gs.lives=Math.max(0,gs.lives-1);gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);lostLife=true;}
          if(gs.lives<=0){endLevel(false);return false;}
          return false;
        }
        // DRIFT MINE expired — explodes! −1 life + kills 3 nearby targets
        if(t.type==="drift_mine"){
          const BLAST_R=130;
          const victims=targetsRef.current.filter(o=>o!==t&&!o.dying&&Math.hypot(o.x-t.x,o.y-t.y)<BLAST_R&&o.type!=="boss"&&o.type!=="bomb").slice(0,3);
          victims.forEach(o=>{o.dying=performance.now();spawnParticles(o.x,o.y,"#ef4444",5,"spark");});
          spawnParticles(t.x,t.y,"#ef4444",25,"spark");spawnParticles(t.x,t.y,"#fca5a5",14,"dot");
          particlesRef.current.push({type:"shockwave",x:t.x,y:t.y,color:"#ef4444",size:BLAST_R*1.3,born:performance.now(),duration:600,alpha:0.8});
          spawnPopup(t.x,t.y-32,"💣 BOOM! −1 LIFE","#ef4444",20);
          sfx("miss");vibrate([25,12,25]);setScreenShake(true);setTimeout(()=>setScreenShake(false),400);
          const hasShDM=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now)||activePwrRef.current.find(p=>p.type==="SHIELD_WALL"&&p.endsAt>now);
          if(hasShDM&&typeof hasShDM==="object"){
            const sw=hasShDM;sw.hitsLeft=(sw.hitsLeft||1)-1;
            if(sw.hitsLeft<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_WALL");setActivePwrDisp([...activePwrRef.current]);}
          } else if(typeof hasShDM==="boolean"&&hasShDM){
            activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);
          } else{gs.lives=Math.max(0,gs.lives-1);gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);lostLife=true;}
          if(gs.lives<=0){endLevel(false);return false;}
          return false;
        }
        // SPIRAL BOMB expired (fully wound) — explodes! −1 life
        if(t.type==="spiral_bomb"){
          spawnParticles(t.x,t.y,"#f97316",20,"spark");spawnParticles(t.x,t.y,"#ffd700",10,"dot");
          particlesRef.current.push({type:"shockwave",x:t.x,y:t.y,color:"#f97316",size:120,born:performance.now(),duration:600,alpha:0.75});
          spawnPopup(t.x,t.y-28,"🌀 EXPLODED! −1 LIFE","#f97316",18);
          sfx("miss");vibrate([18,8,20]);setScreenShake(true);setTimeout(()=>setScreenShake(false),350);
          const hasShSB=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now)||activePwrRef.current.find(p=>p.type==="SHIELD_WALL"&&p.endsAt>now);
          if(hasShSB&&typeof hasShSB==="object"){const sw=hasShSB;sw.hitsLeft=(sw.hitsLeft||1)-1;if(sw.hitsLeft<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_WALL");setActivePwrDisp([...activePwrRef.current]);}}
          else if(typeof hasShSB==="boolean"&&hasShSB){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);}
          else{gs.lives=Math.max(0,gs.lives-1);gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);lostLife=true;}
          if(gs.lives<=0){endLevel(false);return false;}
          return false;
        }
        // BUBBLE WAVE expired (burst) — −1 life
        if(t.type==="bubble_wave"){
          spawnParticles(t.x,t.y,"#38bdf8",20,"spark");spawnParticles(t.x,t.y,"#bae6fd",10,"dot");
          particlesRef.current.push({type:"shockwave",x:t.x,y:t.y,color:"#38bdf8",size:150,born:performance.now(),duration:600,alpha:0.7});
          spawnPopup(t.x,t.y-28,"🫧 BURST! −1 LIFE","#38bdf8",18);
          sfx("miss");vibrate([16,8,18]);setScreenShake(true);setTimeout(()=>setScreenShake(false),300);
          const hasShBW=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now)||activePwrRef.current.find(p=>p.type==="SHIELD_WALL"&&p.endsAt>now);
          if(hasShBW&&typeof hasShBW==="object"){const sw=hasShBW;sw.hitsLeft=(sw.hitsLeft||1)-1;if(sw.hitsLeft<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_WALL");setActivePwrDisp([...activePwrRef.current]);}}
          else if(typeof hasShBW==="boolean"&&hasShBW){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);}
          else{gs.lives=Math.max(0,gs.lives-1);gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);lostLife=true;}
          if(gs.lives<=0){endLevel(false);return false;}
          return false;
        }
        // VOID PULSAR expired without being tapped — −150pts score drain
        if(t.type==="void_pulsar"){
          const drain=Math.min(150,gsRef.current.score);
          gsRef.current.score=Math.max(0,gsRef.current.score-drain);
          spawnParticles(t.x,t.y,"#7c3aed",12,"spark");
          spawnPopup(t.x,t.y-24,"🌌 VOID DRAIN! −150","#a78bfa",16);
          sfx("miss");vibrate([8,4,10]);
          return false;
        }
        // CLOCKWORK BOMB expired (not fully defused) — −1 life
        if(t.type==="clockwork_bomb"&&t.hitsLeft>0){
          spawnParticles(t.x,t.y,"#f59e0b",20,"spark");spawnParticles(t.x,t.y,"#fcd34d",10,"dot");
          particlesRef.current.push({type:"shockwave",x:t.x,y:t.y,color:"#f59e0b",size:130,born:performance.now(),duration:600,alpha:0.75});
          spawnPopup(t.x,t.y-28,"⚙️ KABOOM! −1 LIFE","#f59e0b",18);
          sfx("miss");vibrate([18,8,20]);setScreenShake(true);setTimeout(()=>setScreenShake(false),350);
          const hasShCW=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now)||activePwrRef.current.find(p=>p.type==="SHIELD_WALL"&&p.endsAt>now);
          if(hasShCW&&typeof hasShCW==="object"){const sw=hasShCW;sw.hitsLeft=(sw.hitsLeft||1)-1;if(sw.hitsLeft<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_WALL");setActivePwrDisp([...activePwrRef.current]);}}
          else if(typeof hasShCW==="boolean"&&hasShCW){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);}
          else{gs.lives=Math.max(0,gs.lives-1);gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);lostLife=true;}
          if(gs.lives<=0){endLevel(false);return false;}
          return false;
        }
        // BOLT CATCHER expired — zaps a random nearby target for -150pts
        if(t.type==="bolt_catcher"){
          const victim=targetsRef.current.find(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<200);
          if(victim){victim.dying=performance.now();spawnParticles(victim.x,victim.y,"#fbbf24",10,"spark");}
          gsRef.current.score=Math.max(0,gsRef.current.score-150);
          spawnParticles(t.x,t.y,"#fbbf24",12,"spark");
          spawnPopup(t.x,t.y-24,"⚡ ZAP! −150","#f59e0b",16);
          sfx("miss");vibrate([8,4,8]);
          return false;
        }
        // ABYSS ORB expired — drains 200pts from score
        if(t.type==="abyss_orb"){
          const drain=Math.min(200,gsRef.current.score);
          gsRef.current.score=Math.max(0,gsRef.current.score-drain);
          spawnParticles(t.x,t.y,"#7c3aed",14,"spark");
          spawnPopup(t.x,t.y-28,"🌑 ABYSS! −200","#a78bfa",18);
          sfx("miss");vibrate([10,5,15]);
          return false;
        }
        // MIRAGE expired — player correctly avoided it; reward with achievement + small pts
        if(t.type==="mirage"){
          gsRef.current.score+=30;// tiny bonus for not falling for the mirage
          spawnPopup(t.x,t.y-20,"🏜️ Mirage!","#f59e0b",11);
          unlock("mirage_avoid");
          return false;
        }
        // SIPHON expired — drains 1 life (unless shielded)
        if(t.type==="siphon"){
          spawnParticles(t.x,t.y,"#dc2626",20,"spark");
          spawnPopup(t.x,t.y-30,"⚠️ SIPHON! -1 LIFE","#ef4444",18);
          sfx("miss");vibrate([20,10,20,10,30]);
          const hasShS=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now);
          if(hasShS){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);}
          else{gs.lives--;gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);lostLife=true;}
          return false;
        }
        // POISON expired — apply poison debuff (next target gives 50% pts)
        if(t.type==="poison"){
          gs._poisonedUntil=Date.now()+4000;
          spawnParticles(t.x,t.y,"#4ade80",14,"dot");
          spawnPopup(t.x,t.y-26,"☣️ POISONED! -50%","#4ade80",16);
          sfx("miss");vibrate([15,8,15]);
          return false;
        }
        // STAR BOMB auto-detonate on expiry — bonus pts to nearby normals, no life lost
        if(t.type==="star_bomb"){
          const combo44c=Math.min(10,1+Math.floor((gsRef.current?.streak||0)/5));
          const feverMult44c=(gsRef.current?.feverActive)?2:1;
          const blastNormals44=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<160).slice(0,5);
          blastNormals44.forEach(o=>{o.dying=performance.now();gs.score+=Math.round(40*combo44c*feverMult44c);spawnParticles(o.x,o.y,"#fbbf24",5,"spark");});
          particlesRef.current.push({type:"shockwave",x:t.x,y:t.y,r:t.radius,maxR:200,born:performance.now(),color:"#fbbf24",alpha:0.7,life:600});
          spawnPopup(t.x,t.y-28,"💥 AUTO-BOOM!","#fde68a",18);
          spawnParticles(t.x,t.y,"#fde68a",16,"spark");
          sfx("chainBonus");vibrate([10,5,14,5,10]);
          return false;
        }
        // VOLATILE explosion on expiry — costs a life, removes nearby targets
        if(t.type==="volatile"){
          const burstSh=activePwrRef.current.find(p=>p.type==="BURST_SHIELD"&&(p.charges||0)>0);
          if(burstSh){
            burstSh.charges=0;
            activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BURST_SHIELD");
            setActivePwrDisp([...activePwrRef.current]);
            const bsBonus=200;gs.score+=bsBonus;
            spawnParticles(t.x,t.y,"#fcd34d",16,"spark");
            spawnPopup(t.x,t.y-28,`💣🛡 ABSORBED! +${bsBonus}`,"#fbbf24",18);
            sfx("shieldBreak");vibrate([10,5,10,5,15]);
            return false;
          }
          const BLAST_R=90;
          const blasted=targetsRef.current.filter(o=>o!==t&&!o.dying&&o.type==="normal"&&Math.hypot(o.x-t.x,o.y-t.y)<BLAST_R);
          blasted.forEach(o=>{o.dying=performance.now();spawnParticles(o.x,o.y,"#ff4500",6,"spark");});
          spawnParticles(t.x,t.y,"#ff4500",30,"spark");spawnParticles(t.x,t.y,"#ffd700",15,"dot");
          spawnPopup(t.x,t.y-30,"💥 BOOM! -1 LIFE","#ef4444",18);
          sfx("miss");vibrate([30,15,30]);setScreenShake(true);setTimeout(()=>setScreenShake(false),400);
          const hasShieldV=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now);
          if(!hasShieldV){gs.lives--;gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);}
          else{activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);}
          return false;
        }
        if(t.type==="normal"&&!t._isShard){gs.sessionStats.missedTargets=(gs.sessionStats.missedTargets||0)+1;gs._luckyTaps=0;}
        // BOUNCE_BACK — respawn this normal target once with short lifetime
        if((t.type==="normal"||t.type==="temporal_orb")&&!t._isShard&&!t._bounced){
          const bbPwr=activePwrRef.current.find(p=>p.type==="BOUNCE_BACK"&&(p.charges||0)>0);
          if(bbPwr){
            bbPwr.charges=(bbPwr.charges||1)-1;
            if(bbPwr.charges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="BOUNCE_BACK");}
            setActivePwrDisp([...activePwrRef.current]);
            const cw44=canvasRef.current?.width||360;const ch44=canvasRef.current?.height||640;
            const bx=Math.max(30,Math.min(cw44-30,Math.random()*cw44));
            const by=Math.max(30,Math.min(ch44-30,Math.random()*ch44));
            targetsRef.current.push({...t,id:Math.random().toString(36).slice(2),
              x:bx,y:by,cx:bx,cy:by,
              spawnedAt:Date.now(),born:performance.now(),
              lifetime:2000,dying:null,_bounced:true,_chronoPhase:"active",_justRewound:null});
            spawnPopup(bx,by-20,"🔄 BOUNCED!","#34d399",14);
            spawnParticles(bx,by,"#6ee7b7",8,"spark");
          }
        }
        // INVINCIBLE — complete miss immunity
        if(activePwrRef.current.some(p=>p.type==="INVINCIBLE"&&p.endsAt>now)){
          spawnParticles(t.x,t.y,"#fef3c7",6,"spark");
          spawnPopup(t.x,t.y-16,"💫 IMMUNE!","#fbbf24",12);
          return false;
        }
        const hasShield=activePwrRef.current.some(p=>p.type==="SHIELD"&&p.endsAt>now);
        const lifeShieldP=activePwrRef.current.find(p=>p.type==="LIFE_SHIELD"&&(p.charges||0)>0);
        const shieldWall=activePwrRef.current.find(p=>p.type==="SHIELD_WALL"&&p.endsAt>now);
        if(hasShield){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD");setActivePwrDisp([...activePwrRef.current]);}
        else if(lifeShieldP){
          lifeShieldP.charges=0;
          activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_SHIELD");
          setActivePwrDisp([...activePwrRef.current]);
          spawnParticles(t.x,t.y,"#4ade80",10,"spark");
          spawnPopup(t.x,t.y-20,"🛡️ LIFE SAVED!","#86efac",16);
          sfx("shieldBreak");vibrate([6,4,8]);
        }
        else if(shieldWall){
          shieldWall.hitsLeft=(shieldWall.hitsLeft||1)-1;
          spawnParticles(t.x,t.y,"#a78bfa",8,"spark");
          spawnPopup(t.x,t.y-16,`🏰 WALL: ${shieldWall.hitsLeft} left`,"#a78bfa",12);
          if(shieldWall._surgeSrc){shieldWall._surgeBlocks=(shieldWall._surgeBlocks||0)+1;if(shieldWall._surgeBlocks>=3)unlock("shield_surge_blk");}
          if(shieldWall.hitsLeft<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_WALL");setActivePwrDisp([...activePwrRef.current]);sfx("shieldBreak");}
          else setActivePwrDisp([...activePwrRef.current]);
        } else if(activePwrRef.current.find(p=>p.type==="ETERNAL_LIFE"&&!p.triggered&&p.endsAt>now)){
          // ETERNAL_LIFE — prevent death, reset to 3 lives
          const elPwr100=activePwrRef.current.find(p=>p.type==="ETERNAL_LIFE"&&!p.triggered&&p.endsAt>now);
          elPwr100.triggered=true;
          gs.lives=3;
          spawnParticles(t.x,t.y,"#fecaca",14,"spark");
          spawnPopup(t.x,t.y-28,"♾️ ETERNAL! LIVES →3","#f43f5e",20);
          sfx("comboNote",2);vibrate([8,4,8,4,8]);
          setActivePwrDisp([...activePwrRef.current]);
          unlock("eternal_life_hero");
        } else if(activePwrRef.current.find(p=>p.type==="SHIELD_BURST"&&(p.charges||0)>0&&p.endsAt>now)){
          // SHIELD_BURST — absorbs miss, grants +50 pts
          const sbPwr92=activePwrRef.current.find(p=>p.type==="SHIELD_BURST"&&(p.charges||0)>0&&p.endsAt>now);
          sbPwr92.charges--;sbPwr92.tanked=(sbPwr92.tanked||0)+1;
          const burstBonus92=50;gs.score+=burstBonus92;
          if(sbPwr92.charges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHIELD_BURST");setActivePwrDisp([...activePwrRef.current]);}
          else setActivePwrDisp([...activePwrRef.current]);
          spawnParticles(t.x,t.y,"#38bdf8",12,"spark");
          spawnPopup(t.x,t.y-20,`🛡️💥 TANKED! +${burstBonus92} (${sbPwr92.charges} left)`,"#38bdf8",14);
          sfx("shieldBreak");vibrate([8,4,12]);
          if(sbPwr92.tanked>=3)unlock("shield_burst_tank");
        } else if(streakShRef.current){
          // Streak Shield — absorbs one miss, preserves streak!
          streakShRef.current=false;setStreakShieldActive(false);sfx("shieldBreak");vibrate([8,12,8]);
        } else if(gs._streakSaverActive&&(gs.streak||0)>0){
          // Streak Saver shop item — save streak on first miss
          gs._streakSaverActive=false;gs.lives--;lostLife=true;
          sfx("shieldBreak");vibrate([8,5,8]);
          spawnParticles(t.x,t.y,"#fbbf24",6,"spark");
          spawnPopup(t.x,t.y-18,"💛 STREAK SAVED!","#fbbf24",14);
        } else{
          gs.lives--;sfx("miss");vibrate(42);lostLife=true;
          // Skill: combo_guard — absorb N misses before clearing streak
          const cgLvl=(saveRef.current.skills||{}).combo_guard||0;
          gs._comboGuardCount=gs._comboGuardCount||0;
          if(cgLvl>0&&gs.streak>0&&gs._comboGuardCount<cgLvl){
            gs._comboGuardCount++;
            spawnParticles(t.x,t.y,"#fbbf24",6,"spark");
          } else if(activePwrRef.current.some(p=>p.type==="COMBO_FREEZE"&&p.endsAt>now)){
            // COMBO_FREEZE active — protect streak, just flash blue
            spawnParticles(t.x,t.y,"#67e8f9",8,"spark");
            spawnPopup(t.x,t.y-18,"🧊 COMBO SAVED!","#67e8f9",13);
          } else if(gs._scoreShieldCharges>0){
            // SCORE_SHIELD active — absorb one miss without breaking streak
            gs._scoreShieldCharges--;
            const shields=activePwrRef.current.find(p=>p.type==="SCORE_SHIELD");
            if(shields)shields.charges=gs._scoreShieldCharges;
            if(gs._scoreShieldCharges<=0)activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SCORE_SHIELD");
            setActivePwrDisp([...activePwrRef.current]);
            spawnParticles(t.x,t.y,"#a3e635",8,"spark");
            spawnPopup(t.x,t.y-18,`🔰 SHIELDED! (${gs._scoreShieldCharges} left)`,"#a3e635",13);
          } else if(activePwrRef.current.some(p=>p.type==="COMBO_SHIELD"&&p.endsAt>now)){
            // COMBO_SHIELD — protect streak from reset
            gs._comboGuardCount=0;
            spawnParticles(t.x,t.y,"#fcd34d",6,"spark");
            spawnPopup(t.x,t.y-18,"🔒 PROTECTED!","#fcd34d",12);
          } else if(activePwrRef.current.some(p=>p.type==="STREAK_LOCK"&&p.endsAt>now)){
            // STREAK_LOCK active — miss doesn't reset streak
            gs._comboGuardCount=0;
            spawnParticles(t.x,t.y,"#fbbf24",6,"spark");
            spawnPopup(t.x,t.y-18,"🔐 LOCKED!","#fbbf24",14);
            unlock("streak_lock_save");
          } else {
            gs._comboGuardCount=0;
            gs.streak=0;streakShRef.current=false;setStreakShieldActive(false);setMascotMood("sad");
            showMascotSpeech("miss");
            setTimeout(()=>setMascotMood("idle"),1200);
          }
          // Screen shake on miss
          setScreenShake(true);setTimeout(()=>setScreenShake(false),280);
          // Red flash at target position
          spawnParticles(t.x,t.y,"#ef4444",8,"spark");
          // Log miss for adaptive difficulty
          if(gs._hitLog){gs._hitLog.push({t:Date.now(),hit:false});if(gs._hitLog.length>80)gs._hitLog.shift();}
          // no_miss modifier: instant fail
          if(cfg?.modifier?.type==="no_miss"){endLevel(false);return false;}
        }
        return false;
      }
      // Bomb danger zone — subtle pulsing red ring around bombs showing tap-exclusion zone
      if(t.type==="bomb"&&!t.dying){
        const bdPulse=0.4+0.4*Math.abs(Math.sin(ts*0.006));
        ctx.save();ctx.globalAlpha=bdPulse*0.18;
        ctx.fillStyle="#ef4444";ctx.shadowColor="#ef4444";ctx.shadowBlur=0;
        ctx.beginPath();ctx.arc(t.x,t.y,t.radius*2.2,0,Math.PI*2);ctx.fill();
        ctx.globalAlpha=bdPulse*0.5;
        ctx.strokeStyle="#ef4444";ctx.lineWidth=1.5;
        ctx.setLineDash([5,4]);
        ctx.beginPath();ctx.arc(t.x,t.y,t.radius*2.2,0,Math.PI*2);ctx.stroke();
        ctx.setLineDash([]);
        ctx.restore();
      }
      // Bounty target indicator — golden crown + pulsing ring above the target
      if(t._isBounty&&!t.dying){
        const bpulse=0.5+0.5*Math.sin(ts*0.009);
        ctx.save();
        ctx.globalAlpha=0.55+bpulse*0.35;
        ctx.strokeStyle="#ffd700";ctx.lineWidth=2.5;ctx.shadowColor="#ffd700";ctx.shadowBlur=12+bpulse*8;
        ctx.setLineDash([5,3]);ctx.lineDashOffset=-ts*0.05;
        ctx.beginPath();ctx.arc(t.x,t.y,t.radius+8,0,Math.PI*2);ctx.stroke();
        ctx.setLineDash([]);ctx.globalAlpha=0.9;
        ctx.font=`${Math.round(t.radius*0.7)}px sans-serif`;
        ctx.textAlign="center";ctx.textBaseline="bottom";ctx.shadowBlur=6;
        ctx.fillText("👑",t.x,t.y-t.radius-2);
        ctx.restore();
      }
      // Edge warning flash overlay
      if(t._edgeWarnAt){
        const edgeFrac=Math.min(1,(now-t._edgeWarnAt)/1500);
        const flashAlpha=0.35+0.45*Math.sin(ts*0.025)*(0.5+edgeFrac*0.5);
        ctx.save();ctx.globalAlpha=flashAlpha;
        ctx.strokeStyle="#ef4444";ctx.lineWidth=3+edgeFrac*3;
        ctx.shadowColor="#ef4444";ctx.shadowBlur=12+edgeFrac*10;
        ctx.beginPath();ctx.arc(t.x,t.y,t.radius+4,0,Math.PI*2);ctx.stroke();
        ctx.restore();
      }
      // Near-expiry warning — target flashes orange when < 20% lifetime remains
      if(t.type==="normal"&&!t.dying&&!t._edgeWarnAt&&t.lifetime>0){
        const lifeLeft=(t.spawnedAt+t.lifetime-now)/t.lifetime;
        if(lifeLeft<0.2&&lifeLeft>0){
          const urgency=1-lifeLeft/0.2;
          const expAlpha=(0.3+0.5*urgency)*Math.abs(Math.sin(ts*0.03*(1+urgency*2)));
          ctx.save();ctx.globalAlpha=expAlpha;
          ctx.strokeStyle=lifeLeft<0.08?"#ef4444":"#f97316";
          ctx.lineWidth=2.5;ctx.shadowColor=lifeLeft<0.08?"#ef4444":"#f97316";ctx.shadowBlur=8+urgency*8;
          ctx.beginPath();ctx.arc(t.x,t.y,t.radius+3,0,Math.PI*2);ctx.stroke();
          ctx.restore();
        }
      }
      drawTarget(ctx,t,ts);return true;
    });

    // World foreground silhouettes + weather + depth vignette (on top of targets)
    drawWorldForeground(ctx,w,h,cfg?cfg.world:0,ts);
    drawWeatherParticles(ctx,w,h,cfg?cfg.world:0,ts);
    drawVignette(ctx,w,h,accentColor);

    // ECHO_LIFE — restore 1 life 3s after a loss
    {const elPwr110=activePwrRef.current.find(p=>p.type==="ECHO_LIFE"&&p.endsAt>now);
    if(elPwr110&&lostLife&&(now-elPwr110.lastEcho)>4000){
      elPwr110.lastEcho=now;
      setTimeout(()=>{
        const gs110=gsRef.current;const el110=activePwrRef.current.find(p=>p.type==="ECHO_LIFE"&&p.endsAt>Date.now());
        if(gs110&&el110){
          gs110.lives=Math.min(5,gs110.lives+1);
          el110.totalRestored=(el110.totalRestored||0)+1;
          spawnPopup(180,150,"❤️〰️ ECHO +1 LIFE","#f43f5e",18);
          sfx("comboNote",2);
          if(el110.totalRestored>=5)unlock("echo_life_max");
        }
      },3000);
    }}
    // SPECTRAL_COIN — spawn 3 ghost coins on miss
    {const spMissPwr106=activePwrRef.current.find(p=>p.type==="SPECTRAL_COIN"&&p.endsAt>now);
    if(spMissPwr106&&lostLife){
      const cw106=canvasRef.current?.width||360;const ch106=canvasRef.current?.height||700;
      for(let spi=0;spi<3;spi++){
        spMissPwr106.pool=spMissPwr106.pool||[];
        spMissPwr106.pool.push({x:50+Math.random()*(cw106-100),y:80+Math.random()*(ch106-200),collected:false,expiresAt:Date.now()+5000});
      }
      spMissPwr106.pool=spMissPwr106.pool.filter(sc=>sc.expiresAt>Date.now());
    }}
    // SHADOW_BLOOM — absorbs up to 4 misses silently
    {const sbPwr115=activePwrRef.current.find(p=>p.type==="SHADOW_BLOOM"&&p.endsAt>now);
    if(sbPwr115&&lostLife&&(sbPwr115.missesAbsorbed||0)<4){
      gs.lives=Math.min(5,gs.lives+1);
      sbPwr115.missesAbsorbed=(sbPwr115.missesAbsorbed||0)+1;
      spawnPopup(180,130,`🌑🌸 ABSORBED! (${sbPwr115.missesAbsorbed}/4)`,"#6d28d9",18);
      spawnParticles(180,130,"#ddd6fe",6,"dot");
      sfx("comboNote",2);
      if(sbPwr115.missesAbsorbed>=4){unlock("shadow_bloom_max");activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="SHADOW_BLOOM");}
      lostLife=false;
    }}
    if(lostLife){
      // VENOM_SURGE — convert miss to +50pts bonus
      {const vsPwr102=activePwrRef.current.find(p=>p.type==="VENOM_SURGE"&&p.endsAt>now);
      if(vsPwr102){
        gs.lives=Math.min(5,gs.lives+1); // restore life
        const venomBonus102=50*(gs.feverActive?2:1);
        gs.score+=venomBonus102;
        vsPwr102.missesConverted=(vsPwr102.missesConverted||0)+1;
        spawnPopup(180,120,`☠️ VENOM +${venomBonus102}`,"#84cc16",20);
        spawnParticles(180,120,"#d9f99d",6,"dot");
        sfx("comboNote",2);
        if(vsPwr102.missesConverted>=5)unlock("venom_surge_max");
        lostLife=false;
      }}
      // GOLD_SHIELD — convert life loss to +100 coins
      {const gsPwr63=activePwrRef.current.find(p=>p.type==="GOLD_SHIELD"&&p.endsAt>now);
      if(gsPwr63){
        gs.lives=Math.min(5,gs.lives+1); // restore the life
        saveRef.current.coins=(saveRef.current.coins||0)+100;
        gsPwr63.procCount=(gsPwr63.procCount||0)+1;
        spawnPopup(180,120,"🛡️ +100🪙 SAVED!","#fbbf24",20);
        sfx("coin");vibrate(8);
        unlock("gold_shield_proc");
        lostLife=false;
      }}
      // PULSE_SHIELD — block up to 2 miss life losses
      {const psPwr73=activePwrRef.current.find(p=>p.type==="PULSE_SHIELD"&&(p.charges||0)>0&&p.endsAt>now);
      if(psPwr73){
        psPwr73.charges=(psPwr73.charges||2)-1;
        if(psPwr73.charges<=0){activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PULSE_SHIELD");setActivePwrDisp([...activePwrRef.current]);}
        else{setActivePwrDisp([...activePwrRef.current]);}
        gs.lives=Math.min(5,gs.lives+1); // restore the life
        spawnPopup(180,100,"💠 SHIELD BLOCK!","#38bdf8",20);
        sfx("powerUp");vibrate([4,3,8]);
        unlock("pulse_shield_blk");
        lostLife=false;
      }}
      // PHANTOM_LIFE — last resort save
      {const plPwr64=activePwrRef.current.find(p=>p.type==="PHANTOM_LIFE"&&!p.used);
      if(plPwr64&&gs.lives<=0){
        plPwr64.used=true;gs.lives=1;
        spawnPopup(180,160,"👻 PHANTOM SAVE!","#c084fc",24);
        sfx("unlock");vibrate([10,5,20,5,10]);
        unlock("phantom_life_save");
        lostLife=false;
        activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="PHANTOM_LIFE");
        setActivePwrDisp([...activePwrRef.current]);
      }}
      // LIFE_GUARD — prevent game over once; revive with 1 life
      if(gs.lives<=0){
        const lgPwr83=activePwrRef.current.find(p=>p.type==="LIFE_GUARD"&&p.endsAt>now&&!p.used);
        if(lgPwr83){
          lgPwr83.used=true;gs.lives=1;
          spawnPopup(canvasRef.current?.width/2||200,canvasRef.current?.height/2||350,"❤️🛡️ GUARDED!","#fb7185",26);
          spawnParticles(canvasRef.current?.width/2||200,canvasRef.current?.height/2||350,"#fca5a5",16,"spark");
          sfx("shieldBreak");vibrate([8,4,14,4,8]);
          activePwrRef.current=activePwrRef.current.filter(p=>p.type!=="LIFE_GUARD");
          setActivePwrDisp([...activePwrRef.current]);
          unlock("life_guard_save");
        } else {
          endLevel(false);return;
        }
      }
      // SCORE_SNAP — when life is lost, award 50pts buffer compensation
      if(lostLife){
        const ssPwr83=activePwrRef.current.find(p=>p.type==="SCORE_SNAP"&&p.endsAt>now);
        if(ssPwr83){
          const savedPts83=Math.max(20,Math.round(gs.score*0.05));
          gs.score+=savedPts83;ssPwr83.savedPts=(ssPwr83.savedPts||0)+savedPts83;
          spawnPopup((canvasRef.current?.width||400)*0.5,(canvasRef.current?.height||700)*0.5,`📸 SNAP +${savedPts83}`,"#a3e635",18);
          if(ssPwr83.savedPts>=500)unlock("score_snap_big");
        }
      }
      if(lostLife){setDamageFlash(true);setTimeout(()=>setDamageFlash(false),500);}
    }

    // Expire power-ups
    const pl=activePwrRef.current.length;
    activePwrRef.current=activePwrRef.current.filter(p=>p.endsAt>now);
    if(activePwrRef.current.length!==pl)setActivePwrDisp([...activePwrRef.current]);

    // Fever
    if(gs.feverActive){gs.feverTimeLeft-=dt;if(gs.feverTimeLeft<=0){gs.feverActive=false;setFeverBorder(false);sfx("feverEnd");audioRef.current?.setBgMusicFever?.(false);setMascotMood("idle");}}

    // Adaptive difficulty — every 4s, adjust spawnInterval and targetLifetime
    if(saveRef.current.adaptiveDifficulty&&gs._hitLog&&gs._adaptNextEval&&now>=gs._adaptNextEval){
      gs._adaptNextEval=now+4000;
      const windowMs=12000;
      const recent=gs._hitLog.filter(e=>now-e.t<windowMs);
      if(recent.length>=6){
        const hitRate=recent.filter(e=>e.hit).length/recent.length;
        // Target ~72% hit rate as "just right"
        let newMult=gs._adaptMult||1.0;
        if(hitRate>0.88)newMult=Math.min(1.35,newMult+0.07); // too easy → harder
        else if(hitRate>0.78)newMult=Math.min(1.35,newMult+0.03);
        else if(hitRate<0.45)newMult=Math.max(0.65,newMult-0.10); // too hard → easier
        else if(hitRate<0.60)newMult=Math.max(0.65,newMult-0.05);
        if(Math.abs(newMult-(gs._adaptMult||1.0))>0.01){
          gs._adaptMult=newMult;
          const base=getLevelConfig(cfg.id||1);
          const sm=saveRef.current.speedMode||1.0;
          levelCfgRef.current.spawnInterval=Math.max(200,Math.round(base.spawnInterval/sm/newMult));
          levelCfgRef.current.targetLifetime=Math.max(600,Math.round(base.targetLifetime/sm/newMult));
        }
      }
    }

    // Lightning Storm — triggered by hitting streak 25+ milestone (max once per level)
    if(!cfg.isBoss&&gs.streak>=25&&!gs._stormTriggered&&!gs._stormEndsAt){
      gs._stormTriggered=true;gs._stormEndsAt=now+4000;
      spawnPopup(cw/2,ch*0.3,"⚡ LIGHTNING STORM!","#fbbf24",22);
      setNotif("⚡ LIGHTNING STORM! All targets get zapped!");
      sfx("chainBonus");vibrate([15,8,15,8,25]);
      // Zap all current normal targets for free
      const eligible=targetsRef.current.filter(t=>!t.dying&&t.type==="normal"&&!t._isShard);
      const combo=Math.min(10,1+Math.floor(gs.streak/5));const feverMult=gs.feverActive?2:1;
      eligible.slice(0,5).forEach((t,i)=>{setTimeout(()=>{
        if(!t.dying){t.dying=performance.now();
          const pts=Math.round(40*combo*feverMult);gs.score+=pts;gs.sessionStats.score=gs.score;
          spawnParticles(t.x,t.y,"#fbbf24",10,"spark");spawnPopup(t.x,t.y-20,`⚡+${pts}`,"#fbbf24",13);
          sfx("comboNote",i+2);
        }
      },i*120);});
    }

    // Target Rush — when score hits 90% of goal, spawn at 1.5× rate for 8s
    if(!cfg.isBoss&&!cfg.isZen&&cfg.scoreGoal&&gs.score>=cfg.scoreGoal*0.9&&!gs._rushStarted){
      gs._rushStarted=true;gs._rushEndsAt=now+8000;
      spawnPopup(cw/2,ch/3,"⚡ RUSH! GET IT!","#f97316",20);sfx("feverStart");vibrate([15,8,15]);
      setRushMode(true);setTimeout(()=>setRushMode(false),8000);
    }
    const isRush=gs._rushEndsAt&&now<gs._rushEndsAt;

    // Spawn — rage boss speeds up spawn
    const rageSpawn=cfg._rageSpawn&&targetsRef.current.some(t=>t.rage);
    spawnTimer.current+=dt;
    const rushSpawnMult=isRush?0.65:1;
    const conductorMult=(gs._conductorEndsAt&&Date.now()<gs._conductorEndsAt)?0.45:1;
    const doubleSpawnMult=gs._doubleSpawnActive?0.5:1;
    if(spawnTimer.current>=(rageSpawn?cfg.spawnInterval*0.5:cfg.spawnInterval*rushSpawnMult*conductorMult*doubleSpawnMult)){
      spawnTimer.current=0;spawnTarget();
      // DOUBLE_SPAWN power-up — spawn an additional target
      if(gs._doubleSpawnLeft>0){gs._doubleSpawnLeft--;setTimeout(()=>spawnTarget(),80);}
    }

    // Bounty Target — once per level, designate one normal target as a bounty (3× coins)
    if(!cfg.isBoss&&!cfg.isZen&&!cfg.isGauntlet&&!gs.bountySet){
      gs.bountySet=true;
      // After 4 targets have been tapped, flag the next newly-spawned normal target
      gs._bountyArmed=true;
    }
    if(gs._bountyArmed&&targetsRef.current.length>0){
      const candidate=targetsRef.current.find(t=>t.type==="normal"&&!t._isBounty&&!t.dying&&!t.ghost);
      if(candidate){
        candidate._isBounty=true;
        gs._bountyArmed=false;
      }
    }

    // Wave Surge — every 18-24s, spawn 3-5 targets at once (not in boss/zen/bonus rounds)
    if(!cfg.isBoss&&!cfg.isZen&&!gs.bonusRoundActive&&!cfg.isGauntlet){
      if(!gs.waveAt) gs.waveAt=Date.now()+18000+Math.random()*6000;
      if(Date.now()>=gs.waveAt){
        const waveCount=3+Math.floor(Math.random()*3);
        for(let i=0;i<waveCount;i++) setTimeout(()=>spawnTarget(),i*60);
        spawnPopup(cw/2,ch/3,`🌊 WAVE × ${waveCount}`,"#60a5fa",16);
        sfx("chainBonus");
        gs.waveAt=Date.now()+18000+Math.random()*8000;
      }
    }

    // World modifier: Dragon's Lair (W1) — small ground tremors every ~7-10s
    if(cfg.world===1){
      gs.lairTremorAt=gs.lairTremorAt||(Date.now()+8000+Math.random()*3000);
      if(Date.now()>=gs.lairTremorAt){
        setScreenShake(true);setTimeout(()=>setScreenShake(false),250);
        try{navigator.vibrate?.([20]);}catch{}
        gs.lairTremorAt=Date.now()+7000+Math.random()*4000;
      }
    }
    // World modifier: Haunted Castle (W7) — periodic 'ghost wave' makes targets fade
    if(cfg.world===7){
      gs.ghostWaveAt=gs.ghostWaveAt||(Date.now()+5000);
      gs.ghostWaveEnd=gs.ghostWaveEnd||0;
      if(Date.now()>=gs.ghostWaveAt&&Date.now()>gs.ghostWaveEnd){
        gs.ghostWaveEnd=Date.now()+1400;
        gs.ghostWaveAt=Date.now()+5500+Math.random()*2000;
        // Mark all current normal targets as wave-ghosted
        targetsRef.current.forEach(t=>{if(t.type==="normal")t.waveGhost=Date.now()+1400;});
      }
    }

    // Combo decay — reduce streak after 2.5s of inactivity (1.5s in Viking Fjords frost)
    const decayMs=cfg.world===5?1500:2500; // Viking Fjords: combo slips through frost faster
    const comboSurgePwr72=activePwrRef.current.find(p=>p.type==="COMBO_SURGE"&&p.endsAt>Date.now());
    if(comboSurgePwr72){
      // Freeze decay — just reset lastTapTime so decay timer doesn't tick
      if(gs.lastTapTime)gs.lastTapTime=Date.now();
      comboSurgePwr72.maxStreakDuring=Math.max(comboSurgePwr72.maxStreakDuring||0,gs.streak);
      if((comboSurgePwr72.maxStreakDuring||0)>=20)unlock("combo_surge_big");
    } else if(gs.streak>0&&gs.lastTapTime&&(Date.now()-gs.lastTapTime)>decayMs){
      gs.streak=Math.max(0,gs.streak-1);
      gs.lastTapTime=Date.now()-decayMs;
      setStreakDecaying(gs.streak>0);
    }

    // Score milestone popups — HALFWAY / SO CLOSE (shown once per level)
    if(cfg.scoreGoal&&!cfg.isInfinity&&!cfg.isZen){
      const scorePct=gs.score/cfg.scoreGoal;
      if(scorePct>=0.5&&!gs._shownHalfway){
        gs._shownHalfway=true;
        spawnPopup(cw/2,ch*0.42,"🎯 HALFWAY THERE!","#4ade80",18);sfx("tap");
      }
      if(scorePct>=0.9&&!gs._shownClose){
        gs._shownClose=true;
        spawnPopup(cw/2,ch*0.38,"🔥 SO CLOSE! PUSH IT!","#f97316",20);sfx("lucky");vibrate([15,8,15]);
      }
    }

    // NOVA_STORM — auto-detonate nearest target every 2s
    {const nsPwr69=activePwrRef.current.find(p=>p.type==="NOVA_STORM"&&p.endsAt>Date.now());
    if(nsPwr69&&Date.now()>=nsPwr69.nextStrike){
      nsPwr69.nextStrike=Date.now()+2000;
      const validTargets69=targetsRef.current.filter(o=>!o.dying&&o.type!=="bomb"&&o.type!=="boss");
      if(validTargets69.length>0){
        const auto69=validTargets69[0];
        auto69.dying=performance.now();
        const nsGs69=gsRef.current;
        if(nsGs69){
          const nsPts69=Math.round(60*(nsGs69.feverActive?2:1));
          nsGs69.score+=nsPts69;
          spawnPopup(auto69.x,auto69.y-28,`🌩️ STORM +${nsPts69}`,"#fbbf24",20);
          spawnParticles(auto69.x,auto69.y,"#fde68a",10,"spark");
          particlesRef.current.push({type:"shockwave",x:auto69.x,y:auto69.y,r:auto69.radius,maxR:120,born:performance.now(),color:"#fbbf24",alpha:0.5,life:500});
        }
        unlock("nova_storm_proc");
      }
    }}

    // HUD update 20fps
    if(ts-hudRef.current>50){
      hudRef.current=ts;
      const decayMs2=cfg?.world===5?1500:2500;
      const decayPct=gs.streak>0&&gs.lastTapTime?Math.max(0,1-(Date.now()-gs.lastTapTime)/decayMs2):1;
      const bossT=targetsRef.current.find(t=>t.type==="boss"&&!t.dying);
      // Zen mode: countdown timer
      let zenTimeLeft=null;
      if(cfg?.isZen){
        zenTimeLeft=Math.max(0,Math.ceil((cfg.zenDuration-(Date.now()-gs.startTime))/1000));
        if(zenTimeLeft<=0){endLevel(true);return;}
      }
      const totalAttempted=(gs.sessionStats.tapsTotal||0)+(gs.sessionStats.missedTargets||0);
      const liveAccuracy=totalAttempted>=5?Math.round((gs.sessionStats.tapsTotal||0)/totalAttempted*100):null;
      // Next-tap score estimate (based on current combo + fever + prestige; uses RARE rarity mult)
      const estCombo=Math.min(10,1+Math.floor(gs.streak/5));
      const estFever=gs.feverActive?2:1;
      const estPrestige=1+(Math.min(5,saveRef.current.prestigeLevel||0)*0.05);
      const estScoreBoost=(gs._scoreBoostTaps||0)>0?5:1;
      const estNextPts=Math.round(2*estCombo*estFever*estPrestige*estScoreBoost); // using rare mult ~2
      setHud({score:gs.score,lives:gs.lives,streak:gs.streak,fever:gs.feverActive,coins:saveRef.current.coins,
        timeLeft:zenTimeLeft,modGoal:cfg?.isZen?null:cfg?.modifier?.desc||null,decayPct,
        boss:bossT?{hp:bossT.hitsLeft,max:bossT.maxHits,phase:bossT.bossPhase||1,rage:!!bossT.rage}:null,
        accuracy:liveAccuracy,nextPts:gs.streak>=5?estNextPts:null});
    }

    rafRef.current=requestAnimationFrame(gl=>gameLoopFn(gl));
  },[sfx,spawnTarget,endLevel]);// eslint-disable-line

  const sv=saveRef.current,lvl=getLvl(sv.xp);
  const unlockedMascots=sv.unlockedMascots||(sv.unlockedMascots=["dragon","fox"]);
  const currentMascot=MASCOTS.find(m=>m.id===(sv.mascotId||"dragon")&&unlockedMascots.includes(m.id))||MASCOTS[0];

  // Show mascot speech bubble for 2.5s
  const showSpeech=useCallback((text)=>{
    clearTimeout(speechTimerRef.current);
    setMascotSpeech(text);
    speechTimerRef.current=setTimeout(()=>setMascotSpeech(null),2500);
  },[]);

  // Show mood-specific speech from the current mascot's personality pool
  const showMascotSpeech=useCallback((moodKey)=>{
    const pool=currentMascot.speeches?.[moodKey];
    if(!pool?.length)return;
    const text=pool[Math.floor(Math.random()*pool.length)];
    clearTimeout(speechTimerRef.current);
    setMascotSpeech(text);
    speechTimerRef.current=setTimeout(()=>setMascotSpeech(null),2500);
  },[currentMascot]);

  // Check + apply mascot unlock conditions from current save state
  const checkMascotUnlocks=useCallback(()=>{
    const sv2=saveRef.current;
    const unlocked=sv2.unlockedMascots||(sv2.unlockedMascots=["dragon","fox"]);
    const newlyUnlocked=[];
    for(const m of MASCOTS){
      if(unlocked.includes(m.id)||!m.unlockCond)continue;
      const{type,value}=m.unlockCond;
      let met=false;
      if(type==="level")met=(sv2.unlockedLevel||1)>value; // >value because unlockedLevel updates to next
      else if(type==="streak")met=(sv2.bestStreak||0)>=value;
      else if(type==="threestars"){
        const cnt=Object.values(sv2.levelStars||{}).filter(s=>s>=3).length;
        met=cnt>=value;
      }
      else if(type==="coins")met=(sv2.totalCoins||0)>=value;
      if(met)newlyUnlocked.push(m);
    }
    if(newlyUnlocked.length>0){
      sv2.unlockedMascots=[...unlocked,...newlyUnlocked.map(m=>m.id)];
      return newlyUnlocked;
    }
    return[];
  },[]);

  // Brief mascot bounce on hit
  const mascotBouncePlay=useCallback(()=>{
    clearTimeout(bounceTimerRef.current);
    setMascotBounce(true);
    bounceTimerRef.current=setTimeout(()=>setMascotBounce(false),320);
  },[]);

  // Helper: render the mascot emoji with dance/idle animation
  const MascotEmoji=({mood="idle",dancing=false,size=36,style:sx={}})=>{
    const m=currentMascot;
    const emoji=m.e[mood]||m.e.idle;
    const bounce=mascotBounce&&mood!=="sad"&&mood!=="scared"&&!dancing;
    // Mascot level — drives evolution perks
    const mLvl=Math.min(20,Math.floor(((saveRef.current.mascotXP||{})[m.id]||0)/500));
    // LV15+: faster dance, LV20: even faster
    const danceSpd=mLvl>=20?0.4:mLvl>=15?0.46:0.55;
    const anim=dancing?`${m.dance} ${danceSpd}s ease-in-out infinite`
      :bounce?"mascotBounce 0.32s cubic-bezier(0.34,1.6,0.64,1)"
      :(mood==="excited"||mood==="fire"||mood==="fever")?`${m.dance} ${mLvl>=15?0.9:1.1}s ease-in-out infinite`
      :mood==="sad"?"mascotSad 0.6s ease-in-out 1"
      :mood==="scared"?"mascotShake 0.4s ease-in-out 1"
      :"mascotIdle 2.2s ease-in-out infinite";
    // LV10+: extra glow aura. LV20: rainbow MAX aura
    const auraColor=mLvl>=20?"#ffd700":m.color;
    const auraStrength=mLvl>=20?1.5:mLvl>=10?1.1:1;
    const glow=mood==="fire"||mood==="fever"?`drop-shadow(0 0 ${Math.round(size/2.5*auraStrength)}px ${m.color}) drop-shadow(0 0 ${Math.round(size/1.5)}px #ff6030aa)`
      :mood==="victory"||dancing?`drop-shadow(0 0 ${Math.round(size/2*auraStrength)}px ${auraColor}) drop-shadow(0 0 ${Math.round(size*auraStrength)}px ${auraColor}55)`
      :`drop-shadow(0 0 ${Math.round(size/3*auraStrength)}px ${auraColor})`;
    // Equipped accessory overlay
    const accId=(saveRef.current.mascotAccessories||{})[m.id];
    const acc=accId?MASCOT_ACCESSORIES.find(a=>a.id===accId):null;
    return(
      <span style={{position:"relative",display:"inline-block",...sx}}>
        {/* LV20 MAX rainbow aura ring */}
        {mLvl>=20&&(
          <span style={{
            position:"absolute",inset:`-${size*0.12}px`,borderRadius:"50%",
            background:"conic-gradient(from 0deg,#ff6030,#fbbf24,#34d399,#60a5fa,#f472b6,#a78bfa,#ff6030)",
            filter:`blur(${size/8}px)`,opacity:0.55,
            animation:"slotSpin 3.5s linear infinite",pointerEvents:"none"}}/>
        )}
        {/* LV10+ subtle aura ring */}
        {mLvl>=10&&mLvl<20&&(
          <span style={{
            position:"absolute",inset:`-${size*0.08}px`,borderRadius:"50%",
            background:`radial-gradient(circle,${m.color}55 0%,transparent 70%)`,
            opacity:0.7,pointerEvents:"none",
            animation:"floatGlow 2.4s ease-in-out infinite"}}/>
        )}
        <span style={{fontSize:size,lineHeight:1,display:"inline-block",position:"relative",
          animation:anim,filter:glow}}>
          {emoji}
        </span>
        {acc&&(
          <span style={{
            position:"absolute",top:-size*0.15,left:"50%",transform:"translateX(-50%)",
            fontSize:size*0.5,lineHeight:1,pointerEvents:"none",
            filter:`drop-shadow(0 0 ${Math.round(size/6)}px #ffd70088)`,
            animation:anim}}>
            {acc.emoji}
          </span>
        )}
      </span>
    );
  };

  const streakColor=()=>{
    const s=hud.streak;
    if(s>=35)return"#ff00ff";if(s>=20)return"#ef4444";if(s>=10)return"#f97316";if(s>=5)return"#fbbf24";return"#ffffff";
  };

  // ── Score Tension Ramp — fires whenever score changes ──
  useEffect(()=>{
    if(screen!=="playing")return;
    const cfg=levelCfgRef.current;if(!cfg)return;
    const pct=hud.score/cfg.scoreGoal;
    const newT=pct>=0.99?4:pct>=0.95?3:pct>=0.90?2:pct>=0.75?1:0;
    if(newT!==tensionRef.current){
      tensionRef.current=newT;
      setTensionLevel(newT);
      if(newT===2){sfx("comboNote",9);showMascotSpeech("close");}
      if(newT===3){sfx("comboNote",11);try{navigator.vibrate?.([15,10,15]);}catch{}showMascotSpeech("close");}
      if(newT===4){sfx("comboNote",12);try{navigator.vibrate?.([15,10,15,10,20]);}catch{}showMascotSpeech("close");}
    }
  },[hud.score,screen]);// eslint-disable-line

  // ── Rescue countdown ──
  useEffect(()=>{
    if(rescueSecondsLeft<=0)return;
    const t=setTimeout(()=>setRescueSecondsLeft(s=>Math.max(0,s-1)),1000);
    return()=>clearTimeout(t);
  },[rescueSecondsLeft]);

  // ── Coin/XP payout roll-up ──
  useEffect(()=>{
    if(!levelCompleteData)return;
    const{coinsEarned,xpEarned}=levelCompleteData;
    setDisplayedCoins(0);setDisplayedXP(0);setPayoutDone(false);
    let frame=0;const FRAMES=45;
    const id=setInterval(()=>{
      frame++;
      const ease=1-Math.pow(1-frame/FRAMES,3);
      setDisplayedCoins(Math.floor(ease*coinsEarned));
      setDisplayedXP(Math.floor(ease*xpEarned));
      if(frame%5===0)sfx("comboNote",Math.min(11,Math.floor((frame/FRAMES)*12)));
      if(frame>=FRAMES){clearInterval(id);setPayoutDone(true);}
    },35);
    return()=>clearInterval(id);
  },[levelCompleteData]);// eslint-disable-line

  // ═════════════════════════════════════════════════════════════
  // SCREEN RENDERERS
  // ═════════════════════════════════════════════════════════════

  // ── World Story ──
  const renderWorldStory=()=>{
    if(!storyData)return null;
    const{worldId,type,onDone,bossIntro,panelIndex=0}=storyData;
    // Boss intro panel
    if(type==="boss"&&bossIntro){
      const wld=WORLDS[(worldId||1)-1];
      return(
        <div className="absolute inset-0 z-50 flex flex-col items-center justify-center px-6"
          style={{background:`linear-gradient(160deg,${wld.bg} 0%,rgba(0,0,0,0.92) 100%)`}}>
          <style>{`@keyframes storyFloat{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}} @keyframes bossShake{0%,100%{transform:rotate(-2deg)}50%{transform:rotate(2deg)}}`}</style>
          <div className="w-full max-w-sm flex flex-col items-center gap-5 text-center">
            <div className="text-xs uppercase tracking-widest font-bold" style={{color:wld.color,opacity:0.7}}>⚠️ BOSS STAGE ⚠️</div>
            <div className="text-6xl" style={{animation:"bossShake 0.4s ease-in-out infinite",filter:`drop-shadow(0 0 18px ${wld.color})`}}>{bossIntro.emoji}</div>
            <div className="font-black text-2xl leading-tight" style={{
              color:wld.color,fontFamily:"'Rajdhani','Exo 2',sans-serif",
              textShadow:`0 0 24px ${wld.color}88`}}>{bossIntro.title}</div>
            <div className="rounded-3xl px-6 py-5 text-sm leading-relaxed"
              style={{background:`${wld.color}12`,border:`1px solid ${wld.color}55`,
                backdropFilter:"blur(10px)",color:"#f0f0e8",lineHeight:1.7,fontSize:"1rem"}}>
              {bossIntro.text}
            </div>
            <NeonButton onClick={()=>{
              const bs=saveRef.current.seenBossIntros||[];
              if(!bs.includes(worldId))saveRef.current.seenBossIntros=[...bs,worldId];
              debounceSave();onDone&&onDone();setStoryData(null);
            }} className="w-full py-4 text-lg font-black"
              style={{background:`linear-gradient(135deg,${wld.accent},${wld.color})`,
                boxShadow:`0 0 32px ${wld.color}77`}}>
              🥊 I'm Ready! Let's Go!
            </NeonButton>
          </div>
        </div>
      );
    }
    if(type==="main"){
      const panels=MAIN_STORY.intro.map((text,i)=>({emoji:["🌈","🌩️","🦸"][i]||"✨",text}));
      const idx=panelIndex||0;
      const panel=panels[idx];
      const isLast=idx===panels.length-1;
      return(
        <div className="absolute inset-0 z-50 flex flex-col items-center justify-center px-6"
          style={{background:"linear-gradient(160deg,#060010 0%,#001020 60%,#080800 100%)"}}>
          <style>{`@keyframes storyFloat{0%,100%{transform:translateY(0)}50%{transform:translateY(-8px)}}`}</style>
          {/* Stars bg */}
          <div className="absolute inset-0 pointer-events-none overflow-hidden">
            {[...Array(22)].map((_,i)=>(
              <div key={i} style={{position:"absolute",width:2,height:2,borderRadius:"50%",background:"#fff",
                left:`${(i*37+7)%100}%`,top:`${(i*23+5)%100}%`,opacity:0.2+Math.random()*0.4}}/>
            ))}
          </div>
          <div className="relative z-10 w-full max-w-sm flex flex-col items-center gap-6">
            <div className="text-5xl" style={{animation:"storyFloat 3s ease-in-out infinite"}}>{panel.emoji}</div>
            <div className="text-center font-black text-xl leading-tight" style={{
              color:"#ffd700",fontFamily:"'Rajdhani','Exo 2',sans-serif",
              textShadow:"0 0 20px #ffd70088"}}>
              {MAIN_STORY.title}
            </div>
            <div className="rounded-3xl px-6 py-5 text-center text-sm leading-relaxed"
              style={{background:"rgba(255,255,255,0.06)",border:"1px solid rgba(255,215,0,0.25)",
                backdropFilter:"blur(10px)",color:"#f0e8c0",lineHeight:1.7,fontSize:"1rem"}}>
              {panel.text}
            </div>
            {/* Dots */}
            <div className="flex gap-2">
              {panels.map((_,i)=>(
                <div key={i} style={{width:8,height:8,borderRadius:"50%",background:i===idx?"#ffd700":"#ffffff22",
                  boxShadow:i===idx?"0 0 8px #ffd700":"none"}}/>
              ))}
            </div>
            <NeonButton onClick={()=>{
              if(!isLast){setStoryData(d=>({...d,panelIndex:(d.panelIndex||0)+1}));}
              else{saveRef.current.seenMainStory=true;debounceSave();onDone&&onDone();setStoryData(null);}
            }} className="w-full py-4 text-lg font-black"
              style={{background:"linear-gradient(135deg,#b8860b,#ffd700)",boxShadow:"0 0 28px #ffd70066",color:"#000"}}>
              {isLast?"🚀 START THE QUEST!":"Next →"}
            </NeonButton>
            {idx===0&&<button onClick={()=>{saveRef.current.seenMainStory=true;debounceSave();onDone&&onDone();setStoryData(null);}}
              className="text-xs opacity-30 mt-1" style={{color:"#fff",background:"none",border:"none",cursor:"pointer"}}>
              Skip
            </button>}
          </div>
        </div>
      );
    }
    // World story
    const ws=WORLD_STORIES.find(s=>s.worldId===worldId);
    if(!ws)return null;
    const idx=panelIndex||0;
    const panel=ws.panels[idx];
    const isLast=idx===ws.panels.length-1;
    const wld=WORLDS[worldId-1];
    return(
      <div className="absolute inset-0 z-50 flex flex-col items-center justify-center px-6"
        style={{background:`linear-gradient(160deg,${wld.bg} 0%,${wld.color}18 100%)`}}>
        <div className="relative z-10 w-full max-w-sm flex flex-col items-center gap-5">
          {/* World title */}
          <div className="flex items-center gap-2">
            <span style={{fontSize:32}}>{wld.emoji}</span>
            <div className="font-black text-lg" style={{color:wld.color,fontFamily:"'Rajdhani','Exo 2',sans-serif",
              textShadow:`0 0 16px ${wld.color}88`}}>{ws.title}</div>
          </div>
          {/* Panel emoji big */}
          <div className="text-6xl" style={{animation:"storyFloat 3s ease-in-out infinite",filter:`drop-shadow(0 0 12px ${wld.color}88)`}}>{panel.emoji}</div>
          {/* Story text */}
          <div className="rounded-3xl px-6 py-5 text-center text-sm leading-relaxed w-full"
            style={{background:`${wld.color}10`,border:`1px solid ${wld.color}44`,
              backdropFilter:"blur(10px)",color:"#f0f0e8",lineHeight:1.7,fontSize:"0.97rem"}}>
            {panel.text}
          </div>
          {/* Dots */}
          <div className="flex gap-2">
            {ws.panels.map((_,i)=>(
              <div key={i} style={{width:8,height:8,borderRadius:"50%",background:i===idx?wld.color:"#ffffff22",
                boxShadow:i===idx?`0 0 8px ${wld.color}`:"none"}}/>
            ))}
          </div>
          <NeonButton onClick={()=>{
            if(!isLast){setStoryData(d=>({...d,panelIndex:(d.panelIndex||0)+1}));}
            else{
              const seen=saveRef.current.seenWorldStories||[];
              if(!seen.includes(worldId))saveRef.current.seenWorldStories=[...seen,worldId];
              debounceSave();onDone&&onDone();setStoryData(null);
            }
          }} className="w-full py-4 text-lg font-black"
            style={{background:`linear-gradient(135deg,${wld.accent},${wld.color})`,
              boxShadow:`0 0 28px ${wld.color}66`}}>
            {isLast?"▶ Let's Go! 🎮":"Next →"}
          </NeonButton>
          {idx===0&&<button onClick={()=>{
            const seen=saveRef.current.seenWorldStories||[];
            if(!seen.includes(worldId))saveRef.current.seenWorldStories=[...seen,worldId];
            debounceSave();onDone&&onDone();setStoryData(null);
          }} className="text-xs opacity-30 mt-1" style={{color:"#fff",background:"none",border:"none",cursor:"pointer"}}>
            Skip
          </button>}
        </div>
      </div>
    );
  };

  // ── Tutorial ──
  const TUT_STEPS=[
    {emoji:"👆",title:"Tap the targets!",body:"Glowing circles appear on screen. Tap them before they disappear to score points!",cta:"Got it! →",demo:true},
    {emoji:"🔥",title:"Build your streak!",body:"Tap targets one after another without missing. The longer your streak, the bigger your bonus!",cta:"Cool! →"},
    {emoji:"💣",title:"Avoid bombs!",body:"Red bombs will appear — do NOT tap them! They cost you a life. Tap AROUND them!",cta:"Understood! →"},
    {emoji:"🌟",title:"You're ready!",body:"Collect stars, unlock new worlds, and chase the high score. Have fun on your adventure!",cta:"LET'S GO! 🚀"},
  ];
  const renderTutorial=()=>{
    const step=TUT_STEPS[tutStep]||TUT_STEPS[0];
    const isLast=tutStep===TUT_STEPS.length-1;
    return(
      <div className="absolute inset-0 z-50 flex flex-col items-center justify-center px-6"
        style={{background:"linear-gradient(160deg,#05001a 0%,#001020 60%,#050a00 100%)"}}>
        <div className="w-full max-w-sm flex flex-col items-center gap-6 text-center">
          <div className="text-xs uppercase tracking-widest opacity-50" style={{color:theme.accent}}>
            Step {(tutStep||0)+1} / {TUT_STEPS.length}
          </div>
          <div className="text-7xl" style={{animation:"storyFloat 1.8s ease-in-out infinite",filter:`drop-shadow(0 0 24px ${theme.accent})`}}>
            {step.emoji}
          </div>
          {/* Demo fake tap target */}
          {step.demo&&(
            <div onClick={()=>{sfx("tap");spawnParticles&&null;}}
              className="w-16 h-16 rounded-full flex items-center justify-center cursor-pointer"
              style={{background:`radial-gradient(circle at 38% 36%,${theme.accent}cc,${theme.accent}44)`,
                boxShadow:`0 0 32px ${theme.accent}99`,border:`2px solid ${theme.accent}`,
                animation:"floatGlow 1s ease-in-out infinite"}}>
              <span className="text-2xl font-black" style={{color:"#fff"}}>!</span>
            </div>
          )}
          <h2 className="font-black text-2xl" style={{color:theme.accent,fontFamily:"'Rajdhani','Exo 2',sans-serif",textShadow:`0 0 20px ${theme.accent}88`}}>
            {step.title}
          </h2>
          <p className="text-base leading-relaxed opacity-80" style={{color:"#e8e8f0",lineHeight:1.7}}>
            {step.body}
          </p>
          {/* Dot indicators */}
          <div className="flex gap-2">
            {TUT_STEPS.map((_,i)=>(
              <div key={i} style={{width:8,height:8,borderRadius:"50%",background:i===(tutStep||0)?theme.accent:"#ffffff22"}}/>
            ))}
          </div>
          <NeonButton onClick={()=>{
            if(isLast){
              saveRef.current.seenTutorial=true;debounceSave();
              setTutStep(null);
              if(!saveRef.current.seenMainStory){
                setStoryData({type:"main",panelIndex:0,onDone:()=>go("levelmap")});
              } else {setScreen("levelmap");}
            } else {
              setTutStep((tutStep||0)+1);
            }
          }} className="w-full py-4 text-lg font-black"
            style={{background:`linear-gradient(135deg,${theme.secondary||theme.accent},${theme.accent})`,
              boxShadow:`0 0 32px ${theme.accent}77`}}>
            {step.cta}
          </NeonButton>
          <button onClick={()=>{
            saveRef.current.seenTutorial=true;debounceSave();setTutStep(null);go("levelmap");
          }} style={{color:"#ffffff30",background:"none",border:"none",cursor:"pointer",fontSize:"0.75rem"}}>
            Skip tutorial
          </button>
        </div>
      </div>
    );
  };

  // ── Menu ──
  const renderMenu=()=>{
    // Cycle through world emojis for the title decoration
    const worldEmojiRow=WORLDS.map(w=>w.emoji).join(" ");
    return(
    <div className="flex flex-col items-center justify-center h-full gap-4 px-5 relative z-10 pb-6">
      {/* Title block */}
      <div className="text-center">
        <div className="text-base tracking-widest opacity-50 mb-1" style={{color:theme.accent}}>🐉 🌿 ✨ 🔮 ❄️ ⛏️ 👻 🌊 ⛰️ 🌈</div>
        <h1 className="font-black leading-none" style={{
          fontSize:"clamp(2.8rem,11vw,4.2rem)",letterSpacing:"0.1em",
          fontFamily:"'Rajdhani','Exo 2',sans-serif",
          background:`linear-gradient(160deg,${theme.accent} 0%,#ffffff 50%,${theme.accent} 100%)`,
          backgroundSize:"200% auto",
          WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent",
          filter:`drop-shadow(0 0 28px ${theme.accent}aa)`,
          animation:"shimmer 4s linear infinite",
        }}>
          REALM<br/><span style={{fontSize:"0.62em",letterSpacing:"0.22em"}}>QUEST</span>
        </h1>
        <p className="text-xs mt-2 opacity-60 tracking-widest uppercase" style={{color:theme.accent,fontFamily:"'Rajdhani',sans-serif"}}>
          10 Worlds · 100 Levels · Fun for Everyone!
        </p>
        {/* XP bar */}
        <div className="mt-3 flex items-center gap-2 justify-center">
          <span className="text-xs font-bold px-2 py-0.5 rounded-full" style={{background:theme.accent+"22",color:theme.accent}}>LV {lvl}</span>
          <div className="w-28 h-1.5 rounded-full" style={{background:"#ffffff18"}}>
            <div className="h-full rounded-full" style={{width:`${(sv.xp%XP_PER_LVL)/XP_PER_LVL*100}%`,background:theme.accent,boxShadow:`0 0 8px ${theme.accent}`}}/>
          </div>
          <span className="text-xs opacity-35" style={{color:theme.accent}}>{xpToNext(sv.xp)}xp</span>
          {(sv.skillPoints||0)>0&&(
            <button onClick={()=>setScreen("skilltree")}
              className="ml-2 px-2 py-0.5 rounded-lg font-black text-xs"
              style={{background:`${theme.accent}33`,border:`1px solid ${theme.accent}88`,color:theme.accent,animation:"heartbeat 1.5s ease-in-out infinite",outline:"none"}}>
              ✨ {sv.skillPoints}
            </button>
          )}
        </div>
      </div>

      {/* ── Mascot showcase on menu ── */}
      <div className="flex flex-col items-center gap-2" style={{marginTop:-4,marginBottom:-4}}>
        {/* Time-based greeting speech bubble */}
        {(()=>{
          const h=new Date().getHours();
          const streak=sv.loginStreak||0;
          const name=currentMascot.name;
          let greeting;
          if(streak>=7&&Math.random()<0.4) greeting=`Day ${streak} streak! 🔥`;
          else if(h>=5&&h<11) greeting=`Good morning! ☀️`;
          else if(h>=11&&h<17) greeting=`Hey there! 👋`;
          else if(h>=17&&h<22) greeting=`Good evening! 🌙`;
          else greeting=`Up late? Let's play! 🌌`;
          return(
            <div style={{
              fontSize:11,fontWeight:"bold",padding:"5px 12px",borderRadius:14,
              background:`${currentMascot.color}22`,border:`1px solid ${currentMascot.color}55`,
              color:currentMascot.color,
              animation:"speechBubble 0.5s cubic-bezier(0.34,1.5,0.64,1) both",
              marginBottom:4,position:"relative"}}>
              {greeting}
            </div>
          );
        })()}
        <MascotEmoji mood="idle" size={90}/>
        <div className="font-black tracking-widest" style={{
          fontSize:"0.8rem",color:currentMascot.color,
          textShadow:`0 0 14px ${currentMascot.color}`,animation:"floatGlow 2s ease-in-out infinite"}}>
          {currentMascot.name} — your companion! 🌟
        </div>
      </div>

      {/* Quest story teaser */}
      {(sv.unlockedLevel||1)<=3&&(
        <div className="w-full rounded-2xl px-4 py-3 flex items-center gap-3 cursor-pointer"
          style={{background:`${theme.accent}0c`,border:`1px solid ${theme.accent}30`}}
          onClick={()=>setStoryData({type:"main",panelIndex:0,onDone:()=>go("levelmap")})}>
          <span style={{fontSize:28}}>🌈</span>
          <div>
            <div className="text-xs font-bold mb-0.5" style={{color:theme.accent}}>🗺️ Your Quest</div>
            <div className="text-xs opacity-60 leading-snug" style={{color:theme.accent}}>{MAIN_STORY.intro[0].slice(0,70)}…</div>
          </div>
        </div>
      )}

      {/* World progress strip */}
      <div className="flex gap-1 justify-center flex-wrap">
        {WORLDS.map(w=>{
          const wLevels=Array.from({length:10},(_,i)=>(w.id-1)*10+i+1);
          const done=wLevels.filter(l=>(sv.levelStars[l]||0)>0).length;
          return(
            <div key={w.id} className="flex flex-col items-center" style={{opacity:done>0?1:0.3}}>
              <span style={{fontSize:18}}>{w.emoji}</span>
              <div style={{width:20,height:3,borderRadius:2,background:done===10?w.color:`${w.color}44`,boxShadow:done===10?`0 0 6px ${w.color}`:"none"}}/>
            </div>
          );
        })}
      </div>

      {/* Stats strip */}
      <div className="flex items-center gap-2 flex-wrap justify-center">
        <div className="flex items-center gap-1 px-3 py-1.5 rounded-xl" style={{background:"#fbbf2415",border:"1px solid #fbbf2430"}}>
          <span>🪙</span><span className="font-bold text-sm" style={{color:"#fbbf24"}}>{sv.coins||0}</span>
        </div>
        {/* 7-day login streak calendar */}
        <div className="flex items-center gap-1 px-2.5 py-1.5 rounded-xl" style={{background:theme.accent+"12",border:`1px solid ${theme.accent}28`}}>
          {Array.from({length:7},(_,i)=>{
            const dayPos=i+1;
            const streak=sv.loginStreak||0;
            const dayInCycle=((streak-1)%7)+1;
            const filled=streak>=7?true:dayPos<=dayInCycle;
            const isToday=dayPos===dayInCycle;
            return(
              <div key={i} style={{
                width:16,height:16,borderRadius:4,
                background:filled?"#fbbf24":"#ffffff12",
                border:isToday?`2px solid ${theme.accent}`:`1px solid ${filled?"#fbbf2444":"#ffffff10"}`,
                display:"flex",alignItems:"center",justifyContent:"center",fontSize:8,
                boxShadow:filled?"0 0 6px #fbbf2455":"none",
              }}>
                {filled?"✓":""}
              </div>
            );
          })}
          <span style={{fontSize:10,color:theme.accent,fontWeight:"bold",marginLeft:3}}>🔥 {sv.loginStreak||1}</span>
        </div>
        {(sv.unlockedAchievements||[]).length>0&&(
          <div className="flex items-center gap-1 px-3 py-1.5 rounded-xl" style={{background:"#ffffff08",border:"1px solid #ffffff15"}}>
            <span className="text-xs opacity-60">🏆 {sv.unlockedAchievements.length}/{ACHIEVEMENTS.length}</span>
          </div>
        )}
        {(sv.prestigeLevel||0)>0&&(()=>{
          const pt=PRESTIGE_TIERS[sv.prestigeLevel];
          return pt?.badge?(
            <div className="flex items-center gap-1 px-3 py-1.5 rounded-xl"
              style={{background:`${pt.color}18`,border:`1px solid ${pt.color}55`}}>
              <span style={{fontSize:13}}>{pt.badge}</span>
              <span className="text-xs font-bold" style={{color:pt.color}}>{pt.title}</span>
            </div>
          ):null;
        })()}
      </div>

      {/* Play button */}
      <NeonButton onClick={()=>{
        if(!saveRef.current.seenTutorial){
          setTutStep(0);
        } else if(!saveRef.current.seenMainStory){
          setStoryData({type:"main",panelIndex:0,onDone:()=>go("levelmap")});
        } else {
          setScreen("levelmap");
        }
      }} className="w-full py-5 text-2xl"
        style={{background:`linear-gradient(135deg,${theme.secondary},${theme.accent})`,boxShadow:`0 0 36px ${theme.accent}66`,
          fontSize:"1.4rem",letterSpacing:"0.1em"}}>
        🌟 START ADVENTURE!
      </NeonButton>

      {/* Daily Spin */}
      {(()=>{const alreadySpun=sv.lastSpinDate===getTodayKey();return(
        <NeonButton onClick={()=>!alreadySpun&&go("spinwheel")} className="w-full py-3 text-base"
          style={{background:alreadySpun?"#ffffff08":`${theme.accent}22`,border:`1px solid ${alreadySpun?"#ffffff15":theme.accent+"66"}`,
            color:alreadySpun?"#ffffff30":theme.accent,opacity:alreadySpun?0.5:1}}>
          🎡 {alreadySpun?"Daily Spin (come back tomorrow)":"Daily Spin — FREE prize!"}
        </NeonButton>
      );})()}

      {/* Zen Mode — always accessible from level 5+ */}
      {(sv.unlockedLevel||1)>=5&&(
        <NeonButton onClick={()=>go("zen")}
          className="w-full py-3 text-base font-black"
          style={{background:"linear-gradient(135deg,#05966922,#34d39933)",border:"2px solid #34d39966",
            color:"#34d399",boxShadow:"0 0 20px #34d39933",letterSpacing:"0.06em"}}>
          ☯ ZEN MODE {sv.zenBest>0?`• Best: ${sv.zenBest.toLocaleString()}`:""}
        </NeonButton>
      )}
      {/* Time Attack Mode — unlocked after level 3 */}
      {(sv.unlockedLevel||1)>=3&&(
        <NeonButton onClick={()=>go("timeattack")}
          className="w-full py-3 text-base font-black"
          style={{background:"linear-gradient(135deg,#ea580c22,#f9731633)",border:"2px solid #f9731666",
            color:"#f97316",boxShadow:"0 0 20px #f9731633",letterSpacing:"0.06em"}}>
          ⚡ TIME ATTACK {sv.taBest>0?`• Best: ${sv.taBest.toLocaleString()}`:""}
        </NeonButton>
      )}
      {/* Infinity Mode — unlocked after level 100 */}
      {(sv.unlockedLevel||1)>100&&(
        <NeonButton onClick={()=>go("infinity")}
          className="w-full py-3 text-base font-black"
          style={{background:"linear-gradient(135deg,#a78bfa22,#6d28d955)",border:"2px solid #a78bfa66",
            color:"#a78bfa",boxShadow:"0 0 24px #a78bfa33",letterSpacing:"0.06em"}}>
          ∞ INFINITY MODE {sv.infinityBest>0?`• Best: ${sv.infinityBest.toLocaleString()}`:""}
        </NeonButton>
      )}
      {/* Daily Boss Gauntlet */}
      {(()=>{
        const alreadyDone=sv.gauntletDate===getTodayKey();
        if((sv.unlockedLevel||1)<11)return null;
        return(
          <NeonButton onClick={()=>{if(!alreadyDone)go("gauntlet");}}
            className="w-full py-3 text-sm font-black"
            style={{background:alreadyDone?"#ffffff08":"#ff6b3522",border:`1px solid ${alreadyDone?"#ffffff15":"#ff6b3566"}`,
              color:alreadyDone?"#ffffff30":"#ff6b35",opacity:alreadyDone?0.5:1}}>
            ⚔️ {alreadyDone?"Daily Gauntlet (done today)":`Daily Boss Gauntlet! ${sv.gauntletBest>0?`• Record: ${sv.gauntletBest} bosses`:""}`}
          </NeonButton>
        );
      })()}
      {/* Weekly Challenge */}
      {(()=>{
        if((sv.unlockedLevel||1)<20)return null;
        const wk=getWeekKey();
        const isThisWeek=sv.weeklyChallengeDate===wk;
        const done=isThisWeek&&sv.weeklyChallengeCompleted;
        const wkLvl=getWeeklyChallengeLevel();
        return(
          <NeonButton onClick={()=>{if(!done)go("weekly");}}
            className="w-full py-3 text-sm font-black"
            style={{background:done?"#ffffff08":"linear-gradient(135deg,#a78bfa22,#7c3aed22)",
              border:`1px solid ${done?"#ffffff15":"#a78bfa66"}`,
              color:done?"#ffffff30":"#a78bfa",opacity:done?0.5:1}}>
            🗓️ {done?"Weekly Done — see you Monday!":`Weekly Challenge: Level ${wkLvl}!`}
          </NeonButton>
        );
      })()}
      {/* Daily Tournament */}
      {(()=>{
        if((sv.unlockedLevel||1)<5)return null;
        const today=getTodayKey();
        const isToday=sv.tournamentDate===today;
        const todayBest=isToday?(sv.tournamentBest||0):0;
        const dailyLvl=getDailyChallengeLevel();
        return(
          <NeonButton onClick={()=>go("tournament")}
            className="w-full py-3 text-sm font-black"
            style={{background:"linear-gradient(135deg,#fbbf2422,#d9770622)",
              border:"1px solid #fbbf2466",color:"#fbbf24"}}>
            🏆 Daily Tournament: Lv {dailyLvl} {todayBest>0?`• Best: ${todayBest.toLocaleString()}`:""}
          </NeonButton>
        );
      })()}
      {/* Quick links */}
      <div className="grid grid-cols-2 gap-3 w-full">
        {[{label:"🎯 Daily Quests",sc:"missions"},{label:"🏆 High Scores",sc:"leaderboard"},{label:"🏅 Trophies",sc:"achievements"},{label:"🌿 Skill Tree",sc:"skilltree"},{label:"⚙️ Settings",sc:"settings"}].map(b=>(
          <NeonButton key={b.sc} onClick={()=>setScreen(b.sc)} className="py-3 text-sm"
            style={{background:"#ffffff0d",border:`1px solid ${theme.accent}30`}}>{b.label}</NeonButton>
        ))}
      </div>
    </div>
  );};

  // ── Daily Spin Wheel ──
  const SPIN_PRIZES=[
    {label:"20 coins",   emoji:"🪙", value:20,  type:"coins", color:"#fbbf24"},
    {label:"50 coins",   emoji:"🪙", value:50,  type:"coins", color:"#f59e0b"},
    {label:"100 coins",  emoji:"💰", value:100, type:"coins", color:"#d97706"},
    {label:"100 XP",     emoji:"⭐", value:100, type:"xp",    color:"#60a5fa"},
    {label:"200 XP",     emoji:"🌟", value:200, type:"xp",    color:"#818cf8"},
    {label:"+1 Life",    emoji:"❤️", value:1,   type:"life",  color:"#f87171"},
    {label:"Lucky Star", emoji:"✨", value:0,   type:"lucky", color:"#ffd700"},
    {label:"Power-Up",   emoji:"⚡", value:0,   type:"powerup",color:"#34d399"},
  ];
  const renderSpinWheel=()=>{
    const sliceAngle=360/SPIN_PRIZES.length;
    const canSpin=!spinState;
    const doSpin=()=>{
      if(!canSpin)return;
      const prizeIdx=Math.floor(Math.random()*SPIN_PRIZES.length);
      const targetDeg=1800+prizeIdx*sliceAngle+(sliceAngle/2-10);
      setSpinState("spinning");setSpinDeg(prev=>prev+targetDeg);
      sfx("jackpot");
      setTimeout(()=>{
        const prize=SPIN_PRIZES[prizeIdx];
        const sv2=saveRef.current;
        if(prize.type==="coins"){sv2.coins=(sv2.coins||0)+prize.value;sv2.totalCoins=(sv2.totalCoins||0)+prize.value;}
        else if(prize.type==="xp"){sv2.xp=(sv2.xp||0)+prize.value;}
        else if(prize.type==="lucky"){/* handled in next session via lucky mode */}
        else if(prize.type==="life"){/* awarded at start of next level */sv2.pendingLife=true;}
        sv2.lastSpinDate=getTodayKey();
        flushSave();
        setSpinResult(prize);setSpinState("done");
        sfx("levelComplete");vibrate([20,30,20,60]);
      },2400);
    };
    return(
      <div className="flex flex-col h-full overflow-y-auto relative z-10 items-center justify-center px-5 gap-6"
        style={{background:"radial-gradient(ellipse at top,#0d0025 0%,#000510 60%)"}}>
        <style>{`@keyframes wheelSpin{to{transform:rotate(var(--wheel-end))}} @keyframes prizeReveal{0%{transform:scale(0.4);opacity:0}70%{transform:scale(1.18)}100%{transform:scale(1);opacity:1}}`}</style>
        <div className="flex items-center gap-3 self-start">
          <NeonButton onClick={()=>{setSpinState(null);setSpinResult(null);go("menu");}} className="px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
          <h2 className="text-xl font-black" style={{color:theme.accent}}>🎡 Daily Spin</h2>
        </div>
        {/* Wheel */}
        <div className="relative flex items-center justify-center" style={{width:260,height:260}}>
          {/* Pointer */}
          <div className="absolute z-10" style={{top:-12,left:"50%",transform:"translateX(-50%)",width:0,height:0,
            borderLeft:"10px solid transparent",borderRight:"10px solid transparent",
            borderTop:`22px solid ${theme.accent}`,filter:`drop-shadow(0 0 8px ${theme.accent})`}}/>
          {/* Wheel SVG */}
          <div style={{width:260,height:260,borderRadius:"50%",overflow:"hidden",
            transform:`rotate(${spinDeg}deg)`,
            transition:spinState==="spinning"?"transform 2.4s cubic-bezier(0.17,0.67,0.24,1)":"none",
            boxShadow:`0 0 40px ${theme.accent}55`,border:`3px solid ${theme.accent}66`}}>
            <svg width="260" height="260" viewBox="0 0 260 260">
              {SPIN_PRIZES.map((p,i)=>{
                const startAngle=(i*sliceAngle-90)*Math.PI/180;
                const endAngle=((i+1)*sliceAngle-90)*Math.PI/180;
                const x1=130+120*Math.cos(startAngle),y1=130+120*Math.sin(startAngle);
                const x2=130+120*Math.cos(endAngle),y2=130+120*Math.sin(endAngle);
                const mx=130+75*Math.cos((startAngle+endAngle)/2),my=130+75*Math.sin((startAngle+endAngle)/2);
                return(
                  <g key={i}>
                    <path d={`M130,130 L${x1},${y1} A120,120 0 0,1 ${x2},${y2} Z`} fill={p.color} opacity={0.85}/>
                    <text x={mx} y={my} textAnchor="middle" dominantBaseline="middle"
                      fontSize="18" style={{userSelect:"none"}}>{p.emoji}</text>
                  </g>
                );
              })}
              <circle cx="130" cy="130" r="18" fill="#1a1a2e" stroke={theme.accent} strokeWidth="2"/>
            </svg>
          </div>
        </div>
        {/* Prize reveal */}
        {spinState==="done"&&spinResult&&(
          <div className="flex flex-col items-center gap-3 text-center" style={{animation:"prizeReveal 0.6s ease-out"}}>
            <div style={{fontSize:56,filter:`drop-shadow(0 0 20px ${spinResult.color})`}}>{spinResult.emoji}</div>
            <div className="font-black text-2xl" style={{color:spinResult.color,fontFamily:"'Rajdhani',sans-serif",
              textShadow:`0 0 20px ${spinResult.color}`}}>
              YOU WON: {spinResult.label.toUpperCase()}!
            </div>
            <NeonButton onClick={()=>{setSpinState(null);setSpinResult(null);go("menu");}} className="px-8 py-3 text-base"
              style={{background:`linear-gradient(135deg,${theme.secondary||theme.accent},${theme.accent})`}}>
              🎉 Awesome! Collect
            </NeonButton>
          </div>
        )}
        {/* Spin button */}
        {!spinState&&(
          <NeonButton onClick={doSpin} className="px-12 py-4 text-xl font-black"
            style={{background:`linear-gradient(135deg,${theme.secondary||theme.accent},${theme.accent})`,
              boxShadow:`0 0 40px ${theme.accent}88`,fontSize:"1.3rem",letterSpacing:"0.08em"}}>
            🎡 SPIN!
          </NeonButton>
        )}
        {spinState==="spinning"&&(
          <div className="font-black text-lg" style={{color:theme.accent,animation:"shimmer 1s linear infinite"}}>
            ✨ Spinning... ✨
          </div>
        )}
        <p className="text-xs opacity-40 text-center" style={{color:theme.accent}}>One free spin per day — come back tomorrow!</p>
        {/* 10D — Bonus spin for login streak >= 7 */}
        {(sv.loginStreak||0)>=7&&(()=>{
          const bonusSpinKey=`bonusSpin_${getTodayKey()}`;
          const alreadyBonused=sv[bonusSpinKey];
          return!alreadyBonused?(
            <div className="w-full rounded-2xl p-4 text-center" style={{background:"#ffd70015",border:"2px solid #ffd70066",boxShadow:"0 0 24px #ffd70033"}}>
              <div className="font-black text-sm mb-1" style={{color:"#ffd700",letterSpacing:"0.06em"}}>🔥 7-DAY STREAK BONUS!</div>
              <div className="text-xs opacity-60 mb-3" style={{color:"#fbbf24"}}>You get an extra bonus spin today!</div>
              <NeonButton onClick={()=>{
                if(spinState==="spinning")return;
                const sv2=saveRef.current;
                sv2[bonusSpinKey]=true;
                // Bonus spin always gives double rewards
                const prizeIdx=Math.floor(Math.random()*SPIN_PRIZES.length);
                const prize=SPIN_PRIZES[prizeIdx];
                if(prize.type==="coins"){const b=prize.value*2;sv2.coins=(sv2.coins||0)+b;sv2.totalCoins=(sv2.totalCoins||0)+b;}
                else if(prize.type==="xp"){sv2.xp=(sv2.xp||0)+prize.value*2;}
                flushSave();
                sfx("jackpot");vibrate([30,15,30,15,60]);
                setNotif(`🎁 BONUS SPIN: ${prize.emoji} ${prize.label} ×2! 🔥`);
                setTimeout(()=>setNotif(null),3000);
              }} style={{background:"linear-gradient(135deg,#b45309,#fbbf24)",boxShadow:"0 0 20px #ffd70066",letterSpacing:"0.05em"}}>
                🎡 CLAIM BONUS SPIN
              </NeonButton>
            </div>
          ):null;
        })()}
      </div>
    );
  };

  // ── Level Map ──
  const renderLevelMap=()=>{
    const unlockedTo=sv.unlockedLevel||1;
    // Zigzag column positions: 0=left(20%), 1=center(50%), 2=right(80%)
    const colX=(col,w)=>w*[0.16,0.5,0.84][col];
    const colPattern=[0,1,2,1,0,1,2,1,0,1]; // per position in world (0-9)
    return(
      <div className="flex flex-col h-full relative z-10">
        {/* Header */}
        <div className="flex items-center gap-3 px-4 py-3 z-10" style={{background:"rgba(0,0,0,0.6)",backdropFilter:"blur(6px)",borderBottom:`1px solid ${theme.accent}22`}}>
          <NeonButton onClick={()=>go("menu")} className="px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
          <h2 className="text-xl font-black" style={{color:theme.accent}}>🗺️ Adventure Map</h2>
          <span className="ml-auto text-sm font-bold" style={{color:"#fbbf24"}}>🪙 {sv.coins||0}</span>
        </div>
        {/* Scrollable map */}
        <div className="flex-1 overflow-y-auto" style={{WebkitOverflowScrolling:"touch"}}>
          {WORLDS.map(world=>{
            const levelStart=(world.id-1)*10+1;
            const levels=ALL_LEVELS.slice(levelStart-1,levelStart+9);
            const doneLevels=levels.filter(l=>(sv.levelStars[l.id]||0)>0).length;
            const worldFullyDone=doneLevels===10;
            // Total stars in this world (max 30)
            const worldStars=levels.reduce((s,lv)=>s+(sv.levelStars[lv.id]||0),0);
            // Is this the "current" world? (player is unlocking levels here)
            const isCurrentWorld=unlockedTo>=levelStart&&unlockedTo<=levelStart+9;
            return(
              <div key={world.id} className="mb-2">
                {/* World banner — immersive, tappable for story */}
                <div className="flex items-center gap-3 px-4 py-3 mx-2 mt-3 rounded-2xl relative overflow-hidden"
                  onClick={()=>{
                    setStoryData({type:"world",worldId:world.id,panelIndex:0,onDone:()=>{}});
                  }}
                  style={{background:`${world.color}15`,border:`1px solid ${world.color}${isCurrentWorld?"99":"55"}`,backdropFilter:"blur(6px)",
                    boxShadow:worldFullyDone?`0 0 24px ${world.color}66`:isCurrentWorld?`0 0 16px ${world.color}55`:"none",
                    cursor:"pointer"}}>
                  {/* World-color gradient bg */}
                  <div style={{position:"absolute",inset:0,background:`linear-gradient(120deg,${world.color}18 0%,transparent 65%)`,pointerEvents:"none"}}/>
                  {/* Shimmer sweep — only for fully-done worlds */}
                  {worldFullyDone&&(
                    <div style={{position:"absolute",inset:0,pointerEvents:"none",
                      background:`linear-gradient(120deg,transparent 35%,${world.color}33 50%,transparent 65%)`,
                      backgroundSize:"200% 100%",animation:"shimmer 4s linear infinite"}}/>
                  )}
                  <span style={{fontSize:32,lineHeight:1,zIndex:1,
                    filter:`drop-shadow(0 0 ${worldFullyDone?12:6}px ${world.color})`,
                    animation:isCurrentWorld?"floatGlow 2.4s ease-in-out infinite":"none"}}>{world.emoji}</span>
                  <div style={{zIndex:1,flex:1}}>
                    <div className="font-black text-base leading-tight" style={{color:world.color}}>{world.name}</div>
                    <div className="text-xs opacity-60 flex items-center gap-2" style={{color:world.color}}>
                      <span>World {world.id}</span>
                      <span style={{opacity:0.5}}>·</span>
                      <span>⭐ {worldStars}/30</span>
                      <span style={{opacity:0.5}}>·</span>
                      <span>📖 Story</span>
                    </div>
                  </div>
                  {worldFullyDone&&<div className="ml-auto flex flex-col items-center gap-0.5" style={{zIndex:1}}>
                    <span style={{fontSize:22,animation:"heartbeat 1.6s ease-in-out infinite",
                      filter:`drop-shadow(0 0 8px ${world.color})`}}>🏆</span>
                    {worldStars>=30
                      ?<span style={{fontSize:8,fontWeight:"black",letterSpacing:"0.06em",
                          color:"#ffd700",background:"#ffd70022",padding:"1px 6px",borderRadius:6,
                          border:"1px solid #ffd70066",animation:"shimmer 2s linear infinite",whiteSpace:"nowrap"}}>⭐ PERFECT</span>
                      :<span style={{fontSize:8,color:world.color,opacity:0.7}}>⭐ {worldStars}/30</span>
                    }
                  </div>}
                  {!worldFullyDone&&<div className="ml-auto flex flex-col items-end gap-0.5" style={{zIndex:1}}>
                    {isCurrentWorld&&<span style={{fontSize:8,fontWeight:"black",letterSpacing:"0.06em",
                      color:world.color,background:`${world.color}22`,padding:"1px 5px",borderRadius:6,
                      border:`1px solid ${world.color}66`}}>📍 HERE</span>}
                    <div className="flex gap-0.5">
                      {Array.from({length:10},(_,i)=>(
                        <div key={i} style={{width:5,height:14,borderRadius:2,
                          background:i<doneLevels?world.color:`${world.color}25`,
                          boxShadow:i<doneLevels?`0 0 4px ${world.color}`:"none"}}/>
                      ))}
                    </div>
                  </div>}
                </div>
                {/* Level nodes in zigzag */}
                <div className="relative" style={{height:levels.length*88+20}}>
                  {/* SVG connection lines */}
                  <svg className="absolute inset-0 pointer-events-none" width="100%" height="100%">
                    {levels.map((lv,i)=>{
                      if(i>=levels.length-1)return null;
                      const W=window.innerWidth;
                      const c1=colPattern[i],c2=colPattern[i+1];
                      const x1=colX(c1,W),y1=40+i*88;
                      const x2=colX(c2,W),y2=40+(i+1)*88;
                      const unlocked=(sv.levelStars[lv.id]||0)>0;
                      return<line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke={unlocked?world.color+"77":"#ffffff15"} strokeWidth={unlocked?3:1.5} strokeDasharray={unlocked?"none":"6,4"}/>;
                    })}
                  </svg>
                  {/* Level nodes */}
                  {levels.map((lv,i)=>{
                    const W=window.innerWidth;
                    const col=colPattern[i];
                    const x=colX(col,W);
                    const y=40+i*88;
                    const stars=sv.levelStars[lv.id]||0;
                    const locked=lv.id>unlockedTo;
                    const isAvailable=lv.id===unlockedTo;
                    const isCurrent=lv.id===selectedLevel;
                    const nodeR=lv.isBoss?38:30;
                    const nodeLabel=locked?"🔒":lv.isBoss?(stars>0?"👑":world.emoji):lv.id;
                    return(
                      <div key={lv.id}
                        className="absolute flex flex-col items-center"
                        style={{left:x,top:y,transform:"translate(-50%,-50%)",width:90,cursor:locked?"default":"pointer"}}
                        onClick={()=>{if(!locked){
                          if(stars>0&&lv.id<(sv.unlockedLevel||1)){setReplayModal(lv.id);}
                          else{setSelectedLevel(lv.id);go("shop");}
                        }}}
                        onTouchEnd={e=>{e.preventDefault();if(!locked){
                          if(stars>0&&lv.id<(sv.unlockedLevel||1)){setReplayModal(lv.id);}
                          else{setSelectedLevel(lv.id);go("shop");}
                        }}}
                      >
                        {/* Node circle */}
                        <div className="flex items-center justify-center rounded-full font-black transition-all"
                          style={{
                            width:nodeR*2,height:nodeR*2,
                            background:locked?"#111118":stars>0?`${world.color}44`:`${world.color}1a`,
                            border:`${isCurrent||isAvailable?3:2}px solid ${locked?"#2a2a3a":world.color}`,
                            boxShadow:isAvailable?`0 0 24px ${world.color}99,0 0 48px ${world.color}33`:
                                       stars===3?`0 0 16px ${world.color}77`:
                                       lv.isBoss&&!locked?`0 0 12px ${world.color}55`:"none",
                            color:locked?"#333":world.color,
                            animation:isAvailable?"levelPulse 1.5s ease-in-out infinite":"none",
                            fontSize:lv.isBoss?22:13,
                          }}>
                          {nodeLabel}
                        </div>
                        {/* Stars */}
                        <div className="flex gap-0.5 mt-1">
                          {[1,2,3].map(s=>(
                            <span key={s} style={{fontSize:10,opacity:stars>=s?1:0.15,color:"#fbbf24",
                              filter:stars>=s?"drop-shadow(0 0 4px #fbbf24)":"none"}}>★</span>
                          ))}
                        </div>
                        {/* Name shown for available + boss levels */}
                        {(!locked&&(isAvailable||isCurrent||lv.isBoss))&&(
                          <div className="text-center mt-0.5" style={{fontSize:9,color:world.color,opacity:0.85,maxWidth:82,lineHeight:1.2,fontWeight:"bold"}}>
                            {lv.name}
                          </div>
                        )}
                      </div>
                    );
                  })}
                </div>
              </div>
            );
          })}
          <div style={{height:60}}/>
        </div>
      {/* Replay modal — absolute overlay inside the levelmap container */}
      {replayModal&&(()=>{
        const rlv=getLevelConfig(replayModal);
        const rstars=sv.levelStars[replayModal]||0;
        const _s0=sv.scores?.[0];const prevBest=sv.levelScores?.[replayModal]||(typeof _s0==="number"?_s0:_s0?.score)||0;
        return(
          <div className="absolute inset-0 flex items-center justify-center z-50 px-6"
            style={{background:"rgba(0,0,0,0.88)",backdropFilter:"blur(12px)"}}>
            <div className="w-full max-w-sm rounded-3xl p-6 flex flex-col gap-4"
              style={{background:`${rlv.worldColor}14`,border:`2px solid ${rlv.worldColor}55`}}>
              <div className="text-center">
                <div className="font-black text-lg" style={{color:rlv.worldColor}}>Level {rlv.id}: {rlv.name}</div>
                <div className="flex justify-center gap-1 mt-1">
                  {[1,2,3].map(s=><span key={s} style={{fontSize:18,opacity:rstars>=s?1:0.2,color:"#fbbf24"}}>★</span>)}
                </div>
                {prevBest>0&&<div className="text-xs mt-1 opacity-60" style={{color:rlv.worldColor}}>Best: {prevBest.toLocaleString()} pts</div>}
              </div>
              <NeonButton onClick={()=>{setReplayModal(null);setSelectedLevel(replayModal);go("shop");}}
                className="w-full py-4 font-black"
                style={{background:`linear-gradient(135deg,${rlv.worldColor}33,${rlv.worldColor}55)`,border:`2px solid ${rlv.worldColor}`,color:rlv.worldColor}}>
                🎮 Play Again
              </NeonButton>
              {rstars>0&&(
                <NeonButton onClick={()=>{
                  // Pick a random challenge modifier (not boss_kill or final_boss)
                  const challengeMods=[
                    {type:"combo_20",desc:"20× combo challenge ⭐"},
                    {type:"no_miss",desc:"Perfect run — no misses! 🎯"},
                    {type:"moving_only",desc:"Moving targets only! 💨"},
                    {type:"ghost_rush",desc:"Find the ghosts! 👻"},
                    {type:"fever_2",desc:"Activate Fever twice! ✨"},
                  ];
                  const mod=challengeMods[Math.floor(Math.random()*challengeMods.length)];
                  const challengeCfg=getLevelConfig(replayModal);
                  // Apply modifier and +50% XP bonus flag
                  challengeCfg.modifier=mod;
                  challengeCfg._challengeBonus=true; // endLevel checks this for +50% XP
                  setReplayModal(null);
                  // Start directly (no shop)
                  startGame(replayModal,[],null);
                  // Override the config after startGame sets it
                  setTimeout(()=>{if(levelCfgRef.current){levelCfgRef.current.modifier=mod;levelCfgRef.current._challengeBonus=true;}},50);
                }}
                  className="w-full py-3 font-black"
                  style={{background:"linear-gradient(135deg,#fbbf2422,#f9780433)",border:"2px solid #fbbf2488",color:"#fbbf24"}}>
                  ⚡ Challenge Mode <span style={{fontSize:10,opacity:0.7}}>+50% XP</span>
                </NeonButton>
              )}
              <NeonButton onClick={()=>setReplayModal(null)}
                className="w-full py-3 text-sm" style={{background:"#ffffff08",border:"1px solid #ffffff15"}}>
                Cancel
              </NeonButton>
            </div>
          </div>
        );
      })()}
      </div>
    );
  };

  // ── Shop ──
  const renderShop=()=>{
    const cfg=getLevelConfig(selectedLevel);
    const coins=sv.coins||0;
    return(
      <div className="flex flex-col h-full px-4 py-5 gap-4 overflow-y-auto relative z-10">
        <div className="flex items-center justify-between">
          <div className="flex items-center gap-3">
            <NeonButton onClick={()=>go("levelmap")} className="px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Map</NeonButton>
            <div>
              <div className="font-black text-base" style={{color:cfg.worldColor}}>Level {cfg.id}</div>
              <div className="text-xs opacity-60" style={{color:cfg.worldColor}}>{cfg.name}</div>
            </div>
          </div>
          <div className="flex items-center gap-1 px-3 py-1 rounded-full" style={{background:"#fbbf2415"}}>
            <span>🪙</span><span className="font-bold text-sm" style={{color:"#fbbf24"}}>{coins}</span>
          </div>
        </div>
        {/* Level info */}
        <div className="rounded-2xl p-4" style={{background:`${cfg.worldColor}12`,border:`1px solid ${cfg.worldColor}44`}}>
          <div className="flex justify-between items-center mb-2">
            <span className="text-sm font-bold" style={{color:cfg.worldColor}}>Goal: {cfg.scoreGoal.toLocaleString()} pts</span>
            <div className="flex items-center gap-2">
              {/* Difficulty rating D1-D5 */}
              {(()=>{
                const diff=Math.min(5,Math.ceil((cfg.id||1)/20));
                const diffColors=["","#4ade80","#fbbf24","#f97316","#ef4444","#a855f7"];
                const diffLabels=["","Easy","Medium","Hard","Expert","Legendary"];
                return(
                  <span className="text-xs font-black px-1.5 py-0.5 rounded"
                    style={{background:diffColors[diff]+"22",color:diffColors[diff],border:`1px solid ${diffColors[diff]}55`}}>
                    D{diff} {diffLabels[diff]}
                  </span>
                );
              })()}
              <div className="flex gap-0.5">{[1,2,3].map(s=><span key={s} style={{fontSize:12,opacity:(sv.levelStars[cfg.id]||0)>=s?1:0.2,color:"#fbbf24"}}>★</span>)}</div>
            </div>
          </div>
          <div className="flex gap-4 text-xs opacity-60" style={{color:cfg.worldColor}}>
            <span>❤️ {cfg.lives} lives</span>
            {cfg.modifier&&<span className="font-bold" style={{color:"#fbbf24"}}>⚡ {cfg.modifier.desc}</span>}
          </div>
        </div>
        {/* Target type preview — show which special targets may appear in this level */}
        {(()=>{
          const id=cfg.id||1;
          const previews=[];
          if(id>=1) previews.push({icon:"🟣",label:"Normal",desc:"Standard tap target"});
          if(id>=1) previews.push({icon:"💣",label:"Bomb",desc:"DON'T tap! Costs a life"});
          if(id>=3) previews.push({icon:"⚡",label:"Power-up",desc:"Special abilities"});
          if(id>=5) previews.push({icon:"🫧",label:"Bubble",desc:"Easy pop, wide zone"});
          if(id>=5) previews.push({icon:"❓",label:"Mystery",desc:"Spin for surprise!"});
          if(id>=8) previews.push({icon:"🪙",label:"Chest",desc:"Drops 3-5 coins"});
          if(id>=10) previews.push({icon:"🔮",label:"Treasure",desc:"Jackpot score bonus"});
          if(id>=12) previews.push({icon:"🎭",label:"Mimic",desc:"Copies last rarity"});
          if(id>=14) previews.push({icon:"🧲",label:"Magnet",desc:"Pulls nearby targets"});
          if(id>=16) previews.push({icon:"🌀",label:"Anchor",desc:"Freezes all movers"});
          if(id>=18) previews.push({icon:"💚",label:"Healer",desc:"Restore 1 life"});
          if(id>=16) previews.push({icon:"⚡",label:"Bouncy",desc:"Fast! Bounces around"});
          if(id>=20) previews.push({icon:"👻",label:"Phantom",desc:"Ultra-short 1.1s life"});
          if(id>=22) previews.push({icon:"✦",label:"Twin",desc:"Tap one, score two!"});
          if(id>=24) previews.push({icon:"❄️",label:"Frozen",desc:"Tiny hitbox, 3× pts"});
          if(id>=24) previews.push({icon:"🌀",label:"Tornado",desc:"Scatters all targets"});
          if(id>=25) previews.push({icon:"💥",label:"Volatile",desc:"Defuse it before boom!"});
          if(id>=28) previews.push({icon:"🟣",label:"Shielded",desc:"Needs 2 taps"});
          if(id>=30) previews.push({icon:"🌈",label:"Rainbow",desc:"Cycling rarity tiers"});
          if(id>=35) previews.push({icon:"🥷",label:"Ninja",desc:"Invisible mostly!"});
          if(id>=40) previews.push({icon:"💥",label:"Splitter",desc:"Splits into 3!"});
          if(id>=10) previews.push({icon:"🍀",label:"Clover",desc:"Random ×1-10 multiplier!"});
          if(id>=12) previews.push({icon:"🎯",label:"Ricochet",desc:"Kills 2 nearby targets!"});
          if(id>=15) previews.push({icon:"⚡",label:"Voltage",desc:"Chain zaps 3 targets!"});
          if(id>=15) previews.push({icon:"💥",label:"Particle Bomb",desc:"Explodes into sparks!"});
          if(id>=20) previews.push({icon:"🌀",label:"Portal",desc:"Teleports all targets!"});
          if(id>=28) previews.push({icon:"🌌",label:"Aurora",desc:"Rhythm tap multiplier"});
          if(id>=35) previews.push({icon:"🌀",label:"Void",desc:"Absorbs nearby targets"});
          if(cfg.isBoss) previews.push({icon:WORLDS[cfg.world-1].emoji,label:"BOSS",desc:"Multi-hit epic fight!"});
          const show=previews.slice(-8); // Show last 8 relevant types for this level
          return(
            <div className="rounded-2xl px-4 py-3" style={{background:`${cfg.worldColor}08`,border:`1px solid ${cfg.worldColor}22`}}>
              <div className="text-xs font-bold opacity-40 mb-2 uppercase tracking-widest" style={{color:cfg.worldColor}}>🎯 Targets in this level</div>
              <div className="flex flex-wrap gap-1.5">
                {show.map((t,i)=>(
                  <div key={i} className="flex items-center gap-1 px-2 py-1 rounded-lg"
                    style={{background:`${cfg.worldColor}12`,border:`1px solid ${cfg.worldColor}25`}}>
                    <span style={{fontSize:12}}>{t.icon}</span>
                    <span className="text-xs font-bold" style={{color:cfg.worldColor,opacity:0.8}}>{t.label}</span>
                  </div>
                ))}
              </div>
            </div>
          );
        })()}

        {/* Boss incoming warning — level X9 means next level is a boss */}
        {cfg.id%10===9&&!cfg.isBoss&&(
          <div className="rounded-2xl px-4 py-3 flex items-center gap-3"
            style={{background:"#ff000012",border:"1px solid #ff000044",
              animation:"heartbeat 1.2s ease-in-out infinite"}}>
            <span style={{fontSize:24}}>⚠️</span>
            <div>
              <div className="font-black text-sm" style={{color:"#ff6b35"}}>BOSS INCOMING!</div>
              <div className="text-xs opacity-70" style={{color:"#ff9f80"}}>
                Level {cfg.id+1} features a powerful boss. Consider buying a Shield or Extra Life!
              </div>
            </div>
          </div>
        )}
        {/* Story teaser card */}
        {(()=>{const ws=WORLD_STORIES.find(s=>s.worldId===cfg.world);if(!ws)return null;
          const snippet=cfg.isBoss?ws.bossIntro?.text:ws.panels[0]?.text;
          if(!snippet)return null;
          const emo=cfg.isBoss?(ws.bossIntro?.emoji||WORLDS[cfg.world-1].emoji):ws.panels[0]?.emoji;
          return(
            <div className="rounded-2xl px-4 py-3 flex items-center gap-3 cursor-pointer"
              style={{background:`${cfg.worldColor}0c`,border:`1px solid ${cfg.worldColor}33`}}
              onClick={()=>{
                if(cfg.isBoss){setStoryData({type:"boss",worldId:cfg.world,bossIntro:ws.bossIntro,onDone:()=>{}});}
                else{setStoryData({type:"world",worldId:cfg.world,panelIndex:0,onDone:()=>{}});}
              }}>
              <span style={{fontSize:26,flexShrink:0}}>{emo}</span>
              <div>
                <div className="text-xs font-bold mb-0.5" style={{color:cfg.worldColor}}>📖 Story</div>
                <div className="text-xs opacity-60 leading-snug" style={{color:cfg.worldColor,overflow:"hidden",display:"-webkit-box",WebkitLineClamp:2,WebkitBoxOrient:"vertical"}}>{snippet}</div>
              </div>
            </div>
          );
        })()}
        <p className="text-xs opacity-40 uppercase tracking-widest" style={{color:cfg.worldColor}}>Optional Boosts</p>
        {SHOP_ITEMS.map(item=>{
          const inCart=cartItems.includes(item.id),canBuy=coins>=item.cost||inCart;
          return(
            <div key={item.id} className="rounded-2xl p-3 flex items-center gap-3"
              style={{background:inCart?`${cfg.worldColor}18`:"#ffffff08",border:`1px solid ${inCart?cfg.worldColor:"#ffffff15"}`}}>
              <span className="text-2xl">{item.icon}</span>
              <div className="flex-1">
                <div className="font-bold text-sm" style={{color:inCart?cfg.worldColor:"#fff"}}>{item.name}</div>
                <div className="text-xs opacity-50">{item.desc}</div>
              </div>
              <NeonButton onClick={()=>{
                if(inCart){setCartItems(c=>c.filter(i=>i!==item.id));}
                else if(coins>=item.cost){setCartItems(c=>[...c,item.id]);}
              }} className="px-3 py-2 text-xs"
                style={{background:inCart?`${cfg.worldColor}33`:"#ffffff0d",border:`1px solid ${inCart?cfg.worldColor:"#ffffff22"}`,
                  color:inCart?cfg.worldColor:canBuy?"#fff":"#444"}}>
                {inCart?"✓":canBuy?`🪙${item.cost}`:`🔒${item.cost}`}
              </NeonButton>
            </div>
          );
        })}
        <NeonButton onClick={()=>{
          const total=cartItems.reduce((s,id)=>s+(SHOP_ITEMS.find(i=>i.id===id)?.cost||0),0);
          if(sv.coins<total){setNotif("Not enough coins! 🪙");return;}
          // The actual start logic, extracted so confirmation can call it
          const doStart=()=>{
            sv.coins-=total;flushSave();
            const isFirstLevelOfWorld=cfg.id%10===1;
            const seenWorlds=saveRef.current.seenWorldStories||[];
            const seenBosses=saveRef.current.seenBossIntros||[];
            if(cfg.isBoss&&!seenBosses.includes(cfg.world)){
              const ws=WORLD_STORIES.find(s=>s.worldId===cfg.world);
              if(ws?.bossIntro){
                setStoryData({type:"boss",worldId:cfg.world,bossIntro:ws.bossIntro,onDone:()=>startGame(selectedLevel,cartItems)});
                return;
              }
            }
            if(isFirstLevelOfWorld&&!seenWorlds.includes(cfg.world)){
              setStoryData({type:"world",worldId:cfg.world,panelIndex:0,onDone:()=>startGame(selectedLevel,cartItems)});
            } else {
              startGame(selectedLevel,cartItems);
            }
          };
          // Confirm if buying anything
          if(total>0){
            setShopConfirm({total,items:[...cartItems],onConfirm:()=>{setShopConfirm(null);doStart();}});
          } else {
            doStart();
          }
        }} className="w-full py-4 text-xl mt-1"
          style={{background:`linear-gradient(135deg,${cfg.worldColor}88,${cfg.worldColor})`,boxShadow:`0 0 28px ${cfg.worldColor}55`}}>
          {cartItems.length>0?`▶ START (${cartItems.reduce((s,id)=>s+(SHOP_ITEMS.find(i=>i.id===id)?.cost||0),0)}🪙)`:"▶ START FREE"}
        </NeonButton>
      </div>
    );
  };

  // ── Playing overlay ──
  const renderPlaying=()=>{
    const cfg=levelCfgRef.current;
    const wc=cfg?WORLDS[cfg.world-1].color:theme.accent;
    const speedMode=sv.speedMode||1.0;
    const speedLabel=speedMode<=0.75?"🐢":speedMode>=1.5?"🔥":speedMode>=1.2?"⚡":speedMode>=1?"":"";
    return(
      <div className="absolute inset-0" onTouchStart={handleTap} onClick={handleTap}
        onTouchMove={e=>{const r=e.currentTarget.getBoundingClientRect();const t=e.touches[0];if(t)tapTrailRef.current.push({x:t.clientX-r.left,y:t.clientY-r.top,ts:performance.now()});}}
        onMouseMove={e=>{const r=e.currentTarget.getBoundingClientRect();tapTrailRef.current.push({x:e.clientX-r.left,y:e.clientY-r.top,ts:performance.now()});}}
        style={{touchAction:"none",zIndex:10}}>
        {/* Mode indicators (top corners, non-default modes only) */}
        <div className="absolute top-2 right-2 z-30 flex flex-col gap-1 items-end pointer-events-none">
          {speedMode!==1.0&&(
            <div style={{padding:"3px 8px",borderRadius:8,background:`${wc}33`,border:`1px solid ${wc}66`,
              color:"#fff",fontSize:10,fontWeight:"bold",letterSpacing:"0.04em",boxShadow:`0 0 8px ${wc}44`}}>
              {speedLabel} {speedMode.toFixed(1)}×
            </div>
          )}
          {sv.colorblindMode&&(
            <div style={{padding:"3px 8px",borderRadius:8,background:`${wc}33`,border:`1px solid ${wc}66`,
              color:"#fff",fontSize:10,fontWeight:"bold",letterSpacing:"0.04em"}}>
              ♛ CB
            </div>
          )}
          {sv.adaptiveDifficulty&&(
            <div style={{padding:"3px 8px",borderRadius:8,background:"#06b6d433",border:"1px solid #06b6d466",
              color:"#fff",fontSize:10,fontWeight:"bold",letterSpacing:"0.04em"}}>
              🎯 AUTO
            </div>
          )}
        </div>
        {/* HUD */}
        <div className="absolute top-0 left-0 right-0 z-20 flex items-stretch"
          style={{background:"rgba(0,0,0,0.62)",backdropFilter:"blur(6px)",borderBottom:`1px solid ${wc}22`}}>
          <div className="flex-1 flex flex-col items-center justify-center py-2 px-1" style={{position:"relative"}}>
            {cfg&&!cfg.isInfinity&&!cfg.isZen&&(()=>{
              const pct=Math.min(1,hud.score/cfg.scoreGoal);
              const r=29,s=3,cx=31,cy=31;
              const circ=2*Math.PI*r;
              const dash=pct*circ;
              const clr=pct>=0.85?"#f97316":pct>=0.6?"#fbbf24":"#4ade80";
              return(
                <svg width={62} height={62} style={{position:"absolute",top:"50%",left:"50%",transform:"translate(-50%,-50%)",pointerEvents:"none",overflow:"visible",zIndex:0}}>
                  <circle cx={cx} cy={cy} r={r} fill="none" stroke="#ffffff08" strokeWidth={s}/>
                  <circle cx={cx} cy={cy} r={r} fill="none" stroke={clr} strokeWidth={s}
                    strokeDasharray={`${dash} ${circ}`} strokeLinecap="round"
                    transform={`rotate(-90 ${cx} ${cy})`}
                    style={{transition:"stroke-dasharray 0.35s ease-out",filter:`drop-shadow(0 0 4px ${clr})`}}/>
                </svg>
              );
            })()}
            {cfg?.isZen&&hud.timeLeft!=null&&(()=>{
              const totalSec=Math.ceil((cfg.zenDuration||90000)/1000);
              const pct=hud.timeLeft/totalSec;
              const r=29,s=3.5,cx=31,cy=31;
              const circ=2*Math.PI*r;
              const dash=pct*circ;
              const clr=pct<0.2?"#ef4444":pct<0.45?"#f97316":pct<0.7?"#fbbf24":"#34d399";
              return(
                <svg width={62} height={62} style={{position:"absolute",top:"50%",left:"50%",transform:"translate(-50%,-50%)",pointerEvents:"none",overflow:"visible",zIndex:0}}>
                  <circle cx={cx} cy={cy} r={r} fill="none" stroke="#ffffff06" strokeWidth={s}/>
                  <circle cx={cx} cy={cy} r={r} fill="none" stroke={clr} strokeWidth={s}
                    strokeDasharray={`${dash} ${circ}`} strokeLinecap="round"
                    transform={`rotate(-90 ${cx} ${cy})`}
                    style={{transition:"stroke-dasharray 0.9s linear",filter:`drop-shadow(0 0 5px ${clr})`,
                      animation:pct<0.2?"heartbeat 0.7s ease-in-out infinite":"none"}}/>
                </svg>
              );
            })()}
            <div className="text-xs opacity-35 tracking-widest uppercase" style={{color:wc,position:"relative",zIndex:1}}>
              {cfg?.isZen?"TIME":"Score"}
            </div>
            <div className="text-xl font-black tabular-nums" style={{color:wc,position:"relative",zIndex:1}}>{hud.score.toLocaleString()}</div>
            {cfg?.isZen&&hud.timeLeft!=null
              ?<div className="text-xs font-bold tabular-nums" style={{color:hud.timeLeft<=15?"#ef4444":hud.timeLeft<=30?"#f97316":"#34d399",position:"relative",zIndex:1,textShadow:hud.timeLeft<=15?"0 0 8px #ef4444":"none"}}>
                  {hud.timeLeft}s ☯
                </div>
              :<>{cfg&&!cfg.isZen&&<div className="text-xs opacity-40 tabular-nums" style={{color:wc,position:"relative",zIndex:1}}>/{cfg.scoreGoal.toLocaleString()}</div>}</>
            }
          </div>
          <div className="flex flex-col items-center justify-center px-2 py-1.5 gap-1">
            <div className="flex gap-0.5">{Array.from({length:MAX_LIVES},(_,i)=><span key={i} style={{fontSize:13,opacity:i<hud.lives?1:0.18}}>{i<hud.lives?"❤️":"🖤"}</span>)}</div>
            {cfg&&<div className="text-xs font-bold px-1.5 py-0.5 rounded-full" style={{background:wc+"22",color:wc}}>{WORLDS[cfg.world-1].emoji} L{cfg.id}</div>}
            {/* Mascot HUD chip — tiny */}
            <span style={{fontSize:16,lineHeight:1}}>{currentMascot.e[mascotMood]||currentMascot.e.idle}</span>
          </div>
          <div className="flex-1 flex flex-col items-center justify-center py-2 px-1" style={{position:"relative"}}>
            {hud.streak>0&&(()=>{
              const dp=Math.max(0,hud.decayPct??1);
              const r=29,s=3,cx=31,cy=31;
              const circ=2*Math.PI*r;
              const dash=dp*circ;
              const clr=streakDecaying?"#ef4444":streakColor();
              return(
                <svg width={62} height={62} style={{position:"absolute",top:"50%",left:"50%",transform:"translate(-50%,-50%)",pointerEvents:"none",overflow:"visible",zIndex:0}}>
                  <circle cx={cx} cy={cy} r={r} fill="none" stroke="#ffffff06" strokeWidth={s}/>
                  <circle cx={cx} cy={cy} r={r} fill="none" stroke={clr} strokeWidth={s}
                    strokeDasharray={`${dash} ${circ}`} strokeLinecap="round"
                    transform={`rotate(-90 ${cx} ${cy})`}
                    style={{transition:"stroke-dasharray 0.12s linear",filter:`drop-shadow(0 0 4px ${clr})`}}/>
                </svg>
              );
            })()}
            <div className="text-xs opacity-35 tracking-widest uppercase" style={{color:wc,position:"relative",zIndex:1}}>Streak</div>
            <div className="text-xl font-black tabular-nums"
              style={{
                color:streakDecaying&&hud.streak>0?"#ef4444":streakColor(),
                textShadow:hud.streak>=5?`0 0 14px ${streakDecaying?"#ef4444":streakColor()}`:"none",
                animation:streakDecaying&&hud.streak>0?"heartbeat 0.6s ease-in-out infinite":"none",
                position:"relative",zIndex:1,
              }}>
              {hud.streak}×{streakDecaying&&hud.streak>0?"⚠️":""}
            </div>
            {hud.streak>=5&&<div style={{fontSize:9,fontWeight:"black",color:streakColor(),opacity:0.8,letterSpacing:"0.04em",position:"relative",zIndex:1}}>
              ×{Math.min(10,1+Math.floor(hud.streak/5))} MULT
            </div>}
            {hud.nextPts!=null&&hud.streak>=5&&<div style={{fontSize:7,color:"#fbbf24",opacity:0.65,letterSpacing:"0.02em",position:"relative",zIndex:1}}>
              ~{hud.nextPts}pts
            </div>}
            {hud.accuracy!=null&&hud.streak===0&&<div style={{fontSize:8,color:hud.accuracy>=95?"#4ade80":hud.accuracy>=80?"#fbbf24":"#f87171",opacity:0.75,letterSpacing:"0.03em",position:"relative",zIndex:1}}>
              {hud.accuracy}% ACC
            </div>}
          </div>
          <button onTouchStart={e=>{e.stopPropagation();togglePause();}} onClick={e=>{e.stopPropagation();togglePause();}}
            className="flex items-center justify-center px-4"
            style={{color:wc,fontSize:18,background:"transparent",border:"none",WebkitTapHighlightColor:"transparent"}}>
            {paused?"▶":"⏸"}
          </button>
        </div>
        {/* Fragment progress chip — shown when boss fragments collected */}
        {(sv.bossFragments||0)>0&&(
          <div className="absolute z-20 pointer-events-none"
            style={{top:78,right:8}}>
            <div className="flex items-center gap-1 px-2 py-0.5 rounded-lg"
              style={{background:"#c084fc22",border:"1px solid #c084fc66",fontSize:9,fontWeight:"bold",color:"#c084fc"}}>
              💎 {sv.bossFragments}/10
              <div style={{width:28,height:4,background:"#ffffff15",borderRadius:2,overflow:"hidden",display:"inline-block",verticalAlign:"middle",marginLeft:2}}>
                <div style={{width:`${(sv.bossFragments/10)*100}%`,height:"100%",background:"#c084fc",boxShadow:"0 0 4px #c084fc"}}/>
              </div>
            </div>
          </div>
        )}
        {/* Mission mini-tracker — compact 3-dot indicator showing daily quest progress */}
        {!hud.boss&&!sv.missionCompleted&&(()=>{
          const missions=getDailyMissions();
          const prog=sv.missionProgress||{};
          const dots=missions.map(m=>({done:(prog[m.id]||0)>=m.goal,pct:Math.min(1,(prog[m.id]||0)/m.goal)}));
          const anyProgress=dots.some(d=>d.pct>0);
          if(!anyProgress)return null;
          return(
            <div className="absolute z-20 pointer-events-none flex items-center gap-1"
              style={{top:76,left:"50%",transform:"translateX(-50%)",background:"rgba(0,0,0,0.5)",
                borderRadius:12,padding:"3px 8px",border:"1px solid #ffffff10"}}>
              {dots.map((d,i)=>(
                <div key={i} style={{width:22,height:4,borderRadius:2,background:"#ffffff18",overflow:"hidden"}}>
                  <div style={{width:`${d.pct*100}%`,height:"100%",borderRadius:2,
                    background:d.done?"#34d399":"#fbbf24",boxShadow:d.done?"0 0 4px #34d399":"none",
                    transition:"width 0.3s ease"}}/>
                </div>
              ))}
              <span style={{fontSize:8,color:"#fbbf24",fontWeight:"bold"}}>{dots.filter(d=>d.done).length}/3</span>
            </div>
          );
        })()}
        {/* Boss HP Bar */}
        {hud.boss&&(
          <div className="absolute left-0 right-0 z-20 pointer-events-none flex flex-col items-center gap-0.5" style={{top:76}}>
            <div style={{fontSize:9,fontWeight:"black",letterSpacing:"0.08em",color:hud.boss.rage?"#ff0000":hud.boss.phase===3?"#ef4444":hud.boss.phase===2?"#fbbf24":"#ff6030",
              textTransform:"uppercase",textShadow:hud.boss.rage?"0 0 8px #ff0000":hud.boss.phase===2?"0 0 6px #fbbf24":"none",
              animation:hud.boss.rage?"heartbeat 0.5s ease-in-out infinite":"none"}}>
              {hud.boss.rage?"⚠️ RAGE":"💀 BOSS"} {BOSS_EMOJIS[(cfg?.world||1)-1]}
            </div>
            <div style={{width:160,height:8,background:"#ffffff15",borderRadius:4,overflow:"hidden",
              boxShadow:"0 0 8px rgba(0,0,0,0.5)"}}>
              <div style={{
                width:`${(hud.boss.hp/hud.boss.max)*100}%`,height:"100%",borderRadius:4,
                background:hud.boss.rage?"#ff0000":hud.boss.phase===3?"#ef4444":hud.boss.phase===2?"#fbbf24":"#4ade80",
                boxShadow:hud.boss.rage?"0 0 8px #ff0000":hud.boss.phase===2?"0 0 6px #fbbf24":"0 0 4px #4ade80",
                transition:"width 0.25s ease-out",
              }}/>
            </div>
            <div style={{fontSize:8,color:"#ffffff44",letterSpacing:"0.04em"}}>
              {Array.from({length:hud.boss.max},(_,i)=>(
                <span key={i} style={{marginRight:1,opacity:i<hud.boss.hp?1:0.15}}>♦</span>
              ))}
            </div>
          </div>
        )}
        {/* Boss Speech Bubble — shows during phase transitions */}
        {bossTaunt&&hud.boss&&(
          <div className="absolute left-0 right-0 flex justify-center z-30 pointer-events-none" style={{top:130}}>
            <div className="relative px-4 py-2 rounded-2xl max-w-xs text-center"
              style={{
                background:bossTaunt.phase===3?"#ff000022":bossTaunt.phase===2?"#fbbf2418":"#2a2a3a",
                border:`1px solid ${bossTaunt.phase===3?"#ff0000":bossTaunt.phase===2?"#fbbf24":"#666666"}88`,
                color:bossTaunt.phase===3?"#ff4444":bossTaunt.phase===2?"#fbbf24":"#aaaaaa",
                fontSize:13,fontWeight:"bold",
                animation:"speechBubble 0.3s cubic-bezier(0.34,1.5,0.64,1) both",
                boxShadow:`0 0 20px ${bossTaunt.phase===3?"#ff000066":bossTaunt.phase===2?"#fbbf2444":"#44444444"}`,
              }}>
              {bossTaunt.phase==="dying"?"💀":bossTaunt.phase===3?"😡":"😤"} "{bossTaunt.text}"
              {/* Speech bubble tail */}
              <div style={{position:"absolute",top:-7,left:"50%",transform:"translateX(-50%)",
                width:0,height:0,
                borderLeft:"8px solid transparent",borderRight:"8px solid transparent",
                borderBottom:`8px solid ${bossTaunt.phase===3?"#ff000044":bossTaunt.phase===2?"#fbbf2430":"#44444444"}`}}/>
            </div>
          </div>
        )}
        {/* Modifier goal hint */}
        {hud.modGoal&&(
          <div className="absolute left-0 right-0 flex justify-center z-20" style={{top:hud.boss?120:76}}>
            <div className="px-3 py-1 rounded-xl text-xs font-bold" style={{background:"#fbbf2420",border:"1px solid #fbbf2444",color:"#fbbf24"}}>
              ⚡ {hud.modGoal}
            </div>
          </div>
        )}
        {/* Stacked multiplier banner — show when DOUBLE + MULTIPLIER both active */}
        {(()=>{
          const hasDouble=activePwrDisp.some(p=>p.type==="DOUBLE"&&p.endsAt>Date.now());
          const hasMultiplier=activePwrDisp.some(p=>p.type==="MULTIPLIER"&&p.endsAt>Date.now());
          const hasFever=hud.fever;
          const totalMult=(hasDouble?2:1)*(hasMultiplier?3:1)*(hasFever?2:1);
          if(totalMult<=1)return null;
          return(
            <div className="absolute z-30 pointer-events-none" style={{top:72,left:"50%",transform:"translateX(-50%)",
              background:"linear-gradient(90deg,#f43f5e,#f97316)",borderRadius:20,padding:"2px 10px",
              fontSize:11,fontWeight:"black",color:"#fff",letterSpacing:"0.08em",
              boxShadow:"0 0 16px #f43f5e66",animation:"heartbeat 0.8s ease-in-out infinite",whiteSpace:"nowrap"}}>
              ×{totalMult} SUPER MULT!
            </div>
          );
        })()}
        {/* Power-ups */}
        {/* 9D — Power-up queue visual with time bars */}
        {activePwrDisp.length>0&&(
          <div className="absolute left-0 right-0 flex justify-center gap-1.5 z-20" style={{top:hud.boss?148:hud.modGoal?108:76}}>
            {activePwrDisp.map(p=>{
              const secsLeft=Math.max(0,Math.ceil((p.endsAt-Date.now())/1000));
              const totalSecs=p.type==="SHIELD"?25:p.type==="SLOW"?8:p.type==="DOUBLE"?10:p.type==="FREEZE"?4:p.type==="MULTIPLIER"?8:p.type==="COMBO_FREEZE"?10:p.type==="TIME_WARP"?6:12;
              const pct=Math.min(100,Math.round(secsLeft/totalSecs*100));
              const isExpiring=secsLeft<=2&&secsLeft>0;
              const colors={SHIELD:"#fbbf24",SLOW:"#60a5fa",DOUBLE:"#f97316",FREEZE:"#06b6d4",LIFE:"#4ade80",LUCKY:"#ffd700",MIRROR:"#c084fc",MULTIPLIER:"#f43f5e",COMBO_FREEZE:"#67e8f9",GRAVITY:"#a78bfa",TIME_WARP:"#818cf8",SCORE_BOOST:"#f43f5e",LIFE_SURGE:"#4ade80",MAGNET_FIELD:"#ec4899",OVERCLOCK:"#fbbf24",SHIELD_WALL:"#a78bfa",PHANTOM_TOUCH:"#818cf8",COIN_RAIN:"#ffd700",BERSERKER:"#ef4444",GHOST_MODE:"#818cf8",SCORE_SHIELD:"#a3e635",DOUBLE_SPAWN:"#f0abfc",STAR_RAIN:"#ffd700",TIME_STOP:"#67e8f9",MEGA_TAP:"#fb923c",RAINBOW_SURGE:"#f472b6",TWIN_STRIKE:"#f472b6",GOLDEN_TOUCH:"#ffd700",LUCK_SURGE:"#4ade80",FREEZE_ZONE:"#67e8f9",SCORE_X2:"#ffd700",MAGNET_PULL:"#ec4899"};
              const icons={SHIELD:"🛡",SLOW:"🐢",DOUBLE:"×2",FREEZE:"❄️",LIFE:"❤️",LUCKY:"⭐",MIRROR:"🪞",MULTIPLIER:"×3",COMBO_FREEZE:"🧊",GRAVITY:"🌐",TIME_WARP:"⏱️",SCORE_BOOST:"×5",LIFE_SURGE:"💚",MAGNET_FIELD:"🧲",OVERCLOCK:"⚡",SHIELD_WALL:"🏰",PHANTOM_TOUCH:"👻",COIN_RAIN:"🪙",BERSERKER:"⚔️",GHOST_MODE:"👻",SCORE_SHIELD:"🔰",DOUBLE_SPAWN:"×2+",STAR_RAIN:"⭐",TIME_STOP:"⏸️",MEGA_TAP:"💥",RAINBOW_SURGE:"🌈",TWIN_STRIKE:"👥",GOLDEN_TOUCH:"✨",LUCK_SURGE:"🍀",FREEZE_ZONE:"❄️+",SCORE_X2:"×2!",MAGNET_PULL:"🧲+",COMBO_SHIELD:"🔒",PERFECT_AIM:"🎯",CHAIN_BURST:"🔥",MAGNET_BURST:"🧲💥",SCORE_PULSE:"💫",LIFE_SHIELD:"🛡️+",MULTIPLIER_SURGE:"⚡×",BOUNCE_BACK:"🔄",FEVER_EXTEND:"🌡️",COIN_RUSH:"🪙+",GRAVITY_WELL:"🌀",CLONE_ARMY:"👥+",RUSH_MODE:"⚡!",INVINCIBLE:"💫",HEALER:"❤️+",SCORE_GUARD:"🔰+",SWIPE_BOOST:"💨",LUCKY_COIN:"🍀",ECHO_SHOT:"📡",BURST_SHIELD:"💣🛡",MAGNET_ORBIT:"🌀🧲",SCORE_ECHO:"📢",PHANTOM_BURST:"👻💥",STREAK_STORM:"⛈️",TIME_BUBBLE:"⏱️💫",CRIT_BOOST:"💥×3",GOLD_RUSH:"🥇",MIRROR_MAZE:"🪞🌀",BLAST_WAVE:"💥🌊",TEMPO_SYNC:"🎵⚡",LIFE_LEECH:"❤️🩸",SCORE_RAIN:"🌧️⭐",CHAIN_MAGNET:"🧲⛓️",SCORE_SURGE:"💹",ORBIT_SHIELD:"🛸🛡",ECHO_WAVE:"〰️💥",COMBO_BURST:"💥⛓️",PHASE_LOCK:"🔒⏸",SCORE_LEECH:"🔋💰",SPLIT_SHOT:"🎯🎯",FEVER_STORM:"🌪️🌡️",RARITY_BOOST:"✨⬆️",TWIN_FEVER:"🌡️×2",REFLECT_WAVE:"💥🔄",NOVA_BOOST:"💥🌟",STREAK_LOCK:"🔐⛓️",MAGNET_STORM:"🌪️🧲",SCORE_FREEZE:"❄️🔒",CHAIN_FEVER:"⛓️🌡️",LIFE_BOMB:"💣❤️",TURBO_TAP:"⚡⚡⚡",COIN_MAGNET:"🧲🪙",BLAST_RADIUS:"💥🌐",SCORE_ACCELERATOR:"📈⚡",LIFE_CHAIN:"❤️⛓️",SCORE_MIRROR:"🪞💯",FEVER_LOCK:"🔒🌡️",STREAK_SURGE:"🔥⬆️",COMBO_ECHO:"🔊⛓️",GOLD_SHIELD:"🛡️🥇",PHANTOM_LIFE:"👻❤️",SCORE_DOUBLER:"×2🎯",MAGNET_BLITZ:"🧲⚡",CRIT_STORM:"💥🌩️",BONUS_WAVE:"🌊🎁",TAP_FURY:"🔥👊",SCORE_SPIKE:"📊⚡",LIFE_BURST:"❤️💥",PHASE_BURST:"💠⚡",VORTEX_TAP:"🌀👊",CHAIN_LOCK:"🔐⛓️",NOVA_STORM:"🌩️💥",ECHO_CHAIN:"🔊⛓️",GRAVITY_BURST:"🌀💥",SCORE_FLOOD:"🌊💰",LIFE_LEECH_PLUS:"❤️🩸+",COMBO_SURGE:"🔥⛓️",STAR_FORGE:"⭐🔥",PULSE_SHIELD:"💠🛡️",SCORE_AMPLIFY:"📈🔊",RICOCHET_BLAST:"🎯⛓️",FEVER_FOUNTAIN:"🌡️⛲",DOUBLE_FEVER:"🌡️×2",BOMB_CONVERT:"💣💡",SCORE_MAGNET:"🧲💰",LIFE_OVERFLOW:"❤️⬆️",STREAK_HEAL:"❤️🔥",TEMPO_BURST:"🎵💥",CRIT_SURGE:"💥🔥",LIFE_PULSE:"💓⚡",CHAIN_WAVE:"🌊⛓️",SCORE_OVERDRIVE:"📈🔥",PHANTOM_STRIKE:"👻⚡",COIN_SURGE:"🪙🚀",FREEZE_SURGE:"❄️⚡",LUCKY_STRIKE:"🍀⚡",ECHO_BURST:"🔊💥",SHIELD_SURGE:"🛡️🚀",SCORE_SNAP:"📸💰",LIFE_GUARD:"❤️🛡️",BURST_FEVER:"🌡️💥",SCORE_TRAIL:"✨📈",CHAIN_SNAP:"🔗📸",LIFE_ECHO:"❤️🔊",SCORE_VAULT:"🏦💰",STREAK_ECHO:"🔥🔊",NOVA_PULSE:"💥🫀",COIN_ECHO:"🪙🔊",TIME_SHIELD:"⏱️🛡️",SCORE_BLITZ:"⚡💯",PHANTOM_RUSH:"👻⚡",COIN_STORM:"🪙🌪️",STREAK_BLAZE:"🔥🌪️",LIFE_BLOOM:"❤️🌸",FOCUS_BLAST:"🎯💥",COIN_CHAIN:"🪙⛓️",SHIELD_BURST:"🛡️💥",SCORE_ECHO2:"📊🔁",COMBO_SURGE2:"🔥⛓️",LIFE_SPIRAL:"❤️🌀",TEMPO_ECHO:"🎵🔊",COIN_BLOOM:"🪙🌸",STREAK_NOVA:"⛓️💥",LIFE_STORM:"❤️⛈️",GOLD_FRENZY:"🥇🔥",PHANTOM_COIN:"👻🪙",SCORE_FRENZY:"📊🔥",LIFE_BEACON:"❤️🔦",BLAZE_TRAIL:"🔥✨",COIN_PULSE:"🪙💓",FEVER_BLOOM:"🌡️🌸",COIN_CASCADE:"🪙💦",SCORE_OMEGA:"Ω💥",ETERNAL_LIFE:"♾️❤️",ARC_SURGE:"⚡🌊",RELIC_PULSE:"🏺💓",VENOM_SURGE:"☠️⚡",LUNAR_BLOOM:"🌙🌸",STAR_CHAIN:"⭐⛓️",VOID_BLOOM:"🌑🌸",PULSE_WAVE:"〰️💥",COIN_VORTEX:"🪙🌀",GRAVITY_BURST2:"🌀💥",SCORE_AVALANCHE:"🏔️💰",INFERNO_CHAIN:"🔥⛓️",SPECTRAL_COIN:"👻💰",MAGMA_WAVE:"🌋〰️",COSMIC_ECHO:"🌌🔊",FROST_LANCE:"❄️🗡️",LIFE_MATRIX:"❤️🔲",THUNDER_BURST:"⚡💥",SHIMMER_COIN:"✨🪙",SCORE_QUAKE:"🌍💥",ECHO_LIFE:"❤️〰️",NOVA_CASCADE:"💥🌊",TIME_WARP2:"⏱️×2",SOLAR_PULSE:"☀️💓",VOID_CHAIN:"🌑⛓️",EMBER_WAVE:"🔥〰️",CRYSTAL_NET:"💎🕸️",PRISM_ECHO:"🌈🔊",VOLT_SURGE:"⚡🔥",SHADOW_BLOOM:"🌑🌸",NEXUS_WAVE:"🌐〰️",QUANTUM_ECHO:"🔮🔊",FROST_CROWN:"❄️👑"};
              const c=colors[p.type]||"#60a5fa";
              return(
                <div key={p.type} className="flex flex-col items-center gap-0.5"
                  style={{background:"#0a0a1acc",borderRadius:8,padding:"3px 6px",border:`1px solid ${c}${isExpiring?"cc":"44"}`,minWidth:36,
                    animation:isExpiring?"heartbeat 0.4s ease-in-out infinite":"none",
                    boxShadow:isExpiring?`0 0 8px ${c}88`:"none"}}>
                  <div className="text-xs font-black" style={{color:c,fontSize:11}}>{icons[p.type]||"⚡"}</div>
                  <div className="text-xs font-bold tabular-nums" style={{color:isExpiring?"#ef4444":c,fontSize:9,lineHeight:1,fontWeight:isExpiring?"black":"bold"}}>{secsLeft}s</div>
                  <div style={{width:28,height:3,background:"#ffffff15",borderRadius:2,overflow:"hidden"}}>
                    <div style={{width:`${pct}%`,height:"100%",background:isExpiring?"#ef4444":c,borderRadius:2,
                      boxShadow:`0 0 4px ${isExpiring?"#ef4444":c}`,transition:"width 0.5s linear"}}/>
                  </div>
                </div>
              );
            })}
          </div>
        )}
        {/* Streak Shield indicator */}
        {streakShieldActive&&(
          <div className="absolute right-3 z-25 flex items-center gap-1 px-2 py-1 rounded-xl"
            style={{top:82,background:"#ffd70022",border:"1px solid #ffd70066",animation:"floatGlow 1s ease-in-out infinite"}}>
            <span style={{fontSize:14}}>🛡️</span>
            <span className="text-xs font-black" style={{color:"#ffd700"}}>STREAK SAFE</span>
          </div>
        )}
        {/* Lucky Mode banner */}
        {luckyMode&&(
          <div className="absolute left-0 right-0 flex justify-center pointer-events-none z-30" style={{top:140}}>
            <div className="font-black text-lg px-5 py-2 rounded-2xl"
              style={{color:"#ffd700",textShadow:"0 0 24px #ffd700",background:"#ffd70020",border:"2px solid #ffd700aa",
                animation:"legendaryRainbow 1.5s linear infinite"}}>
              ⭐ LUCKY STARS! TAP EVERYTHING! ⭐
            </div>
          </div>
        )}
        {/* New Record banner */}
        {newRecord&&(
          <div className="absolute left-0 right-0 flex justify-center pointer-events-none z-30" style={{top:luckyMode?180:140}}>
            <div className="font-black text-sm px-4 py-1.5 rounded-xl"
              style={{color:"#34d399",textShadow:"0 0 16px #34d399",background:"#34d39920",border:"1px solid #34d39966",
                animation:"waveIn 0.4s ease-out"}}>
              🏆 NEW PERSONAL BEST!
            </div>
          </div>
        )}
        {/* Fever */}
        {rushMode&&!feverBorder&&<div className="absolute inset-0 pointer-events-none z-10" style={{border:"3px solid #f97316",boxShadow:"inset 0 0 40px #f9731633,0 0 40px #f9731633",animation:"feverPulse 0.45s ease-in-out infinite alternate"}}/>}
        {rushMode&&<div className="absolute left-0 right-0 flex justify-center pointer-events-none z-30" style={{top:luckyMode||newRecord?184:feverBorder?166:140}}>
          <span className="font-black text-sm px-3 py-1 rounded-full" style={{color:"#f97316",textShadow:"0 0 16px #f97316",background:"#f9731620",border:"1px solid #f9731666",animation:"heartbeat 0.55s ease-in-out infinite"}}>⚡ RUSH MODE!</span>
        </div>}
        {goldRushMode&&<div className="absolute inset-0 pointer-events-none z-10" style={{border:"3px solid #ffd700",boxShadow:"inset 0 0 50px #ffd70028,0 0 50px #ffd70028",animation:"feverPulse 0.5s ease-in-out infinite alternate"}}/>}
        {goldRushMode&&<div className="absolute left-0 right-0 flex justify-center pointer-events-none z-30" style={{top:feverBorder?178:rushMode?158:140}}>
          <span className="font-black text-sm px-3 py-1 rounded-full" style={{color:"#ffd700",textShadow:"0 0 18px #ffd700",background:"#ffd70018",border:"1px solid #ffd70066",animation:"heartbeat 0.6s ease-in-out infinite"}}>🥇 GOLD RUSH! 3× COINS!</span>
        </div>}
        {/* Score Boost — rose border + tap counter */}
        {(gsRef.current?._scoreBoostTaps||0)>0&&(
          <div className="absolute left-0 right-0 flex justify-center pointer-events-none z-29" style={{top:feverBorder?200:goldRushMode?178:158}}>
            <span className="font-black text-xs px-3 py-1 rounded-full" style={{color:"#f43f5e",textShadow:"0 0 12px #f43f5e",background:"#f43f5e18",border:"1px solid #f43f5e66",letterSpacing:"0.06em",animation:"heartbeat 0.5s ease-in-out infinite"}}>
              ×5 SCORE BOOST — {gsRef.current._scoreBoostTaps} tap{gsRef.current._scoreBoostTaps!==1?"s":""} left!
            </span>
          </div>
        )}
        {/* Lightning Storm visual — yellow border flash */}
        {gsRef.current?._stormEndsAt&&Date.now()<gsRef.current._stormEndsAt&&(
          <div className="absolute inset-0 pointer-events-none z-10" style={{
            border:"3px solid #fbbf24",
            boxShadow:"inset 0 0 60px #fbbf2428,0 0 60px #fbbf2428",
            animation:"feverPulse 0.25s ease-in-out infinite alternate"}}/>
        )}
        {/* Time Warp — deep purple slowdown border + banner */}
        {activePwrDisp.some(p=>p.type==="TIME_WARP"&&p.endsAt>Date.now())&&(
          <>
            <div className="absolute inset-0 pointer-events-none z-10" style={{
              border:"3px solid #818cf8",
              boxShadow:"inset 0 0 60px #818cf822,0 0 60px #818cf822",
              animation:"feverPulse 1.2s ease-in-out infinite alternate"}}/>
            <div className="absolute left-0 right-0 flex justify-center pointer-events-none z-29" style={{top:feverBorder?200:goldRushMode?178:158}}>
              <span className="font-black text-xs px-3 py-1 rounded-full" style={{color:"#818cf8",textShadow:"0 0 12px #818cf8",background:"#818cf818",border:"1px solid #818cf866",letterSpacing:"0.06em"}}>⏱️ TIME WARP ACTIVE</span>
            </div>
          </>
        )}
        {feverBorder&&<div className="absolute inset-0 pointer-events-none z-10" style={{border:"4px solid #fbbf24",boxShadow:"inset 0 0 60px #fbbf2445,0 0 60px #fbbf2445",animation:"feverPulse 0.6s ease-in-out infinite alternate"}}/>}
        {feverBorder&&<div className="absolute left-0 right-0 flex justify-center pointer-events-none z-30" style={{top:luckyMode||newRecord?184:140}}>
          <span className="font-black text-base px-4 py-1 rounded-full" style={{color:"#fbbf24",textShadow:"0 0 20px #fbbf24",background:"#fbbf2420",animation:"feverPulse 0.5s infinite alternate"}}>✨ MAGIC MODE!</span>
        </div>}
        {damageFlash&&<div className="absolute inset-0 pointer-events-none z-10" style={{background:"#ef444428",boxShadow:"inset 0 0 80px #ef444466",animation:"epicFlash 0.5s ease-out forwards"}}/>}
        {epicFlash&&<div className="absolute inset-0 pointer-events-none z-10" style={{background:"#f472b633",animation:"epicFlash 0.5s ease-out forwards"}}/>}
        {perfectFlash&&<div className="absolute inset-0 pointer-events-none z-10" style={{background:"#fbbf2422",animation:"epicFlash 0.35s ease-out forwards"}}/>}
        {comboLabel&&(
          <div className="absolute left-0 right-0 flex justify-center pointer-events-none z-30" style={{top:"40%",transform:"translateY(-50%)"}}>
            <div className="font-black px-6 py-2 rounded-2xl"
              style={{fontSize:"clamp(1.4rem,6vw,2.2rem)",color:"#fff",
                textShadow:`0 0 30px ${theme.accent},0 0 60px ${theme.accent}55`,
                background:theme.accent+"22",border:`2px solid ${theme.accent}`,
                animation:"comboAnnounce 0.35s cubic-bezier(0.34,1.5,0.64,1)"}}>
              {comboLabel}
            </div>
          </div>
        )}
        {/* Bounty Board — wanted rarity chip */}
        {bountyData&&!hud.fever&&(()=>{
          const secsLeft=Math.max(0,Math.round((bountyData.expiresAt-Date.now())/1000));
          const urgent=secsLeft<=8;
          return(
            <div className="absolute pointer-events-none z-20" style={{top:88,left:"50%",transform:"translateX(-50%)"}}>
              <div style={{
                display:"flex",alignItems:"center",gap:5,
                background:"#0a0a1aee",border:`1.5px solid ${bountyData.color}`,
                borderRadius:16,padding:"3px 10px",
                boxShadow:`0 0 10px ${bountyData.color}44`,
                animation:urgent?"heartbeat 0.5s ease-in-out infinite":"none"
              }}>
                <span style={{fontSize:11}}>🎯</span>
                <span style={{fontSize:9,color:bountyData.color,fontWeight:"bold",letterSpacing:"0.06em"}}>WANTED: {bountyData.icon} {bountyData.rarity}</span>
                <span style={{fontSize:9,color:urgent?"#ef4444":"#6b7280",fontWeight:"bold"}}>×{bountyData.mult} {secsLeft}s</span>
              </div>
            </div>
          );
        })()}
        {/* Combo milestone progress bar — shows distance to next milestone */}
        {hud.streak>0&&(()=>{
          const COMBO_MILESTONES=[5,10,20,25,30,35,50];
          const streak=hud.streak;
          const nextMs=COMBO_MILESTONES.find(m=>m>streak)||55;
          const prevMs=COMBO_MILESTONES.filter(m=>m<=streak).pop()||0;
          const pct=Math.min(100,((streak-prevMs)/(nextMs-prevMs))*100);
          const comboColor=streak>=35?"#a855f7":streak>=20?"#ef4444":streak>=10?"#f97316":streak>=5?"#fbbf24":"#34d399";
          return(
            <div className="absolute left-4 right-4 pointer-events-none z-20" style={{bottom:6}}>
              <div style={{display:"flex",alignItems:"center",gap:4,marginBottom:1}}>
                <span style={{fontSize:7,color:comboColor,opacity:0.7,letterSpacing:"0.05em"}}>{streak}×</span>
                <div style={{flex:1,height:2.5,background:"#ffffff10",borderRadius:2,overflow:"hidden"}}>
                  <div style={{width:`${pct}%`,height:"100%",background:comboColor,borderRadius:2,
                    boxShadow:`0 0 5px ${comboColor}`,transition:"width 0.15s ease-out"}}/>
                </div>
                <span style={{fontSize:7,color:comboColor,opacity:0.5}}>→{nextMs}×</span>
              </div>
            </div>
          );
        })()}
        {/* BIG MASCOT COMPANION — right side, cheering you on */}
        <div className="absolute pointer-events-none z-15" style={{bottom:18,right:10,display:"flex",flexDirection:"column",alignItems:"center",gap:4}}>
          {/* Speech bubble */}
          {mascotSpeech&&(
            <div style={{
              background:"rgba(0,0,0,0.88)",border:`2px solid ${currentMascot.color}`,
              borderRadius:14,padding:"6px 10px",maxWidth:130,textAlign:"center",
              fontSize:11,fontWeight:"bold",color:currentMascot.color,lineHeight:1.3,
              boxShadow:`0 0 14px ${currentMascot.color}44`,
              animation:"speechBubble 0.3s cubic-bezier(0.34,1.5,0.64,1)",
              position:"relative"}}>
              {mascotSpeech}
              {/* Triangle pointer */}
              <div style={{
                position:"absolute",bottom:-9,left:"50%",transform:"translateX(-50%)",
                width:0,height:0,borderLeft:"7px solid transparent",
                borderRight:"7px solid transparent",
                borderTop:`9px solid ${currentMascot.color}`}}/>
            </div>
          )}
          <MascotEmoji mood={mascotMood} size={86}/>
          <div style={{fontSize:9,color:currentMascot.color,fontWeight:"bold",opacity:0.75,letterSpacing:"0.05em"}}>
            {currentMascot.name.toUpperCase()}
          </div>
          {/* 9B — Happiness bar */}
          {(()=>{
            const happy=mascotHappyRef.current||0;
            const maxHappy=75;
            const pct=Math.min(100,Math.round((happy%75)/75*100));
            const milestone=happy<25?"😐":happy<50?"😊":happy<75?"😄":"🌟";
            return(
              <div style={{width:50}}>
                <div style={{width:"100%",height:3,background:"#ffffff15",borderRadius:2,overflow:"hidden"}}>
                  <div style={{width:`${pct}%`,height:"100%",background:currentMascot.color,borderRadius:2,
                    boxShadow:`0 0 3px ${currentMascot.color}`,transition:"width 0.3s"}}/>
                </div>
                <div style={{textAlign:"center",fontSize:7,color:currentMascot.color,opacity:0.6,marginTop:1}}>{milestone} {happy}</div>
              </div>
            );
          })()}
        </div>
        {/* Chain flash popup */}
        {chainFlash&&(Date.now()-chainFlash.at<900)&&(
          <div key={chainFlash.at} className="absolute left-1/2 pointer-events-none z-40"
            style={{top:"45%",transform:"translateX(-50%)",animation:"streakBurstAnim 0.85s cubic-bezier(0.34,1.4,0.64,1) forwards",textAlign:"center",whiteSpace:"nowrap"}}>
            <div className="font-black" style={{fontSize:"clamp(1.2rem,6vw,2rem)",color:"#60a5fa",
              textShadow:"0 0 30px #60a5fa,0 0 60px #60a5fa55",letterSpacing:"0.04em"}}>
              {chainFlash.label}
            </div>
          </div>
        )}
        {/* Streak milestone burst + 50× combo finisher (10C) */}
        {streakBurst&&(
          <div className="absolute left-1/2 pointer-events-none z-40"
            style={{top:"30%",transform:"translateX(-50%)",
              animation:streakBurst.n>=50?"comboFinisher 1.4s cubic-bezier(0.34,1.4,0.64,1) forwards":"streakBurstAnim 1.1s cubic-bezier(0.34,1.4,0.64,1) forwards",
              textAlign:"center",whiteSpace:"nowrap"}}>
            {streakBurst.n>=50&&(
              <div style={{position:"absolute",inset:"-30px",background:"radial-gradient(ellipse at center,#ffd70033 0%,transparent 70%)",animation:"legendaryRainbow 0.8s linear infinite",borderRadius:"50%"}}/>
            )}
            <div className="font-black" style={{fontSize:streakBurst.n>=50?"clamp(2rem,10vw,3.6rem)":"clamp(1.6rem,8vw,2.8rem)",
              color:streakBurst.color,
              textShadow:streakBurst.n>=50?`0 0 60px ${streakBurst.color},0 0 120px ${streakBurst.color}88,0 0 200px #ffd70044`:`0 0 40px ${streakBurst.color},0 0 80px ${streakBurst.color}55`,
              letterSpacing:"0.04em"}}>
              {streakBurst.label}
            </div>
            <div style={{fontSize:"clamp(0.7rem,3vw,1rem)",color:streakBurst.color,opacity:0.8}}>
              {streakBurst.n}× combo!{streakBurst.n>=50?" 🌈 LEGENDARY!":""}
            </div>
          </div>
        )}
        {/* Score Tension Ramp — border glow escalates toward goal */}
        {/* Last-life danger border — persistent red pulse when 1 life remaining */}
        {hud.lives===1&&!cfg?.isZen&&(
          <div className="absolute inset-0 pointer-events-none z-5" style={{
            border:"3px solid #ef4444",
            boxShadow:"inset 0 0 50px #ef444428,0 0 30px #ef444422",
            animation:"heartbeat 0.7s ease-in-out infinite"}}/>
        )}
        {hud.lives===1&&!cfg?.isZen&&(
          <div className="absolute left-0 right-0 top-16 flex justify-center pointer-events-none z-25">
            <span style={{fontSize:9,fontWeight:"black",color:"#ef4444",letterSpacing:"0.15em",
              textShadow:"0 0 8px #ef4444",animation:"heartbeat 0.7s ease-in-out infinite"}}>⚠️ LAST LIFE</span>
          </div>
        )}
        {tensionLevel>=1&&(
          <div className="absolute inset-0 pointer-events-none z-6" style={{
            border:`${tensionLevel+1}px solid`,
            borderColor:["","#fbbf2444","#f9780477","#ff980099","#ffd700cc"][tensionLevel],
            boxShadow:tensionLevel>=3?`inset 0 0 60px #ffd70018`:"none",
            animation:tensionLevel>=3?"feverPulse 0.35s ease-in-out infinite alternate":"none"}}/>
        )}
        {tensionLevel>=3&&(
          <div className="absolute left-0 right-0 pointer-events-none z-27" style={{bottom:76,textAlign:"center"}}>
            <span style={{
              fontWeight:"black",
              color:tensionLevel===4?"#ffd700":"#ff9800",
              fontSize:tensionLevel===4?"clamp(1.2rem,6vw,1.6rem)":"clamp(0.9rem,4.5vw,1.15rem)",
              textShadow:`0 0 28px ${tensionLevel===4?"#ffd700":"#ff9800"}`,
              animation:"floatGlow 0.25s ease-in-out infinite",letterSpacing:"0.04em"
            }}>
              {tensionLevel===4?"⭐ ONE TAP AWAY! ⭐":"🔥 ALMOST THERE!"}
            </span>
          </div>
        )}
        {/* Bonus Round — golden glow + banner */}
        {bonusRound&&(
          <>
            <div className="absolute inset-0 pointer-events-none z-6" style={{
              border:"4px solid #ffd700",
              boxShadow:"inset 0 0 70px #ffd70025,0 0 70px #ffd70025",
              animation:"scorePulse 0.45s ease-in-out infinite alternate"}}/>
            {/* Falling coin rain during bonus round */}
            <div className="absolute inset-0 pointer-events-none z-7 overflow-hidden">
              {[...Array(14)].map((_,i)=>(
                <div key={i} style={{
                  position:"absolute",top:-20,
                  left:`${(i*7+3)%100}%`,
                  fontSize:12+Math.floor(i%3)*4,
                  animation:`coinFall ${1.8+i*0.22}s ${i*0.18}s linear infinite`,
                  opacity:0.75,pointerEvents:"none"}}>
                  🪙
                </div>
              ))}
            </div>
            <div className="absolute left-0 right-0 flex justify-center pointer-events-none z-30" style={{top:88}}>
              <div style={{
                display:"inline-block",padding:"6px 20px",borderRadius:12,
                background:"linear-gradient(135deg,#b8860b,#ffd700,#b8860b)",
                fontWeight:"black",fontSize:"clamp(0.9rem,4.5vw,1.1rem)",color:"#000",
                animation:"heartbeat 0.55s ease-in-out infinite",
                boxShadow:"0 0 30px #ffd70099",letterSpacing:"0.06em"}}>
                🌟 BONUS ROUND — ALL LEGENDARY! 🌟
              </div>
            </div>
          </>
        )}
        {/* Mystery Box slot-reveal overlay */}
        {mysteryReveal&&(
          <div className="absolute left-1/2 pointer-events-none z-45" style={{
            top:"25%",transform:"translateX(-50%)",
            background:"rgba(0,0,0,0.92)",border:"3px solid #ffd700",
            borderRadius:20,padding:"18px 28px",textAlign:"center",
            boxShadow:"0 0 50px #ffd70055",minWidth:200}}>
            <div style={{color:"#ffd700",fontWeight:"black",fontSize:"1rem",marginBottom:10,letterSpacing:"0.06em"}}>
              🎁 MYSTERY BOX!
            </div>
            <div style={{display:"flex",gap:14,justifyContent:"center",marginBottom:12}}>
              {[0,1,2].map(i=>(
                <div key={i} style={{
                  width:52,height:52,borderRadius:12,
                  border:`2px solid ${mysteryReveal.phase>i?"#ffd700":"#ffffff22"}`,
                  background:mysteryReveal.phase>i?"#ffd70022":"#ffffff08",
                  display:"flex",alignItems:"center",justifyContent:"center",
                  fontSize:28,
                  animation:mysteryReveal.phase<=i?"slotSpin 0.15s linear infinite":"none",
                  boxShadow:mysteryReveal.phase>i?`0 0 18px #ffd700`:"none",
                  transition:"all 0.15s"}}>
                  {mysteryReveal.phase>i
                    ?MYSTERY_PRIZES[mysteryReveal.reels[i]].emoji
                    :"❓"}
                </div>
              ))}
            </div>
            {mysteryReveal.phase===3&&(
              <div style={{color:"#ffd700",fontWeight:"black",fontSize:"1.1rem",
                animation:"victoryBurst 0.4s cubic-bezier(0.34,1.5,0.64,1)"}}>
                {mysteryReveal.prize.label}
              </div>
            )}
          </div>
        )}
        {/* Screen edge world-color glow (intensifies with fever/lucky) */}
        <div className="absolute inset-0 pointer-events-none z-5" style={{
          boxShadow:luckyMode
            ?`inset 0 0 80px #ffd70033,inset 0 0 160px #ffd70018`
            :`inset 0 0 80px ${wc}0a,inset 0 0 160px ${wc}05`,
          border:`1px solid ${luckyMode?"#ffd70033":wc+"0a"}`}}/>
        {/* HEAT METER — streak-powered bar above score bar */}
        {cfg&&(
          <div className="absolute bottom-0 left-0 right-0 z-20" style={{height:10,background:"#00000055"}}>
            {/* Score bar (bottom 5px) */}
            <div style={{position:"absolute",bottom:0,left:0,right:0,height:5,background:"#00000055"}}>
              {(()=>{const pct=Math.min(100,hud.score/cfg.scoreGoal*100);const isClose=pct>=82;return(
                <div className="h-full transition-all duration-300 relative"
                  style={{width:`${pct}%`,
                    background:isClose?`linear-gradient(90deg,${wc}aa,#ffd700)`:
                      `linear-gradient(90deg,${wc}aa,${wc})`,
                    boxShadow:isClose?`0 0 16px #ffd700,0 0 6px #ffd700`:`0 0 12px ${wc},0 0 4px ${wc}`,
                    animation:isClose?"scorePulse 0.4s ease-in-out infinite alternate":"none"}}>
                  <div style={{position:"absolute",right:0,top:-2,width:8,height:9,borderRadius:"50%",background:isClose?"#ffd700":wc,boxShadow:`0 0 10px ${isClose?"#ffd700":wc}`}}/>
                </div>
              );})()}
              {/* SO CLOSE text */}
              {cfg&&hud.score/cfg.scoreGoal>=0.82&&hud.score<cfg.scoreGoal&&(
                <div className="absolute right-2 pointer-events-none" style={{bottom:6}}>
                  <span className="text-xs font-black" style={{color:"#ffd700",textShadow:"0 0 8px #ffd700",
                    animation:"floatGlow 0.5s ease-in-out infinite"}}>SO CLOSE! 🔥</span>
                </div>
              )}
            </div>
            {/* Heat bar (top 5px) — fills with combo */}
            <div style={{position:"absolute",top:0,left:0,right:0,height:5,background:"#00000033"}}>
              {(()=>{const heat=Math.min(100,(hud.streak/FEVER_STREAK)*100);const isHot=heat>=60;return(
                <div style={{width:`${heat}%`,height:"100%",transition:"width 0.15s ease",
                  background:isHot?
                    `linear-gradient(90deg,#ff6030,#ff9800,#ffd700)`:
                    `linear-gradient(90deg,${wc}66,${wc}aa)`,
                  boxShadow:isHot?`0 0 8px #ff9800`:`0 0 4px ${wc}88`}}>
                </div>
              );})()}
              {hud.streak>=3&&<div className="absolute right-1 top-0 flex items-center pointer-events-none" style={{height:5}}>
                <span style={{fontSize:8,color:"#ff9800",fontWeight:"black",lineHeight:1}}>🔥</span>
              </div>}
            </div>
          </div>
        )}
        {countdownVal!==null&&(
          <div className="absolute inset-0 flex flex-col items-center justify-center z-50 pointer-events-none" style={{background:"rgba(0,0,0,0.55)"}}>
            <div className="font-black text-9xl" key={countdownVal}
              style={{color:countdownVal==="GO!"?"#34d399":wc,textShadow:`0 0 70px ${countdownVal==="GO!"?"#34d399":wc}`,animation:"countAnim 0.5s ease-out"}}>
              {countdownVal}
            </div>
            {countdownVal!=="GO!"&&(()=>{
              const tips=["Tap targets before they vanish! ⚡","🛡 Shielded targets need 2 taps!","Build streaks for fever mode! 🔥","🧲 Magnet targets pull others!","Rare targets = more points! ✨","Perfect timing = Perfect tap bonus! 🎯"];
              const tip=tips[Math.floor((Date.now()/1000)%tips.length)];
              return <div style={{fontSize:12,color:"#ffffff66",marginTop:16,textAlign:"center",maxWidth:220,padding:"0 20px"}}>{currentMascot.e.idle} {tip}</div>;
            })()}
          </div>
        )}
        {paused&&(
          <div className="absolute inset-0 flex flex-col items-center justify-center z-50" style={{background:"rgba(0,0,0,0.8)",backdropFilter:"blur(10px)"}}>
            <div className="text-4xl font-black mb-6" style={{color:wc}}>PAUSED</div>
            <NeonButton onClick={togglePause} className="w-48 py-4 text-lg mb-3"
              style={{background:`linear-gradient(135deg,${theme.secondary},${wc})`,boxShadow:`0 0 24px ${wc}55`}}>▶ RESUME</NeonButton>
            {/* Quick restart — 11A */}
            <NeonButton onClick={()=>{
              const lvId=levelCfgRef.current?.id;
              if(!lvId)return;
              if(rafRef.current){cancelAnimationFrame(rafRef.current);rafRef.current=null;}
              gsRef.current=null;targetsRef.current=[];particlesRef.current=[];activePwrRef.current=[];
              setActivePwrDisp([]);setFeverBorder(false);setPaused(false);pausedRef.current=false;
              setTimeout(()=>startGame(lvId,[]),50);
            }} className="w-48 py-3 mb-3" style={{background:`${wc}22`,border:`1px solid ${wc}66`,boxShadow:`0 0 12px ${wc}33`}}>
              🔄 RESTART
            </NeonButton>
            <NeonButton onClick={()=>{if(rafRef.current){cancelAnimationFrame(rafRef.current);rafRef.current=null;}gsRef.current=null;targetsRef.current=[];particlesRef.current=[];activePwrRef.current=[];setActivePwrDisp([]);setFeverBorder(false);setPaused(false);pausedRef.current=false;go("levelmap");}}
              className="w-48 py-3" style={{background:"#ffffff10",border:`1px solid ${wc}44`}}>✕ QUIT</NeonButton>
          </div>
        )}
      </div>
    );
  };

  // ── Level Complete ──
  const renderLevelComplete=()=>{
    if(!levelCompleteData)return null;
    const{score,stars,newStars,xpEarned,coinsEarned,levelId,isLast,sessionStats,isPerfectRun}=levelCompleteData;
    const cfg=getLevelConfig(levelId);
    const wld=WORLDS[cfg.world-1];
    const isBossLevel=cfg.isBoss;
    const totalAttempted=(sessionStats.tapsTotal||0)+(sessionStats.missedTargets||0);
    const accuracyPct=totalAttempted>0?Math.round((sessionStats.tapsTotal||0)/totalAttempted*100):100;
    const isSniper=accuracyPct>=95&&totalAttempted>=8;
    return(<>
      <div className="flex flex-col items-center h-full overflow-y-auto px-5 py-5 gap-3.5 relative z-10">
        {/* World-themed confetti explosion — 9E */}
        {(()=>{
          // Each world has themed confetti emojis
          const worldConfetti=[
            ["🌿","🍃","✨","💚","🌱"],["🔥","💥","⚡","🌋","🌟"],
            ["❄️","💙","🌊","💎","⛄"],["🌵","🏜️","💛","⭐","🪙"],
            ["🌸","🌺","💜","🌙","✨"],["⚙️","🔩","🤖","⚡","💎"],
            ["🌊","🐠","💙","🐚","✨"],["👻","💜","🕷️","🌟","💀"],
            ["🚀","⭐","🌌","💫","🪐"],["🌈","✨","💎","🌟","🎊"],
          ];
          const wEmoji=worldConfetti[(wld.id-1)%10];
          const count=isBossLevel?30:16;
          return(
            <div className="absolute inset-0 pointer-events-none overflow-hidden z-0">
              {Array.from({length:count},(_,i)=>(
                <div key={i} style={{
                  position:"absolute",
                  top:`${-5-Math.random()*10}%`,
                  left:`${(i*(100/count)+Math.random()*8)%100}%`,
                  fontSize:isBossLevel?`${14+Math.floor(Math.random()*10)}px`:`${12+Math.floor(Math.random()*8)}px`,
                  animation:`coinFall ${1.2+Math.random()*2.2}s ${i*0.09}s ease-in forwards`,
                  opacity:0.9,
                  transform:`rotate(${Math.floor(Math.random()*360)}deg)`,
                }}>
                  {wEmoji[i%wEmoji.length]}
                </div>
              ))}
            </div>
          );
        })()}
        {/* ── BIG DANCING MASCOT — victory celebration ── */}
        <div className="flex flex-col items-center gap-2" style={{animation:"victoryBurst 0.6s cubic-bezier(0.34,1.5,0.64,1)"}}>
          <MascotEmoji mood="victory" dancing={true} size={isBossLevel?118:96}/>
          <div className="font-black tracking-widest" style={{
            fontSize:"clamp(0.7rem,3.5vw,0.9rem)",
            color:currentMascot.color,
            textShadow:`0 0 18px ${currentMascot.color}`,
            animation:"floatGlow 1.2s ease-in-out infinite"}}>
            {isBossLevel?`🎊🎊 ${currentMascot.name.toUpperCase()} IS ABSOLUTELY HYPED! 🎊🎊`:`🎉 ${currentMascot.name.toUpperCase()} IS CHEERING FOR YOU! 🎉`}
          </div>
        </div>
        {/* Cinematic hero banner */}
        <div className="w-full rounded-3xl relative overflow-hidden" style={{
          background:`linear-gradient(160deg,${wld.color}22 0%,${wld.bg} 60%)`,
          border:`1px solid ${wld.color}55`,
          boxShadow:`0 0 40px ${wld.color}33,inset 0 0 30px ${wld.color}0a`,
          padding:"20px 20px 16px",animation:"victoryBurst 0.5s cubic-bezier(0.34,1.4,0.64,1)"}}>
          {/* World label */}
          <div className="flex items-center gap-2 mb-2">
            <span style={{fontSize:22}}>{wld.emoji}</span>
            <div>
              <div className="text-xs uppercase tracking-widest opacity-55 font-bold" style={{color:wld.color}}>{wld.name}</div>
              <div className="text-base font-black leading-tight" style={{color:wld.color}}>{cfg.name}</div>
            </div>
            <div className="ml-auto px-2 py-0.5 rounded-full text-xs font-black"
              style={{background:`${wld.color}22`,color:wld.color,border:`1px solid ${wld.color}44`,letterSpacing:"0.08em"}}>
              {isBossLevel?"👑 BOSS BEATEN!":"⭐ CLEARED!"}
            </div>
          </div>
          {/* Star row */}
          <div className="flex justify-center gap-3 my-2">
            {[1,2,3].map(s=>(
              <span key={s} style={{fontSize:s<=stars?46:30,opacity:s<=stars?1:0.12,
                filter:s<=stars?`drop-shadow(0 0 14px #fbbf24) drop-shadow(0 0 28px #fbbf2488)`:"none",
                animation:s<=stars?`starPop ${0.25+s*0.18}s cubic-bezier(0.34,1.5,0.64,1) both`:"none",
                animationDelay:`${(s-1)*0.12}s`}}>⭐</span>
            ))}
          </div>
          {newStars>stars&&<div className="text-center text-xs" style={{color:"#fbbf24",opacity:0.7}}>Personal best: {newStars}★</div>}
          {isPerfectRun&&<div className="text-center text-xs font-black mt-1" style={{color:"#4ade80",textShadow:"0 0 12px #4ade80",animation:"floatGlow 1s ease-in-out infinite",letterSpacing:"0.06em"}}>🎯 PERFECT RUN! 3× COIN BONUS!</div>}
          {/* Letter grade rank */}
          {(()=>{
            const ratio=score/(cfg.scoreGoal||1);
            const rank=ratio>=3.5?"S+":ratio>=2.8?"S":ratio>=2?"A+":ratio>=1.5?"A":ratio>=1.2?"B+":ratio>=1?"B":"C";
            const rColors={"S+":"#ffd700","S":"#ffd700","A+":"#f97316","A":"#f97316","B+":"#60a5fa","B":"#60a5fa","C":"#94a3b8"};
            const rColor=rColors[rank]||"#94a3b8";
            return(
              <div className="absolute top-3 right-3 flex flex-col items-center">
                <div style={{fontSize:28,fontWeight:"black",color:rColor,
                  textShadow:`0 0 16px ${rColor}`,lineHeight:1,
                  animation:rank.startsWith("S")?"legendaryRainbow 1.5s linear infinite":"none"}}>
                  {rank}
                </div>
                <div style={{fontSize:8,color:rColor,opacity:0.7,letterSpacing:"0.05em"}}>RANK</div>
              </div>
            );
          })()}
          {/* Score */}
          <div className="text-center mt-2">
            <div className="text-xs uppercase opacity-40 tracking-widest mb-0.5" style={{color:wld.color}}>Score</div>
            <div className="font-black tabular-nums" style={{fontSize:"clamp(2rem,10vw,3rem)",color:wld.color,
              textShadow:`0 0 30px ${wld.color}66`,lineHeight:1}}>{score.toLocaleString()}</div>
          </div>
        </div>

        {/* Rewards row */}
        <div className="flex gap-2.5 w-full">
          <div className="flex-1 rounded-2xl py-3 flex flex-col items-center gap-0.5"
            style={{background:`${wld.color}12`,border:`1px solid ${wld.color}33`,backdropFilter:"blur(8px)"}}>
            <div className="text-lg">✨</div>
            <div className="font-black text-sm" style={{
              color:wld.color,
              animation:payoutDone?"none":"heartbeat 0.35s ease-in-out infinite",
              textShadow:payoutDone?"none":`0 0 12px ${wld.color}`}}>+{displayedXP} XP</div>
          </div>
          {/* 11C — Coins earned with animated pop overlay */}
          <div className="flex-1 rounded-2xl py-3 flex flex-col items-center gap-0.5 relative overflow-visible"
            style={{background:"#fbbf2412",border:"1px solid #fbbf2433",backdropFilter:"blur(8px)"}}>
            <div className="text-lg">🪙</div>
            <div className="font-black text-sm" style={{
              color:"#fbbf24",
              animation:payoutDone?"none":"heartbeat 0.35s ease-in-out infinite",
              textShadow:payoutDone?"none":"0 0 14px #fbbf24"}}>+{displayedCoins}</div>
            {payoutDone&&coinsEarned>0&&(
              <div style={{position:"absolute",top:"-22px",left:"50%",transform:"translateX(-50%)",
                fontSize:11,fontWeight:"black",color:"#ffd700",whiteSpace:"nowrap",
                animation:"coinPop 1.2s ease-out forwards",pointerEvents:"none",
                textShadow:"0 0 8px #ffd700"}}>+🪙{coinsEarned}!</div>
            )}
          </div>
          <div className="flex-1 rounded-2xl py-3 flex flex-col items-center gap-0.5"
            style={{background:"#ffffff08",border:"1px solid #ffffff15",backdropFilter:"blur(8px)"}}>
            <div className="text-lg">⚡</div>
            <div className="font-black text-sm" style={{color:"#fff"}}>×{sv.bestStreak} best</div>
          </div>
        </div>

        {/* Stats grid */}
        <div className="w-full rounded-2xl py-3 px-4 grid grid-cols-3 gap-2"
          style={{background:"rgba(0,0,0,0.35)",border:`1px solid ${wld.color}1a`,backdropFilter:"blur(10px)"}}>
          {[["👆 Taps",sessionStats.tapsTotal],["✨ Magic+",sessionStats.rareHits],["🐉 Bosses",sessionStats.bossKills||0],
            ["🎯 Perfect",sessionStats.perfectTaps||0],["⚡ Magic Mode",sessionStats.feverCount||0],["⏱ Time",sessionStats.timeSurvived+"s"]
          ].map(([l,v])=>(
            <div key={l} className="text-center py-1">
              <div className="text-xs opacity-40 mb-0.5" style={{color:wld.color,letterSpacing:"0.02em"}}>{l}</div>
              <div className="font-black text-sm" style={{color:wld.color}}>{v}</div>
            </div>
          ))}
          {/* Accuracy rating */}
          <div className="col-span-3 flex items-center justify-center gap-2 py-2 rounded-xl mt-1"
            style={{background:isSniper?"#ffd70015":"#ffffff06",border:`1px solid ${isSniper?"#ffd70066":"#ffffff10"}`}}>
            <span style={{fontSize:16}}>{isSniper?"🎯":"📊"}</span>
            <div>
              <div style={{fontSize:10,color:isSniper?"#ffd700":"#ffffff55",fontWeight:"bold",letterSpacing:"0.06em"}}>
                {isSniper?"⭐ SNIPER ACCURACY!":"Accuracy"}
              </div>
              <div style={{fontSize:13,fontWeight:"black",color:isSniper?"#ffd700":wld.color}}>{accuracyPct}%</div>
            </div>
            <div style={{flex:1,height:4,background:"#ffffff10",borderRadius:2,overflow:"hidden",maxWidth:60}}>
              <div style={{width:`${accuracyPct}%`,height:"100%",background:isSniper?"#ffd700":wld.color,boxShadow:`0 0 4px ${wld.color}`}}/>
            </div>
          </div>
        </div>

        {/* 11C — Animated coins earned overlay */}
        {coinsEarned>0&&(
          <div className="w-full rounded-2xl px-4 py-3 flex items-center justify-between gap-3"
            style={{background:"#ffd70014",border:"1px solid #ffd70044",backdropFilter:"blur(8px)"}}>
            <div className="flex items-center gap-2">
              <span style={{fontSize:22,animation:"coinFall 0.6s ease-out both,floatGlow 1.2s ease-in-out 0.6s infinite"}}>🪙</span>
              <div>
                <div className="text-xs opacity-55 font-bold tracking-widest uppercase" style={{color:"#ffd700"}}>Coins Earned</div>
                <div className="text-2xl font-black tabular-nums" style={{color:"#ffd700",
                  textShadow:"0 0 16px #ffd700",animation:"scorePulse 0.5s cubic-bezier(0.34,1.5,0.64,1) both"}}>
                  +{coinsEarned}
                </div>
              </div>
            </div>
            <div className="flex flex-col items-end gap-1">
              <div className="text-xs opacity-40" style={{color:"#ffd700"}}>Balance</div>
              <div className="text-sm font-black" style={{color:"#ffd700"}}>🪙 {(sv.coins||0).toLocaleString()}</div>
            </div>
          </div>
        )}

        {/* Boss victory story */}
        {isBossLevel&&(()=>{const ws=WORLD_STORIES.find(s=>s.worldId===cfg.world);return ws?(
          <div className="w-full rounded-2xl px-4 py-4 text-center text-sm leading-relaxed"
            style={{background:`${wld.color}14`,border:`1px solid ${wld.color}44`,backdropFilter:"blur(8px)",color:"#f0f0d8",lineHeight:1.65}}>
            <div className="text-2xl mb-2">🎉</div>
            <div style={{color:wld.color,fontWeight:"bold",marginBottom:6}}>{wld.name} Conquered!</div>
            {ws.victory}
          </div>
        ):null;})()}

        {/* Teaser for next level / next world */}
        {!isLast&&(()=>{
          const nextId=levelId+1;
          const nextCfg=getLevelConfig(nextId);
          const nextWld=WORLDS[nextCfg.world-1];
          const isNewWorld=nextCfg.world!==cfg.world;
          const isNextBoss=nextCfg.isBoss;
          return(
            <div className="w-full rounded-2xl px-4 py-3 flex items-center gap-3"
              style={{background:`${nextWld.color}14`,border:`1px solid ${nextWld.color}40`,backdropFilter:"blur(8px)"}}>
              <span style={{fontSize:28,animation:isNewWorld?"floatGlow 1.5s ease-in-out infinite":"none"}}>
                {isNewWorld?nextWld.emoji:isNextBoss?"👑":"▶"}
              </span>
              <div className="flex-1 min-w-0">
                <div className="text-xs font-black" style={{color:nextWld.color}}>
                  {isNewWorld?`🌍 NEW WORLD: ${nextWld.name.toUpperCase()}!`:isNextBoss?"👑 BOSS BATTLE INCOMING!":"NEXT STAGE"}
                </div>
                <div className="text-xs opacity-55 truncate" style={{color:nextWld.color}}>
                  {isNewWorld?`Prepare for ${nextWld.name} — new enemies await!`:nextCfg.name}
                </div>
              </div>
              <span className="text-base" style={{color:nextWld.color,opacity:0.6}}>→</span>
            </div>
          );
        })()}
        {/* CTA buttons */}
        {!isLast&&(
          <NeonButton onClick={()=>{setMascotDancing(false);setMascotMood("idle");setSelectedLevel(levelId+1);setCartItems([]);go("shop");}}
            className="w-full py-4 text-lg font-black"
            style={{background:`linear-gradient(135deg,${wld.accent},${wld.color})`,
              boxShadow:`0 0 36px ${wld.color}55,0 4px 20px rgba(0,0,0,0.5)`,letterSpacing:"0.06em"}}>
            🚀 NEXT STAGE →
          </NeonButton>
        )}
        {isLast&&<div className="text-center font-black text-2xl py-2"
          style={{color:"#ffd700",textShadow:"0 0 40px #ffd700,0 0 80px #ffd70044",animation:"floatGlow 2s ease-in-out infinite"}}>
          🌈 YOU ARE THE QUEST MASTER! 🌈
        </div>}
        {/* PRESTIGE button — available after completing level 100 (max 5 times) */}
        {levelCompleteData?.canPrestige&&(
          <NeonButton onClick={()=>{
            if(window.confirm(`PRESTIGE! Reset levels 1-100 and earn +5% permanent score bonus? (Prestige ${(sv.prestigeLevel||0)+1}/5)`)){
              sv.prestigeLevel=(sv.prestigeLevel||0)+1;sv.unlockedLevel=1;
              unlock("prestige_1");
              flushSave();sfx("prestige");vibrate([30,15,30,15,60,15,100]);
              setPrestigeAnim(true);setTimeout(()=>setPrestigeAnim(false),2000);
              setNotif(`👑 PRESTIGE ${sv.prestigeLevel}! +${sv.prestigeLevel*5}% score bonus forever!`);
              setMascotDancing(false);setMascotMood("idle");go("levelmap");
            }
          }} className="w-full py-4 text-base font-black"
            style={{background:"linear-gradient(135deg,#ffd70033,#ffd70066)",border:"2px solid #ffd700",
              boxShadow:"0 0 40px #ffd70066",color:"#ffd700",letterSpacing:"0.07em"}}>
            👑 PRESTIGE! ({(sv.prestigeLevel||0)+1}/5) — +5% Score Forever
          </NeonButton>
        )}
        {/* Score sharing card — 9A */}
        <NeonButton onClick={()=>{
          const mascot=currentMascot;
          const starStr="⭐".repeat(stars)+"☆".repeat(3-stars);
          const accuracyStr=isSniper?` 🎯 ${accuracyPct}% Sniper!`:`📊 ${accuracyPct}% accuracy`;
          const streakStr=sessionStats.bestCombo>0?` | ⚡ ${sessionStats.bestCombo}× combo`:"";
          const txt=[
            `🎮 NexusTap Score Card`,
            `─────────────────────`,
            `${wld.emoji} ${cfg.name} (Level ${levelId})`,
            `🏆 Score: ${score.toLocaleString()} ${starStr}`,
            `${accuracyStr}${streakStr}`,
            `🐾 Mascot: ${mascot.emoji.idle} ${mascot.name}`,
            `─────────────────────`,
            `Can you beat me? 👇`,
          ].join("\n");
          if(navigator.share){navigator.share({title:"NexusTap",text:txt}).catch(()=>{});}
          else if(navigator.clipboard){navigator.clipboard.writeText(txt);setNotif("📋 Score card copied!");}
        }} className="w-full py-3 text-sm"
          style={{background:"rgba(255,255,255,0.06)",border:`1px solid ${wld.color}44`,backdropFilter:"blur(8px)"}}>
          📤 Share Score Card
        </NeonButton>
        <NeonButton onClick={()=>{setMascotDancing(false);setMascotMood("idle");setScrollToLevel(levelId);go("levelmap");}}
          className="w-full py-3 text-sm" style={{background:"rgba(255,255,255,0.06)",border:`1px solid ${wld.color}33`,backdropFilter:"blur(8px)"}}>
          ← Adventure Map
        </NeonButton>
      </div>
      {/* ── MASCOT UNLOCK OVERLAY ── */}
      {mascotUnlockedData&&(
        <div className="absolute inset-0 flex flex-col items-center justify-center z-50 px-6"
          style={{background:"rgba(0,0,0,0.88)",backdropFilter:"blur(12px)"}}>
          {/* Confetti sparkles */}
          <div className="absolute inset-0 pointer-events-none overflow-hidden">
            {[...Array(18)].map((_,i)=>(
              <div key={i} style={{
                position:"absolute",
                left:`${(i*37)%100}%`,
                top:`${(i*53)%80}%`,
                fontSize:14,
                animation:`sparkleFloat ${1.5+i*0.18}s ease-in-out ${i*0.12}s infinite`,
                opacity:0.7}}>
                {["✨","🌟","⭐","💫","🎊","🎉"][i%6]}
              </div>
            ))}
          </div>
          <div className="flex flex-col items-center gap-4 relative z-10"
            style={{animation:"victoryBurst 0.6s cubic-bezier(0.34,1.5,0.64,1)"}}>
            {/* Rarity banner */}
            {(()=>{
              const rs={starter:"#60a5fa",common:"#4ecb71",rare:"#6ec0f5",epic:"#b06de8",legendary:"#ffd700"}[mascotUnlockedData.rarity]||"#60a5fa";
              return(
                <div style={{
                  background:`linear-gradient(135deg,${rs}22,${rs}44)`,
                  border:`2px solid ${rs}`,borderRadius:14,
                  padding:"4px 18px",
                  fontSize:11,fontWeight:"black",color:rs,letterSpacing:"0.1em",
                  boxShadow:`0 0 24px ${rs}55`,
                  animation:`floatGlow 1s ease-in-out infinite`}}>
                  {({starter:"STARTER",common:"COMMON",rare:"RARE ✦",epic:"EPIC ✦✦",legendary:"LEGENDARY ✦✦✦"}[mascotUnlockedData.rarity]||"NEW")} MASCOT UNLOCKED!
                </div>
              );
            })()}
            <div style={{
              fontSize:11,color:"#ffffff88",fontWeight:"bold",letterSpacing:"0.06em"}}>
              🎉 NEW COMPANION! 🎉
            </div>
            {/* Big animated mascot */}
            <div style={{
              fontSize:100,lineHeight:1,
              filter:`drop-shadow(0 0 30px ${mascotUnlockedData.color}) drop-shadow(0 0 60px ${mascotUnlockedData.color}55)`,
              animation:`${mascotUnlockedData.dance} 0.7s ease-in-out infinite`}}>
              {mascotUnlockedData.e.victory}
            </div>
            <div style={{fontWeight:"black",fontSize:"1.8rem",color:mascotUnlockedData.color,
              textShadow:`0 0 30px ${mascotUnlockedData.color}`,letterSpacing:"0.06em"}}>
              {mascotUnlockedData.name}
            </div>
            <div style={{
              fontSize:13,color:"#ffffffaa",textAlign:"center",
              fontStyle:"italic",maxWidth:240,lineHeight:1.4}}>
              "{mascotUnlockedData.catchphrase}"
            </div>
            <div className="flex gap-3 w-full mt-2">
              <NeonButton
                onClick={()=>{saveRef.current.mascotId=mascotUnlockedData.id;debounceSave();setMascotUnlockedData(null);}}
                className="flex-1 py-3 font-black"
                style={{background:`linear-gradient(135deg,${mascotUnlockedData.color}33,${mascotUnlockedData.color}55)`,
                  border:`2px solid ${mascotUnlockedData.color}`,color:mascotUnlockedData.color,
                  boxShadow:`0 0 24px ${mascotUnlockedData.color}44`,fontSize:13}}>
                Choose {mascotUnlockedData.name}! 🌟
              </NeonButton>
              <NeonButton
                onClick={()=>setMascotUnlockedData(null)}
                className="flex-1 py-3 font-black text-sm"
                style={{background:"#ffffff10",color:"#fff"}}>
                Later
              </NeonButton>
            </div>
          </div>
        </div>
      )}
    </>);
  };

  // ── Game Over ──
  const renderGameOver=()=>{
    if(!gameOverData)return null;
    const{score,levelId,levelName,scoreGoal,sessionStats,nearMiss,shortfall}=gameOverData;
    const cfg=getLevelConfig(levelId);
    const pct=Math.min(100,Math.round(score/scoreGoal*100));
    return(
      <div className="flex flex-col items-center h-full overflow-y-auto px-5 py-6 gap-4 relative z-10">
        {/* Near-Miss special header */}
        {/* Sad mascot on game over */}
        <div className="flex flex-col items-center gap-2" style={{animation:"waveIn 0.5s ease-out"}}>
          <MascotEmoji mood="scared" size={88}/>
          <div className="font-bold text-center" style={{
            fontSize:"0.85rem",color:currentMascot.color,
            textShadow:`0 0 12px ${currentMascot.color}55`,maxWidth:200}}>
            {nearMiss
              ?"Nooo!! So close! I believe in you!! 💪"
              :"Awww! Let's try again together! 🌈"}
          </div>
        </div>
        {nearMiss?(
          <div className="w-full text-center rounded-3xl px-4 py-4" style={{
            background:`linear-gradient(160deg,#ff980014,rgba(0,0,0,0.3))`,
            border:"1px solid #ff980055",backdropFilter:"blur(10px)",animation:"waveIn 0.5s ease-out"}}>
            <div className="font-black text-xl mb-1" style={{color:"#ff9800",textShadow:"0 0 20px #ff980077"}}>SO CLOSE!</div>
            <div className="text-sm opacity-80" style={{color:"#ffc87a"}}>
              Only <span className="font-black" style={{color:"#ffd700"}}>{shortfall?.toLocaleString()}</span> more points needed!
            </div>
            <div className="text-xs opacity-50 mt-1" style={{color:"#ff9800"}}>You reached {pct}% of the goal 🎯</div>
          </div>
        ):(
          <div className="text-center">
            <div className="text-sm uppercase tracking-widest opacity-50 mb-1" style={{color:cfg.worldColor}}>{WORLDS[cfg.world-1].emoji} {WORLDS[cfg.world-1].name}</div>
            <div className="text-xs uppercase tracking-widest font-bold opacity-60 mb-1" style={{color:"#ef4444"}}>— Almost there! Try again! —</div>
            <div className="text-xl font-black" style={{color:cfg.worldColor}}>{levelName}</div>
          </div>
        )}
        {/* Score + progress */}
        <div className="w-full rounded-3xl p-4 relative" style={{
          background:`linear-gradient(160deg,${cfg.worldColor}14 0%,rgba(0,0,0,0.4) 100%)`,
          border:`1px solid ${cfg.worldColor}44`,backdropFilter:"blur(10px)"}}>
          {/* Letter grade on game over */}
          {(()=>{
            const grade=pct>=90?"A":pct>=70?"B":pct>=50?"C":"D";
            const gc=pct>=90?"#fbbf24":pct>=70?"#60a5fa":pct>=50?"#94a3b8":"#ef4444";
            return <div style={{position:"absolute",top:8,right:10,fontSize:24,fontWeight:"black",color:gc,textShadow:`0 0 12px ${gc}`}}>{grade}</div>;
          })()}
          <div className="text-center mb-3">
            <div className="text-xs uppercase tracking-widest opacity-40 mb-1" style={{color:cfg.worldColor}}>Score</div>
            <div className="font-black tabular-nums" style={{fontSize:"clamp(1.8rem,9vw,2.8rem)",color:cfg.worldColor,
              textShadow:`0 0 24px ${cfg.worldColor}55`}}>{score.toLocaleString()}</div>
          </div>
          <div className="flex justify-between text-xs mb-1.5 opacity-55" style={{color:cfg.worldColor}}>
            <span>Quest progress</span><span>{pct}%</span>
          </div>
          <div className="h-3 rounded-full relative" style={{background:"#ffffff12"}}>
            <div className="h-full rounded-full transition-all" style={{width:`${pct}%`,
              background:nearMiss?`linear-gradient(90deg,${cfg.worldColor}88,#ff9800)`:
                `linear-gradient(90deg,${cfg.worldColor}88,${cfg.worldColor})`,
              boxShadow:nearMiss?`0 0 16px #ff980088`:`0 0 12px ${cfg.worldColor}88`}}/>
            {/* Near-miss gap indicator */}
            {nearMiss&&<div style={{position:"absolute",right:0,top:-2,height:18,width:2,background:"#ff9800",
              boxShadow:"0 0 8px #ff9800",borderRadius:2}}/>}
          </div>
          {nearMiss&&<div className="text-xs text-center mt-2 font-bold" style={{color:"#ff9800"}}>⬅ This close to winning!</div>}
        </div>
        <div className="w-full rounded-2xl p-3 grid grid-cols-3 gap-2"
          style={{background:"rgba(0,0,0,0.3)",border:`1px solid ${cfg.worldColor}1a`,backdropFilter:"blur(8px)"}}>
          {[["👆 Taps",sessionStats.tapsTotal],["✨ Magic+",sessionStats.rareHits],["🔥 Streak",sessionStats.bestCombo+"×"],
            ["🐉 Bosses",sessionStats.bossKills||0],["🎯 Perfect",sessionStats.perfectTaps||0],["⏱ Time",sessionStats.timeSurvived+"s"]
          ].map(([l,v])=>(
            <div key={l} className="text-center py-1">
              <div className="text-xs opacity-40 mb-0.5" style={{color:cfg.worldColor}}>{l}</div>
              <div className="font-black text-sm" style={{color:cfg.worldColor}}>{v}</div>
            </div>
          ))}
        </div>
        {/* ── RESCUE GAMBLE ── */}
        {nearMiss&&rescueSecondsLeft>0&&sv.coins>=50&&(
          <div className="w-full rounded-2xl overflow-hidden" style={{
            background:"linear-gradient(135deg,#7c2d12,#c2410c,#7c2d12)",
            border:"3px solid #ff6030",
            boxShadow:"0 0 40px #ff603055",
            animation:"rescuePulse 0.6s ease-in-out infinite alternate"}}>
            <div className="flex flex-col items-center gap-2 px-4 py-4">
              <div style={{fontSize:42,animation:"mascotShake 0.4s ease-in-out infinite"}}>
                {currentMascot.e.scared}
              </div>
              <div style={{color:"#ffd700",fontWeight:"black",fontSize:"1.3rem",letterSpacing:"0.05em",textShadow:"0 0 20px #ffd700"}}>
                🚨 LAST CHANCE!
              </div>
              <div style={{color:"#ffc87a",fontSize:"0.85rem",textAlign:"center"}}>
                Restart with <span style={{color:"#ffd700",fontWeight:"bold"}}>FULL LIVES + 400 BONUS</span>
              </div>
              <div style={{color:"#ff9800",fontWeight:"black",fontSize:"1.5rem",textShadow:"0 0 16px #ff9800"}}>
                {rescueSecondsLeft}s
              </div>
              <NeonButton
                onClick={()=>{
                  saveRef.current.coins-=50;flushSave();
                  setRescueSecondsLeft(0);rescuedRef.current=true;
                  startGame(levelId,[]);
                }}
                className="w-full py-4 text-xl font-black"
                style={{background:"linear-gradient(135deg,#fbbf24,#f59e0b)",color:"#000",
                  boxShadow:"0 0 30px #fbbf2488",letterSpacing:"0.08em"}}>
                RESCUE ME! (50🪙)
              </NeonButton>
              <div style={{color:"#ff9800",fontSize:"0.75rem",opacity:0.7}}>
                You have {sv.coins}🪙
              </div>
            </div>
          </div>
        )}
        <NeonButton onClick={()=>{setRescueSecondsLeft(0);setSelectedLevel(levelId);setCartItems([]);go("shop");}}
          className="w-full py-4 text-lg font-black"
          style={{background:nearMiss?`linear-gradient(135deg,#b45309,#ff9800)`:
            `linear-gradient(135deg,${cfg.worldColor}99,${cfg.worldColor})`,
            boxShadow:nearMiss?`0 0 36px #ff980066,0 4px 20px rgba(0,0,0,0.5)`:
              `0 0 32px ${cfg.worldColor}55,0 4px 20px rgba(0,0,0,0.5)`,
            letterSpacing:"0.06em",fontSize:nearMiss?"1.2rem":"1rem"}}>
          {nearMiss?"🔥 SO CLOSE — TRY AGAIN!":"🌟 TRY AGAIN!"}
        </NeonButton>
        <NeonButton onClick={()=>go("levelmap")} className="w-full py-3 text-sm"
          style={{background:"rgba(255,255,255,0.06)",border:`1px solid ${cfg.worldColor}33`,backdropFilter:"blur(8px)"}}>
          ← Adventure Map
        </NeonButton>
      </div>
    );
  };

  // ── Missions ──
  const renderMissions=()=>{
    const missions=getDailyMissions(),prog=sv.missionProgress||{};
    return(
      <div className="flex flex-col h-full px-4 py-5 gap-4 overflow-y-auto relative z-10">
        <div className="flex items-center gap-3">
          <NeonButton onClick={()=>go("menu")} className="px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
          <h2 className="text-xl font-black" style={{color:theme.accent}}>Daily Missions</h2>
          {sv.missionCompleted&&<span className="text-xs px-2 py-1 rounded-full font-bold" style={{background:"#34d39922",color:"#34d399"}}>DONE ✓</span>}
        </div>
        <p className="text-xs opacity-40 -mt-2" style={{color:theme.accent}}>Complete all 3 for +200 XP bonus</p>
        {missions.map(m=>{
          const cur=Math.min(prog[m.id]||0,m.goal),pct=cur/m.goal*100,done=cur>=m.goal;
          return(
            <div key={m.id} className="rounded-2xl p-4" style={{background:done?`${theme.accent}18`:"#ffffff07",border:`1px solid ${done?theme.accent+"55":"#ffffff12"}`}}>
              <div className="flex justify-between items-center mb-2">
                <span className="font-bold text-sm" style={{color:done?theme.accent:"#e2e8f0"}}>{done?"✓ ":""}{m.desc}</span>
                <span className="text-xs opacity-50 tabular-nums" style={{color:theme.accent}}>{cur}/{m.goal}</span>
              </div>
              <div className="h-2.5 rounded-full" style={{background:"#ffffff15"}}>
                <div className="h-full rounded-full transition-all duration-500" style={{width:`${pct}%`,background:done?theme.accent:`${theme.accent}88`,boxShadow:done?`0 0 8px ${theme.accent}`:""}}/>
              </div>
            </div>
          );
        })}
        {sv.missionCompleted&&<div className="text-center font-bold py-2" style={{color:"#fbbf24"}}>🏆 All complete! +200 XP</div>}
      </div>
    );
  };

  // ── Skill Tree ──
  const renderSkillTree=()=>{
    const skills=sv.skills||{};
    const pts=sv.skillPoints||0;
    return(
      <div className="flex flex-col h-full px-4 py-5 gap-4 overflow-y-auto relative z-10">
        <div className="flex items-center justify-between">
          <div className="flex items-center gap-3">
            <NeonButton onClick={()=>go("menu")} className="px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
            <h2 className="text-xl font-black" style={{color:theme.accent}}>Skill Tree</h2>
          </div>
          <div className="px-3 py-1.5 rounded-xl font-black text-sm" style={{background:`${theme.accent}22`,border:`1px solid ${theme.accent}66`,color:theme.accent}}>
            ✨ {pts} pt{pts!==1?"s":""}
          </div>
        </div>
        <p className="text-xs opacity-40 -mt-2">Earn skill points by leveling up. Permanent bonuses for all future runs.</p>
        <div className="flex flex-col gap-3">
          {SKILL_TREE.map(skill=>{
            const lvl=skills[skill.id]||0;
            const maxed=lvl>=3;
            const cost=maxed?0:skill.cost[lvl];
            const canAfford=pts>=cost&&!maxed;
            return(
              <div key={skill.id} className="rounded-2xl p-4" style={{background:"#ffffff07",border:`1px solid ${maxed?"#ffd70066":theme.accent+"22"}`}}>
                <div className="flex items-center justify-between mb-2">
                  <div className="flex items-center gap-2">
                    <span style={{fontSize:22}}>{skill.icon}</span>
                    <div>
                      <div className="font-black text-sm" style={{color:maxed?"#ffd700":theme.accent}}>{skill.name}{maxed?" ★ MAX":""}</div>
                      <div style={{fontSize:10,color:"#ffffff44"}}>{skill.desc}</div>
                    </div>
                  </div>
                  {!maxed&&(
                    <NeonButton
                      onClick={()=>{
                        if(!canAfford)return;
                        sv.skills={...skills,[skill.id]:lvl+1};
                        sv.skillPoints=pts-cost;
                        flushSave();
                        setNotif(`${skill.icon} ${skill.name} → Lv ${lvl+1}!`);
                      }}
                      className="px-4 py-2 text-xs font-black"
                      disabled={!canAfford}
                      style={{
                        background:canAfford?`${theme.accent}25`:"#ffffff08",
                        border:`1px solid ${canAfford?theme.accent+"88":"#ffffff15"}`,
                        color:canAfford?theme.accent:"#666",
                        opacity:canAfford?1:0.5,
                      }}>
                      ✨{cost} → Lv{lvl+1}
                    </NeonButton>
                  )}
                </div>
                {/* Level pips */}
                <div className="flex gap-1.5 mb-1.5">
                  {[0,1,2].map(i=>(
                    <div key={i} className="h-2 flex-1 rounded-full" style={{
                      background:i<lvl?theme.accent:(i===lvl&&!maxed?"#ffffff18":"#ffffff0a"),
                      boxShadow:i<lvl?`0 0 6px ${theme.accent}88`:"none",
                    }}/>
                  ))}
                </div>
                {/* Current effect label */}
                <div style={{fontSize:11,color:lvl>0?theme.accent:"#ffffff33"}}>
                  {lvl>0?`Active: ${skill.levels[lvl-1]}`:`Inactive — upgrade to unlock`}
                  {!maxed&&<span style={{color:"#ffffff33",marginLeft:6}}>Next: {skill.levels[lvl]}</span>}
                </div>
              </div>
            );
          })}
        </div>
        <div className="text-center text-xs opacity-30 pb-4">Skill points earned by leveling up (every {XP_PER_LVL} XP)</div>
      </div>
    );
  };

  // ── Achievements ──
  // Achievement tab state — "all" | "gameplay" | "progression" | "social"
  const [achTab, setAchTab] = React.useState("all");
  const ACH_CATS = {
    gameplay:    ["first_tap","combo_10","boss_1","fever_1","perfect_5","combo_15","chain_4","mimic_hit","shielded_hit","speed_demon","phantom_catch","volatile_defuse","frozen_catch","bouncy_catch","ninja_catch","loot_chest","tornado_catch","bubble_pop","bomb_defuse","echo_tap","echo_bonus","crystal_shatter","crystal_chain","rage_tap","rage_max","divider_tap","chain_lightning_hit","gold_rush","first_tap_fever","bounty_hit","vanishing_tap","vanishing_blind","tap_frenzy","homing_tap","homing_center","gemstone_tap","morph_tap","morph_legendary","time_warp_use","conductor_tap","siphon_tap","glitch_tap","glitch_perfect","prism_tap","comet_tap","comet_early","mirrorball_tap","nexus_tap","phoenix_tap","phoenix_risen","icecomet_tap","voltage_tap","void_tap","void_master","clover_tap","clover_jackpot","ricochet_tap","ricochet_double","aurora_tap","aurora_perfect","fury_mode","score_10k","portal_tap","portal_chaos","particlebomb_tap","beacon_tap","beacon_surge","shadow_tap","shadow_clone_catch","spectral_tap","spectral_perfect","heart_tap","nova_tap","firefly_tap","firefly_swift","geode_crack","geode_gem","timebomb_defuse","timebomb_clutch","thunderbolt_tap","thunderbolt_clutch","gravityorb_tap","gravityorb_cluster","crystalball_tap","quantum_tap","quantum_stable","clockwork_tap","bloom_tap","bloom_harvest","reflector_tap","reflector_chaos","chain_collect","chain_triple","stardust_tap","stardust_shower","score_25k","solarflare_tap","solarflare_early","magma_tap","prismbomb_tap","prismbomb_triple","icecage_tap","icecage_full","pinwheel_tap","pinwheel_jackpot","supercell_tap","supercell_storm","vortex_tap","vortex_feast","pixel_tap","pixel_speedrun","neon_tap","neon_gold","lantern_tap","lantern_chain","drift_tap","drift_max","berserker_use","ghost_mode_use","nebula_tap","nebula_cluster","wisp_tap","wisp_still","meteor_tap","meteor_early","aura_tap","aura_edge","thunderclap_tap","thunderclap_surge","mirror_tap","mirror_triple","rune_tap","rune_power","score_50k","fractal_tap","fractal_cascade","plasma_tap","plasma_surge","timewarp_target_tap","timewarp_hoard","solar_drone_tap","solar_drone_orbit","cluster_tap","cluster_rainbow","swarm_tap","swarm_master","celestial_tap","celestial_legend","smoke_tap","smoke_early","chainbomb_tap","chainbomb_safe","warpgate_tap","warpgate_chaos","mirage_tap","mirage_avoid","surgeball_tap","surgeball_chain","clock_tap","clock_jackpot","echopulse_tap","echopulse_bonus","gravwell_tap","gravwell_feast","spectrum_tap","spectrum_gold","shardstorm_tap","shardstorm_align","pulsenova_tap","pulsenova_release","bubble_tap","bubble_all","voidshard_tap","voidshard_rich","stasis_tap","stasis_core","crystalline_tap","crystalline_facets","qleap_tap","qleap_between","magma_burst_tap","magma_burst_hot","lightning_rod_tap","lightning_rod_surge","prismgate_tap","prismgate_triple","solarburst_tap","solarburst_aoe","frostcomet_tap","frostcomet_freeze","energyweb_tap","energyweb_nodes","voidrift_tap","voidrift_feast","lasergrid_tap","lasergrid_beam","bouncing_tap","bouncing_4x","timecapsule_tap","timecapsule_golden","nethershard_tap","nethershard_max","accumulator_tap","accumulator_max","jackpot_tap","phaseorb_tap","phaseorb_bonus","ripplewave_tap","ripplewave_chain","score_100k","venomorb_tap","venomorb_antidote","thunderegg_tap","thunderegg_crack","moonglyph_tap","moonglyph_match","comettail_tap","comettail_early","emberring_tap","emberring_max","supernova_tap","supernova_collect","crystalmatrix_tap","crystalmatrix_shatter","plasmastorm_tap","plasmastorm_surge","nexusgate_tap","nexusgate_chain","arclight_tap","arclight_web","pulsechain_tap","pulsechain_burst","ghostorb_tap","ghostorb_phantom","timefracture_tap","timefracture_haul","solarorbit_tap","solarorbit_align","chroma_tap","chroma_gold","pulsar_tap","pulsar_peak","driftmine_tap","driftmine_clutch","eclipse_tap","eclipse_moon","spinbloom_tap","spinbloom_full","teslanode_tap","teslanode_rich","runecircle_tap","runecircle_jackpot","starforge_tap","starforge_max","magicdice_tap","magicdice_six","wormhole_tap","wormhole_chaos","shadowpulse_tap","shadowpulse_dark","qsplit_tap","qsplit_collect","frostbolt_tap","frostbolt_speed","vortexstar_tap","vortexstar_max","pixelburst_tap","pixelburst_full","neoncrystal_tap","neoncrystal_hot","sonarping_tap","sonarping_cluster","gravitybomb_tap","gravitybomb_early","speednova_tap","speednova_max","omega_tap","omega_legendary","thunderball_tap","thunderball_chain","cosmicaura_tap","comborush_tap","comborush_max","score_500k","twilight_tap","twilight_catch","lifeorb_tap","scoreamp_tap","scoreamp_multi","nightorb_tap","nightorb_bright","shift_tap","shift_legendary","luckwheel_tap","luckwheel_max","abyss_tap","portal_tap2","portal_bonus2","flamering_tap","flamering_sweet","iceclock_tap","iceclock_5","holodecoy_tap","holodecoy_real","mirrorgate_tap","mirrorgate_echo","spiralbomb_tap","spiralbomb_full","chainorb_tap","chainorb_max","bolt_tap","bolt_fast","gemharvest_tap","gemharvest_rich","prismpulse_tap","prismpulse_match","darkmatter_tap","darkmatter_blind","cometblast_tap","cometblast_all","timeprison_tap","timeprison_max","shadowtwin_tap","shadowtwin_lucky","photon_tap","photon_precise","vault_tap","vault_rich","nanoswarm_tap","nanoswarm_all","runechain_tap","runechain_max","plasmaweb_tap","plasmaweb_dense","astral_tap","astral_instant","crystal_clock_tap","crystal_clock_max","score_250k","score_1m","sunflower_tap","sunflower_full","toxic_tap","toxic_zone","laserprism_tap","laserprism_chain","echostar_tap","echostar_all","meteor_shower_tap","meteor_shower_all","golden_gate_tap","pulse_ring_tap","pulse_ring_sweet","clockwork_tap","clockwork_max","nebula_vortex_tap","nebula_vortex_big","frost_hex_tap","frost_hex_freeze","lightning_orb_tap","lightning_orb_chain","starburst_tap","starburst_full","crystalspire_tap","crystalspire_all","phasegate_tap","phasegate_clutch","voidpulsar_tap","voidpulsar_early","novacannon_tap","novacannon_mega","hexgrid_tap","hexgrid_full","qtunnel_tap","qtunnel_both","inferno_tap","inferno_max","chrono_tap","chrono_surge","prism_lens_tap","prism_lens_focus","shadow_mirror_tap","shadow_mirror_hit","plasma_comet_tap","plasma_comet_apex","abyssal_tap","abyssal_drain","warpball_tap","warpball_instant","crystalspike_tap","crystalspike_ow","energycore_green","energycore_red","arcane_tap","arcane_match","stellar_tap","stellar_far","bubblewave_tap","bubblewave_clutch","ironshield_tap","ironshield_break","timeecho_tap","timeecho_perfect","combo_shield_use","perfect_aim_use","gravity2_tap","gravity2_max","pixelswarm_tap","pixelswarm_full","chain_burst_use","magnet_burst_use","chrono_tap","chrono_bonus","voidseed_tap","voidseed_max","score_pulse_use","life_shield_use","prism_shard_tap","prism_shard_full","temporal_tap","temporal_rewind","mult_surge_use","bounce_back_use","lchain_tap","lchain_max","magmaseed_tap","magmaseed_full","fever_extend_use","coin_rush_use","gemini_tap","gemini_combo","aurora_ring_tap","aurora_ring_max","gravity_well_use","clone_army_use","neonpulse_tap","neonpulse_bright","mirrortwin_tap","mirrortwin_both","rush_mode_use","invincible_use","starcluster_tap","starcluster_full","voidcrystal_tap","voidcrystal_tiny","healer_use","score_guard_use","energy_orb_tap","energy_orb_surge","comettrail_tap","comettrail_max","swipe_boost_use","lucky_coin_use","runeburst_tap","runeburst_max","golem_tap","golem_destroy","echo_shot_use","burst_shield_use","freeze_orb_tap","freeze_orb_bonus","twincomet_tap","twincomet_clean","magnet_orbit_use","score_echo_use","score_echo_proc","plasmavortex_tap","plasmavortex_peak","starbomb_tap","phantom_burst_use","streak_storm_use","streak_storm_bonus","glowshard_tap","glowshard_full","quantumleech_tap","time_bubble_use","crit_boost_use","crit_boost_proc","novastar_tap","novastar_sweep","icephoenix_tap","gold_rush_use","mirror_maze_use","spiralorb_tap","spiralorb_far","darkpulse_tap","darkpulse_bonus","blast_wave_use","blast_wave_sweep","tempo_sync_use","lunareclipse_tap","lunareclipse_max","chainwall_tap","life_leech_use","life_leech_proc","score_rain_use","bounceweb_tap","bounceweb_snare","meteorshower_tap","chain_magnet_use","chain_magnet_pull","score_surge_use","score_surge_peak","prismatic_tap","prismatic_bonus","voidtether_tap","orbit_shield_use","orbit_shield_abs","echo_wave_use","echo_wave_multi","crystalpulsar_tap","crystalpulsar_peak","shadowecho_tap","combo_burst_use","combo_burst_big","phase_lock_use","phase_lock_haul","fluxring_tap","fluxring_early","quantumflare_tap","score_leech_use","score_leech_gain","split_shot_use","split_shot_double","nebulacrystal_tap","nebulacrystal_3","timeshard_tap","fever_storm_use","rarity_boost_use","rarity_boost_leg","gravitynode_tap","gravitynode_pull","lightningcage_tap","lightningcage_all","twin_fever_use","reflect_wave_use","reflect_wave_hit","plasmacomet_tap","plasmacomet_early","crystalweb_tap","crystalweb_chain","nova_boost_use","nova_boost_proc","streak_lock_use","streak_lock_save","arcpulse_tap","arcpulse_arc","voidbloom_tap","voidbloom_full","magnet_storm_use","magnet_storm_pull","score_freeze_use","score_freeze_save","stellarorb_tap","stellarorb_max","runegate_tap","runegate_chain","chain_fever_use","chain_fever_proc","life_bomb_use","life_bomb_proc","novacrystal_tap","novacrystal_full","phaseshift_tap","phaseshift_solid","turbo_tap_use","turbo_tap_triple","coin_magnet_use","coin_magnet_rich","solarring_tap","solarring_edge","chronovortex_tap","chronovortex_slow","blast_radius_use","blast_radius_aoe","score_accel_use","score_accel_peak","prismnova_tap","prismnova_shards","echoblast_tap","echoblast_ring","life_chain_use","life_chain_heal","score_mirror_use","score_mirror_big","vortexstar_61_tap","vortexstar_61_out","qdrift_tap","qdrift_freeze","fever_lock_use","fever_lock_save","streak_surge_use","streak_surge_big","plasmaburst_tap","plasmaburst_all","starclock_tap","starclock_zero","combo_echo_use","combo_echo_proc","gold_shield_use","gold_shield_proc","nebulaclock_tap","nebulaclock_all","iceshatter_tap","iceshatter_done","phantom_life_use","phantom_life_save","score_doubler_use","score_doubler_tri","hue_orb_tap","hue_orb_match","lchain_64_tap","lchain_64_max","magnet_blitz_use","magnet_blitz_pull","crit_storm_use","crit_storm_proc","arcanerune_tap","arcanerune_match","pulsegem_tap","pulsegem_peak","bonus_wave_use","bonus_wave_all","tap_fury_use","tap_fury_big","cosmicweb_tap","cosmicweb_all","warporb_tap","warporb_crit","score_spike_use","score_spike_proc","life_burst_use","life_burst_heal","darkmirror_tap","darkmirror_peak","stormball_tap","stormball_early","phase_burst_use","phase_burst_bomb","vortex_tap_use","vortex_tap_pull","chainnova_tap","chainnova_ring","mirrorflash_tap","mirrorflash_true","chain_lock_use","chain_lock_max","nova_storm_use","nova_storm_proc","prismclock_tap","prismclock_match","voidpulse_tap","voidpulse_max","echo_chain_use","echo_chain_proc","gravity_burst_use","gravity_burst_big","arcbloom_tap","arcbloom_full","neonbolt_tap","neonbolt_fast","score_flood_use","score_flood_big","life_leech_plus","life_leech_plus_3","crystalburst_tap","crystalburst_all","timerift_tap","timerift_clutch","combo_surge_use","combo_surge_big","star_forge_use","star_forge_leg","glitchorb_tap","glitchorb_vis","solarflare72_tap","solarflare72_peak","pulse_shield_use","pulse_shield_blk","score_amplify_use","score_amplify_big","frostnova_tap","frostnova_freeze","lightbomb_tap","lightbomb_chain","ricochet_blast_use","ricochet_blast_nb","fever_fountain_use","fever_fountain_ext","omegapulse_tap","omegapulse_fast","spiralorb74_tap","spiralorb74_center","double_fever_use","double_fever_2nd","bomb_convert_use","bomb_convert_proc","nexuscore_tap","nexuscore_edge","blinkstar_tap","blinkstar_crit","score_magnet_use","score_magnet_save","life_overflow_use","life_overflow_bonus","etherOrb_tap","etherOrb_copies","qblast_tap","qblast_tiny","streak_heal_use","streak_heal_full","tempo_burst_use","tempo_burst_peak","plasmadisk_tap","plasmadisk_edge","voidring_tap","voidring_inner","crit_surge_use","crit_surge_big","life_pulse_use","life_pulse_full","sunpetal_tap","sunpetal_bloom","ghostbell_tap","ghostbell_silent","chain_wave_use","chain_wave_chain","score_od_use","score_od_peak","moon_orb_tap","moon_orb_phase","lava_core_tap","lava_core_chain","phantom_strike_use","phantom_strike_hit","coin_surge_use","coin_surge_rich","arcnode_tap","arcnode_link","prismaticburst_tap","prismaticburst_all","freeze_surge_use","freeze_surge_big","lucky_strike_use","lucky_strike_jack","crystalwing_tap","crystalwing_pair","stormcloud_tap","stormcloud_bolt","echo_burst_use","echo_burst_big","shield_surge_use","shield_surge_blk","solarpetal_tap","solarpetal_chain","cosmicshard_tap","cosmicshard_align","score_snap_use","score_snap_big","life_guard_use","life_guard_save","novadrift_tap","novadrift_center","energypulse_tap","energypulse_max","burst_fever_use","burst_fever_peak","score_trail_use","score_trail_big","icedagger_tap","icedagger_tip","shadowflare_tap","shadowflare_bright","chain_snap_use","chain_snap_proc","life_echo_use","life_echo_trigger","plasmaring_tap","plasmaring_inside","voidstar_tap","voidstar_core","score_vault_use","score_vault_big","streak_echo_use","streak_echo_peak","aurorawave_tap","aurorawave_crest","crystalprism_tap","crystalprism_split","nova_pulse_use","nova_pulse_big","coin_echo_use","coin_echo_rich","thunderbead_tap","thunderbead_chain","mirrorstar_tap","mirrorstar_match","time_shield_use","time_shield_save","score_blitz_use","score_blitz_peak","frostgem_tap","frostgem_freeze","heatorb_tap","heatorb_hot","phantom_rush_use","phantom_rush_big","coin_storm_use","coin_storm_rich","nebuladrop_tap","nebuladrop_collect","pulsegate_tap","pulsegate_open","streak_blaze_use","streak_blaze_max","life_bloom_use","life_bloom_max","spiralbead_tap","spiralbead_center","quantumflux_tap","quantumflux_stable","focus_blast_use","focus_blast_big","coin_chain_use","coin_chain_max","nebulacore_tap","nebulacore_center","twilightorb_tap","twilightorb_dusk","shield_burst_use","shield_burst_tank","score_echo_use","score_echo_big","flameorb_tap","flameorb_super","crystalshell_tap","crystalshell_core","combo_surge2_use","combo_surge2_peak","life_spiral_use","life_spiral_max","starpulse_tap","starpulse_sync","voidnova_tap","voidnova_burst","tempo_echo_use","tempo_echo_beat","coin_bloom_use","coin_bloom_full","solarbeam_tap","solarbeam_center","iceprism_tap","iceprism_split","streak_nova_use","streak_nova_peak","life_storm_use","life_storm_max","prismaticeye_tap","prismaticeye_gaze","thundercrown_tap","thundercrown_zap","gold_frenzy_use","gold_frenzy_rich","phantom_coin_use","phantom_coin_haul","cosmicdust_tap","cosmicdust_cloud","novabell_tap","novabell_ring","score_frenzy_use","score_frenzy_big","life_beacon_use","life_beacon_save","warpglyph_tap","warpglyph_portal","etherbolt_tap","etherbolt_chain","blaze_trail_use","blaze_trail_long","coin_pulse_use","coin_pulse_rich","lunarrune_tap","lunarrune_full","stormseed_tap","stormseed_grow","fever_bloom_use","fever_bloom_peak","coin_cascade_use","coin_cascade_max","plasmahalo_tap","plasmahalo_sweet","glitchnode_tap","glitchnode_burst","score_omega_use","score_omega_max","eternal_life_use","eternal_life_hero","omegaheart_tap","omegaheart_sync","nexusgem_tap","nexusgem_core","arc_surge_use","arc_surge_chain","relic_pulse_use","relic_pulse_max","shadowcrystal_tap","shadowcrystal_dark","tideorb_tap","tideorb_crest","venom_surge_use","venom_surge_max","lunar_bloom_use","lunar_bloom_max","crystalpetal_tap","crystalpetal_align","twilightshard_tap","twilightshard_dusk","star_chain_use","star_chain_max","void_bloom_use","void_bloom_max","novaspire_tap","novaspire_peak","echolens_tap","echolens_focus","pulse_wave_use","pulse_wave_max","coin_vortex_use","coin_vortex_max","frosthalo_tap","frosthalo_freeze","emberclock_tap","emberclock_clutch","gravity_burst2_use","gravity_burst2_max","score_avalanche_use","score_avalanche_max","prismcoil_tap","prismcoil_sync","starveil_tap","starveil_reveal","inferno_chain_use","inferno_chain_max","spectral_coin_use","spectral_coin_max","driftorb_tap","driftorb_still","chronostar_tap","chronostar_apex","magma_wave_use","magma_wave_max","cosmic_echo_use","cosmic_echo_max","fluxring2_tap","fluxring2_inner","solarvortex_tap","solarvortex_eye","frost_lance_use","frost_lance_max","life_matrix_use","life_matrix_max","quantumburst2_tap","quantumburst2_tiny","nebularing_tap","nebularing_outer","thunder_burst_use","thunder_burst_max","shimmer_coin_use","shimmer_coin_max","glacierspike_tap","glacierspike_tip","plasmapulse2_tap","plasmapulse2_peak","score_quake_use","score_quake_max","echo_life_use","echo_life_max","darkpulsar_tap","darkpulsar_burst","crystalbomb_tap","crystalbomb_safe","nova_cascade_use","nova_cascade_max","time_warp2_use","time_warp2_max","astralcore_tap","astralcore_align","lightbolt_tap","lightbolt_charged","solar_pulse_use","solar_pulse_max","void_chain_use","void_chain_max","solareye_tap","solareye_open","voidserpent_tap","voidserpent_vert","ember_wave_use","ember_wave_max","crystal_net_use","crystal_net_max","moonpetal_tap","moonpetal_bloom","stardust2_tap","stardust2_trail","prism_echo_use","prism_echo_max","volt_surge_use","volt_surge_max","icenova2_tap","icenova2_freeze","arcgem_tap","arcgem_charged","shadow_bloom_use","shadow_bloom_max","nexus_wave_use","nexus_wave_max","flameorb2_tap","flameorb2_peak","thorgem_tap","thorgem_storm","quantum_echo_use","quantum_echo_max","frost_crown_use","frost_crown_max","cosmiclens_tap","cosmiclens_focus","nebulaburst2_tap","nebulaburst2_nova"],
    progression: ["level_10","level_50","level_100","prestige_1","three_stars_5","mascot_lv10","streak_25","streak_50","streak_10","streak_5","score_2000","world_complete","score_100k","score_250k","score_500k","score_1m"],
    social:      ["missions_all","daily_7","friday_fever","weekend_warrior","all_worlds","streak_saver","loot_chest"],
  };
  const renderAchievements=()=>{
    const filtered=achTab==="all"?ACHIEVEMENTS:ACHIEVEMENTS.filter(a=>ACH_CATS[achTab]?.includes(a.id));
    const unlockedCount=sv.unlockedAchievements.length;
    return(
    <div className="flex flex-col h-full px-4 py-5 gap-4 overflow-y-auto relative z-10">
      <div className="flex items-center gap-3">
        <NeonButton onClick={()=>go("menu")} className="px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
        <h2 className="text-xl font-black" style={{color:theme.accent}}>Medals</h2>
        <span className="text-xs opacity-40" style={{color:theme.accent}}>{unlockedCount}/{ACHIEVEMENTS.length}</span>
      </div>
      {/* Tab bar */}
      <div className="flex gap-1.5 rounded-xl p-1" style={{background:"rgba(0,0,0,0.3)"}}>
        {[["all","All"],["gameplay","🎮"],["progression","📈"],["social","🤝"]].map(([t,label])=>(
          <button key={t} onClick={()=>setAchTab(t)}
            className="flex-1 py-1.5 rounded-lg text-xs font-black transition-all"
            style={{background:achTab===t?theme.accent+"33":"transparent",color:achTab===t?theme.accent:"#ffffff55",
              border:achTab===t?`1px solid ${theme.accent}66`:"1px solid transparent"}}>
            {label}
          </button>
        ))}
      </div>
      {/* Overall XP from achievements */}
      {achTab==="all"&&(
        <div className="rounded-xl px-3 py-2 flex items-center gap-2" style={{background:`${theme.accent}12`,border:`1px solid ${theme.accent}33`}}>
          <span style={{fontSize:18}}>⚡</span>
          <div className="text-xs" style={{color:theme.accent}}>
            Total achievement XP: <span className="font-black">{ACHIEVEMENTS.filter(a=>sv.unlockedAchievements.includes(a.id)).reduce((s,a)=>s+(a.xp||0),0)}</span>
          </div>
          <div className="ml-auto text-xs font-black" style={{color:theme.accent}}>{Math.round(unlockedCount/ACHIEVEMENTS.length*100)}%</div>
        </div>
      )}
      <div className="grid grid-cols-2 gap-3">
        {filtered.map(a=>{
          const unlocked=sv.unlockedAchievements.includes(a.id);
          return(
            <div key={a.id} className="rounded-2xl p-3 text-center"
              style={{background:unlocked?`${theme.accent}18`:"#ffffff05",border:`1px solid ${unlocked?theme.accent+"55":"#ffffff10"}`,opacity:unlocked?1:0.4}}>
              <div className="text-2xl mb-1" style={{filter:unlocked?`drop-shadow(0 0 6px ${theme.accent})`:"none"}}>{a.icon}</div>
              <div className="text-xs font-bold mb-0.5" style={{color:unlocked?theme.accent:"#ccc"}}>{a.label}</div>
              <div className="text-xs opacity-50" style={{color:unlocked?theme.accent:"#888"}}>{a.desc}</div>
              {unlocked&&<div className="text-xs opacity-55 mt-0.5" style={{color:theme.accent}}>+{a.xp}xp</div>}
            </div>
          );
        })}
      </div>
    </div>
    );
  };

  // ── Leaderboard ──
  const renderLeaderboard=()=>{
    const todayKey=getTodayKey(),weekKey=getWeekKey();
    // Normalise scores — entries may be numbers (legacy) or {score,date,week}
    const normScores=(sv.scores||[]).map(e=>typeof e==="number"?{score:e,date:"2000-0-0",week:"2000-W0"}:e);
    const todayScores=normScores.filter(e=>e.date===todayKey).sort((a,b)=>b.score-a.score).slice(0,10);
    const weekScores=normScores.filter(e=>e.week===weekKey).sort((a,b)=>b.score-a.score).slice(0,10);
    const allScores=normScores.sort((a,b)=>b.score-a.score).slice(0,10);
    const activeScores=lbTab==="today"?todayScores:lbTab==="week"?weekScores:allScores;
    const infAll=[...(sv.infinityScores||[])].sort((a,b)=>b-a).slice(0,5);
    const hasInfinity=(sv.unlockedLevel||1)>100||(sv.infinityScores||[]).length>0;
    // Compute world-by-world star progress
    const worldStars=WORLDS.map(w=>{
      const lvls=Array.from({length:10},(_,i)=>w.id===10?91+i:(w.id-1)*10+i+1);
      const stars=lvls.reduce((s,l)=>s+(sv.levelStars?.[l]||0),0);
      return{world:w,stars,max:30,pct:Math.round((stars/30)*100)};
    });
    const totalStars=worldStars.reduce((s,w)=>s+w.stars,0);
    const achPct=Math.round(((sv.unlockedAchievements||[]).length/ACHIEVEMENTS.length)*100);
    return(
      <div className="flex flex-col h-full px-4 py-5 gap-4 overflow-y-auto relative z-10">
        <div className="flex items-center gap-3">
          <NeonButton onClick={()=>go("menu")} className="px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
          <h2 className="text-xl font-black" style={{color:theme.accent}}>Best Scores</h2>
          {(()=>{const pt=PRESTIGE_TIERS[Math.min(5,sv.prestigeLevel||0)];return pt?.badge?(
            <span style={{fontSize:12,color:pt.color,background:`${pt.color}18`,border:`1px solid ${pt.color}66`,borderRadius:8,padding:"2px 10px",fontWeight:"black",
              textShadow:`0 0 10px ${pt.color}88`,boxShadow:`0 0 8px ${pt.color}33`}}>
              {pt.badge} {pt.title} Ⅰ×{sv.prestigeLevel}
            </span>
          ):null;})()}
        </div>
        {/* ── PERSONAL STATS SUMMARY ── */}
        <div className="grid grid-cols-2 gap-2">
          <div className="p-3 rounded-2xl" style={{background:`${theme.accent}10`,border:`1px solid ${theme.accent}33`}}>
            <div className="text-xs opacity-50 uppercase tracking-widest" style={{color:theme.accent}}>High Score</div>
            <div className="text-xl font-black tabular-nums" style={{color:theme.accent}}>{(sv.highScore||0).toLocaleString()}</div>
          </div>
          <div className="p-3 rounded-2xl" style={{background:"#fbbf2410",border:"1px solid #fbbf2433"}}>
            <div className="text-xs opacity-50 uppercase tracking-widest" style={{color:"#fbbf24"}}>Best Streak</div>
            <div className="text-xl font-black tabular-nums" style={{color:"#fbbf24"}}>{sv.bestStreak||0}×</div>
          </div>
          <div className="p-3 rounded-2xl" style={{background:"#34d39910",border:"1px solid #34d39933"}}>
            <div className="text-xs opacity-50 uppercase tracking-widest" style={{color:"#34d399"}}>Total Coins</div>
            <div className="text-xl font-black tabular-nums" style={{color:"#34d399"}}>🪙 {(sv.totalCoins||0).toLocaleString()}</div>
          </div>
          <div className="p-3 rounded-2xl" style={{background:"#a78bfa10",border:"1px solid #a78bfa33"}}>
            <div className="text-xs opacity-50 uppercase tracking-widest" style={{color:"#a78bfa"}}>Stars Total</div>
            <div className="text-xl font-black tabular-nums" style={{color:"#a78bfa"}}>⭐ {totalStars}<span className="text-xs opacity-50">/300</span></div>
          </div>
        </div>
        {/* ── PRESTIGE COSMETICS PANEL — 12A ── */}
        {(sv.prestigeLevel||0)>0&&(()=>{
          const pt=PRESTIGE_TIERS[Math.min(5,sv.prestigeLevel||0)];
          const next=sv.prestigeLevel<5?PRESTIGE_TIERS[sv.prestigeLevel+1]:null;
          return(
            <div className="rounded-2xl p-4" style={{background:`linear-gradient(135deg,${pt.color}15 0%,#0a0218 80%)`,border:`1px solid ${pt.color}55`,boxShadow:`0 0 24px ${pt.color}22`}}>
              <div className="flex items-center gap-3 mb-2">
                <span style={{fontSize:28,filter:`drop-shadow(0 0 8px ${pt.color})`}}>{pt.badge}</span>
                <div>
                  <div className="font-black" style={{color:pt.color,letterSpacing:"0.06em",fontSize:13}}>{pt.title}</div>
                  <div className="text-xs opacity-60" style={{color:pt.color}}>{pt.desc}</div>
                </div>
              </div>
              {/* Prestige progress pips */}
              <div className="flex gap-2 mt-2">
                {PRESTIGE_TIERS.slice(1).map(t=>(
                  <div key={t.level} className="flex-1 h-2 rounded-full overflow-hidden" style={{background:"#ffffff10"}}>
                    <div style={{width:(sv.prestigeLevel||0)>=t.level?"100%":"0%",height:"100%",background:t.color,transition:"width 0.6s",boxShadow:`0 0 4px ${t.color}`}}/>
                  </div>
                ))}
              </div>
              {next&&<div className="text-xs mt-2 opacity-50" style={{color:pt.color}}>Next: {next.badge} {next.title} (Prestige {next.level})</div>}
              {!next&&<div className="text-xs mt-2 text-center font-black" style={{color:"#ffd700",textShadow:"0 0 10px #ffd700",animation:"floatGlow 2s ease-in-out infinite"}}>🌟 MAXIMUM PRESTIGE ACHIEVED 🌟</div>}
            </div>
          );
        })()}
        {/* ── WORLD PROGRESS RIBBON ── */}
        <div className="rounded-2xl p-3" style={{background:"#ffffff05",border:"1px solid #ffffff10"}}>
          <div className="text-xs font-bold opacity-50 mb-2 uppercase tracking-widest" style={{color:theme.accent}}>World Progress</div>
          <div className="space-y-1.5">
            {worldStars.map(w=>(
              <div key={w.world.id} className="flex items-center gap-2">
                <span style={{fontSize:16,minWidth:22}}>{w.world.emoji}</span>
                <div className="flex-1">
                  <div className="flex justify-between items-baseline">
                    <span style={{fontSize:10,color:w.world.color,fontWeight:"bold",letterSpacing:"0.04em"}}>{w.world.name}</span>
                    <span style={{fontSize:9,color:w.world.color,opacity:0.7}}>{w.stars}/30 ⭐</span>
                  </div>
                  <div style={{height:4,background:"#ffffff10",borderRadius:2,overflow:"hidden",marginTop:1}}>
                    <div style={{width:`${w.pct}%`,height:"100%",background:w.world.color,boxShadow:`0 0 4px ${w.world.color}`,transition:"width 0.5s"}}/>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
        {/* ── WORLD RECORDS HALL — 12B ── */}
        {Object.keys(sv.worldBest||{}).length>0&&(
          <div className="rounded-2xl p-3" style={{background:"#ffffff05",border:"1px solid #ffffff10"}}>
            <div className="flex items-center justify-between mb-2">
              <div className="text-xs font-bold opacity-50 uppercase tracking-widest" style={{color:theme.accent}}>🌍 World Records</div>
              <div className="text-xs opacity-40">{Object.keys(sv.worldBest||{}).length}/{WORLDS.length} worlds</div>
            </div>
            <div className="grid grid-cols-2 gap-1.5">
              {WORLDS.map(w=>{
                const best=(sv.worldBest||{})[w.id];
                if(!best)return null;
                const worldLvl=getLevelConfig(w.id*10);
                const ratio=best/(worldLvl.scoreGoal||1);
                const rank=ratio>=3?"S+":ratio>=2.2?"S":ratio>=1.5?"A":ratio>=1?"B":"C";
                const rankColor={"S+":"#ffd700","S":"#f97316","A":"#a78bfa","B":"#60a5fa","C":"#94a3b8"}[rank]||"#fff";
                return(
                  <div key={w.id} className="flex items-center gap-1.5 rounded-xl px-2 py-1.5"
                    style={{background:`${w.color}0e`,border:`1px solid ${w.color}33`}}>
                    <span style={{fontSize:14}}>{w.emoji}</span>
                    <div className="flex-1 min-w-0">
                      <div style={{fontSize:9,color:w.color,opacity:0.7,letterSpacing:"0.04em",fontWeight:"bold"}}>{w.name}</div>
                      <div style={{fontSize:12,color:w.color,fontWeight:"black",tabularNums:true}}>{best.toLocaleString()}</div>
                    </div>
                    <div style={{fontSize:13,fontWeight:"black",color:rankColor,
                      textShadow:`0 0 8px ${rankColor}88`,minWidth:22,textAlign:"center"}}>{rank}</div>
                  </div>
                );
              }).filter(Boolean)}
            </div>
            {Object.keys(sv.worldBest||{}).length===WORLDS.length&&(
              <div className="mt-2 text-center text-xs font-bold" style={{color:"#ffd700",
                textShadow:"0 0 12px #ffd70088"}}>🌟 All worlds conquered! 🌟</div>
            )}
          </div>
        )}
        {/* ── TIME-TABBED SCORES ── */}
        <div>
          {/* Tab row */}
          <div className="flex gap-1 mb-3 rounded-2xl p-1" style={{background:"#ffffff08"}}>
            {[["today","Today","📅"],["week","This Week","📆"],["all","All Time","🏆"]].map(([key,label,icon])=>(
              <button key={key} onClick={()=>setLbTab(key)}
                className="flex-1 text-xs font-bold py-2 rounded-xl transition-all"
                style={{
                  background:lbTab===key?`${theme.accent}28`:"transparent",
                  border:`1px solid ${lbTab===key?theme.accent+"88":"transparent"}`,
                  color:lbTab===key?theme.accent:"#888",
                  outline:"none",letterSpacing:"0.03em",
                }}>
                {icon} {label}
              </button>
            ))}
          </div>
          {/* Score list for active tab */}
          {activeScores.length===0
            ?<p className="text-center opacity-40 text-sm py-4" style={{color:theme.accent}}>
                {lbTab==="today"?"No runs today yet!":lbTab==="week"?"No runs this week yet!":"No scores yet!"}
              </p>
            :activeScores.map((e,i)=>(
              <div key={i} className="flex items-center justify-between px-4 py-3 mb-1.5 rounded-2xl"
                style={{background:i===0?`${theme.accent}18`:"#ffffff05",border:`1px solid ${i<3?theme.accent+"44":"#ffffff0d"}`}}>
                <span className="font-black text-xl" style={{color:i===0?"#fbbf24":i===1?"#d1d5db":i===2?"#d97706":theme.accent,minWidth:32}}>
                  {i===0?"🥇":i===1?"🥈":i===2?"🥉":`#${i+1}`}
                </span>
                <span className="font-bold text-xl tabular-nums" style={{color:theme.accent}}>{(e.score||e).toLocaleString()}</span>
                {lbTab!=="all"&&<span style={{fontSize:9,color:"#ffffff33"}}>{e.date||""}</span>}
              </div>
            ))
          }
        </div>
        {hasInfinity&&<>
          <div className="text-xs font-bold opacity-40 uppercase tracking-widest mt-2" style={{color:"#a78bfa"}}>∞ Infinity Mode</div>
          {infAll.length===0
            ?<p className="text-center opacity-40 text-sm" style={{color:"#a78bfa"}}>No infinity runs yet</p>
            :infAll.map((s,i)=>(
              <div key={i} className="flex items-center justify-between px-4 py-3 rounded-2xl"
                style={{background:i===0?"#a78bfa18":"#ffffff05",border:`1px solid ${i===0?"#a78bfa44":"#ffffff0d"}`}}>
                <span className="font-black" style={{color:"#a78bfa",minWidth:32}}>{i===0?"∞🥇":`∞#${i+1}`}</span>
                <span className="font-bold tabular-nums" style={{color:"#a78bfa"}}>{s.toLocaleString()}</span>
              </div>
            ))
          }
        </>}
        {/* ── COMPLETION CARDS ── */}
        <div className="grid grid-cols-2 gap-2 mt-2">
          <div className="p-3 rounded-2xl text-center" style={{background:"#ffffff06",border:`1px solid ${theme.accent}22`}}>
            <div className="text-xs opacity-40 mb-1" style={{color:theme.accent}}>Levels Unlocked</div>
            <div className="text-2xl font-black" style={{color:theme.accent}}>{Math.min(100,sv.unlockedLevel||1)}<span className="text-sm opacity-50">/100</span></div>
          </div>
          <div className="p-3 rounded-2xl text-center" style={{background:"#ffffff06",border:"1px solid #fbbf2422"}}>
            <div className="text-xs opacity-40 mb-1" style={{color:"#fbbf24"}}>Achievements</div>
            <div className="text-2xl font-black" style={{color:"#fbbf24"}}>{(sv.unlockedAchievements||[]).length}<span className="text-sm opacity-50">/{ACHIEVEMENTS.length}</span></div>
            <div className="text-xs opacity-50 mt-0.5" style={{color:"#fbbf24"}}>{achPct}% complete</div>
          </div>
        </div>
      </div>
    );
  };

  // ── Mascot Collection ──
  const renderMascotCollection=()=>{
    const sv2=saveRef.current;
    const unlocked=sv2.unlockedMascots||["dragon","fox"];
    const RARITY_STYLE={
      starter:{border:"#60a5fa",bg:"#60a5fa",label:"STARTER"},
      common: {border:"#4ecb71",bg:"#4ecb71",label:"COMMON"},
      rare:   {border:"#6ec0f5",bg:"#6ec0f5",label:"RARE"},
      epic:   {border:"#b06de8",bg:"#b06de8",label:"EPIC"},
      legendary:{border:"#ffd700",bg:"#ffd700",label:"LEGENDARY"},
    };
    return(
      <div className="flex flex-col h-full bg-transparent relative z-10">
        {/* Header */}
        <div className="flex items-center gap-3 px-4 pt-5 pb-3" style={{flexShrink:0}}>
          <NeonButton onClick={()=>go("settings")} className="px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
          <div>
            <h2 className="text-xl font-black" style={{color:theme.accent}}>🐾 Mascot Collection</h2>
            <p className="text-xs opacity-50" style={{color:"#fff"}}>{unlocked.length} / {MASCOTS.length} unlocked</p>
          </div>
        </div>
        {/* Grid */}
        <div className="overflow-y-auto flex-1 px-3 pb-6" style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:12,alignContent:"start"}}>
          {MASCOTS.map(m=>{
            const isUnlocked=unlocked.includes(m.id);
            const isActive=sv2.mascotId===m.id;
            const rs=RARITY_STYLE[m.rarity]||RARITY_STYLE.common;
            const newlyUnlocked=isUnlocked&&!isActive;
            return(
              <button key={m.id} disabled={!isUnlocked}
                onClick={()=>{if(isUnlocked){sv2.mascotId=m.id;debounceSave();go("mascotcollection");}}}
                style={{
                  position:"relative",display:"flex",flexDirection:"column",alignItems:"center",
                  gap:6,padding:"18px 10px 14px",borderRadius:20,
                  border:`2px solid ${isActive?rs.border:isUnlocked?"#ffffff22":"#ffffff10"}`,
                  background:isActive?`${rs.bg}18`:isUnlocked?"#ffffff08":"#00000040",
                  boxShadow:isActive?`0 0 24px ${rs.border}55`:"none",
                  cursor:isUnlocked?"pointer":"default",
                  WebkitTapHighlightColor:"transparent",transition:"all 0.18s",
                  opacity:isUnlocked?1:0.55}}>
                {/* Rarity top stripe */}
                <div style={{
                  position:"absolute",top:0,left:0,right:0,height:4,
                  borderRadius:"18px 18px 0 0",
                  background:isUnlocked?`linear-gradient(90deg,${rs.border},${rs.border}88,${rs.border})`:"#333"}}/>
                {/* Rarity badge */}
                <div style={{
                  position:"absolute",top:8,right:8,
                  background:isUnlocked?rs.bg:"#555",
                  color:isUnlocked?"#000":"#888",
                  fontSize:7,fontWeight:"black",padding:"2px 6px",borderRadius:6,
                  letterSpacing:"0.06em"}}>
                  {rs.label}
                </div>
                {/* ACTIVE badge */}
                {isActive&&(
                  <div style={{
                    position:"absolute",top:8,left:8,
                    background:"#ffd700",color:"#000",
                    fontSize:7,fontWeight:"black",padding:"2px 6px",borderRadius:6,
                    letterSpacing:"0.04em",boxShadow:"0 0 8px #ffd70066"}}>
                    ✓ ACTIVE
                  </div>
                )}
                {/* Mascot emoji */}
                <div style={{position:"relative",marginTop:8}}>
                  {isUnlocked?(
                    <span style={{
                      fontSize:52,lineHeight:1,display:"block",
                      filter:`drop-shadow(0 0 ${isActive?16:6}px ${rs.border})`,
                      animation:isActive?`${m.dance} 0.9s ease-in-out infinite`:"mascotIdle 2.8s ease-in-out infinite"}}>
                      {m.e.idle}
                    </span>
                  ):(
                    <div style={{
                      width:52,height:52,borderRadius:"50%",background:"#222",
                      display:"flex",alignItems:"center",justifyContent:"center",
                      border:"2px solid #ffffff15",fontSize:26}}>
                      🔒
                    </div>
                  )}
                </div>
                {/* Name */}
                <div style={{
                  fontWeight:"black",fontSize:13,letterSpacing:"0.04em",
                  color:isActive?rs.border:isUnlocked?"#fff":"#555"}}>
                  {m.name}
                </div>
                {/* Catchphrase or unlock hint */}
                <div style={{
                  fontSize:9,textAlign:"center",lineHeight:1.35,
                  color:isUnlocked?"#ffffff66":"#ffffff33",maxWidth:110}}>
                  {isUnlocked?m.catchphrase:`🔒 ${m.unlockHint||"Coming soon"}`}
                </div>
                {/* Mascot XP bar */}
                {isUnlocked&&(()=>{
                  const mxp=(sv2.mascotXP||{})[m.id]||0;
                  const mlvl=Math.min(20,Math.floor(mxp/500));
                  const pct=mlvl>=20?100:((mxp%500)/500)*100;
                  return(
                    <div style={{width:"100%",marginTop:2}}>
                      <div style={{display:"flex",justifyContent:"space-between",fontSize:8,opacity:0.6,marginBottom:2,color:rs.border}}>
                        <span>LV {mlvl}</span><span>{mlvl>=20?"MAX":Math.floor(pct)+"%"}</span>
                      </div>
                      <div style={{width:"100%",height:4,borderRadius:2,background:"#ffffff18"}}>
                        <div style={{width:`${pct}%`,height:"100%",borderRadius:2,background:rs.border,boxShadow:`0 0 4px ${rs.border}`}}/>
                      </div>
                      {mlvl>=5&&<div style={{fontSize:7,marginTop:2,color:rs.border,opacity:0.8}}>
                        {[mlvl>=5?"💰+5%coin":null,mlvl>=10?"🛡️shield":null,mlvl>=15?"⭐+100pts":null,mlvl>=20?"👑legendary":null].filter(Boolean).join(" ")}
                      </div>}
                    </div>
                  );
                })()}
                {/* Personal best with this mascot */}
                {isUnlocked&&(()=>{
                  const mBest=(sv2.mascotScores||{})[m.id]||0;
                  return mBest>0?(
                    <div style={{fontSize:8,color:rs.border,opacity:0.75,letterSpacing:"0.03em"}}>
                      🏆 {mBest.toLocaleString()} pts
                    </div>
                  ):null;
                })()}
                {/* SELECT button for unlocked-but-not-active */}
                {isUnlocked&&!isActive&&(
                  <div style={{
                    marginTop:4,padding:"4px 14px",borderRadius:10,
                    background:`${rs.bg}22`,border:`1px solid ${rs.border}66`,
                    fontSize:10,fontWeight:"bold",color:rs.border,letterSpacing:"0.05em"}}>
                    SELECT
                  </div>
                )}
              </button>
            );
          })}
        </div>
        {/* ── Accessory shop for active mascot ── */}
        {(()=>{
          const activeId=sv2.mascotId||"dragon";
          const activeM=MASCOTS.find(m=>m.id===activeId)||MASCOTS[0];
          const owned=sv2.ownedAccessories||{};
          const equipped=(sv2.mascotAccessories||{})[activeId];
          return(
            <div style={{padding:"12px 16px 24px",borderTop:"1px solid #ffffff15",flexShrink:0}}>
              <div className="flex items-center justify-between mb-3">
                <div>
                  <div className="text-sm font-black" style={{color:activeM.color,letterSpacing:"0.05em"}}>
                    🎩 {activeM.name}'s Accessories
                  </div>
                  <div className="text-xs opacity-50" style={{color:"#fff"}}>Coins: 🪙 {sv2.coins||0}</div>
                </div>
                {equipped&&(
                  <button onClick={()=>{
                    const ma={...(sv2.mascotAccessories||{})};delete ma[activeId];sv2.mascotAccessories=ma;
                    flushSave();setNotif("Accessory removed");setTimeout(()=>setNotif(null),1500);
                  }} className="text-xs px-3 py-1 rounded-lg font-bold"
                    style={{background:"#ffffff10",color:"#ffffff90",border:"1px solid #ffffff20"}}>
                    Remove
                  </button>
                )}
              </div>
              <div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:8}}>
                {MASCOT_ACCESSORIES.map(a=>{
                  const ownKey=`${activeId}:${a.id}`;
                  const isOwned=!!owned[ownKey];
                  const isEquipped=equipped===a.id;
                  const canBuy=(sv2.coins||0)>=a.cost;
                  return(
                    <button key={a.id} onClick={()=>{
                      if(isOwned){
                        // toggle equip
                        sv2.mascotAccessories={...(sv2.mascotAccessories||{}),[activeId]:isEquipped?null:a.id};
                        if(isEquipped){delete sv2.mascotAccessories[activeId];}
                        flushSave();sfx("tap");
                      }else if(canBuy){
                        sv2.coins-=a.cost;
                        sv2.ownedAccessories={...(sv2.ownedAccessories||{}),[ownKey]:true};
                        sv2.mascotAccessories={...(sv2.mascotAccessories||{}),[activeId]:a.id};
                        flushSave();sfx("coin");vibrate([20]);
                        setNotif(`✨ ${a.name} equipped!`);setTimeout(()=>setNotif(null),1800);
                      }
                    }} disabled={!isOwned&&!canBuy}
                      style={{
                        position:"relative",padding:"10px 4px 8px",borderRadius:14,
                        border:`2px solid ${isEquipped?"#ffd700":isOwned?activeM.color+"66":canBuy?"#ffffff22":"#ffffff08"}`,
                        background:isEquipped?"#ffd70015":isOwned?activeM.color+"10":canBuy?"#ffffff05":"#00000040",
                        cursor:(isOwned||canBuy)?"pointer":"not-allowed",
                        opacity:(!isOwned&&!canBuy)?0.5:1,
                        boxShadow:isEquipped?`0 0 12px #ffd70055`:"none",
                        WebkitTapHighlightColor:"transparent"
                      }}>
                      {isEquipped&&<div style={{position:"absolute",top:4,right:4,fontSize:9,padding:"1px 5px",background:"#ffd700",color:"#000",borderRadius:5,fontWeight:"black"}}>ON</div>}
                      <div style={{fontSize:30,lineHeight:1,marginBottom:4}}>{a.emoji}</div>
                      <div style={{fontSize:10,fontWeight:"bold",color:"#fff",lineHeight:1.1}}>{a.name}</div>
                      <div style={{fontSize:9,marginTop:3,color:isOwned?"#34d399":canBuy?"#fbbf24":"#ffffff40",fontWeight:"bold"}}>
                        {isOwned?(isEquipped?"✓ Equipped":"Tap to wear"):`🪙 ${a.cost}`}
                      </div>
                    </button>
                  );
                })}
              </div>
            </div>
          );
        })()}
      </div>
    );
  };

  // ── Settings ──
  const renderSettings=()=>{
    const SPEED_MODES=[{v:0.7,label:"Easy 🐢",desc:"Slower, longer"},{v:1.0,label:"Normal ⚖️",desc:"Standard"},{v:1.3,label:"Hard ⚡",desc:"Faster, shorter"},{v:1.6,label:"Expert 🔥",desc:"Maximum speed"}];
    return(
    <div className="flex flex-col h-full px-4 py-5 gap-5 overflow-y-auto relative z-10">
      <div className="flex items-center gap-3">
        <NeonButton onClick={()=>go("menu")} className="px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
        <h2 className="text-xl font-black" style={{color:theme.accent}}>Settings</h2>
      </div>
      {/* Sound on/off */}
      <div>
        <p className="text-xs font-bold opacity-40 mb-2 uppercase tracking-widest" style={{color:theme.accent}}>Sound</p>
        <NeonButton onClick={()=>{const n=!soundOn;setSoundOn(n);sv.soundEnabled=n;debounceSave();}}
          className="px-6 py-3"
          style={{background:soundOn?`${theme.accent}22`:"#ffffff0a",border:`1px solid ${soundOn?theme.accent:"#ffffff22"}`,color:soundOn?theme.accent:"#888"}}>
          {soundOn?"🔊  Sound ON":"🔇  Sound OFF"}
        </NeonButton>
      </div>
      {/* Volume sliders */}
      {soundOn&&<div>
        <p className="text-xs font-bold opacity-40 mb-3 uppercase tracking-widest" style={{color:theme.accent}}>Volume</p>
        <div className="flex flex-col gap-3">
          <div className="flex items-center gap-3">
            <span className="text-xs w-14" style={{color:theme.accent}}>🎵 Music</span>
            <input type="range" min={0} max={100} value={sv.musicVol??80}
              onChange={e=>{const v=parseInt(e.target.value);sv.musicVol=v;debounceSave();bgMusicCtl?.setVol?.(v*0.00032);}}
              style={{flex:1,accentColor:theme.accent}}/>
            <span className="text-xs w-8 text-right opacity-50" style={{color:theme.accent}}>{sv.musicVol??80}</span>
          </div>
          <div className="flex items-center gap-3">
            <span className="text-xs w-14" style={{color:theme.accent}}>🔔 SFX</span>
            <input type="range" min={0} max={100} value={sv.sfxVol??80}
              onChange={e=>{const v=parseInt(e.target.value);sv.sfxVol=v;debounceSave();}}
              style={{flex:1,accentColor:theme.accent}}/>
            <span className="text-xs w-8 text-right opacity-50" style={{color:theme.accent}}>{sv.sfxVol??80}</span>
          </div>
        </div>
      </div>}
      {/* Haptic toggle */}
      <div>
        <p className="text-xs font-bold opacity-40 mb-2 uppercase tracking-widest" style={{color:theme.accent}}>Haptic Feedback</p>
        <NeonButton onClick={()=>{const n=sv.hapticEnabled===false;sv.hapticEnabled=n;_hapticOn=n;debounceSave();}}
          className="px-6 py-3"
          style={{background:(sv.hapticEnabled!==false)?`${theme.accent}22`:"#ffffff0a",
            border:`1px solid ${(sv.hapticEnabled!==false)?theme.accent:"#ffffff22"}`,
            color:(sv.hapticEnabled!==false)?theme.accent:"#888"}}>
          {(sv.hapticEnabled!==false)?"📳  Vibration ON":"📴  Vibration OFF"}
        </NeonButton>
      </div>
      {/* Haptic intensity slider */}
      {(sv.hapticEnabled!==false)&&<div>
        <p className="text-xs font-bold opacity-40 mb-3 uppercase tracking-widest" style={{color:theme.accent}}>Haptic Intensity</p>
        <div className="flex items-center gap-3">
          <span className="text-xs" style={{color:theme.accent}}>📳</span>
          <input type="range" min={10} max={100} step={10} value={sv.hapticIntensity??70}
            onChange={e=>{const v=parseInt(e.target.value);sv.hapticIntensity=v;_hapticIntensity=v;debounceSave();vibrate([v]);}}
            style={{flex:1,accentColor:theme.accent}}/>
          <span className="text-xs w-10 text-right opacity-50" style={{color:theme.accent}}>{sv.hapticIntensity??70}%</span>
        </div>
        <p className="text-xs opacity-30 mt-1" style={{color:"#fff"}}>Higher = stronger vibrations</p>
      </div>}
      {/* Colorblind mode */}
      <div>
        <p className="text-xs font-bold opacity-40 mb-2 uppercase tracking-widest" style={{color:theme.accent}}>Accessibility</p>
        <NeonButton onClick={()=>{const n=!sv.colorblindMode;sv.colorblindMode=n;_colorblindOn=n;debounceSave();}}
          className="px-6 py-3"
          style={{background:sv.colorblindMode?`${theme.accent}22`:"#ffffff0a",
            border:`1px solid ${sv.colorblindMode?theme.accent:"#ffffff22"}`,
            color:sv.colorblindMode?theme.accent:"#888"}}>
          {sv.colorblindMode?"♛  Colorblind Symbols ON":"♛  Colorblind Symbols OFF"}
        </NeonButton>
      </div>
      {/* Adaptive difficulty */}
      <div>
        <p className="text-xs font-bold opacity-40 mb-2 uppercase tracking-widest" style={{color:theme.accent}}>Adaptive Difficulty</p>
        <NeonButton onClick={()=>{sv.adaptiveDifficulty=!sv.adaptiveDifficulty;debounceSave();}}
          className="px-6 py-3"
          style={{background:sv.adaptiveDifficulty?`${theme.accent}22`:"#ffffff0a",
            border:`1px solid ${sv.adaptiveDifficulty?theme.accent:"#ffffff22"}`,
            color:sv.adaptiveDifficulty?theme.accent:"#888"}}>
          {sv.adaptiveDifficulty?"🎯  Auto-Adjust ON  — speed adapts to your skill":"🎯  Auto-Adjust OFF — fixed speed mode"}
        </NeonButton>
      </div>
      {/* Speed mode */}
      <div>
        <p className="text-xs font-bold opacity-40 mb-2 uppercase tracking-widest" style={{color:theme.accent}}>Game Speed</p>
        <div className="flex flex-col gap-2">
          {SPEED_MODES.map(sm=>(
            <NeonButton key={sm.v} onClick={()=>{sv.speedMode=sm.v;debounceSave();}}
              className="flex items-center justify-between px-4 py-3 rounded-2xl"
              style={{background:(sv.speedMode||1)===sm.v?`${theme.accent}20`:"#ffffff06",
                border:`1px solid ${(sv.speedMode||1)===sm.v?theme.accent:"#ffffff10"}`}}>
              <div className="flex flex-col items-start">
                <span style={{color:(sv.speedMode||1)===sm.v?theme.accent:"#aaa",fontWeight:"bold"}}>{sm.label}</span>
                <span style={{fontSize:10,color:"#ffffff44"}}>{sm.desc}</span>
              </div>
              {(sv.speedMode||1)===sm.v&&<span style={{color:theme.accent}}>✓</span>}
            </NeonButton>
          ))}
        </div>
      </div>
      <div>
        <div className="flex justify-between items-baseline mb-2">
          <p className="text-xs font-bold opacity-40 uppercase tracking-widest" style={{color:theme.accent}}>Theme</p>
          <p className="text-xs opacity-50" style={{color:theme.accent}}>Your Level: <span style={{color:"#fff",fontWeight:"bold"}}>Lv {lvl}</span></p>
        </div>
        <div className="flex flex-col gap-2">
          {THEMES.map(th=>{
            const locked=lvl<th.unlockLevel;
            const levelsAway=th.unlockLevel-lvl;
            return(
              <NeonButton key={th.id} disabled={locked}
                onClick={()=>{setTheme(th);sv.themeId=th.id;debounceSave();}}
                className="flex items-center justify-between px-4 py-3 rounded-2xl"
                style={{background:theme.id===th.id?`${th.accent}20`:"#ffffff06",border:`1px solid ${theme.id===th.id?th.accent:"#ffffff10"}`,opacity:locked?0.55:1}}>
                <div className="flex items-center gap-3 flex-1 min-w-0">
                  <div className="w-4 h-4 rounded-full flex-shrink-0" style={{background:locked?"#444":th.accent,boxShadow:locked?"none":`0 0 8px ${th.accent}`}}/>
                  <div className="flex-1 min-w-0">
                    <div style={{color:locked?"#666":th.accent,fontWeight:"bold"}}>{th.name}</div>
                    {locked&&(
                      <div className="text-xs opacity-60 mt-0.5" style={{color:"#fff"}}>
                        Unlocks at Lv {th.unlockLevel} {levelsAway>0?`· ${levelsAway} to go`:""}
                      </div>
                    )}
                  </div>
                </div>
                {locked?(
                  <span className="text-xs flex items-center gap-1" style={{color:"#888"}}>
                    🔒 Lv{th.unlockLevel}
                  </span>
                ):theme.id===th.id?<span style={{color:th.accent,fontSize:18}}>✓</span>:<span className="text-xs opacity-40" style={{color:th.accent}}>tap to use</span>}
              </NeonButton>
            );
          })}
        </div>
      </div>
      {/* ── Mascot Chooser ── */}
      <div>
        <p className="text-xs font-bold opacity-40 mb-3 uppercase tracking-widest" style={{color:theme.accent}}>Your Companion</p>
        {/* Active mascot preview */}
        <NeonButton onClick={()=>go("mascotcollection")}
          className="w-full flex items-center gap-4 px-4 py-3 rounded-2xl"
          style={{background:`${currentMascot.color}12`,border:`2px solid ${currentMascot.color}44`,
            boxShadow:`0 0 20px ${currentMascot.color}22`}}>
          <span style={{fontSize:46,lineHeight:1,
            filter:`drop-shadow(0 0 12px ${currentMascot.color})`,
            animation:`${currentMascot.dance} 0.9s ease-in-out infinite`}}>
            {currentMascot.e.idle}
          </span>
          <div className="flex flex-col items-start flex-1">
            <span className="font-black text-base" style={{color:currentMascot.color}}>{currentMascot.name}</span>
            <span className="text-xs opacity-60" style={{color:"#fff"}}>
              {({starter:"Starter",common:"Common",rare:"Rare ✦",epic:"Epic ✦✦",legendary:"Legendary ✦✦✦"}[currentMascot.rarity]||"")}
            </span>
            <span className="text-xs mt-1" style={{color:"#ffffff55",fontStyle:"italic"}}>"{currentMascot.catchphrase}"</span>
          </div>
          <div style={{color:`${currentMascot.color}88`,fontSize:12,fontWeight:"bold"}}>
            {(sv.unlockedMascots||["dragon","fox"]).length}/{MASCOTS.length} collected →
          </div>
        </NeonButton>
      </div>
      {/* Data management */}
      <div className="border-t border-white border-opacity-10 pt-4 flex flex-col gap-2">
        <p className="text-xs font-bold opacity-40 uppercase tracking-widest mb-1" style={{color:theme.accent}}>Data</p>
        <NeonButton onClick={()=>{
          const data=JSON.stringify(saveRef.current,null,2);
          const blob=new Blob([data],{type:"application/json"});
          const url=URL.createObjectURL(blob);
          const a=document.createElement("a");a.href=url;a.download="nexustap_save.json";a.click();
          URL.revokeObjectURL(url);
          setNotif("💾 Save exported!");
        }} className="w-full py-3 text-sm" style={{background:"#3b82f618",border:"1px solid #3b82f655",color:"#60a5fa"}}>
          💾 Export Save Data
        </NeonButton>
        <NeonButton onClick={()=>{
          const inp=document.createElement("input");inp.type="file";inp.accept=".json";
          inp.onchange=e=>{
            const file=e.target.files[0];if(!file)return;
            const reader=new FileReader();
            reader.onload=ev=>{
              try{
                const imported=JSON.parse(ev.target.result);
                if(imported.highScore!==undefined&&imported.coins!==undefined){
                  Object.assign(saveRef.current,imported);flushSave();
                  setNotif("✅ Save imported!");go("menu");
                } else setNotif("❌ Invalid save file");
              } catch{setNotif("❌ Could not read file");}
            };reader.readAsText(file);
          };inp.click();
        }} className="w-full py-3 text-sm" style={{background:"#8b5cf618",border:"1px solid #8b5cf655",color:"#a78bfa"}}>
          📂 Import Save Data
        </NeonButton>
        <NeonButton onClick={()=>{if(window.confirm("Reset ALL progress? Cannot be undone.")){saveRef.current={...DEFAULT_SAVE};flushSave();setTheme(THEMES[0]);setSoundOn(true);go("menu");}}}
          className="w-full py-3 text-sm" style={{background:"#ef444418",border:"1px solid #ef444455",color:"#ef4444"}}>
          Reset All Progress
        </NeonButton>
      </div>
    </div>
  );};

  // ── Infinity Mode Screen ──
  const renderInfinity=()=>{
    const infBest=sv.infinityBest||0;
    const infRound=infinityRound;
    const cfg=getInfinityLevelConfig(infRound);
    return(
      <div className="flex flex-col h-full px-5 py-6 gap-5 relative z-10 items-center justify-center">
        <NeonButton onClick={()=>go("menu")} className="absolute top-4 left-4 px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
        <div className="text-center">
          <div className="text-5xl mb-2" style={{animation:"floatGlow 2s ease-in-out infinite",filter:"drop-shadow(0 0 20px #a78bfa)"}}>∞</div>
          <h2 className="font-black text-3xl" style={{color:"#a78bfa",textShadow:"0 0 30px #a78bfaaa",fontFamily:"'Rajdhani',sans-serif",letterSpacing:"0.1em"}}>INFINITY MODE</h2>
          <p className="text-sm opacity-60 mt-1" style={{color:"#a78bfa"}}>No end. No mercy. Pure reflex.</p>
        </div>
        {infBest>0&&(
          <div className="px-6 py-3 rounded-2xl text-center" style={{background:"#a78bfa18",border:"1px solid #a78bfa44"}}>
            <div className="text-xs opacity-50 mb-1" style={{color:"#a78bfa"}}>Personal Best</div>
            <div className="text-2xl font-black" style={{color:"#a78bfa"}}>{infBest.toLocaleString()}</div>
          </div>
        )}
        <div className="w-full rounded-2xl p-4" style={{background:"#ffffff08",border:"1px solid #a78bfa33"}}>
          <div className="text-xs opacity-40 mb-2 uppercase tracking-widest" style={{color:"#a78bfa"}}>Starting Round</div>
          <div className="flex gap-2 justify-center">
            {[1,5,10,20].map(r=>(
              <NeonButton key={r} onClick={()=>setInfinityRound(r)}
                className="flex-1 py-3 text-sm font-black"
                style={{background:infRound===r?"#a78bfa33":"#ffffff08",border:`1px solid ${infRound===r?"#a78bfa":"#ffffff15"}`,color:infRound===r?"#a78bfa":"#888"}}>
                R{r}
              </NeonButton>
            ))}
          </div>
          <div className="text-xs text-center mt-2 opacity-40" style={{color:"#a78bfa"}}>Goal: {cfg.scoreGoal.toLocaleString()} pts · {cfg.lives} lives</div>
        </div>
        <NeonButton onClick={()=>{
          const infCfg=getInfinityLevelConfig(infRound);
          startGame(0,[],infCfg);
        }} className="w-full py-5 text-xl font-black"
          style={{background:"linear-gradient(135deg,#6d28d9,#a78bfa)",boxShadow:"0 0 40px #a78bfa66",letterSpacing:"0.1em"}}>
          ∞ DIVE IN — Round {infRound}
        </NeonButton>
        <p className="text-xs text-center opacity-30" style={{color:"#a78bfa"}}>2× coin rewards · Levels cycle through all 10 worlds</p>
      </div>
    );
  };

  // ── Zen Mode Screen ──
  const renderZen=()=>{

    const zenBest=sv.zenBest||0;
    const worldCfg=getZenConfig(zenWorld);
    return(
      <div className="flex flex-col h-full px-5 py-6 gap-5 relative z-10 items-center justify-center">
        <NeonButton onClick={()=>go("menu")} className="absolute top-4 left-4 px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
        <div className="text-center">
          <div className="text-5xl mb-2" style={{animation:"floatGlow 2.5s ease-in-out infinite",filter:"drop-shadow(0 0 20px #34d399)"}}>☯</div>
          <h2 className="font-black text-3xl" style={{color:"#34d399",textShadow:"0 0 30px #34d399aa",fontFamily:"'Rajdhani',sans-serif",letterSpacing:"0.1em"}}>ZEN MODE</h2>
          <p className="text-sm opacity-60 mt-1" style={{color:"#34d399"}}>90 seconds. No bombs. Pure flow.</p>
        </div>
        {zenBest>0&&(
          <div className="px-6 py-3 rounded-2xl text-center" style={{background:"#34d39918",border:"1px solid #34d39944"}}>
            <div className="text-xs opacity-50 mb-1" style={{color:"#34d399"}}>Personal Best</div>
            <div className="text-2xl font-black" style={{color:"#34d399"}}>{zenBest.toLocaleString()}</div>
            <div className="text-xs opacity-40" style={{color:"#34d399"}}>World {sv.zenBestWorld}: {WORLDS[(sv.zenBestWorld||1)-1].name}</div>
          </div>
        )}
        {/* World selector */}
        <div className="w-full rounded-2xl p-3" style={{background:"#ffffff08",border:"1px solid #34d39933"}}>
          <div className="text-xs opacity-40 mb-2 uppercase tracking-widest text-center" style={{color:"#34d399"}}>Choose World</div>
          <div className="grid grid-cols-5 gap-1.5">
            {WORLDS.map(w=>(
              <NeonButton key={w.id} onClick={()=>setZenWorld(w.id)}
                className="py-2 text-center text-lg rounded-xl"
                style={{background:zenWorld===w.id?`${w.color}33`:"#ffffff06",
                  border:`1px solid ${zenWorld===w.id?w.color:"#ffffff10"}`,
                  boxShadow:zenWorld===w.id?`0 0 8px ${w.color}66`:"none"}}>
                {w.emoji}
              </NeonButton>
            ))}
          </div>
          <div className="text-center mt-2 text-xs font-bold" style={{color:WORLDS[zenWorld-1].color}}>{WORLDS[zenWorld-1].name}</div>
        </div>
        <NeonButton onClick={()=>startGame(0,[],worldCfg)}
          className="w-full py-5 text-xl font-black"
          style={{background:"linear-gradient(135deg,#059669,#34d399)",boxShadow:"0 0 40px #34d39966",letterSpacing:"0.1em"}}>
          ☯ BEGIN ZEN
        </NeonButton>
        <p className="text-xs text-center opacity-30" style={{color:"#34d399"}}>Unlimited lives · No bombs · 90 second countdown</p>
      </div>
    );
  };

  // ── Time Attack Mode ──
  const renderTimeAttack=()=>{
    const taBest=sv.taBest||0;
    const worldCfg=getTimeAttackConfig(taWorld);
    const taWorld_=WORLDS[taWorld-1];
    return(
      <div className="flex flex-col h-full px-5 py-6 gap-5 relative z-10 items-center justify-center">
        <NeonButton onClick={()=>go("menu")} className="absolute top-4 left-4 px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
        <div className="text-center">
          <div className="text-5xl mb-2" style={{animation:"heartbeat 0.8s ease-in-out infinite",filter:"drop-shadow(0 0 20px #f97316)"}}>⚡</div>
          <h2 className="font-black text-3xl" style={{color:"#f97316",textShadow:"0 0 30px #f97316aa",fontFamily:"'Rajdhani',sans-serif",letterSpacing:"0.1em"}}>TIME ATTACK</h2>
          <p className="text-sm opacity-60 mt-1" style={{color:"#f97316"}}>45 seconds. Max score wins. No mercy.</p>
        </div>
        {taBest>0&&(
          <div className="px-6 py-3 rounded-2xl text-center" style={{background:"#f9731618",border:"1px solid #f9731644"}}>
            <div className="text-xs opacity-50 mb-1" style={{color:"#f97316"}}>Personal Best</div>
            <div className="text-2xl font-black" style={{color:"#f97316"}}>{taBest.toLocaleString()}</div>
            {sv.taBestWorld&&<div className="text-xs opacity-40" style={{color:"#f97316"}}>World {sv.taBestWorld}: {WORLDS[(sv.taBestWorld||1)-1].name}</div>}
          </div>
        )}
        {/* World selector */}
        <div className="w-full rounded-2xl p-3" style={{background:"#ffffff08",border:"1px solid #f9731633"}}>
          <div className="text-xs opacity-40 mb-2 uppercase tracking-widest text-center" style={{color:"#f97316"}}>Choose World</div>
          <div className="grid grid-cols-5 gap-1.5">
            {WORLDS.map(w=>(
              <NeonButton key={w.id} onClick={()=>setTaWorld(w.id)}
                className="py-2 text-center text-lg rounded-xl"
                style={{background:taWorld===w.id?`${w.color}33`:"#ffffff06",
                  border:`1px solid ${taWorld===w.id?w.color:"#ffffff10"}`,
                  boxShadow:taWorld===w.id?`0 0 8px ${w.color}66`:"none"}}>
                {w.emoji}
              </NeonButton>
            ))}
          </div>
          <div className="text-center mt-2 text-xs font-bold" style={{color:taWorld_.color}}>{taWorld_.name}</div>
        </div>
        {/* Rules */}
        <div className="w-full rounded-xl px-4 py-3 text-xs" style={{background:"#ffffff06",border:"1px solid #f9731622",color:"#aaa",lineHeight:1.7}}>
          ⏱ <strong style={{color:"#f97316"}}>45 seconds</strong> — tap everything you can<br/>
          💣 <strong style={{color:"#ef4444"}}>Bombs</strong> are present — watch out!<br/>
          🌈 <strong style={{color:"#a78bfa"}}>All rarities</strong> spawn rapidly — chain them!<br/>
          🏆 Score is saved to your personal best
        </div>
        <NeonButton onClick={()=>startGame(0,[],worldCfg)}
          className="w-full py-5 text-xl font-black"
          style={{background:"linear-gradient(135deg,#ea580c,#f97316)",boxShadow:"0 0 40px #f9731666",letterSpacing:"0.1em"}}>
          ⚡ START ATTACK!
        </NeonButton>
        <p className="text-xs text-center opacity-30" style={{color:"#f97316"}}>3 lives · Bombs active · Ultra-fast spawn rate</p>
      </div>
    );
  };

  // ── Weekly Challenge Screen ──
  const renderWeekly=()=>{
    const wk=getWeekKey();
    const wkLvl=getWeeklyChallengeLevel();
    const cfg=getLevelConfig(wkLvl);
    const wld=WORLDS[cfg.world-1];
    const isThisWeek=sv.weeklyChallengeDate===wk;
    const completed=isThisWeek&&sv.weeklyChallengeCompleted;
    const best=isThisWeek?(sv.weeklyChallengeBest||0):0;
    return(
      <div className="flex flex-col h-full px-5 py-6 gap-5 relative z-10 items-center justify-center">
        <NeonButton onClick={()=>go("menu")} className="absolute top-4 left-4 px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
        <div className="text-center">
          <div className="text-5xl mb-2" style={{animation:"floatGlow 2s ease-in-out infinite",filter:"drop-shadow(0 0 20px #a78bfa)"}}>🗓️</div>
          <h2 className="font-black text-3xl" style={{color:"#a78bfa",textShadow:"0 0 30px #a78bfaaa",letterSpacing:"0.08em"}}>WEEKLY CHALLENGE</h2>
          <p className="text-sm opacity-60 mt-1" style={{color:"#a78bfa"}}>Resets every Monday · 3× coin reward</p>
        </div>
        <div className="w-full rounded-2xl p-5" style={{background:`linear-gradient(160deg,${wld.color}22 0%,${wld.bg} 60%)`,border:`1px solid ${wld.color}55`,boxShadow:`0 0 30px ${wld.color}33`}}>
          <div className="flex items-center gap-3 mb-3">
            <span style={{fontSize:36}}>{wld.emoji}</span>
            <div>
              <div className="text-xs uppercase tracking-widest opacity-55 font-bold" style={{color:wld.color}}>{wld.name}</div>
              <div className="text-lg font-black" style={{color:wld.color}}>Level {wkLvl}: {cfg.name}</div>
            </div>
          </div>
          <div className="grid grid-cols-2 gap-2 text-xs">
            <div className="p-2 rounded-lg" style={{background:"#ffffff08"}}>
              <div className="opacity-50" style={{color:wld.color}}>Goal</div>
              <div className="font-bold" style={{color:"#fff"}}>{cfg.scoreGoal.toLocaleString()} pts</div>
            </div>
            <div className="p-2 rounded-lg" style={{background:"#ffffff08"}}>
              <div className="opacity-50" style={{color:wld.color}}>Lives</div>
              <div className="font-bold" style={{color:"#fff"}}>{cfg.lives}</div>
            </div>
          </div>
          {best>0&&(
            <div className="mt-3 p-2 rounded-lg text-center" style={{background:"#a78bfa18",border:"1px solid #a78bfa44"}}>
              <div className="text-xs opacity-60" style={{color:"#a78bfa"}}>Your best this week</div>
              <div className="text-xl font-black" style={{color:"#a78bfa"}}>{best.toLocaleString()}</div>
            </div>
          )}
        </div>
        <div className="w-full rounded-xl p-3 text-center" style={{background:"#fbbf2418",border:"1px solid #fbbf2444"}}>
          <div className="text-xs opacity-70" style={{color:"#fbbf24"}}>🏆 Reward for clearing</div>
          <div className="font-black text-base" style={{color:"#fbbf24"}}>+500 🪙 · +200 XP · Unique badge</div>
        </div>
        <NeonButton onClick={()=>{
          if(completed)return;
          setSelectedLevel(wkLvl);
          sv.weeklyChallengeDate=wk;
          if(!isThisWeek){sv.weeklyChallengeBest=0;sv.weeklyChallengeCompleted=false;}
          flushSave();
          go("shop");
        }} disabled={completed}
          className="w-full py-5 text-xl font-black"
          style={{background:completed?"#ffffff10":"linear-gradient(135deg,#6d28d9,#a78bfa)",
            boxShadow:completed?"none":"0 0 40px #a78bfa66",
            color:completed?"#ffffff40":"#fff",letterSpacing:"0.06em",
            opacity:completed?0.5:1}}>
          {completed?"✓ Done! See you Monday":"🗓️ BEGIN WEEKLY CHALLENGE"}
        </NeonButton>
      </div>
    );
  };

  // ── Daily Tournament Screen ──
  const renderTournament=()=>{
    const today=getTodayKey();
    const dailyLvl=getDailyChallengeLevel();
    const cfg=getLevelConfig(dailyLvl);
    const wld=WORLDS[cfg.world-1];
    const isToday=sv.tournamentDate===today;
    const todayBest=isToday?(sv.tournamentBest||0):0;
    // Estimate rank from score — heuristic: higher score = better rank
    // Rough mapping: score / scoreGoal ratio → percentile
    const ratio=todayBest/(cfg.scoreGoal||1);
    const estRank=todayBest===0?"—":ratio>=3?"Top 5%":ratio>=2.2?"Top 15%":ratio>=1.5?"Top 35%":ratio>=1?"Top 60%":"Bottom 40%";
    const history=(sv.tournamentHistory||[]).slice(0,7);
    return(
      <div className="flex flex-col h-full px-5 py-6 gap-4 relative z-10 overflow-y-auto">
        <NeonButton onClick={()=>go("menu")} className="absolute top-4 left-4 px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
        <div className="text-center pt-8">
          <div className="text-5xl mb-2" style={{animation:"floatGlow 2s ease-in-out infinite",filter:"drop-shadow(0 0 20px #fbbf24)"}}>🏆</div>
          <h2 className="font-black text-3xl" style={{color:"#fbbf24",textShadow:"0 0 30px #fbbf24aa",letterSpacing:"0.08em"}}>DAILY TOURNAMENT</h2>
          <p className="text-sm opacity-60 mt-1" style={{color:"#fbbf24"}}>Same level for all players today · 2× coin reward</p>
        </div>
        <div className="w-full rounded-2xl p-5" style={{background:`linear-gradient(160deg,${wld.color}22 0%,${wld.bg} 60%)`,border:`1px solid ${wld.color}55`,boxShadow:`0 0 30px ${wld.color}33`}}>
          <div className="flex items-center gap-3 mb-3">
            <span style={{fontSize:36}}>{wld.emoji}</span>
            <div>
              <div className="text-xs uppercase tracking-widest opacity-55 font-bold" style={{color:wld.color}}>{wld.name}</div>
              <div className="text-lg font-black" style={{color:wld.color}}>Level {dailyLvl}: {cfg.name}</div>
            </div>
          </div>
          {todayBest>0?(
            <div className="mt-3 grid grid-cols-2 gap-2 text-center">
              <div className="p-2 rounded-lg" style={{background:"#fbbf2418",border:"1px solid #fbbf2444"}}>
                <div className="text-xs opacity-60" style={{color:"#fbbf24"}}>Today's Best</div>
                <div className="text-xl font-black" style={{color:"#fbbf24"}}>{todayBest.toLocaleString()}</div>
              </div>
              <div className="p-2 rounded-lg" style={{background:"#a78bfa18",border:"1px solid #a78bfa44"}}>
                <div className="text-xs opacity-60" style={{color:"#a78bfa"}}>Estimated Rank</div>
                <div className="text-xl font-black" style={{color:"#a78bfa"}}>{estRank}</div>
              </div>
            </div>
          ):(
            <div className="text-sm text-center opacity-50 mt-2" style={{color:"#fff"}}>No attempt yet — go for the high score!</div>
          )}
        </div>
        {/* 7-day history */}
        {history.length>0&&(
          <div className="w-full rounded-xl p-3" style={{background:"#ffffff05",border:"1px solid #ffffff10"}}>
            <div className="text-xs uppercase tracking-widest opacity-50 mb-2" style={{color:"#fbbf24"}}>Last 7 Tournaments</div>
            <div className="space-y-1">
              {history.map((h,i)=>(
                <div key={i} className="flex justify-between text-xs" style={{color:"#ffffff80"}}>
                  <span>{h.date}</span>
                  <span style={{color:"#fbbf24",fontWeight:"bold"}}>{h.score.toLocaleString()} · {h.rank}</span>
                </div>
              ))}
            </div>
          </div>
        )}
        <NeonButton onClick={()=>{setSelectedLevel(dailyLvl);go("shop");}}
          className="w-full py-5 text-xl font-black"
          style={{background:"linear-gradient(135deg,#b45309,#fbbf24)",
            boxShadow:"0 0 40px #fbbf2466",letterSpacing:"0.06em"}}>
          🏆 ENTER TOURNAMENT
        </NeonButton>
        <p className="text-xs text-center opacity-30" style={{color:"#fbbf24"}}>Coins from this run are doubled</p>
      </div>
    );
  };

  // ── Daily Boss Gauntlet Screen ──
  const renderGauntlet=()=>{
    const alreadyDone=sv.gauntletDate===getTodayKey();
    const bossWorlds=WORLDS.slice(0,10);
    return(
      <div className="flex flex-col h-full px-5 py-5 gap-4 relative z-10 overflow-y-auto">
        <div className="flex items-center gap-3">
          <NeonButton onClick={()=>go("menu")} className="px-3 py-2 text-sm" style={{background:"#ffffff10"}}>← Back</NeonButton>
          <div>
            <h2 className="text-xl font-black" style={{color:"#ff6b35"}}>⚔️ Daily Gauntlet</h2>
            <p className="text-xs opacity-50" style={{color:"#ff6b35"}}>Defeat all 10 world bosses in a row!</p>
          </div>
        </div>
        {alreadyDone&&(
          <div className="rounded-2xl px-4 py-3 text-center" style={{background:"#ff6b3518",border:"1px solid #ff6b3544"}}>
            <div style={{color:"#ff6b35",fontWeight:"bold",fontSize:14}}>✓ Completed today!</div>
            <div className="text-xs opacity-60 mt-1" style={{color:"#ff6b35"}}>Come back tomorrow for another run.</div>
            {(sv.gauntletBest||0)>0&&<div className="text-xs mt-1" style={{color:"#ff6b35"}}>Record: {sv.gauntletBest} bosses defeated</div>}
          </div>
        )}
        <div className="rounded-2xl p-4" style={{background:"#ffffff08",border:"1px solid #ff6b3533"}}>
          <div className="text-xs opacity-40 mb-3 uppercase tracking-widest" style={{color:"#ff6b35"}}>Boss Lineup</div>
          <div className="grid grid-cols-5 gap-2">
            {bossWorlds.map((w,i)=>(
              <div key={w.id} className="flex flex-col items-center gap-1 p-2 rounded-xl"
                style={{background:`${w.color}15`,border:`1px solid ${w.color}33`}}>
                <span style={{fontSize:22}}>{w.emoji}</span>
                <span style={{fontSize:8,color:w.color,opacity:0.8,textAlign:"center",lineHeight:1.2}}>{w.name.split(" ")[0]}</span>
              </div>
            ))}
          </div>
        </div>
        <div className="rounded-2xl px-4 py-3" style={{background:"#ffd70018",border:"1px solid #ffd70044"}}>
          <div className="text-sm font-bold" style={{color:"#ffd700"}}>🏆 Reward: 500🪙 + Boss Slayer achievement</div>
          <div className="text-xs opacity-60 mt-1" style={{color:"#ffd700"}}>5 lives total — no recharge between bosses</div>
        </div>
        <NeonButton onClick={()=>{
          if(alreadyDone)return;
          // Start gauntlet as a special boss-only level (world 1 boss first)
          setGauntletState({bossIndex:0,lives:5,score:0});
          const gauntletCfg={
            id:"gauntlet0",world:1,worldName:WORLDS[0].name,worldColor:WORLDS[0].color,
            worldBg:WORLDS[0].bg,worldGrid:WORLDS[0].grid,
            name:"Gauntlet: "+WORLDS[0].name,scoreGoal:1,lives:5,spawnInterval:2000,
            targetLifetime:30000,bombRate:0,movingRate:0,ghostRate:0,
            bossEnabled:true,bossRate:1.0,modifier:null,rarityBonus:0,
            isBoss:true,isLast:false,isGauntlet:true,gauntletBossIndex:0,
          };
          startGame(10,[],gauntletCfg); // level 10 cfg as base but override with gauntletCfg
        }} disabled={alreadyDone}
          className="w-full py-5 text-xl font-black"
          style={{background:alreadyDone?"#ffffff08":"linear-gradient(135deg,#b45309,#ff6b35)",
            boxShadow:alreadyDone?"none":"0 0 36px #ff6b3555",
            color:alreadyDone?"#ffffff30":"#fff",letterSpacing:"0.06em",opacity:alreadyDone?0.5:1}}>
          {alreadyDone?"⚔️ See you tomorrow!":"⚔️ BEGIN GAUNTLET!"}
        </NeonButton>
      </div>
    );
  };

  // ═════════════════════════════════════════════════════════════
  // MAIN RENDER
  // ═════════════════════════════════════════════════════════════
  const activeWorldColor = levelCfgRef.current ? WORLDS[levelCfgRef.current.world-1].color : theme.accent;
  return(
    <div className="relative w-full h-screen overflow-hidden select-none"
      style={{background:theme.bg,fontFamily:"'Exo 2','Rajdhani','Segoe UI',system-ui,sans-serif",
        transform:screenShake?`translate(${(Math.random()>0.5?1:-1)*5}px,${(Math.random()>0.5?1:-1)*3}px)`:"none",
        transition:screenShake?"none":"transform 0.04s ease"}}>

      <style>{`
        @keyframes feverPulse{0%{box-shadow:inset 0 0 40px #fbbf2430,0 0 40px #fbbf2430}100%{box-shadow:inset 0 0 80px #fbbf2455,0 0 80px #fbbf2455}}
        @keyframes epicFlash{0%{opacity:1}100%{opacity:0}}
        @keyframes legendaryRainbow{0%{opacity:1;filter:hue-rotate(0deg) brightness(1.6)}40%{opacity:0.9;filter:hue-rotate(180deg) brightness(1.4)}100%{opacity:0;filter:hue-rotate(360deg) brightness(1)}}
        @keyframes comboAnnounce{0%{transform:scale(.4) translateY(20px);opacity:0}55%{transform:scale(1.18) translateY(-4px)}75%{transform:scale(0.96)}100%{transform:scale(1) translateY(0);opacity:1}}
        @keyframes countAnim{0%{transform:scale(2.2);opacity:0}40%{transform:scale(0.95);opacity:1}80%{transform:scale(1);opacity:1}100%{transform:scale(.75);opacity:0}}
        @keyframes levelPulse{0%,100%{box-shadow:0 0 14px var(--wc,#a78bfa),0 0 0 2px var(--wc,#a78bfa)33}50%{box-shadow:0 0 34px var(--wc,#a78bfa),0 0 0 4px var(--wc,#a78bfa)55}}
        @keyframes starPop{0%{transform:scale(0) rotate(-40deg);opacity:0}60%{transform:scale(1.4) rotate(10deg)}85%{transform:scale(0.92)}100%{transform:scale(1) rotate(0);opacity:1}}
        @keyframes waveIn{0%{transform:scale(.55) translateY(-28px);opacity:0}65%{transform:scale(1.06)}85%{transform:scale(0.97)}100%{transform:scale(1);opacity:1}}
        @keyframes shimmer{0%{background-position:200% center}100%{background-position:-200% center}}
        @keyframes floatGlow{0%,100%{transform:translateY(0);filter:brightness(1)}50%{transform:translateY(-4px);filter:brightness(1.15)}}
        @keyframes bannerSlide{0%{transform:translateX(-20px);opacity:0}100%{transform:translateX(0);opacity:1}}
        @keyframes victoryBurst{0%{transform:scale(0.5);opacity:0}60%{transform:scale(1.08)}100%{transform:scale(1);opacity:1}}
        @keyframes coinFall{0%{transform:translateY(0) rotate(0deg);opacity:0.9}100%{transform:translateY(110vh) rotate(720deg);opacity:0}}
        @keyframes scorePulse{0%{box-shadow:0 0 12px #ffd700,0 0 4px #ffd700}100%{box-shadow:0 0 24px #ffd700,0 0 10px #ffd700}}
        @keyframes heartbeat{0%,100%{transform:scale(1)}15%{transform:scale(1.18)}30%{transform:scale(1)}45%{transform:scale(1.1)}60%{transform:scale(1)}}
        @keyframes perfectPop{0%{transform:scale(0.6) rotate(-8deg);opacity:0}50%{transform:scale(1.15) rotate(3deg)}100%{transform:scale(1) rotate(0);opacity:1}}
        @keyframes notifSlide{0%{transform:translateX(-50%) translateY(-24px);opacity:0}15%{transform:translateX(-50%) translateY(0);opacity:1}80%{transform:translateX(-50%) translateY(0);opacity:1}100%{transform:translateX(-50%) translateY(-12px);opacity:0}}
        @keyframes sparkleFloat{0%{transform:translateY(0) rotate(0deg);opacity:0.7}50%{transform:translateY(-18px) rotate(180deg);opacity:1}100%{transform:translateY(0) rotate(360deg);opacity:0.7}}
        @keyframes mascotIdle{0%,100%{transform:translateY(0) scale(1)}50%{transform:translateY(-7px) scale(1.05)}}
        @keyframes mascotBounce{0%{transform:scale(1) translateY(0)}30%{transform:scale(0.85,1.2) translateY(0)}60%{transform:scale(1.15,0.88) translateY(-22px)}85%{transform:scale(0.95,1.06) translateY(0)}100%{transform:scale(1) translateY(0)}}
        @keyframes mascotSad{0%,100%{transform:rotate(0) translateY(0)}25%{transform:rotate(-8deg) translateY(4px)}75%{transform:rotate(8deg) translateY(4px)}}
        @keyframes mascotShake{0%,100%{transform:translateX(0)}20%{transform:translateX(-6px)}40%{transform:translateX(6px)}60%{transform:translateX(-4px)}80%{transform:translateX(4px)}}
        @keyframes slotSpin{0%{transform:translateY(-8px)}50%{transform:translateY(8px)}100%{transform:translateY(-8px)}}
        @keyframes speechBubble{0%{transform:scale(0.5) translateY(8px);opacity:0}60%{transform:scale(1.06) translateY(-2px)}100%{transform:scale(1) translateY(0);opacity:1}}
        @keyframes rescuePulse{0%{box-shadow:0 0 30px #ff603044}100%{box-shadow:0 0 60px #ff6030aa,0 0 100px #ff603033}}
        @keyframes danceDragon{0%,100%{transform:scale(1) rotate(-8deg)}25%{transform:scale(1.25) rotate(14deg) translateY(-10px)}50%{transform:scale(1.1) rotate(-6deg) translateY(-5px)}75%{transform:scale(1.2) rotate(10deg) translateY(-8px)}}
        @keyframes danceFox{0%{transform:rotate(0) scale(1)}30%{transform:rotate(-180deg) scale(1.2) translateY(-8px)}60%{transform:rotate(-360deg) scale(1)}80%{transform:rotate(-360deg) scale(1.15) translateY(-6px)}100%{transform:rotate(-360deg) scale(1)}}
        @keyframes danceCat{0%,100%{transform:translateY(0) rotate(-10deg)}33%{transform:translateY(-20px) rotate(12deg)}66%{transform:translateY(-10px) rotate(-5deg)}}
        @keyframes danceFrog{0%,100%{transform:scaleY(1) scaleX(1) translateY(0)}30%{transform:scaleX(0.85) scaleY(1.15) translateY(-24px)}55%{transform:scaleX(0.9) scaleY(1.1) translateY(-18px)}75%{transform:scaleX(1.15) scaleY(0.88) translateY(2px)}}
        @keyframes danceLion{0%,100%{transform:scale(1) rotate(0)}20%{transform:scale(1.22) rotate(-8deg)}40%{transform:scale(1.18) rotate(8deg)}60%{transform:scale(1.25) rotate(-6deg)}80%{transform:scale(1.2) rotate(6deg)}}
        @keyframes dancePanda{0%,100%{transform:translateX(0) rotate(0)}25%{transform:translateX(-14px) rotate(-15deg)}75%{transform:translateX(14px) rotate(15deg)}}
        @keyframes dancePenguin{0%,100%{transform:translateX(0) rotate(0) translateY(0)}30%{transform:translateX(-10px) rotate(-12deg) translateY(-4px)}70%{transform:translateX(10px) rotate(12deg) translateY(-4px)}}
        @keyframes danceOctopus{0%{transform:rotate(0) scale(1)}15%{transform:rotate(-40deg) scale(1.15)}35%{transform:rotate(35deg) scale(0.9)}55%{transform:rotate(-25deg) scale(1.2)}75%{transform:rotate(30deg) scale(0.95)}100%{transform:rotate(0) scale(1)}}
        @keyframes danceButterfly{0%,100%{transform:translateY(0) rotate(-12deg) scale(1)}25%{transform:translateY(-18px) rotate(14deg) scale(1.15)}50%{transform:translateY(-8px) rotate(-8deg) scale(0.92)}75%{transform:translateY(-14px) rotate(10deg) scale(1.1)}}
        @keyframes danceUnicorn{0%,100%{transform:translateY(0) scaleX(1)}20%{transform:translateY(-14px) scaleX(0.92) rotate(-5deg)}40%{transform:translateY(-8px) scaleX(1.08) rotate(4deg)}60%{transform:translateY(-18px) scaleX(0.95) rotate(-4deg)}80%{transform:translateY(-6px) scaleX(1.05) rotate(3deg)}}
        @keyframes streakBurstAnim{0%{transform:translateX(-50%) scale(0.4);opacity:0}20%{transform:translateX(-50%) scale(1.25);opacity:1}70%{transform:translateX(-50%) scale(1);opacity:1}90%{transform:translateX(-50%) scale(0.95);opacity:0.6}100%{transform:translateX(-50%) scale(0.8);opacity:0}}
        @keyframes comboFinisher{0%{transform:translateX(-50%) scale(0.2) rotate(-8deg);opacity:0}15%{transform:translateX(-50%) scale(1.5) rotate(4deg);opacity:1}35%{transform:translateX(-50%) scale(1.1) rotate(-2deg);opacity:1}60%{transform:translateX(-50%) scale(1.2) rotate(0);opacity:1}85%{transform:translateX(-50%) scale(1.05);opacity:0.9}100%{transform:translateX(-50%) scale(0.7);opacity:0}}
        @keyframes coinPop{0%{transform:translateX(-50%) translateY(0) scale(0.6);opacity:0}20%{transform:translateX(-50%) translateY(-8px) scale(1.2);opacity:1}60%{transform:translateX(-50%) translateY(-18px) scale(1);opacity:1}100%{transform:translateX(-50%) translateY(-30px) scale(0.8);opacity:0}}
        @keyframes mascotIdle{0%,100%{transform:translateY(0) scale(1)}50%{transform:translateY(-5px) scale(1.04)}}
        @keyframes bossRagePulse{0%,100%{box-shadow:0 0 0 0 #ff000044}50%{box-shadow:0 0 0 12px #ff000022}}
        @keyframes prestigePop{0%{transform:scale(0.3) rotate(-20deg);opacity:0}50%{transform:scale(1.3) rotate(5deg)}75%{transform:scale(0.95) rotate(-2deg)}100%{transform:scale(1) rotate(0);opacity:1}}
        @keyframes infinityPulse{0%,100%{text-shadow:0 0 20px #a78bfa,0 0 40px #a78bfa55}50%{text-shadow:0 0 40px #a78bfa,0 0 80px #a78bfaaa}}
        @keyframes vtScreenIn{0%{opacity:0;transform:translateY(14px)}100%{opacity:1;transform:translateY(0)}}
        @keyframes vtScreenOut{0%{opacity:1;transform:translateY(0)}100%{opacity:0;transform:translateY(-10px)}}
        ::view-transition-old(root){animation:vtScreenOut 0.18s ease-in both}
        ::view-transition-new(root){animation:vtScreenIn 0.22s ease-out both}
        *{-webkit-tap-highlight-color:transparent;box-sizing:border-box}
        ::-webkit-scrollbar{width:0}
        .shine-btn{position:relative;overflow:hidden}
        .shine-btn::after{content:'';position:absolute;top:-50%;left:-75%;width:50%;height:200%;background:linear-gradient(to right,transparent,rgba(255,255,255,0.12),transparent);transform:skewX(-20deg);transition:left 0.4s}
        .shine-btn:active::after{left:125%}
      `}</style>

      <canvas ref={canvasRef} className="absolute inset-0 z-0" style={{width:"100%",height:"100%",pointerEvents:"none"}}/>

      {legendaryFlash&&(
        <div className="absolute inset-0 pointer-events-none z-40"
          style={{background:"linear-gradient(135deg,rgba(255,0,80,0.18),rgba(255,160,0,0.15),rgba(255,230,0,0.12),rgba(0,200,80,0.12),rgba(0,140,255,0.15),rgba(160,0,255,0.18))",
            animation:"legendaryRainbow 0.9s ease-out forwards"}}/>
      )}

      {notif&&(
        <div className="absolute top-4 left-1/2 z-50 px-4 py-2.5 rounded-2xl text-sm font-bold pointer-events-none"
          style={{transform:"translateX(-50%)",background:"rgba(0,0,0,0.92)",border:`1px solid ${activeWorldColor}`,color:activeWorldColor,
            boxShadow:`0 0 28px ${activeWorldColor}55,0 4px 24px rgba(0,0,0,0.6)`,maxWidth:"85vw",whiteSpace:"nowrap",textAlign:"center",
            backdropFilter:"blur(12px)",animation:"notifSlide 2.6s ease-in-out forwards",letterSpacing:"0.04em"}}>
          {notif}
        </div>
      )}
      {/* Mid-game achievement popup — richer display with icon + XP */}
      {achPopup&&(
        <>
          {/* Background flash for high-XP achievements */}
          {achPopup.xp>=60&&<div className="absolute inset-0 z-49 pointer-events-none"
            style={{background:"radial-gradient(circle at 50% 78%,#ffd70033 0%,transparent 70%)",
              animation:"epicFlash 0.6s ease-out forwards"}}/>}
          <div className="absolute z-50 pointer-events-none"
            style={{bottom:"22%",left:"50%",transform:"translateX(-50%)",animation:"perfectPop 2.2s cubic-bezier(0.34,1.4,0.64,1) forwards",textAlign:"center",whiteSpace:"nowrap"}}>
            <div className="flex items-center gap-2 px-4 py-3 rounded-2xl"
              style={{background:"rgba(0,0,0,0.92)",
                border:`2px solid ${achPopup.xp>=75?"#ffd700":achPopup.xp>=50?"#f97316":"#ffd70066"}`,
                boxShadow:`0 0 ${achPopup.xp>=75?"48px #ffd700aa":"28px #ffd70044"},0 4px 20px rgba(0,0,0,0.6)`,
                backdropFilter:"blur(12px)",
                animation:achPopup.xp>=75?"perfectPop 2.2s cubic-bezier(0.34,1.4,0.64,1) forwards,legendaryRainbow 1.5s linear infinite":"perfectPop 2.2s cubic-bezier(0.34,1.4,0.64,1) forwards",
              }}>
              <span style={{fontSize:32,filter:achPopup.xp>=75?"drop-shadow(0 0 12px #ffd700)":"none"}}>{achPopup.icon}</span>
              <div className="flex flex-col items-start">
                <div style={{fontSize:11,color:achPopup.xp>=75?"#ffd700":"#ffd700",fontWeight:"black",letterSpacing:"0.08em",textTransform:"uppercase"}}>
                  {achPopup.xp>=75?"⭐ EPIC ACHIEVEMENT!":achPopup.xp>=50?"🏆 ACHIEVEMENT!":"Achievement!"}
                </div>
                <div style={{fontSize:14,color:"#fff",fontWeight:"bold"}}>{achPopup.label}</div>
                <div style={{fontSize:10,color:"#fbbf24",opacity:0.9}}>+{achPopup.xp} XP ✨</div>
              </div>
            </div>
          </div>
        </>
      )}

      <div className="screen-root" style={{position:"absolute",inset:0}}>
      {screen==="menu"          &&renderMenu()}
      {screen==="levelmap"      &&renderLevelMap()}
      {screen==="shop"          &&renderShop()}
      {screen==="playing"       &&renderPlaying()}
      {screen==="levelcomplete" &&renderLevelComplete()}
      {screen==="gameover"      &&renderGameOver()}
      {screen==="missions"      &&renderMissions()}
      {screen==="achievements"  &&renderAchievements()}
      {screen==="leaderboard"   &&renderLeaderboard()}
      {screen==="skilltree"     &&renderSkillTree()}
      {screen==="settings"         &&renderSettings()}
      {screen==="mascotcollection" &&renderMascotCollection()}
      {screen==="spinwheel"     &&renderSpinWheel()}
      {screen==="infinity"      &&renderInfinity()}
      {screen==="zen"           &&renderZen()}
      {screen==="timeattack"    &&renderTimeAttack()}
      {screen==="gauntlet"      &&renderGauntlet()}
      {screen==="weekly"        &&renderWeekly()}
      {screen==="tournament"    &&renderTournament()}
      </div>
      {storyData&&renderWorldStory()}
      {tutStep!==null&&renderTutorial()}
      {/* ── SHOP PURCHASE CONFIRMATION ── */}
      {shopConfirm&&(
        <div className="absolute inset-0 flex items-center justify-center z-50 px-5"
          style={{background:"rgba(0,0,0,0.82)",backdropFilter:"blur(10px)",animation:"vtScreenIn 0.18s ease-out"}}
          onClick={(e)=>{if(e.target===e.currentTarget)setShopConfirm(null);}}>
          <div className="w-full max-w-sm rounded-3xl p-5"
            style={{background:"linear-gradient(160deg,#fbbf2422 0%,#180400 70%)",
              border:"1px solid #fbbf2455",boxShadow:"0 0 50px #fbbf2466",
              animation:"victoryBurst 0.36s cubic-bezier(0.34,1.4,0.64,1)"}}>
            <div className="text-center mb-3">
              <div className="text-3xl mb-1">🛒</div>
              <h3 className="font-black text-lg" style={{color:"#fbbf24"}}>Confirm Purchase</h3>
              <p className="text-xs opacity-60" style={{color:"#fff"}}>Coins are spent at start — no refunds</p>
            </div>
            {/* Items list */}
            <div className="space-y-1 mb-3 max-h-44 overflow-y-auto">
              {shopConfirm.items.map(id=>{const item=SHOP_ITEMS.find(i=>i.id===id);return item?(
                <div key={id} className="flex items-center justify-between px-3 py-2 rounded-xl"
                  style={{background:"#ffffff08",border:"1px solid #ffffff15"}}>
                  <div className="flex items-center gap-2">
                    <span style={{fontSize:20}}>{item.icon}</span>
                    <div>
                      <div className="text-sm font-bold" style={{color:"#fff"}}>{item.name}</div>
                      <div className="text-xs opacity-50" style={{color:"#fff"}}>{item.desc}</div>
                    </div>
                  </div>
                  <div className="text-sm font-black" style={{color:"#fbbf24"}}>🪙 {item.cost}</div>
                </div>
              ):null;})}
            </div>
            {/* Total + balance */}
            <div className="rounded-xl p-3 mb-4" style={{background:"#fbbf2410",border:"1px solid #fbbf2433"}}>
              <div className="flex justify-between text-sm mb-1">
                <span className="opacity-60" style={{color:"#fff"}}>Total cost</span>
                <span className="font-black" style={{color:"#fbbf24"}}>🪙 {shopConfirm.total}</span>
              </div>
              <div className="flex justify-between text-sm mb-1">
                <span className="opacity-60" style={{color:"#fff"}}>Your coins</span>
                <span style={{color:"#fff"}}>🪙 {(sv.coins||0).toLocaleString()}</span>
              </div>
              <div className="border-t border-white border-opacity-10 my-1.5"></div>
              <div className="flex justify-between text-sm">
                <span className="opacity-60" style={{color:"#fff"}}>After purchase</span>
                <span className="font-black" style={{color:"#34d399"}}>🪙 {((sv.coins||0)-shopConfirm.total).toLocaleString()}</span>
              </div>
            </div>
            <div className="flex gap-3">
              <NeonButton onClick={()=>setShopConfirm(null)}
                className="flex-1 py-3 text-sm font-bold"
                style={{background:"#ffffff10",border:"1px solid #ffffff20",color:"#ffffff90"}}>
                Cancel
              </NeonButton>
              <NeonButton onClick={()=>{sfx("coin");shopConfirm.onConfirm();}}
                className="flex-1 py-3 text-sm font-black"
                style={{background:"linear-gradient(135deg,#b45309,#fbbf24)",boxShadow:"0 0 24px #fbbf2455",color:"#fff",letterSpacing:"0.04em"}}>
                ✓ Confirm
              </NeonButton>
            </div>
          </div>
        </div>
      )}
      {/* ── DAILY LOGIN BONUS POPUP ── */}
      {dailyBonusData&&(
        <div className="absolute inset-0 flex flex-col items-center justify-center z-50 px-5"
          style={{background:"rgba(0,0,0,0.86)",backdropFilter:"blur(14px)",animation:"vtScreenIn 0.3s ease-out"}}
          onClick={(e)=>{if(e.target===e.currentTarget)setDailyBonusData(null);}}>
          <div className="w-full max-w-sm rounded-3xl p-6 text-center"
            style={{
              background:dailyBonusData.isJackpot?"linear-gradient(160deg,#fbbf2422 0%,#180400 70%)":"linear-gradient(160deg,#a78bfa22 0%,#0a0218 70%)",
              border:`1px solid ${dailyBonusData.isJackpot?"#fbbf24":"#a78bfa"}55`,
              boxShadow:`0 0 60px ${dailyBonusData.isJackpot?"#fbbf24":"#a78bfa"}55`,
              animation:"victoryBurst 0.5s cubic-bezier(0.34,1.4,0.64,1)"}}>
            <div className="text-5xl mb-2" style={{
              animation:"floatGlow 1.6s ease-in-out infinite",
              filter:`drop-shadow(0 0 24px ${dailyBonusData.isJackpot?"#fbbf24":"#a78bfa"})`}}>
              {dailyBonusData.isJackpot?"🎁":"🌞"}
            </div>
            <h2 className="text-xl font-black mb-1" style={{color:dailyBonusData.isJackpot?"#fbbf24":"#a78bfa"}}>
              {dailyBonusData.isJackpot?"WEEKLY JACKPOT!":"Daily Login Bonus!"}
            </h2>
            <p className="text-xs opacity-60 mb-4" style={{color:"#fff"}}>
              Day {dailyBonusData.streak} · {dailyBonusData.isJackpot?"You did it! 🎉":"Welcome back!"}
            </p>
            {/* 7-day calendar */}
            <div className="grid grid-cols-7 gap-1.5 mb-4">
              {[1,2,3,4,5,6,7].map(d=>{
                const isToday=d===dailyBonusData.day;
                const isPast=d<dailyBonusData.day;
                const COIN=[40,60,80,120,160,220,400];
                return(
                  <div key={d} className="flex flex-col items-center" style={{
                    padding:"6px 2px",borderRadius:9,
                    background:isToday?(d===7?"#fbbf2433":"#a78bfa33"):isPast?"#ffffff10":"#ffffff05",
                    border:`1px solid ${isToday?(d===7?"#fbbf24":"#a78bfa"):isPast?"#ffffff20":"#ffffff10"}`,
                    boxShadow:isToday?`0 0 12px ${d===7?"#fbbf24":"#a78bfa"}66`:"none"}}>
                    <div style={{fontSize:9,opacity:0.6,color:isToday?(d===7?"#fbbf24":"#a78bfa"):"#fff"}}>D{d}</div>
                    <div style={{fontSize:14,marginTop:1}}>{isPast?"✓":d===7?"🎁":"🪙"}</div>
                    <div style={{fontSize:8,marginTop:1,opacity:0.7,color:isToday?(d===7?"#fbbf24":"#a78bfa"):"#fff"}}>{COIN[d-1]}</div>
                  </div>
                );
              })}
            </div>
            <div className="flex gap-3 justify-center mb-4">
              <div className="px-3 py-2 rounded-xl flex-1" style={{background:"#fbbf2418",border:"1px solid #fbbf2444"}}>
                <div className="text-xs opacity-50" style={{color:"#fbbf24"}}>Coins</div>
                <div className="text-lg font-black" style={{color:"#fbbf24"}}>+🪙 {dailyBonusData.coins}</div>
              </div>
              <div className="px-3 py-2 rounded-xl flex-1" style={{background:"#a78bfa18",border:"1px solid #a78bfa44"}}>
                <div className="text-xs opacity-50" style={{color:"#a78bfa"}}>XP</div>
                <div className="text-lg font-black" style={{color:"#a78bfa"}}>+{dailyBonusData.xp}</div>
              </div>
            </div>
            <NeonButton onClick={()=>{sfx("coin");setDailyBonusData(null);}}
              className="w-full py-3 text-base font-black"
              style={{background:dailyBonusData.isJackpot?"linear-gradient(135deg,#b45309,#fbbf24)":"linear-gradient(135deg,#6d28d9,#a78bfa)",
                boxShadow:`0 0 30px ${dailyBonusData.isJackpot?"#fbbf24":"#a78bfa"}66`,letterSpacing:"0.05em"}}>
              {dailyBonusData.isJackpot?"🎉 CLAIM JACKPOT":"✨ Awesome!"}
            </NeonButton>
          </div>
        </div>
      )}

      {/* Offline coins overlay */}
      {offlineCoinsData&&(
        <div className="fixed inset-0 z-[120] flex items-center justify-center" style={{background:"rgba(0,0,0,0.72)"}}>
          <div className="relative rounded-2xl p-6 flex flex-col items-center gap-3 text-center"
            style={{background:"linear-gradient(160deg,#14532d22 0%,#0a0218 70%)",border:"1px solid #4ade8055",
              boxShadow:"0 0 60px #4ade8055",maxWidth:320,width:"90%"}}>
            <div className="text-5xl mb-1" style={{filter:"drop-shadow(0 0 16px #4ade80)"}}>🌙</div>
            <h2 className="text-xl font-black" style={{color:"#4ade80"}}>Welcome Back!</h2>
            <p className="text-sm" style={{color:"#86efac"}}>Your coins kept rolling while you were away.</p>
            <div className="rounded-xl px-5 py-3 mt-1" style={{background:"#4ade8018",border:"1px solid #4ade8044"}}>
              <div className="text-4xl font-black" style={{color:"#4ade80"}}>+🪙 {offlineCoinsData.coins}</div>
              <div className="text-xs mt-1" style={{color:"#86efac"}}>
                {offlineCoinsData.hours>=3.9?"4h (max)":offlineCoinsData.hours>=1?`${offlineCoinsData.hours.toFixed(1)}h away`:"~1h away"}
              </div>
            </div>
            <p className="text-xs" style={{color:"#4ade8066"}}>Earn up to 1 coin/10s · max 4 hours</p>
            <NeonButton onClick={()=>setOfflineCoinsData(null)}
              style={{background:"linear-gradient(135deg,#15803d,#4ade80)",boxShadow:"0 0 20px #4ade8066",letterSpacing:"0.05em"}}>
              ✅ Sweet!
            </NeonButton>
          </div>
        </div>
      )}

      {/* Weekly recap overlay — 12C, shown on Sunday */}
      {weeklyRecapData&&(
        <div className="fixed inset-0 z-[120] flex items-center justify-center" style={{background:"rgba(0,0,0,0.80)"}}>
          <div className="relative rounded-2xl p-6 flex flex-col items-center gap-4 text-center"
            style={{background:"linear-gradient(160deg,#1e1b4b22 0%,#0a0218 70%)",border:"1px solid #6366f155",
              boxShadow:"0 0 60px #6366f133",maxWidth:340,width:"90%"}}>
            {/* Sparkles */}
            <div className="absolute inset-0 pointer-events-none overflow-hidden rounded-2xl">
              {[...Array(10)].map((_,i)=>(
                <div key={i} style={{position:"absolute",left:`${(i*31)%100}%`,top:`${(i*47)%80}%`,
                  fontSize:12,animation:`sparkleFloat ${1.5+i*0.2}s ease-in-out ${i*0.1}s infinite`,opacity:0.6}}>
                  {["⭐","💫","✨","🌟"][i%4]}
                </div>
              ))}
            </div>
            <div className="text-5xl relative z-10" style={{filter:"drop-shadow(0 0 20px #6366f1)"}}>📊</div>
            <h2 className="text-xl font-black relative z-10" style={{color:"#818cf8"}}>Weekly Recap</h2>
            <p className="text-xs relative z-10" style={{color:"#a5b4fc"}}>Here's how your week went, adventurer!</p>
            <div className="w-full grid grid-cols-3 gap-2 relative z-10">
              <div className="rounded-xl py-3 flex flex-col items-center gap-1" style={{background:"#6366f115",border:"1px solid #6366f133"}}>
                <div style={{fontSize:20}}>🎮</div>
                <div className="font-black text-lg" style={{color:"#818cf8"}}>{weeklyRecapData.levelsPlayed}</div>
                <div className="text-xs opacity-50" style={{color:"#818cf8"}}>Levels</div>
              </div>
              <div className="rounded-xl py-3 flex flex-col items-center gap-1" style={{background:"#fbbf2415",border:"1px solid #fbbf2433"}}>
                <div style={{fontSize:20}}>🏆</div>
                <div className="font-black text-base" style={{color:"#fbbf24"}}>{weeklyRecapData.bestScore.toLocaleString()}</div>
                <div className="text-xs opacity-50" style={{color:"#fbbf24"}}>Best Score</div>
              </div>
              <div className="rounded-xl py-3 flex flex-col items-center gap-1" style={{background:"#4ade8015",border:"1px solid #4ade8033"}}>
                <div style={{fontSize:20}}>🪙</div>
                <div className="font-black text-lg" style={{color:"#4ade80"}}>~{weeklyRecapData.coinsEarned.toLocaleString()}</div>
                <div className="text-xs opacity-50" style={{color:"#4ade80"}}>Coins</div>
              </div>
            </div>
            <p className="text-xs opacity-40 relative z-10" style={{color:"#818cf8"}}>New week starts tomorrow — keep going!</p>
            <NeonButton onClick={()=>setWeeklyRecapData(null)} className="relative z-10"
              style={{background:"linear-gradient(135deg,#4338ca,#6366f1)",boxShadow:"0 0 20px #6366f155",letterSpacing:"0.05em"}}>
              🚀 Let's go!
            </NeonButton>
          </div>
        </div>
      )}
    </div>
  );
}
